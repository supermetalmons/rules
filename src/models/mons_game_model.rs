#[cfg(any(target_arch = "wasm32", test))]
use crate::models::scoring::{
    evaluate_preferability_with_weights_and_exact_policy, ScoringWeights,
    BALANCED_DISTANCE_SCORING_WEIGHTS, DEFAULT_SCORING_WEIGHTS,
    FINISHER_BALANCED_SOFT_AGGRESSIVE_SCORING_WEIGHTS, FINISHER_BALANCED_SOFT_SCORING_WEIGHTS,
    MANA_RACE_LITE_D2_TUNED_SCORING_WEIGHTS, RUNTIME_FAST_BOOLEAN_DRAINER_SCORING_WEIGHTS,
    RUNTIME_FAST_BOOLEAN_DRAINER_SCORING_WEIGHTS_POTION_PREF,
    RUNTIME_FAST_DRAINER_CONTEXT_SCORING_WEIGHTS_POTION_PREF,
    TACTICAL_BALANCED_AGGRESSIVE_SCORING_WEIGHTS, TACTICAL_BALANCED_SCORING_WEIGHTS,
};
use crate::*;

#[cfg(any(target_arch = "wasm32", test))]
#[path = "automove_runtime_variants.rs"]
pub(crate) mod automove_runtime_variants;

#[wasm_bindgen]
#[derive(Debug)]
pub struct MonsGameModel {
    game: MonsGame,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum TurnEngineRerankOverrideAcceptance {
    Accepted,
    Rejected(TurnEngineRerankOverrideRejectReason),
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
enum TurnEngineRerankOverrideRejectReason {
    NotInRoot,
    MissingTop,
    TopNotTacticalOrUnsafe,
    TopWins,
    CandidateUnsafe,
    ProgressGate,
    TacticalGate,
    SafetyProgressGate,
}

impl Clone for MonsGameModel {
    fn clone(&self) -> Self {
        Self::with_game(self.game.clone())
    }
}

#[cfg(any(target_arch = "wasm32", test))]
const MIN_SMART_SEARCH_DEPTH: usize = 1;
#[cfg(any(target_arch = "wasm32", test))]
const MAX_SMART_SEARCH_DEPTH: usize = 5;
#[cfg(any(target_arch = "wasm32", test))]
const MIN_SMART_MAX_VISITED_NODES: usize = 32;
#[cfg(any(target_arch = "wasm32", test))]
const MAX_SMART_MAX_VISITED_NODES: usize = 180_000;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_TERMINAL_SCORE: i32 = i32::MAX / 8;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_MAX_INPUT_CHAIN: usize = 8;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_TRANSPOSITION_TABLE_MAX_ENTRIES: usize = 12_000;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_NO_EFFECT_ROOT_PENALTY: i32 = 120;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_LOW_IMPACT_ROOT_PENALTY: i32 = 40;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_EFFICIENCY_SCORE_MARGIN: i32 = 2_500;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_BACKTRACK_PENALTY: i32 = 140;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_TT_BEST_CHILD_BONUS: i32 = 2_400;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_TWO_PASS_ROOT_SCOUT_DEPTH: usize = 2;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_TWO_PASS_ROOT_SCOUT_MIN_NODES: usize = 96;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_TWO_PASS_ROOT_FOCUS_SCORE_MARGIN: i32 = 2_000;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_TWO_PASS_ROOT_VOLATILITY_KEEP: usize = 2;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_TWO_PASS_ROOT_VOLATILITY_MARGIN: i32 = 600;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_MANA_HANDOFF_PENALTY: i32 = 220;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_DRAINER_SAFETY_SCORE_MARGIN: i32 = 2_200;
#[cfg(any(target_arch = "wasm32", test))]
#[cfg(test)]
const SMART_NORMAL_ROOT_SAFETY_SHORTLIST_MAX: usize = 4;
#[cfg(any(target_arch = "wasm32", test))]
#[cfg(test)]
const SMART_NORMAL_ROOT_SAFETY_SCORE_MARGIN: i32 = 3_000;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_NORMAL_ROOT_SAFETY_REPLY_LIMIT_MIN: usize = 12;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_NORMAL_ROOT_SAFETY_REPLY_LIMIT_MAX: usize = 36;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_NORMAL_ROOT_SAFETY_DEEP_FLOOR_SCORE_RACE_TRIGGER: i32 = 3;
#[cfg(any(target_arch = "wasm32", test))]
#[cfg(test)]
const SMART_NORMAL_ROOT_SAFETY_DEEP_FLOOR_SCORE_MARGIN: i32 = 2_400;
#[cfg(any(target_arch = "wasm32", test))]
#[cfg(test)]
const SMART_NORMAL_ROOT_SAFETY_DEEP_FLOOR_MAX_CANDIDATES: usize = 3;
#[cfg(any(target_arch = "wasm32", test))]
#[cfg(test)]
const SMART_NORMAL_ROOT_SAFETY_DEEP_FLOOR_REPLY_LIMIT_MIN: usize = 8;
#[cfg(any(target_arch = "wasm32", test))]
#[cfg(test)]
const SMART_NORMAL_ROOT_SAFETY_DEEP_FLOOR_REPLY_LIMIT_MAX: usize = 16;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_REPLY_RISK_SCORE_MARGIN: i32 = 140;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_REPLY_RISK_SHORTLIST_FAST: usize = 3;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_REPLY_RISK_REPLY_LIMIT_FAST: usize = 8;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_REPLY_RISK_NODE_SHARE_BP_FAST: i32 = 600;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_REPLY_RISK_WINNER_SPREAD_SKIP: i32 = SMART_TWO_PASS_ROOT_NARROW_SPREAD_FALLBACK;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_TWO_PASS_ROOT_NARROW_SPREAD_FALLBACK: i32 = 700;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_MOVE_CLASS_ROOT_SCORE_MARGIN: i32 = 120;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_MOVE_CLASS_CHILD_SCORE_MARGIN: i32 = 110;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_ANTI_HELP_SCORE_MARGIN: i32 = 180;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_ANTI_HELP_REPLY_LIMIT_FAST: usize = 6;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_SELECTIVE_EXTENSION_NODE_SHARE_BP_NORMAL: i32 = 1_200;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_SPIRIT_DEVELOPMENT_SCORE_MARGIN: i32 = 700;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_INTERVIEW_SOFT_PRIORITY_SCORE_MARGIN: i32 = 120;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_INTERVIEW_SOFT_SUPERMANA_PROGRESS_BONUS: i32 = 240;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_INTERVIEW_SOFT_SUPERMANA_SCORE_BONUS: i32 = 420;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_INTERVIEW_SOFT_OPPONENT_MANA_PROGRESS_BONUS: i32 = 210;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_INTERVIEW_SOFT_OPPONENT_MANA_SCORE_BONUS: i32 = 360;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_INTERVIEW_SOFT_MANA_HANDOFF_PENALTY: i32 = 220;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_INTERVIEW_SOFT_ROUNDTRIP_PENALTY: i32 = 140;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_POTION_SPEND_NO_COMPENSATION_PENALTY_FAST: i32 = 340;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_POTION_SPEND_NO_COMPENSATION_PENALTY_NORMAL: i32 = 260;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_ROOT_POTION_HOLD_SCORE_MARGIN: i32 = 180;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_SPIRIT_DEPLOY_EFFICIENCY_BONUS: i32 = 90;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_SPIRIT_ACTION_TARGET_DELTA_WEIGHT: i32 = 22;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_FORCED_DRAINER_ATTACK_FALLBACK_FAST_CANDIDATES: usize = 4;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_FORCED_DRAINER_ATTACK_FALLBACK_NORMAL_CANDIDATES: usize = 6;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_FORCED_DRAINER_ATTACK_FALLBACK_NODE_BUDGET_FAST: usize = 600;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_FORCED_DRAINER_ATTACK_FALLBACK_NODE_BUDGET_NORMAL: usize = 1_800;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_FORCED_DRAINER_ATTACK_FALLBACK_ENUM_LIMIT_FAST: usize = 220;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_FORCED_DRAINER_ATTACK_FALLBACK_ENUM_LIMIT_NORMAL: usize = 280;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_AUTOMOVE_FAST_DEPTH: i32 = 2;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_AUTOMOVE_FAST_MAX_VISITED_NODES: i32 = 480;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_AUTOMOVE_NORMAL_DEPTH: i32 = 3;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_AUTOMOVE_NORMAL_MAX_VISITED_NODES: i32 = 3800;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_AUTOMOVE_PRO_DEPTH: i32 = 4;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_AUTOMOVE_PRO_MAX_VISITED_NODES: i32 =
    SMART_AUTOMOVE_NORMAL_MAX_VISITED_NODES * 369 / 100;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_MOVE_EFFICIENCY_SNAPSHOT_CACHE_MAX_ENTRIES: usize = 16_384;
#[cfg(any(target_arch = "wasm32", test))]
const SMART_SEARCH_PREFERABILITY_CACHE_MAX_ENTRIES: usize = 32_768;

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Default)]
struct IdentityU64Hasher(u64);

#[cfg(any(target_arch = "wasm32", test))]
impl std::hash::Hasher for IdentityU64Hasher {
    fn finish(&self) -> u64 {
        self.0
    }

    fn write(&mut self, bytes: &[u8]) {
        let mut hash = 0xcbf29ce484222325u64;
        for byte in bytes {
            hash ^= u64::from(*byte);
            hash = hash.wrapping_mul(0x100000001b3);
        }
        self.0 = hash;
    }

    fn write_u64(&mut self, value: u64) {
        self.0 = value;
    }
}

#[cfg(any(target_arch = "wasm32", test))]
type U64BuildHasher = std::hash::BuildHasherDefault<IdentityU64Hasher>;

#[cfg(any(target_arch = "wasm32", test))]
type U64HashMap<V> = std::collections::HashMap<u64, V, U64BuildHasher>;

#[cfg(any(target_arch = "wasm32", test))]
type U64HashSet = std::collections::HashSet<u64, U64BuildHasher>;

#[cfg(any(target_arch = "wasm32", test))]
type FastHashMap<K, V> = std::collections::HashMap<K, V, U64BuildHasher>;
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_BALANCED_DISTANCE_SPIRIT_BASE_SCORING_WEIGHTS: ScoringWeights =
    ScoringWeights {
        use_legacy_formula: false,
        confirmed_score: 900,
        spirit_on_own_base_penalty: 260,
        score_race_path_progress: 86,
        opponent_score_race_path_progress: 184,
        immediate_score_window: 96,
        opponent_immediate_score_window: 245,
        angel_guarding_drainer: 120,
        supermana_race_control: 30,
        opponent_mana_denial: 24,
        drainer_holding_mana: 470,
        drainer_immediate_threat: -55,
        drainer_best_mana_path: 58,
        drainer_pickup_score_this_turn: 90,
        mana_carrier_score_this_turn: 150,
        drainer_close_to_mana: 360,
        spirit_action_utility: 86,
        ..BALANCED_DISTANCE_SCORING_WEIGHTS
    };
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_TACTICAL_BALANCED_SPIRIT_BASE_SCORING_WEIGHTS: ScoringWeights =
    ScoringWeights {
        use_legacy_formula: false,
        confirmed_score: 900,
        spirit_on_own_base_penalty: 260,
        score_race_path_progress: 94,
        opponent_score_race_path_progress: 220,
        immediate_score_window: 102,
        opponent_immediate_score_window: 310,
        angel_guarding_drainer: 180,
        supermana_race_control: 34,
        opponent_mana_denial: 30,
        drainer_holding_mana: 500,
        drainer_immediate_threat: -90,
        drainer_best_mana_path: 84,
        drainer_pickup_score_this_turn: 110,
        mana_carrier_score_this_turn: 180,
        drainer_close_to_mana: 390,
        spirit_action_utility: 90,
        ..TACTICAL_BALANCED_SCORING_WEIGHTS
    };
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_TACTICAL_BALANCED_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS: ScoringWeights =
    ScoringWeights {
        use_legacy_formula: false,
        confirmed_score: 890,
        spirit_on_own_base_penalty: 260,
        score_race_path_progress: 104,
        opponent_score_race_path_progress: 255,
        immediate_score_window: 114,
        opponent_immediate_score_window: 360,
        angel_guarding_drainer: 190,
        supermana_race_control: 40,
        opponent_mana_denial: 34,
        drainer_holding_mana: 520,
        drainer_immediate_threat: -120,
        drainer_best_mana_path: 96,
        drainer_pickup_score_this_turn: 130,
        mana_carrier_score_this_turn: 220,
        drainer_close_to_mana: 410,
        spirit_action_utility: 94,
        ..TACTICAL_BALANCED_AGGRESSIVE_SCORING_WEIGHTS
    };
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_FINISHER_BALANCED_SOFT_SPIRIT_BASE_SCORING_WEIGHTS: ScoringWeights =
    ScoringWeights {
        use_legacy_formula: false,
        confirmed_score: 930,
        spirit_on_own_base_penalty: 260,
        score_race_path_progress: 170,
        opponent_score_race_path_progress: 170,
        immediate_score_window: 275,
        opponent_immediate_score_window: 235,
        supermana_race_control: 32,
        opponent_mana_denial: 28,
        drainer_holding_mana: 500,
        drainer_best_mana_path: 72,
        drainer_pickup_score_this_turn: 120,
        mana_carrier_score_this_turn: 240,
        drainer_close_to_mana: 375,
        angel_guarding_drainer: 170,
        spirit_action_utility: 88,
        ..FINISHER_BALANCED_SOFT_SCORING_WEIGHTS
    };
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_FINISHER_BALANCED_SOFT_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS: ScoringWeights =
    ScoringWeights {
        use_legacy_formula: false,
        confirmed_score: 940,
        spirit_on_own_base_penalty: 260,
        score_race_path_progress: 195,
        opponent_score_race_path_progress: 185,
        immediate_score_window: 330,
        opponent_immediate_score_window: 265,
        supermana_race_control: 36,
        opponent_mana_denial: 30,
        drainer_holding_mana: 520,
        drainer_best_mana_path: 84,
        drainer_pickup_score_this_turn: 140,
        mana_carrier_score_this_turn: 280,
        drainer_close_to_mana: 395,
        angel_guarding_drainer: 180,
        spirit_action_utility: 90,
        ..FINISHER_BALANCED_SOFT_AGGRESSIVE_SCORING_WEIGHTS
    };

#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_BOOLEAN_DRAINER_BALANCED_DISTANCE_SPIRIT_BASE_SCORING_WEIGHTS: ScoringWeights =
    ScoringWeights {
        drainer_danger_boolean: -1200,
        mana_carrier_danger_boolean: -800,
        ..RUNTIME_NORMAL_BALANCED_DISTANCE_SPIRIT_BASE_SCORING_WEIGHTS
    };
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_BOOLEAN_DRAINER_TACTICAL_BALANCED_SPIRIT_BASE_SCORING_WEIGHTS: ScoringWeights =
    ScoringWeights {
        drainer_danger_boolean: -1200,
        mana_carrier_danger_boolean: -800,
        ..RUNTIME_NORMAL_TACTICAL_BALANCED_SPIRIT_BASE_SCORING_WEIGHTS
    };
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_BOOLEAN_DRAINER_TACTICAL_BALANCED_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    drainer_danger_boolean: -1200,
    mana_carrier_danger_boolean: -800,
    ..RUNTIME_NORMAL_TACTICAL_BALANCED_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS
};
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_BOOLEAN_DRAINER_FINISHER_BALANCED_SOFT_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    drainer_danger_boolean: -1200,
    mana_carrier_danger_boolean: -800,
    ..RUNTIME_NORMAL_FINISHER_BALANCED_SOFT_SPIRIT_BASE_SCORING_WEIGHTS
};
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_BOOLEAN_DRAINER_FINISHER_BALANCED_SOFT_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    drainer_danger_boolean: -1200,
    mana_carrier_danger_boolean: -800,
    ..RUNTIME_NORMAL_FINISHER_BALANCED_SOFT_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS
};

#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_WALK_THREAT_MEDIUM_BALANCED_DISTANCE_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    drainer_walk_threat_boolean: -300,
    mana_carrier_walk_threat_boolean: -150,
    ..RUNTIME_NORMAL_BOOLEAN_DRAINER_BALANCED_DISTANCE_SPIRIT_BASE_SCORING_WEIGHTS
};
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_WALK_THREAT_MEDIUM_TACTICAL_BALANCED_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    drainer_walk_threat_boolean: -300,
    mana_carrier_walk_threat_boolean: -150,
    ..RUNTIME_NORMAL_BOOLEAN_DRAINER_TACTICAL_BALANCED_SPIRIT_BASE_SCORING_WEIGHTS
};
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_WALK_THREAT_MEDIUM_TACTICAL_BALANCED_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    drainer_walk_threat_boolean: -300,
    mana_carrier_walk_threat_boolean: -150,
    ..RUNTIME_NORMAL_BOOLEAN_DRAINER_TACTICAL_BALANCED_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS
};
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_WALK_THREAT_MEDIUM_FINISHER_BALANCED_SOFT_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    drainer_walk_threat_boolean: -300,
    mana_carrier_walk_threat_boolean: -150,
    ..RUNTIME_NORMAL_BOOLEAN_DRAINER_FINISHER_BALANCED_SOFT_SPIRIT_BASE_SCORING_WEIGHTS
};
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_WALK_THREAT_MEDIUM_FINISHER_BALANCED_SOFT_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    drainer_walk_threat_boolean: -300,
    mana_carrier_walk_threat_boolean: -150,
    ..RUNTIME_NORMAL_BOOLEAN_DRAINER_FINISHER_BALANCED_SOFT_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS
};

#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_ATTACKER_PROXIMITY_BALANCED_DISTANCE_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    attacker_close_to_opponent_drainer: 200,
    ..RUNTIME_NORMAL_BOOLEAN_DRAINER_BALANCED_DISTANCE_SPIRIT_BASE_SCORING_WEIGHTS
};
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_ATTACKER_PROXIMITY_TACTICAL_BALANCED_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    attacker_close_to_opponent_drainer: 200,
    ..RUNTIME_NORMAL_BOOLEAN_DRAINER_TACTICAL_BALANCED_SPIRIT_BASE_SCORING_WEIGHTS
};
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_ATTACKER_PROXIMITY_TACTICAL_BALANCED_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    attacker_close_to_opponent_drainer: 200,
    ..RUNTIME_NORMAL_BOOLEAN_DRAINER_TACTICAL_BALANCED_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS
};
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_ATTACKER_PROXIMITY_FINISHER_BALANCED_SOFT_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    attacker_close_to_opponent_drainer: 200,
    ..RUNTIME_NORMAL_BOOLEAN_DRAINER_FINISHER_BALANCED_SOFT_SPIRIT_BASE_SCORING_WEIGHTS
};
#[cfg(any(target_arch = "wasm32", test))]
const RUNTIME_NORMAL_ATTACKER_PROXIMITY_FINISHER_BALANCED_SOFT_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS:
    ScoringWeights = ScoringWeights {
    attacker_close_to_opponent_drainer: 200,
    ..RUNTIME_NORMAL_BOOLEAN_DRAINER_FINISHER_BALANCED_SOFT_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS
};

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
enum SmartAutomovePreference {
    Fast,
    Normal,
    Pro,
}

#[cfg(any(target_arch = "wasm32", test))]
impl SmartAutomovePreference {
    fn from_api_value(value: &str) -> Option<Self> {
        let normalized = value.trim();
        if normalized.eq_ignore_ascii_case("fast") {
            Some(Self::Fast)
        } else if normalized.eq_ignore_ascii_case("normal") {
            Some(Self::Normal)
        } else if normalized.eq_ignore_ascii_case("pro") {
            Some(Self::Pro)
        } else {
            None
        }
    }

    fn as_api_value(self) -> &'static str {
        match self {
            Self::Fast => "fast",
            Self::Normal => "normal",
            Self::Pro => "pro",
        }
    }

    fn depth_and_max_nodes(self) -> (i32, i32) {
        match self {
            Self::Fast => (
                SMART_AUTOMOVE_FAST_DEPTH,
                SMART_AUTOMOVE_FAST_MAX_VISITED_NODES,
            ),
            Self::Normal => (
                SMART_AUTOMOVE_NORMAL_DEPTH,
                SMART_AUTOMOVE_NORMAL_MAX_VISITED_NODES,
            ),
            Self::Pro => (
                SMART_AUTOMOVE_PRO_DEPTH,
                SMART_AUTOMOVE_PRO_MAX_VISITED_NODES,
            ),
        }
    }
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy)]
pub(crate) struct AutomoveSearchConfig {
    depth: usize,
    max_visited_nodes: usize,
    root_enum_limit: usize,
    root_branch_limit: usize,
    node_enum_limit: usize,
    node_branch_limit: usize,
    scoring_weights: &'static ScoringWeights,
    enable_two_pass_root_allocation: bool,
    root_focus_k: usize,
    root_focus_budget_share_bp: i32,
    enable_selective_extensions: bool,
    enable_quiet_reductions: bool,
    max_extensions_per_path: usize,
    selective_extension_node_share_bp: i32,
    enable_targeted_drainer_attack_fallback: bool,
    enable_forced_tactical_prepass: bool,
    enable_turn_head_rerank: bool,
    enable_turn_engine_selector: bool,
    enable_turn_engine_low_budget_guard: bool,
    enable_turn_engine_mid_turn_tactical_guard: bool,
    enable_turn_engine_secondary_analysis: bool,
    enable_turn_engine_selected_followup_projection: bool,
    enable_turn_engine_late_safe_mana_root_preference: bool,
    turn_engine_mode: TurnEngineMode,
    turn_engine_seed_cap: usize,
    turn_engine_beam_width: usize,
    turn_engine_per_node_family_cap: usize,
    turn_engine_step_cap: usize,
    turn_engine_opponent_seed_cap: usize,
    turn_engine_opponent_beam_width: usize,
    turn_engine_reply_seed_cap: usize,
    turn_engine_reply_beam_width: usize,
    turn_engine_expansion_cap: usize,
    turn_engine_enable_spirit_family: bool,
    enable_exact_lite_progress_checks: bool,
    enable_exact_lite_spirit_window_checks: bool,
    exact_lite_root_call_budget: usize,
    exact_lite_static_call_budget: usize,
    enable_root_spirit_development_pref: bool,
    enable_root_reply_risk_guard: bool,
    root_reply_risk_score_margin: i32,
    root_reply_risk_shortlist_max: usize,
    root_reply_risk_reply_limit: usize,
    root_reply_risk_node_share_bp: i32,
    root_anti_help_score_margin: i32,
    root_anti_help_reply_limit: usize,
    enable_two_pass_volatility_focus: bool,
    enable_normal_root_safety_rerank: bool,
    enable_normal_root_safety_deep_floor: bool,
    enable_interview_hard_spirit_deploy: bool,
    enable_interview_deterministic_tiebreak: bool,
    prefer_clean_reply_risk_roots: bool,
    root_drainer_safety_score_margin: i32,
    root_mana_handoff_penalty: i32,
    root_backtrack_penalty: i32,
    root_efficiency_score_margin: i32,
    potion_spend_penalty_fast: i32,
    potion_spend_penalty_normal: i32,
    interview_soft_score_margin: i32,
    interview_soft_supermana_progress_bonus: i32,
    interview_soft_supermana_score_bonus: i32,
    interview_soft_opponent_mana_progress_bonus: i32,
    interview_soft_opponent_mana_score_bonus: i32,
    interview_soft_mana_handoff_penalty: i32,
    interview_soft_roundtrip_penalty: i32,
    enable_supermana_prepass_exception: bool,
    enable_quiescence_search: bool,
    quiescence_node_budget: usize,
    enable_quiescence_tactical_children_only: bool,
    quiescence_tactical_enum_limit: usize,
    quiet_reduction_depth_threshold: usize,
    enable_futility_pruning: bool,
    futility_margin: i32,
}

#[cfg(any(target_arch = "wasm32", test))]
impl AutomoveSearchConfig {
    fn from_preference(preference: SmartAutomovePreference) -> Self {
        let (depth, max_visited_nodes) = preference.depth_and_max_nodes();
        let config = Self::from_budget(depth, max_visited_nodes).for_runtime();
        match preference {
            SmartAutomovePreference::Fast => {
                let mut tuned = Self::with_fast_wideroot_shape(config);
                tuned.enable_two_pass_root_allocation = false;
                tuned.root_focus_k = 2;
                tuned.root_focus_budget_share_bp = 6_000;
                tuned.enable_selective_extensions = false;
                tuned.enable_quiet_reductions = true;
                tuned.max_extensions_per_path = 1;
                tuned.selective_extension_node_share_bp = 0;
                tuned.enable_forced_tactical_prepass = true;
                tuned.enable_root_spirit_development_pref = true;
                tuned.enable_root_reply_risk_guard = true;
                tuned.root_reply_risk_score_margin = 125;
                tuned.root_reply_risk_shortlist_max = 4;
                tuned.root_reply_risk_reply_limit = 10;
                tuned.root_reply_risk_node_share_bp = 650;
                tuned.root_anti_help_score_margin = 220;
                tuned.root_anti_help_reply_limit = SMART_ROOT_ANTI_HELP_REPLY_LIMIT_FAST;
                tuned.enable_two_pass_volatility_focus = false;
                tuned.enable_normal_root_safety_rerank = false;
                tuned.enable_normal_root_safety_deep_floor = false;
                tuned.enable_interview_hard_spirit_deploy = false;
                tuned.enable_interview_deterministic_tiebreak = false;
                tuned.prefer_clean_reply_risk_roots = false;
                tuned.root_drainer_safety_score_margin = SMART_ROOT_DRAINER_SAFETY_SCORE_MARGIN;
                tuned.root_mana_handoff_penalty = 300;
                tuned.root_backtrack_penalty = 220;
                tuned.root_efficiency_score_margin = 1_700;
                tuned.potion_spend_penalty_fast = 220;
                tuned.potion_spend_penalty_normal =
                    SMART_POTION_SPEND_NO_COMPENSATION_PENALTY_NORMAL;
                tuned.interview_soft_score_margin = 80;
                tuned.interview_soft_supermana_progress_bonus = 320;
                tuned.interview_soft_supermana_score_bonus = 600;
                tuned.interview_soft_opponent_mana_progress_bonus = 200;
                tuned.interview_soft_opponent_mana_score_bonus = 310;
                tuned.interview_soft_mana_handoff_penalty = 280;
                tuned.interview_soft_roundtrip_penalty = 220;
                tuned.enable_supermana_prepass_exception = true;
                tuned.scoring_weights = &RUNTIME_FAST_DRAINER_CONTEXT_SCORING_WEIGHTS_POTION_PREF;
                tuned
            }
            SmartAutomovePreference::Normal => {
                let mut tuned = Self::with_normal_deeper_shape(config);
                tuned.max_visited_nodes = (tuned.max_visited_nodes * 3 / 2)
                    .clamp(tuned.max_visited_nodes, MAX_SMART_MAX_VISITED_NODES);
                tuned.max_visited_nodes = (tuned.max_visited_nodes * 112 / 100)
                    .clamp(tuned.max_visited_nodes, MAX_SMART_MAX_VISITED_NODES);
                tuned.root_branch_limit = tuned.root_branch_limit.saturating_sub(1).clamp(12, 38);
                tuned.node_branch_limit = (tuned.node_branch_limit + 2).clamp(8, 18);
                tuned.root_enum_limit =
                    (tuned.root_branch_limit * 6).clamp(tuned.root_branch_limit, 240);
                tuned.node_enum_limit =
                    ((tuned.node_branch_limit + 2) * 6).clamp(tuned.node_branch_limit, 156);
                tuned.enable_two_pass_root_allocation = true;
                tuned.root_focus_k = 3;
                tuned.root_focus_budget_share_bp = 7_000;
                tuned.enable_selective_extensions = false;
                tuned.enable_quiet_reductions = false;
                tuned.max_extensions_per_path = 1;
                tuned.selective_extension_node_share_bp =
                    SMART_SELECTIVE_EXTENSION_NODE_SHARE_BP_NORMAL;
                tuned.enable_forced_tactical_prepass = true;
                tuned.enable_root_spirit_development_pref = true;
                tuned.enable_root_reply_risk_guard = true;
                tuned.root_reply_risk_score_margin = 145;
                tuned.root_reply_risk_shortlist_max = 7;
                tuned.root_reply_risk_reply_limit = 16;
                tuned.root_reply_risk_node_share_bp = 1_350;
                tuned.root_anti_help_score_margin = 300;
                tuned.root_anti_help_reply_limit = 10;
                tuned.enable_two_pass_volatility_focus = true;
                tuned.enable_normal_root_safety_rerank = true;
                tuned.enable_normal_root_safety_deep_floor = true;
                tuned.enable_interview_hard_spirit_deploy = true;
                tuned.enable_interview_deterministic_tiebreak = false;
                tuned.prefer_clean_reply_risk_roots = true;
                tuned.root_drainer_safety_score_margin = 4_200;
                tuned.root_mana_handoff_penalty = 340;
                tuned.root_backtrack_penalty = 240;
                tuned.root_efficiency_score_margin = 1_400;
                tuned.selective_extension_node_share_bp = 1_250;
                tuned.potion_spend_penalty_fast = SMART_POTION_SPEND_NO_COMPENSATION_PENALTY_FAST;
                tuned.potion_spend_penalty_normal = 130;
                tuned.interview_soft_score_margin = 80;
                tuned.interview_soft_supermana_progress_bonus = 240;
                tuned.interview_soft_supermana_score_bonus = 300;
                tuned.interview_soft_opponent_mana_progress_bonus = 220;
                tuned.interview_soft_opponent_mana_score_bonus = 280;
                tuned.interview_soft_mana_handoff_penalty = 340;
                tuned.interview_soft_roundtrip_penalty = 260;
                tuned
            }
            SmartAutomovePreference::Pro => {
                let mut tuned = Self::with_normal_deeper_shape(config);
                tuned.max_visited_nodes = SMART_AUTOMOVE_PRO_MAX_VISITED_NODES as usize;
                tuned.root_branch_limit = tuned.root_branch_limit.clamp(14, 34);
                tuned.node_branch_limit = tuned.node_branch_limit.clamp(9, 15);
                tuned.root_enum_limit =
                    (tuned.root_branch_limit * 6).clamp(tuned.root_branch_limit, 204);
                tuned.node_enum_limit =
                    ((tuned.node_branch_limit + 2) * 6).clamp(tuned.node_branch_limit, 132);
                tuned.enable_two_pass_root_allocation = true;
                tuned.root_focus_k = 3;
                tuned.root_focus_budget_share_bp = 7_000;
                tuned.enable_selective_extensions = true;
                tuned.enable_quiet_reductions = true;
                tuned.max_extensions_per_path = 1;
                tuned.selective_extension_node_share_bp = 1_500;
                tuned.enable_forced_tactical_prepass = false;
                tuned.enable_root_spirit_development_pref = true;
                tuned.enable_root_reply_risk_guard = true;
                tuned.root_reply_risk_score_margin = 165;
                tuned.root_reply_risk_shortlist_max = 9;
                tuned.root_reply_risk_reply_limit = 24;
                tuned.root_reply_risk_node_share_bp = 2_000;
                tuned.root_anti_help_score_margin = 300;
                tuned.root_anti_help_reply_limit = 10;
                tuned.enable_two_pass_volatility_focus = true;
                tuned.enable_normal_root_safety_rerank = true;
                tuned.enable_normal_root_safety_deep_floor = true;
                tuned.enable_interview_hard_spirit_deploy = true;
                tuned.enable_interview_deterministic_tiebreak = false;
                tuned.prefer_clean_reply_risk_roots = true;
                tuned.root_drainer_safety_score_margin = 4_800;
                tuned.root_mana_handoff_penalty = 340;
                tuned.root_backtrack_penalty = 240;
                tuned.root_efficiency_score_margin = 1_400;
                tuned.enable_futility_pruning = true;
                tuned.futility_margin = 2_300;
                tuned.quiet_reduction_depth_threshold = 2;
                tuned.potion_spend_penalty_fast = SMART_POTION_SPEND_NO_COMPENSATION_PENALTY_FAST;
                tuned.potion_spend_penalty_normal = 130;
                tuned.interview_soft_score_margin = 80;
                tuned.interview_soft_supermana_progress_bonus = 240;
                tuned.interview_soft_supermana_score_bonus = 300;
                tuned.interview_soft_opponent_mana_progress_bonus = 280;
                tuned.interview_soft_opponent_mana_score_bonus = 340;
                tuned.interview_soft_mana_handoff_penalty = 340;
                tuned.interview_soft_roundtrip_penalty = 260;
                tuned
            }
        }
    }

    fn from_budget(depth: i32, max_visited_nodes: i32) -> Self {
        let depth =
            depth.clamp(MIN_SMART_SEARCH_DEPTH as i32, MAX_SMART_SEARCH_DEPTH as i32) as usize;
        let max_visited_nodes = max_visited_nodes.clamp(
            MIN_SMART_MAX_VISITED_NODES as i32,
            MAX_SMART_MAX_VISITED_NODES as i32,
        ) as usize;

        let root_branch_limit = (max_visited_nodes / 24).clamp(4, 28);
        let node_branch_limit = (max_visited_nodes / 40).clamp(4, 18);
        let root_enum_limit = (root_branch_limit * 5).clamp(root_branch_limit, 180);
        let node_enum_limit = (node_branch_limit * 3).clamp(node_branch_limit, 96);

        Self {
            depth,
            max_visited_nodes,
            root_enum_limit,
            root_branch_limit,
            node_enum_limit,
            node_branch_limit,
            scoring_weights: &DEFAULT_SCORING_WEIGHTS,
            enable_two_pass_root_allocation: false,
            root_focus_k: 2,
            root_focus_budget_share_bp: 7_000,
            enable_selective_extensions: false,
            enable_quiet_reductions: false,
            max_extensions_per_path: 0,
            selective_extension_node_share_bp: 0,
            enable_targeted_drainer_attack_fallback: false,
            enable_forced_tactical_prepass: true,
            enable_turn_head_rerank: false,
            enable_turn_engine_selector: false,
            enable_turn_engine_low_budget_guard: false,
            enable_turn_engine_mid_turn_tactical_guard: false,
            enable_turn_engine_secondary_analysis: true,
            enable_turn_engine_selected_followup_projection: true,
            enable_turn_engine_late_safe_mana_root_preference: false,
            turn_engine_mode: TurnEngineMode::ProV1,
            turn_engine_seed_cap: 0,
            turn_engine_beam_width: 0,
            turn_engine_per_node_family_cap: 0,
            turn_engine_step_cap: 0,
            turn_engine_opponent_seed_cap: 0,
            turn_engine_opponent_beam_width: 0,
            turn_engine_reply_seed_cap: 0,
            turn_engine_reply_beam_width: 0,
            turn_engine_expansion_cap: 0,
            turn_engine_enable_spirit_family: false,
            enable_exact_lite_progress_checks: false,
            enable_exact_lite_spirit_window_checks: false,
            exact_lite_root_call_budget: 0,
            exact_lite_static_call_budget: 0,
            enable_root_spirit_development_pref: true,
            enable_root_reply_risk_guard: false,
            root_reply_risk_score_margin: SMART_ROOT_REPLY_RISK_SCORE_MARGIN,
            root_reply_risk_shortlist_max: SMART_ROOT_REPLY_RISK_SHORTLIST_FAST,
            root_reply_risk_reply_limit: SMART_ROOT_REPLY_RISK_REPLY_LIMIT_FAST,
            root_reply_risk_node_share_bp: SMART_ROOT_REPLY_RISK_NODE_SHARE_BP_FAST,
            root_anti_help_score_margin: SMART_ROOT_ANTI_HELP_SCORE_MARGIN,
            root_anti_help_reply_limit: SMART_ROOT_ANTI_HELP_REPLY_LIMIT_FAST,
            enable_two_pass_volatility_focus: false,
            enable_normal_root_safety_rerank: false,
            enable_normal_root_safety_deep_floor: false,
            enable_interview_hard_spirit_deploy: false,
            enable_interview_deterministic_tiebreak: false,
            prefer_clean_reply_risk_roots: false,
            root_drainer_safety_score_margin: SMART_ROOT_DRAINER_SAFETY_SCORE_MARGIN,
            root_mana_handoff_penalty: SMART_ROOT_MANA_HANDOFF_PENALTY,
            root_backtrack_penalty: SMART_ROOT_BACKTRACK_PENALTY,
            root_efficiency_score_margin: SMART_ROOT_EFFICIENCY_SCORE_MARGIN,
            potion_spend_penalty_fast: SMART_POTION_SPEND_NO_COMPENSATION_PENALTY_FAST,
            potion_spend_penalty_normal: SMART_POTION_SPEND_NO_COMPENSATION_PENALTY_NORMAL,
            interview_soft_score_margin: SMART_INTERVIEW_SOFT_PRIORITY_SCORE_MARGIN,
            interview_soft_supermana_progress_bonus: SMART_INTERVIEW_SOFT_SUPERMANA_PROGRESS_BONUS,
            interview_soft_supermana_score_bonus: SMART_INTERVIEW_SOFT_SUPERMANA_SCORE_BONUS,
            interview_soft_opponent_mana_progress_bonus:
                SMART_INTERVIEW_SOFT_OPPONENT_MANA_PROGRESS_BONUS,
            interview_soft_opponent_mana_score_bonus:
                SMART_INTERVIEW_SOFT_OPPONENT_MANA_SCORE_BONUS,
            interview_soft_mana_handoff_penalty: SMART_INTERVIEW_SOFT_MANA_HANDOFF_PENALTY,
            interview_soft_roundtrip_penalty: SMART_INTERVIEW_SOFT_ROUNDTRIP_PENALTY,
            enable_supermana_prepass_exception: false,
            enable_quiescence_search: false,
            quiescence_node_budget: 0,
            enable_quiescence_tactical_children_only: false,
            quiescence_tactical_enum_limit: 0,
            quiet_reduction_depth_threshold: 3,
            enable_futility_pruning: false,
            futility_margin: 3000,
        }
    }

    fn for_runtime(self) -> Self {
        let mut tuned = self;

        if tuned.depth >= 3 {
            tuned.root_branch_limit = (tuned.root_branch_limit + 10).clamp(6, 36);
            tuned.node_branch_limit = tuned.node_branch_limit.saturating_sub(11).clamp(6, 18);
            tuned.root_enum_limit =
                (tuned.root_branch_limit * 6).clamp(tuned.root_branch_limit, 220);
            tuned.node_enum_limit =
                (tuned.node_branch_limit * 4).clamp(tuned.node_branch_limit, 108);
            tuned.scoring_weights = &BALANCED_DISTANCE_SCORING_WEIGHTS;
        } else {
            tuned.scoring_weights = &MANA_RACE_LITE_D2_TUNED_SCORING_WEIGHTS;
        }

        tuned
    }

    fn with_fast_wideroot_shape(self) -> Self {
        let mut tuned = self;
        tuned.root_branch_limit = (self.root_branch_limit + 8).clamp(8, 40);
        tuned.node_branch_limit = self.node_branch_limit.saturating_sub(2).clamp(6, 18);
        tuned.root_enum_limit = (tuned.root_branch_limit * 6).clamp(tuned.root_branch_limit, 240);
        tuned.node_enum_limit = (tuned.node_branch_limit * 4).clamp(tuned.node_branch_limit, 108);
        tuned
    }

    fn with_normal_deeper_shape(self) -> Self {
        let mut tuned = self;
        tuned.root_branch_limit = self.root_branch_limit.clamp(8, 36);
        tuned.node_branch_limit = (self.node_branch_limit + 3).clamp(9, 18);
        tuned.root_enum_limit = (tuned.root_branch_limit * 6).clamp(tuned.root_branch_limit, 220);
        tuned.node_enum_limit = (tuned.node_branch_limit * 6).clamp(tuned.node_branch_limit, 132);
        tuned
    }
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone)]
struct ScoredRootMove {
    root_rank: usize,
    inputs: Vec<Input>,
    game: MonsGame,
    heuristic: i32,
    efficiency: i32,
    wins_immediately: bool,
    attacks_opponent_drainer: bool,
    own_drainer_vulnerable: bool,
    own_drainer_walk_vulnerable: bool,
    spirit_development: bool,
    keeps_awake_spirit_on_base: bool,
    mana_handoff_to_opponent: bool,
    has_roundtrip: bool,
    scores_supermana_this_turn: bool,
    scores_opponent_mana_this_turn: bool,
    safe_supermana_pickup_now: bool,
    safe_opponent_mana_pickup_now: bool,
    safe_supermana_progress_steps: i32,
    safe_opponent_mana_progress_steps: i32,
    score_path_best_steps: i32,
    same_turn_score_window_value: i32,
    spirit_setup_gain: i32,
    spirit_same_turn_score_setup_now: bool,
    spirit_own_mana_setup_now: bool,
    supermana_progress: bool,
    opponent_mana_progress: bool,
    interview_soft_priority: i32,
    classes: MoveClassFlags,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone)]
pub(crate) struct RootEvaluation {
    root_rank: usize,
    score: i32,
    efficiency: i32,
    inputs: Vec<Input>,
    game: MonsGame,
    wins_immediately: bool,
    attacks_opponent_drainer: bool,
    own_drainer_vulnerable: bool,
    own_drainer_walk_vulnerable: bool,
    spirit_development: bool,
    keeps_awake_spirit_on_base: bool,
    mana_handoff_to_opponent: bool,
    has_roundtrip: bool,
    scores_supermana_this_turn: bool,
    scores_opponent_mana_this_turn: bool,
    safe_supermana_pickup_now: bool,
    safe_opponent_mana_pickup_now: bool,
    safe_supermana_progress_steps: i32,
    safe_opponent_mana_progress_steps: i32,
    score_path_best_steps: i32,
    same_turn_score_window_value: i32,
    spirit_setup_gain: i32,
    spirit_same_turn_score_setup_now: bool,
    spirit_own_mana_setup_now: bool,
    supermana_progress: bool,
    opponent_mana_progress: bool,
    interview_soft_priority: i32,
    classes: MoveClassFlags,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone)]
struct TurnEngineRootProjection {
    plan: TurnPlan,
}

#[cfg(test)]
#[derive(Debug, Clone, Copy, Default)]
pub(crate) struct TurnEngineSelectorDiagnostics {
    pub head_plan_calls: usize,
    pub head_plan_hits: usize,
    pub selected_override_calls: usize,
    pub selected_override_plan_calls: usize,
    pub selected_override_plan_hits: usize,
    pub spirit_projection_passes: usize,
    pub spirit_projection_root_count: usize,
    pub spirit_projection_plan_calls: usize,
    pub spirit_projection_plan_hits: usize,
    pub reply_projection_passes: usize,
    pub reply_projection_root_count: usize,
    pub reply_projection_plan_calls: usize,
    pub reply_projection_plan_hits: usize,
    pub followup_floor_calls: usize,
    pub ranked_child_states_calls: usize,
    pub ranked_child_states_children_enumerated: usize,
    pub ranked_child_states_children_fully_scored: usize,
    pub move_efficiency_snapshot_builds: usize,
    pub move_efficiency_snapshot_cache_hits: usize,
    pub search_preferability_builds: usize,
    pub search_preferability_cache_hits: usize,
    pub selector_disable_reason: &'static str,
    pub last_return_stage: &'static str,
    pub top_level_selector_disable_reason: &'static str,
    pub top_level_last_return_stage: &'static str,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub(crate) enum CurrentProRootAdvisorReasonCode {
    RankedRoot,
    ReplyRiskShortlist,
    PreserveSpiritRepresentative,
    PreserveSafeProgressRepresentative,
    PreserveManaTempoRepresentative,
    OmittedRootReentry,
    AdmitInjectedMacroRoot,
    RejectInjectedMacroRoot,
    ApprovedReplyRiskGuard,
    ApprovedBaselineSelector,
    ApprovedFamilyCompetition,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Debug, PartialEq, Eq)]
pub(crate) struct CurrentProRootAdvisorEntry {
    pub inputs: Vec<Input>,
    pub family: TurnPlanFamily,
    pub root_rank: usize,
    pub reason: CurrentProRootAdvisorReasonCode,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Debug, PartialEq, Eq)]
pub(crate) struct CurrentProInjectedRootAdvisorDecision {
    pub inputs: Vec<Input>,
    pub family: TurnPlanFamily,
    pub admitted: bool,
    pub reason: CurrentProRootAdvisorReasonCode,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Debug, Default, PartialEq, Eq)]
pub(crate) struct CurrentProRootAdvisorDecision {
    pub ordered_shortlist: Vec<CurrentProRootAdvisorEntry>,
    pub preserved_family_representatives: Vec<CurrentProRootAdvisorEntry>,
    pub approved_root: Option<CurrentProRootAdvisorEntry>,
    pub injected_root: Option<CurrentProInjectedRootAdvisorDecision>,
}

#[cfg(test)]
thread_local! {
    static TURN_ENGINE_SELECTOR_DIAGNOSTICS: std::cell::RefCell<TurnEngineSelectorDiagnostics> =
        std::cell::RefCell::new(TurnEngineSelectorDiagnostics::default());
    static TURN_ENGINE_SELECTOR_RUNTIME_DEPTH: std::cell::Cell<usize> =
        const { std::cell::Cell::new(0) };
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
struct TurnEngineSelectorFollowupFloorCacheKey {
    state_hash: u64,
    perspective: Color,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
struct TurnEngineSelectedOverrideCacheKey {
    state_hash: u64,
    family: TurnPlanFamily,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
struct MoveEfficiencySnapshotCacheKey {
    state_hash: u64,
    perspective: Color,
    include_tactical_exact: bool,
    include_strategic_exact: bool,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash)]
struct SearchPreferabilityCacheKey {
    state_hash: u64,
    perspective: Color,
    scoring_weights_key: u64,
}

#[cfg(any(target_arch = "wasm32", test))]
thread_local! {
    static TURN_ENGINE_SELECTOR_FOLLOWUP_FLOOR_CACHE: std::cell::RefCell<
        std::collections::HashMap<TurnEngineSelectorFollowupFloorCacheKey, i32>
    > = std::cell::RefCell::new(std::collections::HashMap::new());
    static TURN_ENGINE_SELECTED_OVERRIDE_UTILITY_CACHE: std::cell::RefCell<
        std::collections::HashMap<TurnEngineSelectedOverrideCacheKey, TurnEngineUtility>
    > = std::cell::RefCell::new(std::collections::HashMap::new());
    static MOVE_EFFICIENCY_SNAPSHOT_CACHE: std::cell::RefCell<
        FastHashMap<MoveEfficiencySnapshotCacheKey, MoveEfficiencySnapshot>
    > = std::cell::RefCell::new(FastHashMap::default());
    static SEARCH_PREFERABILITY_CACHE: std::cell::RefCell<
        FastHashMap<SearchPreferabilityCacheKey, i32>
    > = std::cell::RefCell::new(FastHashMap::default());
}

#[cfg(any(target_arch = "wasm32", test))]
fn clear_turn_engine_selector_followup_floor_cache() {
    TURN_ENGINE_SELECTOR_FOLLOWUP_FLOOR_CACHE.with(|cache| cache.borrow_mut().clear());
    TURN_ENGINE_SELECTED_OVERRIDE_UTILITY_CACHE.with(|cache| cache.borrow_mut().clear());
    MOVE_EFFICIENCY_SNAPSHOT_CACHE.with(|cache| cache.borrow_mut().clear());
    SEARCH_PREFERABILITY_CACHE.with(|cache| cache.borrow_mut().clear());
}

#[cfg(test)]
pub(crate) fn clear_turn_engine_selector_diagnostics() {
    TURN_ENGINE_SELECTOR_DIAGNOSTICS.with(|diagnostics| {
        *diagnostics.borrow_mut() = TurnEngineSelectorDiagnostics::default();
    });
    clear_turn_engine_selector_followup_floor_cache();
}

#[cfg(test)]
pub(crate) fn turn_engine_selector_diagnostics_snapshot() -> TurnEngineSelectorDiagnostics {
    TURN_ENGINE_SELECTOR_DIAGNOSTICS.with(|diagnostics| *diagnostics.borrow())
}

#[cfg(test)]
fn move_efficiency_snapshot_cache_len() -> usize {
    MOVE_EFFICIENCY_SNAPSHOT_CACHE.with(|cache| cache.borrow().len())
}

#[cfg(test)]
fn search_preferability_cache_len() -> usize {
    SEARCH_PREFERABILITY_CACHE.with(|cache| cache.borrow().len())
}

#[cfg(test)]
fn update_turn_engine_selector_diagnostics(
    update: impl FnOnce(&mut TurnEngineSelectorDiagnostics),
) {
    TURN_ENGINE_SELECTOR_DIAGNOSTICS.with(|diagnostics| update(&mut diagnostics.borrow_mut()));
}

#[cfg(test)]
struct TurnEngineSelectorRuntimeDepthGuard;

#[cfg(test)]
impl TurnEngineSelectorRuntimeDepthGuard {
    fn enter() -> Self {
        TURN_ENGINE_SELECTOR_RUNTIME_DEPTH.with(|depth| depth.set(depth.get().saturating_add(1)));
        Self
    }
}

#[cfg(test)]
impl Drop for TurnEngineSelectorRuntimeDepthGuard {
    fn drop(&mut self) {
        TURN_ENGINE_SELECTOR_RUNTIME_DEPTH.with(|depth| {
            depth.set(depth.get().saturating_sub(1));
        });
    }
}

#[cfg(test)]
fn turn_engine_selector_runtime_depth() -> usize {
    TURN_ENGINE_SELECTOR_RUNTIME_DEPTH.with(|depth| depth.get())
}

#[cfg(test)]
fn update_top_level_turn_engine_selector_disable_reason(reason: &'static str) {
    if turn_engine_selector_runtime_depth() == 1 {
        update_turn_engine_selector_diagnostics(|diagnostics| {
            diagnostics.top_level_selector_disable_reason = reason;
        });
    }
}

#[cfg(test)]
fn update_top_level_turn_engine_selector_last_return_stage(stage: &'static str) {
    if turn_engine_selector_runtime_depth() == 1 {
        update_turn_engine_selector_diagnostics(|diagnostics| {
            diagnostics.top_level_last_return_stage = stage;
        });
    }
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
struct MoveEfficiencySnapshot {
    my_best_carrier_steps: i32,
    opponent_best_carrier_steps: i32,
    my_best_drainer_to_mana_steps: i32,
    opponent_best_drainer_to_mana_steps: i32,
    my_carrier_count: i32,
    opponent_carrier_count: i32,
    my_spirit_on_base: bool,
    opponent_spirit_on_base: bool,
    my_spirit_action_targets: i32,
    opponent_spirit_action_targets: i32,
    my_same_turn_score_value: i32,
    opponent_same_turn_score_value: i32,
    my_same_turn_opponent_mana_score_value: i32,
    opponent_same_turn_opponent_mana_score_value: i32,
    my_safe_supermana_progress: bool,
    opponent_safe_supermana_progress: bool,
    my_safe_opponent_mana_progress: bool,
    opponent_safe_opponent_mana_progress: bool,
    my_safe_supermana_progress_steps: i32,
    opponent_safe_supermana_progress_steps: i32,
    my_safe_opponent_mana_progress_steps: i32,
    opponent_safe_opponent_mana_progress_steps: i32,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy)]
struct CarrierProgressSnapshot {
    carrier_count: i32,
    best_carrier_steps: i32,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy)]
struct NormalRootSafetySnapshot {
    allows_immediate_opponent_win: bool,
    opponent_reaches_match_point: bool,
    opponent_max_score_gain: i32,
    my_score_gain: i32,
    worst_reply_score: i32,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
enum MoveClass {
    ImmediateScore,
    DrainerAttack,
    DrainerSafetyRecover,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy, Debug, Default)]
struct MoveClassFlags {
    immediate_score: bool,
    drainer_attack: bool,
    drainer_safety_recover: bool,
    carrier_progress: bool,
    material: bool,
    quiet: bool,
}

#[cfg(any(target_arch = "wasm32", test))]
impl MoveClassFlags {
    fn has(self, class: MoveClass) -> bool {
        match class {
            MoveClass::ImmediateScore => self.immediate_score,
            MoveClass::DrainerAttack => self.drainer_attack,
            MoveClass::DrainerSafetyRecover => self.drainer_safety_recover,
        }
    }

    fn is_tactical_priority(self) -> bool {
        self.immediate_score || self.drainer_attack || self.drainer_safety_recover
    }
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy)]
struct RootReplyRiskSnapshot {
    allows_immediate_opponent_win: bool,
    opponent_reaches_match_point: bool,
    worst_reply_score: i32,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy)]
enum TranspositionBound {
    Exact,
    LowerBound,
    UpperBound,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone, Copy)]
struct TranspositionEntry {
    depth: usize,
    score: i32,
    bound: TranspositionBound,
    best_child_hash: u64,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone)]
struct RankedChildState {
    game: MonsGame,
    hash: u64,
    ordering_efficiency: i32,
    tactical_extension_trigger: bool,
    quiet_reduction_candidate: bool,
    classes: MoveClassFlags,
}

#[cfg(any(target_arch = "wasm32", test))]
struct ChildOrderingScratch {
    before_efficiency_snapshot: MoveEfficiencySnapshot,
    own_drainer_vulnerable_before: bool,
}

#[cfg(any(target_arch = "wasm32", test))]
#[derive(Clone)]
pub(crate) struct LegalInputTransition {
    pub inputs: Vec<Input>,
    pub game: MonsGame,
    pub events: Vec<Event>,
}

#[cfg(any(target_arch = "wasm32", test))]
type RootMoveRepresentativeSpec = (CurrentProRootAdvisorReasonCode, fn(&ScoredRootMove) -> bool);

#[cfg(any(target_arch = "wasm32", test))]
type RootEvaluationRepresentativeSpec =
    (CurrentProRootAdvisorReasonCode, fn(&RootEvaluation) -> bool);

#[cfg(test)]
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
enum AsyncSmartSearchPhase {
    PendingRootRanking,
    PendingFocusedCandidates,
    Scoring,
}

#[cfg(test)]
#[allow(clippy::large_enum_variant)]
enum AsyncSmartSearchStart {
    Immediate(OutputModel),
    Pending(Box<AsyncSmartSearchState>),
}

#[cfg(test)]
struct AsyncSmartSearchState {
    game: MonsGame,
    perspective: Color,
    config: AutomoveSearchConfig,
    phase: AsyncSmartSearchPhase,
    root_moves: Vec<ScoredRootMove>,
    pending_output: Option<OutputModel>,
    next_index: usize,
    visited_nodes: usize,
    alpha: i32,
    extension_nodes_used: usize,
    extension_node_budget: usize,
    scored_roots: Vec<RootEvaluation>,
    transposition_table: U64HashMap<TranspositionEntry>,
    quiescence_nodes_used: usize,
}

#[wasm_bindgen]
impl MonsGameModel {
    fn with_game(game: MonsGame) -> Self {
        Self { game }
    }

    pub fn new(variant: GameVariant) -> MonsGameModel {
        Self::with_game(MonsGame::new(true, variant))
    }

    #[wasm_bindgen(js_name = newForSimulation)]
    pub fn new_for_simulation(variant: GameVariant) -> MonsGameModel {
        let mut game = MonsGame::new(false, variant);
        game.set_takeback_history_tracking(false);
        Self::with_game(game)
    }

    pub fn from_fen(fen: &str) -> Option<MonsGameModel> {
        MonsGame::from_fen(fen, true).map(Self::with_game)
    }

    #[wasm_bindgen(js_name = fromFenForSimulation)]
    pub fn from_fen_for_simulation(fen: &str) -> Option<MonsGameModel> {
        MonsGame::from_fen(fen, false).map(|mut game| {
            game.set_takeback_history_tracking(false);
            Self::with_game(game)
        })
    }

    pub fn without_last_turn(&self, takeback_fens: Vec<String>) -> Option<MonsGameModel> {
        let mut verbose_tracking_entities = self.game.verbose_tracking_entities.clone();

        if verbose_tracking_entities.len() <= 1 {
            return None;
        }

        verbose_tracking_entities.pop();

        let fen = verbose_tracking_entities
            .last()
            .map(|e| e.fen.clone())
            .unwrap_or_else(|| self.game.fen());

        if let Some(mut new_game) = MonsGame::from_fen(fen.as_str(), true) {
            new_game.takeback_fens = takeback_fens;
            new_game.verbose_tracking_entities = verbose_tracking_entities;
            new_game.with_verbose_tracking = self.game.with_verbose_tracking;
            new_game.is_moves_verified = self.game.is_moves_verified;
            return Some(Self::with_game(new_game));
        }

        None
    }

    pub fn fen(&self) -> String {
        self.game.fen()
    }

    #[cfg(target_arch = "wasm32")]
    #[wasm_bindgen(js_name = smartAutomove)]
    pub fn smart_automove(&self, preference: &str) -> Result<OutputModel, JsValue> {
        let Some(preference) = SmartAutomovePreference::from_api_value(preference) else {
            let message = format!(
                "invalid smart automove mode; expected '{}', '{}', or '{}'",
                SmartAutomovePreference::Fast.as_api_value(),
                SmartAutomovePreference::Normal.as_api_value(),
                SmartAutomovePreference::Pro.as_api_value(),
            );
            return Err(JsValue::from_str(message.as_str()));
        };

        Ok(self.smart_automove_output(preference))
    }

    pub fn automove(&mut self) -> OutputModel {
        Self::automove_game(&mut self.game)
    }

    fn automove_game(game: &mut MonsGame) -> OutputModel {
        let automove_start_options = Some(SuggestedStartInputOptions::for_automove());
        let mut inputs = Vec::new();
        let mut output =
            game.process_input_with_start_options_slice(&[], false, false, automove_start_options);

        loop {
            match output {
                Output::InvalidInput => {
                    return OutputModel::new(Output::InvalidInput, "");
                }
                Output::LocationsToStartFrom(locations) => {
                    if locations.is_empty() {
                        return OutputModel::new(Output::InvalidInput, "");
                    }
                    let random_index = random_index(locations.len());
                    let location = locations[random_index];
                    inputs.push(Input::Location(location));
                    output = game.process_input_with_start_options_slice(
                        inputs.as_slice(),
                        false,
                        false,
                        automove_start_options,
                    );
                }
                Output::NextInputOptions(options) => {
                    if options.is_empty() {
                        return OutputModel::new(Output::InvalidInput, "");
                    }
                    let random_index = random_index(options.len());
                    let next_input = options[random_index].input;
                    inputs.push(next_input);
                    output = game.process_input_with_start_options_slice(
                        inputs.as_slice(),
                        false,
                        false,
                        automove_start_options,
                    );
                }
                Output::Events(events) => {
                    let input_fen = Input::fen_from_array(&inputs);
                    return OutputModel::new(Output::Events(events), input_fen.as_str());
                }
            }
        }
    }

    pub fn process_input(
        &mut self,
        locations: Vec<Location>,
        modifier: Option<Modifier>,
    ) -> OutputModel {
        let mut inputs: Vec<Input> = locations.into_iter().map(Input::Location).collect();
        if let Some(modifier) = modifier {
            inputs.push(Input::Modifier(modifier));
        }
        let input_fen = Input::fen_from_array(&inputs);
        let output = self.game.process_input(inputs, false, false);
        OutputModel::new(output, input_fen.as_str())
    }

    pub fn can_takeback(&self, color: Color) -> bool {
        self.game.can_takeback(color)
    }

    #[wasm_bindgen(js_name = setVerboseTracking)]
    pub fn set_verbose_tracking(&mut self, enabled: bool) {
        self.game.set_verbose_tracking(enabled);
    }

    #[wasm_bindgen(js_name = clearTracking)]
    pub fn clear_tracking(&mut self) {
        self.game.clear_tracking();
    }

    pub fn takeback(&mut self) -> OutputModel {
        let inputs: Vec<Input> = vec![Input::Takeback];
        let input_fen = Input::fen_from_array(&inputs);
        let output = self.game.process_input(inputs, false, false);
        OutputModel::new(output, input_fen.as_str())
    }

    pub fn process_input_fen(&mut self, input_fen: &str) -> OutputModel {
        let inputs = Input::array_from_fen(input_fen);
        let output = self.game.process_input(inputs, false, false);
        OutputModel::new(output, input_fen)
    }

    pub fn remove_item(&mut self, location: Location) {
        self.game.board.remove_item(location);
        self.game.invalidate_process_input_cache();
    }

    pub fn item(&self, at: Location) -> Option<ItemModel> {
        self.game.board.item(at).map(ItemModel::new)
    }

    pub fn square(&self, at: Location) -> SquareModel {
        let square = self.game.board.square(at);
        SquareModel::new(&square)
    }

    pub fn is_later_than(&self, other_fen: &str) -> bool {
        if let Some(other_game) = MonsGame::from_fen(other_fen, false) {
            self.game.is_later_than(&other_game)
        } else {
            true
        }
    }

    pub fn is_moves_verified(&self) -> bool {
        self.game.is_moves_verified
    }

    pub fn verify_moves(&mut self, flat_moves_string_w: &str, flat_moves_string_b: &str) -> bool {
        let moves_w: Vec<&str> = if flat_moves_string_w.is_empty() {
            Vec::new()
        } else {
            flat_moves_string_w.split("-").collect()
        };
        let moves_b: Vec<&str> = if flat_moves_string_b.is_empty() {
            Vec::new()
        } else {
            flat_moves_string_b.split("-").collect()
        };

        let with_verbose_tracking = self.game.with_verbose_tracking;
        let mut fresh_verification_game = MonsGame::new(with_verbose_tracking, self.game.variant());

        let mut w_index = 0;
        let mut b_index = 0;

        while w_index < moves_w.len() || b_index < moves_b.len() {
            if fresh_verification_game.active_color == Color::White {
                if w_index >= moves_w.len() {
                    return false;
                }
                let inputs = Input::array_from_fen(moves_w[w_index]);
                _ = fresh_verification_game.process_input(inputs, false, false);
                w_index += 1;
            } else {
                if b_index >= moves_b.len() {
                    return false;
                }
                let inputs = Input::array_from_fen(moves_b[b_index]);
                _ = fresh_verification_game.process_input(inputs, false, false);
                b_index += 1;
            }
        }

        if fresh_verification_game.fen() == self.game.fen() {
            self.game.takeback_fens = fresh_verification_game.takeback_fens;
            if with_verbose_tracking {
                self.game.verbose_tracking_entities =
                    fresh_verification_game.verbose_tracking_entities;
            } else {
                self.game.verbose_tracking_entities.clear();
                self.game.verbose_tracking_entities.shrink_to_fit();
            }
            self.game.is_moves_verified = true;
            true
        } else {
            false
        }
    }

    pub fn active_color(&self) -> Color {
        self.game.active_color
    }

    pub fn winner_color(&self) -> Option<Color> {
        self.game.winner_color()
    }

    pub fn black_score(&self) -> i32 {
        self.game.black_score
    }

    pub fn white_score(&self) -> i32 {
        self.game.white_score
    }

    pub fn turn_number(&self) -> i32 {
        self.game.turn_number
    }

    pub fn inactive_player_items_counters(&self) -> Vec<i32> {
        let player_potions_count = match self.game.active_color.other() {
            Color::White => self.game.white_potions_count,
            Color::Black => self.game.black_potions_count,
        };
        [0, 0, 0, player_potions_count].to_vec()
    }

    pub fn takeback_fens(&self) -> Vec<String> {
        self.game.takeback_fens.clone()
    }

    pub fn available_move_kinds(&self) -> Vec<i32> {
        let map = self.game.available_move_kinds();
        [
            map[&AvailableMoveKind::MonMove],
            map[&AvailableMoveKind::ManaMove],
            map[&AvailableMoveKind::Action],
            map[&AvailableMoveKind::Potion],
        ]
        .to_vec()
    }

    pub fn locations_with_content(&self) -> Vec<Location> {
        let mut locations: Vec<Location> = self
            .game
            .board
            .items
            .iter()
            .enumerate()
            .filter_map(|(idx, opt)| opt.as_ref().map(|_| Location::from_index(idx)))
            .collect();
        let mons_bases = self.game.board.all_mons_bases();
        locations.extend(mons_bases);
        locations.sort();
        locations.dedup();
        locations
    }

    pub fn verbose_tracking_entities(&self) -> Vec<VerboseTrackingEntityModel> {
        self.game
            .verbose_tracking_entities
            .iter()
            .map(VerboseTrackingEntityModel::new)
            .collect()
    }
}

#[cfg(any(target_arch = "wasm32", test))]
impl MonsGameModel {
    fn public_runtime_inputs(
        &self,
        preference: SmartAutomovePreference,
        config: AutomoveSearchConfig,
    ) -> Vec<Input> {
        match preference {
            SmartAutomovePreference::Pro => {
                automove_runtime_variants::select_current_pro_bounded_tactical_inputs(
                    &self.game, config,
                )
            }
            SmartAutomovePreference::Fast | SmartAutomovePreference::Normal => {
                automove_runtime_variants::select_shipping_search_inputs(&self.game, config)
            }
        }
    }

    fn output_model_from_runtime_inputs(&self, inputs: Vec<Input>) -> OutputModel {
        if inputs.is_empty() {
            let mut game = self.game.clone_for_simulation();
            return Self::automove_game(&mut game);
        }

        let mut game = self.game.clone_for_simulation();
        let input_fen = Input::fen_from_array(&inputs);
        let output = game.process_input(inputs, false, false);
        OutputModel::new(output, input_fen.as_str())
    }

    fn smart_automove_output(&self, preference: SmartAutomovePreference) -> OutputModel {
        let config = self.shipping_search_config_for_preference(preference);
        let inputs = self.public_runtime_inputs(preference, config);
        self.output_model_from_runtime_inputs(inputs)
    }

    #[cfg(test)]
    fn new_async_smart_search_state(
        game: MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> AsyncSmartSearchState {
        AsyncSmartSearchState {
            game,
            perspective,
            config,
            phase: AsyncSmartSearchPhase::PendingRootRanking,
            root_moves: Vec::new(),
            pending_output: None,
            next_index: 0,
            visited_nodes: 0,
            alpha: i32::MIN,
            extension_nodes_used: 0,
            extension_node_budget: 0,
            scored_roots: Vec::new(),
            transposition_table: U64HashMap::default(),
            quiescence_nodes_used: 0,
        }
    }

    #[cfg(test)]
    fn begin_async_smart_search(
        &self,
        preference: SmartAutomovePreference,
    ) -> AsyncSmartSearchStart {
        clear_exact_state_analysis_cache();
        let config = self.shipping_search_config_for_preference(preference);
        if matches!(preference, SmartAutomovePreference::Pro) {
            let inputs = self.public_runtime_inputs(preference, config);
            return AsyncSmartSearchStart::Immediate(self.output_model_from_runtime_inputs(inputs));
        }
        let perspective = self.game.active_color;
        let game = self.game.clone_for_simulation();
        AsyncSmartSearchStart::Pending(Box::new(Self::new_async_smart_search_state(
            game,
            perspective,
            config,
        )))
    }

    fn shipping_search_config_for_preference(
        &self,
        preference: SmartAutomovePreference,
    ) -> AutomoveSearchConfig {
        Self::shipping_search_config_for_game(&self.game, preference)
    }

    #[cfg(test)]
    fn smart_automove_output_via_async_loop(
        &self,
        preference: SmartAutomovePreference,
    ) -> OutputModel {
        let mut state = match self.begin_async_smart_search(preference) {
            AsyncSmartSearchStart::Immediate(output) => return output,
            AsyncSmartSearchStart::Pending(state) => *state,
        };

        for _ in 0..1_000_000usize {
            if Self::advance_async_search(&mut state) {
                return Self::finalize_async_search(&mut state);
            }
        }

        panic!(
            "async smart search did not finish for {} within tick budget",
            preference.as_api_value()
        );
    }

    fn shipping_search_config_for_game(
        game: &MonsGame,
        preference: SmartAutomovePreference,
    ) -> AutomoveSearchConfig {
        let mut config = Self::with_runtime_scoring_weights(
            game,
            AutomoveSearchConfig::from_preference(preference),
        );
        if Self::uses_deep_runtime_context(preference) {
            config = Self::apply_deep_runtime_context_profile(game, config, preference);
        }
        config = Self::with_pre_exact_runtime_policy(config);
        if matches!(preference, SmartAutomovePreference::Normal) {
            config = Self::apply_runtime_normal_fast_core_budget_spend_profile(config);
        }
        config
    }

    fn uses_deep_runtime_context(preference: SmartAutomovePreference) -> bool {
        matches!(preference, SmartAutomovePreference::Pro)
    }

    fn apply_deep_runtime_context_profile(
        game: &MonsGame,
        config: AutomoveSearchConfig,
        preference: SmartAutomovePreference,
    ) -> AutomoveSearchConfig {
        match preference {
            SmartAutomovePreference::Pro => Self::apply_pro_primary_profile(game, config),
            SmartAutomovePreference::Fast | SmartAutomovePreference::Normal => config,
        }
    }

    fn apply_runtime_normal_fast_policy_block(
        mut config: AutomoveSearchConfig,
    ) -> AutomoveSearchConfig {
        config.root_branch_limit = (config.root_branch_limit + 5).clamp(12, 40);
        config.node_branch_limit = config.node_branch_limit.saturating_sub(2).clamp(8, 18);
        config.root_enum_limit =
            (config.root_branch_limit * 6).clamp(config.root_branch_limit, 240);
        config.node_enum_limit =
            (config.node_branch_limit * 4).clamp(config.node_branch_limit, 108);
        config.enable_two_pass_root_allocation = false;
        config.root_focus_k = 2;
        config.root_focus_budget_share_bp = 6_000;
        config.enable_selective_extensions = false;
        config.selective_extension_node_share_bp = 0;
        config.enable_quiet_reductions = true;
        config.root_reply_risk_score_margin = 125;
        config.root_reply_risk_shortlist_max = 4;
        config.root_reply_risk_reply_limit = 10;
        config.root_reply_risk_node_share_bp = 650;
        config.root_anti_help_score_margin = 220;
        config.root_anti_help_reply_limit = SMART_ROOT_ANTI_HELP_REPLY_LIMIT_FAST;
        config.enable_two_pass_volatility_focus = false;
        config.enable_normal_root_safety_rerank = false;
        config.enable_normal_root_safety_deep_floor = false;
        config.enable_interview_hard_spirit_deploy = false;
        config.enable_interview_deterministic_tiebreak = false;
        config.prefer_clean_reply_risk_roots = false;
        config.root_drainer_safety_score_margin = SMART_ROOT_DRAINER_SAFETY_SCORE_MARGIN;
        config.root_mana_handoff_penalty = 300;
        config.root_backtrack_penalty = 220;
        config.root_efficiency_score_margin = 1_700;
        config.potion_spend_penalty_fast = 220;
        config.potion_spend_penalty_normal = SMART_POTION_SPEND_NO_COMPENSATION_PENALTY_NORMAL;
        config.interview_soft_score_margin = 80;
        config.interview_soft_supermana_progress_bonus = 320;
        config.interview_soft_supermana_score_bonus = 600;
        config.interview_soft_opponent_mana_progress_bonus = 200;
        config.interview_soft_opponent_mana_score_bonus = 310;
        config.interview_soft_mana_handoff_penalty = 280;
        config.interview_soft_roundtrip_penalty = 220;
        config.enable_supermana_prepass_exception = true;
        config.scoring_weights = &RUNTIME_FAST_DRAINER_CONTEXT_SCORING_WEIGHTS_POTION_PREF;
        config
    }

    fn apply_runtime_normal_fast_core_budget_spend_profile(
        mut config: AutomoveSearchConfig,
    ) -> AutomoveSearchConfig {
        if config.depth < 3 || config.depth >= SMART_AUTOMOVE_PRO_DEPTH as usize {
            return config;
        }
        config = Self::apply_runtime_normal_fast_policy_block(config);
        config.enable_exact_lite_progress_checks = true;
        config.enable_exact_lite_spirit_window_checks = true;
        config.exact_lite_root_call_budget = 1;
        config.exact_lite_static_call_budget = 1;
        config.max_visited_nodes = config.max_visited_nodes.saturating_mul(130) / 100;
        config.root_branch_limit = (config.root_branch_limit + 2).clamp(12, 40);
        config.root_enum_limit =
            (config.root_branch_limit * 6).clamp(config.root_branch_limit, 240);
        config.root_reply_risk_shortlist_max = config.root_reply_risk_shortlist_max.max(5);
        config.root_reply_risk_reply_limit = config.root_reply_risk_reply_limit.max(12);
        config.root_reply_risk_node_share_bp = config.root_reply_risk_node_share_bp.max(900);
        config
    }

    fn apply_pro_primary_profile(
        game: &MonsGame,
        mut config: AutomoveSearchConfig,
    ) -> AutomoveSearchConfig {
        if config.depth < SMART_AUTOMOVE_PRO_DEPTH as usize {
            return config;
        }
        config.max_visited_nodes = SMART_AUTOMOVE_PRO_MAX_VISITED_NODES as usize;
        config.enable_forced_tactical_prepass = false;
        config.root_branch_limit = config.root_branch_limit.clamp(14, 34);
        config.node_branch_limit = config.node_branch_limit.clamp(9, 15);
        config.root_enum_limit =
            (config.root_branch_limit * 6).clamp(config.root_branch_limit, 204);
        config.node_enum_limit =
            ((config.node_branch_limit + 2) * 6).clamp(config.node_branch_limit, 132);
        config.enable_futility_pruning = true;
        config.futility_margin = 2_300;
        config.enable_quiet_reductions = true;
        config.quiet_reduction_depth_threshold = 2;
        config.enable_root_reply_risk_guard = true;
        config.root_reply_risk_score_margin = 165;
        config.root_reply_risk_shortlist_max = 9;
        config.root_reply_risk_reply_limit = 24;
        config.root_reply_risk_node_share_bp = 2_000;
        config.enable_normal_root_safety_rerank = true;
        config.enable_normal_root_safety_deep_floor = true;
        config.root_drainer_safety_score_margin = 4_800;
        config.enable_selective_extensions = true;
        config.max_extensions_per_path = 1;
        config.selective_extension_node_share_bp = 1_500;
        config.enable_interview_deterministic_tiebreak = true;
        config.max_visited_nodes = config.max_visited_nodes.saturating_mul(9) / 8;
        config.root_branch_limit = config.root_branch_limit.saturating_add(1).min(16);
        config.node_branch_limit = config.node_branch_limit.saturating_add(1).min(12);
        config.enable_turn_head_rerank = true;
        config.scoring_weights =
            Self::runtime_phase_adaptive_attacker_proximity_scoring_weights(game, config.depth);
        config.interview_soft_opponent_mana_progress_bonus = 320;
        config.interview_soft_opponent_mana_score_bonus = 400;
        config.enable_quiescence_search = true;
        config.quiescence_node_budget = 120;
        config.enable_quiescence_tactical_children_only = true;
        config.quiescence_tactical_enum_limit = 12;
        config
    }

    fn with_pre_exact_runtime_policy(mut config: AutomoveSearchConfig) -> AutomoveSearchConfig {
        config.enable_exact_lite_progress_checks = false;
        config.enable_exact_lite_spirit_window_checks = false;
        config.exact_lite_root_call_budget = 0;
        config.exact_lite_static_call_budget = 0;
        config
    }

    fn with_runtime_scoring_weights(
        game: &MonsGame,
        mut config: AutomoveSearchConfig,
    ) -> AutomoveSearchConfig {
        config.scoring_weights = if config.depth < 3 {
            &RUNTIME_FAST_BOOLEAN_DRAINER_SCORING_WEIGHTS_POTION_PREF
        } else {
            Self::runtime_phase_adaptive_walk_threat_medium_scoring_weights(game, config.depth)
        };
        if config.depth >= 3 {
            config.max_visited_nodes = (config.max_visited_nodes * 120) / 100;
        }
        config
    }

    fn runtime_phase_adaptive_walk_threat_medium_scoring_weights(
        game: &MonsGame,
        depth: usize,
    ) -> &'static ScoringWeights {
        if depth < 3 {
            return &RUNTIME_FAST_BOOLEAN_DRAINER_SCORING_WEIGHTS;
        }

        let (my_score, opponent_score) = if game.active_color == Color::White {
            (game.white_score, game.black_score)
        } else {
            (game.black_score, game.white_score)
        };
        let my_distance_to_win = Config::TARGET_SCORE - my_score;
        let opponent_distance_to_win = Config::TARGET_SCORE - opponent_score;
        let score_gap = my_score - opponent_score;

        if my_distance_to_win <= 1 {
            &RUNTIME_NORMAL_WALK_THREAT_MEDIUM_FINISHER_BALANCED_SOFT_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS
        } else if opponent_distance_to_win <= 1 {
            &RUNTIME_NORMAL_WALK_THREAT_MEDIUM_TACTICAL_BALANCED_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS
        } else if my_distance_to_win <= 2 {
            &RUNTIME_NORMAL_WALK_THREAT_MEDIUM_FINISHER_BALANCED_SOFT_SPIRIT_BASE_SCORING_WEIGHTS
        } else if opponent_distance_to_win <= 2 || score_gap <= -1 {
            &RUNTIME_NORMAL_WALK_THREAT_MEDIUM_TACTICAL_BALANCED_SPIRIT_BASE_SCORING_WEIGHTS
        } else {
            &RUNTIME_NORMAL_WALK_THREAT_MEDIUM_BALANCED_DISTANCE_SPIRIT_BASE_SCORING_WEIGHTS
        }
    }

    fn runtime_phase_adaptive_attacker_proximity_scoring_weights(
        game: &MonsGame,
        depth: usize,
    ) -> &'static ScoringWeights {
        if depth < 3 {
            return &RUNTIME_FAST_BOOLEAN_DRAINER_SCORING_WEIGHTS;
        }

        let (my_score, opponent_score) = if game.active_color == Color::White {
            (game.white_score, game.black_score)
        } else {
            (game.black_score, game.white_score)
        };
        let my_distance_to_win = Config::TARGET_SCORE - my_score;
        let opponent_distance_to_win = Config::TARGET_SCORE - opponent_score;
        let score_gap = my_score - opponent_score;

        if my_distance_to_win <= 1 {
            &RUNTIME_NORMAL_ATTACKER_PROXIMITY_FINISHER_BALANCED_SOFT_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS
        } else if opponent_distance_to_win <= 1 {
            &RUNTIME_NORMAL_ATTACKER_PROXIMITY_TACTICAL_BALANCED_AGGRESSIVE_SPIRIT_BASE_SCORING_WEIGHTS
        } else if my_distance_to_win <= 2 {
            &RUNTIME_NORMAL_ATTACKER_PROXIMITY_FINISHER_BALANCED_SOFT_SPIRIT_BASE_SCORING_WEIGHTS
        } else if opponent_distance_to_win <= 2 || score_gap <= -1 {
            &RUNTIME_NORMAL_ATTACKER_PROXIMITY_TACTICAL_BALANCED_SPIRIT_BASE_SCORING_WEIGHTS
        } else {
            &RUNTIME_NORMAL_ATTACKER_PROXIMITY_BALANCED_DISTANCE_SPIRIT_BASE_SCORING_WEIGHTS
        }
    }

    fn build_scored_root_move(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        own_drainer_vulnerable_before: bool,
        inputs: &[Input],
    ) -> Option<ScoredRootMove> {
        let (simulated_game, events) = Self::apply_inputs_for_search_with_events(game, inputs)?;
        Self::build_scored_root_move_from_transition(
            game,
            perspective,
            config,
            own_drainer_vulnerable_before,
            inputs.to_vec(),
            simulated_game,
            events,
        )
    }

    fn live_spirit_setup_gain(
        exact_turn: &ExactTurnSummary,
        spirit_development: bool,
        spirit_same_turn_score_setup_now: bool,
        spirit_own_mana_setup_now: bool,
    ) -> i32 {
        let mut gain = 0;
        if spirit_same_turn_score_setup_now {
            gain += 80;
        }
        if spirit_own_mana_setup_now {
            gain += 48;
        }
        if spirit_development {
            gain += 24;
        }
        if exact_turn.spirit_assisted_score {
            gain += 72;
        }
        if exact_turn.spirit_assisted_denial {
            gain += 40;
        }
        if exact_turn.spirit_assisted_supermana_progress {
            gain += 36;
        }
        if exact_turn.spirit_assisted_opponent_mana_progress {
            gain += 36;
        }
        gain += exact_turn.same_turn_score_window_value.max(0) * 20;
        if let Some(steps) = exact_turn.safe_supermana_progress_steps {
            gain += 12i32.saturating_sub(steps.clamp(0, 12)) * 4;
        }
        if let Some(steps) = exact_turn.safe_opponent_mana_progress_steps {
            gain += 12i32.saturating_sub(steps.clamp(0, 12)) * 4;
        }
        if let Some(steps) = exact_turn.score_path_best_steps {
            gain += 8i32.saturating_sub(steps.clamp(0, 8)) * 3;
        }
        gain
    }

    fn build_scored_root_move_from_transition(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        own_drainer_vulnerable_before: bool,
        inputs: Vec<Input>,
        simulated_game: MonsGame,
        events: Vec<Event>,
    ) -> Option<ScoredRootMove> {
        if crate::models::automove_deadline::checkpoint() {
            return None;
        }
        let before_state_hash = Self::search_state_hash(game);
        let simulated_state_hash = Self::search_state_hash(&simulated_game);
        let before_efficiency_snapshot = Self::move_efficiency_snapshot_with_hash(
            game,
            perspective,
            false,
            false,
            before_state_hash,
        );
        let efficiency = Self::move_efficiency_delta_from_before_snapshot(
            game,
            &simulated_game,
            perspective,
            &events,
            before_efficiency_snapshot,
            simulated_state_hash,
            true,
            true,
            true,
            false,
            false,
            config.root_backtrack_penalty,
            config.root_mana_handoff_penalty,
        );
        if crate::models::automove_deadline::checkpoint() {
            return None;
        }
        let heuristic = Self::score_state(
            &simulated_game,
            perspective,
            config.depth.saturating_sub(1),
            config.depth,
            config.scoring_weights,
        );
        if crate::models::automove_deadline::checkpoint() {
            return None;
        }
        let ordering_bonus = Self::ordering_event_bonus(game.active_color, perspective, &events);
        let attacks_opponent_drainer =
            Self::events_include_opponent_drainer_fainted(&events, perspective);
        let wins_immediately = simulated_game.winner_color() == Some(perspective);
        let spirit_development =
            Self::has_spirit_development_turn(game, &simulated_game, perspective, &events);
        let keeps_awake_spirit_on_base = Self::has_awake_spirit_on_base(&game.board, perspective)
            && Self::has_awake_spirit_on_base(&simulated_game.board, perspective);
        let scores_supermana_this_turn = Self::events_score_supermana(&events);
        let scores_opponent_mana_this_turn = Self::events_score_opponent_mana(&events, perspective);
        let allow_exact_lite_static = Self::allow_exact_lite_static_analysis(config);
        let exact_turn = if simulated_game.active_color == perspective {
            if Self::should_use_root_exact_summary_for_transition(&events, config) {
                exact_turn_summary(&simulated_game, perspective)
            } else {
                Self::approximate_active_turn_summary(
                    &simulated_game,
                    perspective,
                    allow_exact_lite_static,
                )
            }
        } else {
            ExactTurnSummary {
                ..ExactTurnSummary::default()
            }
        };
        if crate::models::automove_deadline::checkpoint() {
            return None;
        }
        let unknown_progress_steps = Config::BOARD_SIZE + 4;
        let safe_supermana_progress_steps = exact_turn
            .safe_supermana_progress_steps
            .unwrap_or(unknown_progress_steps);
        let safe_opponent_mana_progress_steps = exact_turn
            .safe_opponent_mana_progress_steps
            .unwrap_or(unknown_progress_steps);
        let unknown_score_path_steps = Config::BOARD_SIZE * 3;
        let same_turn_score_window_value = exact_turn.same_turn_score_window_value;
        let score_path_best_steps = exact_turn
            .score_path_best_steps
            .unwrap_or(unknown_score_path_steps);
        let spirit_same_turn_score_setup_now = Self::events_include_spirit_target_move(&events)
            && simulated_game.active_color == perspective
            && same_turn_score_window_value > 0;
        let spirit_own_mana_setup_now =
            Self::events_spirit_scoring_mana_setup(&events, &simulated_game.board, perspective);
        let spirit_setup_gain = Self::live_spirit_setup_gain(
            &exact_turn,
            spirit_development,
            spirit_same_turn_score_setup_now,
            spirit_own_mana_setup_now,
        );
        if crate::models::automove_deadline::checkpoint() {
            return None;
        }
        let safe_supermana_pickup_now = Self::events_pickup_supermana(&events)
            && Self::own_drainer_carries_specific_mana_safely(
                &simulated_game.board,
                perspective,
                Mana::Supermana,
            );
        let safe_opponent_mana_pickup_now = Self::events_pickup_opponent_mana(&events, perspective)
            && Self::own_drainer_carries_specific_mana_safely(
                &simulated_game.board,
                perspective,
                Mana::Regular(perspective.other()),
            );
        let supermana_progress = scores_supermana_this_turn
            || Self::events_pickup_supermana(&events)
            || Self::events_move_supermana_toward_color(&events, perspective)
            || Self::events_spirit_supermana_setup(&events, &simulated_game.board, perspective)
            || exact_turn.safe_supermana_progress
            || exact_turn.spirit_assisted_supermana_progress;
        let opponent_mana_progress = scores_opponent_mana_this_turn
            || Self::events_pickup_opponent_mana(&events, perspective)
            || Self::events_move_opponent_mana_toward_color(&events, perspective)
            || Self::events_spirit_opponent_mana_setup(&events, &simulated_game.board, perspective)
            || exact_turn.safe_opponent_mana_progress
            || exact_turn.spirit_assisted_opponent_mana_progress
            || exact_turn.spirit_assisted_denial;
        let spirit_assisted_score = exact_turn.spirit_assisted_score;
        let own_drainer_vulnerable =
            Self::is_own_drainer_vulnerable_next_turn(&simulated_game, perspective, true);
        let own_drainer_walk_vulnerable = false;
        if crate::models::automove_deadline::checkpoint() {
            return None;
        }
        let score_before = Self::score_for_color(game, perspective);
        let score_after = Self::score_for_color(&simulated_game, perspective);
        let scored_two_or_more = score_after >= score_before.saturating_add(2);
        let spent_potion = Self::events_use_potion(&events);
        let potion_compensated = wins_immediately
            || attacks_opponent_drainer
            || scored_two_or_more
            || scores_supermana_this_turn
            || scores_opponent_mana_this_turn
            || spirit_assisted_score
            || (!own_drainer_vulnerable && (supermana_progress || opponent_mana_progress));
        let potion_spend_penalty = if spent_potion && !potion_compensated {
            if config.depth >= 3 {
                config.potion_spend_penalty_normal.max(0)
            } else {
                config.potion_spend_penalty_fast.max(0)
            }
        } else {
            0
        };
        let root_compensates_handoff = wins_immediately
            || attacks_opponent_drainer
            || scores_supermana_this_turn
            || scores_opponent_mana_this_turn
            || spirit_assisted_score;
        let mana_handoff_to_opponent = !root_compensates_handoff
            && Self::mana_handoff_penalty(
                &events,
                perspective,
                config.root_mana_handoff_penalty.max(0),
            ) > 0;
        let has_roundtrip = Self::has_roundtrip_mon_move(&events);
        let classes = Self::classify_move_classes(
            game,
            &simulated_game,
            perspective,
            &events,
            own_drainer_vulnerable_before,
            own_drainer_vulnerable,
        );
        let interview_soft_priority = Self::interview_root_soft_priority(
            config,
            supermana_progress,
            opponent_mana_progress,
            safe_supermana_progress_steps,
            safe_opponent_mana_progress_steps,
            scores_supermana_this_turn,
            scores_opponent_mana_this_turn,
            own_drainer_vulnerable,
            mana_handoff_to_opponent,
            has_roundtrip,
        );

        let heuristic_with_policy = heuristic
            .saturating_add(ordering_bonus)
            .saturating_add(interview_soft_priority)
            .saturating_sub(potion_spend_penalty);
        if crate::models::automove_deadline::checkpoint() {
            return None;
        }
        Some(ScoredRootMove {
            root_rank: 0,
            inputs,
            game: simulated_game,
            heuristic: heuristic_with_policy,
            efficiency,
            wins_immediately,
            attacks_opponent_drainer,
            own_drainer_vulnerable,
            own_drainer_walk_vulnerable,
            spirit_development,
            keeps_awake_spirit_on_base,
            mana_handoff_to_opponent,
            has_roundtrip,
            scores_supermana_this_turn,
            scores_opponent_mana_this_turn,
            safe_supermana_pickup_now,
            safe_opponent_mana_pickup_now,
            safe_supermana_progress_steps,
            safe_opponent_mana_progress_steps,
            score_path_best_steps,
            same_turn_score_window_value,
            spirit_setup_gain,
            spirit_same_turn_score_setup_now,
            spirit_own_mana_setup_now,
            supermana_progress,
            opponent_mana_progress,
            interview_soft_priority,
            classes,
        })
    }

    fn spirit_base_for_color(board: &Board, color: Color) -> Location {
        board.base(Mon::new(MonKind::Spirit, color, 0))
    }

    fn is_awake_spirit_item_for_color(item: &Item, color: Color) -> bool {
        item.mon()
            .map(|mon| mon.kind == MonKind::Spirit && mon.color == color && !mon.is_fainted())
            .unwrap_or(false)
    }

    fn has_awake_spirit_on_base(board: &Board, color: Color) -> bool {
        let base = Self::spirit_base_for_color(board, color);
        board
            .item(base)
            .map(|item| Self::is_awake_spirit_item_for_color(item, color))
            .unwrap_or(false)
    }

    fn has_awake_spirit_off_base(board: &Board, color: Color) -> bool {
        let base = Self::spirit_base_for_color(board, color);
        board.occupied().any(|(location, item)| {
            location != base && Self::is_awake_spirit_item_for_color(item, color)
        })
    }

    fn has_spirit_development_turn(
        before: &MonsGame,
        after: &MonsGame,
        perspective: Color,
        events: &[Event],
    ) -> bool {
        if events
            .iter()
            .any(|event| matches!(event, Event::SpiritTargetMove { .. }))
        {
            return true;
        }

        Self::has_awake_spirit_on_base(&before.board, perspective)
            && Self::has_awake_spirit_off_base(&after.board, perspective)
    }

    fn events_score_supermana(events: &[Event]) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::ManaScored {
                    mana: Mana::Supermana,
                    ..
                }
            )
        })
    }

    fn events_score_opponent_mana(events: &[Event], perspective: Color) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::ManaScored {
                    mana: Mana::Regular(owner),
                    ..
                } if *owner == perspective.other()
            )
        })
    }

    fn events_include_spirit_target_move(events: &[Event]) -> bool {
        events
            .iter()
            .any(|event| matches!(event, Event::SpiritTargetMove { .. }))
    }

    fn events_use_potion(events: &[Event]) -> bool {
        events
            .iter()
            .any(|event| matches!(event, Event::UsePotion { .. }))
    }

    fn events_pickup_supermana(events: &[Event]) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::PickupMana {
                    mana: Mana::Supermana,
                    ..
                }
            )
        })
    }

    fn events_pickup_opponent_mana(events: &[Event], perspective: Color) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::PickupMana {
                    mana: Mana::Regular(owner),
                    ..
                } if *owner == perspective.other()
            )
        })
    }

    fn mana_move_toward_color(from: Location, to: Location, color: Color) -> bool {
        let before = Self::distance_to_color_pool_steps_for_efficiency(from, color);
        let after = Self::distance_to_color_pool_steps_for_efficiency(to, color);
        after < before
    }

    fn events_move_supermana_toward_color(events: &[Event], color: Color) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::ManaMove {
                    mana: Mana::Supermana,
                    from,
                    to
                } if Self::mana_move_toward_color(*from, *to, color)
            )
        })
    }

    fn events_move_opponent_mana_toward_color(events: &[Event], perspective: Color) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::ManaMove {
                    mana: Mana::Regular(owner),
                    from,
                    to
                } if *owner == perspective.other() && Self::mana_move_toward_color(*from, *to, perspective)
            )
        })
    }

    fn events_spirit_move_own_mana_toward_color(events: &[Event], perspective: Color) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::SpiritTargetMove {
                    item: Item::Mana {
                        mana: Mana::Regular(owner),
                    },
                    from,
                    to,
                    ..
                } if *owner == perspective && Self::mana_move_toward_color(*from, *to, perspective)
            )
        })
    }

    fn events_spirit_move_opponent_mana_toward_color(events: &[Event], perspective: Color) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::SpiritTargetMove {
                    item: Item::Mana {
                        mana: Mana::Regular(owner),
                    },
                    from,
                    to,
                    ..
                } if *owner == perspective.other()
                    && Self::mana_move_toward_color(*from, *to, perspective)
            )
        })
    }

    fn events_spirit_move_opponent_mana_onto_safe_drainer(
        events: &[Event],
        board_after: &Board,
        perspective: Color,
    ) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::SpiritTargetMove {
                    item: Item::Mana {
                        mana: Mana::Regular(owner),
                    },
                    ..
                } if *owner == perspective.other()
            )
        }) && Self::own_drainer_carries_specific_mana_safely(
            board_after,
            perspective,
            Mana::Regular(perspective.other()),
        )
    }

    fn events_spirit_move_supermana_toward_color(events: &[Event], perspective: Color) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::SpiritTargetMove {
                    item: Item::Mana {
                        mana: Mana::Supermana,
                    },
                    from,
                    to,
                    ..
                } if Self::mana_move_toward_color(*from, *to, perspective)
            )
        })
    }

    fn events_spirit_move_supermana_onto_safe_drainer(
        events: &[Event],
        board_after: &Board,
        perspective: Color,
    ) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::SpiritTargetMove {
                    item: Item::Mana {
                        mana: Mana::Supermana,
                    },
                    ..
                }
            )
        }) && Self::own_drainer_carries_specific_mana_safely(
            board_after,
            perspective,
            Mana::Supermana,
        )
    }

    fn events_spirit_supermana_setup(
        events: &[Event],
        board_after: &Board,
        perspective: Color,
    ) -> bool {
        Self::events_spirit_move_supermana_toward_color(events, perspective)
            || Self::events_spirit_move_supermana_onto_safe_drainer(
                events,
                board_after,
                perspective,
            )
    }

    fn events_spirit_opponent_mana_setup(
        events: &[Event],
        board_after: &Board,
        perspective: Color,
    ) -> bool {
        Self::events_spirit_move_opponent_mana_toward_color(events, perspective)
            || Self::events_spirit_move_opponent_mana_onto_safe_drainer(
                events,
                board_after,
                perspective,
            )
    }

    fn events_spirit_scoring_mana_setup(
        events: &[Event],
        board_after: &Board,
        perspective: Color,
    ) -> bool {
        Self::events_spirit_move_own_mana_toward_color(events, perspective)
            || Self::events_spirit_supermana_setup(events, board_after, perspective)
            || Self::events_spirit_opponent_mana_setup(events, board_after, perspective)
    }

    #[allow(clippy::too_many_arguments)]
    fn interview_root_soft_priority(
        config: AutomoveSearchConfig,
        supermana_progress: bool,
        opponent_mana_progress: bool,
        safe_supermana_progress_steps: i32,
        safe_opponent_mana_progress_steps: i32,
        scores_supermana_this_turn: bool,
        scores_opponent_mana_this_turn: bool,
        own_drainer_vulnerable: bool,
        mana_handoff_to_opponent: bool,
        has_roundtrip: bool,
    ) -> i32 {
        let mut score = 0i32;
        if scores_supermana_this_turn {
            score = score.saturating_add(config.interview_soft_supermana_score_bonus.max(0));
        } else if supermana_progress && !own_drainer_vulnerable {
            score = score
                .saturating_add(config.interview_soft_supermana_progress_bonus.max(0))
                .saturating_add(Self::root_progress_step_soft_bonus(
                    safe_supermana_progress_steps,
                    8,
                ));
        }

        if scores_opponent_mana_this_turn {
            score = score.saturating_add(config.interview_soft_opponent_mana_score_bonus.max(0));
        } else if opponent_mana_progress && !own_drainer_vulnerable {
            score = score
                .saturating_add(config.interview_soft_opponent_mana_progress_bonus.max(0))
                .saturating_add(Self::root_progress_step_soft_bonus(
                    safe_opponent_mana_progress_steps,
                    6,
                ));
        }

        if mana_handoff_to_opponent {
            score = score.saturating_sub(config.interview_soft_mana_handoff_penalty.max(0));
        }
        if has_roundtrip {
            score = score.saturating_sub(config.interview_soft_roundtrip_penalty.max(0));
        }

        score
    }

    fn root_progress_step_soft_bonus(steps: i32, per_step_bonus: i32) -> i32 {
        let unknown_steps = Config::BOARD_SIZE + 4;
        if steps >= unknown_steps || per_step_bonus <= 0 {
            return 0;
        }
        let clamped_steps = steps.clamp(0, Config::MONS_MOVES_PER_TURN);
        (Config::MONS_MOVES_PER_TURN - clamped_steps) * per_step_bonus
    }

    fn root_progress_steps_better(candidate_steps: i32, incumbent_steps: i32) -> bool {
        let unknown_steps = Config::BOARD_SIZE + 4;
        let candidate_known = candidate_steps < unknown_steps;
        let incumbent_known = incumbent_steps < unknown_steps;
        match (candidate_known, incumbent_known) {
            (true, true) => candidate_steps < incumbent_steps,
            (true, false) => true,
            _ => false,
        }
    }

    fn root_progress_or_setup_better(
        candidate: &RootEvaluation,
        incumbent: &RootEvaluation,
    ) -> bool {
        Self::root_progress_steps_better(
            candidate.safe_supermana_progress_steps,
            incumbent.safe_supermana_progress_steps,
        ) || Self::root_progress_steps_better(
            candidate.safe_opponent_mana_progress_steps,
            incumbent.safe_opponent_mana_progress_steps,
        ) || candidate.spirit_setup_gain > incumbent.spirit_setup_gain
    }

    fn root_score_path_steps_better(candidate_steps: i32, incumbent_steps: i32) -> bool {
        let unknown_steps = Config::BOARD_SIZE * 3;
        let candidate_known = candidate_steps < unknown_steps;
        let incumbent_known = incumbent_steps < unknown_steps;
        match (candidate_known, incumbent_known) {
            (true, true) => candidate_steps < incumbent_steps,
            (true, false) => true,
            _ => false,
        }
    }

    fn forced_drainer_attack_fallback_candidates_limit(config: AutomoveSearchConfig) -> usize {
        if config.depth >= 3 {
            SMART_FORCED_DRAINER_ATTACK_FALLBACK_NORMAL_CANDIDATES
        } else {
            SMART_FORCED_DRAINER_ATTACK_FALLBACK_FAST_CANDIDATES
        }
    }

    fn forced_drainer_attack_fallback_node_budget(config: AutomoveSearchConfig) -> usize {
        if config.depth >= 3 {
            SMART_FORCED_DRAINER_ATTACK_FALLBACK_NODE_BUDGET_NORMAL
        } else {
            SMART_FORCED_DRAINER_ATTACK_FALLBACK_NODE_BUDGET_FAST
        }
    }

    fn forced_drainer_attack_fallback_enum_limit(config: AutomoveSearchConfig) -> usize {
        if config.depth >= 3 {
            SMART_FORCED_DRAINER_ATTACK_FALLBACK_ENUM_LIMIT_NORMAL
        } else {
            SMART_FORCED_DRAINER_ATTACK_FALLBACK_ENUM_LIMIT_FAST
        }
    }

    fn spirit_setup_fallback_candidates_limit(config: AutomoveSearchConfig) -> usize {
        if config.depth >= 3 {
            8
        } else {
            4
        }
    }

    fn spirit_setup_fallback_enum_limit(config: AutomoveSearchConfig) -> usize {
        if config.depth >= 3 {
            256
        } else {
            128
        }
    }

    fn safe_drainer_pickup_fallback_candidates_limit(config: AutomoveSearchConfig) -> usize {
        if config.depth >= 3 {
            8
        } else {
            4
        }
    }

    fn drainer_safety_fallback_candidates_limit(config: AutomoveSearchConfig) -> usize {
        if config.depth >= 3 {
            8
        } else {
            4
        }
    }

    fn drainer_safety_fallback_enum_limit(config: AutomoveSearchConfig) -> usize {
        if config.depth >= 3 {
            192
        } else {
            96
        }
    }

    fn generic_root_fallback_enum_limit(config: AutomoveSearchConfig) -> usize {
        if config.depth >= 3 {
            24
        } else {
            12
        }
    }

    fn automove_start_input_options() -> SuggestedStartInputOptions {
        SuggestedStartInputOptions {
            include_mana_starts_with_potion_action: true,
        }
    }

    fn opponent_awake_drainer_location(board: &Board, perspective: Color) -> Option<Location> {
        board.occupied().find_map(|(location, item)| {
            let mon = item.mon()?;
            if mon.color == perspective.other() && mon.kind == MonKind::Drainer && !mon.is_fainted()
            {
                Some(location)
            } else {
                None
            }
        })
    }

    fn min_steps_to_mystic_attack_source(from: Location, target: Location) -> i32 {
        target
            .reachable_by_mystic_action_ref()
            .iter()
            .map(|source| from.distance(source))
            .min()
            .unwrap_or(i32::MAX)
    }

    fn min_steps_to_demon_attack_source(from: Location, target: Location) -> i32 {
        target
            .reachable_by_demon_action_ref()
            .iter()
            .map(|source| from.distance(source))
            .min()
            .unwrap_or(i32::MAX)
    }

    fn find_potential_drainer_attacker_locations(
        game: &MonsGame,
        perspective: Color,
    ) -> Vec<Location> {
        let mut attackers = Vec::new();

        let Some(opponent_drainer_location) =
            Self::opponent_awake_drainer_location(&game.board, perspective)
        else {
            return attackers;
        };

        let remaining_mon_moves = (Config::MONS_MOVES_PER_TURN - game.mons_moves_count).max(0);
        if remaining_mon_moves <= 0 {
            return attackers;
        }

        let opponent_drainer_guarded = Self::is_location_guarded_by_angel(
            &game.board,
            perspective.other(),
            opponent_drainer_location,
        );
        let bomb_pickup_locations = game
            .board
            .occupied()
            .filter_map(|(location, item)| match item {
                Item::Consumable {
                    consumable: Consumable::BombOrPotion,
                } => Some(location),
                _ => None,
            })
            .collect::<Vec<_>>();

        for (location, item) in game.board.occupied() {
            let Some(mon) = item.mon() else {
                continue;
            };
            if mon.color != perspective || mon.is_fainted() {
                continue;
            }

            let has_bomb = matches!(
                item,
                Item::MonWithConsumable {
                    consumable: Consumable::Bomb,
                    ..
                }
            );
            if has_bomb && location.distance(&opponent_drainer_location) <= remaining_mon_moves + 3
            {
                attackers.push(location);
                continue;
            }

            if !opponent_drainer_guarded {
                let action_distance = match mon.kind {
                    MonKind::Mystic => {
                        Self::min_steps_to_mystic_attack_source(location, opponent_drainer_location)
                    }
                    MonKind::Demon => {
                        Self::min_steps_to_demon_attack_source(location, opponent_drainer_location)
                    }
                    _ => i32::MAX,
                };
                if action_distance <= remaining_mon_moves {
                    attackers.push(location);
                    continue;
                }
            }

            for bomb_location in &bomb_pickup_locations {
                let to_bomb = location.distance(bomb_location);
                if to_bomb > remaining_mon_moves {
                    continue;
                }
                let moves_after_pickup = remaining_mon_moves - to_bomb;
                if bomb_location.distance(&opponent_drainer_location) <= moves_after_pickup + 3 {
                    attackers.push(location);
                    break;
                }
            }
        }

        attackers
    }

    fn can_attempt_forced_drainer_attack_fallback(game: &MonsGame, perspective: Color) -> bool {
        if !game.player_can_move_mon() {
            return false;
        }
        !Self::find_potential_drainer_attacker_locations(game, perspective).is_empty()
    }

    fn find_awake_spirit_locations(game: &MonsGame, perspective: Color) -> Vec<Location> {
        game.board
            .occupied()
            .filter_map(|(location, item)| {
                let mon = item.mon()?;
                (mon.color == perspective && mon.kind == MonKind::Spirit && !mon.is_fainted())
                    .then_some(location)
            })
            .collect()
    }

    fn find_awake_drainer_locations(game: &MonsGame, perspective: Color) -> Vec<Location> {
        game.board
            .occupied()
            .filter_map(|(location, item)| {
                let mon = item.mon()?;
                (mon.color == perspective && mon.kind == MonKind::Drainer && !mon.is_fainted())
                    .then_some(location)
            })
            .collect()
    }

    fn find_awake_mon_locations(game: &MonsGame, perspective: Color) -> Vec<Location> {
        game.board
            .occupied()
            .filter_map(|(location, item)| {
                let mon = item.mon()?;
                (mon.color == perspective && !mon.is_fainted()).then_some(location)
            })
            .collect()
    }

    fn collect_targeted_spirit_setup_inputs(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        max_candidates: usize,
    ) -> Vec<LegalInputTransition> {
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        let spirit_locations = Self::find_awake_spirit_locations(game, perspective);
        if spirit_locations.is_empty() || !game.player_can_use_action() {
            return Vec::new();
        }

        let start_options = Self::automove_start_input_options();
        let per_spirit_enum_limit = Self::spirit_setup_fallback_enum_limit(config);
        let mut setup_inputs = Vec::new();

        for spirit_loc in spirit_locations {
            if crate::models::automove_deadline::checkpoint() {
                return Vec::new();
            }
            if setup_inputs.len() >= max_candidates.max(1) {
                break;
            }

            let mut spirit_transitions = Vec::new();
            let mut partial_inputs = vec![Input::Location(spirit_loc)];
            let mut simulated_game = game.clone_for_simulation();
            Self::collect_legal_transitions(
                &mut simulated_game,
                &mut partial_inputs,
                &mut spirit_transitions,
                per_spirit_enum_limit,
                start_options,
            );
            if crate::models::automove_deadline::cancelled() {
                return Vec::new();
            }
            spirit_transitions.sort_by(|a, b| a.inputs.cmp(&b.inputs));

            for transition in spirit_transitions {
                if crate::models::automove_deadline::checkpoint() {
                    return Vec::new();
                }
                if setup_inputs.len() >= max_candidates.max(1) {
                    break;
                }
                if !Self::events_spirit_scoring_mana_setup(
                    &transition.events,
                    &transition.game.board,
                    perspective,
                ) {
                    continue;
                }
                setup_inputs.push(transition);
            }
        }

        setup_inputs
    }

    fn collect_targeted_drainer_safety_inputs(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        max_candidates: usize,
    ) -> Vec<LegalInputTransition> {
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        let actor_locations = Self::find_awake_mon_locations(game, perspective);
        if actor_locations.is_empty() {
            return Vec::new();
        }

        let start_options = Self::automove_start_input_options();
        let per_actor_enum_limit = Self::drainer_safety_fallback_enum_limit(config);
        let mut safety_inputs = Vec::new();
        let mut seen_inputs = std::collections::HashSet::new();

        for actor_loc in actor_locations {
            if crate::models::automove_deadline::checkpoint() {
                return Vec::new();
            }
            if safety_inputs.len() >= max_candidates.max(1) {
                break;
            }

            let mut actor_transitions = Vec::new();
            let mut partial_inputs = vec![Input::Location(actor_loc)];
            let mut simulated_game = game.clone_for_simulation();
            Self::collect_legal_transitions(
                &mut simulated_game,
                &mut partial_inputs,
                &mut actor_transitions,
                per_actor_enum_limit,
                start_options,
            );
            if crate::models::automove_deadline::cancelled() {
                return Vec::new();
            }
            actor_transitions.sort_by(|a, b| a.inputs.cmp(&b.inputs));

            for transition in actor_transitions {
                if crate::models::automove_deadline::checkpoint() {
                    return Vec::new();
                }
                if safety_inputs.len() >= max_candidates.max(1) {
                    break;
                }
                if Self::is_own_drainer_vulnerable_next_turn(&transition.game, perspective, true) {
                    continue;
                }
                if seen_inputs.insert(transition.inputs.clone()) {
                    safety_inputs.push(transition);
                }
            }
        }

        safety_inputs.sort_by(|a, b| a.inputs.cmp(&b.inputs));
        safety_inputs
    }

    fn collect_targeted_safe_drainer_pickup_inputs(
        game: &MonsGame,
        perspective: Color,
        max_candidates: usize,
        wanted_mana: Mana,
    ) -> Vec<LegalInputTransition> {
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        let drainer_locations = Self::find_awake_drainer_locations(game, perspective);
        if drainer_locations.is_empty() || !game.player_can_move_mon() {
            return Vec::new();
        }

        let mut pickup_inputs = Vec::new();
        let mut seen_inputs = std::collections::HashSet::new();

        for drainer_loc in drainer_locations {
            if crate::models::automove_deadline::checkpoint() {
                return Vec::new();
            }
            if pickup_inputs.len() >= max_candidates.max(1) {
                break;
            }

            let Some(path) =
                exact_secure_specific_mana_path_from(game, perspective, drainer_loc, wanted_mana)
            else {
                continue;
            };
            if crate::models::automove_deadline::cancelled() {
                return Vec::new();
            }
            if path.is_empty() {
                continue;
            }

            let mut inputs = Vec::with_capacity(path.len() + 1);
            inputs.push(Input::Location(drainer_loc));
            inputs.extend(path.into_iter().map(Input::Location));
            let Some((after_game, events)) =
                Self::apply_inputs_for_search_with_events(game, &inputs)
            else {
                continue;
            };
            let picked_wanted_mana = match wanted_mana {
                Mana::Supermana => Self::events_pickup_supermana(&events),
                Mana::Regular(owner) if owner == perspective.other() => {
                    Self::events_pickup_opponent_mana(&events, perspective)
                }
                _ => false,
            };
            if picked_wanted_mana
                && (Self::own_drainer_carries_specific_mana_safely(
                    &after_game.board,
                    perspective,
                    wanted_mana,
                ) || match wanted_mana {
                    Mana::Supermana => Self::events_score_supermana(&events),
                    Mana::Regular(owner) if owner == perspective.other() => {
                        Self::events_score_opponent_mana(&events, perspective)
                    }
                    _ => false,
                })
                && seen_inputs.insert(inputs.clone())
            {
                pickup_inputs.push(LegalInputTransition {
                    inputs,
                    game: after_game,
                    events,
                });
            }
        }

        pickup_inputs.sort_by(|a, b| a.inputs.cmp(&b.inputs));
        pickup_inputs
    }

    fn collect_targeted_drainer_attack_inputs(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        max_candidates: usize,
    ) -> Vec<LegalInputTransition> {
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        let attacker_locations = Self::find_potential_drainer_attacker_locations(game, perspective);
        if attacker_locations.is_empty() {
            return Vec::new();
        }
        let mut attacker_mask = [false; BOARD_CELLS];
        for attacker_location in attacker_locations {
            attacker_mask[attacker_location.index()] = true;
        }

        let mut memo_true = U64HashSet::default();
        let mut attack_inputs = Vec::new();
        let base_budget = Self::forced_drainer_attack_fallback_node_budget(config);
        let mut continuation_budget = base_budget * 2;
        let base_enum = Self::forced_drainer_attack_fallback_enum_limit(config);
        let enum_limit = base_enum * 2;
        let start_options = Self::automove_start_input_options();

        let root_transitions = Self::enumerate_legal_transitions_lexicographic_bounded(
            game,
            enum_limit,
            start_options,
            Some(&attacker_mask),
        );
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }

        for transition in root_transitions {
            if crate::models::automove_deadline::checkpoint() {
                return Vec::new();
            }
            if attack_inputs.len() >= max_candidates.max(1) {
                break;
            }

            if Self::events_include_opponent_drainer_fainted(&transition.events, perspective) {
                attack_inputs.push(transition);
                continue;
            }
            if transition.game.active_color != perspective {
                continue;
            }
            if Self::can_attack_opponent_drainer_before_turn_ends(
                &transition.game,
                perspective,
                enum_limit,
                start_options,
                &mut continuation_budget,
                &mut memo_true,
            ) {
                attack_inputs.push(transition);
            }
        }

        attack_inputs
    }

    fn collect_forced_drainer_attack_inputs(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        max_candidates: usize,
    ) -> Vec<LegalInputTransition> {
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        let mut memo_true = U64HashSet::default();
        let mut attack_inputs = Vec::new();
        let mut continuation_budget = Self::forced_drainer_attack_fallback_node_budget(config);
        let enum_limit = Self::forced_drainer_attack_fallback_enum_limit(config);
        let start_options = Self::automove_start_input_options();
        let root_transitions = Self::enumerate_legal_transitions_lexicographic_bounded(
            game,
            enum_limit,
            start_options,
            None,
        );
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        for transition in root_transitions {
            if crate::models::automove_deadline::checkpoint() {
                return Vec::new();
            }
            if attack_inputs.len() >= max_candidates.max(1) {
                break;
            }

            if Self::events_include_opponent_drainer_fainted(&transition.events, perspective) {
                attack_inputs.push(transition);
                continue;
            }
            if transition.game.active_color != perspective {
                continue;
            }
            if Self::can_attack_opponent_drainer_before_turn_ends(
                &transition.game,
                perspective,
                enum_limit,
                start_options,
                &mut continuation_budget,
                &mut memo_true,
            ) {
                attack_inputs.push(transition);
            }
        }

        attack_inputs
    }

    fn can_attack_opponent_drainer_before_turn_ends(
        game: &MonsGame,
        perspective: Color,
        _enum_limit: usize,
        _start_options: SuggestedStartInputOptions,
        continuation_budget: &mut usize,
        memo_true: &mut U64HashSet,
    ) -> bool {
        if crate::models::automove_deadline::checkpoint() {
            return false;
        }
        if game.active_color != perspective || *continuation_budget == 0 {
            return false;
        }
        let state_hash = Self::search_state_hash(game);
        if memo_true.contains(&state_hash) {
            return true;
        }
        *continuation_budget = continuation_budget.saturating_sub(1);
        let can_attack = can_attack_opponent_drainer_this_turn(game, perspective);
        if can_attack && crate::models::automove_deadline::cache_write_allowed() {
            memo_true.insert(state_hash);
        }
        can_attack
    }

    fn ranked_root_moves(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Vec<ScoredRootMove> {
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        let mut candidates = Vec::new();
        let own_drainer_vulnerable_before =
            Self::is_own_drainer_vulnerable_next_turn(game, perspective, true);

        let start_options = Self::automove_start_input_options();
        let mut root_transitions =
            Self::enumerate_legal_transitions(game, config.root_enum_limit, start_options);

        if own_drainer_vulnerable_before
            && !root_transitions.iter().any(|transition| {
                !Self::is_own_drainer_vulnerable_next_turn(&transition.game, perspective, true)
            })
        {
            let fallback_limit = Self::drainer_safety_fallback_candidates_limit(config);
            let fallback_inputs = Self::collect_targeted_drainer_safety_inputs(
                game,
                perspective,
                config,
                fallback_limit,
            );
            if !fallback_inputs.is_empty() {
                let mut seen_inputs = root_transitions
                    .iter()
                    .map(|transition| transition.inputs.clone())
                    .collect::<std::collections::HashSet<_>>();
                for transition in fallback_inputs {
                    if seen_inputs.insert(transition.inputs.clone()) {
                        root_transitions.push(transition);
                    }
                }
            }
        }
        if crate::models::automove_deadline::cancelled() {
            return Vec::new();
        }
        if crate::models::automove_deadline::cancelled() {
            return Vec::new();
        }

        let exact_turn_before = Self::approximate_active_turn_summary(game, perspective, false);
        let exact_spirit_setup_gain_before =
            Self::live_spirit_setup_gain(&exact_turn_before, false, false, false);
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        if exact_turn_before.safe_supermana_progress
            && !root_transitions.iter().any(|transition| {
                Self::events_pickup_supermana(&transition.events)
                    && Self::own_drainer_carries_specific_mana_safely(
                        &transition.game.board,
                        perspective,
                        Mana::Supermana,
                    )
            })
        {
            let fallback_limit = Self::safe_drainer_pickup_fallback_candidates_limit(config);
            let fallback_inputs = Self::collect_targeted_safe_drainer_pickup_inputs(
                game,
                perspective,
                fallback_limit,
                Mana::Supermana,
            );
            if !fallback_inputs.is_empty() {
                let mut seen_inputs = root_transitions
                    .iter()
                    .map(|transition| transition.inputs.clone())
                    .collect::<std::collections::HashSet<_>>();
                for transition in fallback_inputs {
                    if seen_inputs.insert(transition.inputs.clone()) {
                        root_transitions.push(transition);
                    }
                }
            }
        }
        if crate::models::automove_deadline::cancelled() {
            return Vec::new();
        }
        if exact_turn_before.safe_opponent_mana_progress
            && !root_transitions.iter().any(|transition| {
                Self::events_pickup_opponent_mana(&transition.events, perspective)
                    && Self::own_drainer_carries_specific_mana_safely(
                        &transition.game.board,
                        perspective,
                        Mana::Regular(perspective.other()),
                    )
            })
        {
            let fallback_limit = Self::safe_drainer_pickup_fallback_candidates_limit(config);
            let fallback_inputs = Self::collect_targeted_safe_drainer_pickup_inputs(
                game,
                perspective,
                fallback_limit,
                Mana::Regular(perspective.other()),
            );
            if !fallback_inputs.is_empty() {
                let mut seen_inputs = root_transitions
                    .iter()
                    .map(|transition| transition.inputs.clone())
                    .collect::<std::collections::HashSet<_>>();
                for transition in fallback_inputs {
                    if seen_inputs.insert(transition.inputs.clone()) {
                        root_transitions.push(transition);
                    }
                }
            }
        }
        if crate::models::automove_deadline::cancelled() {
            return Vec::new();
        }
        if (config.enable_interview_hard_spirit_deploy
            || config.enable_root_spirit_development_pref)
            && (Self::should_prefer_spirit_development(game, perspective)
                || exact_spirit_setup_gain_before > 0
                || exact_turn_before.spirit_assisted_supermana_progress
                || exact_turn_before.spirit_assisted_opponent_mana_progress)
            && !root_transitions.iter().any(|transition| {
                Self::events_spirit_scoring_mana_setup(
                    &transition.events,
                    &transition.game.board,
                    perspective,
                )
            })
        {
            let fallback_limit = Self::spirit_setup_fallback_candidates_limit(config);
            let fallback_inputs = Self::collect_targeted_spirit_setup_inputs(
                game,
                perspective,
                config,
                fallback_limit,
            );
            if !fallback_inputs.is_empty() {
                let mut seen_inputs = root_transitions
                    .iter()
                    .map(|transition| transition.inputs.clone())
                    .collect::<std::collections::HashSet<_>>();
                for transition in fallback_inputs {
                    if seen_inputs.insert(transition.inputs.clone()) {
                        root_transitions.push(transition);
                    }
                }
            }
        }
        if crate::models::automove_deadline::cancelled() {
            return Vec::new();
        }

        let mut remaining_exact_lite_root_calls = config.exact_lite_root_call_budget;
        let mut remaining_exact_lite_static_calls = config.exact_lite_static_call_budget;
        for transition in root_transitions {
            if crate::models::automove_deadline::checkpoint() {
                return Vec::new();
            }
            let transition_config = Self::with_exact_lite_budgeted_transition_config(
                config,
                perspective,
                &transition.game,
                &transition.events,
                &mut remaining_exact_lite_root_calls,
                &mut remaining_exact_lite_static_calls,
            );
            let candidate = Self::build_scored_root_move_from_transition(
                game,
                perspective,
                transition_config,
                own_drainer_vulnerable_before,
                transition.inputs,
                transition.game,
                transition.events,
            );
            if crate::models::automove_deadline::cancelled() {
                return Vec::new();
            }
            if let Some(candidate) = candidate {
                candidates.push(candidate);
            }
        }

        if candidates.is_empty() {
            let fallback_limit = Self::generic_root_fallback_enum_limit(config);
            let fallback_transitions =
                Self::enumerate_legal_transitions(game, fallback_limit, start_options);
            if crate::models::automove_deadline::cancelled() {
                return Vec::new();
            }
            for transition in fallback_transitions {
                if crate::models::automove_deadline::checkpoint() {
                    return Vec::new();
                }
                let transition_config = Self::with_exact_lite_budgeted_transition_config(
                    config,
                    perspective,
                    &transition.game,
                    &transition.events,
                    &mut remaining_exact_lite_root_calls,
                    &mut remaining_exact_lite_static_calls,
                );
                let candidate = Self::build_scored_root_move_from_transition(
                    game,
                    perspective,
                    transition_config,
                    own_drainer_vulnerable_before,
                    transition.inputs,
                    transition.game,
                    transition.events,
                );
                if crate::models::automove_deadline::cancelled() {
                    return Vec::new();
                }
                if let Some(candidate) = candidate {
                    candidates.push(candidate);
                }
            }
        }

        Self::sort_root_candidates_by_search_priority(candidates.as_mut_slice());
        let mut has_winning_candidate = candidates
            .iter()
            .any(|candidate| candidate.wins_immediately);
        let mut forced_turn_attack_inputs: Option<std::collections::HashSet<Vec<Input>>> = None;
        if !has_winning_candidate
            && !candidates
                .iter()
                .any(|candidate| candidate.attacks_opponent_drainer)
            && Self::can_attempt_forced_drainer_attack_fallback(game, perspective)
        {
            let fallback_limit = Self::forced_drainer_attack_fallback_candidates_limit(config);
            let fallback_inputs = if config.enable_targeted_drainer_attack_fallback {
                Self::collect_targeted_drainer_attack_inputs(
                    game,
                    perspective,
                    config,
                    fallback_limit,
                )
            } else {
                Self::collect_forced_drainer_attack_inputs(
                    game,
                    perspective,
                    config,
                    fallback_limit,
                )
            };
            if crate::models::automove_deadline::cancelled() {
                return Vec::new();
            }

            if !fallback_inputs.is_empty() {
                let forced_inputs = fallback_inputs
                    .iter()
                    .map(|transition| transition.inputs.clone())
                    .collect::<std::collections::HashSet<_>>();
                let mut seen_inputs = candidates
                    .iter()
                    .map(|candidate| candidate.inputs.clone())
                    .collect::<std::collections::HashSet<_>>();

                for transition in fallback_inputs {
                    if crate::models::automove_deadline::checkpoint() {
                        return Vec::new();
                    }
                    if !seen_inputs.insert(transition.inputs.clone()) {
                        continue;
                    }
                    let transition_config = Self::with_exact_lite_budgeted_transition_config(
                        config,
                        perspective,
                        &transition.game,
                        &transition.events,
                        &mut remaining_exact_lite_root_calls,
                        &mut remaining_exact_lite_static_calls,
                    );
                    let candidate = Self::build_scored_root_move_from_transition(
                        game,
                        perspective,
                        transition_config,
                        own_drainer_vulnerable_before,
                        transition.inputs,
                        transition.game,
                        transition.events,
                    );
                    if crate::models::automove_deadline::cancelled() {
                        return Vec::new();
                    }
                    if let Some(candidate) = candidate {
                        candidates.push(candidate);
                    }
                }

                Self::sort_root_candidates_by_search_priority(candidates.as_mut_slice());
                has_winning_candidate = candidates
                    .iter()
                    .any(|candidate| candidate.wins_immediately);
                forced_turn_attack_inputs = Some(forced_inputs);
            }
        }

        if !has_winning_candidate
            && candidates
                .iter()
                .any(|candidate| candidate.attacks_opponent_drainer)
        {
            candidates.retain(|candidate| candidate.attacks_opponent_drainer);
        } else {
            if let Some(forced_inputs) = forced_turn_attack_inputs {
                candidates.retain(|candidate| forced_inputs.contains(&candidate.inputs));
            }
        }
        if candidates.len() > config.root_branch_limit {
            candidates = Self::truncate_root_candidates_with_class_coverage(
                candidates,
                config.root_branch_limit,
                true,
            );
        }
        for (root_rank, candidate) in candidates.iter_mut().enumerate() {
            candidate.root_rank = root_rank;
        }
        if crate::models::automove_deadline::checkpoint() {
            Vec::new()
        } else {
            candidates
        }
    }

    fn is_better_tactical_root_candidate(
        candidate: &ScoredRootMove,
        incumbent: &ScoredRootMove,
    ) -> bool {
        if candidate.wins_immediately != incumbent.wins_immediately {
            return candidate.wins_immediately;
        }
        if candidate.attacks_opponent_drainer != incumbent.attacks_opponent_drainer {
            return candidate.attacks_opponent_drainer;
        }
        if candidate.own_drainer_vulnerable != incumbent.own_drainer_vulnerable {
            return !candidate.own_drainer_vulnerable;
        }
        if candidate.classes.immediate_score != incumbent.classes.immediate_score {
            return candidate.classes.immediate_score;
        }
        if candidate.scores_supermana_this_turn != incumbent.scores_supermana_this_turn {
            return candidate.scores_supermana_this_turn;
        }
        if candidate.scores_opponent_mana_this_turn != incumbent.scores_opponent_mana_this_turn {
            return candidate.scores_opponent_mana_this_turn;
        }
        if candidate.safe_supermana_pickup_now != incumbent.safe_supermana_pickup_now {
            return candidate.safe_supermana_pickup_now;
        }
        if candidate.safe_opponent_mana_pickup_now != incumbent.safe_opponent_mana_pickup_now {
            return candidate.safe_opponent_mana_pickup_now;
        }
        if candidate.same_turn_score_window_value != incumbent.same_turn_score_window_value {
            return candidate.same_turn_score_window_value > incumbent.same_turn_score_window_value;
        }
        if candidate.spirit_same_turn_score_setup_now != incumbent.spirit_same_turn_score_setup_now
        {
            return candidate.spirit_same_turn_score_setup_now;
        }
        if candidate.spirit_own_mana_setup_now != incumbent.spirit_own_mana_setup_now {
            return candidate.spirit_own_mana_setup_now;
        }
        if candidate.spirit_own_mana_setup_now
            && incumbent.spirit_own_mana_setup_now
            && candidate.supermana_progress
            && incumbent.supermana_progress
            && candidate.safe_supermana_progress_steps != incumbent.safe_supermana_progress_steps
        {
            return Self::root_progress_steps_better(
                candidate.safe_supermana_progress_steps,
                incumbent.safe_supermana_progress_steps,
            );
        }
        if candidate.spirit_own_mana_setup_now
            && incumbent.spirit_own_mana_setup_now
            && candidate.opponent_mana_progress
            && incumbent.opponent_mana_progress
            && candidate.safe_opponent_mana_progress_steps
                != incumbent.safe_opponent_mana_progress_steps
        {
            return Self::root_progress_steps_better(
                candidate.safe_opponent_mana_progress_steps,
                incumbent.safe_opponent_mana_progress_steps,
            );
        }
        if candidate.spirit_own_mana_setup_now
            && incumbent.spirit_own_mana_setup_now
            && candidate.score_path_best_steps != incumbent.score_path_best_steps
        {
            return Self::root_score_path_steps_better(
                candidate.score_path_best_steps,
                incumbent.score_path_best_steps,
            );
        }
        if candidate.supermana_progress != incumbent.supermana_progress {
            return candidate.supermana_progress;
        }
        if candidate.supermana_progress
            && incumbent.supermana_progress
            && candidate.safe_supermana_progress_steps != incumbent.safe_supermana_progress_steps
        {
            return Self::root_progress_steps_better(
                candidate.safe_supermana_progress_steps,
                incumbent.safe_supermana_progress_steps,
            );
        }
        if candidate.opponent_mana_progress != incumbent.opponent_mana_progress {
            return candidate.opponent_mana_progress;
        }
        if candidate.opponent_mana_progress
            && incumbent.opponent_mana_progress
            && candidate.safe_opponent_mana_progress_steps
                != incumbent.safe_opponent_mana_progress_steps
        {
            return Self::root_progress_steps_better(
                candidate.safe_opponent_mana_progress_steps,
                incumbent.safe_opponent_mana_progress_steps,
            );
        }
        if candidate.mana_handoff_to_opponent != incumbent.mana_handoff_to_opponent {
            return !candidate.mana_handoff_to_opponent;
        }
        if candidate.has_roundtrip != incumbent.has_roundtrip {
            return !candidate.has_roundtrip;
        }
        if candidate.spirit_development != incumbent.spirit_development {
            return candidate.spirit_development;
        }
        if candidate.efficiency != incumbent.efficiency {
            return candidate.efficiency > incumbent.efficiency;
        }
        if candidate.heuristic != incumbent.heuristic {
            return candidate.heuristic > incumbent.heuristic;
        }
        false
    }

    fn compare_tactical_root_candidates(
        candidate: &ScoredRootMove,
        incumbent: &ScoredRootMove,
    ) -> std::cmp::Ordering {
        if Self::is_better_tactical_root_candidate(candidate, incumbent) {
            std::cmp::Ordering::Less
        } else if Self::is_better_tactical_root_candidate(incumbent, candidate) {
            std::cmp::Ordering::Greater
        } else {
            std::cmp::Ordering::Equal
        }
    }

    fn sort_root_candidates_by_search_priority(candidates: &mut [ScoredRootMove]) {
        candidates.sort_by(|a, b| {
            b.heuristic
                .cmp(&a.heuristic)
                .then_with(|| Self::compare_tactical_root_candidates(a, b))
                .then_with(|| a.inputs.cmp(&b.inputs))
        });
    }

    fn child_class_priority_score(classes: MoveClassFlags) -> i32 {
        let mut score = 0;
        if classes.immediate_score {
            score += 1_000;
        }
        if classes.drainer_attack {
            score += 700;
        }
        if classes.drainer_safety_recover {
            score += 500;
        }
        if classes.carrier_progress {
            score += 220;
        }
        if classes.material {
            score += 80;
        }
        score
    }

    fn compare_ranked_child_entries(
        a: &(i32, RankedChildState),
        b: &(i32, RankedChildState),
        maximizing: bool,
    ) -> std::cmp::Ordering {
        let heuristic_cmp = if maximizing {
            b.0.cmp(&a.0)
        } else {
            a.0.cmp(&b.0)
        };
        heuristic_cmp
            .then_with(|| b.1.ordering_efficiency.cmp(&a.1.ordering_efficiency))
            .then_with(|| {
                Self::child_class_priority_score(b.1.classes)
                    .cmp(&Self::child_class_priority_score(a.1.classes))
            })
            .then_with(|| b.1.hash.cmp(&a.1.hash))
    }

    fn is_child_search_priority_class(classes: MoveClassFlags) -> bool {
        classes.is_tactical_priority() || classes.carrier_progress
    }

    fn is_exact_child_continuation_candidate(
        ordering_efficiency: i32,
        classes: MoveClassFlags,
    ) -> bool {
        ordering_efficiency > 0 && !classes.material
    }

    fn is_child_search_priority_candidate(state: &RankedChildState) -> bool {
        Self::is_child_search_priority_class(state.classes)
            || Self::is_exact_child_continuation_candidate(state.ordering_efficiency, state.classes)
    }

    fn is_quiet_reduction_candidate(
        ordering_efficiency: i32,
        tactical_extension_trigger: bool,
        classes: MoveClassFlags,
    ) -> bool {
        !classes.material
            && ordering_efficiency <= 0
            && !tactical_extension_trigger
            && !classes.is_tactical_priority()
            && !classes.carrier_progress
    }

    fn is_selective_extension_candidate(
        tactical_extension_trigger: bool,
        ordering_efficiency: i32,
        classes: MoveClassFlags,
    ) -> bool {
        tactical_extension_trigger
            || (ordering_efficiency > 0 && !classes.quiet && !classes.material)
    }

    fn child_score_within_coverage_margin(
        score: i32,
        reference_score: i32,
        maximizing: bool,
    ) -> bool {
        let margin = SMART_MOVE_CLASS_CHILD_SCORE_MARGIN.max(0);
        if maximizing {
            score.saturating_add(margin) >= reference_score
        } else {
            score <= reference_score.saturating_add(margin)
        }
    }

    fn truncate_child_states_with_coverage(
        scored_states: Vec<(i32, RankedChildState)>,
        limit: usize,
        maximizing: bool,
        strict_guarantees: bool,
    ) -> Vec<(i32, RankedChildState)> {
        if scored_states.len() <= limit || limit == 0 {
            return scored_states;
        }

        let cutoff_score = scored_states[limit - 1].0;
        let preserve_index =
            scored_states
                .iter()
                .enumerate()
                .skip(limit)
                .find_map(|(index, (score, state))| {
                    if !Self::is_child_search_priority_candidate(state) {
                        return None;
                    }
                    if strict_guarantees
                        || Self::child_score_within_coverage_margin(
                            *score,
                            cutoff_score,
                            maximizing,
                        )
                    {
                        Some(index)
                    } else {
                        None
                    }
                });

        let Some(preserve_index) = preserve_index else {
            return scored_states.into_iter().take(limit).collect();
        };

        let mut selected = vec![false; scored_states.len()];
        selected[preserve_index] = true;
        let mut selected_count = 1usize;
        for selected_entry in selected.iter_mut() {
            if selected_count >= limit {
                break;
            }
            if *selected_entry {
                continue;
            }
            *selected_entry = true;
            selected_count += 1;
        }

        scored_states
            .into_iter()
            .enumerate()
            .filter_map(|(index, entry)| selected[index].then_some(entry))
            .collect()
    }

    fn approximate_can_attack_opponent_drainer_this_turn(game: &MonsGame, color: Color) -> bool {
        game.active_color == color
            && game.player_can_use_action()
            && !Self::find_potential_drainer_attacker_locations(game, color).is_empty()
    }

    fn has_frontier_tactical_potential(game: &MonsGame) -> bool {
        let active_color = game.active_color;
        let turn_summary = Self::approximate_active_turn_summary(game, active_color, false);
        turn_summary.same_turn_score_window_value > 0
            || Self::approximate_can_attack_opponent_drainer_this_turn(game, active_color)
            || turn_summary.safe_supermana_progress
            || turn_summary.safe_opponent_mana_progress
    }

    fn compare_ranked_root_indices(
        root_moves: &[ScoredRootMove],
        a: (usize, i32),
        b: (usize, i32),
    ) -> std::cmp::Ordering {
        b.1.cmp(&a.1)
            .then_with(|| {
                Self::compare_tactical_root_candidates(&root_moves[a.0], &root_moves[b.0])
            })
            .then_with(|| a.0.cmp(&b.0))
    }

    fn root_scout_progress_bonus(candidate: &ScoredRootMove) -> i32 {
        let mut bonus = 0i32;

        if candidate.supermana_progress
            && !candidate.scores_supermana_this_turn
            && !candidate.safe_supermana_pickup_now
            && !candidate.spirit_same_turn_score_setup_now
            && !candidate.spirit_own_mana_setup_now
        {
            bonus = bonus
                .saturating_add(520)
                .saturating_add(Self::root_progress_step_soft_bonus(
                    candidate.safe_supermana_progress_steps,
                    48,
                ));
        }

        if candidate.opponent_mana_progress
            && !candidate.scores_opponent_mana_this_turn
            && !candidate.safe_opponent_mana_pickup_now
            && !candidate.spirit_same_turn_score_setup_now
            && !candidate.spirit_own_mana_setup_now
        {
            bonus = bonus
                .saturating_add(480)
                .saturating_add(Self::root_progress_step_soft_bonus(
                    candidate.safe_opponent_mana_progress_steps,
                    40,
                ));
        }

        bonus
    }

    fn root_focus_scout_score(candidate: &ScoredRootMove) -> i32 {
        candidate
            .heuristic
            .saturating_add(candidate.efficiency / 2)
            .saturating_add(Self::root_scout_progress_bonus(candidate))
    }

    fn reorder_root_moves_by_ranked_indices(
        root_moves: Vec<ScoredRootMove>,
        ranked_indices: &[(usize, i32)],
    ) -> Vec<ScoredRootMove> {
        let mut owned_root_moves = root_moves.into_iter().map(Some).collect::<Vec<_>>();
        let mut ordered_root_moves = Vec::with_capacity(owned_root_moves.len());
        for (index, _) in ranked_indices {
            if let Some(candidate) = owned_root_moves[*index].take() {
                ordered_root_moves.push(candidate);
            }
        }
        ordered_root_moves
    }

    fn prioritize_root_inputs(
        mut root_moves: Vec<ScoredRootMove>,
        priority_inputs: Option<&[Vec<Input>]>,
        forced_inputs: Option<&[Input]>,
    ) -> Vec<ScoredRootMove> {
        let mut ordered_inputs = Vec::<Vec<Input>>::new();
        if let Some(forced_inputs) = forced_inputs {
            ordered_inputs.push(forced_inputs.to_vec());
        }
        if let Some(priority_inputs) = priority_inputs {
            for inputs in priority_inputs {
                if !ordered_inputs.contains(inputs) {
                    ordered_inputs.push(inputs.clone());
                }
            }
        }
        if ordered_inputs.is_empty() {
            return root_moves;
        }

        let mut insert_at = 0usize;
        for inputs in ordered_inputs {
            let Some(index) = root_moves
                .iter()
                .position(|candidate| candidate.inputs == inputs)
            else {
                continue;
            };
            if index == insert_at {
                insert_at += 1;
                continue;
            }
            let prioritized = root_moves.remove(index);
            root_moves.insert(insert_at, prioritized);
            insert_at += 1;
        }

        root_moves
    }

    #[cfg(test)]
    fn sort_root_moves_by_ranked_scores(
        root_moves: Vec<ScoredRootMove>,
        scores: &[i32],
    ) -> Vec<ScoredRootMove> {
        debug_assert_eq!(root_moves.len(), scores.len());
        let mut ranked_indices = scores.iter().copied().enumerate().collect::<Vec<_>>();
        ranked_indices.sort_by(|a, b| Self::compare_ranked_root_indices(&root_moves, *a, *b));
        Self::reorder_root_moves_by_ranked_indices(root_moves, ranked_indices.as_slice())
    }

    fn compare_tactical_root_evaluations(
        candidate: &RootEvaluation,
        incumbent: &RootEvaluation,
    ) -> std::cmp::Ordering {
        if candidate.wins_immediately != incumbent.wins_immediately {
            return if candidate.wins_immediately {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.attacks_opponent_drainer != incumbent.attacks_opponent_drainer {
            return if candidate.attacks_opponent_drainer {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.own_drainer_vulnerable != incumbent.own_drainer_vulnerable {
            return if !candidate.own_drainer_vulnerable {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.classes.immediate_score != incumbent.classes.immediate_score {
            return if candidate.classes.immediate_score {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.scores_supermana_this_turn != incumbent.scores_supermana_this_turn {
            return if candidate.scores_supermana_this_turn {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.scores_opponent_mana_this_turn != incumbent.scores_opponent_mana_this_turn {
            return if candidate.scores_opponent_mana_this_turn {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.safe_supermana_pickup_now != incumbent.safe_supermana_pickup_now {
            return if candidate.safe_supermana_pickup_now {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.safe_opponent_mana_pickup_now != incumbent.safe_opponent_mana_pickup_now {
            return if candidate.safe_opponent_mana_pickup_now {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.same_turn_score_window_value != incumbent.same_turn_score_window_value {
            return incumbent
                .same_turn_score_window_value
                .cmp(&candidate.same_turn_score_window_value);
        }
        if candidate.spirit_same_turn_score_setup_now != incumbent.spirit_same_turn_score_setup_now
        {
            return if candidate.spirit_same_turn_score_setup_now {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.spirit_own_mana_setup_now != incumbent.spirit_own_mana_setup_now {
            return if candidate.spirit_own_mana_setup_now {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.spirit_own_mana_setup_now
            && incumbent.spirit_own_mana_setup_now
            && candidate.supermana_progress
            && incumbent.supermana_progress
            && candidate.safe_supermana_progress_steps != incumbent.safe_supermana_progress_steps
        {
            return if Self::root_progress_steps_better(
                candidate.safe_supermana_progress_steps,
                incumbent.safe_supermana_progress_steps,
            ) {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.spirit_own_mana_setup_now
            && incumbent.spirit_own_mana_setup_now
            && candidate.opponent_mana_progress
            && incumbent.opponent_mana_progress
            && candidate.safe_opponent_mana_progress_steps
                != incumbent.safe_opponent_mana_progress_steps
        {
            return if Self::root_progress_steps_better(
                candidate.safe_opponent_mana_progress_steps,
                incumbent.safe_opponent_mana_progress_steps,
            ) {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.spirit_own_mana_setup_now
            && incumbent.spirit_own_mana_setup_now
            && candidate.score_path_best_steps != incumbent.score_path_best_steps
        {
            return if Self::root_score_path_steps_better(
                candidate.score_path_best_steps,
                incumbent.score_path_best_steps,
            ) {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.supermana_progress != incumbent.supermana_progress {
            return if candidate.supermana_progress {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.supermana_progress
            && incumbent.supermana_progress
            && candidate.safe_supermana_progress_steps != incumbent.safe_supermana_progress_steps
        {
            return if Self::root_progress_steps_better(
                candidate.safe_supermana_progress_steps,
                incumbent.safe_supermana_progress_steps,
            ) {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.opponent_mana_progress != incumbent.opponent_mana_progress {
            return if candidate.opponent_mana_progress {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.opponent_mana_progress
            && incumbent.opponent_mana_progress
            && candidate.safe_opponent_mana_progress_steps
                != incumbent.safe_opponent_mana_progress_steps
        {
            return if Self::root_progress_steps_better(
                candidate.safe_opponent_mana_progress_steps,
                incumbent.safe_opponent_mana_progress_steps,
            ) {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.mana_handoff_to_opponent != incumbent.mana_handoff_to_opponent {
            return if !candidate.mana_handoff_to_opponent {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.has_roundtrip != incumbent.has_roundtrip {
            return if !candidate.has_roundtrip {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.spirit_development != incumbent.spirit_development {
            return if candidate.spirit_development {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            };
        }
        if candidate.interview_soft_priority != incumbent.interview_soft_priority {
            return incumbent
                .interview_soft_priority
                .cmp(&candidate.interview_soft_priority);
        }
        if candidate.efficiency != incumbent.efficiency {
            return incumbent.efficiency.cmp(&candidate.efficiency);
        }
        std::cmp::Ordering::Equal
    }

    fn compare_ranked_scored_root_indices(
        scored_roots: &[RootEvaluation],
        a: usize,
        b: usize,
    ) -> std::cmp::Ordering {
        scored_roots[b]
            .score
            .cmp(&scored_roots[a].score)
            .then_with(|| {
                Self::compare_tactical_root_evaluations(&scored_roots[a], &scored_roots[b])
            })
            .then_with(|| a.cmp(&b))
    }

    fn best_tactical_root_index<F>(root_moves: &[ScoredRootMove], predicate: F) -> Option<usize>
    where
        F: Fn(&ScoredRootMove) -> bool,
    {
        let mut best_index: Option<usize> = None;
        for (index, candidate) in root_moves.iter().enumerate() {
            if !predicate(candidate) {
                continue;
            }
            let is_better = match best_index {
                None => true,
                Some(incumbent_index) => {
                    Self::is_better_tactical_root_candidate(candidate, &root_moves[incumbent_index])
                }
            };
            if is_better {
                best_index = Some(index);
            }
        }
        best_index
    }

    fn forced_tactical_prepass_choice(
        game: &MonsGame,
        perspective: Color,
        root_moves: &[ScoredRootMove],
        config: AutomoveSearchConfig,
    ) -> Option<Vec<Input>> {
        if !config.enable_forced_tactical_prepass || root_moves.is_empty() {
            return None;
        }

        if let Some(index) =
            Self::best_tactical_root_index(root_moves, |candidate| candidate.wins_immediately)
        {
            return Some(root_moves[index].inputs.clone());
        }

        let has_supermana_scoring = config.enable_supermana_prepass_exception
            && root_moves.iter().any(|m| m.scores_supermana_this_turn);
        let has_safe_supermana_pickup = config.enable_supermana_prepass_exception
            && root_moves.iter().any(|m| {
                m.safe_supermana_pickup_now
                    && !m.own_drainer_vulnerable
                    && !m.mana_handoff_to_opponent
                    && !m.wins_immediately
                    && !m.attacks_opponent_drainer
            });
        let has_tactical_prepass_exception = has_supermana_scoring || has_safe_supermana_pickup;

        if config.enable_supermana_prepass_exception {
            if let Some(index) = Self::best_tactical_root_index(root_moves, |candidate| {
                candidate.scores_supermana_this_turn
            }) {
                return Some(root_moves[index].inputs.clone());
            }
            if let Some(index) = Self::best_tactical_root_index(root_moves, |candidate| {
                candidate.safe_supermana_pickup_now
                    && !candidate.own_drainer_vulnerable
                    && !candidate.mana_handoff_to_opponent
                    && !candidate.wins_immediately
                    && !candidate.attacks_opponent_drainer
            }) {
                return Some(root_moves[index].inputs.clone());
            }
        }

        if !has_tactical_prepass_exception {
            if let Some(index) = Self::best_tactical_root_index(root_moves, |candidate| {
                candidate.attacks_opponent_drainer
            }) {
                return Some(root_moves[index].inputs.clone());
            }
        }

        if !has_tactical_prepass_exception
            && Self::is_own_drainer_vulnerable_next_turn(game, perspective, true)
        {
            if let Some(index) = Self::best_tactical_root_index(root_moves, |candidate| {
                !candidate.own_drainer_vulnerable
            }) {
                return Some(root_moves[index].inputs.clone());
            }
        }

        let opponent_score = if perspective == Color::White {
            game.black_score
        } else {
            game.white_score
        };
        let opponent_distance_to_win = Config::TARGET_SCORE - opponent_score;
        if opponent_distance_to_win <= 1 {
            if let Some(index) = Self::best_tactical_root_index(root_moves, |candidate| {
                candidate.classes.immediate_score
            }) {
                return Some(root_moves[index].inputs.clone());
            }
        }

        None
    }

    fn forced_low_budget_turn_engine_prepass_choice(
        game: &MonsGame,
        root_moves: &[ScoredRootMove],
        plan: &TurnPlan,
        config: AutomoveSearchConfig,
    ) -> Option<Vec<Input>> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || !config.enable_turn_engine_low_budget_guard
            || root_moves.is_empty()
        {
            return None;
        }

        let low_budget_opening = Self::current_pro_is_early_white_turn_start(game)
            || Self::current_pro_is_safe_early_black_opening_state(game);
        if !low_budget_opening {
            return None;
        }

        let candidate_inputs = plan.compiled_chunks.first()?;
        let candidate_index = root_moves
            .iter()
            .position(|candidate| candidate.inputs.as_slice() == candidate_inputs.as_slice())?;
        let candidate = &root_moves[candidate_index];
        if Self::turn_engine_root_move_is_unsafe(candidate) {
            return None;
        }
        let top = &root_moves[0];
        if top.wins_immediately
            || top.attacks_opponent_drainer
            || top.classes.drainer_safety_recover
        {
            return None;
        }

        let heuristic_gap = top.heuristic.saturating_sub(candidate.heuristic);
        let candidate_progress_like = matches!(
            plan.head_family,
            TurnPlanFamily::SafeSupermanaProgress
                | TurnPlanFamily::SafeOpponentManaProgress
                | TurnPlanFamily::DrainerSafetyRecovery
        );
        let top_plain_spirit = top.spirit_development
            && !top.spirit_same_turn_score_setup_now
            && !top.spirit_own_mana_setup_now;
        let candidate_progress_surface =
            Self::turn_engine_root_move_has_progress_surface(candidate);
        let top_unsafe = Self::turn_engine_root_move_is_unsafe(top);

        if candidate_index == 0 && candidate_progress_like {
            return Some(candidate.inputs.clone());
        }

        if Self::current_pro_is_early_white_turn_start(game)
            && top_plain_spirit
            && !top_unsafe
            && candidate_progress_surface
            && candidate_index <= 2
            && heuristic_gap <= 96
        {
            return Some(candidate.inputs.clone());
        }

        if Self::current_pro_is_safe_early_black_opening_state(game)
            && candidate_index <= 1
            && heuristic_gap <= 96
            && (candidate_progress_surface || candidate.spirit_development)
        {
            return Some(candidate.inputs.clone());
        }

        None
    }

    fn should_skip_current_pro_head_plan_for_root_context(
        game: &MonsGame,
        root_moves: &[ScoredRootMove],
        config: AutomoveSearchConfig,
    ) -> bool {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || !config.enable_turn_engine_low_budget_guard
            || root_moves.is_empty()
        {
            return false;
        }

        if Self::current_pro_is_early_white_turn_start(game) {
            return false;
        }

        if !game.player_can_use_action()
            && !game.player_can_move_mana()
            && game.mons_moves_count >= 4
        {
            return true;
        }

        game.active_color == Color::Black && game.turn_number == 2 && game.mons_moves_count <= 1
    }

    fn truncate_root_candidates_with_class_coverage(
        candidates: Vec<ScoredRootMove>,
        limit: usize,
        strict_guarantees: bool,
    ) -> Vec<ScoredRootMove> {
        if candidates.len() <= limit {
            return candidates;
        }

        let mut selected_mask = vec![false; candidates.len()];
        let mut priority_indices = Vec::new();
        let best_heuristic = candidates[0].heuristic;
        let min_critical_heuristic =
            best_heuristic.saturating_sub(SMART_MOVE_CLASS_ROOT_SCORE_MARGIN.max(0));
        let mut mark_priority = |index: usize| {
            selected_mask[index] = true;
            if !priority_indices.contains(&index) {
                priority_indices.push(index);
            }
        };
        for class in [
            MoveClass::ImmediateScore,
            MoveClass::DrainerAttack,
            MoveClass::DrainerSafetyRecover,
        ] {
            let chosen = if strict_guarantees {
                candidates
                    .iter()
                    .enumerate()
                    .find(|(_, candidate)| candidate.classes.has(class))
            } else {
                candidates.iter().enumerate().find(|(_, candidate)| {
                    candidate.classes.has(class) && candidate.heuristic >= min_critical_heuristic
                })
            };
            if let Some((index, _)) = chosen {
                mark_priority(index);
            }
        }
        if let Some(index) = Self::best_tactical_root_index(candidates.as_slice(), |candidate| {
            candidate.same_turn_score_window_value > 0
                && (strict_guarantees || candidate.heuristic >= min_critical_heuristic)
        }) {
            mark_priority(index);
        }
        let unknown_progress_steps = Config::BOARD_SIZE + 4;
        for (index, candidate) in candidates.iter().enumerate() {
            let keep_direct_high_value = candidate.scores_supermana_this_turn
                || candidate.scores_opponent_mana_this_turn
                || candidate.safe_supermana_pickup_now
                || candidate.safe_opponent_mana_pickup_now
                || candidate.spirit_same_turn_score_setup_now
                || candidate.same_turn_score_window_value > 0
                || candidate.spirit_own_mana_setup_now;
            let keep_exact_progress = (candidate.supermana_progress
                && candidate.safe_supermana_progress_steps < unknown_progress_steps)
                || (candidate.opponent_mana_progress
                    && candidate.safe_opponent_mana_progress_steps < unknown_progress_steps);
            if !keep_direct_high_value && !keep_exact_progress {
                continue;
            }
            if strict_guarantees || candidate.heuristic >= min_critical_heuristic {
                selected_mask[index] = true;
            }
        }

        let mut owned_candidates = candidates.into_iter().map(Some).collect::<Vec<_>>();
        let mut shortlisted = Vec::with_capacity(limit.min(owned_candidates.len()));
        for index in priority_indices {
            if shortlisted.len() >= limit {
                break;
            }
            if let Some(candidate) = owned_candidates[index].take() {
                shortlisted.push(candidate);
            }
        }
        for index in 0..owned_candidates.len() {
            if shortlisted.len() >= limit {
                break;
            }
            if selected_mask[index] {
                if let Some(candidate) = owned_candidates[index].take() {
                    shortlisted.push(candidate);
                }
            }
        }
        for index in 0..owned_candidates.len() {
            if shortlisted.len() >= limit {
                break;
            }
            if selected_mask[index] {
                continue;
            }
            if let Some(candidate) = owned_candidates[index].take() {
                shortlisted.push(candidate);
            }
        }
        shortlisted
    }

    fn classify_move_classes(
        before: &MonsGame,
        after: &MonsGame,
        actor_color: Color,
        events: &[Event],
        own_drainer_vulnerable_before: bool,
        own_drainer_vulnerable_after: bool,
    ) -> MoveClassFlags {
        let immediate_score = events
            .iter()
            .any(|event| matches!(event, Event::ManaScored { .. }))
            || Self::score_for_color(after, actor_color)
                > Self::score_for_color(before, actor_color);
        let drainer_attack = Self::events_include_opponent_drainer_fainted(events, actor_color);
        let drainer_safety_recover = own_drainer_vulnerable_before && !own_drainer_vulnerable_after;
        let spirit_development =
            Self::has_spirit_development_turn(before, after, actor_color, events);
        let material = Self::has_material_event(events);
        let carrier_progress = Self::has_actor_carrier_progress(before, after, actor_color);
        let quiet = !immediate_score
            && !drainer_attack
            && !drainer_safety_recover
            && !spirit_development
            && !carrier_progress
            && !material;
        MoveClassFlags {
            immediate_score,
            drainer_attack,
            drainer_safety_recover,
            carrier_progress,
            material,
            quiet,
        }
    }

    fn has_actor_carrier_progress(before: &MonsGame, after: &MonsGame, actor_color: Color) -> bool {
        let before_snapshot = Self::carrier_progress_snapshot(before, actor_color);
        let after_snapshot = Self::carrier_progress_snapshot(after, actor_color);
        Self::has_actor_carrier_progress_from_snapshots(before_snapshot, after_snapshot)
    }

    fn has_actor_carrier_progress_from_snapshots(
        before: CarrierProgressSnapshot,
        after: CarrierProgressSnapshot,
    ) -> bool {
        if after.carrier_count > before.carrier_count {
            return true;
        }
        let unknown_steps = Config::BOARD_SIZE + 4;
        after.best_carrier_steps < before.best_carrier_steps
            && after.best_carrier_steps < unknown_steps
    }

    fn carrier_progress_snapshot(game: &MonsGame, color: Color) -> CarrierProgressSnapshot {
        let unknown_steps = Config::BOARD_SIZE + 4;
        let mut snapshot = CarrierProgressSnapshot {
            carrier_count: 0,
            best_carrier_steps: unknown_steps,
        };
        for (location, item) in game.board.occupied() {
            let Item::MonWithMana { mon, .. } = item else {
                continue;
            };
            if mon.color != color || mon.is_fainted() {
                continue;
            }
            snapshot.carrier_count += 1;
            snapshot.best_carrier_steps = snapshot
                .best_carrier_steps
                .min(Self::distance_to_any_pool_steps_for_efficiency(location).saturating_sub(1));
        }
        snapshot
    }

    fn root_volatility_score(candidate: &ScoredRootMove) -> i32 {
        let mut score = 0i32;
        if candidate.wins_immediately {
            score += 5_000;
        }
        if candidate.attacks_opponent_drainer || candidate.classes.drainer_attack {
            score += 2_800;
        }
        if candidate.own_drainer_vulnerable {
            score += 2_200;
        }
        if candidate.classes.immediate_score {
            score += 1_700;
        }
        if candidate.classes.drainer_safety_recover {
            score += 1_500;
        }
        if candidate.mana_handoff_to_opponent {
            score += 900;
        }
        if candidate.has_roundtrip {
            score += 700;
        }
        if candidate.classes.material {
            score += 240;
        }
        if candidate.efficiency < 0 {
            score += (-candidate.efficiency).min(400);
        }
        score
    }

    fn enforce_tactical_child_top2(
        scored_states: &mut [(i32, RankedChildState)],
        maximizing: bool,
        strict_guarantees: bool,
    ) {
        if scored_states.len() < 3 {
            return;
        }
        let top_has_tactical = scored_states
            .iter()
            .take(2)
            .any(|(_, state)| Self::is_child_search_priority_candidate(state));
        if top_has_tactical {
            return;
        }

        let second_score = scored_states[1].0;
        let replacement_index =
            scored_states
                .iter()
                .enumerate()
                .skip(2)
                .find_map(|(index, (score, state))| {
                    if !Self::is_child_search_priority_candidate(state) {
                        return None;
                    }
                    if strict_guarantees {
                        Some(index)
                    } else {
                        let tactical_margin = SMART_MOVE_CLASS_CHILD_SCORE_MARGIN.max(0);
                        let close_enough = if maximizing {
                            score.saturating_add(tactical_margin) >= second_score
                        } else {
                            *score <= second_score.saturating_add(tactical_margin)
                        };
                        if close_enough {
                            Some(index)
                        } else {
                            None
                        }
                    }
                });
        let Some(replacement_index) = replacement_index else {
            return;
        };

        let swap_index = 1;
        scored_states.swap(swap_index, replacement_index);
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn smart_search_best_inputs(game: &MonsGame, config: AutomoveSearchConfig) -> Vec<Input> {
        Self::smart_search_best_inputs_internal(game, config, true)
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_config_from_search_config(config: AutomoveSearchConfig) -> TurnEngineConfig {
        automove_runtime_variants::turn_engine_config_from_search_config(config)
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_mode_uses_macro_plans(mode: TurnEngineMode) -> bool {
        matches!(mode, TurnEngineMode::CurrentPro)
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_config_for_game(
        game: &MonsGame,
        config: AutomoveSearchConfig,
    ) -> TurnEngineConfig {
        let mut engine = Self::turn_engine_config_from_search_config(config);
        let clamp_early_white_turn_start =
            Self::turn_engine_mode_uses_macro_plans(config.turn_engine_mode)
                && config.enable_turn_engine_low_budget_guard
                && Self::current_pro_is_early_white_turn_start(game);
        if clamp_early_white_turn_start {
            engine.own_seed_cap = engine.own_seed_cap.clamp(1, 14);
            engine.own_beam = engine.own_beam.clamp(1, 5);
            engine.per_node_family_cap = engine.per_node_family_cap.clamp(1, 4);
            engine.step_cap = engine.step_cap.clamp(1, 6);
            engine.opponent_seed_cap = 1;
            engine.opponent_beam = 1;
            engine.reply_seed_cap = 1;
            engine.reply_beam = 1;
            engine.expansion_cap = engine.expansion_cap.clamp(1, 48);
        }
        engine
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_rerank_config(config: AutomoveSearchConfig) -> TurnEngineConfig {
        let mut engine = Self::turn_engine_config_from_search_config(config);
        let current_pro = Self::turn_engine_mode_uses_macro_plans(config.turn_engine_mode);
        engine.own_seed_cap = engine
            .own_seed_cap
            .clamp(1, if current_pro { 12 } else { 8 });
        engine.own_beam = engine.own_beam.clamp(1, if current_pro { 5 } else { 4 });
        engine.per_node_family_cap = engine
            .per_node_family_cap
            .clamp(1, if current_pro { 4 } else { 3 });
        engine.step_cap = engine.step_cap.clamp(1, 5);
        engine.opponent_seed_cap = engine
            .opponent_seed_cap
            .clamp(1, if current_pro { 6 } else { 4 });
        engine.opponent_beam = engine
            .opponent_beam
            .clamp(1, if current_pro { 3 } else { 2 });
        engine.reply_seed_cap = engine
            .reply_seed_cap
            .clamp(1, if current_pro { 3 } else { 2 });
        engine.reply_beam = if current_pro { 2 } else { 1 };
        engine.expansion_cap = engine
            .expansion_cap
            .clamp(1, if current_pro { 144 } else { 96 });
        engine
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_projection_config_for_game(
        game: &MonsGame,
        config: AutomoveSearchConfig,
    ) -> TurnEngineConfig {
        let mut engine = Self::turn_engine_config_for_game(game, config);
        let current_pro = Self::turn_engine_mode_uses_macro_plans(config.turn_engine_mode);
        engine.own_seed_cap = engine
            .own_seed_cap
            .clamp(1, if current_pro { 8 } else { 6 });
        engine.own_beam = engine.own_beam.clamp(1, if current_pro { 3 } else { 2 });
        engine.per_node_family_cap = engine
            .per_node_family_cap
            .clamp(1, if current_pro { 3 } else { 2 });
        engine.step_cap = engine.step_cap.clamp(1, 4);
        engine.opponent_seed_cap = engine
            .opponent_seed_cap
            .clamp(1, if current_pro { 2 } else { 1 });
        engine.opponent_beam = 1;
        engine.reply_seed_cap = 1;
        engine.reply_beam = 1;
        engine.expansion_cap = engine
            .expansion_cap
            .clamp(1, if current_pro { 64 } else { 48 });
        engine
    }

    fn accept_turn_engine_cached_step(
        root_moves: &[ScoredRootMove],
        cached_inputs: &[Input],
        mode: TurnEngineMode,
    ) -> bool {
        let Some(index) = root_moves
            .iter()
            .position(|candidate| candidate.inputs.as_slice() == cached_inputs)
        else {
            return false;
        };
        if index == 0 {
            return true;
        }
        let Some(top) = root_moves.first() else {
            return false;
        };
        if top.wins_immediately {
            return false;
        }
        let candidate = &root_moves[index];
        let heuristic_gap = top.heuristic.saturating_sub(candidate.heuristic);
        let candidate_unsafe = candidate.mana_handoff_to_opponent
            || (candidate.own_drainer_vulnerable
                && !candidate.classes.drainer_safety_recover
                && !candidate.attacks_opponent_drainer
                && !Self::turn_engine_root_move_has_concrete_score_surface(candidate));
        if matches!(mode, TurnEngineMode::ProV1) {
            return index <= 2 && heuristic_gap <= 96 && !candidate_unsafe;
        }

        let top_unsafe = top.mana_handoff_to_opponent
            || (top.own_drainer_vulnerable
                && !top.classes.drainer_safety_recover
                && !top.attacks_opponent_drainer
                && !Self::turn_engine_root_move_has_concrete_score_surface(top));
        let candidate_tactical = candidate.wins_immediately
            || candidate.attacks_opponent_drainer
            || candidate.classes.drainer_safety_recover
            || candidate.spirit_same_turn_score_setup_now
            || candidate.spirit_own_mana_setup_now
            || candidate.spirit_development
            || candidate.scores_supermana_this_turn
            || candidate.scores_opponent_mana_this_turn
            || candidate.safe_supermana_pickup_now
            || candidate.safe_opponent_mana_pickup_now
            || Self::root_progress_steps_better(
                candidate.safe_supermana_progress_steps,
                top.safe_supermana_progress_steps,
            )
            || Self::root_progress_steps_better(
                candidate.safe_opponent_mana_progress_steps,
                top.safe_opponent_mana_progress_steps,
            );

        (!candidate_unsafe && index <= 4 && heuristic_gap <= 128)
            || (!candidate_unsafe && candidate_tactical && index <= 8 && heuristic_gap <= 224)
            || (!candidate_unsafe && top_unsafe && index <= 10 && heuristic_gap <= 256)
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn seed_turn_engine_followup_cache_if_safe(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        mode: TurnEngineMode,
        plan: &crate::models::automove_turn_engine::TurnPlan,
        _engine_config: TurnEngineConfig,
    ) {
        if !Self::turn_engine_mode_uses_macro_plans(mode) || plan.compiled_chunks.len() < 2 {
            return;
        }
        let Some(first_chunk) = plan.compiled_chunks.first() else {
            return;
        };
        let Some(second_chunk) = plan.compiled_chunks.get(1) else {
            return;
        };
        let Some(after_first) = Self::apply_inputs_for_search(game, first_chunk.as_slice()) else {
            return;
        };
        if after_first.active_color != perspective {
            return;
        }
        let after_first_engine_config = Self::turn_engine_config_for_game(&after_first, config);
        let root_moves = Self::ranked_root_moves(&after_first, perspective, config);
        if !Self::should_resume_turn_engine_cached_step(
            root_moves.as_slice(),
            second_chunk.as_slice(),
            mode,
        ) {
            return;
        }
        crate::models::automove_turn_engine::turn_engine_store_cached_step(
            &after_first,
            mode,
            after_first_engine_config,
            second_chunk.as_slice(),
        );
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn should_resume_turn_engine_cached_step(
        root_moves: &[ScoredRootMove],
        cached_inputs: &[Input],
        mode: TurnEngineMode,
    ) -> bool {
        if !Self::turn_engine_mode_uses_macro_plans(mode) {
            return false;
        }
        Self::accept_turn_engine_cached_step(root_moves, cached_inputs, mode)
            && root_moves
                .first()
                .map(|root| root.inputs.as_slice() == cached_inputs)
                .unwrap_or(false)
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_root_move_family(root: &ScoredRootMove) -> TurnPlanFamily {
        if root.wins_immediately
            || root.scores_supermana_this_turn
            || root.scores_opponent_mana_this_turn
            || root.safe_supermana_pickup_now
            || root.safe_opponent_mana_pickup_now
        {
            TurnPlanFamily::ImmediateScore
        } else if root.attacks_opponent_drainer {
            TurnPlanFamily::DrainerKill
        } else if root.classes.drainer_safety_recover {
            TurnPlanFamily::DrainerSafetyRecovery
        } else if root.spirit_same_turn_score_setup_now
            || root.spirit_own_mana_setup_now
            || root.spirit_development
        {
            TurnPlanFamily::SpiritImpact
        } else if root.supermana_progress {
            TurnPlanFamily::SafeSupermanaProgress
        } else if root.opponent_mana_progress {
            TurnPlanFamily::SafeOpponentManaProgress
        } else {
            TurnPlanFamily::ManaTempo
        }
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_root_evaluation_family(root: &RootEvaluation) -> TurnPlanFamily {
        if root.wins_immediately
            || root.scores_supermana_this_turn
            || root.scores_opponent_mana_this_turn
            || root.safe_supermana_pickup_now
            || root.safe_opponent_mana_pickup_now
        {
            TurnPlanFamily::ImmediateScore
        } else if root.attacks_opponent_drainer {
            TurnPlanFamily::DrainerKill
        } else if root.classes.drainer_safety_recover {
            TurnPlanFamily::DrainerSafetyRecovery
        } else if root.spirit_same_turn_score_setup_now
            || root.spirit_own_mana_setup_now
            || root.spirit_development
        {
            TurnPlanFamily::SpiritImpact
        } else if root.supermana_progress {
            TurnPlanFamily::SafeSupermanaProgress
        } else if root.opponent_mana_progress {
            TurnPlanFamily::SafeOpponentManaProgress
        } else {
            TurnPlanFamily::ManaTempo
        }
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_root_move_has_progress_surface(root: &ScoredRootMove) -> bool {
        root.safe_supermana_pickup_now
            || root.safe_opponent_mana_pickup_now
            || root.supermana_progress
            || root.opponent_mana_progress
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_root_evaluation_has_progress_surface(root: &RootEvaluation) -> bool {
        root.safe_supermana_pickup_now
            || root.safe_opponent_mana_pickup_now
            || root.supermana_progress
            || root.opponent_mana_progress
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_root_move_has_concrete_score_surface(root: &ScoredRootMove) -> bool {
        root.wins_immediately
            || root.scores_supermana_this_turn
            || root.scores_opponent_mana_this_turn
            || root.safe_supermana_pickup_now
            || root.safe_opponent_mana_pickup_now
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_root_evaluation_has_concrete_score_surface(root: &RootEvaluation) -> bool {
        root.wins_immediately
            || root.scores_supermana_this_turn
            || root.scores_opponent_mana_this_turn
            || root.safe_supermana_pickup_now
            || root.safe_opponent_mana_pickup_now
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_root_move_is_unsafe(root: &ScoredRootMove) -> bool {
        root.mana_handoff_to_opponent
            || (root.own_drainer_vulnerable
                && !root.classes.drainer_safety_recover
                && !root.attacks_opponent_drainer
                && !Self::turn_engine_root_move_has_concrete_score_surface(root))
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_root_evaluation_is_unsafe(root: &RootEvaluation) -> bool {
        root.mana_handoff_to_opponent
            || (root.own_drainer_vulnerable
                && !root.classes.drainer_safety_recover
                && !root.attacks_opponent_drainer
                && !Self::turn_engine_root_evaluation_has_concrete_score_surface(root))
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_scored_root_utility(
        root: &MonsGame,
        candidate: &ScoredRootMove,
        perspective: Color,
        config: AutomoveSearchConfig,
        family: TurnPlanFamily,
    ) -> TurnEngineUtility {
        let engine_config = Self::turn_engine_config_from_search_config(config);
        let mut plan = TurnPlan {
            actions: Vec::new(),
            compiled_chunks: vec![candidate.inputs.clone()],
            end_game: candidate.game.clone_for_simulation(),
            #[cfg(test)]
            end_snapshot: TurnSnapshot::from_game(&candidate.game),
            utility: turn_engine_evaluate_state_utility(
                &candidate.game,
                root,
                perspective,
                engine_config,
            ),
            head_utility: turn_engine_evaluate_state_utility(
                &candidate.game,
                root,
                perspective,
                engine_config,
            ),
            head_family: family,
            goal_family: family,
            package_meta: TurnPackageMeta::default(),
        };
        plan.utility =
            turn_engine_evaluate_plan_with_replies(root, &plan, perspective, engine_config);
        plan.utility
    }

    #[cfg(any(target_arch = "wasm32", test))]
    pub(crate) fn focused_candidate_rank_for_runtime_inputs(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        selected_inputs: &[Input],
    ) -> Option<usize> {
        let mut root_moves = Self::ranked_root_moves(game, perspective, config);
        let engine_plan = if config.enable_turn_engine_selector {
            turn_engine_candidate_plan(
                game,
                perspective,
                Self::turn_engine_config_for_game(game, config),
            )
        } else {
            None
        };
        let advisor_decision = Self::current_pro_root_advisor_presearch(
            game,
            perspective,
            config,
            &mut root_moves,
            engine_plan.as_ref(),
        );
        let advisor_priority_inputs = advisor_decision
            .as_ref()
            .map(Self::current_pro_root_advisor_priority_inputs)
            .unwrap_or_default();
        let (root_moves, _) = Self::focused_root_candidates_with_priority_inputs(
            game,
            perspective,
            root_moves,
            config,
            true,
            (!advisor_priority_inputs.is_empty()).then_some(advisor_priority_inputs.as_slice()),
            None,
        );
        root_moves
            .iter()
            .position(|root| root.inputs.as_slice() == selected_inputs)
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_root_plan_utility(
        root: &MonsGame,
        selected: &RootEvaluation,
        perspective: Color,
        config: AutomoveSearchConfig,
        family: TurnPlanFamily,
    ) -> TurnEngineUtility {
        let engine_config = Self::turn_engine_config_from_search_config(config);
        Self::turn_engine_root_plan_utility_with_engine_config(
            root,
            selected,
            perspective,
            engine_config,
            family,
        )
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_root_plan_utility_with_engine_config(
        root: &MonsGame,
        selected: &RootEvaluation,
        perspective: Color,
        engine_config: TurnEngineConfig,
        family: TurnPlanFamily,
    ) -> TurnEngineUtility {
        let mut selected_plan = TurnPlan {
            actions: Vec::new(),
            compiled_chunks: vec![selected.inputs.clone()],
            end_game: selected.game.clone_for_simulation(),
            #[cfg(test)]
            end_snapshot: TurnSnapshot::from_game(&selected.game),
            utility: turn_engine_evaluate_state_utility(
                &selected.game,
                root,
                perspective,
                engine_config,
            ),
            head_utility: turn_engine_evaluate_state_utility(
                &selected.game,
                root,
                perspective,
                engine_config,
            ),
            head_family: family,
            goal_family: family,
            package_meta: TurnPackageMeta::default(),
        };
        selected_plan.utility = turn_engine_evaluate_plan_with_replies(
            root,
            &selected_plan,
            perspective,
            engine_config,
        );
        selected_plan.utility
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_selected_override_utility(
        root: &MonsGame,
        selected: &RootEvaluation,
        perspective: Color,
        config: AutomoveSearchConfig,
        family: TurnPlanFamily,
    ) -> TurnEngineUtility {
        let cache_key = TurnEngineSelectedOverrideCacheKey {
            state_hash: Self::search_state_hash(&selected.game),
            family,
        };
        if let Some(cached) = TURN_ENGINE_SELECTED_OVERRIDE_UTILITY_CACHE
            .with(|cache| cache.borrow().get(&cache_key).copied())
        {
            return cached;
        }

        #[cfg(test)]
        update_turn_engine_selector_diagnostics(|diagnostics| {
            diagnostics.selected_override_calls += 1
        });
        let engine_config = Self::turn_engine_projection_config_for_game(&selected.game, config);
        let base_state_utility =
            turn_engine_evaluate_state_utility(&selected.game, root, perspective, engine_config);
        let result = if !Self::current_pro_secondary_analysis_live(config) {
            base_state_utility
        } else {
            let base_utility = Self::turn_engine_root_plan_utility_with_engine_config(
                root,
                selected,
                perspective,
                engine_config,
                family,
            );
            if !Self::current_pro_selected_followup_projection_live(config)
                || !Self::can_turn_engine_project_root(selected, perspective)
            {
                base_utility.max(base_state_utility)
            } else {
                let selected_has_followup_surface = selected.own_drainer_vulnerable
                    || selected.spirit_development
                    || selected.spirit_same_turn_score_setup_now
                    || selected.spirit_own_mana_setup_now
                    || Self::turn_engine_root_evaluation_has_progress_surface(selected);
                let selected_safe_mana_tempo_projection = selected.game.active_color
                    == Color::Black
                    && matches!(
                        family,
                        TurnPlanFamily::ManaTempo | TurnPlanFamily::DrainerSafetyRecovery
                    )
                    && !selected_has_followup_surface
                    && !Self::turn_engine_root_evaluation_is_unsafe(selected)
                    && !selected.mana_handoff_to_opponent
                    && !selected.has_roundtrip
                    && !selected.wins_immediately
                    && !selected.attacks_opponent_drainer;
                if !selected_has_followup_surface && !selected_safe_mana_tempo_projection {
                    base_utility
                } else {
                    #[cfg(test)]
                    update_turn_engine_selector_diagnostics(|diagnostics| {
                        diagnostics.selected_override_plan_calls += 1;
                    });
                    let projected_plan =
                        turn_engine_candidate_plan(&selected.game, perspective, engine_config);
                    #[cfg(test)]
                    if projected_plan.is_some() {
                        update_turn_engine_selector_diagnostics(|diagnostics| {
                            diagnostics.selected_override_plan_hits += 1;
                        });
                    }
                    projected_plan
                        .map(|plan| plan.utility.max(base_utility))
                        .unwrap_or(base_utility)
                }
            }
        };
        if crate::models::automove_deadline::cache_write_allowed() {
            TURN_ENGINE_SELECTED_OVERRIDE_UTILITY_CACHE.with(|cache| {
                cache.borrow_mut().insert(cache_key, result);
            });
        }
        result
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn inject_turn_engine_root_candidate(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        root_moves: &mut Vec<ScoredRootMove>,
        plan: &TurnPlan,
    ) -> Option<Vec<Input>> {
        let macro_mode = Self::turn_engine_mode_uses_macro_plans(config.turn_engine_mode);
        let candidate_inputs = plan.compiled_chunks.first()?.clone();
        let existing_candidate = root_moves
            .iter()
            .find(|candidate| candidate.inputs == candidate_inputs)
            .cloned();
        let own_drainer_vulnerable_before =
            Self::is_own_drainer_vulnerable_next_turn(game, perspective, true);
        let candidate = if let Some(candidate) = existing_candidate.clone() {
            candidate
        } else {
            Self::build_scored_root_move(
                game,
                perspective,
                config,
                own_drainer_vulnerable_before,
                candidate_inputs.as_slice(),
            )?
        };
        let top = root_moves.first()?;
        if top.wins_immediately && !candidate.wins_immediately {
            return None;
        }
        let top_unsafe = Self::turn_engine_root_move_is_unsafe(top);
        let candidate_unsafe = Self::turn_engine_root_move_is_unsafe(&candidate);
        let top_family = Self::turn_engine_root_move_family(top);
        let top_utility =
            Self::turn_engine_scored_root_utility(game, top, perspective, config, top_family);
        let mut projected_state = game.clone_for_simulation();
        let mut projected_events = Vec::new();
        let mut projected_complete = true;
        for chunk in plan.compiled_chunks.iter() {
            let Some((after, events)) =
                Self::apply_inputs_for_search_with_events(&projected_state, chunk.as_slice())
            else {
                projected_complete = false;
                break;
            };
            projected_events.extend(events);
            projected_state = after;
        }
        let projected_turn_finished = projected_complete
            && (projected_state.winner_color().is_some()
                || projected_state.active_color != perspective
                || (!projected_state.player_can_move_mon()
                    && !projected_state.player_can_use_action()
                    && !projected_state.player_can_move_mana()));
        let projected_near_turn_completion = projected_complete
            && (projected_turn_finished
                || plan.compiled_chunks.len() >= 4
                || !projected_state.player_can_move_mon()
                || (!projected_state.player_can_use_action()
                    && !projected_state.player_can_move_mana()));
        let projected_mana_handoff = projected_complete
            && Self::mana_handoff_penalty(
                projected_events.as_slice(),
                perspective,
                config.root_mana_handoff_penalty.max(1),
            ) > 0;
        let projected_roundtrip =
            projected_complete && Self::has_roundtrip_mon_move(projected_events.as_slice());
        let projected_end_vulnerable = projected_complete
            && projected_state.winner_color() != Some(perspective)
            && Self::is_own_drainer_vulnerable_next_turn(&projected_state, perspective, true);
        let completed_plan_override = if macro_mode {
            let projected_end_safe = projected_near_turn_completion
                && !projected_mana_handoff
                && !projected_roundtrip
                && !projected_end_vulnerable;
            projected_end_safe
                && plan.compiled_chunks.len() > 1
                && !matches!(plan.goal_family, TurnPlanFamily::ManaTempo)
                && (crate::models::automove_turn_engine::compare_utility_primary_axes(
                    plan.utility,
                    top_utility,
                ) != std::cmp::Ordering::Less
                    || plan.utility.passes_override_guard(top_utility)
                    || plan
                        .utility
                        .supports_primary_axes_eval_tolerance(top_utility, 160)
                    || (matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
                        && plan.utility.improves_non_score_override_axes(top_utility)))
        } else {
            false
        };
        let injected_progress_head_regresses_top_surface = macro_mode
            && existing_candidate.is_none()
            && matches!(
                plan.head_family,
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            )
            && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
            && plan.compiled_chunks.len() > 1
            && !candidate_unsafe
            && root_moves.iter().take(3).any(|root| {
                let root_unsafe = Self::turn_engine_root_move_is_unsafe(root);
                let root_surface = root.spirit_development
                    || root.spirit_same_turn_score_setup_now
                    || root.spirit_own_mana_setup_now
                    || root.safe_supermana_pickup_now
                    || root.safe_opponent_mana_pickup_now
                    || root.wins_immediately
                    || root.scores_supermana_this_turn
                    || root.scores_opponent_mana_this_turn;
                if root_unsafe || !root_surface {
                    return false;
                }
                let root_family = Self::turn_engine_root_move_family(root);
                let root_utility = Self::turn_engine_scored_root_utility(
                    game,
                    root,
                    perspective,
                    config,
                    root_family,
                );
                crate::models::automove_turn_engine::compare_utility_primary_axes(
                    plan.head_utility,
                    root_utility,
                ) == std::cmp::Ordering::Less
            });
        let injected_progress_head_blocks_concrete_spirit_top = macro_mode
            && existing_candidate.is_none()
            && matches!(
                plan.head_family,
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            )
            && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
            && plan.compiled_chunks.len() > 1
            && root_moves.iter().take(3).any(|root| {
                root.spirit_same_turn_score_setup_now
                    || root.spirit_own_mana_setup_now
                    || root.same_turn_score_window_value > 0
            });
        let injected_progress_head_duplicates_safe_progress_top = macro_mode
            && existing_candidate.is_none()
            && matches!(
                plan.head_family,
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            )
            && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
            && plan.compiled_chunks.len() > 1
            && root_moves.iter().take(3).any(|root| {
                !Self::turn_engine_root_move_is_unsafe(root)
                    && Self::turn_engine_root_move_has_progress_surface(root)
            });
        let injected_progress_head_blocks_safe_non_progress_top = macro_mode
            && existing_candidate.is_none()
            && matches!(
                plan.head_family,
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            )
            && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
            && plan.compiled_chunks.len() > 1
            && !top_unsafe
            && !top.wins_immediately
            && !top.attacks_opponent_drainer
            && !top.scores_supermana_this_turn
            && !top.scores_opponent_mana_this_turn
            && !top.spirit_development
            && !top.spirit_same_turn_score_setup_now
            && !top.spirit_own_mana_setup_now
            && !Self::turn_engine_root_move_has_progress_surface(top);
        let injected_progress_head_replaces_concrete_progress_cluster_with_window = macro_mode
            && existing_candidate.is_none()
            && matches!(
                plan.head_family,
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            )
            && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
            && !candidate.wins_immediately
            && !candidate.attacks_opponent_drainer
            && !candidate.safe_supermana_pickup_now
            && !candidate.safe_opponent_mana_pickup_now
            && !candidate.spirit_development
            && !candidate.spirit_same_turn_score_setup_now
            && !candidate.spirit_own_mana_setup_now
            && candidate.same_turn_score_window_value > 0
            && !candidate.supermana_progress
            && !candidate.opponent_mana_progress
            && root_moves
                .iter()
                .take(6)
                .filter(|root| {
                    Self::turn_engine_root_move_has_progress_surface(root)
                        && root.same_turn_score_window_value == 0
                        && !root.spirit_same_turn_score_setup_now
                        && !root.spirit_own_mana_setup_now
                })
                .count()
                >= 3;
        let forced_plain_spirit_head_regresses_top_spirit_cluster = macro_mode
            && existing_candidate.is_some()
            && matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
            && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
            && candidate.spirit_development
            && !candidate.spirit_same_turn_score_setup_now
            && !candidate.spirit_own_mana_setup_now
            && !candidate.attacks_opponent_drainer
            && !candidate.scores_supermana_this_turn
            && !candidate.scores_opponent_mana_this_turn
            && !candidate.safe_supermana_pickup_now
            && !candidate.safe_opponent_mana_pickup_now
            && root_moves.iter().take(3).any(|root| {
                if root.inputs == candidate.inputs {
                    return false;
                }
                let root_is_plain_spirit = root.spirit_development
                    && !root.spirit_same_turn_score_setup_now
                    && !root.spirit_own_mana_setup_now
                    && !root.attacks_opponent_drainer
                    && !root.scores_supermana_this_turn
                    && !root.scores_opponent_mana_this_turn
                    && !root.safe_supermana_pickup_now
                    && !root.safe_opponent_mana_pickup_now;
                if !root_is_plain_spirit {
                    return false;
                }
                let root_utility = Self::turn_engine_scored_root_utility(
                    game,
                    root,
                    perspective,
                    config,
                    Self::turn_engine_root_move_family(root),
                );
                root.spirit_setup_gain >= candidate.spirit_setup_gain
                    && root.safe_supermana_progress_steps <= candidate.safe_supermana_progress_steps
                    && root.safe_opponent_mana_progress_steps
                        <= candidate.safe_opponent_mana_progress_steps
                    && crate::models::automove_turn_engine::compare_utility_primary_axes(
                        root_utility,
                        plan.head_utility,
                    ) != std::cmp::Ordering::Less
            });
        let completed_plan_override = completed_plan_override
            && !injected_progress_head_regresses_top_surface
            && !injected_progress_head_blocks_concrete_spirit_top;
        let completed_plan_override = completed_plan_override
            && !injected_progress_head_duplicates_safe_progress_top
            && !injected_progress_head_blocks_safe_non_progress_top;
        let utility_override = if macro_mode {
            root_moves.iter().take(3).all(|root| {
                let root_family = Self::turn_engine_root_move_family(root);
                let root_utility = Self::turn_engine_scored_root_utility(
                    game,
                    root,
                    perspective,
                    config,
                    root_family,
                );
                let root_unsafe = Self::turn_engine_root_move_is_unsafe(root);
                plan.utility.passes_override_guard(root_utility)
                    && (!candidate_unsafe || root_unsafe)
            }) || completed_plan_override
        } else {
            false
        };
        let top_spirit_surface = root_moves.iter().take(3).any(|root| {
            root.spirit_development
                || root.spirit_same_turn_score_setup_now
                || root.spirit_own_mana_setup_now
        });
        let candidate_spirit_tactical = candidate.spirit_same_turn_score_setup_now
            || candidate.same_turn_score_window_value > 0
            || candidate.attacks_opponent_drainer
            || candidate.scores_supermana_this_turn
            || candidate.scores_opponent_mana_this_turn
            || candidate.safe_supermana_pickup_now
            || candidate.safe_opponent_mana_pickup_now;
        let candidate_progress_surface =
            Self::turn_engine_root_move_has_progress_surface(&candidate);
        let allow_current_pro_black_turn_six_plain_spirit_head_injection = macro_mode
            && Self::current_pro_black_turn_six_plain_spirit_head_injection_override(
                game,
                config,
                root_moves.as_slice(),
                &candidate,
                plan,
            );
        if injected_progress_head_replaces_concrete_progress_cluster_with_window {
            return None;
        }
        if forced_plain_spirit_head_regresses_top_spirit_cluster {
            return None;
        }
        if macro_mode {
            if matches!(plan.head_family, TurnPlanFamily::DrainerKill)
                && !candidate.attacks_opponent_drainer
            {
                return None;
            }
            if matches!(
                plan.head_family,
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            ) && !candidate_progress_surface
                && !completed_plan_override
            {
                return None;
            }
            if !utility_override
                && matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
                && !candidate_spirit_tactical
                && !candidate.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !allow_current_pro_black_turn_six_plain_spirit_head_injection
            {
                return None;
            }
        }
        if matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
            && !top_spirit_surface
            && !candidate_spirit_tactical
            && !utility_override
        {
            let allow_current_pro_spirit_setup = macro_mode
                && (candidate.spirit_same_turn_score_setup_now
                    || candidate.spirit_own_mana_setup_now);
            if !allow_current_pro_spirit_setup
                && !allow_current_pro_black_turn_six_plain_spirit_head_injection
            {
                return None;
            }
        }
        if candidate_unsafe && !top_unsafe && !utility_override {
            return None;
        }
        if existing_candidate.is_none() {
            root_moves.push(candidate);
            Self::sort_root_candidates_by_search_priority(root_moves.as_mut_slice());
        }
        Some(candidate_inputs)
    }

    fn current_pro_black_turn_six_plain_spirit_head_injection_override(
        game: &MonsGame,
        config: AutomoveSearchConfig,
        root_moves: &[ScoredRootMove],
        candidate: &ScoredRootMove,
        plan: &TurnPlan,
    ) -> bool {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number != 6
            || game.mons_moves_count != 0
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || !matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
        {
            return false;
        }

        let candidate_plain_spirit = candidate.spirit_development
            && !candidate.spirit_same_turn_score_setup_now
            && !candidate.spirit_own_mana_setup_now;
        if !candidate_plain_spirit
            || candidate.attacks_opponent_drainer
            || candidate.scores_supermana_this_turn
            || candidate.scores_opponent_mana_this_turn
            || candidate.safe_supermana_pickup_now
            || candidate.safe_opponent_mana_pickup_now
            || candidate.same_turn_score_window_value > 0
            || candidate.mana_handoff_to_opponent
            || candidate.has_roundtrip
        {
            return false;
        }

        root_moves
            .iter()
            .take(4)
            .filter(|root| {
                Self::is_current_pro_black_turn_six_plain_spirit_followup_setup_pair_move(
                    game, candidate, root, config,
                )
            })
            .count()
            >= 2
    }

    fn is_current_pro_black_turn_six_plain_spirit_followup_setup_pair_move(
        game: &MonsGame,
        plain: &ScoredRootMove,
        setup: &ScoredRootMove,
        config: AutomoveSearchConfig,
    ) -> bool {
        matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && game.active_color == Color::Black
            && game.turn_number == 6
            && game.mons_moves_count == 0
            && game.player_can_use_action()
            && game.player_can_move_mana()
            && Self::turn_engine_root_move_family(plain) == TurnPlanFamily::SpiritImpact
            && Self::turn_engine_root_move_family(setup) == TurnPlanFamily::SpiritImpact
            && plain.spirit_development
            && !plain.spirit_same_turn_score_setup_now
            && !plain.spirit_own_mana_setup_now
            && setup.spirit_own_mana_setup_now
            && !setup.spirit_same_turn_score_setup_now
            && plain.inputs.first() == setup.inputs.first()
            && plain.own_drainer_vulnerable == setup.own_drainer_vulnerable
            && plain.own_drainer_walk_vulnerable == setup.own_drainer_walk_vulnerable
            && !plain.mana_handoff_to_opponent
            && !setup.mana_handoff_to_opponent
            && !plain.has_roundtrip
            && !setup.has_roundtrip
            && !plain.wins_immediately
            && !setup.wins_immediately
            && !plain.attacks_opponent_drainer
            && !setup.attacks_opponent_drainer
            && !plain.scores_supermana_this_turn
            && !setup.scores_supermana_this_turn
            && !plain.scores_opponent_mana_this_turn
            && !setup.scores_opponent_mana_this_turn
            && !plain.safe_supermana_pickup_now
            && !setup.safe_supermana_pickup_now
            && !plain.safe_opponent_mana_pickup_now
            && !setup.safe_opponent_mana_pickup_now
            && plain.same_turn_score_window_value == 0
            && setup.same_turn_score_window_value == 0
            && !plain.supermana_progress
            && !setup.supermana_progress
            && !plain.opponent_mana_progress
            && !setup.opponent_mana_progress
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn accept_turn_engine_head_after_search(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        scored_roots: &[RootEvaluation],
        selected_inputs: &[Input],
        plan: &TurnPlan,
    ) -> bool {
        let macro_mode = Self::turn_engine_mode_uses_macro_plans(config.turn_engine_mode);
        let Some(candidate_inputs) = plan.compiled_chunks.first() else {
            return false;
        };
        let Some(candidate_index) = scored_roots
            .iter()
            .position(|candidate| candidate.inputs.as_slice() == candidate_inputs.as_slice())
        else {
            return false;
        };
        let Some(selected_index) = scored_roots
            .iter()
            .position(|candidate| candidate.inputs.as_slice() == selected_inputs)
        else {
            return false;
        };
        if candidate_index == selected_index {
            return true;
        }

        let candidate = &scored_roots[candidate_index];
        let selected = &scored_roots[selected_index];
        if selected.wins_immediately && !candidate.wins_immediately {
            return false;
        }

        let candidate_unsafe = Self::turn_engine_root_evaluation_is_unsafe(candidate);
        let selected_unsafe = Self::turn_engine_root_evaluation_is_unsafe(selected);
        let candidate_progress_surface =
            Self::turn_engine_root_evaluation_has_progress_surface(candidate);
        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);

        let score_gap = selected.score.saturating_sub(candidate.score);
        let same_turn_window_better =
            candidate.same_turn_score_window_value > selected.same_turn_score_window_value;
        let drainer_attack_better =
            candidate.attacks_opponent_drainer && !selected.attacks_opponent_drainer;
        let scores_now_better = (candidate.scores_supermana_this_turn
            || candidate.scores_opponent_mana_this_turn)
            && !(selected.scores_supermana_this_turn || selected.scores_opponent_mana_this_turn);
        let safety_recover_better = candidate.classes.drainer_safety_recover
            && !selected.classes.drainer_safety_recover
            && selected.own_drainer_vulnerable
            && !candidate.own_drainer_vulnerable;
        let spirit_window_better = candidate.spirit_same_turn_score_setup_now
            && !selected.spirit_same_turn_score_setup_now
            && candidate.same_turn_score_window_value >= selected.same_turn_score_window_value;
        let spirit_development_better =
            candidate.spirit_development && !selected.spirit_development;
        let candidate_spirit_tactical = candidate.spirit_same_turn_score_setup_now
            || candidate.same_turn_score_window_value > 0
            || candidate.attacks_opponent_drainer
            || candidate.scores_supermana_this_turn
            || candidate.scores_opponent_mana_this_turn
            || candidate.safe_supermana_pickup_now
            || candidate.safe_opponent_mana_pickup_now;
        let narrow_black_mana_only_unsafe_score_override =
            matches!(plan.head_family, TurnPlanFamily::ImmediateScore)
                && game.active_color == Color::Black
                && game.turn_number <= 4
                && game.mons_moves_count == 0
                && !game.player_can_use_action()
                && game.player_can_move_mana()
                && candidate_unsafe
                && !selected_unsafe
                && !candidate.wins_immediately
                && !scores_now_better
                && !drainer_attack_better
                && candidate.same_turn_score_window_value <= selected.same_turn_score_window_value;
        let progress_better = (Self::root_progress_steps_better(
            candidate.safe_supermana_progress_steps,
            selected.safe_supermana_progress_steps,
        ) || Self::root_progress_steps_better(
            candidate.safe_opponent_mana_progress_steps,
            selected.safe_opponent_mana_progress_steps,
        )) && !selected.wins_immediately
            && !selected.attacks_opponent_drainer
            && !selected.spirit_same_turn_score_setup_now;
        let selected_spirit_phase = selected.spirit_development
            || selected.spirit_same_turn_score_setup_now
            || selected.spirit_own_mana_setup_now;
        let candidate_family = Self::turn_engine_root_evaluation_family(candidate);
        let selected_family = Self::turn_engine_root_evaluation_family(selected);
        let mut candidate_utility = None;
        let mut candidate_utility_value = || {
            *candidate_utility.get_or_insert_with(|| {
                Self::turn_engine_selected_override_utility(
                    game,
                    candidate,
                    perspective,
                    config,
                    candidate_family,
                )
            })
        };
        let mut selected_utility = None;
        let mut selected_utility_value = || {
            *selected_utility.get_or_insert_with(|| {
                Self::turn_engine_selected_override_utility(
                    game,
                    selected,
                    perspective,
                    config,
                    selected_family,
                )
            })
        };
        let black_plain_spirit_followup_head_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
                && Self::is_current_pro_black_plain_spirit_followup_setup_pair(
                    game, candidate, selected, config,
                )
                && candidate.score > selected.score;
        let black_turn_six_route_change_plain_spirit_head_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::Black
                && game.turn_number == 6
                && game.mons_moves_count == 0
                && game.player_can_use_action()
                && game.player_can_move_mana()
                && matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
                && matches!(plan.goal_family, TurnPlanFamily::SpiritImpact)
                && candidate_index <= 1
                && candidate_unsafe == selected_unsafe
                && candidate.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !candidate_progress_surface
                && !candidate_spirit_tactical
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && selected_unsafe
                && !selected.spirit_development
                && !selected.spirit_same_turn_score_setup_now
                && !selected.spirit_own_mana_setup_now
                && !Self::turn_engine_root_evaluation_has_progress_surface(selected)
                && !selected.wins_immediately
                && !selected.attacks_opponent_drainer
                && !selected.scores_supermana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !selected.safe_supermana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && selected.same_turn_score_window_value == 0
                && candidate.inputs.first() != selected.inputs.first()
                && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                && candidate.own_drainer_walk_vulnerable == selected.own_drainer_walk_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && score_gap <= 1024
                && candidate_utility_value()
                    .supports_primary_axes_eval_tolerance(selected_utility_value(), 64);
        let white_spirit_setup_gain_head_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
                && !selected_unsafe
                && !candidate_unsafe
                && candidate.spirit_own_mana_setup_now
                && !candidate.spirit_same_turn_score_setup_now
                && !selected.spirit_own_mana_setup_now
                && !selected.spirit_same_turn_score_setup_now
                && selected.spirit_development
                && candidate.spirit_development
                && candidate.spirit_setup_gain >= selected.spirit_setup_gain.saturating_add(32)
                && candidate.safe_supermana_progress_steps
                    <= selected.safe_supermana_progress_steps
                && candidate.safe_opponent_mana_progress_steps
                    <= selected.safe_opponent_mana_progress_steps
                && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && !scores_now_better
                && !drainer_attack_better
                && score_gap <= 96;
        let early_white_safe_mana_blocks_setup_head_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && game.turn_number == 3
                && game.mons_moves_count <= 2
                && game.player_can_use_action()
                && game.player_can_move_mana()
                && matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
                && Self::turn_engine_root_evaluation_family(selected) == TurnPlanFamily::ManaTempo
                && !selected_unsafe
                && !candidate_unsafe
                && !selected.spirit_development
                && !selected.spirit_same_turn_score_setup_now
                && !selected.spirit_own_mana_setup_now
                && candidate.spirit_own_mana_setup_now
                && !candidate.spirit_same_turn_score_setup_now
                && candidate.spirit_development
                && !candidate.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && candidate.same_turn_score_window_value == 0
                && !scores_now_better
                && !drainer_attack_better;
        let black_turn_start_safe_mana_blocks_plain_spirit_head_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::Black
                && game.turn_number >= 5
                && game.mons_moves_count == 0
                && game.player_can_use_action()
                && game.player_can_move_mana()
                && matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
                && Self::turn_engine_root_evaluation_family(selected) == TurnPlanFamily::ManaTempo
                && !selected.spirit_development
                && !selected.spirit_same_turn_score_setup_now
                && !selected.spirit_own_mana_setup_now
                && Self::is_plain_spirit_development_root(candidate)
                && !candidate_progress_surface
                && !candidate_spirit_tactical
                && candidate.inputs.first() == selected.inputs.first()
                && candidate_unsafe == selected_unsafe
                && candidate.safe_supermana_progress_steps
                    >= selected.safe_supermana_progress_steps
                && candidate.safe_opponent_mana_progress_steps
                    >= selected.safe_opponent_mana_progress_steps
                && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                && candidate.own_drainer_walk_vulnerable == selected.own_drainer_walk_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better
                && score_gap > 96;
        let white_turn_start_safe_mana_blocks_plain_spirit_head_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && game.turn_number >= 5
                && game.mons_moves_count == 0
                && game.player_can_use_action()
                && game.player_can_move_mana()
                && matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
                && Self::turn_engine_root_evaluation_family(selected) == TurnPlanFamily::ManaTempo
                && !selected_unsafe
                && !candidate_unsafe
                && !selected.spirit_development
                && !selected.spirit_same_turn_score_setup_now
                && !selected.spirit_own_mana_setup_now
                && Self::is_plain_spirit_development_root(candidate)
                && candidate_progress_surface
                && !candidate_spirit_tactical
                && candidate.inputs.first() == selected.inputs.first()
                && candidate.safe_supermana_progress_steps
                    >= selected.safe_supermana_progress_steps
                && candidate.safe_opponent_mana_progress_steps
                    >= selected.safe_opponent_mana_progress_steps
                && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                && candidate.own_drainer_walk_vulnerable == selected.own_drainer_walk_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && candidate.score <= selected.score
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better;
        let white_late_safe_mana_blocks_plain_spirit_head_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && game.turn_number >= 5
                && game.mons_moves_count >= 1
                && matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
                && Self::turn_engine_root_evaluation_family(selected) == TurnPlanFamily::ManaTempo
                && !selected.spirit_development
                && !selected.spirit_same_turn_score_setup_now
                && !selected.spirit_own_mana_setup_now
                && candidate.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !candidate_progress_surface
                && !candidate_spirit_tactical
                && candidate.same_turn_score_window_value == 0
                && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                && candidate.own_drainer_walk_vulnerable == selected.own_drainer_walk_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && candidate.score <= selected.score
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better;
        let black_no_action_vulnerable_progress_head_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::Black
                && game.turn_number >= 6
                && game.mons_moves_count == 0
                && !game.player_can_use_action()
                && game.player_can_move_mana()
                && matches!(
                    candidate_family,
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && !selected.spirit_development
                && !selected.spirit_same_turn_score_setup_now
                && !selected.spirit_own_mana_setup_now
                && !candidate.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && candidate.same_turn_score_window_value == 0
                && selected.same_turn_score_window_value == 0
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && candidate.own_drainer_vulnerable
                && !selected.own_drainer_vulnerable
                && candidate.own_drainer_walk_vulnerable == selected.own_drainer_walk_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better;
        let black_early_same_window_mana_head_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::Black
                && game.turn_number <= 4
                && !game.player_can_use_action()
                && game.player_can_move_mana()
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && candidate.same_turn_score_window_value > 0
                && candidate.same_turn_score_window_value == selected.same_turn_score_window_value
                && candidate.safe_supermana_progress_steps
                    == selected.safe_supermana_progress_steps
                && candidate.safe_opponent_mana_progress_steps
                    == selected.safe_opponent_mana_progress_steps
                && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                && candidate.own_drainer_walk_vulnerable == selected.own_drainer_walk_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && !scores_now_better
                && !drainer_attack_better;
        let black_no_action_windowed_vulnerable_mana_head_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::Black
                && game.turn_number >= 6
                && game.mons_moves_count == 0
                && !game.player_can_use_action()
                && game.player_can_move_mana()
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && candidate.same_turn_score_window_value > 0
                && candidate.same_turn_score_window_value == selected.same_turn_score_window_value
                && candidate.safe_supermana_progress_steps
                    == selected.safe_supermana_progress_steps
                && candidate.safe_opponent_mana_progress_steps
                    == selected.safe_opponent_mana_progress_steps
                && candidate.own_drainer_vulnerable
                && !selected.own_drainer_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && !scores_now_better
                && !drainer_attack_better;
        let black_late_windowed_vulnerable_mana_head_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::Black
                && game.turn_number >= 8
                && game.mons_moves_count >= 1
                && !game.player_can_use_action()
                && game.player_can_move_mana()
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && candidate.inputs.first() == selected.inputs.first()
                && candidate.same_turn_score_window_value > 0
                && selected.same_turn_score_window_value == 0
                && candidate.own_drainer_vulnerable
                && !selected.own_drainer_vulnerable
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && candidate.score.saturating_sub(selected.score) <= 32
                && !scores_now_better
                && !drainer_attack_better;
        if matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) {
            let selected_utility = selected_utility_value();
            let candidate_utility = candidate_utility_value();
            let black_non_concrete_window_head_blocks_spirit_progress = game.active_color
                == Color::Black
                && game.turn_number <= 6
                && matches!(
                    plan.head_family,
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
                && candidate_family == TurnPlanFamily::ManaTempo
                && selected_family == TurnPlanFamily::SpiritImpact
                && Self::is_current_pro_non_concrete_mana_window_root(candidate)
                && Self::turn_engine_root_evaluation_has_progress_surface(selected)
                && crate::models::automove_turn_engine::compare_utility_primary_axes(
                    plan.head_utility,
                    selected_utility,
                ) == std::cmp::Ordering::Less;
            if black_non_concrete_window_head_blocks_spirit_progress {
                return false;
            }
            let plan_dominates_selected =
                crate::models::automove_turn_engine::compare_utility_primary_axes(
                    plan.utility,
                    selected_utility,
                ) == std::cmp::Ordering::Greater
                    && (plan
                        .utility
                        .strictly_dominates_override_axes(selected_utility)
                        || plan
                            .head_utility
                            .strictly_dominates_override_axes(selected_utility));
            let candidate_dominates_selected =
                crate::models::automove_turn_engine::compare_utility_primary_axes(
                    candidate_utility,
                    selected_utility,
                ) == std::cmp::Ordering::Greater
                    && candidate_utility.strictly_dominates_override_axes(selected_utility);
            if !black_turn_six_route_change_plain_spirit_head_override
                && !white_spirit_setup_gain_head_override
                && !plan_dominates_selected
                && !candidate_dominates_selected
            {
                return false;
            }
        }
        if early_white_safe_mana_blocks_setup_head_override {
            return false;
        }
        if black_turn_start_safe_mana_blocks_plain_spirit_head_override {
            return false;
        }
        if white_turn_start_safe_mana_blocks_plain_spirit_head_override {
            return false;
        }
        if white_late_safe_mana_blocks_plain_spirit_head_override {
            return false;
        }
        if black_no_action_vulnerable_progress_head_override {
            return false;
        }
        if black_early_same_window_mana_head_override {
            return false;
        }
        if black_no_action_windowed_vulnerable_mana_head_override {
            return false;
        }
        if black_late_windowed_vulnerable_mana_head_override {
            return false;
        }
        let selected_progress_surface =
            Self::turn_engine_root_evaluation_has_progress_surface(selected);
        let pickup_upgrade = (candidate.safe_supermana_pickup_now
            && !selected.safe_supermana_pickup_now)
            || (candidate.safe_opponent_mana_pickup_now && !selected.safe_opponent_mana_pickup_now);
        let selected_early_black_progress_root_blocks_mana_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::Black
                && game.turn_number <= 4
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(
                    selected_family,
                    TurnPlanFamily::ManaTempo
                        | TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && !candidate.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !candidate.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !pickup_upgrade
                && score_gap > 0
                && (Self::root_progress_or_setup_better(selected, candidate)
                    || (selected_progress_surface && !candidate_progress_surface));
        let selected_early_black_progress_root_blocks_non_concrete_window_mana_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::Black
                && game.turn_number <= 4
                && matches!(
                    plan.head_family,
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && candidate.same_turn_score_window_value > 0
                && selected.same_turn_score_window_value == 0
                && !candidate.wins_immediately
                && !scores_now_better
                && !drainer_attack_better
                && !pickup_upgrade
                && selected.own_drainer_vulnerable == candidate.own_drainer_vulnerable
                && selected.own_drainer_walk_vulnerable == candidate.own_drainer_walk_vulnerable
                && selected.mana_handoff_to_opponent == candidate.mana_handoff_to_opponent
                && selected.has_roundtrip == candidate.has_roundtrip
                && Self::root_progress_or_setup_better(selected, candidate)
                && score_gap >= -192
                && !plan
                    .utility
                    .improves_non_score_override_axes(selected_utility_value());
        if selected_early_black_progress_root_blocks_mana_head {
            return false;
        }
        if selected_early_black_progress_root_blocks_non_concrete_window_mana_head {
            return false;
        }
        let near_tie_progress = candidate.safe_supermana_progress_steps
            == selected.safe_supermana_progress_steps
            && candidate.safe_opponent_mana_progress_steps
                == selected.safe_opponent_mana_progress_steps
            && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
            && candidate.efficiency == selected.efficiency
            && candidate.supermana_progress == selected.supermana_progress
            && candidate.opponent_mana_progress == selected.opponent_mana_progress;
        let primary_axes_order = crate::models::automove_turn_engine::compare_utility_primary_axes(
            plan.utility,
            selected_utility_value(),
        );
        let strategic_override_axes_better = plan
            .utility
            .improves_non_score_override_axes(selected_utility_value());
        let selected_early_black_safe_mana_root_blocks_weaker_mana_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::Black
                && game.turn_number <= 4
                && game.mons_moves_count >= 1
                && game.player_can_use_action()
                && game.player_can_move_mana()
                && matches!(plan.head_family, TurnPlanFamily::ManaTempo)
                && matches!(plan.goal_family, TurnPlanFamily::SpiritImpact)
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && !candidate_unsafe
                && !selected_unsafe
                && !candidate.spirit_development
                && !selected.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !selected.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !selected.spirit_own_mana_setup_now
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && candidate.same_turn_score_window_value == selected.same_turn_score_window_value
                && !candidate_progress_surface
                && !selected_progress_surface
                && candidate.safe_supermana_progress_steps
                    == selected.safe_supermana_progress_steps
                && candidate.safe_opponent_mana_progress_steps
                    == selected.safe_opponent_mana_progress_steps
                && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                && candidate.own_drainer_walk_vulnerable == selected.own_drainer_walk_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && selected.score > candidate.score.saturating_add(128)
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better
                && !pickup_upgrade;
        let selected_black_quiet_mana_root_blocks_lower_scored_mana_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::Black
                && game.turn_number <= 6
                && game.mons_moves_count >= 1
                && game.player_can_move_mana()
                && matches!(
                    plan.head_family,
                    TurnPlanFamily::ManaTempo
                        | TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && candidate_unsafe == selected_unsafe
                && !candidate.spirit_development
                && !selected.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !selected.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !selected.spirit_own_mana_setup_now
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && candidate.same_turn_score_window_value == 0
                && selected.same_turn_score_window_value == 0
                && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                && candidate.own_drainer_walk_vulnerable == selected.own_drainer_walk_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && selected.score > candidate.score.saturating_add(48)
                && selected.safe_supermana_progress_steps
                    <= candidate.safe_supermana_progress_steps.saturating_add(1)
                && selected.safe_opponent_mana_progress_steps
                    <= candidate
                        .safe_opponent_mana_progress_steps
                        .saturating_add(1)
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better
                && !pickup_upgrade;
        let selected_white_same_window_mana_root_blocks_lower_scored_mana_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && game.turn_number == 5
                && game.mons_moves_count == 0
                && game.player_can_move_mana()
                && matches!(
                    plan.head_family,
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && candidate_unsafe == selected_unsafe
                && !candidate.spirit_development
                && !selected.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !selected.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !selected.spirit_own_mana_setup_now
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && candidate.same_turn_score_window_value > 0
                && candidate.same_turn_score_window_value == selected.same_turn_score_window_value
                && !candidate_progress_surface
                && !selected_progress_surface
                && candidate.safe_supermana_progress_steps
                    == selected.safe_supermana_progress_steps
                && candidate.safe_opponent_mana_progress_steps
                    == selected.safe_opponent_mana_progress_steps
                && candidate.inputs.first() == selected.inputs.first()
                && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                && candidate.own_drainer_walk_vulnerable == selected.own_drainer_walk_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && selected.score > candidate.score
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better
                && !pickup_upgrade;
        let selected_white_mid_turn_mana_root_blocks_lower_scored_window_mana_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && game.turn_number == 5
                && game.mons_moves_count >= 1
                && game.player_can_move_mana()
                && matches!(
                    plan.head_family,
                    TurnPlanFamily::ImmediateScore
                        | TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && candidate_unsafe == selected_unsafe
                && !candidate.spirit_development
                && !selected.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !selected.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !selected.spirit_own_mana_setup_now
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && candidate.same_turn_score_window_value > 0
                && candidate.same_turn_score_window_value == selected.same_turn_score_window_value
                && !candidate_progress_surface
                && !selected_progress_surface
                && candidate.safe_supermana_progress_steps
                    == selected.safe_supermana_progress_steps
                && candidate.safe_opponent_mana_progress_steps
                    == selected.safe_opponent_mana_progress_steps
                && selected.score_path_best_steps >= candidate.score_path_best_steps
                && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                && candidate.own_drainer_walk_vulnerable == selected.own_drainer_walk_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && selected.score > candidate.score
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better
                && !pickup_upgrade;
        let selected_white_mid_turn_spirit_setup_root_blocks_window_mana_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && game.turn_number == 5
                && game.mons_moves_count >= 1
                && game.player_can_use_action()
                && game.player_can_move_mana()
                && matches!(
                    plan.head_family,
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(selected_family, TurnPlanFamily::SpiritImpact)
                && candidate_unsafe == selected_unsafe
                && !candidate.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && selected.spirit_development
                && selected.spirit_same_turn_score_setup_now
                && selected.spirit_own_mana_setup_now
                && selected.spirit_setup_gain >= candidate.spirit_setup_gain.saturating_add(64)
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && candidate.same_turn_score_window_value > 0
                && candidate.same_turn_score_window_value == selected.same_turn_score_window_value
                && !candidate_progress_surface
                && !selected_progress_surface
                && candidate.safe_supermana_progress_steps
                    == selected.safe_supermana_progress_steps
                && candidate.safe_opponent_mana_progress_steps
                    == selected.safe_opponent_mana_progress_steps
                && selected.score_path_best_steps > candidate.score_path_best_steps
                && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                && candidate.own_drainer_walk_vulnerable == selected.own_drainer_walk_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && candidate.score <= selected.score.saturating_add(64)
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better
                && !pickup_upgrade;
        let selected_white_turn_start_spirit_setup_root_blocks_window_mana_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && game.turn_number == 5
                && game.mons_moves_count == 0
                && game.player_can_use_action()
                && game.player_can_move_mana()
                && exact_context.delta.same_turn_score_window_value <= 1
                && exact_context.delta.opponent_window_deny_gain <= 1
                && !exact_context.delta.drainer_attack_available
                && (exact_context.delta.same_turn_score_window_value > 0
                    || exact_context.delta.opponent_window_deny_gain > 0)
                && matches!(
                    plan.head_family,
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
                && Self::is_current_pro_non_concrete_mana_window_root(candidate)
                && matches!(selected_family, TurnPlanFamily::SpiritImpact)
                && candidate_unsafe
                && !selected_unsafe
                && !candidate.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && selected.spirit_development
                && selected.spirit_same_turn_score_setup_now
                && !selected.spirit_own_mana_setup_now
                && selected.spirit_setup_gain >= candidate.spirit_setup_gain.saturating_add(96)
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && candidate.same_turn_score_window_value > 0
                && candidate.same_turn_score_window_value == selected.same_turn_score_window_value
                && !candidate_progress_surface
                && !selected_progress_surface
                && candidate.safe_supermana_progress_steps
                    == selected.safe_supermana_progress_steps
                && candidate.safe_opponent_mana_progress_steps
                    == selected.safe_opponent_mana_progress_steps
                && selected.score_path_best_steps > candidate.score_path_best_steps
                && candidate.own_drainer_vulnerable
                && !selected.own_drainer_vulnerable
                && candidate.own_drainer_walk_vulnerable == selected.own_drainer_walk_vulnerable
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better
                && !pickup_upgrade;
        let selected_white_safe_mana_root_blocks_deferred_recovery_progress_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && game.turn_number == 5
                && game.mons_moves_count >= 1
                && !game.player_can_use_action()
                && game.player_can_move_mana()
                && matches!(
                    plan.head_family,
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && matches!(plan.goal_family, TurnPlanFamily::DrainerSafetyRecovery)
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && !candidate_unsafe
                && !selected_unsafe
                && !candidate_progress_surface
                && !selected_progress_surface
                && !candidate.spirit_development
                && !selected.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !selected.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !selected.spirit_own_mana_setup_now
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && candidate.same_turn_score_window_value == 0
                && selected.same_turn_score_window_value == 0
                && candidate.safe_supermana_progress_steps
                    <= selected.safe_supermana_progress_steps
                && candidate.safe_opponent_mana_progress_steps
                    <= selected.safe_opponent_mana_progress_steps
                && candidate.score_path_best_steps == selected.score_path_best_steps
                && candidate.spirit_setup_gain <= selected.spirit_setup_gain.saturating_add(16)
                && !candidate.own_drainer_vulnerable
                && !selected.own_drainer_vulnerable
                && !candidate.own_drainer_walk_vulnerable
                && !selected.own_drainer_walk_vulnerable
                && !candidate.mana_handoff_to_opponent
                && !selected.mana_handoff_to_opponent
                && !candidate.has_roundtrip
                && !selected.has_roundtrip
                && candidate.root_rank >= selected.root_rank.saturating_add(8)
                && candidate.score.saturating_sub(selected.score) <= 128
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better
                && !pickup_upgrade
                && !plan
                    .head_utility
                    .supports_primary_axes_eval_tolerance(selected_utility_value(), 192);
        let selected_white_safe_mana_root_blocks_vulnerable_mana_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && game.turn_number == 3
                && game.mons_moves_count >= 1
                && game.player_can_move_mana()
                && matches!(
                    plan.head_family,
                    TurnPlanFamily::ManaTempo
                        | TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && matches!(plan.goal_family, TurnPlanFamily::DrainerSafetyRecovery)
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && candidate_unsafe
                && !selected_unsafe
                && !candidate.spirit_development
                && !selected.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !selected.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !selected.spirit_own_mana_setup_now
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && candidate.same_turn_score_window_value == 0
                && selected.same_turn_score_window_value == 0
                && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                && candidate.has_roundtrip == selected.has_roundtrip
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better
                && !pickup_upgrade;
        let selected_black_late_safe_progress_root_blocks_quiet_mana_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::Black
                && game.turn_number == 6
                && game.mons_moves_count == 0
                && !game.player_can_use_action()
                && game.player_can_move_mana()
                && exact_context.delta.same_turn_score_window_value <= 1
                && exact_context.delta.opponent_window_deny_gain <= 1
                && !exact_context.delta.drainer_attack_available
                && (exact_context.delta.same_turn_score_window_value > 0
                    || exact_context.delta.opponent_window_deny_gain > 0)
                && matches!(plan.head_family, TurnPlanFamily::ManaTempo)
                && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
                && matches!(candidate_family, TurnPlanFamily::ManaTempo)
                && matches!(
                    selected_family,
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && !candidate_unsafe
                && !candidate.spirit_development
                && !selected.spirit_development
                && !candidate.spirit_same_turn_score_setup_now
                && !selected.spirit_same_turn_score_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !selected.spirit_own_mana_setup_now
                && !candidate.wins_immediately
                && !selected.wins_immediately
                && !candidate.attacks_opponent_drainer
                && !selected.attacks_opponent_drainer
                && !candidate.scores_supermana_this_turn
                && !selected.scores_supermana_this_turn
                && !candidate.scores_opponent_mana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !candidate.safe_supermana_pickup_now
                && !selected.safe_supermana_pickup_now
                && !candidate.safe_opponent_mana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && candidate.same_turn_score_window_value == 0
                && selected.same_turn_score_window_value == 0
                && !candidate_progress_surface
                && Self::turn_engine_root_evaluation_has_progress_surface(selected)
                && selected.safe_supermana_progress_steps < candidate.safe_supermana_progress_steps
                && selected.safe_opponent_mana_progress_steps
                    < candidate.safe_opponent_mana_progress_steps
                && selected.score_path_best_steps == candidate.score_path_best_steps
                && selected.own_drainer_vulnerable
                && !candidate.own_drainer_vulnerable
                && selected.own_drainer_walk_vulnerable == candidate.own_drainer_walk_vulnerable
                && selected.mana_handoff_to_opponent == candidate.mana_handoff_to_opponent
                && selected.has_roundtrip == candidate.has_roundtrip
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better
                && !pickup_upgrade;
        let selected_black_recovery_root_blocks_non_concrete_window_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::Black
                && game.turn_number == 4
                && game.mons_moves_count >= 1
                && !game.player_can_use_action()
                && game.player_can_move_mana()
                && exact_context.delta.same_turn_score_window_value <= 1
                && exact_context.delta.opponent_window_deny_gain <= 1
                && !exact_context.delta.drainer_attack_available
                && exact_context.delta.drainer_safety < 0
                && (exact_context.delta.same_turn_score_window_value > 0
                    || exact_context.delta.opponent_window_deny_gain > 0)
                && matches!(plan.head_family, TurnPlanFamily::ImmediateScore)
                && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
                && Self::is_current_pro_non_concrete_mana_window_root(candidate)
                && matches!(selected_family, TurnPlanFamily::DrainerSafetyRecovery)
                && selected.classes.drainer_safety_recover
                && !selected_unsafe
                && !selected.own_drainer_vulnerable
                && !selected.own_drainer_walk_vulnerable
                && !selected.wins_immediately
                && !selected.attacks_opponent_drainer
                && !selected.spirit_development
                && !selected.spirit_same_turn_score_setup_now
                && !selected.spirit_own_mana_setup_now
                && selected.same_turn_score_window_value == 0
                && !selected.scores_supermana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !selected.safe_supermana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && !selected.mana_handoff_to_opponent
                && !selected.has_roundtrip
                && candidate.inputs.first() == selected.inputs.first()
                && candidate.own_drainer_vulnerable
                && !candidate.classes.drainer_safety_recover
                && selected.safe_supermana_progress_steps
                    <= candidate.safe_supermana_progress_steps
                && selected.safe_opponent_mana_progress_steps
                    <= candidate.safe_opponent_mana_progress_steps
                && selected.score_path_best_steps > candidate.score_path_best_steps
                && !scores_now_better
                && !drainer_attack_better
                && !pickup_upgrade;
        let selected_white_recovery_root_blocks_non_concrete_window_head =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && game.turn_number == 3
                && game.mons_moves_count == 0
                && !game.player_can_use_action()
                && game.player_can_move_mana()
                && matches!(
                    plan.head_family,
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
                && Self::is_current_pro_non_concrete_mana_window_root(candidate)
                && matches!(selected_family, TurnPlanFamily::DrainerSafetyRecovery)
                && selected.classes.drainer_safety_recover
                && !selected_unsafe
                && !selected.own_drainer_vulnerable
                && !selected.own_drainer_walk_vulnerable
                && !selected.wins_immediately
                && !selected.attacks_opponent_drainer
                && !selected.spirit_development
                && !selected.spirit_same_turn_score_setup_now
                && !selected.spirit_own_mana_setup_now
                && selected.same_turn_score_window_value == 0
                && !selected.scores_supermana_this_turn
                && !selected.scores_opponent_mana_this_turn
                && !selected.safe_supermana_pickup_now
                && !selected.safe_opponent_mana_pickup_now
                && !selected.mana_handoff_to_opponent
                && !selected.has_roundtrip
                && candidate.inputs.first() == selected.inputs.first()
                && candidate.own_drainer_vulnerable
                && !candidate.classes.drainer_safety_recover
                && selected.safe_supermana_progress_steps
                    <= candidate.safe_supermana_progress_steps
                && selected.safe_opponent_mana_progress_steps
                    <= candidate.safe_opponent_mana_progress_steps
                && selected.score_path_best_steps > candidate.score_path_best_steps
                && !scores_now_better
                && !drainer_attack_better
                && !pickup_upgrade;
        if selected_early_black_safe_mana_root_blocks_weaker_mana_head {
            return false;
        }
        if selected_black_quiet_mana_root_blocks_lower_scored_mana_head {
            return false;
        }
        if selected_white_same_window_mana_root_blocks_lower_scored_mana_head {
            return false;
        }
        if selected_white_mid_turn_mana_root_blocks_lower_scored_window_mana_head {
            return false;
        }
        if selected_white_mid_turn_spirit_setup_root_blocks_window_mana_head {
            return false;
        }
        if selected_white_turn_start_spirit_setup_root_blocks_window_mana_head {
            return false;
        }
        if selected_white_safe_mana_root_blocks_deferred_recovery_progress_head {
            return false;
        }
        if selected_white_safe_mana_root_blocks_vulnerable_mana_head {
            return false;
        }
        if selected_black_late_safe_progress_root_blocks_quiet_mana_head {
            return false;
        }
        if selected_black_recovery_root_blocks_non_concrete_window_head {
            return false;
        }
        if selected_white_recovery_root_blocks_non_concrete_window_head {
            return false;
        }
        let narrow_white_mana_only_progress_tie_override = macro_mode
            && matches!(
                plan.head_family,
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            )
            && game.active_color == Color::White
            && game.turn_number >= 5
            && game.mons_moves_count <= 1
            && !game.player_can_use_action()
            && game.player_can_move_mana()
            && !candidate_unsafe
            && !selected_unsafe
            && candidate_progress_surface
            && selected_progress_surface
            && !selected_spirit_phase
            && near_tie_progress
            && score_gap > 48
            && primary_axes_order == std::cmp::Ordering::Equal
            && !pickup_upgrade
            && !strategic_override_axes_better;
        let mut projected_state = game.clone_for_simulation();
        let mut projected_events = Vec::new();
        let mut projected_complete = true;
        for chunk in plan.compiled_chunks.iter() {
            let Some((after, events)) =
                Self::apply_inputs_for_search_with_events(&projected_state, chunk.as_slice())
            else {
                projected_complete = false;
                break;
            };
            projected_events.extend(events);
            projected_state = after;
        }
        let projected_turn_finished = projected_complete
            && (projected_state.winner_color().is_some()
                || projected_state.active_color != perspective
                || (!projected_state.player_can_move_mon()
                    && !projected_state.player_can_use_action()
                    && !projected_state.player_can_move_mana()));
        let projected_near_turn_completion = projected_complete
            && (projected_turn_finished
                || plan.compiled_chunks.len() >= 4
                || !projected_state.player_can_move_mon()
                || (!projected_state.player_can_use_action()
                    && !projected_state.player_can_move_mana()));
        let projected_mana_handoff = projected_complete
            && Self::mana_handoff_penalty(
                projected_events.as_slice(),
                perspective,
                config.root_mana_handoff_penalty.max(1),
            ) > 0;
        let projected_roundtrip =
            projected_complete && Self::has_roundtrip_mon_move(projected_events.as_slice());
        let projected_end_vulnerable = projected_complete
            && projected_state.winner_color() != Some(perspective)
            && Self::is_own_drainer_vulnerable_next_turn(&projected_state, perspective, true);
        let projected_end_safe = projected_complete
            && projected_near_turn_completion
            && !projected_mana_handoff
            && !projected_roundtrip
            && !projected_end_vulnerable;
        let projected_reply_floor_not_worse =
            crate::models::automove_turn_engine::compare_utility_primary_axes(
                plan.utility,
                selected_utility_value(),
            ) != std::cmp::Ordering::Less;
        let projected_head_primary_axes_not_worse =
            crate::models::automove_turn_engine::compare_utility_primary_axes(
                plan.head_utility,
                selected_utility_value(),
            ) != std::cmp::Ordering::Less;
        let selected_safe_pickup_now = !selected_unsafe
            && (selected.safe_supermana_pickup_now || selected.safe_opponent_mana_pickup_now);
        let projected_progress_head_regresses_safe_pickup = macro_mode
            && selected_safe_pickup_now
            && matches!(
                plan.head_family,
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            )
            && !pickup_upgrade
            && !projected_head_primary_axes_not_worse;
        let projected_deferred_recovery_progress_head_without_concrete_gain = macro_mode
            && selected_unsafe
            && candidate_unsafe
            && !selected_spirit_phase
            && !selected_progress_surface
            && candidate_progress_surface
            && matches!(
                plan.head_family,
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            )
            && matches!(plan.goal_family, TurnPlanFamily::DrainerSafetyRecovery)
            && !candidate.classes.drainer_safety_recover
            && candidate.own_drainer_vulnerable
            && !pickup_upgrade
            && !scores_now_better
            && !drainer_attack_better
            && !same_turn_window_better
            && score_gap > 48;
        let projected_completed_plan_override = macro_mode
            && projected_end_safe
            && !selected.wins_immediately
            && !matches!(plan.goal_family, TurnPlanFamily::ManaTempo)
            && candidate_index
                <= if plan.compiled_chunks.len() > 1 {
                    16
                } else {
                    10
                }
            && projected_reply_floor_not_worse
            && (plan.utility.passes_override_guard(selected_utility_value())
                || plan
                    .utility
                    .supports_primary_axes_eval_tolerance(selected_utility_value(), 96))
            && (candidate_unsafe
                || plan.compiled_chunks.len() > 1
                || plan
                    .utility
                    .improves_non_score_override_axes(selected_utility_value()));
        let projected_safe_root_blocks_plain_spirit_override = macro_mode
            && game.active_color == Color::Black
            && matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
            && !selected_unsafe
            && !candidate_unsafe
            && !selected_progress_surface
            && !selected_spirit_phase
            && !candidate_progress_surface
            && !candidate_spirit_tactical
            && !candidate.spirit_own_mana_setup_now
            && score_gap > 96
            && !strategic_override_axes_better;
        let projected_safe_root_blocks_plain_spirit_progress_override = macro_mode
            && game.active_color == Color::Black
            && matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
            && !selected_unsafe
            && !candidate_unsafe
            && !selected_progress_surface
            && !selected_spirit_phase
            && candidate.spirit_development
            && !candidate.spirit_same_turn_score_setup_now
            && !candidate.spirit_own_mana_setup_now
            && candidate_progress_surface
            && !candidate_spirit_tactical
            && !progress_better
            && candidate.safe_supermana_progress_steps >= selected.safe_supermana_progress_steps
            && candidate.safe_opponent_mana_progress_steps
                >= selected.safe_opponent_mana_progress_steps
            && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
            && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
            && candidate.has_roundtrip == selected.has_roundtrip
            && !scores_now_better
            && !drainer_attack_better
            && score_gap > 64
            && !strategic_override_axes_better;
        let projected_plain_spirit_sibling_blocks_override = macro_mode
            && matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
            && !selected_unsafe
            && !candidate_unsafe
            && selected.spirit_development
            && !selected.spirit_same_turn_score_setup_now
            && !selected.spirit_own_mana_setup_now
            && candidate.spirit_development
            && !candidate.spirit_same_turn_score_setup_now
            && !candidate.spirit_own_mana_setup_now
            && score_gap >= 0
            && !progress_better
            && candidate.spirit_setup_gain <= selected.spirit_setup_gain
            && candidate.safe_supermana_progress_steps >= selected.safe_supermana_progress_steps
            && candidate.safe_opponent_mana_progress_steps
                >= selected.safe_opponent_mana_progress_steps
            && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
            && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
            && candidate.has_roundtrip == selected.has_roundtrip
            && !scores_now_better
            && !drainer_attack_better
            && !candidate_spirit_tactical;
        let projected_completed_plan_override = projected_completed_plan_override
            && !projected_safe_root_blocks_plain_spirit_override
            && !projected_safe_root_blocks_plain_spirit_progress_override
            && !projected_plain_spirit_sibling_blocks_override
            && !projected_progress_head_regresses_safe_pickup
            && !projected_deferred_recovery_progress_head_without_concrete_gain
            && !narrow_black_mana_only_unsafe_score_override
            && !narrow_white_mana_only_progress_tie_override;
        if candidate_unsafe && !selected_unsafe && !projected_completed_plan_override {
            return false;
        }
        let allow_non_concrete_white_progress_head = macro_mode
            && matches!(
                plan.head_family,
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            )
            && !candidate_progress_surface
            && Self::current_pro_is_early_white_turn_start(game)
            && Self::is_plain_spirit_development_root(selected)
            && !selected_unsafe
            && (!candidate_unsafe || projected_completed_plan_override)
            && candidate_index <= 3
            && candidate.score >= selected.score
            && plan
                .utility
                .supports_primary_axes_eval_tolerance(selected_utility_value(), 64);
        if white_spirit_setup_gain_head_override {
            return true;
        }
        if macro_mode
            && crate::models::automove_turn_engine::compare_utility_primary_axes(
                selected_utility_value(),
                plan.utility,
            ) == std::cmp::Ordering::Greater
            && !white_spirit_setup_gain_head_override
        {
            return false;
        }
        if macro_mode
            && matches!(
                plan.head_family,
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            )
            && !candidate_progress_surface
            && !projected_completed_plan_override
            && !allow_non_concrete_white_progress_head
        {
            return false;
        }
        if macro_mode
            && matches!(plan.head_family, TurnPlanFamily::DrainerKill)
            && !candidate.attacks_opponent_drainer
        {
            return false;
        }
        let current_pro_secondary_analysis = Self::current_pro_secondary_analysis_live(config);
        let selected_white_setup_recovery_blocks_utility_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && matches!(plan.head_family, TurnPlanFamily::DrainerSafetyRecovery)
                && selected.spirit_own_mana_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !selected_unsafe
                && !candidate_unsafe
                && selected.same_turn_score_window_value == 0
                && candidate.same_turn_score_window_value == 0
                && selected.spirit_setup_gain >= candidate.spirit_setup_gain + 48
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better;
        let selected_white_vulnerable_progress_head_blocks_immediate_score_override =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && game.active_color == Color::White
                && matches!(
                    plan.head_family,
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                )
                && matches!(plan.goal_family, TurnPlanFamily::ImmediateScore)
                && matches!(selected_family, TurnPlanFamily::ManaTempo)
                && !selected_progress_surface
                && candidate_progress_surface
                && selected.own_drainer_vulnerable
                && candidate.own_drainer_vulnerable
                && selected.own_drainer_walk_vulnerable == candidate.own_drainer_walk_vulnerable
                && selected.mana_handoff_to_opponent == candidate.mana_handoff_to_opponent
                && selected.has_roundtrip == candidate.has_roundtrip
                && !selected.spirit_development
                && !candidate.spirit_development
                && !selected.spirit_same_turn_score_setup_now
                && !candidate.spirit_same_turn_score_setup_now
                && !selected.spirit_own_mana_setup_now
                && !candidate.spirit_own_mana_setup_now
                && !scores_now_better
                && !drainer_attack_better
                && !same_turn_window_better
                && !pickup_upgrade
                && !candidate.classes.drainer_safety_recover
                && candidate.safe_supermana_progress_steps.saturating_add(1)
                    >= selected.safe_supermana_progress_steps
                && candidate
                    .safe_opponent_mana_progress_steps
                    .saturating_add(1)
                    >= selected.safe_opponent_mana_progress_steps
                && candidate.score <= selected.score.saturating_add(16)
                && !plan
                    .utility
                    .improves_non_score_override_axes(selected_utility_value());
        if selected_white_vulnerable_progress_head_blocks_immediate_score_override {
            return false;
        }
        let allow_generic_current_pro_override = match plan.head_family {
            TurnPlanFamily::ImmediateScore
            | TurnPlanFamily::DenyOpponentWindow
            | TurnPlanFamily::DrainerKill => true,
            TurnPlanFamily::DrainerSafetyRecovery => current_pro_secondary_analysis,
            TurnPlanFamily::SpiritImpact
            | TurnPlanFamily::SafeSupermanaProgress
            | TurnPlanFamily::SafeOpponentManaProgress
            | TurnPlanFamily::ManaTempo => false,
        };
        if macro_mode
            && !selected.wins_immediately
            && allow_generic_current_pro_override
            && plan.utility.passes_override_guard(selected_utility_value())
            && !selected_white_setup_recovery_blocks_utility_override
            && (!candidate_unsafe || selected_unsafe)
        {
            return true;
        }
        if matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
            && !candidate_spirit_tactical
            && !candidate.spirit_development
            && !candidate.spirit_own_mana_setup_now
            && !projected_completed_plan_override
        {
            return false;
        }
        if projected_completed_plan_override {
            return true;
        }

        match config.turn_engine_mode {
            TurnEngineMode::ProV1 => match plan.head_family {
                TurnPlanFamily::ImmediateScore => {
                    (candidate.wins_immediately || scores_now_better || same_turn_window_better)
                        && score_gap <= 280
                }
                TurnPlanFamily::DenyOpponentWindow => {
                    same_turn_window_better || safety_recover_better || drainer_attack_better
                }
                TurnPlanFamily::DrainerKill => drainer_attack_better && score_gap <= 180,
                TurnPlanFamily::DrainerSafetyRecovery => {
                    let engine_not_worse_than_selected = plan.utility >= selected_utility_value();
                    safety_recover_better && engine_not_worse_than_selected && score_gap <= 140
                }
                TurnPlanFamily::SpiritImpact => {
                    let engine_not_worse_than_selected = plan.utility >= selected_utility_value();
                    let candidate_rank_cap = if plan.compiled_chunks.len() > 1 {
                        12
                    } else {
                        6
                    };
                    candidate_index <= candidate_rank_cap
                        && score_gap <= 120
                        && (spirit_window_better
                            || spirit_development_better
                            || (selected_spirit_phase && engine_not_worse_than_selected))
                }
                TurnPlanFamily::SafeSupermanaProgress
                | TurnPlanFamily::SafeOpponentManaProgress => {
                    let engine_not_worse_than_selected = plan.utility >= selected_utility_value();
                    let candidate_rank_cap = if plan.compiled_chunks.len() > 1 { 3 } else { 1 };
                    let near_tie_progress = candidate.safe_supermana_progress_steps
                        == selected.safe_supermana_progress_steps
                        && candidate.safe_opponent_mana_progress_steps
                            == selected.safe_opponent_mana_progress_steps
                        && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                        && candidate.efficiency == selected.efficiency
                        && candidate.supermana_progress == selected.supermana_progress
                        && candidate.opponent_mana_progress == selected.opponent_mana_progress;
                    !selected_spirit_phase
                        && candidate_index <= candidate_rank_cap
                        && score_gap <= 80
                        && (progress_better
                            || engine_not_worse_than_selected
                            || (near_tie_progress && score_gap <= 32))
                }
                TurnPlanFamily::ManaTempo => false,
            },
            TurnEngineMode::CurrentPro => match plan.head_family {
                TurnPlanFamily::ImmediateScore => {
                    (candidate.wins_immediately
                        || scores_now_better
                        || same_turn_window_better
                        || (candidate_index <= 16
                            && plan.utility.passes_override_guard(selected_utility_value())
                            && (!candidate_unsafe || selected_unsafe)))
                        && score_gap <= 360
                }
                TurnPlanFamily::DenyOpponentWindow => {
                    same_turn_window_better
                        || safety_recover_better
                        || drainer_attack_better
                        || (candidate_index <= 16
                            && score_gap <= 220
                            && !candidate_unsafe
                            && plan.utility.passes_override_guard(selected_utility_value()))
                }
                TurnPlanFamily::DrainerKill => {
                    candidate.attacks_opponent_drainer
                        && candidate_index <= 16
                        && score_gap <= 260
                        && (drainer_attack_better || plan.utility >= selected_utility_value())
                }
                TurnPlanFamily::DrainerSafetyRecovery => {
                    let engine_not_worse_than_selected = plan
                        .utility
                        .supports_family_fallback(selected_utility_value());
                    let safer_now =
                        selected.own_drainer_vulnerable && !candidate.own_drainer_vulnerable;
                    let utility_only_recovery_override = current_pro_secondary_analysis
                        && engine_not_worse_than_selected
                        && (selected_unsafe || !candidate_unsafe)
                        && !selected_white_setup_recovery_blocks_utility_override;
                    candidate.classes.drainer_safety_recover
                        && candidate_index <= 16
                        && score_gap <= 240
                        && (safety_recover_better || safer_now || utility_only_recovery_override)
                }
                TurnPlanFamily::SpiritImpact => {
                    let selected_utility = selected_utility_value();
                    let engine_not_worse_than_selected =
                        plan.utility.supports_family_fallback(selected_utility);
                    let engine_better_than_selected =
                        plan.utility.passes_override_guard(selected_utility);
                    let selected_safe_projected_root_blocks_plain_spirit = game.active_color
                        == Color::Black
                        && !selected_unsafe
                        && !candidate_unsafe
                        && !selected_spirit_phase
                        && !selected_progress_surface
                        && !candidate_spirit_tactical
                        && !candidate.spirit_own_mana_setup_now
                        && !candidate_progress_surface
                        && score_gap > 96
                        && !strategic_override_axes_better;
                    if selected_safe_projected_root_blocks_plain_spirit {
                        return false;
                    }
                    let selected_safe_projected_root_blocks_plain_spirit_progress = game
                        .active_color
                        == Color::Black
                        && !selected_unsafe
                        && !candidate_unsafe
                        && !selected_spirit_phase
                        && !selected_progress_surface
                        && candidate.spirit_development
                        && !candidate.spirit_same_turn_score_setup_now
                        && !candidate.spirit_own_mana_setup_now
                        && candidate_progress_surface
                        && !candidate_spirit_tactical
                        && !progress_better
                        && candidate.safe_supermana_progress_steps
                            >= selected.safe_supermana_progress_steps
                        && candidate.safe_opponent_mana_progress_steps
                            >= selected.safe_opponent_mana_progress_steps
                        && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                        && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                        && candidate.has_roundtrip == selected.has_roundtrip
                        && !scores_now_better
                        && !drainer_attack_better
                        && score_gap > 64
                        && !strategic_override_axes_better;
                    if selected_safe_projected_root_blocks_plain_spirit_progress {
                        return false;
                    }
                    let selected_concrete_spirit_setup = selected.spirit_same_turn_score_setup_now
                        || selected.spirit_own_mana_setup_now
                        || selected.same_turn_score_window_value > 0;
                    let plain_followup_overrides_concrete_setup = selected_concrete_spirit_setup
                        && !black_plain_spirit_followup_head_override
                        && !selected_unsafe
                        && !candidate_unsafe
                        && !candidate.spirit_same_turn_score_setup_now
                        && !candidate.spirit_own_mana_setup_now
                        && candidate.same_turn_score_window_value
                            <= selected.same_turn_score_window_value
                        && candidate.spirit_setup_gain <= selected.spirit_setup_gain
                        && !scores_now_better
                        && !drainer_attack_better;
                    if plain_followup_overrides_concrete_setup {
                        return false;
                    }
                    let plain_spirit_head_regresses_selected_spirit = selected.spirit_development
                        && !selected.spirit_same_turn_score_setup_now
                        && !selected.spirit_own_mana_setup_now
                        && candidate.spirit_development
                        && !candidate.spirit_same_turn_score_setup_now
                        && !candidate.spirit_own_mana_setup_now
                        && score_gap >= 0
                        && !progress_better
                        && candidate.spirit_setup_gain <= selected.spirit_setup_gain
                        && candidate.safe_supermana_progress_steps
                            >= selected.safe_supermana_progress_steps
                        && candidate.safe_opponent_mana_progress_steps
                            >= selected.safe_opponent_mana_progress_steps
                        && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                        && candidate.mana_handoff_to_opponent == selected.mana_handoff_to_opponent
                        && candidate.has_roundtrip == selected.has_roundtrip
                        && !scores_now_better
                        && !drainer_attack_better
                        && !candidate_spirit_tactical;
                    if plain_spirit_head_regresses_selected_spirit {
                        return false;
                    }
                    let candidate_rank_cap = if plan.compiled_chunks.len() > 1 {
                        16
                    } else {
                        10
                    };
                    let spirit_head_override =
                        black_turn_six_route_change_plain_spirit_head_override
                            || (score_gap <= 220
                                && (spirit_window_better
                                    || spirit_development_better
                                    || white_spirit_setup_gain_head_override
                                    || (candidate.spirit_own_mana_setup_now
                                        && !selected.spirit_own_mana_setup_now
                                        && engine_not_worse_than_selected)
                                    || engine_better_than_selected
                                    || (selected_spirit_phase && engine_not_worse_than_selected)));
                    white_spirit_setup_gain_head_override
                        || (candidate_index <= candidate_rank_cap && spirit_head_override)
                }
                TurnPlanFamily::SafeSupermanaProgress
                | TurnPlanFamily::SafeOpponentManaProgress => {
                    const PROGRESS_HEAD_SCORE_DELTA_FORCE: i32 = 220;

                    let selected_utility = selected_utility_value();
                    let primary_axes_order =
                        crate::models::automove_turn_engine::compare_utility_primary_axes(
                            plan.utility,
                            selected_utility,
                        );
                    let primary_axes_better = primary_axes_order == std::cmp::Ordering::Greater;
                    let primary_axes_not_worse = primary_axes_order != std::cmp::Ordering::Less;
                    let white_plain_spirit_eval_tolerable = plan
                        .utility
                        .supports_primary_axes_eval_tolerance(selected_utility, 64);
                    let selected_progress_family = matches!(
                        selected_family,
                        TurnPlanFamily::SafeSupermanaProgress
                            | TurnPlanFamily::SafeOpponentManaProgress
                    );
                    let concrete_progress_override = candidate_progress_surface || selected_unsafe;
                    let engine_not_worse_than_selected = concrete_progress_override
                        && plan.utility.supports_family_fallback(selected_utility);
                    let pickup_upgrade = (candidate.safe_supermana_pickup_now
                        && !selected.safe_supermana_pickup_now)
                        || (candidate.safe_opponent_mana_pickup_now
                            && !selected.safe_opponent_mana_pickup_now);
                    let strategic_override_axes_better = plan
                        .utility
                        .improves_non_score_override_axes(selected_utility);
                    let selected_safe_non_progress = !selected_unsafe
                        && !selected_spirit_phase
                        && !Self::turn_engine_root_evaluation_has_progress_surface(selected);
                    let selected_safe_progress = !selected_unsafe
                        && Self::turn_engine_root_evaluation_has_progress_surface(selected);
                    let unsafe_progress_head_has_material_override = plan
                        .utility
                        .improves_non_score_override_axes(selected_utility)
                        || plan.utility.has_score_delta_force(
                            selected_utility,
                            PROGRESS_HEAD_SCORE_DELTA_FORCE,
                        );
                    let unsafe_progress_head_needs_material_override = selected_unsafe
                        && candidate_unsafe
                        && !selected_spirit_phase
                        && !selected_progress_surface
                        && candidate_progress_surface
                        && score_gap > 0
                        && !pickup_upgrade
                        && !safety_recover_better
                        && !scores_now_better
                        && !drainer_attack_better
                        && !same_turn_window_better
                        && !unsafe_progress_head_has_material_override;
                    let deferred_recovery_progress_head_without_concrete_gain = selected_unsafe
                        && candidate_unsafe
                        && !selected_spirit_phase
                        && !selected_progress_surface
                        && candidate_progress_surface
                        && matches!(plan.goal_family, TurnPlanFamily::DrainerSafetyRecovery)
                        && !candidate.classes.drainer_safety_recover
                        && candidate.own_drainer_vulnerable
                        && !pickup_upgrade
                        && !scores_now_better
                        && !drainer_attack_better
                        && !same_turn_window_better
                        && score_gap > 48;
                    let allow_safe_progress_fallback =
                        selected_unsafe || (!selected_spirit_phase && !selected_safe_progress);
                    let large_safe_search_lead =
                        score_gap > 48 && !selected_unsafe && !candidate_unsafe;
                    let safe_non_progress_fallback = !candidate_unsafe
                        && engine_not_worse_than_selected
                        && allow_safe_progress_fallback
                        && (!selected_safe_non_progress
                            || score_gap <= 32
                            || strategic_override_axes_better
                            || pickup_upgrade);
                    let engine_better_than_selected = engine_not_worse_than_selected
                        && plan
                            .utility
                            .strictly_dominates_override_axes(selected_utility)
                        && (!selected_safe_non_progress || strategic_override_axes_better);
                    let white_plain_spirit_progress_override =
                        allow_non_concrete_white_progress_head
                            || (Self::current_pro_is_early_white_turn_start(game)
                                && Self::is_plain_spirit_development_root(selected)
                                && !selected_unsafe
                                && !candidate_unsafe
                                && candidate_progress_surface
                                && candidate_index <= 2
                                && score_gap <= 96
                                && (primary_axes_better
                                    || (primary_axes_not_worse
                                        && white_plain_spirit_eval_tolerable)));
                    let allow_soft_progress_override = !selected_spirit_phase
                        || selected_unsafe
                        || white_plain_spirit_progress_override;
                    let candidate_rank_cap = if plan.compiled_chunks.len() > 1 {
                        12
                    } else {
                        6
                    };
                    if unsafe_progress_head_needs_material_override {
                        return false;
                    }
                    if deferred_recovery_progress_head_without_concrete_gain {
                        return false;
                    }
                    let near_tie_progress = candidate.safe_supermana_progress_steps
                        == selected.safe_supermana_progress_steps
                        && candidate.safe_opponent_mana_progress_steps
                            == selected.safe_opponent_mana_progress_steps
                        && candidate.own_drainer_vulnerable == selected.own_drainer_vulnerable
                        && candidate.efficiency == selected.efficiency
                        && candidate.supermana_progress == selected.supermana_progress
                        && candidate.opponent_mana_progress == selected.opponent_mana_progress;
                    let safe_search_lead_override_guard = (!selected_safe_progress
                        && !selected_safe_non_progress)
                        || !large_safe_search_lead
                        || strategic_override_axes_better
                        || pickup_upgrade;
                    let progress_better_override = allow_soft_progress_override
                        && progress_better
                        && safe_search_lead_override_guard;
                    let near_tie_progress_override = allow_soft_progress_override
                        && near_tie_progress
                        && score_gap <= if selected_progress_family { 32 } else { 64 }
                        && safe_search_lead_override_guard;
                    let engine_progress_override = engine_better_than_selected
                        && (!large_safe_search_lead
                            || strategic_override_axes_better
                            || pickup_upgrade)
                        && (!selected_safe_progress
                            || strategic_override_axes_better
                            || pickup_upgrade
                            || (progress_better && !selected_spirit_phase));
                    candidate_index <= candidate_rank_cap
                        && score_gap <= 220
                        && (progress_better_override
                            || white_plain_spirit_progress_override
                            || pickup_upgrade
                            || engine_progress_override
                            || safe_non_progress_fallback
                            || near_tie_progress_override)
                }
                TurnPlanFamily::ManaTempo => false,
            },
        }
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn classify_turn_engine_rerank_override(
        root_moves: &[ScoredRootMove],
        override_inputs: &[Input],
    ) -> TurnEngineRerankOverrideAcceptance {
        let Some(index) = root_moves
            .iter()
            .position(|candidate| candidate.inputs.as_slice() == override_inputs)
        else {
            return TurnEngineRerankOverrideAcceptance::Rejected(
                TurnEngineRerankOverrideRejectReason::NotInRoot,
            );
        };
        let Some(top) = root_moves.first() else {
            return TurnEngineRerankOverrideAcceptance::Rejected(
                TurnEngineRerankOverrideRejectReason::MissingTop,
            );
        };
        let top_unsafe = top.mana_handoff_to_opponent
            || (top.own_drainer_vulnerable
                && !top.classes.drainer_safety_recover
                && !top.attacks_opponent_drainer
                && !Self::turn_engine_root_move_has_concrete_score_surface(top));
        if index == 0 {
            let top_is_tactical = top.wins_immediately
                || top.attacks_opponent_drainer
                || top.scores_supermana_this_turn
                || top.scores_opponent_mana_this_turn
                || top.safe_supermana_pickup_now
                || top.safe_opponent_mana_pickup_now
                || top.supermana_progress
                || top.opponent_mana_progress
                || top.same_turn_score_window_value > 0
                || top.spirit_same_turn_score_setup_now
                || top.spirit_development
                || top.classes.drainer_safety_recover;
            return if top_is_tactical && !top_unsafe {
                TurnEngineRerankOverrideAcceptance::Accepted
            } else {
                TurnEngineRerankOverrideAcceptance::Rejected(
                    TurnEngineRerankOverrideRejectReason::TopNotTacticalOrUnsafe,
                )
            };
        }
        let candidate = &root_moves[index];
        if candidate.wins_immediately {
            return TurnEngineRerankOverrideAcceptance::Accepted;
        }
        if top.wins_immediately {
            return TurnEngineRerankOverrideAcceptance::Rejected(
                TurnEngineRerankOverrideRejectReason::TopWins,
            );
        }

        let decisive_tactical = candidate.attacks_opponent_drainer
            || candidate.scores_supermana_this_turn
            || candidate.scores_opponent_mana_this_turn
            || candidate.safe_supermana_pickup_now
            || candidate.safe_opponent_mana_pickup_now;
        let soft_tactical = candidate.same_turn_score_window_value > 0
            || candidate.spirit_same_turn_score_setup_now;
        let candidate_unsafe = candidate.mana_handoff_to_opponent
            || (candidate.own_drainer_vulnerable
                && !candidate.classes.drainer_safety_recover
                && !candidate.attacks_opponent_drainer
                && !Self::turn_engine_root_move_has_concrete_score_surface(candidate));
        if candidate_unsafe {
            return TurnEngineRerankOverrideAcceptance::Rejected(
                TurnEngineRerankOverrideRejectReason::CandidateUnsafe,
            );
        }

        let heuristic_gap = top.heuristic.saturating_sub(candidate.heuristic);

        let material_advantage = (candidate.attacks_opponent_drainer
            && !top.attacks_opponent_drainer)
            || ((candidate.scores_supermana_this_turn || candidate.scores_opponent_mana_this_turn)
                && !(top.scores_supermana_this_turn || top.scores_opponent_mana_this_turn))
            || ((candidate.safe_supermana_pickup_now || candidate.safe_opponent_mana_pickup_now)
                && !(top.safe_supermana_pickup_now || top.safe_opponent_mana_pickup_now))
            || (candidate.classes.drainer_safety_recover
                && !top.classes.drainer_safety_recover
                && top.own_drainer_vulnerable
                && !candidate.own_drainer_vulnerable)
            || (candidate.same_turn_score_window_value
                > top.same_turn_score_window_value.saturating_add(1));

        let candidate_progress_better = candidate.safe_supermana_progress_steps
            < top.safe_supermana_progress_steps
            || candidate.safe_opponent_mana_progress_steps < top.safe_opponent_mana_progress_steps;
        let candidate_safer = (!candidate.own_drainer_vulnerable && top.own_drainer_vulnerable)
            || (!candidate.mana_handoff_to_opponent && top.mana_handoff_to_opponent);
        let progress_signal = candidate.supermana_progress
            || candidate.opponent_mana_progress
            || candidate_progress_better;
        let progress_only = progress_signal
            && !decisive_tactical
            && !soft_tactical
            && !candidate.classes.drainer_safety_recover
            && !candidate.spirit_same_turn_score_setup_now;
        let tactical_or_progress = decisive_tactical
            || soft_tactical
            || candidate.classes.drainer_safety_recover
            || candidate.spirit_same_turn_score_setup_now
            || progress_signal;

        if progress_only {
            if !(candidate_safer || top_unsafe) {
                return TurnEngineRerankOverrideAcceptance::Rejected(
                    TurnEngineRerankOverrideRejectReason::ProgressGate,
                );
            }
            if index <= 4 && heuristic_gap <= 180 {
                return TurnEngineRerankOverrideAcceptance::Accepted;
            }
            return TurnEngineRerankOverrideAcceptance::Rejected(
                TurnEngineRerankOverrideRejectReason::ProgressGate,
            );
        }

        if tactical_or_progress {
            if candidate.own_drainer_vulnerable && !candidate.classes.drainer_safety_recover {
                return TurnEngineRerankOverrideAcceptance::Rejected(
                    TurnEngineRerankOverrideRejectReason::CandidateUnsafe,
                );
            }
            let tactical_signal = material_advantage
                || (candidate_progress_better && (candidate_safer || top_unsafe))
                || (candidate.classes.drainer_safety_recover
                    && top.own_drainer_vulnerable
                    && !candidate.own_drainer_vulnerable)
                || (candidate.spirit_same_turn_score_setup_now
                    && candidate.same_turn_score_window_value > top.same_turn_score_window_value);
            if tactical_signal && index <= 6 && heuristic_gap <= 520 {
                return TurnEngineRerankOverrideAcceptance::Accepted;
            }
            if material_advantage && index <= 8 && heuristic_gap <= 640 {
                return TurnEngineRerankOverrideAcceptance::Accepted;
            }
            return TurnEngineRerankOverrideAcceptance::Rejected(
                TurnEngineRerankOverrideRejectReason::TacticalGate,
            );
        }

        if (candidate_safer && candidate_progress_better) && index <= 5 && heuristic_gap <= 220 {
            return TurnEngineRerankOverrideAcceptance::Accepted;
        }

        TurnEngineRerankOverrideAcceptance::Rejected(
            TurnEngineRerankOverrideRejectReason::SafetyProgressGate,
        )
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn current_pro_root_advisor_conflicts_with_choice(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        root_moves: &[ScoredRootMove],
        engine_head_plan: Option<&TurnPlan>,
        inputs: &[Input],
    ) -> bool {
        let mut advisor_root_moves = root_moves.to_vec();
        let decision = Self::current_pro_root_advisor_presearch(
            game,
            perspective,
            config,
            &mut advisor_root_moves,
            engine_head_plan,
        );
        decision
            .as_ref()
            .and_then(|decision| decision.approved_root.as_ref())
            .is_some_and(|approved| approved.inputs.as_slice() != inputs)
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn turn_engine_allowed_rerank_override_candidate(
        root_moves: &[ScoredRootMove],
        inputs: &[Input],
    ) -> bool {
        let Some(candidate) = root_moves
            .iter()
            .find(|candidate| candidate.inputs.as_slice() == inputs)
        else {
            return false;
        };

        candidate.wins_immediately
            || candidate.attacks_opponent_drainer
            || candidate.scores_supermana_this_turn
            || candidate.scores_opponent_mana_this_turn
            || candidate.safe_supermana_pickup_now
            || candidate.safe_opponent_mana_pickup_now
            || candidate.classes.drainer_safety_recover
            || candidate.same_turn_score_window_value > 0
            || candidate.spirit_same_turn_score_setup_now
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn should_invoke_turn_head_rerank(root_moves: &[ScoredRootMove]) -> bool {
        let Some(top) = root_moves.first() else {
            return false;
        };
        if top.wins_immediately
            || top.attacks_opponent_drainer
            || top.same_turn_score_window_value >= 2
            || top.scores_supermana_this_turn
            || top.scores_opponent_mana_this_turn
            || top.safe_supermana_pickup_now
            || top.safe_opponent_mana_pickup_now
            || top.supermana_progress
            || top.opponent_mana_progress
            || top.spirit_same_turn_score_setup_now
            || top.spirit_development
            || top.classes.drainer_safety_recover
        {
            return true;
        }

        root_moves.iter().take(3).any(|candidate| {
            candidate.attacks_opponent_drainer
                || candidate.scores_supermana_this_turn
                || candidate.scores_opponent_mana_this_turn
                || candidate.safe_supermana_pickup_now
                || candidate.safe_opponent_mana_pickup_now
                || candidate.supermana_progress
                || candidate.opponent_mana_progress
                || candidate.same_turn_score_window_value >= 2
                || candidate.spirit_same_turn_score_setup_now
                || candidate.spirit_development
                || candidate.classes.drainer_safety_recover
        })
    }

    #[cfg(any(target_arch = "wasm32", test))]
    fn smart_search_best_inputs_internal(
        game: &MonsGame,
        config: AutomoveSearchConfig,
        use_transposition_table: bool,
    ) -> Vec<Input> {
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        #[cfg(test)]
        let _turn_engine_selector_runtime_depth = TurnEngineSelectorRuntimeDepthGuard::enter();
        clear_exact_state_analysis_cache();
        clear_turn_engine_selector_followup_floor_cache();
        let mut config = config;
        let perspective = game.active_color;
        let live_turn_engine_config = Self::turn_engine_config_for_game(game, config);
        #[cfg(test)]
        update_turn_engine_selector_diagnostics(|diagnostics| {
            diagnostics.selector_disable_reason = if config.enable_turn_engine_selector {
                "not_disabled"
            } else {
                "pre_disabled"
            };
        });
        #[cfg(test)]
        update_top_level_turn_engine_selector_disable_reason(
            if config.enable_turn_engine_selector {
                "not_disabled"
            } else {
                "pre_disabled"
            },
        );
        let mut prechecked_cached_inputs = None;
        if Self::current_pro_low_budget_guard_live(config) {
            prechecked_cached_inputs = turn_engine_cached_step(game, live_turn_engine_config);
            if prechecked_cached_inputs.is_none()
                && Self::should_skip_current_pro_low_budget_state(game)
            {
                config.enable_turn_engine_selector = false;
                #[cfg(test)]
                update_turn_engine_selector_diagnostics(|diagnostics| {
                    diagnostics.selector_disable_reason = "low_budget_guard";
                });
                #[cfg(test)]
                update_top_level_turn_engine_selector_disable_reason("low_budget_guard");
            }
        }
        if Self::current_pro_mid_turn_tactical_guard_live(config)
            && Self::should_disable_current_pro_mid_turn_tactical_engine(game)
        {
            prechecked_cached_inputs = None;
            config.enable_turn_engine_selector = false;
            #[cfg(test)]
            update_turn_engine_selector_diagnostics(|diagnostics| {
                diagnostics.selector_disable_reason = "mid_turn_tactical_guard";
            });
            #[cfg(test)]
            update_top_level_turn_engine_selector_disable_reason("mid_turn_tactical_guard");
        }
        if Self::current_pro_low_budget_guard_live(config)
            && Self::current_pro_is_safe_early_black_opening_state(game)
        {
            config.enable_turn_engine_secondary_analysis = false;
            config.enable_turn_engine_selected_followup_projection = false;
        }
        config = Self::apply_current_pro_low_budget_search_clamp(game, config);
        #[cfg(test)]
        update_turn_engine_selector_diagnostics(|diagnostics| {
            diagnostics.last_return_stage = if config.enable_turn_engine_selector {
                "engine_enabled"
            } else {
                "engine_disabled"
            };
        });
        #[cfg(test)]
        update_top_level_turn_engine_selector_last_return_stage(
            if config.enable_turn_engine_selector {
                "engine_enabled"
            } else {
                "engine_disabled"
            },
        );
        if !config.enable_turn_engine_selector
            && matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && Self::current_pro_is_white_turn_one_mana_only_followup(game)
        {
            let mut baseline_fallback = config;
            baseline_fallback.turn_engine_mode = TurnEngineMode::ProV1;
            #[cfg(test)]
            update_turn_engine_selector_diagnostics(|diagnostics| {
                diagnostics.last_return_stage = "engine_disabled_prov1_fallback";
            });
            #[cfg(test)]
            update_top_level_turn_engine_selector_last_return_stage(
                "engine_disabled_prov1_fallback",
            );
            return Self::smart_search_best_inputs_internal(
                game,
                baseline_fallback,
                use_transposition_table,
            );
        }
        if config.enable_turn_engine_selector {
            let mut root_moves = Self::ranked_root_moves(game, perspective, config);
            if crate::models::automove_deadline::cancelled() {
                return Vec::new();
            }
            if root_moves.is_empty() {
                return Vec::new();
            }
            let engine_mode = config.turn_engine_mode;
            let cached_inputs = prechecked_cached_inputs
                .or_else(|| turn_engine_cached_step(game, live_turn_engine_config))
                .filter(|inputs| {
                    Self::accept_turn_engine_cached_step(
                        root_moves.as_slice(),
                        inputs.as_slice(),
                        engine_mode,
                    )
                });
            let engine_config = Self::turn_engine_config_for_game(game, config);
            let skip_head_plan = Self::should_skip_current_pro_head_plan_for_root_context(
                game,
                root_moves.as_slice(),
                config,
            );
            #[cfg(test)]
            if !skip_head_plan {
                update_turn_engine_selector_diagnostics(|diagnostics| {
                    diagnostics.head_plan_calls += 1
                });
            }
            let engine_head_plan = if skip_head_plan {
                None
            } else if Self::current_pro_use_fresh_live_head_plan(game, config) {
                turn_engine_candidate_plan_live(game, perspective, engine_config)
            } else {
                turn_engine_candidate_plan(game, perspective, engine_config)
            };
            if crate::models::automove_deadline::checkpoint() {
                return Vec::new();
            }
            #[cfg(test)]
            if engine_head_plan.is_some() {
                update_turn_engine_selector_diagnostics(|diagnostics| {
                    diagnostics.head_plan_hits += 1
                });
            }
            if matches!(engine_mode, TurnEngineMode::CurrentPro) {
                if let Some(cached_inputs) = cached_inputs.as_ref() {
                    if Self::should_resume_turn_engine_cached_step(
                        root_moves.as_slice(),
                        cached_inputs.as_slice(),
                        engine_mode,
                    ) {
                        #[cfg(test)]
                        update_turn_engine_selector_diagnostics(|diagnostics| {
                            diagnostics.last_return_stage = "engine_cached_resume";
                        });
                        return cached_inputs.clone();
                    }
                }
                if let (Some(cached_inputs), Some(plan)) =
                    (cached_inputs.as_ref(), engine_head_plan.as_ref())
                {
                    if plan.compiled_chunks.first() == Some(cached_inputs) {
                        #[cfg(test)]
                        update_turn_engine_selector_diagnostics(|diagnostics| {
                            diagnostics.last_return_stage = "engine_cached_resume";
                        });
                        turn_engine_commit_plan(
                            game,
                            perspective,
                            engine_mode,
                            plan,
                            engine_config,
                        );
                        Self::seed_turn_engine_followup_cache_if_safe(
                            game,
                            perspective,
                            config,
                            engine_mode,
                            plan,
                            engine_config,
                        );
                        return cached_inputs.clone();
                    }
                }
            }
            if root_moves.len() > 1
                && config.enable_turn_head_rerank
                && Self::should_invoke_turn_head_rerank(root_moves.as_slice())
            {
                let allowed_root_steps = root_moves
                    .iter()
                    .map(|candidate| candidate.inputs.clone())
                    .collect::<Vec<_>>();
                let allowed_head_matches = |plan: &TurnPlan| {
                    plan.compiled_chunks.first().is_some_and(|chunk| {
                        allowed_root_steps.iter().any(|inputs| inputs == chunk)
                    })
                };
                let mut allowed_engine_plan = engine_head_plan
                    .as_ref()
                    .filter(|plan| allowed_head_matches(plan))
                    .cloned();
                if allowed_engine_plan.is_none() {
                    let rerank_engine_config = Self::turn_engine_rerank_config(config);
                    allowed_engine_plan = turn_engine_candidate_plan_from_allowed_heads(
                        game,
                        perspective,
                        rerank_engine_config,
                        allowed_root_steps.as_slice(),
                    );
                }
                if let Some(plan) = allowed_engine_plan {
                    if let Some(inputs) = plan.compiled_chunks.first().cloned() {
                        match Self::classify_turn_engine_rerank_override(
                            root_moves.as_slice(),
                            inputs.as_slice(),
                        ) {
                            TurnEngineRerankOverrideAcceptance::Accepted => {
                                let override_candidate =
                                    Self::turn_engine_allowed_rerank_override_candidate(
                                        root_moves.as_slice(),
                                        inputs.as_slice(),
                                    );
                                let advisor_conflict = override_candidate
                                    && Self::current_pro_root_advisor_conflicts_with_choice(
                                        game,
                                        perspective,
                                        config,
                                        root_moves.as_slice(),
                                        engine_head_plan.as_ref(),
                                        inputs.as_slice(),
                                    );
                                if override_candidate && !advisor_conflict {
                                    #[cfg(test)]
                                    update_turn_engine_selector_diagnostics(|diagnostics| {
                                        diagnostics.last_return_stage = "engine_allowed_head";
                                    });
                                    #[cfg(test)]
                                    update_top_level_turn_engine_selector_last_return_stage(
                                        "engine_allowed_head",
                                    );
                                    turn_engine_commit_plan(
                                        game,
                                        perspective,
                                        engine_mode,
                                        &plan,
                                        engine_config,
                                    );
                                    Self::seed_turn_engine_followup_cache_if_safe(
                                        game,
                                        perspective,
                                        config,
                                        engine_mode,
                                        &plan,
                                        engine_config,
                                    );
                                    return inputs;
                                }
                            }
                            TurnEngineRerankOverrideAcceptance::Rejected(_) => {}
                        }
                    }
                }
            }
            if let Some(forced_inputs) = Self::forced_tactical_prepass_choice(
                game,
                perspective,
                root_moves.as_slice(),
                config,
            ) {
                #[cfg(test)]
                update_turn_engine_selector_diagnostics(|diagnostics| {
                    diagnostics.last_return_stage = "engine_forced_prepass";
                });
                #[cfg(test)]
                update_top_level_turn_engine_selector_last_return_stage("engine_forced_prepass");
                if let Some(plan) = engine_head_plan.as_ref() {
                    if plan.compiled_chunks.first() == Some(&forced_inputs) {
                        turn_engine_commit_plan(
                            game,
                            perspective,
                            engine_mode,
                            plan,
                            engine_config,
                        );
                        Self::seed_turn_engine_followup_cache_if_safe(
                            game,
                            perspective,
                            config,
                            engine_mode,
                            plan,
                            engine_config,
                        );
                    }
                }
                return forced_inputs;
            }
            if let Some(plan) = engine_head_plan.as_ref() {
                if let Some(forced_inputs) = Self::forced_low_budget_turn_engine_prepass_choice(
                    game,
                    root_moves.as_slice(),
                    plan,
                    config,
                ) {
                    #[cfg(test)]
                    update_turn_engine_selector_diagnostics(|diagnostics| {
                        diagnostics.last_return_stage = "engine_low_budget_prepass";
                    });
                    #[cfg(test)]
                    update_top_level_turn_engine_selector_last_return_stage(
                        "engine_low_budget_prepass",
                    );
                    turn_engine_commit_plan(game, perspective, engine_mode, plan, engine_config);
                    Self::seed_turn_engine_followup_cache_if_safe(
                        game,
                        perspective,
                        config,
                        engine_mode,
                        plan,
                        engine_config,
                    );
                    return forced_inputs;
                }
            }
            let root_advisor_decision = Self::current_pro_root_advisor_presearch(
                game,
                perspective,
                config,
                &mut root_moves,
                engine_head_plan.as_ref(),
            );
            let advisor_priority_inputs = root_advisor_decision
                .as_ref()
                .map(Self::current_pro_root_advisor_priority_inputs)
                .unwrap_or_default();

            let (root_moves, scout_visited_nodes) =
                Self::focused_root_candidates_with_priority_inputs(
                    game,
                    perspective,
                    root_moves,
                    config,
                    use_transposition_table,
                    (!advisor_priority_inputs.is_empty())
                        .then_some(advisor_priority_inputs.as_slice()),
                    None,
                );
            if crate::models::automove_deadline::cancelled() {
                return Vec::new();
            }
            if root_moves.is_empty() {
                return Vec::new();
            }

            let mut visited_nodes = scout_visited_nodes;
            let mut alpha = i32::MIN;
            let mut scored_roots = Vec::with_capacity(root_moves.len());
            let mut transposition_table = U64HashMap::default();
            let extension_node_budget = if config.enable_selective_extensions
                && config.selective_extension_node_share_bp > 0
            {
                ((config.max_visited_nodes * config.selective_extension_node_share_bp as usize)
                    / 10_000)
                    .max(1)
            } else {
                0
            };
            let mut extension_nodes_used = 0usize;
            let mut quiescence_nodes_used = 0usize;

            for candidate in root_moves.into_iter() {
                if visited_nodes >= config.max_visited_nodes {
                    break;
                }

                visited_nodes += 1;
                let candidate_score = Self::evaluate_root_candidate_score(
                    &candidate,
                    perspective,
                    alpha,
                    &mut visited_nodes,
                    config,
                    &mut transposition_table,
                    &mut extension_nodes_used,
                    extension_node_budget,
                    use_transposition_table,
                    &mut quiescence_nodes_used,
                );

                scored_roots.push(RootEvaluation {
                    root_rank: candidate.root_rank,
                    score: candidate_score,
                    efficiency: candidate.efficiency,
                    inputs: candidate.inputs,
                    game: candidate.game,
                    wins_immediately: candidate.wins_immediately,
                    attacks_opponent_drainer: candidate.attacks_opponent_drainer,
                    own_drainer_vulnerable: candidate.own_drainer_vulnerable,
                    own_drainer_walk_vulnerable: candidate.own_drainer_walk_vulnerable,
                    spirit_development: candidate.spirit_development,
                    keeps_awake_spirit_on_base: candidate.keeps_awake_spirit_on_base,
                    mana_handoff_to_opponent: candidate.mana_handoff_to_opponent,
                    has_roundtrip: candidate.has_roundtrip,
                    scores_supermana_this_turn: candidate.scores_supermana_this_turn,
                    scores_opponent_mana_this_turn: candidate.scores_opponent_mana_this_turn,
                    safe_supermana_pickup_now: candidate.safe_supermana_pickup_now,
                    safe_opponent_mana_pickup_now: candidate.safe_opponent_mana_pickup_now,
                    safe_supermana_progress_steps: candidate.safe_supermana_progress_steps,
                    safe_opponent_mana_progress_steps: candidate.safe_opponent_mana_progress_steps,
                    score_path_best_steps: candidate.score_path_best_steps,
                    same_turn_score_window_value: candidate.same_turn_score_window_value,
                    spirit_setup_gain: candidate.spirit_setup_gain,
                    spirit_same_turn_score_setup_now: candidate.spirit_same_turn_score_setup_now,
                    spirit_own_mana_setup_now: candidate.spirit_own_mana_setup_now,
                    supermana_progress: candidate.supermana_progress,
                    opponent_mana_progress: candidate.opponent_mana_progress,
                    interview_soft_priority: candidate.interview_soft_priority,
                    classes: candidate.classes,
                });

                if candidate_score > alpha {
                    alpha = candidate_score;
                }
            }

            if scored_roots.is_empty() {
                return Vec::new();
            }
            let mut selected_inputs =
                Self::pick_root_move_with_exploration(game, &scored_roots, perspective, config);
            if let Some(plan) = engine_head_plan.as_ref() {
                let accepted = Self::accept_turn_engine_head_after_search(
                    game,
                    perspective,
                    config,
                    scored_roots.as_slice(),
                    selected_inputs.as_slice(),
                    plan,
                );
                if accepted {
                    if let Some(chunk) = plan.compiled_chunks.first() {
                        selected_inputs = chunk.clone();
                    }
                }
            }
            if cached_inputs.as_ref() == Some(&selected_inputs) {
                #[cfg(test)]
                update_turn_engine_selector_diagnostics(|diagnostics| {
                    diagnostics.last_return_stage = "engine_cached_selected";
                });
                #[cfg(test)]
                update_top_level_turn_engine_selector_last_return_stage("engine_cached_selected");
                return selected_inputs;
            }
            if let Some(plan) = engine_head_plan.as_ref() {
                if plan.compiled_chunks.first() == Some(&selected_inputs) {
                    turn_engine_commit_plan(game, perspective, engine_mode, plan, engine_config);
                    Self::seed_turn_engine_followup_cache_if_safe(
                        game,
                        perspective,
                        config,
                        engine_mode,
                        plan,
                        engine_config,
                    );
                }
            }
            #[cfg(test)]
            update_turn_engine_selector_diagnostics(|diagnostics| {
                diagnostics.last_return_stage = "engine_post_search";
            });
            #[cfg(test)]
            update_top_level_turn_engine_selector_last_return_stage("engine_post_search");
            return selected_inputs;
        }
        let mut root_moves = Self::ranked_root_moves(game, perspective, config);
        if crate::models::automove_deadline::cancelled() {
            return Vec::new();
        }
        if root_moves.is_empty() {
            return Vec::new();
        }
        if root_moves.len() > 1
            && config.enable_turn_head_rerank
            && Self::should_invoke_turn_head_rerank(root_moves.as_slice())
        {
            let allowed_root_steps = root_moves
                .iter()
                .map(|candidate| candidate.inputs.clone())
                .collect::<Vec<_>>();
            let rerank_engine_config = Self::turn_engine_rerank_config(config);
            if let Some(inputs) = turn_engine_next_inputs_from_allowed_heads(
                game,
                perspective,
                config.turn_engine_mode,
                rerank_engine_config,
                allowed_root_steps.as_slice(),
            ) {
                match Self::classify_turn_engine_rerank_override(
                    root_moves.as_slice(),
                    inputs.as_slice(),
                ) {
                    TurnEngineRerankOverrideAcceptance::Accepted => {
                        let override_candidate =
                            Self::turn_engine_allowed_rerank_override_candidate(
                                root_moves.as_slice(),
                                inputs.as_slice(),
                            );
                        let advisor_conflict = override_candidate
                            && Self::current_pro_root_advisor_conflicts_with_choice(
                                game,
                                perspective,
                                config,
                                root_moves.as_slice(),
                                None,
                                inputs.as_slice(),
                            );
                        if override_candidate && !advisor_conflict {
                            #[cfg(test)]
                            update_turn_engine_selector_diagnostics(|diagnostics| {
                                diagnostics.last_return_stage = "search_only_engine_allowed_head";
                            });
                            #[cfg(test)]
                            update_top_level_turn_engine_selector_last_return_stage(
                                "search_only_engine_allowed_head",
                            );
                            return inputs;
                        }
                    }
                    TurnEngineRerankOverrideAcceptance::Rejected(_) => {}
                }
            }
        }
        if let Some(forced_inputs) =
            Self::forced_tactical_prepass_choice(game, perspective, root_moves.as_slice(), config)
        {
            #[cfg(test)]
            update_turn_engine_selector_diagnostics(|diagnostics| {
                diagnostics.last_return_stage = "search_only_forced_prepass";
            });
            #[cfg(test)]
            update_top_level_turn_engine_selector_last_return_stage("search_only_forced_prepass");
            return forced_inputs;
        }

        let root_advisor_decision = Self::current_pro_root_advisor_presearch(
            game,
            perspective,
            config,
            &mut root_moves,
            None,
        );
        let advisor_priority_inputs = root_advisor_decision
            .as_ref()
            .map(Self::current_pro_root_advisor_priority_inputs)
            .unwrap_or_default();

        let (root_moves, scout_visited_nodes) = Self::focused_root_candidates_with_priority_inputs(
            game,
            perspective,
            root_moves,
            config,
            use_transposition_table,
            (!advisor_priority_inputs.is_empty()).then_some(advisor_priority_inputs.as_slice()),
            None,
        );
        if crate::models::automove_deadline::cancelled() {
            return Vec::new();
        }
        if root_moves.is_empty() {
            return Vec::new();
        }

        let mut visited_nodes = scout_visited_nodes;
        let mut alpha = i32::MIN;
        let mut scored_roots = Vec::with_capacity(root_moves.len());
        let mut transposition_table = U64HashMap::default();
        let extension_node_budget =
            if config.enable_selective_extensions && config.selective_extension_node_share_bp > 0 {
                ((config.max_visited_nodes * config.selective_extension_node_share_bp as usize)
                    / 10_000)
                    .max(1)
            } else {
                0
            };
        let mut extension_nodes_used = 0usize;
        let mut quiescence_nodes_used = 0usize;

        for candidate in root_moves.into_iter() {
            if visited_nodes >= config.max_visited_nodes {
                break;
            }

            visited_nodes += 1;
            let candidate_score = Self::evaluate_root_candidate_score(
                &candidate,
                perspective,
                alpha,
                &mut visited_nodes,
                config,
                &mut transposition_table,
                &mut extension_nodes_used,
                extension_node_budget,
                use_transposition_table,
                &mut quiescence_nodes_used,
            );

            scored_roots.push(RootEvaluation {
                root_rank: candidate.root_rank,
                score: candidate_score,
                efficiency: candidate.efficiency,
                inputs: candidate.inputs,
                game: candidate.game,
                wins_immediately: candidate.wins_immediately,
                attacks_opponent_drainer: candidate.attacks_opponent_drainer,
                own_drainer_vulnerable: candidate.own_drainer_vulnerable,
                own_drainer_walk_vulnerable: candidate.own_drainer_walk_vulnerable,
                spirit_development: candidate.spirit_development,
                keeps_awake_spirit_on_base: candidate.keeps_awake_spirit_on_base,
                mana_handoff_to_opponent: candidate.mana_handoff_to_opponent,
                has_roundtrip: candidate.has_roundtrip,
                scores_supermana_this_turn: candidate.scores_supermana_this_turn,
                scores_opponent_mana_this_turn: candidate.scores_opponent_mana_this_turn,
                safe_supermana_pickup_now: candidate.safe_supermana_pickup_now,
                safe_opponent_mana_pickup_now: candidate.safe_opponent_mana_pickup_now,
                safe_supermana_progress_steps: candidate.safe_supermana_progress_steps,
                safe_opponent_mana_progress_steps: candidate.safe_opponent_mana_progress_steps,
                score_path_best_steps: candidate.score_path_best_steps,
                same_turn_score_window_value: candidate.same_turn_score_window_value,
                spirit_setup_gain: candidate.spirit_setup_gain,
                spirit_same_turn_score_setup_now: candidate.spirit_same_turn_score_setup_now,
                spirit_own_mana_setup_now: candidate.spirit_own_mana_setup_now,
                supermana_progress: candidate.supermana_progress,
                opponent_mana_progress: candidate.opponent_mana_progress,
                interview_soft_priority: candidate.interview_soft_priority,
                classes: candidate.classes,
            });

            if candidate_score > alpha {
                alpha = candidate_score;
            }
        }

        if scored_roots.is_empty() {
            Vec::new()
        } else {
            Self::pick_root_move_with_exploration(game, &scored_roots, perspective, config)
        }
    }

    #[allow(clippy::too_many_arguments)]
    fn evaluate_root_candidate_score(
        candidate: &ScoredRootMove,
        perspective: Color,
        alpha: i32,
        visited_nodes: &mut usize,
        config: AutomoveSearchConfig,
        transposition_table: &mut U64HashMap<TranspositionEntry>,
        extension_nodes_used: &mut usize,
        extension_node_budget: usize,
        use_transposition_table: bool,
        quiescence_nodes_used: &mut usize,
    ) -> i32 {
        if config.depth <= 1 {
            return candidate.heuristic;
        }

        Self::search_score(
            &candidate.game,
            perspective,
            config.depth - 1,
            alpha,
            i32::MAX,
            visited_nodes,
            config,
            transposition_table,
            config.max_extensions_per_path,
            extension_nodes_used,
            extension_node_budget,
            use_transposition_table,
            quiescence_nodes_used,
        )
    }

    #[cfg(test)]
    fn focused_root_candidates(
        game: &MonsGame,
        perspective: Color,
        root_moves: Vec<ScoredRootMove>,
        config: AutomoveSearchConfig,
        use_transposition_table: bool,
    ) -> (Vec<ScoredRootMove>, usize) {
        Self::focused_root_candidates_with_priority_inputs(
            game,
            perspective,
            root_moves,
            config,
            use_transposition_table,
            None,
            None,
        )
    }

    fn focused_root_candidates_with_priority_inputs(
        _game: &MonsGame,
        perspective: Color,
        root_moves: Vec<ScoredRootMove>,
        config: AutomoveSearchConfig,
        use_transposition_table: bool,
        priority_inputs: Option<&[Vec<Input>]>,
        forced_inputs: Option<&[Input]>,
    ) -> (Vec<ScoredRootMove>, usize) {
        Self::focused_root_candidates_with_priority_inputs_impl(
            _game,
            perspective,
            root_moves,
            config,
            use_transposition_table,
            priority_inputs,
            forced_inputs,
        )
    }

    fn focused_root_candidates_with_priority_inputs_impl(
        _game: &MonsGame,
        perspective: Color,
        root_moves: Vec<ScoredRootMove>,
        config: AutomoveSearchConfig,
        use_transposition_table: bool,
        priority_inputs: Option<&[Vec<Input>]>,
        forced_inputs: Option<&[Input]>,
    ) -> (Vec<ScoredRootMove>, usize) {
        if crate::models::automove_deadline::checkpoint() {
            return (Vec::new(), 0);
        }
        if !config.enable_two_pass_root_allocation
            || root_moves.len() <= config.root_focus_k.max(1)
            || config.depth <= 1
        {
            return (
                Self::prioritize_root_inputs(root_moves, priority_inputs, forced_inputs),
                0,
            );
        }

        let scout_depth = if config.enable_two_pass_volatility_focus || config.depth <= 3 {
            1
        } else {
            config.depth.clamp(1, SMART_TWO_PASS_ROOT_SCOUT_DEPTH)
        };
        let scout_share_bp = (10_000 - config.root_focus_budget_share_bp).clamp(500, 4_000);
        let scout_budget = if scout_depth <= 1 {
            root_moves.len()
        } else {
            ((config.max_visited_nodes * scout_share_bp as usize) / 10_000).clamp(
                SMART_TWO_PASS_ROOT_SCOUT_MIN_NODES,
                config.max_visited_nodes.saturating_sub(1),
            )
        };
        if scout_budget < root_moves.len() {
            return (
                Self::prioritize_root_inputs(root_moves, priority_inputs, forced_inputs),
                0,
            );
        }

        let mut scout_config = config;
        scout_config.depth = scout_depth;
        scout_config.max_visited_nodes = scout_budget;
        scout_config.enable_selective_extensions = false;
        scout_config.enable_quiet_reductions = false;

        let mut scout_visited_nodes = 0usize;
        let mut scout_alpha = i32::MIN;
        let mut scout_transposition_table = U64HashMap::default();
        let mut scout_extension_nodes_used = 0usize;
        let mut scout_quiescence_nodes_used = 0usize;
        let mut scout_scores = vec![i32::MIN; root_moves.len()];
        let mut best_scout_score = i32::MIN;

        for (index, candidate) in root_moves.iter().enumerate() {
            if crate::models::automove_deadline::checkpoint() {
                return (Vec::new(), scout_visited_nodes);
            }
            if scout_depth > 1 && scout_visited_nodes >= scout_config.max_visited_nodes {
                break;
            }
            if scout_depth > 1 {
                scout_visited_nodes += 1;
            }
            let score = if scout_depth > 1 {
                Self::search_score(
                    &candidate.game,
                    perspective,
                    scout_depth - 1,
                    scout_alpha,
                    i32::MAX,
                    &mut scout_visited_nodes,
                    scout_config,
                    &mut scout_transposition_table,
                    0,
                    &mut scout_extension_nodes_used,
                    0,
                    use_transposition_table,
                    &mut scout_quiescence_nodes_used,
                )
            } else {
                Self::root_focus_scout_score(candidate)
            };
            if crate::models::automove_deadline::cancelled() {
                return (Vec::new(), scout_visited_nodes);
            }
            scout_scores[index] = score;
            best_scout_score = best_scout_score.max(score);
            scout_alpha = scout_alpha.max(score);
        }

        let focus_k = config.root_focus_k.max(1).min(root_moves.len());
        let mut ranked_indices = (0..root_moves.len())
            .map(|index| {
                let score = if scout_scores[index] == i32::MIN {
                    Self::root_focus_scout_score(&root_moves[index])
                } else {
                    scout_scores[index]
                };
                (index, score)
            })
            .collect::<Vec<_>>();
        ranked_indices.sort_by(|a, b| Self::compare_ranked_root_indices(&root_moves, *a, *b));

        if ranked_indices.len() >= focus_k {
            let best_score = ranked_indices[0].1;
            let kth_score = ranked_indices[focus_k - 1].1;
            if best_score.saturating_sub(kth_score) <= SMART_TWO_PASS_ROOT_NARROW_SPREAD_FALLBACK {
                return (
                    Self::prioritize_root_inputs(
                        Self::reorder_root_moves_by_ranked_indices(
                            root_moves,
                            ranked_indices.as_slice(),
                        ),
                        priority_inputs,
                        forced_inputs,
                    ),
                    0,
                );
            }
        }

        let mut selected_mask = vec![false; root_moves.len()];
        for (index, _) in ranked_indices.iter().take(focus_k) {
            selected_mask[*index] = true;
        }
        if matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && config.enable_turn_engine_selector
            && focus_k <= 3
        {
            let top_focus_has_plain_spirit =
                ranked_indices.iter().take(focus_k).any(|(index, _)| {
                    let root = &root_moves[*index];
                    root.spirit_development
                        && !root.spirit_same_turn_score_setup_now
                        && !root.spirit_own_mana_setup_now
                });
            if !top_focus_has_plain_spirit {
                let spirit_focus_engine_config = Self::turn_engine_rerank_config(config);
                let near_focus_plain_spirit = ranked_indices
                    .iter()
                    .enumerate()
                    .filter_map(|(rank, (index, _))| {
                        if rank >= focus_k + 3 {
                            return None;
                        }
                        let root = &root_moves[*index];
                        (root.spirit_development
                            && !root.spirit_same_turn_score_setup_now
                            && !root.spirit_own_mana_setup_now
                            && !root.own_drainer_vulnerable
                            && !root.mana_handoff_to_opponent
                            && !root.has_roundtrip
                            && root.game.active_color == perspective
                            && turn_engine_candidate_plan(
                                &root.game,
                                perspective,
                                spirit_focus_engine_config,
                            )
                            .is_some_and(|plan| {
                                matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
                                    && plan.utility.has_nonnegative_deny_gain()
                            }))
                        .then_some(*index)
                    })
                    .collect::<Vec<_>>();
                if near_focus_plain_spirit.len() >= 2 {
                    for index in near_focus_plain_spirit {
                        selected_mask[index] = true;
                    }
                }
            }
        }
        for (index, score) in ranked_indices.iter().copied() {
            if score + SMART_TWO_PASS_ROOT_FOCUS_SCORE_MARGIN < best_scout_score {
                continue;
            }
            selected_mask[index] = true;
        }
        for (index, candidate) in root_moves.iter().enumerate() {
            if candidate.attacks_opponent_drainer {
                selected_mask[index] = true;
            }
            if candidate.scores_supermana_this_turn
                || candidate.scores_opponent_mana_this_turn
                || candidate.safe_supermana_pickup_now
                || candidate.safe_opponent_mana_pickup_now
                || candidate.spirit_same_turn_score_setup_now
                || candidate.same_turn_score_window_value > 0
                || candidate.spirit_own_mana_setup_now
            {
                selected_mask[index] = true;
            }
            if matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && config.enable_turn_engine_selector
                && candidate.own_drainer_vulnerable
                && !candidate.own_drainer_walk_vulnerable
                && !candidate.mana_handoff_to_opponent
                && !candidate.has_roundtrip
                && candidate.game.active_color == perspective
                && turn_engine_candidate_plan(
                    &candidate.game,
                    perspective,
                    Self::turn_engine_config_from_search_config(config),
                )
                .is_some_and(|plan| plan.head_family == TurnPlanFamily::DrainerSafetyRecovery)
            {
                selected_mask[index] = true;
            }
        }
        if let Some(priority_inputs) = priority_inputs {
            for inputs in priority_inputs {
                if let Some(index) = root_moves
                    .iter()
                    .position(|candidate| candidate.inputs.as_slice() == inputs.as_slice())
                {
                    selected_mask[index] = true;
                }
            }
        }
        if let Some(forced_inputs) = forced_inputs {
            if let Some(index) = root_moves
                .iter()
                .position(|candidate| candidate.inputs.as_slice() == forced_inputs)
            {
                selected_mask[index] = true;
            }
        }
        if config.enable_two_pass_volatility_focus {
            let mut volatility_ranked = (0..root_moves.len())
                .map(|index| {
                    let scout_score = if scout_scores[index] == i32::MIN {
                        Self::root_focus_scout_score(&root_moves[index])
                    } else {
                        scout_scores[index]
                    };
                    (
                        index,
                        Self::root_volatility_score(&root_moves[index]),
                        scout_score,
                    )
                })
                .filter(|(_, volatility, _)| *volatility > 0)
                .collect::<Vec<_>>();
            volatility_ranked.sort_by(|a, b| b.1.cmp(&a.1).then(b.2.cmp(&a.2)));

            for (index, _, _) in volatility_ranked
                .iter()
                .take(SMART_TWO_PASS_ROOT_VOLATILITY_KEEP.max(1))
            {
                selected_mask[*index] = true;
            }

            if let Some(best_volatility) = volatility_ranked.first().map(|(_, value, _)| *value) {
                for (index, volatility, scout_score) in volatility_ranked {
                    if volatility + SMART_TWO_PASS_ROOT_VOLATILITY_MARGIN < best_volatility {
                        break;
                    }
                    if scout_score + SMART_TWO_PASS_ROOT_FOCUS_SCORE_MARGIN < best_scout_score {
                        continue;
                    }
                    selected_mask[index] = true;
                }
            }
        }

        if !selected_mask.iter().any(|is_selected| *is_selected) {
            return (
                Self::prioritize_root_inputs(root_moves, priority_inputs, forced_inputs),
                0,
            );
        }

        let mut focused_with_scores = selected_mask
            .iter()
            .enumerate()
            .filter_map(|(index, is_selected)| {
                if !*is_selected {
                    return None;
                }
                let score = if scout_scores[index] == i32::MIN {
                    Self::root_focus_scout_score(&root_moves[index])
                } else {
                    scout_scores[index]
                };
                Some((index, score))
            })
            .collect::<Vec<_>>();
        focused_with_scores.sort_by(|a, b| Self::compare_ranked_root_indices(&root_moves, *a, *b));

        let mut owned_root_moves = root_moves.into_iter().map(Some).collect::<Vec<_>>();
        let mut focused_root_moves = Vec::with_capacity(focused_with_scores.len());
        for (index, _) in focused_with_scores {
            if let Some(candidate) = owned_root_moves[index].take() {
                focused_root_moves.push(candidate);
            }
        }

        (
            Self::prioritize_root_inputs(focused_root_moves, priority_inputs, forced_inputs),
            scout_visited_nodes.min(config.max_visited_nodes),
        )
    }

    #[allow(clippy::too_many_arguments)]
    fn search_score(
        game: &MonsGame,
        perspective: Color,
        depth: usize,
        alpha: i32,
        beta: i32,
        visited_nodes: &mut usize,
        config: AutomoveSearchConfig,
        transposition_table: &mut U64HashMap<TranspositionEntry>,
        extensions_remaining: usize,
        extension_nodes_used: &mut usize,
        extension_node_budget: usize,
        use_transposition_table: bool,
        quiescence_nodes_used: &mut usize,
    ) -> i32 {
        if let Some(terminal_score) = Self::terminal_score(game, perspective, depth, config.depth) {
            return terminal_score;
        }
        if *visited_nodes >= config.max_visited_nodes {
            return Self::evaluate_search_preferability(game, perspective, config);
        }
        if depth == 0 {
            if config.enable_quiescence_search
                && *visited_nodes < config.max_visited_nodes
                && (config.quiescence_node_budget == 0
                    || *quiescence_nodes_used < config.quiescence_node_budget)
            {
                let stand_pat = Self::evaluate_search_preferability(game, perspective, config);
                let maximizing = game.active_color == perspective;
                if maximizing {
                    if stand_pat >= beta {
                        return stand_pat;
                    }
                    *quiescence_nodes_used += 1;
                    let mut q_alpha = alpha.max(stand_pat);
                    let mut best = stand_pat;
                    if config.enable_quiescence_tactical_children_only {
                        for child_game in
                            Self::enumerate_quiescence_tactical_games(game, config).iter()
                        {
                            if *visited_nodes >= config.max_visited_nodes {
                                break;
                            }
                            *visited_nodes += 1;
                            let score = Self::evaluate_search_preferability(
                                child_game,
                                perspective,
                                config,
                            );
                            if crate::models::automove_deadline::cancelled() {
                                return 0;
                            }
                            if score > best {
                                best = score;
                            }
                            q_alpha = q_alpha.max(best);
                            if q_alpha >= beta {
                                break;
                            }
                        }
                    } else {
                        let children =
                            Self::ranked_child_states(game, perspective, maximizing, None, config);
                        for child in children.iter() {
                            if child.classes.quiet {
                                continue;
                            }
                            if *visited_nodes >= config.max_visited_nodes {
                                break;
                            }
                            *visited_nodes += 1;
                            let score = Self::evaluate_search_preferability(
                                &child.game,
                                perspective,
                                config,
                            );
                            if crate::models::automove_deadline::cancelled() {
                                return 0;
                            }
                            if score > best {
                                best = score;
                            }
                            q_alpha = q_alpha.max(best);
                            if q_alpha >= beta {
                                break;
                            }
                        }
                    }
                    return best;
                } else {
                    if stand_pat <= alpha {
                        return stand_pat;
                    }
                    *quiescence_nodes_used += 1;
                    let mut q_beta = beta.min(stand_pat);
                    let mut best = stand_pat;
                    if config.enable_quiescence_tactical_children_only {
                        for child_game in
                            Self::enumerate_quiescence_tactical_games(game, config).iter()
                        {
                            if *visited_nodes >= config.max_visited_nodes {
                                break;
                            }
                            *visited_nodes += 1;
                            let score = Self::evaluate_search_preferability(
                                child_game,
                                perspective,
                                config,
                            );
                            if crate::models::automove_deadline::cancelled() {
                                return 0;
                            }
                            if score < best {
                                best = score;
                            }
                            q_beta = q_beta.min(best);
                            if q_beta <= alpha {
                                break;
                            }
                        }
                    } else {
                        let children =
                            Self::ranked_child_states(game, perspective, maximizing, None, config);
                        for child in children.iter() {
                            if child.classes.quiet {
                                continue;
                            }
                            if *visited_nodes >= config.max_visited_nodes {
                                break;
                            }
                            *visited_nodes += 1;
                            let score = Self::evaluate_search_preferability(
                                &child.game,
                                perspective,
                                config,
                            );
                            if crate::models::automove_deadline::cancelled() {
                                return 0;
                            }
                            if score < best {
                                best = score;
                            }
                            q_beta = q_beta.min(best);
                            if q_beta <= alpha {
                                break;
                            }
                        }
                    }
                    return best;
                }
            }
            return Self::evaluate_search_preferability(game, perspective, config);
        }

        let mut alpha = alpha;
        let mut beta = beta;
        let alpha_before = alpha;
        let beta_before = beta;
        let state_key = Self::search_state_hash(game);
        let mut preferred_child_hash = None;

        if use_transposition_table {
            if let Some(entry) = transposition_table.get(&state_key).copied() {
                if entry.best_child_hash != 0 {
                    preferred_child_hash = Some(entry.best_child_hash);
                }
                if entry.depth >= depth {
                    match entry.bound {
                        TranspositionBound::Exact => return entry.score,
                        TranspositionBound::LowerBound => {
                            alpha = alpha.max(entry.score);
                        }
                        TranspositionBound::UpperBound => {
                            beta = beta.min(entry.score);
                        }
                    }
                    if alpha >= beta {
                        return entry.score;
                    }
                }
            }
        }

        let maximizing = game.active_color == perspective;

        // Futility pruning: at frontier nodes (depth=1), skip expansion if static eval
        // is so far from the window that no child move could possibly improve it enough
        if config.enable_futility_pruning
            && depth == 1
            && !Self::has_frontier_tactical_potential(game)
        {
            let static_eval = Self::evaluate_search_preferability(game, perspective, config);
            if maximizing && static_eval + config.futility_margin < alpha {
                return static_eval;
            }
            if !maximizing && static_eval - config.futility_margin > beta {
                return static_eval;
            }
        }

        let mut children =
            Self::ranked_child_states(game, perspective, maximizing, preferred_child_hash, config);
        if children.is_empty() {
            return Self::evaluate_search_preferability(game, perspective, config);
        }

        let mut stopped_by_budget = false;
        let mut best_child_hash = 0u64;
        let value = if maximizing {
            let mut value = i32::MIN;
            for child in children.drain(..) {
                if *visited_nodes >= config.max_visited_nodes {
                    stopped_by_budget = true;
                    break;
                }

                let mut child_depth = depth.saturating_sub(1);
                let mut child_extensions_remaining = extensions_remaining;
                if config.enable_selective_extensions
                    && Self::is_selective_extension_candidate(
                        child.tactical_extension_trigger,
                        child.ordering_efficiency,
                        child.classes,
                    )
                    && child_extensions_remaining > 0
                    && (extension_node_budget == 0 || *extension_nodes_used < extension_node_budget)
                {
                    child_depth = depth;
                    child_extensions_remaining = child_extensions_remaining.saturating_sub(1);
                    if extension_node_budget > 0 {
                        *extension_nodes_used = extension_nodes_used.saturating_add(1);
                    }
                } else if config.enable_quiet_reductions
                    && child.quiet_reduction_candidate
                    && depth >= config.quiet_reduction_depth_threshold
                {
                    child_depth = depth.saturating_sub(2);
                }

                *visited_nodes += 1;
                let score = Self::search_score(
                    &child.game,
                    perspective,
                    child_depth,
                    alpha,
                    beta,
                    visited_nodes,
                    config,
                    transposition_table,
                    child_extensions_remaining,
                    extension_nodes_used,
                    extension_node_budget,
                    use_transposition_table,
                    quiescence_nodes_used,
                );
                if score > value {
                    value = score;
                    best_child_hash = child.hash;
                }
                alpha = alpha.max(value);
                if alpha >= beta {
                    break;
                }
            }

            if value == i32::MIN {
                Self::evaluate_search_preferability(game, perspective, config)
            } else {
                value
            }
        } else {
            let mut value = i32::MAX;
            for child in children.drain(..) {
                if *visited_nodes >= config.max_visited_nodes {
                    stopped_by_budget = true;
                    break;
                }

                let mut child_depth = depth.saturating_sub(1);
                let mut child_extensions_remaining = extensions_remaining;
                if config.enable_selective_extensions
                    && Self::is_selective_extension_candidate(
                        child.tactical_extension_trigger,
                        child.ordering_efficiency,
                        child.classes,
                    )
                    && child_extensions_remaining > 0
                    && (extension_node_budget == 0 || *extension_nodes_used < extension_node_budget)
                {
                    child_depth = depth;
                    child_extensions_remaining = child_extensions_remaining.saturating_sub(1);
                    if extension_node_budget > 0 {
                        *extension_nodes_used = extension_nodes_used.saturating_add(1);
                    }
                } else if config.enable_quiet_reductions
                    && child.quiet_reduction_candidate
                    && depth >= config.quiet_reduction_depth_threshold
                {
                    child_depth = depth.saturating_sub(2);
                }

                *visited_nodes += 1;
                let score = Self::search_score(
                    &child.game,
                    perspective,
                    child_depth,
                    alpha,
                    beta,
                    visited_nodes,
                    config,
                    transposition_table,
                    child_extensions_remaining,
                    extension_nodes_used,
                    extension_node_budget,
                    use_transposition_table,
                    quiescence_nodes_used,
                );
                if score < value {
                    value = score;
                    best_child_hash = child.hash;
                }
                beta = beta.min(value);
                if beta <= alpha {
                    break;
                }
            }

            if value == i32::MAX {
                Self::evaluate_search_preferability(game, perspective, config)
            } else {
                value
            }
        };

        if use_transposition_table
            && !stopped_by_budget
            && crate::models::automove_deadline::cache_write_allowed()
        {
            let bound = if value <= alpha_before {
                TranspositionBound::UpperBound
            } else if value >= beta_before {
                TranspositionBound::LowerBound
            } else {
                TranspositionBound::Exact
            };

            if transposition_table.len() >= SMART_TRANSPOSITION_TABLE_MAX_ENTRIES
                && !transposition_table.contains_key(&state_key)
            {
                transposition_table.clear();
            }
            transposition_table.insert(
                state_key,
                TranspositionEntry {
                    depth,
                    score: value,
                    bound,
                    best_child_hash,
                },
            );
        }

        value
    }

    fn build_child_ordering_scratch(
        game: &MonsGame,
        perspective: Color,
        actor_color: Color,
    ) -> ChildOrderingScratch {
        let before_state_hash = Self::search_state_hash(game);
        let before_efficiency_snapshot = Self::move_efficiency_snapshot_with_hash(
            game,
            perspective,
            false,
            false,
            before_state_hash,
        );
        let own_drainer_vulnerable_before =
            Self::is_own_drainer_vulnerable_next_turn(game, actor_color, true);

        ChildOrderingScratch {
            before_efficiency_snapshot,
            own_drainer_vulnerable_before,
        }
    }

    fn ranked_child_states(
        game: &MonsGame,
        perspective: Color,
        maximizing: bool,
        preferred_child_hash: Option<u64>,
        config: AutomoveSearchConfig,
    ) -> Vec<RankedChildState> {
        #[cfg(test)]
        update_turn_engine_selector_diagnostics(|diagnostics| {
            diagnostics.ranked_child_states_calls += 1;
        });
        let mut scored_states: Vec<(i32, RankedChildState)> = Vec::new();
        let actor_color = game.active_color;
        let scratch = Self::build_child_ordering_scratch(game, perspective, actor_color);
        let start_options = Self::automove_start_input_options();
        let child_transitions =
            Self::enumerate_legal_transitions(game, config.node_enum_limit, start_options);
        #[cfg(test)]
        update_turn_engine_selector_diagnostics(|diagnostics| {
            diagnostics.ranked_child_states_children_enumerated += child_transitions.len();
        });
        for transition in child_transitions {
            if crate::models::automove_deadline::checkpoint() {
                return Vec::new();
            }
            let simulated_game = transition.game;
            let events = transition.events;
            let child_hash = Self::search_state_hash(&simulated_game);
            let mut heuristic = Self::score_state(
                &simulated_game,
                perspective,
                0,
                config.depth,
                config.scoring_weights,
            );

            let ordering_efficiency = Self::move_efficiency_delta_from_before_snapshot(
                game,
                &simulated_game,
                actor_color,
                &events,
                scratch.before_efficiency_snapshot,
                child_hash,
                false,
                false,
                false,
                false,
                false,
                config.root_backtrack_penalty,
                config.root_mana_handoff_penalty,
            );
            heuristic = heuristic.saturating_add(Self::ordering_event_bonus(
                game.active_color,
                perspective,
                &events,
            ));
            if preferred_child_hash.is_some() && preferred_child_hash == Some(child_hash) {
                heuristic = heuristic.saturating_add(SMART_TT_BEST_CHILD_BONUS);
            }

            let own_drainer_vulnerable_before = scratch.own_drainer_vulnerable_before;
            let own_drainer_vulnerable_after =
                Self::is_own_drainer_vulnerable_next_turn(&simulated_game, actor_color, true);
            let own_drainer_vulnerability_transition =
                own_drainer_vulnerable_before != own_drainer_vulnerable_after;
            let tactical_extension_trigger = Self::events_include_any_drainer_fainted(&events)
                || own_drainer_vulnerability_transition
                || events
                    .iter()
                    .any(|event| matches!(event, Event::ManaScored { .. }));
            let classes = Self::classify_move_classes(
                game,
                &simulated_game,
                actor_color,
                &events,
                own_drainer_vulnerable_before,
                own_drainer_vulnerable_after,
            );
            let quiet_reduction_candidate = Self::is_quiet_reduction_candidate(
                ordering_efficiency,
                tactical_extension_trigger,
                classes,
            );
            if crate::models::automove_deadline::cancelled() {
                return Vec::new();
            }
            #[cfg(test)]
            update_turn_engine_selector_diagnostics(|diagnostics| {
                diagnostics.ranked_child_states_children_fully_scored += 1;
            });

            scored_states.push((
                heuristic,
                RankedChildState {
                    game: simulated_game,
                    hash: child_hash,
                    ordering_efficiency,
                    tactical_extension_trigger,
                    quiet_reduction_candidate,
                    classes,
                },
            ));
        }
        scored_states.sort_by(|a, b| Self::compare_ranked_child_entries(a, b, maximizing));

        if scored_states.len() >= 3 {
            Self::enforce_tactical_child_top2(&mut scored_states, maximizing, true);
        }

        if scored_states.len() > config.node_branch_limit {
            scored_states = Self::truncate_child_states_with_coverage(
                scored_states,
                config.node_branch_limit,
                maximizing,
                true,
            );
        }

        scored_states.into_iter().map(|(_, state)| state).collect()
    }

    pub(crate) fn enumerate_legal_transitions(
        game: &MonsGame,
        max_moves: usize,
        start_options: SuggestedStartInputOptions,
    ) -> Vec<LegalInputTransition> {
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        let mut transitions = Vec::new();
        let mut partial_inputs = Vec::new();
        let mut simulated_game = game.clone_for_simulation();
        Self::collect_legal_transitions(
            &mut simulated_game,
            &mut partial_inputs,
            &mut transitions,
            max_moves,
            start_options,
        );
        if crate::models::automove_deadline::cancelled() {
            return Vec::new();
        }
        transitions.sort_by(|a, b| a.inputs.cmp(&b.inputs));
        transitions
    }

    pub(crate) fn enumerate_legal_transitions_with_priority(
        game: &MonsGame,
        max_moves: usize,
        start_options: SuggestedStartInputOptions,
        priority_locations: &[Location],
    ) -> Vec<LegalInputTransition> {
        if priority_locations.is_empty() {
            return Self::enumerate_legal_transitions(game, max_moves, start_options);
        }

        let mut priority_mask = [false; BOARD_CELLS];
        for &location in priority_locations {
            priority_mask[location.index()] = true;
        }
        let priority_budget = (max_moves / 2).max(max_moves.saturating_sub(60));
        let remaining_budget = max_moves.saturating_sub(priority_budget);
        let all_transitions = Self::enumerate_legal_transitions(game, max_moves, start_options);
        let mut priority_transitions = Vec::new();
        let mut other_transitions = Vec::new();

        for transition in all_transitions {
            let is_priority = matches!(
                transition.inputs.first(),
                Some(Input::Location(loc)) if priority_mask[loc.index()]
            );
            if is_priority {
                if priority_transitions.len() < priority_budget {
                    priority_transitions.push(transition);
                }
            } else if remaining_budget > 0 && other_transitions.len() < remaining_budget {
                other_transitions.push(transition);
            }

            if priority_transitions.len() >= priority_budget
                && (remaining_budget == 0 || other_transitions.len() >= remaining_budget)
            {
                break;
            }
        }

        priority_transitions.extend(other_transitions);
        priority_transitions
    }

    fn collect_legal_transitions(
        game: &mut MonsGame,
        partial_inputs: &mut Vec<Input>,
        transitions: &mut Vec<LegalInputTransition>,
        max_moves: usize,
        start_options: SuggestedStartInputOptions,
    ) {
        if crate::models::automove_deadline::checkpoint() {
            transitions.clear();
            return;
        }
        if transitions.len() >= max_moves || partial_inputs.len() > SMART_MAX_INPUT_CHAIN {
            return;
        }

        match game.process_input_with_start_options_slice(
            partial_inputs.as_slice(),
            true,
            false,
            Some(start_options),
        ) {
            Output::InvalidInput => {}
            Output::Events(events) => {
                let mut after_game = game.clone_for_simulation();
                let applied_events = after_game.apply_and_add_resulting_events(events);
                transitions.push(LegalInputTransition {
                    inputs: partial_inputs.clone(),
                    game: after_game,
                    events: applied_events,
                });
            }
            Output::LocationsToStartFrom(locations) => {
                for location in locations {
                    if transitions.len() >= max_moves {
                        break;
                    }
                    partial_inputs.push(Input::Location(location));
                    Self::collect_legal_transitions(
                        game,
                        partial_inputs,
                        transitions,
                        max_moves,
                        start_options,
                    );
                    partial_inputs.pop();
                }
            }
            Output::NextInputOptions(options) => {
                for option in options {
                    if transitions.len() >= max_moves {
                        break;
                    }
                    partial_inputs.push(option.input);
                    Self::collect_legal_transitions(
                        game,
                        partial_inputs,
                        transitions,
                        max_moves,
                        start_options,
                    );
                    partial_inputs.pop();
                }
            }
        }
    }

    /// Enumerates the same lexical prefix produced by full enumeration followed by sorting and
    /// truncation, but stops generating work once that prefix is complete.
    fn enumerate_legal_transitions_lexicographic_bounded(
        game: &MonsGame,
        max_moves: usize,
        start_options: SuggestedStartInputOptions,
        allowed_first_locations: Option<&[bool; BOARD_CELLS]>,
    ) -> Vec<LegalInputTransition> {
        if max_moves == 0 || crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        let mut transitions = Vec::with_capacity(max_moves.min(256));
        let mut partial_inputs = Vec::new();
        let mut simulated_game = game.clone_for_simulation();
        Self::collect_legal_transitions_lexicographic_bounded(
            &mut simulated_game,
            &mut partial_inputs,
            &mut transitions,
            max_moves,
            start_options,
            allowed_first_locations,
        );
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        debug_assert!(transitions
            .windows(2)
            .all(|pair| pair[0].inputs <= pair[1].inputs));
        transitions
    }

    fn collect_legal_transitions_lexicographic_bounded(
        game: &mut MonsGame,
        partial_inputs: &mut Vec<Input>,
        transitions: &mut Vec<LegalInputTransition>,
        max_moves: usize,
        start_options: SuggestedStartInputOptions,
        allowed_first_locations: Option<&[bool; BOARD_CELLS]>,
    ) {
        if crate::models::automove_deadline::checkpoint() {
            transitions.clear();
            return;
        }
        if transitions.len() >= max_moves || partial_inputs.len() > SMART_MAX_INPUT_CHAIN {
            return;
        }
        match game.process_input_with_start_options_slice(
            partial_inputs.as_slice(),
            true,
            false,
            Some(start_options),
        ) {
            Output::InvalidInput => {}
            Output::Events(events) => {
                if allowed_first_locations.is_some() && partial_inputs.is_empty() {
                    return;
                }
                let mut after_game = game.clone_for_simulation();
                let applied_events = after_game.apply_and_add_resulting_events(events);
                transitions.push(LegalInputTransition {
                    inputs: partial_inputs.clone(),
                    game: after_game,
                    events: applied_events,
                });
            }
            output @ (Output::LocationsToStartFrom(_) | Output::NextInputOptions(_)) => {
                let mut child_inputs = match output {
                    Output::LocationsToStartFrom(locations) => locations
                        .into_iter()
                        .map(Input::Location)
                        .collect::<Vec<_>>(),
                    Output::NextInputOptions(options) => options
                        .into_iter()
                        .map(|option| option.input)
                        .collect::<Vec<_>>(),
                    Output::InvalidInput | Output::Events(_) => unreachable!(),
                };
                child_inputs.sort();
                if partial_inputs.is_empty() {
                    if let Some(allowed) = allowed_first_locations {
                        child_inputs.retain(|input| {
                            matches!(input, Input::Location(location) if allowed[location.index()])
                        });
                    }
                }
                for input in child_inputs {
                    if transitions.len() >= max_moves {
                        break;
                    }
                    partial_inputs.push(input);
                    Self::collect_legal_transitions_lexicographic_bounded(
                        game,
                        partial_inputs,
                        transitions,
                        max_moves,
                        start_options,
                        allowed_first_locations,
                    );
                    partial_inputs.pop();
                }
            }
        }
    }

    #[cfg(test)]
    fn enumerate_legal_inputs(
        game: &MonsGame,
        max_moves: usize,
        start_options: SuggestedStartInputOptions,
    ) -> Vec<Vec<Input>> {
        Self::enumerate_legal_transitions(game, max_moves, start_options)
            .into_iter()
            .map(|transition| transition.inputs)
            .collect()
    }

    pub(crate) fn apply_inputs_for_search(game: &MonsGame, inputs: &[Input]) -> Option<MonsGame> {
        Self::apply_inputs_for_search_with_events(game, inputs)
            .map(|(simulated_game, _)| simulated_game)
    }

    pub(crate) fn apply_inputs_for_search_with_events(
        game: &MonsGame,
        inputs: &[Input],
    ) -> Option<(MonsGame, Vec<Event>)> {
        let mut simulated_game = game.clone_for_simulation();
        match simulated_game.process_input_slice(inputs, false, false) {
            Output::Events(events) => Some((simulated_game, events)),
            _ => None,
        }
    }

    fn is_no_effect_turn_transition(
        game: &MonsGame,
        simulated_game: &MonsGame,
        events: &[Event],
    ) -> bool {
        if !Self::is_no_effect_state_transition(game, simulated_game) {
            return false;
        }
        !Self::has_material_event(events)
    }

    fn is_no_effect_state_transition(game: &MonsGame, simulated_game: &MonsGame) -> bool {
        game.board == simulated_game.board
            && game.white_score == simulated_game.white_score
            && game.black_score == simulated_game.black_score
            && game.white_potions_count == simulated_game.white_potions_count
            && game.black_potions_count == simulated_game.black_potions_count
    }

    fn has_material_event(events: &[Event]) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::ManaScored { .. }
                    | Event::PickupMana { .. }
                    | Event::MonFainted { .. }
                    | Event::UsePotion { .. }
                    | Event::PickupBomb { .. }
                    | Event::PickupPotion { .. }
                    | Event::BombAttack { .. }
                    | Event::BombExplosion { .. }
            )
        })
    }

    fn quiescence_transition_is_tactical(events: &[Event]) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::ManaScored { .. }
                    | Event::PickupMana { .. }
                    | Event::MonFainted { .. }
                    | Event::UsePotion { .. }
                    | Event::BombAttack { .. }
                    | Event::BombExplosion { .. }
                    | Event::SpiritTargetMove { .. }
                    | Event::SupermanaBackToBase { .. }
            )
        })
    }

    fn enumerate_quiescence_tactical_games(
        game: &MonsGame,
        config: AutomoveSearchConfig,
    ) -> Vec<MonsGame> {
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        let enum_limit = config
            .quiescence_tactical_enum_limit
            .max(1)
            .min(config.node_enum_limit.max(1));
        let transitions = Self::enumerate_legal_transitions(
            game,
            enum_limit,
            Self::automove_start_input_options(),
        );
        let mut games = Vec::with_capacity(transitions.len());
        for transition in transitions {
            if Self::quiescence_transition_is_tactical(transition.events.as_slice()) {
                games.push(transition.game);
            }
        }
        games
    }

    fn ordering_event_bonus(actor_color: Color, perspective: Color, events: &[Event]) -> i32 {
        let mut bonus = 0i32;
        for event in events {
            match event {
                Event::ManaScored { .. } => {
                    bonus += if actor_color == perspective {
                        780
                    } else {
                        -780
                    };
                }
                Event::PickupMana { .. } => {
                    bonus += if actor_color == perspective {
                        230
                    } else {
                        -230
                    };
                }
                Event::MonFainted { mon, .. } => {
                    bonus += if mon.color == perspective { -360 } else { 360 };
                }
                Event::UsePotion { .. } => {
                    bonus += if actor_color == perspective { -80 } else { 80 };
                }
                Event::PickupBomb { .. } | Event::PickupPotion { .. } => {
                    bonus += if actor_color == perspective { 45 } else { -45 };
                }
                Event::MonMove { .. }
                | Event::ManaMove { .. }
                | Event::MysticAction { .. }
                | Event::DemonAction { .. }
                | Event::DemonAdditionalStep { .. }
                | Event::SpiritTargetMove { .. }
                | Event::ManaDropped { .. }
                | Event::SupermanaBackToBase { .. }
                | Event::BombAttack { .. }
                | Event::MonAwake { .. }
                | Event::BombExplosion { .. }
                | Event::NextTurn { .. }
                | Event::GameOver { .. }
                | Event::Takeback => {}
            }
        }
        bonus
    }

    fn has_roundtrip_mon_move(events: &[Event]) -> bool {
        let mut seen_moves: Vec<(Location, Location, Color, MonKind)> =
            Vec::with_capacity(events.len().min(8));
        for event in events {
            let Event::MonMove { item, from, to } = event else {
                continue;
            };
            let Some(mon) = item.mon() else {
                continue;
            };
            let reverse = (*to, *from, mon.color, mon.kind);
            if seen_moves.contains(&reverse) {
                return true;
            }
            seen_moves.push((*from, *to, mon.color, mon.kind));
        }
        false
    }

    fn root_transition_requires_exact_lite_progress(events: &[Event]) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::ManaMove { .. }
                    | Event::ManaScored { .. }
                    | Event::PickupMana { .. }
                    | Event::ManaDropped { .. }
                    | Event::SupermanaBackToBase { .. }
            )
        })
    }

    fn root_transition_requires_exact_lite_spirit_window(events: &[Event]) -> bool {
        events.iter().any(|event| match event {
            Event::MonMove { item, .. } => {
                matches!(item.mon().map(|mon| mon.kind), Some(MonKind::Spirit))
            }
            Event::SpiritTargetMove { .. } => true,
            _ => false,
        })
    }

    fn allow_exact_lite_root_analysis(config: AutomoveSearchConfig) -> bool {
        config.exact_lite_root_call_budget > 0
            && (config.enable_exact_lite_progress_checks
                || config.enable_exact_lite_spirit_window_checks)
    }

    fn allow_exact_lite_static_analysis(config: AutomoveSearchConfig) -> bool {
        config.exact_lite_static_call_budget > 0 && config.enable_exact_lite_spirit_window_checks
    }

    fn should_use_root_exact_summary_for_transition(
        events: &[Event],
        config: AutomoveSearchConfig,
    ) -> bool {
        if !Self::allow_exact_lite_root_analysis(config) {
            return false;
        }
        (config.enable_exact_lite_progress_checks
            && Self::root_transition_requires_exact_lite_progress(events))
            || (config.enable_exact_lite_spirit_window_checks
                && Self::root_transition_requires_exact_lite_spirit_window(events))
    }

    fn with_exact_lite_budgeted_transition_config(
        mut config: AutomoveSearchConfig,
        perspective: Color,
        transition_game: &MonsGame,
        transition_events: &[Event],
        remaining_root_calls: &mut usize,
        remaining_static_calls: &mut usize,
    ) -> AutomoveSearchConfig {
        if Self::should_use_root_exact_summary_for_transition(transition_events, config) {
            if *remaining_root_calls > 0 {
                *remaining_root_calls = remaining_root_calls.saturating_sub(1);
            } else {
                config.exact_lite_root_call_budget = 0;
            }
        }

        if Self::allow_exact_lite_static_analysis(config)
            && transition_game.active_color == perspective
        {
            if *remaining_static_calls > 0 {
                *remaining_static_calls = remaining_static_calls.saturating_sub(1);
            } else {
                config.exact_lite_static_call_budget = 0;
            }
        }

        config.exact_lite_root_call_budget = config
            .exact_lite_root_call_budget
            .min(*remaining_root_calls);
        config.exact_lite_static_call_budget = config
            .exact_lite_static_call_budget
            .min(*remaining_static_calls);
        config
    }

    fn approximate_active_turn_summary(
        game: &MonsGame,
        color: Color,
        allow_exact_strategic: bool,
    ) -> ExactTurnSummary {
        let strategic =
            allow_exact_strategic.then(|| exact_strategic_analysis(game).color_summary(color));
        let remaining_moves = (Config::MONS_MOVES_PER_TURN - game.mons_moves_count).max(0);
        let safe_supermana_progress_steps =
            Self::approximate_specific_mana_progress_steps(game, color, Mana::Supermana);
        let safe_opponent_mana_progress_steps = Self::approximate_specific_mana_progress_steps(
            game,
            color,
            Mana::Regular(color.other()),
        );
        ExactTurnSummary {
            safe_supermana_progress: safe_supermana_progress_steps
                .map(|steps| steps <= remaining_moves)
                .unwrap_or(false),
            safe_supermana_progress_steps,
            safe_opponent_mana_progress: safe_opponent_mana_progress_steps
                .map(|steps| steps <= remaining_moves)
                .unwrap_or(false),
            safe_opponent_mana_progress_steps,
            same_turn_score_window_value: strategic
                .map(|summary| summary.immediate_window.best_score)
                .unwrap_or_else(|| Self::approximate_same_turn_score_window_value(game, color)),
            score_path_best_steps: strategic
                .and_then(|summary| summary.score_path_window.best_steps)
                .or_else(|| Self::approximate_best_carrier_steps(game, color)),
            ..ExactTurnSummary::default()
        }
    }

    #[cfg(test)]
    #[allow(clippy::too_many_arguments)]
    fn move_efficiency_delta(
        game: &MonsGame,
        simulated_game: &MonsGame,
        perspective: Color,
        events: &[Event],
        is_root: bool,
        apply_backtrack_penalty: bool,
        apply_root_mana_handoff_guard: bool,
        include_tactical_exact: bool,
        include_strategic_exact: bool,
        root_backtrack_penalty: i32,
        root_mana_handoff_penalty: i32,
    ) -> i32 {
        let before_hash = Self::search_state_hash(game);
        let after_hash = Self::search_state_hash(simulated_game);
        let before = Self::move_efficiency_snapshot_with_hash(
            game,
            perspective,
            include_tactical_exact && game.active_color == perspective,
            include_strategic_exact,
            before_hash,
        );
        Self::move_efficiency_delta_from_before_snapshot(
            game,
            simulated_game,
            perspective,
            events,
            before,
            after_hash,
            is_root,
            apply_backtrack_penalty,
            apply_root_mana_handoff_guard,
            include_tactical_exact,
            include_strategic_exact,
            root_backtrack_penalty,
            root_mana_handoff_penalty,
        )
    }

    #[allow(clippy::too_many_arguments)]
    fn move_efficiency_delta_from_before_snapshot(
        game: &MonsGame,
        simulated_game: &MonsGame,
        perspective: Color,
        events: &[Event],
        before: MoveEfficiencySnapshot,
        simulated_state_hash: u64,
        is_root: bool,
        apply_backtrack_penalty: bool,
        apply_root_mana_handoff_guard: bool,
        include_tactical_exact: bool,
        include_strategic_exact: bool,
        root_backtrack_penalty: i32,
        root_mana_handoff_penalty: i32,
    ) -> i32 {
        let after = Self::move_efficiency_snapshot_uncached_with_hash(
            simulated_game,
            perspective,
            include_tactical_exact && simulated_game.active_color == perspective,
            include_strategic_exact,
            simulated_state_hash,
        );
        Self::move_efficiency_delta_from_before_snapshot_with_after_snapshot(
            game,
            simulated_game,
            perspective,
            events,
            before,
            after,
            is_root,
            apply_backtrack_penalty,
            apply_root_mana_handoff_guard,
            root_backtrack_penalty,
            root_mana_handoff_penalty,
        )
    }

    #[allow(clippy::too_many_arguments)]
    fn move_efficiency_delta_from_before_snapshot_with_after_snapshot(
        game: &MonsGame,
        simulated_game: &MonsGame,
        perspective: Color,
        events: &[Event],
        before: MoveEfficiencySnapshot,
        after: MoveEfficiencySnapshot,
        is_root: bool,
        apply_backtrack_penalty: bool,
        apply_root_mana_handoff_guard: bool,
        root_backtrack_penalty: i32,
        root_mana_handoff_penalty: i32,
    ) -> i32 {
        let unknown_steps = Config::BOARD_SIZE + 4;

        let mut delta = 0i32;
        delta += Self::step_progress_delta(
            before.my_best_carrier_steps,
            after.my_best_carrier_steps,
            90,
            130,
            unknown_steps,
        );
        delta -= Self::step_progress_delta(
            before.opponent_best_carrier_steps,
            after.opponent_best_carrier_steps,
            80,
            120,
            unknown_steps,
        );
        delta += Self::step_progress_delta(
            before.my_best_drainer_to_mana_steps,
            after.my_best_drainer_to_mana_steps,
            34,
            50,
            unknown_steps,
        );
        delta -= Self::step_progress_delta(
            before.opponent_best_drainer_to_mana_steps,
            after.opponent_best_drainer_to_mana_steps,
            30,
            44,
            unknown_steps,
        );
        delta += (after.my_carrier_count - before.my_carrier_count) * 55;
        delta -= (after.opponent_carrier_count - before.opponent_carrier_count) * 48;
        if before.my_spirit_on_base && !after.my_spirit_on_base {
            delta += SMART_SPIRIT_DEPLOY_EFFICIENCY_BONUS;
        }
        if !before.opponent_spirit_on_base && after.opponent_spirit_on_base {
            delta += SMART_SPIRIT_DEPLOY_EFFICIENCY_BONUS / 3;
        }
        delta += (after.my_spirit_action_targets - before.my_spirit_action_targets)
            * SMART_SPIRIT_ACTION_TARGET_DELTA_WEIGHT;
        delta -= (after.opponent_spirit_action_targets - before.opponent_spirit_action_targets)
            * (SMART_SPIRIT_ACTION_TARGET_DELTA_WEIGHT / 2);
        delta += (after.my_same_turn_score_value - before.my_same_turn_score_value) * 55;
        delta -=
            (after.opponent_same_turn_score_value - before.opponent_same_turn_score_value) * 45;
        delta += (after.my_same_turn_opponent_mana_score_value
            - before.my_same_turn_opponent_mana_score_value)
            * 90;
        delta -= (after.opponent_same_turn_opponent_mana_score_value
            - before.opponent_same_turn_opponent_mana_score_value)
            * 75;
        if !before.my_safe_supermana_progress && after.my_safe_supermana_progress {
            delta += 140;
        }
        if !before.opponent_safe_supermana_progress && after.opponent_safe_supermana_progress {
            delta -= 120;
        }
        if !before.my_safe_opponent_mana_progress && after.my_safe_opponent_mana_progress {
            delta += 120;
        }
        if !before.opponent_safe_opponent_mana_progress
            && after.opponent_safe_opponent_mana_progress
        {
            delta -= 110;
        }
        delta += Self::step_progress_delta(
            before.my_safe_supermana_progress_steps,
            after.my_safe_supermana_progress_steps,
            26,
            40,
            unknown_steps,
        );
        delta -= Self::step_progress_delta(
            before.opponent_safe_supermana_progress_steps,
            after.opponent_safe_supermana_progress_steps,
            22,
            36,
            unknown_steps,
        );
        delta += Self::step_progress_delta(
            before.my_safe_opponent_mana_progress_steps,
            after.my_safe_opponent_mana_progress_steps,
            22,
            34,
            unknown_steps,
        );
        delta -= Self::step_progress_delta(
            before.opponent_safe_opponent_mana_progress_steps,
            after.opponent_safe_opponent_mana_progress_steps,
            18,
            30,
            unknown_steps,
        );

        if is_root {
            let root_compensates_handoff = events
                .iter()
                .any(|event| matches!(event, Event::ManaScored { .. }))
                || Self::events_include_opponent_drainer_fainted(events, perspective);
            if apply_root_mana_handoff_guard && !root_compensates_handoff {
                delta -= Self::mana_handoff_penalty(events, perspective, root_mana_handoff_penalty);
            }

            if SMART_NO_EFFECT_ROOT_PENALTY != 0
                && Self::is_no_effect_turn_transition(game, simulated_game, events)
            {
                delta -= SMART_NO_EFFECT_ROOT_PENALTY;
            } else if SMART_LOW_IMPACT_ROOT_PENALTY != 0
                && !Self::has_material_event(events)
                && delta <= 0
            {
                delta -= SMART_LOW_IMPACT_ROOT_PENALTY;
            }
            if apply_backtrack_penalty
                && root_backtrack_penalty > 0
                && Self::has_roundtrip_mon_move(events)
            {
                delta -= root_backtrack_penalty;
            }
        }

        delta
    }

    fn mana_handoff_penalty(events: &[Event], perspective: Color, per_step_penalty: i32) -> i32 {
        if per_step_penalty <= 0 {
            return 0;
        }
        let mut penalty = 0i32;
        let opponent = perspective.other();

        for event in events {
            let Event::ManaMove { mana, from, to } = event else {
                continue;
            };

            let my_before = Self::distance_to_color_pool_steps_for_efficiency(*from, perspective);
            let my_after = Self::distance_to_color_pool_steps_for_efficiency(*to, perspective);
            let opponent_before =
                Self::distance_to_color_pool_steps_for_efficiency(*from, opponent);
            let opponent_after = Self::distance_to_color_pool_steps_for_efficiency(*to, opponent);
            let moved_toward_opponent = (opponent_before - opponent_after).max(0);
            let moved_toward_me = (my_before - my_after).max(0);

            if moved_toward_opponent > moved_toward_me {
                penalty += (moved_toward_opponent - moved_toward_me)
                    * mana.score(opponent)
                    * per_step_penalty;
            }
        }

        penalty
    }

    #[cfg(test)]
    fn move_efficiency_snapshot(
        game: &MonsGame,
        perspective: Color,
        include_tactical_exact: bool,
        include_strategic_exact: bool,
    ) -> MoveEfficiencySnapshot {
        Self::move_efficiency_snapshot_with_hash(
            game,
            perspective,
            include_tactical_exact,
            include_strategic_exact,
            Self::search_state_hash(game),
        )
    }

    fn move_efficiency_snapshot_with_hash(
        game: &MonsGame,
        perspective: Color,
        include_tactical_exact: bool,
        include_strategic_exact: bool,
        state_hash: u64,
    ) -> MoveEfficiencySnapshot {
        let cache_key = MoveEfficiencySnapshotCacheKey {
            state_hash,
            perspective,
            include_tactical_exact,
            include_strategic_exact,
        };
        if let Some(cached) =
            MOVE_EFFICIENCY_SNAPSHOT_CACHE.with(|cache| cache.borrow().get(&cache_key).copied())
        {
            #[cfg(test)]
            update_turn_engine_selector_diagnostics(|diagnostics| {
                diagnostics.move_efficiency_snapshot_cache_hits += 1;
            });
            return cached;
        }

        let snapshot = Self::build_move_efficiency_snapshot(
            game,
            perspective,
            include_tactical_exact,
            include_strategic_exact,
            state_hash,
        );

        if crate::models::automove_deadline::cache_write_allowed() {
            MOVE_EFFICIENCY_SNAPSHOT_CACHE.with(|cache| {
                let mut cache = cache.borrow_mut();
                if cache.len() >= SMART_MOVE_EFFICIENCY_SNAPSHOT_CACHE_MAX_ENTRIES
                    && !cache.contains_key(&cache_key)
                {
                    cache.clear();
                }
                cache.insert(cache_key, snapshot);
            });
        }

        snapshot
    }

    fn move_efficiency_snapshot_uncached_with_hash(
        game: &MonsGame,
        perspective: Color,
        include_tactical_exact: bool,
        include_strategic_exact: bool,
        state_hash: u64,
    ) -> MoveEfficiencySnapshot {
        Self::build_move_efficiency_snapshot(
            game,
            perspective,
            include_tactical_exact,
            include_strategic_exact,
            state_hash,
        )
    }

    fn move_efficiency_tactical_projection_flags() -> u8 {
        crate::models::automove_exact::EXACT_TURN_TACTICAL_NEED_SUPERMANA_PROGRESS
            | crate::models::automove_exact::EXACT_TURN_TACTICAL_NEED_OPPONENT_MANA_PROGRESS
            | crate::models::automove_exact::EXACT_TURN_TACTICAL_NEED_SPIRIT_SCORE
            | crate::models::automove_exact::EXACT_TURN_TACTICAL_NEED_SPIRIT_DENIAL
            | crate::models::automove_exact::EXACT_TURN_TACTICAL_NEED_SCORE_WINDOW
    }

    fn build_move_efficiency_snapshot(
        game: &MonsGame,
        perspective: Color,
        include_tactical_exact: bool,
        include_strategic_exact: bool,
        state_hash: u64,
    ) -> MoveEfficiencySnapshot {
        #[cfg(test)]
        update_turn_engine_selector_diagnostics(|diagnostics| {
            diagnostics.move_efficiency_snapshot_builds += 1;
        });
        let unknown_steps = Config::BOARD_SIZE + 4;
        let strategic = include_strategic_exact.then(|| exact_strategic_analysis(game));
        let my_summary = strategic.map(|analysis| analysis.color_summary(perspective));
        let opponent_summary =
            strategic.map(|analysis| analysis.color_summary(perspective.other()));
        let tactical_flags = Self::move_efficiency_tactical_projection_flags();
        let my_turn_summary = if include_tactical_exact && game.active_color == perspective {
            crate::models::automove_exact::exact_turn_tactical_projection_with_search_hash(
                game,
                perspective,
                state_hash,
                tactical_flags,
            )
        } else {
            ExactTurnTacticalProjection {
                same_turn_score_window_value: my_summary
                    .map(|summary| summary.immediate_window.best_score)
                    .unwrap_or_else(|| {
                        Self::approximate_same_turn_score_window_value(game, perspective)
                    }),
                ..ExactTurnTacticalProjection::default()
            }
        };
        let opponent_turn_summary = if include_tactical_exact
            && game.active_color == perspective.other()
        {
            crate::models::automove_exact::exact_turn_tactical_projection_with_search_hash(
                game,
                perspective.other(),
                state_hash,
                tactical_flags,
            )
        } else {
            ExactTurnTacticalProjection {
                same_turn_score_window_value: opponent_summary
                    .map(|summary| summary.immediate_window.best_score)
                    .unwrap_or_else(|| {
                        Self::approximate_same_turn_score_window_value(game, perspective.other())
                    }),
                ..ExactTurnTacticalProjection::default()
            }
        };

        let mut snapshot = MoveEfficiencySnapshot {
            my_best_carrier_steps: my_summary
                .and_then(|summary| summary.best_carrier_steps)
                .or_else(|| Self::approximate_best_carrier_steps(game, perspective))
                .unwrap_or(unknown_steps),
            opponent_best_carrier_steps: opponent_summary
                .and_then(|summary| summary.best_carrier_steps)
                .or_else(|| Self::approximate_best_carrier_steps(game, perspective.other()))
                .unwrap_or(unknown_steps),
            my_best_drainer_to_mana_steps: my_summary
                .and_then(|summary| summary.best_drainer_to_mana_steps)
                .or_else(|| Self::approximate_best_drainer_to_mana_steps(game, perspective))
                .unwrap_or(unknown_steps),
            opponent_best_drainer_to_mana_steps: opponent_summary
                .and_then(|summary| summary.best_drainer_to_mana_steps)
                .or_else(|| Self::approximate_best_drainer_to_mana_steps(game, perspective.other()))
                .unwrap_or(unknown_steps),
            my_carrier_count: 0,
            opponent_carrier_count: 0,
            my_spirit_on_base: false,
            opponent_spirit_on_base: false,
            my_spirit_action_targets: my_summary.map_or(0, |summary| summary.spirit.utility),
            opponent_spirit_action_targets: opponent_summary
                .map_or(0, |summary| summary.spirit.utility),
            my_same_turn_score_value: if game.active_color == perspective {
                if include_tactical_exact {
                    my_turn_summary.spirit_assisted_score_value
                } else {
                    my_turn_summary.same_turn_score_window_value
                }
            } else {
                0
            },
            opponent_same_turn_score_value: if game.active_color == perspective.other() {
                if include_tactical_exact {
                    opponent_turn_summary.spirit_assisted_score_value
                } else {
                    opponent_turn_summary.same_turn_score_window_value
                }
            } else {
                0
            },
            my_same_turn_opponent_mana_score_value: if game.active_color == perspective {
                if include_tactical_exact {
                    my_turn_summary.spirit_assisted_denial_value
                } else {
                    0
                }
            } else {
                0
            },
            opponent_same_turn_opponent_mana_score_value: if game.active_color
                == perspective.other()
            {
                if include_tactical_exact {
                    opponent_turn_summary.spirit_assisted_denial_value
                } else {
                    0
                }
            } else {
                0
            },
            my_safe_supermana_progress: include_tactical_exact
                && my_turn_summary.safe_supermana_progress,
            opponent_safe_supermana_progress: include_tactical_exact
                && opponent_turn_summary.safe_supermana_progress,
            my_safe_opponent_mana_progress: include_tactical_exact
                && my_turn_summary.safe_opponent_mana_progress,
            opponent_safe_opponent_mana_progress: include_tactical_exact
                && opponent_turn_summary.safe_opponent_mana_progress,
            my_safe_supermana_progress_steps: my_turn_summary
                .safe_supermana_progress_steps
                .unwrap_or(unknown_steps),
            opponent_safe_supermana_progress_steps: opponent_turn_summary
                .safe_supermana_progress_steps
                .unwrap_or(unknown_steps),
            my_safe_opponent_mana_progress_steps: my_turn_summary
                .safe_opponent_mana_progress_steps
                .unwrap_or(unknown_steps),
            opponent_safe_opponent_mana_progress_steps: opponent_turn_summary
                .safe_opponent_mana_progress_steps
                .unwrap_or(unknown_steps),
        };
        let my_spirit_base = Self::spirit_base_for_color(&game.board, perspective);
        let opponent_spirit_base = Self::spirit_base_for_color(&game.board, perspective.other());

        for (location, item) in game.board.occupied() {
            match item {
                Item::MonWithMana { mon, .. } => {
                    if mon.is_fainted() {
                        continue;
                    }
                    let pool_steps =
                        Self::distance_to_any_pool_steps_for_efficiency(location).saturating_sub(1);
                    if mon.color == perspective {
                        snapshot.my_carrier_count += 1;
                        snapshot.my_best_carrier_steps =
                            snapshot.my_best_carrier_steps.min(pool_steps);
                    } else {
                        snapshot.opponent_carrier_count += 1;
                        snapshot.opponent_best_carrier_steps =
                            snapshot.opponent_best_carrier_steps.min(pool_steps);
                    }
                }
                Item::Mon { mon } | Item::MonWithConsumable { mon, .. } => {
                    if mon.is_fainted() {
                        continue;
                    }
                    if mon.kind == MonKind::Spirit {
                        if mon.color == perspective {
                            snapshot.my_spirit_on_base = location == my_spirit_base;
                        } else {
                            snapshot.opponent_spirit_on_base = location == opponent_spirit_base;
                        }
                    }
                }
                Item::Mana { .. } | Item::Consumable { .. } => {}
            }
        }

        snapshot
    }

    fn approximate_best_carrier_steps(game: &MonsGame, color: Color) -> Option<i32> {
        game.board
            .occupied()
            .filter_map(|(location, item)| match item {
                Item::MonWithMana { mon, .. } if mon.color == color && !mon.is_fainted() => Some(
                    Self::distance_to_any_pool_steps_for_efficiency(location).saturating_sub(1),
                ),
                _ => None,
            })
            .min()
    }

    fn approximate_specific_mana_progress_steps(
        game: &MonsGame,
        color: Color,
        wanted: Mana,
    ) -> Option<i32> {
        let mut best_steps = None;
        for (location, item) in game.board.occupied() {
            match item {
                Item::MonWithMana { mon, mana }
                    if mon.color == color
                        && mon.kind == MonKind::Drainer
                        && !mon.is_fainted()
                        && *mana == wanted =>
                {
                    best_steps = Some(best_steps.map_or(0, |best: i32| best.min(0)));
                }
                Item::Mon { mon } | Item::MonWithConsumable { mon, .. }
                    if mon.color == color && mon.kind == MonKind::Drainer && !mon.is_fainted() =>
                {
                    for (mana_location, mana_item) in game.board.occupied() {
                        if !matches!(mana_item, Item::Mana { mana } if *mana == wanted) {
                            continue;
                        }
                        let candidate = location.distance(&mana_location);
                        best_steps =
                            Some(best_steps.map_or(candidate, |best: i32| best.min(candidate)));
                    }
                }
                Item::Mon { .. }
                | Item::MonWithMana { .. }
                | Item::MonWithConsumable { .. }
                | Item::Mana { .. }
                | Item::Consumable { .. } => {}
            }
        }
        best_steps
    }

    fn approximate_best_drainer_to_mana_steps(game: &MonsGame, color: Color) -> Option<i32> {
        let mut best_steps = None;
        for (drainer_location, item) in game.board.occupied() {
            let mon = match item {
                Item::Mon { mon }
                | Item::MonWithMana { mon, .. }
                | Item::MonWithConsumable { mon, .. } => mon,
                Item::Mana { .. } | Item::Consumable { .. } => continue,
            };
            if mon.color != color || mon.is_fainted() || mon.kind != MonKind::Drainer {
                continue;
            }

            for (mana_location, mana_item) in game.board.occupied() {
                if !matches!(mana_item, Item::Mana { .. }) {
                    continue;
                }
                let candidate_steps = drainer_location.distance(&mana_location).saturating_sub(1);
                best_steps =
                    Some(best_steps.map_or(candidate_steps, |best: i32| best.min(candidate_steps)));
            }
        }
        best_steps
    }

    fn approximate_same_turn_score_window_value(game: &MonsGame, color: Color) -> i32 {
        if game.active_color != color {
            return 0;
        }
        let remaining_moves = (Config::MONS_MOVES_PER_TURN - game.mons_moves_count).max(0);
        game.board
            .occupied()
            .filter_map(|(location, item)| match item {
                Item::MonWithMana { mon, mana } if mon.color == color && !mon.is_fainted() => {
                    let pool_steps =
                        Self::distance_to_any_pool_steps_for_efficiency(location).saturating_sub(1);
                    (pool_steps <= remaining_moves).then_some(mana.score(color))
                }
                _ => None,
            })
            .max()
            .unwrap_or(0)
    }

    fn distance_to_any_pool_steps_for_efficiency(location: Location) -> i32 {
        let max_index = Config::MAX_LOCATION_INDEX;
        let i = location.i;
        let j = location.j;
        i32::max(i32::min(i, max_index - i), i32::min(j, max_index - j)) + 1
    }

    fn distance_to_color_pool_steps_for_efficiency(location: Location, color: Color) -> i32 {
        let max_index = Config::MAX_LOCATION_INDEX;
        let pool_row = if color == Color::White { max_index } else { 0 };
        let i = location.i;
        let j = location.j;
        i32::max((pool_row - i).abs(), i32::min(j, max_index - j)) + 1
    }

    fn step_progress_delta(
        before_steps: i32,
        after_steps: i32,
        forward_weight: i32,
        backward_weight: i32,
        unknown_steps: i32,
    ) -> i32 {
        let before_known = before_steps < unknown_steps;
        let after_known = after_steps < unknown_steps;
        match (before_known, after_known) {
            (true, true) => {
                let delta_steps = before_steps - after_steps;
                if delta_steps > 0 {
                    delta_steps * forward_weight
                } else if delta_steps < 0 {
                    delta_steps * backward_weight
                } else {
                    0
                }
            }
            (false, true) => forward_weight,
            (true, false) => -backward_weight,
            (false, false) => 0,
        }
    }

    fn score_state(
        game: &MonsGame,
        perspective: Color,
        depth: usize,
        search_depth: usize,
        scoring_weights: &'static ScoringWeights,
    ) -> i32 {
        if let Some(terminal_score) = Self::terminal_score(game, perspective, depth, search_depth) {
            terminal_score
        } else {
            evaluate_preferability_with_weights_and_exact_policy(
                game,
                perspective,
                scoring_weights,
                false,
            )
        }
    }

    fn terminal_score(
        game: &MonsGame,
        perspective: Color,
        depth: usize,
        search_depth: usize,
    ) -> Option<i32> {
        game.winner_color().map(|winner| {
            let ply_count = (search_depth.saturating_sub(depth)) as i32;
            if winner == perspective {
                SMART_TERMINAL_SCORE - ply_count
            } else {
                -SMART_TERMINAL_SCORE + ply_count
            }
        })
    }

    pub(crate) fn search_state_hash(game: &MonsGame) -> u64 {
        let mut state = 0x6a09e667f3bcc909u64;
        for (idx, item) in game.board.items.iter().enumerate() {
            let Some(item) = item else { continue };
            let entry = ((idx as u64)
                .wrapping_add(1)
                .wrapping_mul(0x9e3779b185ebca87))
                ^ Self::search_hash_item(*item);
            state ^= Self::search_mix_u64(entry);
            state = state.rotate_left(17).wrapping_mul(0x94d049bb133111eb);
        }

        state ^= Self::search_mix_u64(game.white_score as i64 as u64 ^ 0x11);
        state ^= Self::search_mix_u64(game.black_score as i64 as u64 ^ 0x23);
        state ^= Self::search_mix_u64(Self::search_hash_color(game.active_color) ^ 0x35);
        state ^= Self::search_mix_u64(game.actions_used_count as i64 as u64 ^ 0x47);
        state ^= Self::search_mix_u64(game.mana_moves_count as i64 as u64 ^ 0x59);
        state ^= Self::search_mix_u64(game.mons_moves_count as i64 as u64 ^ 0x6b);
        state ^= Self::search_mix_u64(game.white_potions_count as i64 as u64 ^ 0x7d);
        state ^= Self::search_mix_u64(game.black_potions_count as i64 as u64 ^ 0x8f);
        state ^= Self::search_mix_u64(game.turn_number as i64 as u64 ^ 0xa1);
        state ^= Self::search_mix_u64(game.variant().id() as i64 as u64 ^ 0xb3);
        Self::search_mix_u64(state)
    }

    #[inline]
    fn search_hash_item(item: Item) -> u64 {
        match item {
            Item::Mon { mon } => 0x100 | Self::search_hash_mon(mon),
            Item::Mana { mana } => 0x200 | Self::search_hash_mana(mana),
            Item::MonWithMana { mon, mana } => {
                0x300 | Self::search_hash_mon(mon) | (Self::search_hash_mana(mana) << 16)
            }
            Item::MonWithConsumable { mon, consumable } => {
                0x400
                    | Self::search_hash_mon(mon)
                    | (Self::search_hash_consumable(consumable) << 16)
            }
            Item::Consumable { consumable } => 0x500 | Self::search_hash_consumable(consumable),
        }
    }

    #[inline]
    fn search_hash_mon(mon: Mon) -> u64 {
        Self::search_hash_mon_kind(mon.kind)
            | (Self::search_hash_color(mon.color) << 4)
            | (((mon.cooldown as i64 as u64) & 0xff) << 8)
    }

    #[inline]
    fn search_hash_mon_kind(kind: MonKind) -> u64 {
        match kind {
            MonKind::Demon => 1,
            MonKind::Drainer => 2,
            MonKind::Angel => 3,
            MonKind::Spirit => 4,
            MonKind::Mystic => 5,
        }
    }

    #[inline]
    fn search_hash_color(color: Color) -> u64 {
        match color {
            Color::White => 1,
            Color::Black => 2,
        }
    }

    #[inline]
    fn search_hash_mana(mana: Mana) -> u64 {
        match mana {
            Mana::Regular(color) => 0x10 | Self::search_hash_color(color),
            Mana::Supermana => 0x20,
        }
    }

    #[inline]
    fn search_hash_consumable(consumable: Consumable) -> u64 {
        match consumable {
            Consumable::Potion => 1,
            Consumable::Bomb => 2,
            Consumable::BombOrPotion => 3,
        }
    }

    #[inline]
    fn search_mix_u64(mut value: u64) -> u64 {
        value = value.wrapping_add(0x9e3779b97f4a7c15);
        value = (value ^ (value >> 30)).wrapping_mul(0xbf58476d1ce4e5b9);
        value = (value ^ (value >> 27)).wrapping_mul(0x94d049bb133111eb);
        value ^ (value >> 31)
    }

    #[cfg(test)]
    fn advance_async_search(state: &mut AsyncSmartSearchState) -> bool {
        match state.phase {
            AsyncSmartSearchPhase::PendingRootRanking => {
                state.root_moves =
                    Self::ranked_root_moves(&state.game, state.perspective, state.config);
                state.phase = AsyncSmartSearchPhase::PendingFocusedCandidates;
                false
            }
            AsyncSmartSearchPhase::PendingFocusedCandidates => {
                if let Some(forced_inputs) = Self::forced_tactical_prepass_choice(
                    &state.game,
                    state.perspective,
                    state.root_moves.as_slice(),
                    state.config,
                ) {
                    let mut forced_game = state.game.clone_for_simulation();
                    let input_fen = Input::fen_from_array(&forced_inputs);
                    let output = forced_game.process_input(forced_inputs, false, false);
                    state.pending_output = Some(OutputModel::new(output, input_fen.as_str()));
                    return true;
                }

                let root_moves = std::mem::take(&mut state.root_moves);
                let (root_moves, scout_visited_nodes) = Self::focused_root_candidates(
                    &state.game,
                    state.perspective,
                    root_moves,
                    state.config,
                    true,
                );
                state.root_moves = root_moves;
                state.visited_nodes = scout_visited_nodes;
                state.extension_node_budget = if state.config.enable_selective_extensions
                    && state.config.selective_extension_node_share_bp > 0
                {
                    ((state.config.max_visited_nodes
                        * state.config.selective_extension_node_share_bp as usize)
                        / 10_000)
                        .max(1)
                } else {
                    0
                };
                state.phase = AsyncSmartSearchPhase::Scoring;
                state.next_index >= state.root_moves.len()
                    || state.visited_nodes >= state.config.max_visited_nodes
            }
            AsyncSmartSearchPhase::Scoring => {
                if state.next_index >= state.root_moves.len()
                    || state.visited_nodes >= state.config.max_visited_nodes
                {
                    return true;
                }

                let candidate = &state.root_moves[state.next_index];
                state.visited_nodes += 1;
                let candidate_score = Self::evaluate_root_candidate_score(
                    candidate,
                    state.perspective,
                    state.alpha,
                    &mut state.visited_nodes,
                    state.config,
                    &mut state.transposition_table,
                    &mut state.extension_nodes_used,
                    state.extension_node_budget,
                    true,
                    &mut state.quiescence_nodes_used,
                );

                state.scored_roots.push(RootEvaluation {
                    root_rank: candidate.root_rank,
                    score: candidate_score,
                    efficiency: candidate.efficiency,
                    inputs: candidate.inputs.clone(),
                    game: candidate.game.clone(),
                    wins_immediately: candidate.wins_immediately,
                    attacks_opponent_drainer: candidate.attacks_opponent_drainer,
                    own_drainer_vulnerable: candidate.own_drainer_vulnerable,
                    own_drainer_walk_vulnerable: candidate.own_drainer_walk_vulnerable,
                    spirit_development: candidate.spirit_development,
                    keeps_awake_spirit_on_base: candidate.keeps_awake_spirit_on_base,
                    mana_handoff_to_opponent: candidate.mana_handoff_to_opponent,
                    has_roundtrip: candidate.has_roundtrip,
                    scores_supermana_this_turn: candidate.scores_supermana_this_turn,
                    scores_opponent_mana_this_turn: candidate.scores_opponent_mana_this_turn,
                    safe_supermana_pickup_now: candidate.safe_supermana_pickup_now,
                    safe_opponent_mana_pickup_now: candidate.safe_opponent_mana_pickup_now,
                    safe_supermana_progress_steps: candidate.safe_supermana_progress_steps,
                    safe_opponent_mana_progress_steps: candidate.safe_opponent_mana_progress_steps,
                    score_path_best_steps: candidate.score_path_best_steps,
                    same_turn_score_window_value: candidate.same_turn_score_window_value,
                    spirit_setup_gain: candidate.spirit_setup_gain,
                    spirit_same_turn_score_setup_now: candidate.spirit_same_turn_score_setup_now,
                    spirit_own_mana_setup_now: candidate.spirit_own_mana_setup_now,
                    supermana_progress: candidate.supermana_progress,
                    opponent_mana_progress: candidate.opponent_mana_progress,
                    interview_soft_priority: candidate.interview_soft_priority,
                    classes: candidate.classes,
                });

                if candidate_score > state.alpha {
                    state.alpha = candidate_score;
                }

                state.next_index += 1;
                state.next_index >= state.root_moves.len()
                    || state.visited_nodes >= state.config.max_visited_nodes
            }
        }
    }

    #[cfg(test)]
    fn finalize_async_search(state: &mut AsyncSmartSearchState) -> OutputModel {
        if let Some(output) = state.pending_output.take() {
            return output;
        }
        if state.scored_roots.is_empty() {
            return Self::automove_game(&mut state.game);
        }

        let best_inputs = Self::pick_root_move_with_exploration(
            &state.game,
            &state.scored_roots,
            state.perspective,
            state.config,
        );
        let input_fen = Input::fen_from_array(&best_inputs);
        let output = state.game.process_input(best_inputs, false, false);
        OutputModel::new(output, input_fen.as_str())
    }

    fn events_include_opponent_drainer_fainted(events: &[Event], perspective: Color) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::MonFainted { mon, .. }
                    if mon.kind == MonKind::Drainer && mon.color == perspective.other()
            )
        })
    }

    fn events_include_any_drainer_fainted(events: &[Event]) -> bool {
        events.iter().any(|event| {
            matches!(
                event,
                Event::MonFainted { mon, .. } if mon.kind == MonKind::Drainer
            )
        })
    }

    pub(crate) fn is_location_guarded_by_angel(
        board: &Board,
        color: Color,
        location: Location,
    ) -> bool {
        board
            .find_awake_angel(color)
            .is_some_and(|angel_location| angel_location.distance(&location) == 1)
    }

    #[inline]
    fn own_awake_drainer_location_and_fainted(
        board: &Board,
        perspective: Color,
    ) -> (Option<Location>, bool) {
        for (location, item) in board.occupied() {
            let Some(mon) = item.mon() else {
                continue;
            };
            if mon.color != perspective || mon.kind != MonKind::Drainer {
                continue;
            }
            if mon.is_fainted() {
                return (None, true);
            }
            return (Some(location), false);
        }
        (None, false)
    }

    fn own_drainer_carries_specific_mana_safely(
        board: &Board,
        perspective: Color,
        wanted_mana: Mana,
    ) -> bool {
        board.occupied().any(|(location, item)| {
            matches!(
                item,
                Item::MonWithMana { mon, mana }
                    if mon.color == perspective
                        && mon.kind == MonKind::Drainer
                        && !mon.is_fainted()
                        && *mana == wanted_mana
                        && crate::models::automove_exact::is_drainer_exactly_safe_next_turn_on_board(
                            board,
                            perspective,
                            location,
                        )
            )
        })
    }

    fn is_own_drainer_immediately_vulnerable_on_board(
        board: &Board,
        perspective: Color,
        opponent_to_move: bool,
        opponent_remaining_moves: i32,
        opponent_can_use_action: bool,
        _enhanced: bool,
    ) -> bool {
        let (own_drainer_location, own_drainer_fainted) =
            Self::own_awake_drainer_location_and_fainted(board, perspective);
        if own_drainer_fainted {
            return true;
        }
        let Some(own_drainer_location) = own_drainer_location else {
            return false;
        };

        if !opponent_to_move || !opponent_can_use_action {
            return false;
        }
        can_attack_target_on_board(
            board,
            perspective.other(),
            perspective,
            own_drainer_location,
            opponent_remaining_moves.max(0),
            opponent_can_use_action,
        )
    }

    #[cfg(test)]
    fn is_own_drainer_immediately_vulnerable(
        game: &MonsGame,
        perspective: Color,
        enhanced: bool,
    ) -> bool {
        Self::is_own_drainer_immediately_vulnerable_on_board(
            &game.board,
            perspective,
            game.active_color == perspective.other(),
            if game.active_color == perspective.other() {
                (Config::MONS_MOVES_PER_TURN - game.mons_moves_count).max(0)
            } else {
                0
            },
            game.player_can_use_action(),
            enhanced,
        )
    }

    fn is_own_drainer_vulnerable_next_turn(
        game: &MonsGame,
        perspective: Color,
        enhanced: bool,
    ) -> bool {
        Self::is_own_drainer_immediately_vulnerable_on_board(
            &game.board,
            perspective,
            true,
            Config::MONS_MOVES_PER_TURN,
            !game.is_first_turn(),
            enhanced,
        )
    }

    pub(crate) fn is_own_drainer_walk_vulnerable_next_turn(
        game: &MonsGame,
        perspective: Color,
        enhanced: bool,
    ) -> bool {
        let opponent_can_use_action = !game.is_first_turn();
        if Self::is_own_drainer_immediately_vulnerable_on_board(
            &game.board,
            perspective,
            true,
            Config::MONS_MOVES_PER_TURN,
            opponent_can_use_action,
            enhanced,
        ) {
            return false;
        }
        let (drainer_location, own_drainer_fainted) =
            Self::own_awake_drainer_location_and_fainted(&game.board, perspective);
        if own_drainer_fainted {
            return false;
        }
        let Some(drainer_location) = drainer_location else {
            return false;
        };
        let drainer_angel_protected =
            Self::is_location_guarded_by_angel(&game.board, perspective, drainer_location);
        is_drainer_under_walk_threat(
            &game.board,
            perspective,
            drainer_location,
            drainer_angel_protected,
        )
    }

    fn should_prefer_spirit_development(game: &MonsGame, perspective: Color) -> bool {
        !game.is_first_turn()
            && game.player_can_move_mon()
            && Self::has_awake_spirit_on_base(&game.board, perspective)
    }

    fn score_for_color(game: &MonsGame, color: Color) -> i32 {
        if color == Color::White {
            game.white_score
        } else {
            game.black_score
        }
    }

    fn potions_for_color(game: &MonsGame, color: Color) -> i32 {
        if color == Color::White {
            game.white_potions_count
        } else {
            game.black_potions_count
        }
    }

    fn should_prefer_potion_takeback_lines(game: &MonsGame, perspective: Color) -> bool {
        game.active_color == perspective
            && !game.is_first_turn()
            && !game.player_can_move_mon()
            && game.actions_used_count >= Config::ACTIONS_PER_TURN
            && game.player_can_move_mana()
            && Self::potions_for_color(game, perspective) > 0
    }

    fn root_spends_potion(
        game_before: &MonsGame,
        root: &RootEvaluation,
        perspective: Color,
    ) -> bool {
        Self::potions_for_color(&root.game, perspective)
            < Self::potions_for_color(game_before, perspective)
    }

    fn root_potion_spend_compensated(
        game_before: &MonsGame,
        root: &RootEvaluation,
        perspective: Color,
    ) -> bool {
        root.wins_immediately
            || root.attacks_opponent_drainer
            || Self::score_for_color(&root.game, perspective)
                >= Self::score_for_color(game_before, perspective).saturating_add(2)
            || root.scores_supermana_this_turn
            || root.scores_opponent_mana_this_turn
            || (!root.own_drainer_vulnerable
                && (root.supermana_progress || root.opponent_mana_progress))
    }

    fn root_allows_immediate_opponent_win_quick(
        state_after_move: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> bool {
        let reply_limit = config.root_anti_help_reply_limit.max(1);
        let snapshot =
            Self::root_reply_risk_snapshot(state_after_move, perspective, config, reply_limit);
        snapshot.allows_immediate_opponent_win
    }

    fn filtered_root_candidate_indices(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Vec<usize> {
        if scored_roots.is_empty() {
            return Vec::new();
        }

        let mut candidate_indices = (0..scored_roots.len()).collect::<Vec<_>>();
        let mut forced_attack_applied = false;

        if candidate_indices
            .iter()
            .any(|index| scored_roots[*index].wins_immediately)
        {
            candidate_indices.retain(|index| scored_roots[*index].wins_immediately);
            return candidate_indices;
        }

        if candidate_indices
            .iter()
            .any(|index| scored_roots[*index].attacks_opponent_drainer)
        {
            candidate_indices.retain(|index| scored_roots[*index].attacks_opponent_drainer);
            forced_attack_applied = true;
        }

        if !forced_attack_applied
            && !candidate_indices
                .iter()
                .any(|index| scored_roots[*index].classes.immediate_score)
        {
            let best_score_window = candidate_indices
                .iter()
                .filter_map(|index| {
                    let root = &scored_roots[*index];
                    (!root.own_drainer_vulnerable && !root.mana_handoff_to_opponent)
                        .then_some(root.same_turn_score_window_value)
                })
                .max()
                .unwrap_or(0);
            if best_score_window > 0 {
                let window_indices = candidate_indices
                    .iter()
                    .copied()
                    .filter(|index| {
                        let root = &scored_roots[*index];
                        root.same_turn_score_window_value == best_score_window
                            && !root.own_drainer_vulnerable
                            && !root.mana_handoff_to_opponent
                    })
                    .collect::<Vec<_>>();
                if !window_indices.is_empty() {
                    candidate_indices = window_indices;
                }
            }
        }

        if !forced_attack_applied
            && candidate_indices
                .iter()
                .any(|index| scored_roots[*index].safe_supermana_pickup_now)
        {
            candidate_indices.retain(|index| {
                let root = &scored_roots[*index];
                root.scores_supermana_this_turn || root.safe_supermana_pickup_now
            });
        } else if !forced_attack_applied
            && candidate_indices
                .iter()
                .any(|index| scored_roots[*index].safe_opponent_mana_pickup_now)
        {
            candidate_indices.retain(|index| {
                let root = &scored_roots[*index];
                root.scores_opponent_mana_this_turn || root.safe_opponent_mana_pickup_now
            });
        }

        if !forced_attack_applied
            && !candidate_indices
                .iter()
                .any(|index| scored_roots[*index].classes.immediate_score)
        {
            let best_spirit_score_window = candidate_indices
                .iter()
                .filter_map(|index| {
                    let root = &scored_roots[*index];
                    (root.spirit_same_turn_score_setup_now
                        && !root.own_drainer_vulnerable
                        && !root.mana_handoff_to_opponent)
                        .then_some(root.same_turn_score_window_value)
                })
                .max()
                .unwrap_or(0);
            let best_non_spirit_score_window = candidate_indices
                .iter()
                .filter_map(|index| {
                    let root = &scored_roots[*index];
                    (!root.spirit_same_turn_score_setup_now
                        && !root.own_drainer_vulnerable
                        && !root.mana_handoff_to_opponent)
                        .then_some(root.same_turn_score_window_value)
                })
                .max()
                .unwrap_or(0);
            if best_spirit_score_window > best_non_spirit_score_window {
                candidate_indices.retain(|index| {
                    let root = &scored_roots[*index];
                    root.spirit_same_turn_score_setup_now
                        && root.same_turn_score_window_value == best_spirit_score_window
                        && !root.own_drainer_vulnerable
                        && !root.mana_handoff_to_opponent
                });
            }
        }

        if !forced_attack_applied
            && !candidate_indices
                .iter()
                .any(|index| scored_roots[*index].classes.immediate_score)
            && candidate_indices.iter().any(|index| {
                let root = &scored_roots[*index];
                root.same_turn_score_window_value > 0
                    && !root.own_drainer_vulnerable
                    && !root.mana_handoff_to_opponent
            })
        {
            let best_score_window = candidate_indices
                .iter()
                .map(|index| scored_roots[*index].same_turn_score_window_value)
                .max()
                .unwrap_or(0);
            if best_score_window > 0 {
                let window_indices = candidate_indices
                    .iter()
                    .copied()
                    .filter(|index| {
                        let root = &scored_roots[*index];
                        root.same_turn_score_window_value == best_score_window
                            && !root.own_drainer_vulnerable
                            && !root.mana_handoff_to_opponent
                    })
                    .collect::<Vec<_>>();
                if !window_indices.is_empty() {
                    candidate_indices = window_indices;
                }
            }
        }

        if !forced_attack_applied {
            let spirit_setup_indices = candidate_indices
                .iter()
                .copied()
                .filter(|index| {
                    let root = &scored_roots[*index];
                    root.spirit_own_mana_setup_now
                        && !root.own_drainer_vulnerable
                        && !root.mana_handoff_to_opponent
                })
                .collect::<Vec<_>>();
            if !spirit_setup_indices.is_empty() {
                let progress_competes = Self::safe_progress_competes_with_spirit_pref(
                    scored_roots,
                    candidate_indices.as_slice(),
                    config.turn_engine_mode,
                );
                let followup_progress_competes = Self::followup_progress_competes_with_spirit_pref(
                    game,
                    scored_roots,
                    candidate_indices.as_slice(),
                    perspective,
                    config,
                );
                let risky_score_competes = Self::risky_score_competes_with_spirit_pref(
                    scored_roots,
                    candidate_indices.as_slice(),
                    config.turn_engine_mode,
                );
                let negative_deny_competes = Self::negative_deny_competes_with_spirit_pref(
                    scored_roots,
                    candidate_indices.as_slice(),
                    perspective,
                    config,
                );
                let negative_deny_competes = if negative_deny_competes
                    && spirit_setup_indices.iter().copied().any(|spirit_index| {
                        candidate_indices
                            .iter()
                            .copied()
                            .filter(|index| !scored_roots[*index].spirit_development)
                            .all(|index| {
                                Self::current_pro_spirit_setup_competes_with_non_spirit_root(
                                    game,
                                    &scored_roots[spirit_index],
                                    &scored_roots[index],
                                    perspective,
                                    config,
                                )
                            })
                    }) {
                    false
                } else {
                    negative_deny_competes
                };
                let score_competes = Self::score_competes_with_spirit_pref(
                    scored_roots,
                    candidate_indices.as_slice(),
                    config.turn_engine_mode,
                );
                let projection_competes = Self::projection_competes_with_spirit_pref(
                    scored_roots,
                    candidate_indices.as_slice(),
                    perspective,
                    config,
                );
                let risky_recovery_competes = Self::risky_recovery_competes_with_spirit_pref(
                    game,
                    scored_roots,
                    candidate_indices.as_slice(),
                    perspective,
                    config,
                );
                if !(progress_competes
                    || followup_progress_competes
                    || risky_score_competes
                    || negative_deny_competes
                    || score_competes
                    || projection_competes
                    || risky_recovery_competes)
                {
                    let unknown_progress_steps = Config::BOARD_SIZE + 4;
                    let spirit_supermana_indices = spirit_setup_indices
                        .iter()
                        .copied()
                        .filter(|index| scored_roots[*index].supermana_progress)
                        .collect::<Vec<_>>();
                    if !spirit_supermana_indices.is_empty() {
                        let best_supermana_steps = spirit_supermana_indices
                            .iter()
                            .map(|index| scored_roots[*index].safe_supermana_progress_steps)
                            .filter(|steps| *steps < unknown_progress_steps)
                            .min();
                        candidate_indices = if let Some(best_steps) = best_supermana_steps {
                            spirit_supermana_indices
                                .into_iter()
                                .filter(|index| {
                                    scored_roots[*index].safe_supermana_progress_steps == best_steps
                                })
                                .collect()
                        } else {
                            spirit_supermana_indices
                        };
                    } else {
                        let spirit_opponent_indices = spirit_setup_indices
                            .iter()
                            .copied()
                            .filter(|index| scored_roots[*index].opponent_mana_progress)
                            .collect::<Vec<_>>();
                        if !spirit_opponent_indices.is_empty() {
                            let best_opponent_steps = spirit_opponent_indices
                                .iter()
                                .map(|index| scored_roots[*index].safe_opponent_mana_progress_steps)
                                .filter(|steps| *steps < unknown_progress_steps)
                                .min();
                            candidate_indices = if let Some(best_steps) = best_opponent_steps {
                                spirit_opponent_indices
                                    .into_iter()
                                    .filter(|index| {
                                        scored_roots[*index].safe_opponent_mana_progress_steps
                                            == best_steps
                                    })
                                    .collect()
                            } else {
                                spirit_opponent_indices
                            };
                        } else {
                            let unknown_score_path_steps = Config::BOARD_SIZE * 3;
                            let best_score_path_steps = spirit_setup_indices
                                .iter()
                                .map(|index| scored_roots[*index].score_path_best_steps)
                                .filter(|steps| *steps < unknown_score_path_steps)
                                .min();
                            candidate_indices = if let Some(best_steps) = best_score_path_steps {
                                spirit_setup_indices
                                    .into_iter()
                                    .filter(|index| {
                                        scored_roots[*index].score_path_best_steps == best_steps
                                    })
                                    .collect()
                            } else {
                                spirit_setup_indices
                            };
                        }
                    }
                }
            }
        }

        if config.enable_interview_hard_spirit_deploy
            && !forced_attack_applied
            && Self::should_prefer_spirit_development(game, perspective)
        {
            let has_safe_high_value_pickup = candidate_indices.iter().any(|index| {
                let root = &scored_roots[*index];
                root.scores_supermana_this_turn
                    || root.scores_opponent_mana_this_turn
                    || root.safe_supermana_pickup_now
                    || root.safe_opponent_mana_pickup_now
            });
            let progress_competes = Self::safe_progress_competes_with_spirit_pref(
                scored_roots,
                candidate_indices.as_slice(),
                config.turn_engine_mode,
            );
            let followup_progress_competes = Self::followup_progress_competes_with_spirit_pref(
                game,
                scored_roots,
                candidate_indices.as_slice(),
                perspective,
                config,
            );
            let risky_score_competes = Self::risky_score_competes_with_spirit_pref(
                scored_roots,
                candidate_indices.as_slice(),
                config.turn_engine_mode,
            );
            let negative_deny_competes = Self::negative_deny_competes_with_spirit_pref(
                scored_roots,
                candidate_indices.as_slice(),
                perspective,
                config,
            );
            let score_competes = Self::score_competes_with_spirit_pref(
                scored_roots,
                candidate_indices.as_slice(),
                config.turn_engine_mode,
            );
            let projection_competes = Self::projection_competes_with_spirit_pref(
                scored_roots,
                candidate_indices.as_slice(),
                perspective,
                config,
            );
            let risky_recovery_competes = Self::risky_recovery_competes_with_spirit_pref(
                game,
                scored_roots,
                candidate_indices.as_slice(),
                perspective,
                config,
            );
            if has_safe_high_value_pickup
                || progress_competes
                || followup_progress_competes
                || risky_score_competes
                || negative_deny_competes
                || score_competes
                || projection_competes
                || risky_recovery_competes
            {
                // Interview priority puts safe supermana/opponent-mana conversion above
                // default spirit deployment.
            } else {
                let spirit_setup_indices = candidate_indices
                    .iter()
                    .copied()
                    .filter(|index| {
                        let root = &scored_roots[*index];
                        root.spirit_own_mana_setup_now
                            && !root.own_drainer_vulnerable
                            && !root.mana_handoff_to_opponent
                    })
                    .collect::<Vec<_>>();
                if !spirit_setup_indices.is_empty() {
                    candidate_indices = spirit_setup_indices;
                } else {
                    let my_score_before = Self::score_for_color(game, perspective);
                    let spirit_ready_indices = candidate_indices
                        .iter()
                        .copied()
                        .filter(|index| !scored_roots[*index].keeps_awake_spirit_on_base)
                        .collect::<Vec<_>>();
                    if !spirit_ready_indices.is_empty() {
                        let safe_spirit_ready_indices = spirit_ready_indices
                            .iter()
                            .copied()
                            .filter(|index| {
                                let root = &scored_roots[*index];
                                !root.own_drainer_vulnerable && !root.mana_handoff_to_opponent
                            })
                            .collect::<Vec<_>>();
                        let preferred_spirit_indices = if !safe_spirit_ready_indices.is_empty() {
                            safe_spirit_ready_indices
                        } else {
                            spirit_ready_indices
                        };

                        let keeps_spirit_and_scores = candidate_indices.iter().any(|index| {
                            let root = &scored_roots[*index];
                            root.keeps_awake_spirit_on_base
                                && Self::score_for_color(&root.game, perspective) > my_score_before
                        });
                        let spirit_line_scores = preferred_spirit_indices.iter().any(|index| {
                            let root = &scored_roots[*index];
                            Self::score_for_color(&root.game, perspective) > my_score_before
                        });

                        if !keeps_spirit_and_scores || spirit_line_scores {
                            candidate_indices = preferred_spirit_indices;
                        }
                    }
                }
            }
        }

        if !forced_attack_applied {
            let pre_safety_candidate_indices = candidate_indices.clone();
            let best_score = candidate_indices
                .iter()
                .map(|index| scored_roots[*index].score)
                .max()
                .unwrap_or(i32::MIN);
            let margin = config.root_drainer_safety_score_margin.max(0);
            let mut recovery_utility_cache =
                std::collections::HashMap::<usize, Option<TurnEngineUtility>>::new();
            let recovery_setup_indices = if config.enable_turn_engine_selector
                && matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            {
                candidate_indices
                    .iter()
                    .copied()
                    .filter(|index| {
                        let root = &scored_roots[*index];
                        if !root.own_drainer_vulnerable
                            || root.mana_handoff_to_opponent
                            || root.has_roundtrip
                            || root.score + margin < best_score
                            || !Self::can_challenge_spirit_preference_root_with_recovery_projection(
                                root,
                                perspective,
                            )
                        {
                            return false;
                        }
                        true
                    })
                    .collect::<Vec<_>>()
            } else {
                Vec::new()
            };
            let safer_indices = candidate_indices
                .iter()
                .copied()
                .filter(|index| {
                    let root = &scored_roots[*index];
                    !root.own_drainer_vulnerable && root.score + margin >= best_score
                })
                .collect::<Vec<_>>();
            let best_safe_utility = if matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && !safer_indices.is_empty()
                && !recovery_setup_indices.is_empty()
            {
                safer_indices
                    .iter()
                    .copied()
                    .map(|index| {
                        let root = &scored_roots[index];
                        let family = Self::turn_engine_root_evaluation_family(root);
                        Self::turn_engine_selected_override_utility(
                            game,
                            root,
                            perspective,
                            config,
                            family,
                        )
                    })
                    .max()
            } else {
                None
            };
            let best_safe_score = safer_indices
                .iter()
                .copied()
                .map(|index| scored_roots[index].score)
                .max()
                .unwrap_or(i32::MIN);
            if !safer_indices.is_empty() {
                candidate_indices = safer_indices;
                for index in recovery_setup_indices {
                    let root = &scored_roots[index];
                    let recovery_head_signal = root.attacks_opponent_drainer
                        || root.classes.drainer_safety_recover
                        || root.same_turn_score_window_value > 0
                        || root.spirit_same_turn_score_setup_now
                        || root.spirit_own_mana_setup_now
                        || Self::turn_engine_root_evaluation_has_progress_surface(root);
                    let recovery_score_gap = best_safe_score.saturating_sub(root.score);
                    let recovery_score_competes = if recovery_head_signal {
                        recovery_score_gap <= 48
                    } else {
                        recovery_score_gap <= 24
                    };
                    if !recovery_score_competes {
                        continue;
                    }
                    let Some(recovery_utility) =
                        *recovery_utility_cache.entry(index).or_insert_with(|| {
                            Self::projected_risky_recovery_root_utility(
                                game,
                                root,
                                perspective,
                                config,
                            )
                        })
                    else {
                        continue;
                    };
                    if let Some(best_safe_utility) = best_safe_utility {
                        let strong_recovery = Self::projected_recovery_utility_competes(
                            recovery_utility,
                            best_safe_utility,
                        );
                        if !strong_recovery {
                            continue;
                        }
                    }
                    if !candidate_indices.contains(&index) {
                        candidate_indices.push(index);
                    }
                }
                if let Some(index) = Self::current_pro_safe_progress_reentry_after_safety_prefilter(
                    game,
                    scored_roots,
                    pre_safety_candidate_indices.as_slice(),
                    candidate_indices.as_slice(),
                    perspective,
                    config,
                ) {
                    if !candidate_indices.contains(&index) {
                        candidate_indices.push(index);
                        candidate_indices.sort_by(|a, b| {
                            Self::compare_ranked_scored_root_indices(scored_roots, *a, *b)
                        });
                    }
                }
            }
        }

        if config.enable_root_spirit_development_pref
            && Self::should_prefer_spirit_development(game, perspective)
            && candidate_indices
                .iter()
                .any(|index| scored_roots[*index].spirit_development)
        {
            let has_safe_high_value_pickup = candidate_indices.iter().any(|index| {
                let root = &scored_roots[*index];
                root.scores_supermana_this_turn
                    || root.scores_opponent_mana_this_turn
                    || root.safe_supermana_pickup_now
                    || root.safe_opponent_mana_pickup_now
            });
            let progress_competes = Self::safe_progress_competes_with_spirit_pref(
                scored_roots,
                candidate_indices.as_slice(),
                config.turn_engine_mode,
            );
            let followup_progress_competes = Self::followup_progress_competes_with_spirit_pref(
                game,
                scored_roots,
                candidate_indices.as_slice(),
                perspective,
                config,
            );
            let risky_score_competes = Self::risky_score_competes_with_spirit_pref(
                scored_roots,
                candidate_indices.as_slice(),
                config.turn_engine_mode,
            );
            let negative_deny_competes = Self::negative_deny_competes_with_spirit_pref(
                scored_roots,
                candidate_indices.as_slice(),
                perspective,
                config,
            );
            let score_competes = Self::score_competes_with_spirit_pref(
                scored_roots,
                candidate_indices.as_slice(),
                config.turn_engine_mode,
            );
            let projection_competes = Self::projection_competes_with_spirit_pref(
                scored_roots,
                candidate_indices.as_slice(),
                perspective,
                config,
            );
            let risky_recovery_competes = Self::risky_recovery_competes_with_spirit_pref(
                game,
                scored_roots,
                candidate_indices.as_slice(),
                perspective,
                config,
            );
            if has_safe_high_value_pickup
                || progress_competes
                || followup_progress_competes
                || risky_score_competes
                || negative_deny_competes
                || score_competes
                || projection_competes
                || risky_recovery_competes
            {
                // Keep the safe high-value pickup candidates instead of forcing spirit deploy.
            } else {
                let best_score = candidate_indices
                    .iter()
                    .map(|index| scored_roots[*index].score)
                    .max()
                    .unwrap_or(i32::MIN);
                let margin = SMART_ROOT_SPIRIT_DEVELOPMENT_SCORE_MARGIN.max(0);
                let spirit_setup_indices = candidate_indices
                    .iter()
                    .copied()
                    .filter(|index| {
                        let root = &scored_roots[*index];
                        root.spirit_own_mana_setup_now && root.score + margin >= best_score
                    })
                    .collect::<Vec<_>>();
                if !spirit_setup_indices.is_empty() {
                    candidate_indices = spirit_setup_indices;
                } else {
                    let spirit_indices = candidate_indices
                        .iter()
                        .copied()
                        .filter(|index| {
                            let root = &scored_roots[*index];
                            root.spirit_development && root.score + margin >= best_score
                        })
                        .collect::<Vec<_>>();
                    if !spirit_indices.is_empty() {
                        candidate_indices = spirit_indices;
                    }
                }
            }
        }

        if !forced_attack_applied
            && candidate_indices.len() > 1
            && Self::should_prefer_potion_takeback_lines(game, perspective)
        {
            let best_score = candidate_indices
                .iter()
                .map(|index| scored_roots[*index].score)
                .max()
                .unwrap_or(i32::MIN);
            let margin = SMART_ROOT_POTION_HOLD_SCORE_MARGIN.max(0);
            let near_best = candidate_indices
                .iter()
                .copied()
                .filter(|index| scored_roots[*index].score + margin >= best_score)
                .collect::<Vec<_>>();

            if near_best.len() > 1 {
                let mut quick_loss_cache = std::collections::HashMap::<usize, bool>::new();
                let has_non_potion_non_losing_alternative =
                    near_best.iter().copied().any(|index| {
                        let root = &scored_roots[index];
                        if Self::root_spends_potion(game, root, perspective) {
                            return false;
                        }
                        let allows_loss = *quick_loss_cache.entry(index).or_insert_with(|| {
                            Self::root_allows_immediate_opponent_win_quick(
                                &root.game,
                                perspective,
                                config,
                            )
                        });
                        !allows_loss
                    });

                if has_non_potion_non_losing_alternative {
                    let near_best_set = near_best
                        .iter()
                        .copied()
                        .collect::<std::collections::HashSet<_>>();
                    let strict_indices = candidate_indices
                        .iter()
                        .copied()
                        .filter(|index| {
                            let root = &scored_roots[*index];
                            if root.wins_immediately || !near_best_set.contains(index) {
                                return true;
                            }
                            !Self::root_spends_potion(game, root, perspective)
                                || Self::root_potion_spend_compensated(game, root, perspective)
                        })
                        .collect::<Vec<_>>();
                    if !strict_indices.is_empty() {
                        candidate_indices = strict_indices;
                    }
                }
            }
        }

        if candidate_indices.len() > 1 {
            let best_score = candidate_indices
                .iter()
                .map(|index| scored_roots[*index].score)
                .max()
                .unwrap_or(i32::MIN);
            let margin = config.root_anti_help_score_margin.max(0);
            let near_best = candidate_indices
                .iter()
                .copied()
                .filter(|index| scored_roots[*index].score + margin >= best_score)
                .collect::<Vec<_>>();
            if near_best.len() > 1 {
                let mut quick_loss_cache = std::collections::HashMap::<usize, bool>::new();
                let has_clean_non_losing_alternative = near_best.iter().copied().any(|index| {
                    let root = &scored_roots[index];
                    if root.mana_handoff_to_opponent || root.has_roundtrip {
                        return false;
                    }
                    let allows_loss = *quick_loss_cache.entry(index).or_insert_with(|| {
                        Self::root_allows_immediate_opponent_win_quick(
                            &root.game,
                            perspective,
                            config,
                        )
                    });
                    !allows_loss
                });

                if has_clean_non_losing_alternative {
                    let near_best_set = near_best
                        .iter()
                        .copied()
                        .collect::<std::collections::HashSet<_>>();
                    let mut strict_indices = candidate_indices
                        .iter()
                        .copied()
                        .filter(|index| {
                            let root = &scored_roots[*index];
                            if root.wins_immediately || !near_best_set.contains(index) {
                                return true;
                            }
                            !root.mana_handoff_to_opponent && !root.has_roundtrip
                        })
                        .collect::<Vec<_>>();
                    if !strict_indices.is_empty() {
                        candidate_indices = std::mem::take(&mut strict_indices);
                    }
                }
            }
        }

        if let Some(index) = Self::current_pro_plain_spirit_cluster_progress_reentry(
            game,
            scored_roots,
            candidate_indices.as_slice(),
            perspective,
            config,
        ) {
            if !candidate_indices.contains(&index) {
                candidate_indices.push(index);
                candidate_indices
                    .sort_by(|a, b| Self::compare_ranked_scored_root_indices(scored_roots, *a, *b));
            }
        }
        if let Some(index) = Self::current_pro_plain_spirit_cluster_risky_recovery_reentry(
            game,
            scored_roots,
            candidate_indices.as_slice(),
            perspective,
            config,
        ) {
            if !candidate_indices.contains(&index) {
                candidate_indices.push(index);
                candidate_indices
                    .sort_by(|a, b| Self::compare_ranked_scored_root_indices(scored_roots, *a, *b));
            }
        }

        candidate_indices
    }

    fn best_scored_root_index(
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
    ) -> usize {
        let mut best_index = candidate_indices.first().copied().unwrap_or(0);
        for index in candidate_indices.iter().copied() {
            if Self::compare_ranked_scored_root_indices(scored_roots, index, best_index)
                == std::cmp::Ordering::Less
            {
                best_index = index;
            }
        }
        best_index
    }

    fn current_pro_root_advisor_entry_from_root_move(
        root: &ScoredRootMove,
        reason: CurrentProRootAdvisorReasonCode,
    ) -> CurrentProRootAdvisorEntry {
        CurrentProRootAdvisorEntry {
            inputs: root.inputs.clone(),
            family: Self::turn_engine_root_move_family(root),
            root_rank: root.root_rank,
            reason,
        }
    }

    fn current_pro_root_advisor_entry_from_root_evaluation(
        root: &RootEvaluation,
        reason: CurrentProRootAdvisorReasonCode,
    ) -> CurrentProRootAdvisorEntry {
        CurrentProRootAdvisorEntry {
            inputs: root.inputs.clone(),
            family: Self::turn_engine_root_evaluation_family(root),
            root_rank: root.root_rank,
            reason,
        }
    }

    fn current_pro_root_advisor_push_unique(
        entries: &mut Vec<CurrentProRootAdvisorEntry>,
        entry: CurrentProRootAdvisorEntry,
    ) {
        if !entries
            .iter()
            .any(|existing| existing.inputs == entry.inputs)
        {
            entries.push(entry);
        }
    }

    fn is_current_pro_black_plain_spirit_followup_setup_pair(
        game: &MonsGame,
        plain: &RootEvaluation,
        setup: &RootEvaluation,
        config: AutomoveSearchConfig,
    ) -> bool {
        matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && game.active_color == Color::Black
            && game.turn_number <= 4
            && Self::is_plain_spirit_development_root(plain)
            && setup.spirit_own_mana_setup_now
            && !setup.spirit_same_turn_score_setup_now
            && plain.inputs.first() == setup.inputs.first()
            && plain.own_drainer_vulnerable == setup.own_drainer_vulnerable
            && plain.own_drainer_walk_vulnerable == setup.own_drainer_walk_vulnerable
            && !plain.mana_handoff_to_opponent
            && !setup.mana_handoff_to_opponent
            && !plain.has_roundtrip
            && !setup.has_roundtrip
            && !plain.wins_immediately
            && !setup.wins_immediately
            && !plain.attacks_opponent_drainer
            && !setup.attacks_opponent_drainer
            && !plain.scores_supermana_this_turn
            && !setup.scores_supermana_this_turn
            && !plain.scores_opponent_mana_this_turn
            && !setup.scores_opponent_mana_this_turn
            && !plain.safe_supermana_pickup_now
            && !setup.safe_supermana_pickup_now
            && !plain.safe_opponent_mana_pickup_now
            && !setup.safe_opponent_mana_pickup_now
            && plain.same_turn_score_window_value == 0
            && setup.same_turn_score_window_value == 0
            && !plain.supermana_progress
            && !setup.supermana_progress
            && !plain.opponent_mana_progress
            && !setup.opponent_mana_progress
    }

    fn is_current_pro_non_concrete_mana_window_root(root: &RootEvaluation) -> bool {
        Self::turn_engine_root_evaluation_family(root) == TurnPlanFamily::ManaTempo
            && root.same_turn_score_window_value > 0
            && root.same_turn_score_window_value <= 1
            && !root.wins_immediately
            && !root.attacks_opponent_drainer
            && !root.scores_supermana_this_turn
            && !root.scores_opponent_mana_this_turn
            && !root.safe_supermana_pickup_now
            && !root.safe_opponent_mana_pickup_now
            && !root.mana_handoff_to_opponent
            && !root.has_roundtrip
    }

    fn is_current_pro_black_spirit_progress_window_pair(
        game: &MonsGame,
        spirit: &RootEvaluation,
        mana: &RootEvaluation,
        config: AutomoveSearchConfig,
    ) -> bool {
        matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && game.active_color == Color::Black
            && game.turn_number <= 4
            && Self::turn_engine_root_evaluation_family(spirit) == TurnPlanFamily::SpiritImpact
            && !spirit.spirit_same_turn_score_setup_now
            && !spirit.spirit_own_mana_setup_now
            && Self::turn_engine_root_evaluation_has_progress_surface(spirit)
            && !spirit.wins_immediately
            && !spirit.attacks_opponent_drainer
            && !spirit.scores_supermana_this_turn
            && !spirit.scores_opponent_mana_this_turn
            && !spirit.safe_supermana_pickup_now
            && !spirit.safe_opponent_mana_pickup_now
            && !spirit.mana_handoff_to_opponent
            && !spirit.has_roundtrip
            && Self::is_current_pro_non_concrete_mana_window_root(mana)
            && spirit.own_drainer_vulnerable == mana.own_drainer_vulnerable
            && spirit.own_drainer_walk_vulnerable == mana.own_drainer_walk_vulnerable
    }

    fn is_current_pro_white_spirit_progress_window_pair(
        game: &MonsGame,
        spirit: &RootEvaluation,
        mana: &RootEvaluation,
        config: AutomoveSearchConfig,
    ) -> bool {
        matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && game.active_color == Color::White
            && game.turn_number >= 5
            && game.mons_moves_count == 0
            && game.player_can_use_action()
            && game.player_can_move_mana()
            && Self::turn_engine_root_evaluation_family(spirit) == TurnPlanFamily::SpiritImpact
            && !spirit.spirit_same_turn_score_setup_now
            && !spirit.spirit_own_mana_setup_now
            && Self::turn_engine_root_evaluation_has_progress_surface(spirit)
            && !spirit.wins_immediately
            && !spirit.attacks_opponent_drainer
            && !spirit.scores_supermana_this_turn
            && !spirit.scores_opponent_mana_this_turn
            && !spirit.safe_supermana_pickup_now
            && !spirit.safe_opponent_mana_pickup_now
            && !spirit.mana_handoff_to_opponent
            && !spirit.has_roundtrip
            && Self::is_current_pro_non_concrete_mana_window_root(mana)
            && spirit.own_drainer_vulnerable == mana.own_drainer_vulnerable
            && spirit.own_drainer_walk_vulnerable == mana.own_drainer_walk_vulnerable
    }

    fn current_pro_black_plain_spirit_followup_representative_competes(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        plain_index: usize,
        perspective: Color,
        config: AutomoveSearchConfig,
        followup_scores: &mut std::collections::HashMap<usize, i32>,
    ) -> bool {
        let plain = &scored_roots[plain_index];
        if !Self::is_plain_spirit_development_root(plain) {
            return false;
        }

        const FOLLOWUP_MARGIN: i32 = 32;
        const SETUP_GAIN_MARGIN: i32 = 32;
        const SETUP_SCORE_MARGIN: i32 = 64;

        let mut plain_competes = false;
        for index in shortlist.iter().copied() {
            let setup = &scored_roots[index];
            if !Self::is_current_pro_black_plain_spirit_followup_setup_pair(
                game, plain, setup, config,
            ) {
                continue;
            }

            let plain_followup = *followup_scores.entry(plain_index).or_insert_with(|| {
                Self::current_pro_spirit_followup_floor_score(&plain.game, perspective, config)
            });
            let setup_followup = *followup_scores.entry(index).or_insert_with(|| {
                Self::current_pro_spirit_followup_floor_score(&setup.game, perspective, config)
            });

            let setup_has_close_top_seed = setup.root_rank <= plain.root_rank
                && setup.score.saturating_add(SETUP_SCORE_MARGIN) >= plain.score
                && setup.spirit_setup_gain
                    >= plain.spirit_setup_gain.saturating_add(SETUP_GAIN_MARGIN);
            if setup_has_close_top_seed {
                return false;
            }

            plain_competes |= plain_followup >= setup_followup.saturating_add(FOLLOWUP_MARGIN)
                || (plain.score >= setup.score && plain_followup >= setup_followup);
        }

        plain_competes
    }

    #[allow(clippy::too_many_arguments)]
    fn current_pro_root_advisor_should_compete_in_approval_shortlist(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        ordered_shortlist: &[usize],
        index: usize,
        root: &RootEvaluation,
        reason: CurrentProRootAdvisorReasonCode,
        perspective: Color,
        config: AutomoveSearchConfig,
        followup_scores: &mut std::collections::HashMap<usize, i32>,
    ) -> bool {
        match reason {
            CurrentProRootAdvisorReasonCode::PreserveSpiritRepresentative => {
                root.spirit_same_turn_score_setup_now
                    || root.spirit_own_mana_setup_now
                    || Self::current_pro_black_plain_spirit_followup_representative_competes(
                        game,
                        scored_roots,
                        ordered_shortlist,
                        index,
                        perspective,
                        config,
                        followup_scores,
                    )
            }
            _ => true,
        }
    }

    fn current_pro_root_advisor_black_family_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        reply_risk_shortlist: &[usize],
        selection_indices: &[usize],
        approved_index: usize,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if !matches!(
            Self::turn_engine_root_evaluation_family(approved),
            TurnPlanFamily::ManaTempo
        ) {
            return None;
        }
        if approved_index > 1
            && !(game.active_color == Color::Black
                && game.turn_number <= 2
                && game.player_can_use_action()
                && game.player_can_move_mana())
        {
            return None;
        }
        let approved_non_concrete_window =
            Self::is_current_pro_non_concrete_mana_window_root(approved);
        if approved.wins_immediately
            || approved.attacks_opponent_drainer
            || (approved.same_turn_score_window_value > 0 && !approved_non_concrete_window)
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
        {
            return None;
        }

        let approved_utility = Self::turn_engine_root_plan_utility(
            game,
            approved,
            perspective,
            config,
            TurnPlanFamily::ManaTempo,
        );
        let approved_unsafe = Self::turn_engine_root_evaluation_is_unsafe(approved);
        let approved_progress_surface =
            Self::turn_engine_root_evaluation_has_progress_surface(approved);
        let root_node_budget = ((config.max_visited_nodes
            * config.root_reply_risk_node_share_bp.max(0) as usize)
            / 10_000)
            .max(reply_risk_shortlist.len())
            .max(1);
        let per_root_reply_limit = (root_node_budget / reply_risk_shortlist.len().max(1))
            .max(1)
            .min(config.root_reply_risk_reply_limit.max(1));
        let mut reply_snapshots = std::collections::HashMap::<usize, RootReplyRiskSnapshot>::new();
        let mut spirit_followup_scores = std::collections::HashMap::<usize, i32>::new();

        let mut qualifying_indices = Vec::new();
        for index in selection_indices.iter().copied() {
            if index == approved_index {
                continue;
            }

            let challenger = &scored_roots[index];
            let family = Self::turn_engine_root_evaluation_family(challenger);
            let concrete_spirit_setup =
                challenger.spirit_own_mana_setup_now || challenger.spirit_same_turn_score_setup_now;
            let spirit_progress_family = family == TurnPlanFamily::SpiritImpact
                && !concrete_spirit_setup
                && Self::turn_engine_root_evaluation_has_progress_surface(challenger);
            let progress_family = matches!(
                family,
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            );
            let progress_family_allowed = progress_family && !approved_non_concrete_window;
            let concrete_spirit_setup_allowed =
                concrete_spirit_setup && !approved_non_concrete_window && game.turn_number <= 4;
            if !concrete_spirit_setup_allowed && !progress_family_allowed && !spirit_progress_family
            {
                continue;
            }

            let challenger_utility = Self::turn_engine_selected_override_utility(
                game,
                challenger,
                perspective,
                config,
                family,
            );
            let progress_surface =
                Self::turn_engine_root_evaluation_has_progress_surface(challenger);
            let progress_better = Self::root_progress_or_setup_better(challenger, approved)
                || (progress_surface && !approved_progress_surface);
            let approved_safe_mana_root_blocks_plain_spirit_progress = family
                == TurnPlanFamily::SpiritImpact
                && !approved_unsafe
                && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                && Self::turn_engine_root_evaluation_family(approved) == TurnPlanFamily::ManaTempo
                && !approved_progress_surface
                && !approved.spirit_development
                && !approved.spirit_same_turn_score_setup_now
                && !approved.spirit_own_mana_setup_now
                && challenger.spirit_development
                && !challenger.spirit_same_turn_score_setup_now
                && !challenger.spirit_own_mana_setup_now
                && progress_surface
                && !challenger.wins_immediately
                && !challenger.attacks_opponent_drainer
                && !challenger.scores_supermana_this_turn
                && !challenger.scores_opponent_mana_this_turn
                && !challenger.safe_supermana_pickup_now
                && !challenger.safe_opponent_mana_pickup_now
                && !challenger.mana_handoff_to_opponent
                && !challenger.has_roundtrip
                && challenger.same_turn_score_window_value == 0
                && challenger.score <= approved.score;
            if approved_safe_mana_root_blocks_plain_spirit_progress {
                continue;
            }
            let early_black_progress_score_override = progress_family_allowed
                && game.turn_number <= 6
                && approved.score < 0
                && challenger.score >= 0
                && progress_better;
            let black_spirit_progress_window_reply_override = spirit_progress_family
                && approved_non_concrete_window
                && Self::is_current_pro_black_spirit_progress_window_pair(
                    game, challenger, approved, config,
                )
                && challenger.root_rank <= approved.root_rank.saturating_add(8)
                && {
                    let challenger_snapshot = *reply_snapshots.entry(index).or_insert_with(|| {
                        Self::root_reply_risk_snapshot(
                            &challenger.game,
                            perspective,
                            config,
                            per_root_reply_limit,
                        )
                    });
                    let approved_snapshot =
                        *reply_snapshots.entry(approved_index).or_insert_with(|| {
                            Self::root_reply_risk_snapshot(
                                &approved.game,
                                perspective,
                                config,
                                per_root_reply_limit,
                            )
                        });
                    let challenger_followup =
                        *spirit_followup_scores.entry(index).or_insert_with(|| {
                            Self::current_pro_spirit_followup_floor_score(
                                &challenger.game,
                                perspective,
                                config,
                            )
                        });
                    let approved_followup = *spirit_followup_scores
                        .entry(approved_index)
                        .or_insert_with(|| {
                            Self::current_pro_spirit_followup_floor_score(
                                &approved.game,
                                perspective,
                                config,
                            )
                        });

                    !challenger_snapshot.allows_immediate_opponent_win
                        && !approved_snapshot.allows_immediate_opponent_win
                        && !challenger_snapshot.opponent_reaches_match_point
                        && !approved_snapshot.opponent_reaches_match_point
                        && Self::root_progress_or_setup_better(challenger, approved)
                        && challenger_snapshot.worst_reply_score.saturating_add(192)
                            >= approved_snapshot.worst_reply_score
                        && challenger_followup.saturating_add(32) >= approved_followup
                };
            if !Self::current_pro_root_advisor_utility_competes(
                challenger_utility,
                approved_utility,
            ) && !early_black_progress_score_override
                && !black_spirit_progress_window_reply_override
            {
                continue;
            }

            let challenger_unsafe = Self::turn_engine_root_evaluation_is_unsafe(challenger);
            if challenger_unsafe
                && !approved_unsafe
                && !concrete_spirit_setup_allowed
                && !early_black_progress_score_override
                && !black_spirit_progress_window_reply_override
            {
                continue;
            }
            let spirit_setup_competes = concrete_spirit_setup_allowed
                && Self::current_pro_spirit_setup_competes_with_non_spirit_root(
                    game,
                    challenger,
                    approved,
                    perspective,
                    config,
                );
            if progress_better
                || spirit_setup_competes
                || black_spirit_progress_window_reply_override
            {
                qualifying_indices.push(index);
            }
        }

        qualifying_indices
            .into_iter()
            .map(|index| {
                let challenger = &scored_roots[index];
                if !challenger.spirit_own_mana_setup_now
                    || challenger.spirit_same_turn_score_setup_now
                {
                    return index;
                }

                let challenger_snapshot = *reply_snapshots.entry(index).or_insert_with(|| {
                    Self::root_reply_risk_snapshot(
                        &challenger.game,
                        perspective,
                        config,
                        per_root_reply_limit,
                    )
                });
                let mut qualifying_plain_indices = Vec::new();
                for plain_index in reply_risk_shortlist.iter().copied() {
                    if plain_index == index
                        || !Self::is_current_pro_black_plain_spirit_followup_setup_pair(
                            game,
                            &scored_roots[plain_index],
                            challenger,
                            config,
                        )
                    {
                        continue;
                    }

                    let plain_snapshot = *reply_snapshots.entry(plain_index).or_insert_with(|| {
                        Self::root_reply_risk_snapshot(
                            &scored_roots[plain_index].game,
                            perspective,
                            config,
                            per_root_reply_limit,
                        )
                    });
                    if matches!(
                        Self::current_pro_black_plain_spirit_followup_reply_order(
                            game,
                            scored_roots,
                            plain_index,
                            plain_snapshot,
                            index,
                            challenger_snapshot,
                            perspective,
                            config,
                            &mut spirit_followup_scores,
                        ),
                        Some(std::cmp::Ordering::Greater)
                    ) {
                        qualifying_plain_indices.push(plain_index);
                    }
                }

                qualifying_plain_indices
                    .into_iter()
                    .max_by(|left, right| {
                        let left_snapshot = *reply_snapshots.entry(*left).or_insert_with(|| {
                            Self::root_reply_risk_snapshot(
                                &scored_roots[*left].game,
                                perspective,
                                config,
                                per_root_reply_limit,
                            )
                        });
                        let right_snapshot = *reply_snapshots.entry(*right).or_insert_with(|| {
                            Self::root_reply_risk_snapshot(
                                &scored_roots[*right].game,
                                perspective,
                                config,
                                per_root_reply_limit,
                            )
                        });
                        let left_followup =
                            *spirit_followup_scores.entry(*left).or_insert_with(|| {
                                Self::current_pro_spirit_followup_floor_score(
                                    &scored_roots[*left].game,
                                    perspective,
                                    config,
                                )
                            });
                        let right_followup =
                            *spirit_followup_scores.entry(*right).or_insert_with(|| {
                                Self::current_pro_spirit_followup_floor_score(
                                    &scored_roots[*right].game,
                                    perspective,
                                    config,
                                )
                            });

                        left_snapshot
                            .worst_reply_score
                            .cmp(&right_snapshot.worst_reply_score)
                            .then_with(|| left_followup.cmp(&right_followup))
                            .then_with(|| {
                                scored_roots[*left].score.cmp(&scored_roots[*right].score)
                            })
                            .then_with(|| {
                                Self::compare_ranked_scored_root_indices(
                                    scored_roots,
                                    *right,
                                    *left,
                                )
                            })
                    })
                    .unwrap_or(index)
            })
            .min_by(|left, right| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })
    }

    fn current_pro_root_advisor_black_opening_setup_sibling_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number > 2
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::SpiritImpact
            || !approved.spirit_own_mana_setup_now
            || approved.spirit_same_turn_score_setup_now
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.same_turn_score_window_value > 0
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
            || approved.inputs.len() < 2
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::SpiritImpact
                    && challenger.spirit_own_mana_setup_now
                    && !challenger.spirit_same_turn_score_setup_now
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && challenger.inputs.len() >= 2
                    && challenger.inputs[0] == approved.inputs[0]
                    && challenger.inputs[1] == approved.inputs[1]
                    && challenger.spirit_setup_gain == approved.spirit_setup_gain
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
                    && challenger.root_rank < approved.root_rank
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_black_early_safe_mana_followup_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number != 2
            || game.mons_moves_count < 2
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value != 0
            || exact_context.delta.opponent_window_deny_gain != 0
            || exact_context.delta.drainer_attack_available
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::SpiritImpact
            || !Self::is_plain_spirit_development_root(approved)
            || Self::turn_engine_root_evaluation_has_progress_surface(approved)
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::ManaTempo
                    && !challenger.spirit_development
                    && !challenger.spirit_same_turn_score_setup_now
                    && !challenger.spirit_own_mana_setup_now
                    && !Self::turn_engine_root_evaluation_has_progress_surface(challenger)
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && challenger.root_rank < approved.root_rank
                    && challenger.score >= approved.score.saturating_add(48)
                    && challenger.safe_supermana_progress_steps
                        <= approved.safe_supermana_progress_steps
                    && challenger.safe_opponent_mana_progress_steps
                        <= approved.safe_opponent_mana_progress_steps
                    && challenger.score_path_best_steps <= approved.score_path_best_steps
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| scored_roots[*right].score.cmp(&scored_roots[*left].score))
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_black_early_plain_spirit_followup_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number != 2
            || game.mons_moves_count > 1
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value != 0
            || exact_context.delta.opponent_window_deny_gain != 0
            || exact_context.delta.drainer_attack_available
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::ManaTempo
            || Self::turn_engine_root_evaluation_has_progress_surface(approved)
            || approved.spirit_development
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::SpiritImpact
                    && Self::is_plain_spirit_development_root(challenger)
                    && !Self::turn_engine_root_evaluation_has_progress_surface(challenger)
                    && !challenger.spirit_same_turn_score_setup_now
                    && !challenger.spirit_own_mana_setup_now
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && challenger.root_rank > approved.root_rank
                    && challenger.root_rank <= approved.root_rank.saturating_add(4)
                    && challenger.score >= approved.score.saturating_sub(32)
                    && challenger.spirit_setup_gain >= approved.spirit_setup_gain.saturating_add(16)
                    && challenger.score_path_best_steps >= approved.score_path_best_steps
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
            })
            .max_by(|left, right| {
                scored_roots[*left]
                    .score
                    .cmp(&scored_roots[*right].score)
                    .then_with(|| {
                        scored_roots[*left]
                            .spirit_setup_gain
                            .cmp(&scored_roots[*right].spirit_setup_gain)
                    })
                    .then_with(|| {
                        scored_roots[*right]
                            .root_rank
                            .cmp(&scored_roots[*left].root_rank)
                    })
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *right, *left)
                    })
            })
    }

    fn current_pro_root_advisor_black_turn_four_vulnerable_progress_mana_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number != 4
            || game.mons_moves_count == 0
            || game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value != 0
            || exact_context.delta.opponent_window_deny_gain != 0
            || exact_context.delta.drainer_attack_available
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if !matches!(
            Self::turn_engine_root_evaluation_family(approved),
            TurnPlanFamily::ManaTempo
                | TurnPlanFamily::SafeSupermanaProgress
                | TurnPlanFamily::SafeOpponentManaProgress
        ) || !approved.own_drainer_vulnerable
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::ManaTempo
                    && !challenger.own_drainer_vulnerable
                    && !challenger.own_drainer_walk_vulnerable
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && !challenger.spirit_development
                    && !challenger.spirit_same_turn_score_setup_now
                    && !challenger.spirit_own_mana_setup_now
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| scored_roots[*right].score.cmp(&scored_roots[*left].score))
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_black_turn_six_attack_vulnerable_progress_mana_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number != 6
            || game.mons_moves_count != 0
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value > 1
            || exact_context.delta.opponent_window_deny_gain > 1
            || !exact_context.delta.drainer_attack_available
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if !matches!(
            Self::turn_engine_root_evaluation_family(approved),
            TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
        ) || !approved.own_drainer_vulnerable
            || approved.own_drainer_walk_vulnerable
            || approved.spirit_development
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
            || approved.same_turn_score_window_value > 0
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::ManaTempo
                    && !challenger.own_drainer_vulnerable
                    && !challenger.own_drainer_walk_vulnerable
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && !challenger.spirit_development
                    && !challenger.spirit_same_turn_score_setup_now
                    && !challenger.spirit_own_mana_setup_now
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && challenger.safe_supermana_progress_steps
                        == approved.safe_supermana_progress_steps
                    && challenger.safe_opponent_mana_progress_steps
                        == approved.safe_opponent_mana_progress_steps.saturating_add(1)
                    && challenger.score_path_best_steps == approved.score_path_best_steps
                    && approved.score.saturating_sub(challenger.score) <= 160
                    && challenger.root_rank <= approved.root_rank.saturating_add(4)
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_black_turn_four_setup_cluster_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number != 4
            || game.mons_moves_count == 0
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value != 0
            || exact_context.delta.opponent_window_deny_gain != 0
            || exact_context.delta.drainer_attack_available
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::SpiritImpact
            || !Self::is_plain_spirit_development_root(approved)
            || approved.spirit_own_mana_setup_now
            || approved.spirit_same_turn_score_setup_now
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::SpiritImpact
                    && challenger.spirit_own_mana_setup_now
                    && !challenger.spirit_same_turn_score_setup_now
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && challenger.inputs.first() == approved.inputs.first()
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
                    && challenger.spirit_setup_gain >= approved.spirit_setup_gain.saturating_add(32)
                    && challenger.root_rank < approved.root_rank
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_early_same_lane_higher_score_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        match game.active_color {
            Color::Black if game.turn_number == 2 => {}
            Color::White if game.turn_number == 3 => {}
            _ => return None,
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value != 0
            || exact_context.delta.opponent_window_deny_gain != 0
            || exact_context.delta.drainer_attack_available
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        let approved_family = Self::turn_engine_root_evaluation_family(approved);
        if !matches!(
            approved_family,
            TurnPlanFamily::ManaTempo | TurnPlanFamily::SpiritImpact
        ) || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == approved_family
                    && challenger.inputs.first() == approved.inputs.first()
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && challenger.spirit_development == approved.spirit_development
                    && challenger.spirit_same_turn_score_setup_now
                        == approved.spirit_same_turn_score_setup_now
                    && challenger.spirit_own_mana_setup_now == approved.spirit_own_mana_setup_now
                    && challenger.supermana_progress == approved.supermana_progress
                    && challenger.opponent_mana_progress == approved.opponent_mana_progress
                    && challenger.safe_supermana_progress_steps
                        == approved.safe_supermana_progress_steps
                    && challenger.safe_opponent_mana_progress_steps
                        == approved.safe_opponent_mana_progress_steps
                    && challenger.score_path_best_steps == approved.score_path_best_steps
                    && challenger.spirit_setup_gain == approved.spirit_setup_gain
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
                    && challenger.score >= approved.score.saturating_add(8)
                    && challenger.root_rank <= approved.root_rank.saturating_add(8)
            })
            .max_by(|left, right| {
                scored_roots[*left]
                    .score
                    .cmp(&scored_roots[*right].score)
                    .then_with(|| {
                        scored_roots[*left]
                            .root_rank
                            .cmp(&scored_roots[*right].root_rank)
                    })
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_white_turn_five_weak_window_setup_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 5
            || game.mons_moves_count != 0
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        let weak_window = exact_context.delta.same_turn_score_window_value <= 1
            && exact_context.delta.opponent_window_deny_gain <= 1
            && !exact_context.delta.drainer_attack_available
            && (exact_context.delta.same_turn_score_window_value > 0
                || exact_context.delta.opponent_window_deny_gain > 0);
        if !weak_window {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if !matches!(
            Self::turn_engine_root_evaluation_family(approved),
            TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
        ) || Self::turn_engine_root_evaluation_is_unsafe(approved)
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.spirit_development
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::SpiritImpact
                    && challenger.spirit_own_mana_setup_now
                    && !challenger.spirit_same_turn_score_setup_now
                    && Self::turn_engine_root_evaluation_has_progress_surface(challenger)
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && challenger.mana_handoff_to_opponent == approved.mana_handoff_to_opponent
                    && challenger.has_roundtrip == approved.has_roundtrip
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
                    && approved.score.saturating_sub(challenger.score) <= 96
                    && challenger.spirit_setup_gain >= approved.spirit_setup_gain.saturating_add(48)
                    && challenger.safe_supermana_progress_steps
                        <= approved.safe_supermana_progress_steps.saturating_add(1)
                    && challenger.safe_opponent_mana_progress_steps
                        <= approved.safe_opponent_mana_progress_steps.saturating_add(1)
                    && challenger.root_rank <= approved.root_rank.saturating_add(4)
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_black_setup_progress_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number < 6
            || game.mons_moves_count != 0
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::SpiritImpact
            || !Self::is_plain_spirit_development_root(approved)
            || !Self::turn_engine_root_evaluation_has_progress_surface(approved)
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
        {
            return None;
        }

        let root_node_budget = ((config.max_visited_nodes
            * config.root_reply_risk_node_share_bp.max(0) as usize)
            / 10_000)
            .max(selection_indices.len())
            .max(1);
        let per_root_reply_limit = (root_node_budget / selection_indices.len().max(1))
            .max(1)
            .min(config.root_reply_risk_reply_limit.max(1));
        let approved_snapshot = Self::root_reply_risk_snapshot(
            &approved.game,
            perspective,
            config,
            per_root_reply_limit,
        );
        if approved_snapshot.allows_immediate_opponent_win
            || approved_snapshot.opponent_reaches_match_point
        {
            return None;
        }
        let approved_utility = Self::turn_engine_selected_override_utility(
            game,
            approved,
            perspective,
            config,
            TurnPlanFamily::SpiritImpact,
        );
        let approved_followup =
            Self::current_pro_spirit_followup_floor_score(&approved.game, perspective, config);
        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        let weak_window_context = exact_context.delta.same_turn_score_window_value <= 1
            && exact_context.delta.opponent_window_deny_gain <= 1;

        let mut utility_cache = std::collections::HashMap::<usize, TurnEngineUtility>::new();
        let mut reply_snapshots = std::collections::HashMap::<usize, RootReplyRiskSnapshot>::new();
        let mut followup_scores = std::collections::HashMap::<usize, i32>::new();
        utility_cache.insert(approved_index, approved_utility);
        reply_snapshots.insert(approved_index, approved_snapshot);
        followup_scores.insert(approved_index, approved_followup);

        let mut best_index: Option<usize> = None;
        for index in selection_indices.iter().copied() {
            if index == approved_index {
                continue;
            }
            let challenger = &scored_roots[index];
            if Self::turn_engine_root_evaluation_family(challenger) != TurnPlanFamily::SpiritImpact
                || !challenger.spirit_own_mana_setup_now
                || challenger.spirit_same_turn_score_setup_now
                || !Self::turn_engine_root_evaluation_has_progress_surface(challenger)
                || challenger.wins_immediately
                || challenger.attacks_opponent_drainer
                || challenger.same_turn_score_window_value > 0
                || challenger.scores_supermana_this_turn
                || challenger.scores_opponent_mana_this_turn
                || challenger.safe_supermana_pickup_now
                || challenger.safe_opponent_mana_pickup_now
                || challenger.mana_handoff_to_opponent
                || challenger.has_roundtrip
                || Self::turn_engine_root_evaluation_is_unsafe(challenger)
                || challenger.inputs.first() != approved.inputs.first()
                || challenger.supermana_progress != approved.supermana_progress
                || challenger.opponent_mana_progress != approved.opponent_mana_progress
                || challenger.own_drainer_vulnerable != approved.own_drainer_vulnerable
                || challenger.own_drainer_walk_vulnerable != approved.own_drainer_walk_vulnerable
            {
                continue;
            }

            let challenger_utility = *utility_cache.entry(index).or_insert_with(|| {
                Self::turn_engine_selected_override_utility(
                    game,
                    challenger,
                    perspective,
                    config,
                    TurnPlanFamily::SpiritImpact,
                )
            });
            let challenger_snapshot = *reply_snapshots.entry(index).or_insert_with(|| {
                Self::root_reply_risk_snapshot(
                    &challenger.game,
                    perspective,
                    config,
                    per_root_reply_limit,
                )
            });
            let challenger_followup = *followup_scores.entry(index).or_insert_with(|| {
                Self::current_pro_spirit_followup_floor_score(&challenger.game, perspective, config)
            });
            let utility_competes = Self::current_pro_root_advisor_utility_competes(
                challenger_utility,
                approved_utility,
            ) || Self::root_progress_or_setup_better(challenger, approved);
            let weak_context_competes = weak_window_context
                && challenger.spirit_setup_gain > approved.spirit_setup_gain
                && challenger.score >= approved.score.saturating_sub(512);
            if (!weak_context_competes && !utility_competes)
                || challenger_snapshot.allows_immediate_opponent_win
                || challenger_snapshot.opponent_reaches_match_point
                || (!weak_context_competes
                    && challenger_snapshot.worst_reply_score.saturating_add(320)
                        < approved_snapshot.worst_reply_score)
                || (!weak_context_competes
                    && challenger_followup.saturating_add(32) < approved_followup)
            {
                continue;
            }

            let replace = match best_index {
                None => true,
                Some(current_best) => {
                    let current_root = &scored_roots[current_best];
                    let current_utility = *utility_cache.entry(current_best).or_insert_with(|| {
                        Self::turn_engine_selected_override_utility(
                            game,
                            current_root,
                            perspective,
                            config,
                            TurnPlanFamily::SpiritImpact,
                        )
                    });
                    let current_snapshot =
                        *reply_snapshots.entry(current_best).or_insert_with(|| {
                            Self::root_reply_risk_snapshot(
                                &current_root.game,
                                perspective,
                                config,
                                per_root_reply_limit,
                            )
                        });
                    let current_followup =
                        *followup_scores.entry(current_best).or_insert_with(|| {
                            Self::current_pro_spirit_followup_floor_score(
                                &current_root.game,
                                perspective,
                                config,
                            )
                        });
                    let utility_order =
                        crate::models::automove_turn_engine::compare_utility_primary_axes(
                            challenger_utility,
                            current_utility,
                        );
                    if weak_window_context {
                        challenger.score > current_root.score
                            || (challenger.score == current_root.score
                                && (challenger.spirit_setup_gain > current_root.spirit_setup_gain
                                    || (challenger.spirit_setup_gain
                                        == current_root.spirit_setup_gain
                                        && Self::compare_ranked_scored_root_indices(
                                            scored_roots,
                                            index,
                                            current_best,
                                        ) == std::cmp::Ordering::Less)))
                    } else if utility_order == std::cmp::Ordering::Greater {
                        true
                    } else if utility_order != std::cmp::Ordering::Equal {
                        false
                    } else if challenger_snapshot.worst_reply_score
                        != current_snapshot.worst_reply_score
                    {
                        challenger_snapshot.worst_reply_score > current_snapshot.worst_reply_score
                    } else if challenger_followup != current_followup {
                        challenger_followup > current_followup
                    } else if challenger.spirit_setup_gain != current_root.spirit_setup_gain {
                        challenger.spirit_setup_gain > current_root.spirit_setup_gain
                    } else if challenger.score != current_root.score {
                        challenger.score > current_root.score
                    } else {
                        Self::compare_ranked_scored_root_indices(scored_roots, index, current_best)
                            == std::cmp::Ordering::Less
                    }
                }
            };
            if replace {
                best_index = Some(index);
            }
        }

        best_index
    }

    fn current_pro_root_advisor_black_plain_spirit_setup_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number < 6
            || game.mons_moves_count != 0
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value != 0
            || exact_context.delta.opponent_window_deny_gain != 0
            || exact_context.delta.drainer_attack_available
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::SpiritImpact
            || !Self::is_plain_spirit_development_root(approved)
            || Self::turn_engine_root_evaluation_has_progress_surface(approved)
            || approved.spirit_own_mana_setup_now
            || approved.spirit_same_turn_score_setup_now
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::SpiritImpact
                    && challenger.spirit_own_mana_setup_now
                    && !challenger.spirit_same_turn_score_setup_now
                    && !Self::turn_engine_root_evaluation_has_progress_surface(challenger)
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && challenger.mana_handoff_to_opponent == approved.mana_handoff_to_opponent
                    && challenger.has_roundtrip == approved.has_roundtrip
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
                    && challenger.inputs.first() == approved.inputs.first()
                    && challenger.spirit_setup_gain >= approved.spirit_setup_gain.saturating_add(32)
                    && challenger.score >= approved.score.saturating_sub(96)
                    && challenger.root_rank <= approved.root_rank.saturating_add(4)
            })
            .max_by(|left, right| {
                scored_roots[*left]
                    .score
                    .cmp(&scored_roots[*right].score)
                    .then_with(|| {
                        scored_roots[*left]
                            .spirit_setup_gain
                            .cmp(&scored_roots[*right].spirit_setup_gain)
                    })
                    .then_with(|| {
                        scored_roots[*right]
                            .root_rank
                            .cmp(&scored_roots[*left].root_rank)
                    })
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *right, *left)
                    })
            })
    }

    fn current_pro_root_advisor_black_no_action_progress_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number < 6
            || game.mons_moves_count != 0
            || game.player_can_use_action()
            || !game.player_can_move_mana()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value > 1
            || exact_context.delta.opponent_window_deny_gain > 1
        {
            return None;
        }
        if exact_context.delta.same_turn_score_window_value == 0
            && exact_context.delta.opponent_window_deny_gain == 0
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::ManaTempo
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.same_turn_score_window_value > 0
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || approved.score < 0
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                matches!(
                    Self::turn_engine_root_evaluation_family(challenger),
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                ) && challenger.score >= 0
                    && challenger.root_rank < approved.root_rank
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
            })
            .max_by(|left, right| {
                scored_roots[*left]
                    .score
                    .cmp(&scored_roots[*right].score)
                    .then_with(|| {
                        scored_roots[*right]
                            .root_rank
                            .cmp(&scored_roots[*left].root_rank)
                    })
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *right, *left)
                    })
            })
    }

    fn current_pro_root_advisor_black_no_action_mana_sibling_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number < 6
            || game.mons_moves_count != 0
            || game.player_can_use_action()
            || !game.player_can_move_mana()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        let allow_windowed_same_lane = approved.same_turn_score_window_value > 0
            && !approved.own_drainer_vulnerable
            && !approved.own_drainer_walk_vulnerable;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::ManaTempo
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || (approved.same_turn_score_window_value > 0 && !allow_windowed_same_lane)
            || (approved.root_rank < 3 && !allow_windowed_same_lane)
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::ManaTempo
                    && challenger.root_rank < approved.root_rank
                    && challenger.same_turn_score_window_value
                        <= approved.same_turn_score_window_value
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && if approved.score >= 0 {
                        challenger.score >= 0
                    } else if allow_windowed_same_lane
                        && challenger.same_turn_score_window_value
                            == approved.same_turn_score_window_value
                        && challenger.safe_supermana_progress_steps
                            == approved.safe_supermana_progress_steps
                        && challenger.safe_opponent_mana_progress_steps
                            == approved.safe_opponent_mana_progress_steps
                        && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                        && challenger.own_drainer_walk_vulnerable
                            == approved.own_drainer_walk_vulnerable
                        && challenger.mana_handoff_to_opponent == approved.mana_handoff_to_opponent
                        && challenger.has_roundtrip == approved.has_roundtrip
                    {
                        true
                    } else {
                        approved.score.saturating_sub(challenger.score) <= 192
                    }
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_black_turn_four_window_mana_sibling_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number != 4
            || game.mons_moves_count != 0
            || game.player_can_use_action()
            || !game.player_can_move_mana()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value > 1
            || exact_context.delta.opponent_window_deny_gain > 1
            || (exact_context.delta.same_turn_score_window_value == 0
                && exact_context.delta.opponent_window_deny_gain == 0)
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::ManaTempo
            || approved.root_rank != 0
            || !approved.own_drainer_vulnerable
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::ManaTempo
                    && challenger.root_rank > approved.root_rank
                    && challenger.root_rank <= approved.root_rank.saturating_add(2)
                    && challenger.same_turn_score_window_value
                        == approved.same_turn_score_window_value
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && challenger.score >= approved.score.saturating_sub(96)
                    && challenger.score_path_best_steps > approved.score_path_best_steps
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_black_baseline_alignment_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        baseline_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || approved_index == baseline_index
            || !selection_indices.contains(&baseline_index)
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        let baseline = scored_roots.get(baseline_index)?;
        let approved_family = Self::turn_engine_root_evaluation_family(approved);
        let baseline_family = Self::turn_engine_root_evaluation_family(baseline);
        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        let approved_non_tactical = !approved.wins_immediately
            && !approved.attacks_opponent_drainer
            && !approved.scores_supermana_this_turn
            && !approved.scores_opponent_mana_this_turn
            && !approved.safe_supermana_pickup_now
            && !approved.safe_opponent_mana_pickup_now
            && !approved.mana_handoff_to_opponent
            && !approved.has_roundtrip;
        let baseline_non_tactical = !baseline.wins_immediately
            && !baseline.attacks_opponent_drainer
            && !baseline.scores_supermana_this_turn
            && !baseline.scores_opponent_mana_this_turn
            && !baseline.safe_supermana_pickup_now
            && !baseline.safe_opponent_mana_pickup_now
            && !baseline.mana_handoff_to_opponent
            && !baseline.has_roundtrip;
        let weak_black_plain_spirit_baseline_mana = game.turn_number >= 6
            && game.mons_moves_count >= 2
            && game.player_can_use_action()
            && game.player_can_move_mana()
            && exact_context.delta.same_turn_score_window_value == 0
            && exact_context.delta.opponent_window_deny_gain == 0
            && !exact_context.delta.drainer_attack_available
            && approved_family == TurnPlanFamily::SpiritImpact
            && baseline_family == TurnPlanFamily::ManaTempo
            && Self::is_plain_spirit_development_root(approved)
            && !Self::turn_engine_root_evaluation_has_progress_surface(approved)
            && approved_non_tactical
            && baseline_non_tactical
            && approved.own_drainer_vulnerable
            && baseline.own_drainer_vulnerable
            && baseline.score >= approved.score;
        if weak_black_plain_spirit_baseline_mana {
            return Some(baseline_index);
        }

        let early_black_setup_branch_baseline_spirit = game.turn_number == 2
            && game.mons_moves_count >= 2
            && game.player_can_use_action()
            && game.player_can_move_mana()
            && exact_context.delta.same_turn_score_window_value == 0
            && exact_context.delta.opponent_window_deny_gain == 0
            && !exact_context.delta.drainer_attack_available
            && approved_family == TurnPlanFamily::SpiritImpact
            && baseline_family == TurnPlanFamily::SpiritImpact
            && approved_non_tactical
            && baseline_non_tactical
            && approved.spirit_own_mana_setup_now
            && baseline.spirit_own_mana_setup_now
            && !approved.spirit_same_turn_score_setup_now
            && !baseline.spirit_same_turn_score_setup_now
            && !Self::turn_engine_root_evaluation_is_unsafe(approved)
            && !Self::turn_engine_root_evaluation_is_unsafe(baseline)
            && approved.inputs.first() == baseline.inputs.first()
            && approved.spirit_setup_gain == baseline.spirit_setup_gain
            && approved.safe_supermana_progress_steps == baseline.safe_supermana_progress_steps
            && approved.safe_opponent_mana_progress_steps
                == baseline.safe_opponent_mana_progress_steps
            && approved.score == baseline.score
            && baseline.root_rank < approved.root_rank
            && approved.root_rank.abs_diff(baseline.root_rank) <= 2;
        if early_black_setup_branch_baseline_spirit {
            return Some(baseline_index);
        }

        let weak_black_no_action_window_baseline_mana = game.turn_number >= 4
            && game.mons_moves_count == 0
            && !game.player_can_use_action()
            && game.player_can_move_mana()
            && exact_context.delta.same_turn_score_window_value <= 1
            && exact_context.delta.opponent_window_deny_gain <= 1
            && (exact_context.delta.same_turn_score_window_value > 0
                || exact_context.delta.opponent_window_deny_gain > 0)
            && approved_family == TurnPlanFamily::ManaTempo
            && baseline_family == TurnPlanFamily::ManaTempo
            && approved_non_tactical
            && baseline_non_tactical
            && approved.same_turn_score_window_value == baseline.same_turn_score_window_value
            && approved.own_drainer_vulnerable == baseline.own_drainer_vulnerable
            && approved.root_rank < baseline.root_rank
            && approved.score >= baseline.score
            && baseline.score_path_best_steps > approved.score_path_best_steps;
        weak_black_no_action_window_baseline_mana.then_some(baseline_index)
    }

    fn current_pro_root_advisor_black_turn_start_guarded_baseline_mana_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number != 6
            || game.mons_moves_count != 0
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || candidate_indices.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        let approved_family = Self::turn_engine_root_evaluation_family(approved);
        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        let approved_non_tactical = !approved.wins_immediately
            && !approved.attacks_opponent_drainer
            && !approved.scores_supermana_this_turn
            && !approved.scores_opponent_mana_this_turn
            && !approved.safe_supermana_pickup_now
            && !approved.safe_opponent_mana_pickup_now
            && !approved.mana_handoff_to_opponent
            && !approved.has_roundtrip;
        if exact_context.delta.same_turn_score_window_value != 0
            || exact_context.delta.opponent_window_deny_gain != 0
            || exact_context.delta.drainer_attack_available
            || approved_family != TurnPlanFamily::SpiritImpact
            || !Self::is_plain_spirit_development_root(approved)
            || Self::turn_engine_root_evaluation_has_progress_surface(approved)
            || !approved_non_tactical
            || !approved.own_drainer_vulnerable
        {
            return None;
        }

        let mut guarded_baseline_config = config;
        guarded_baseline_config.turn_engine_mode = TurnEngineMode::ProV1;
        let baseline_inputs = Self::pick_root_move_with_exploration_from_candidate_indices(
            game,
            scored_roots,
            candidate_indices,
            game.active_color,
            guarded_baseline_config,
        );
        let baseline_index = scored_roots
            .iter()
            .position(|root| root.inputs == baseline_inputs)?;
        if baseline_index == approved_index || !candidate_indices.contains(&baseline_index) {
            return None;
        }

        let baseline = scored_roots.get(baseline_index)?;
        let baseline_family = Self::turn_engine_root_evaluation_family(baseline);
        let baseline_non_tactical = !baseline.wins_immediately
            && !baseline.attacks_opponent_drainer
            && !baseline.scores_supermana_this_turn
            && !baseline.scores_opponent_mana_this_turn
            && !baseline.safe_supermana_pickup_now
            && !baseline.safe_opponent_mana_pickup_now
            && !baseline.mana_handoff_to_opponent
            && !baseline.has_roundtrip;
        if baseline_family != TurnPlanFamily::ManaTempo
            || !baseline_non_tactical
            || !baseline.own_drainer_vulnerable
            || approved.own_drainer_walk_vulnerable != baseline.own_drainer_walk_vulnerable
            || approved.safe_supermana_progress_steps != baseline.safe_supermana_progress_steps
            || approved.safe_opponent_mana_progress_steps
                != baseline.safe_opponent_mana_progress_steps
            || approved.score_path_best_steps != baseline.score_path_best_steps
            || baseline.score < approved.score.saturating_add(256)
        {
            return None;
        }

        let reply_limit = config.root_reply_risk_reply_limit.clamp(1, 24);
        let approved_snapshot =
            Self::root_reply_risk_snapshot(&approved.game, game.active_color, config, reply_limit);
        let baseline_snapshot =
            Self::root_reply_risk_snapshot(&baseline.game, game.active_color, config, reply_limit);
        if approved_snapshot.allows_immediate_opponent_win
            || baseline_snapshot.allows_immediate_opponent_win
            || approved_snapshot.opponent_reaches_match_point
            || baseline_snapshot.opponent_reaches_match_point
            || baseline_snapshot.worst_reply_score
                < approved_snapshot.worst_reply_score.saturating_add(32)
        {
            return None;
        }

        Some(baseline_index)
    }

    fn current_pro_root_advisor_white_turn_three_baseline_alignment_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        baseline_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 3
            || approved_index == baseline_index
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        let baseline = scored_roots.get(baseline_index)?;
        let approved_family = Self::turn_engine_root_evaluation_family(approved);
        let baseline_family = Self::turn_engine_root_evaluation_family(baseline);
        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        let approved_non_tactical = !approved.wins_immediately
            && !approved.attacks_opponent_drainer
            && !approved.scores_supermana_this_turn
            && !approved.scores_opponent_mana_this_turn
            && !approved.safe_supermana_pickup_now
            && !approved.safe_opponent_mana_pickup_now
            && !approved.mana_handoff_to_opponent
            && !approved.has_roundtrip;
        let baseline_non_tactical = !baseline.wins_immediately
            && !baseline.attacks_opponent_drainer
            && !baseline.scores_supermana_this_turn
            && !baseline.scores_opponent_mana_this_turn
            && !baseline.safe_supermana_pickup_now
            && !baseline.safe_opponent_mana_pickup_now
            && !baseline.mana_handoff_to_opponent
            && !baseline.has_roundtrip;

        let white_no_action_split_baseline_mana = game.mons_moves_count >= 3
            && !game.player_can_use_action()
            && game.player_can_move_mana()
            && selection_indices.contains(&baseline_index)
            && exact_context.delta.same_turn_score_window_value == 0
            && exact_context.delta.opponent_window_deny_gain == 0
            && !exact_context.delta.drainer_attack_available
            && approved_family == TurnPlanFamily::ManaTempo
            && baseline_family == TurnPlanFamily::ManaTempo
            && approved_non_tactical
            && baseline_non_tactical
            && !approved.spirit_development
            && !baseline.spirit_development
            && !approved.spirit_same_turn_score_setup_now
            && !baseline.spirit_same_turn_score_setup_now
            && !approved.spirit_own_mana_setup_now
            && !baseline.spirit_own_mana_setup_now
            && !approved.own_drainer_vulnerable
            && !baseline.own_drainer_vulnerable
            && !approved.own_drainer_walk_vulnerable
            && !baseline.own_drainer_walk_vulnerable
            && approved.safe_supermana_progress_steps == baseline.safe_supermana_progress_steps
            && approved.safe_opponent_mana_progress_steps
                == baseline.safe_opponent_mana_progress_steps
            && approved.score_path_best_steps == baseline.score_path_best_steps
            && approved.spirit_setup_gain == baseline.spirit_setup_gain
            && baseline.score >= approved.score.saturating_add(16)
            && baseline.root_rank >= approved.root_rank.saturating_add(2)
            && baseline.root_rank <= approved.root_rank.saturating_add(4);
        if white_no_action_split_baseline_mana {
            return Some(baseline_index);
        }

        None
    }

    fn current_pro_root_advisor_white_turn_three_attack_bridge_escape(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 3
            || selection_indices.len() != 1
            || selection_indices[0] != approved_index
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if !exact_context.delta.drainer_attack_available
            || exact_context.delta.same_turn_score_window_value != 0
            || exact_context.delta.opponent_window_deny_gain != 0
            || game.mons_moves_count < 2
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::SpiritImpact
            || !approved.spirit_development
            || approved.spirit_same_turn_score_setup_now
            || !approved.spirit_own_mana_setup_now
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
            || approved.own_drainer_vulnerable
            || approved.own_drainer_walk_vulnerable
        {
            return None;
        }

        scored_roots
            .iter()
            .enumerate()
            .filter(|(index, root)| {
                *index != approved_index
                    && !selection_indices.contains(index)
                    && Self::turn_engine_root_evaluation_family(root) == TurnPlanFamily::ManaTempo
                    && !root.spirit_development
                    && !root.spirit_same_turn_score_setup_now
                    && !root.spirit_own_mana_setup_now
                    && !root.wins_immediately
                    && !root.attacks_opponent_drainer
                    && !root.scores_supermana_this_turn
                    && !root.scores_opponent_mana_this_turn
                    && !root.safe_supermana_pickup_now
                    && !root.safe_opponent_mana_pickup_now
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(root)
                    && !root.own_drainer_vulnerable
                    && !root.own_drainer_walk_vulnerable
                    && root.safe_supermana_progress_steps == approved.safe_supermana_progress_steps
                    && root.safe_opponent_mana_progress_steps
                        == approved.safe_opponent_mana_progress_steps
                    && root.score_path_best_steps == approved.score_path_best_steps
                    && root.inputs.len() >= 3
                    && root.root_rank <= approved.root_rank.saturating_add(2)
            })
            .min_by(|(left, _), (right, _)| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
            .map(|(index, _)| index)
    }

    fn current_pro_root_advisor_white_mana_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 3
            || game.mons_moves_count < 3
            || scored_roots.is_empty()
        {
            return None;
        }

        const WHITE_MANA_SIBLING_SCORE_GAP_MAX: i32 = 24;
        const WHITE_MANA_SIBLING_ROOT_RANK_GAP_MAX: usize = 4;

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::ManaTempo
            || approved.own_drainer_vulnerable
            || approved.own_drainer_walk_vulnerable
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
        {
            return None;
        }

        let approved_utility = Self::turn_engine_selected_override_utility(
            game,
            approved,
            perspective,
            config,
            TurnPlanFamily::ManaTempo,
        );
        let mut utility_cache = std::collections::HashMap::<usize, TurnEngineUtility>::new();
        let mut best_index: Option<usize> = None;
        let mut best_utility = None;
        let mut best_is_dominance = false;

        for index in selection_indices.iter().copied() {
            if index == approved_index {
                continue;
            }
            let challenger = &scored_roots[index];
            let same_lane_near_best =
                Self::is_current_pro_white_mana_sibling_pair(challenger, approved, config)
                    && approved.score.saturating_sub(challenger.score)
                        <= WHITE_MANA_SIBLING_SCORE_GAP_MAX
                    && approved.root_rank.abs_diff(challenger.root_rank)
                        <= WHITE_MANA_SIBLING_ROOT_RANK_GAP_MAX
                    && challenger.root_rank < approved.root_rank;
            if Self::turn_engine_root_evaluation_family(challenger) != TurnPlanFamily::ManaTempo
                || challenger.own_drainer_vulnerable
                || challenger.own_drainer_walk_vulnerable
                || challenger.mana_handoff_to_opponent
                || challenger.has_roundtrip
                || challenger.wins_immediately
                || challenger.attacks_opponent_drainer
                || challenger.same_turn_score_window_value > 0
                || challenger.scores_supermana_this_turn
                || challenger.scores_opponent_mana_this_turn
                || challenger.safe_supermana_pickup_now
                || challenger.safe_opponent_mana_pickup_now
                || (!same_lane_near_best && challenger.score < approved.score.saturating_sub(96))
                || Self::turn_engine_root_evaluation_is_unsafe(challenger)
                || (challenger.root_rank >= approved.root_rank && !same_lane_near_best)
            {
                continue;
            }

            let challenger_utility = *utility_cache.entry(index).or_insert_with(|| {
                Self::turn_engine_root_plan_utility(
                    game,
                    challenger,
                    perspective,
                    config,
                    TurnPlanFamily::ManaTempo,
                )
            });
            let utility_order = crate::models::automove_turn_engine::compare_utility_primary_axes(
                challenger_utility,
                approved_utility,
            );
            let dominance_competes = utility_order == std::cmp::Ordering::Greater
                || challenger_utility.strictly_dominates_override_axes(approved_utility);
            if !dominance_competes && !same_lane_near_best {
                continue;
            }

            let replace = match (best_index, best_utility) {
                (Some(current_index), Some(current_utility)) => {
                    let current = &scored_roots[current_index];
                    let current_same_lane_near_best =
                        Self::is_current_pro_white_mana_sibling_pair(current, approved, config)
                            && approved.score.saturating_sub(current.score)
                                <= WHITE_MANA_SIBLING_SCORE_GAP_MAX
                            && approved.root_rank.abs_diff(current.root_rank)
                                <= WHITE_MANA_SIBLING_ROOT_RANK_GAP_MAX
                            && current.root_rank < approved.root_rank;
                    if dominance_competes != best_is_dominance {
                        dominance_competes
                    } else if dominance_competes {
                        crate::models::automove_turn_engine::compare_utility_primary_axes(
                            challenger_utility,
                            current_utility,
                        ) == std::cmp::Ordering::Greater
                            || (crate::models::automove_turn_engine::compare_utility_primary_axes(
                                challenger_utility,
                                current_utility,
                            ) == std::cmp::Ordering::Equal
                                && Self::compare_ranked_scored_root_indices(
                                    scored_roots,
                                    index,
                                    current_index,
                                ) == std::cmp::Ordering::Less)
                    } else if same_lane_near_best && current_same_lane_near_best {
                        challenger.root_rank < current.root_rank
                            || (challenger.root_rank == current.root_rank
                                && Self::compare_ranked_scored_root_indices(
                                    scored_roots,
                                    index,
                                    current_index,
                                ) == std::cmp::Ordering::Less)
                    } else {
                        same_lane_near_best
                    }
                }
                _ => true,
            };
            if replace {
                best_index = Some(index);
                best_utility = Some(challenger_utility);
                best_is_dominance = dominance_competes;
            }
        }

        best_index
    }

    fn current_pro_root_advisor_white_no_action_safe_progress_mana_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number < 5
            || game.mons_moves_count != 0
            || game.player_can_use_action()
            || !game.player_can_move_mana()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value != 0
            || exact_context.delta.opponent_window_deny_gain != 0
            || exact_context.delta.drainer_attack_available
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if !matches!(
            Self::turn_engine_root_evaluation_family(approved),
            TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
        ) || !approved.own_drainer_vulnerable
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.same_turn_score_window_value > 0
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::ManaTempo
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && !challenger.own_drainer_vulnerable
                    && !challenger.own_drainer_walk_vulnerable
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && challenger.safe_supermana_progress_steps
                        <= approved.safe_supermana_progress_steps.saturating_add(1)
                    && challenger.safe_opponent_mana_progress_steps
                        <= approved.safe_opponent_mana_progress_steps.saturating_add(1)
                    && challenger.score.saturating_add(448) >= approved.score
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_white_window_progress_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number < 5
            || game.mons_moves_count != 0
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if !Self::is_current_pro_non_concrete_mana_window_root(approved) {
            return None;
        }
        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        let weak_window_context = exact_context.delta.same_turn_score_window_value <= 1
            && exact_context.delta.opponent_window_deny_gain <= 1;

        let root_node_budget = ((config.max_visited_nodes
            * config.root_reply_risk_node_share_bp.max(0) as usize)
            / 10_000)
            .max(selection_indices.len())
            .max(1);
        let per_root_reply_limit = (root_node_budget / selection_indices.len().max(1))
            .max(1)
            .min(config.root_reply_risk_reply_limit.max(1));
        let approved_snapshot = Self::root_reply_risk_snapshot(
            &approved.game,
            perspective,
            config,
            per_root_reply_limit,
        );
        if approved_snapshot.allows_immediate_opponent_win
            || approved_snapshot.opponent_reaches_match_point
        {
            return None;
        }

        let approved_utility = Self::turn_engine_selected_override_utility(
            game,
            approved,
            perspective,
            config,
            TurnPlanFamily::ManaTempo,
        );
        let approved_followup =
            Self::current_pro_spirit_followup_floor_score(&approved.game, perspective, config);
        let mut utility_cache = std::collections::HashMap::<usize, TurnEngineUtility>::new();
        let mut reply_snapshots = std::collections::HashMap::<usize, RootReplyRiskSnapshot>::new();
        let mut followup_scores = std::collections::HashMap::<usize, i32>::new();
        utility_cache.insert(approved_index, approved_utility);
        reply_snapshots.insert(approved_index, approved_snapshot);
        followup_scores.insert(approved_index, approved_followup);

        let mut best_index: Option<usize> = None;
        for index in selection_indices.iter().copied() {
            if index == approved_index {
                continue;
            }

            let challenger = &scored_roots[index];
            if !Self::is_current_pro_white_spirit_progress_window_pair(
                game, challenger, approved, config,
            ) || challenger.root_rank > approved.root_rank.saturating_add(8)
            {
                continue;
            }

            let challenger_utility = *utility_cache.entry(index).or_insert_with(|| {
                Self::turn_engine_selected_override_utility(
                    game,
                    challenger,
                    perspective,
                    config,
                    TurnPlanFamily::SpiritImpact,
                )
            });
            let challenger_snapshot = *reply_snapshots.entry(index).or_insert_with(|| {
                Self::root_reply_risk_snapshot(
                    &challenger.game,
                    perspective,
                    config,
                    per_root_reply_limit,
                )
            });
            let challenger_followup = *followup_scores.entry(index).or_insert_with(|| {
                Self::current_pro_spirit_followup_floor_score(&challenger.game, perspective, config)
            });
            let weak_window_progress_competes = weak_window_context
                && Self::root_progress_or_setup_better(challenger, approved)
                && challenger.score >= approved.score.saturating_sub(32);
            let utility_or_progress_competes =
                Self::current_pro_root_advisor_utility_competes(
                    challenger_utility,
                    approved_utility,
                ) || Self::root_progress_or_setup_better(challenger, approved);
            if challenger_snapshot.allows_immediate_opponent_win
                || challenger_snapshot.opponent_reaches_match_point
                || (!weak_window_progress_competes && !utility_or_progress_competes)
                || (!weak_window_progress_competes
                    && challenger_snapshot.worst_reply_score.saturating_add(192)
                        < approved_snapshot.worst_reply_score)
                || (!weak_window_progress_competes
                    && challenger_followup.saturating_add(32) < approved_followup)
            {
                continue;
            }

            let replace = match best_index {
                None => true,
                Some(current_best) => {
                    let current_snapshot =
                        *reply_snapshots.entry(current_best).or_insert_with(|| {
                            Self::root_reply_risk_snapshot(
                                &scored_roots[current_best].game,
                                perspective,
                                config,
                                per_root_reply_limit,
                            )
                        });
                    let current_followup =
                        *followup_scores.entry(current_best).or_insert_with(|| {
                            Self::current_pro_spirit_followup_floor_score(
                                &scored_roots[current_best].game,
                                perspective,
                                config,
                            )
                        });

                    challenger_followup > current_followup
                        || (challenger_followup == current_followup
                            && challenger_snapshot.worst_reply_score
                                > current_snapshot.worst_reply_score)
                        || (challenger_followup == current_followup
                            && challenger_snapshot.worst_reply_score
                                == current_snapshot.worst_reply_score
                            && Self::compare_ranked_scored_root_indices(
                                scored_roots,
                                index,
                                current_best,
                            ) == std::cmp::Ordering::Less)
                }
            };
            if replace {
                best_index = Some(index);
            }
        }

        best_index
    }

    fn current_pro_root_advisor_white_action_mana_cluster_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        _perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number < 5
            || game.mons_moves_count != 0
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::SpiritImpact
            || !Self::is_plain_spirit_development_root(approved)
            || !Self::turn_engine_root_evaluation_has_progress_surface(approved)
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
        {
            return None;
        }

        let mut best_index: Option<usize> = None;
        for index in selection_indices.iter().copied() {
            if index == approved_index {
                continue;
            }
            let challenger = &scored_roots[index];
            if Self::turn_engine_root_evaluation_family(challenger) != TurnPlanFamily::ManaTempo
                || challenger.spirit_development
                || challenger.spirit_same_turn_score_setup_now
                || challenger.spirit_own_mana_setup_now
                || challenger.wins_immediately
                || challenger.attacks_opponent_drainer
                || challenger.same_turn_score_window_value > 0
                || challenger.scores_supermana_this_turn
                || challenger.scores_opponent_mana_this_turn
                || challenger.safe_supermana_pickup_now
                || challenger.safe_opponent_mana_pickup_now
                || challenger.mana_handoff_to_opponent
                || challenger.has_roundtrip
                || Self::turn_engine_root_evaluation_is_unsafe(challenger)
                || challenger.inputs.first() != approved.inputs.first()
                || challenger.own_drainer_vulnerable != approved.own_drainer_vulnerable
                || challenger.own_drainer_walk_vulnerable != approved.own_drainer_walk_vulnerable
                || challenger.safe_supermana_progress_steps > approved.safe_supermana_progress_steps
                || challenger.safe_opponent_mana_progress_steps
                    > approved.safe_opponent_mana_progress_steps
                || challenger.score < approved.score
            {
                continue;
            }

            let replace = match best_index {
                None => true,
                Some(current_best) => {
                    let current_root = &scored_roots[current_best];
                    challenger.score > current_root.score
                        || (challenger.score == current_root.score
                            && Self::compare_ranked_scored_root_indices(
                                scored_roots,
                                index,
                                current_best,
                            ) == std::cmp::Ordering::Less)
                }
            };
            if replace {
                best_index = Some(index);
            }
        }

        best_index
    }

    fn current_pro_black_turn_six_spirit_reentry(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number != 6
            || !game.player_can_move_mana()
            || shortlist.is_empty()
        {
            return None;
        }

        let shortlist_is_unsafe_mana_lane = shortlist.iter().all(|index| {
            let root = &scored_roots[*index];
            Self::turn_engine_root_evaluation_family(root) == TurnPlanFamily::ManaTempo
                && Self::turn_engine_root_evaluation_is_unsafe(root)
                && !root.wins_immediately
                && !root.attacks_opponent_drainer
                && !root.scores_supermana_this_turn
                && !root.scores_opponent_mana_this_turn
                && !root.safe_supermana_pickup_now
                && !root.safe_opponent_mana_pickup_now
                && root.same_turn_score_window_value == 0
                && !root.mana_handoff_to_opponent
                && !root.has_roundtrip
        });
        if !shortlist_is_unsafe_mana_lane {
            return None;
        }

        let shortlist_best_rank = shortlist
            .iter()
            .map(|index| scored_roots[*index].root_rank)
            .min()?;
        let shortlist_best_score = shortlist
            .iter()
            .map(|index| scored_roots[*index].score)
            .max()
            .unwrap_or(i32::MIN);

        const SPIRIT_SCORE_GAP_MAX: i32 = 1024;
        const SPIRIT_ROOT_RANK_ADVANTAGE_MIN: usize = 4;

        scored_roots
            .iter()
            .enumerate()
            .filter(|(index, _)| !shortlist.contains(index))
            .filter(|(_, root)| {
                Self::turn_engine_root_evaluation_family(root) == TurnPlanFamily::SpiritImpact
                    && root.spirit_development
                    && !root.spirit_same_turn_score_setup_now
                    && !root.spirit_own_mana_setup_now
                    && !root.wins_immediately
                    && !root.attacks_opponent_drainer
                    && !root.scores_supermana_this_turn
                    && !root.scores_opponent_mana_this_turn
                    && !root.safe_supermana_pickup_now
                    && !root.safe_opponent_mana_pickup_now
                    && root.same_turn_score_window_value == 0
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
                    && root
                        .root_rank
                        .saturating_add(SPIRIT_ROOT_RANK_ADVANTAGE_MIN)
                        <= shortlist_best_rank
                    && shortlist_best_score.saturating_sub(root.score) <= SPIRIT_SCORE_GAP_MAX
            })
            .min_by(|(left, _), (right, _)| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })
            .map(|(index, _)| index)
    }

    fn current_pro_black_no_action_safe_progress_reentry(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number < 6
            || game.mons_moves_count != 0
            || game.player_can_use_action()
            || !game.player_can_move_mana()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value > 1
            || exact_context.delta.opponent_window_deny_gain > 1
        {
            return None;
        }
        if exact_context.delta.same_turn_score_window_value == 0
            && exact_context.delta.opponent_window_deny_gain == 0
        {
            return None;
        }

        if shortlist.iter().any(|index| {
            matches!(
                Self::turn_engine_root_evaluation_family(&scored_roots[*index]),
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
            ) && Self::current_pro_root_advisor_root_evaluation_is_safe(&scored_roots[*index])
        }) {
            return None;
        }

        shortlist.first()?;
        scored_roots
            .iter()
            .enumerate()
            .filter(|(index, _)| !shortlist.contains(index))
            .filter(|(_, root)| {
                matches!(
                    Self::turn_engine_root_evaluation_family(root),
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                ) && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
                    && !root.wins_immediately
                    && !root.attacks_opponent_drainer
                    && !root.scores_supermana_this_turn
                    && !root.scores_opponent_mana_this_turn
                    && !root.safe_supermana_pickup_now
                    && !root.safe_opponent_mana_pickup_now
                    && root.same_turn_score_window_value == 0
                    && root.score >= 0
            })
            .max_by(|(left, left_root), (right, right_root)| {
                left_root.score.cmp(&right_root.score).then_with(|| {
                    Self::compare_ranked_scored_root_indices(scored_roots, *right, *left)
                })
            })
            .map(|(index, _)| index)
    }

    fn current_pro_black_no_action_mana_sibling_reentry(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number < 6
            || game.mons_moves_count != 0
            || game.player_can_use_action()
            || !game.player_can_move_mana()
        {
            return None;
        }

        let shortlisted_mana_index = shortlist
            .iter()
            .copied()
            .filter(|index| {
                let root = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(root) == TurnPlanFamily::ManaTempo
                    && Self::current_pro_root_advisor_root_evaluation_is_safe(root)
                    && !root.wins_immediately
                    && !root.attacks_opponent_drainer
                    && !root.scores_supermana_this_turn
                    && !root.scores_opponent_mana_this_turn
                    && !root.safe_supermana_pickup_now
                    && !root.safe_opponent_mana_pickup_now
            })
            .min_by(|left, right| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })?;
        let shortlisted_mana = &scored_roots[shortlisted_mana_index];
        let allow_windowed_same_lane = shortlisted_mana.same_turn_score_window_value > 0
            && !shortlisted_mana.own_drainer_vulnerable
            && !shortlisted_mana.own_drainer_walk_vulnerable;
        if shortlisted_mana.root_rank < 6 && !allow_windowed_same_lane {
            return None;
        }

        scored_roots
            .iter()
            .enumerate()
            .filter(|(index, _)| !shortlist.contains(index))
            .filter(|(_, root)| {
                Self::turn_engine_root_evaluation_family(root) == TurnPlanFamily::ManaTempo
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
                    && !root.wins_immediately
                    && !root.attacks_opponent_drainer
                    && !root.scores_supermana_this_turn
                    && !root.scores_opponent_mana_this_turn
                    && !root.safe_supermana_pickup_now
                    && !root.safe_opponent_mana_pickup_now
                    && (root.root_rank.saturating_add(4) <= shortlisted_mana.root_rank
                        || (allow_windowed_same_lane
                            && root.root_rank < shortlisted_mana.root_rank))
                    && root.same_turn_score_window_value
                        <= shortlisted_mana.same_turn_score_window_value
                    && if shortlisted_mana.score >= 0 {
                        root.score >= 0
                    } else if allow_windowed_same_lane
                        && root.same_turn_score_window_value
                            == shortlisted_mana.same_turn_score_window_value
                        && root.safe_supermana_progress_steps
                            == shortlisted_mana.safe_supermana_progress_steps
                        && root.safe_opponent_mana_progress_steps
                            == shortlisted_mana.safe_opponent_mana_progress_steps
                        && root.own_drainer_vulnerable == shortlisted_mana.own_drainer_vulnerable
                        && root.own_drainer_walk_vulnerable
                            == shortlisted_mana.own_drainer_walk_vulnerable
                        && root.mana_handoff_to_opponent
                            == shortlisted_mana.mana_handoff_to_opponent
                        && root.has_roundtrip == shortlisted_mana.has_roundtrip
                    {
                        true
                    } else {
                        shortlisted_mana.score.saturating_sub(root.score) <= 192
                    }
            })
            .min_by(|(left, _), (right, _)| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })
            .map(|(index, _)| index)
    }

    fn current_pro_root_advisor_white_followup_mana_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 3
            || game.mons_moves_count < 2
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || selection_indices.len() != 1
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::SpiritImpact
            || !approved.spirit_own_mana_setup_now
            || approved.spirit_same_turn_score_setup_now
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.same_turn_score_window_value > 0
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
        {
            return None;
        }

        let approved_utility = Self::turn_engine_selected_override_utility(
            game,
            approved,
            perspective,
            config,
            TurnPlanFamily::SpiritImpact,
        );

        scored_roots
            .iter()
            .enumerate()
            .filter(|(index, root)| {
                *index != approved_index
                    && Self::turn_engine_root_evaluation_family(root) == TurnPlanFamily::ManaTempo
                    && !root.spirit_development
                    && !root.spirit_same_turn_score_setup_now
                    && !root.spirit_own_mana_setup_now
                    && !root.own_drainer_vulnerable
                    && !root.own_drainer_walk_vulnerable
                    && !root.wins_immediately
                    && !root.attacks_opponent_drainer
                    && !root.scores_supermana_this_turn
                    && !root.scores_opponent_mana_this_turn
                    && !root.safe_supermana_pickup_now
                    && !root.safe_opponent_mana_pickup_now
                    && root.same_turn_score_window_value == 0
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(root)
                    && root.root_rank <= approved.root_rank.saturating_add(2)
                    && root.safe_supermana_progress_steps <= approved.safe_supermana_progress_steps
                    && root.safe_opponent_mana_progress_steps
                        <= approved.safe_opponent_mana_progress_steps
                    && root.score_path_best_steps <= approved.score_path_best_steps
            })
            .filter(|(_, root)| {
                let challenger_utility = Self::turn_engine_selected_override_utility(
                    game,
                    root,
                    perspective,
                    config,
                    TurnPlanFamily::ManaTempo,
                );
                let primary_axes_order =
                    crate::models::automove_turn_engine::compare_utility_primary_axes(
                        challenger_utility,
                        approved_utility,
                    );
                primary_axes_order == std::cmp::Ordering::Greater
                    || root.score >= approved.score.saturating_add(512)
            })
            .min_by(|(left, _), (right, _)| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })
            .map(|(index, _)| index)
    }

    fn current_pro_root_advisor_black_late_window_mana_safety_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number < 8
            || game.mons_moves_count == 0
            || game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::ManaTempo
            || !Self::is_current_pro_non_concrete_mana_window_root(approved)
            || !approved.own_drainer_vulnerable
            || approved.own_drainer_walk_vulnerable
        {
            return None;
        }
        let approved_progress_surface =
            Self::turn_engine_root_evaluation_has_progress_surface(approved);

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                let challenger_progress_surface =
                    Self::turn_engine_root_evaluation_has_progress_surface(challenger);
                let challenger_progress_better =
                    Self::root_progress_or_setup_better(challenger, approved)
                        || (challenger_progress_surface && !approved_progress_surface);
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::ManaTempo
                    && challenger.inputs.first() == approved.inputs.first()
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.own_drainer_vulnerable
                    && !challenger.own_drainer_walk_vulnerable
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && (approved.root_rank > 0 || challenger_progress_better)
                    && approved.score.saturating_sub(challenger.score) <= 32
                    && challenger.root_rank <= approved.root_rank.saturating_add(2)
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_black_late_window_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number < 8
            || game.mons_moves_count < 3
            || game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value == 0
            && exact_context.delta.opponent_window_deny_gain == 0
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::ManaTempo
            || approved.same_turn_score_window_value != 0
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
        {
            return None;
        }

        scored_roots
            .iter()
            .enumerate()
            .filter(|(index, root)| {
                *index != approved_index
                    && Self::turn_engine_root_evaluation_family(root) == TurnPlanFamily::ManaTempo
                    && Self::is_current_pro_non_concrete_mana_window_root(root)
                    && root.root_rank == 0
                    && root.own_drainer_vulnerable
                    && !root.own_drainer_walk_vulnerable
                    && approved.score.saturating_sub(root.score) <= 256
            })
            .min_by(|(left, _), (right, _)| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })
            .map(|(index, _)| index)
    }

    fn current_pro_root_advisor_black_late_recovery_progress_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number < 12
            || game.mons_moves_count != 0
            || game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved)
            != TurnPlanFamily::DrainerSafetyRecovery
            || !approved.spirit_development
            || !Self::turn_engine_root_evaluation_has_progress_surface(approved)
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
            || approved.same_turn_score_window_value > 0
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
            || approved.inputs.len() < 3
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::SpiritImpact
                    && challenger.spirit_development
                    && !challenger.spirit_same_turn_score_setup_now
                    && !challenger.spirit_own_mana_setup_now
                    && Self::turn_engine_root_evaluation_has_progress_surface(challenger)
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && challenger.inputs.len() >= 3
                    && challenger.inputs[0] == approved.inputs[0]
                    && challenger.inputs[1] == approved.inputs[1]
                    && approved.score.saturating_sub(challenger.score) <= 1024
                    && challenger.root_rank <= approved.root_rank.saturating_add(2)
                    && Self::root_progress_or_setup_better(challenger, approved)
            })
            .min_by(|left, right| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })
    }

    fn current_pro_root_advisor_white_setup_progress_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number < 5
            || game.mons_moves_count != 0
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::SpiritImpact
            || !Self::is_plain_spirit_development_root(approved)
            || !Self::turn_engine_root_evaluation_has_progress_surface(approved)
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
        {
            return None;
        }

        let root_node_budget = ((config.max_visited_nodes
            * config.root_reply_risk_node_share_bp.max(0) as usize)
            / 10_000)
            .max(selection_indices.len())
            .max(1);
        let per_root_reply_limit = (root_node_budget / selection_indices.len().max(1))
            .max(1)
            .min(config.root_reply_risk_reply_limit.max(1));
        let approved_snapshot = Self::root_reply_risk_snapshot(
            &approved.game,
            perspective,
            config,
            per_root_reply_limit,
        );
        if approved_snapshot.allows_immediate_opponent_win
            || approved_snapshot.opponent_reaches_match_point
        {
            return None;
        }

        let approved_utility = Self::turn_engine_selected_override_utility(
            game,
            approved,
            perspective,
            config,
            TurnPlanFamily::SpiritImpact,
        );
        let approved_followup =
            Self::current_pro_spirit_followup_floor_score(&approved.game, perspective, config);
        let mut utility_cache = std::collections::HashMap::<usize, TurnEngineUtility>::new();
        let mut reply_snapshots = std::collections::HashMap::<usize, RootReplyRiskSnapshot>::new();
        let mut followup_scores = std::collections::HashMap::<usize, i32>::new();
        utility_cache.insert(approved_index, approved_utility);
        reply_snapshots.insert(approved_index, approved_snapshot);
        followup_scores.insert(approved_index, approved_followup);

        let mut best_index: Option<usize> = None;
        for index in selection_indices.iter().copied() {
            if index == approved_index {
                continue;
            }
            let challenger = &scored_roots[index];
            if Self::turn_engine_root_evaluation_family(challenger) != TurnPlanFamily::SpiritImpact
                || !challenger.spirit_own_mana_setup_now
                || challenger.spirit_same_turn_score_setup_now
                || !Self::turn_engine_root_evaluation_has_progress_surface(challenger)
                || !Self::current_pro_root_advisor_root_evaluation_is_safe(challenger)
                || challenger.wins_immediately
                || challenger.attacks_opponent_drainer
                || challenger.same_turn_score_window_value > 0
                || challenger.scores_supermana_this_turn
                || challenger.scores_opponent_mana_this_turn
                || challenger.safe_supermana_pickup_now
                || challenger.safe_opponent_mana_pickup_now
                || challenger.inputs.first() != approved.inputs.first()
                || challenger.own_drainer_vulnerable != approved.own_drainer_vulnerable
                || challenger.own_drainer_walk_vulnerable != approved.own_drainer_walk_vulnerable
            {
                continue;
            }

            let strict_setup_progress_competes = challenger.safe_supermana_progress_steps
                == approved.safe_supermana_progress_steps
                && challenger.safe_opponent_mana_progress_steps
                    == approved.safe_opponent_mana_progress_steps
                && challenger.mana_handoff_to_opponent == approved.mana_handoff_to_opponent
                && challenger.has_roundtrip == approved.has_roundtrip
                && approved.score.saturating_sub(challenger.score) <= 64
                && challenger.spirit_setup_gain >= approved.spirit_setup_gain.saturating_add(32)
                && challenger.root_rank <= approved.root_rank.saturating_add(2);

            let challenger_utility = *utility_cache.entry(index).or_insert_with(|| {
                Self::turn_engine_selected_override_utility(
                    game,
                    challenger,
                    perspective,
                    config,
                    TurnPlanFamily::SpiritImpact,
                )
            });
            let challenger_snapshot = *reply_snapshots.entry(index).or_insert_with(|| {
                Self::root_reply_risk_snapshot(
                    &challenger.game,
                    perspective,
                    config,
                    per_root_reply_limit,
                )
            });
            let challenger_followup = *followup_scores.entry(index).or_insert_with(|| {
                Self::current_pro_spirit_followup_floor_score(&challenger.game, perspective, config)
            });
            let followup_competes = !challenger_snapshot.allows_immediate_opponent_win
                && !challenger_snapshot.opponent_reaches_match_point
                && approved.score.saturating_sub(challenger.score) <= 128
                && challenger_snapshot.worst_reply_score.saturating_add(320)
                    >= approved_snapshot.worst_reply_score
                && challenger_followup.saturating_add(32) >= approved_followup
                && challenger.root_rank <= approved.root_rank.saturating_add(4)
                && (Self::current_pro_root_advisor_utility_competes(
                    challenger_utility,
                    approved_utility,
                ) || Self::root_progress_or_setup_better(challenger, approved));
            if !strict_setup_progress_competes && !followup_competes {
                continue;
            }

            let replace = match best_index {
                None => true,
                Some(current_best) => {
                    let current_root = &scored_roots[current_best];
                    let current_utility = *utility_cache.entry(current_best).or_insert_with(|| {
                        Self::turn_engine_selected_override_utility(
                            game,
                            current_root,
                            perspective,
                            config,
                            TurnPlanFamily::SpiritImpact,
                        )
                    });
                    let current_snapshot =
                        *reply_snapshots.entry(current_best).or_insert_with(|| {
                            Self::root_reply_risk_snapshot(
                                &current_root.game,
                                perspective,
                                config,
                                per_root_reply_limit,
                            )
                        });
                    let current_followup =
                        *followup_scores.entry(current_best).or_insert_with(|| {
                            Self::current_pro_spirit_followup_floor_score(
                                &current_root.game,
                                perspective,
                                config,
                            )
                        });

                    crate::models::automove_turn_engine::compare_utility_primary_axes(
                        challenger_utility,
                        current_utility,
                    ) == std::cmp::Ordering::Greater
                        || (crate::models::automove_turn_engine::compare_utility_primary_axes(
                            challenger_utility,
                            current_utility,
                        ) != std::cmp::Ordering::Less
                            && challenger_followup > current_followup)
                        || (crate::models::automove_turn_engine::compare_utility_primary_axes(
                            challenger_utility,
                            current_utility,
                        ) != std::cmp::Ordering::Less
                            && challenger_followup == current_followup
                            && challenger_snapshot.worst_reply_score
                                > current_snapshot.worst_reply_score)
                        || (challenger_followup == current_followup
                            && challenger_snapshot.worst_reply_score
                                == current_snapshot.worst_reply_score
                            && challenger.spirit_setup_gain > current_root.spirit_setup_gain)
                        || (challenger_followup == current_followup
                            && challenger_snapshot.worst_reply_score
                                == current_snapshot.worst_reply_score
                            && challenger.spirit_setup_gain == current_root.spirit_setup_gain
                            && Self::compare_ranked_scored_root_indices(
                                scored_roots,
                                index,
                                current_best,
                            ) == std::cmp::Ordering::Less)
                }
            };
            if replace {
                best_index = Some(index);
            }
        }

        best_index
    }

    fn current_pro_root_advisor_white_early_followup_setup_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 3
            || game.mons_moves_count != 1
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::SpiritImpact
            || !Self::is_plain_spirit_development_root(approved)
            || !Self::turn_engine_root_evaluation_has_progress_surface(approved)
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::SpiritImpact
                    && challenger.spirit_own_mana_setup_now
                    && !challenger.spirit_same_turn_score_setup_now
                    && Self::turn_engine_root_evaluation_has_progress_surface(challenger)
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && challenger.inputs.first() == approved.inputs.first()
                    && challenger.supermana_progress == approved.supermana_progress
                    && challenger.opponent_mana_progress == approved.opponent_mana_progress
                    && challenger.safe_supermana_progress_steps
                        == approved.safe_supermana_progress_steps
                    && challenger.safe_opponent_mana_progress_steps
                        == approved.safe_opponent_mana_progress_steps
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
                    && approved.score.saturating_sub(challenger.score) <= 64
                    && challenger.spirit_setup_gain >= approved.spirit_setup_gain.saturating_add(32)
                    && challenger.root_rank <= approved.root_rank.saturating_add(4)
            })
            .max_by(|left, right| {
                scored_roots[*left]
                    .score
                    .cmp(&scored_roots[*right].score)
                    .then_with(|| {
                        scored_roots[*right]
                            .root_rank
                            .cmp(&scored_roots[*left].root_rank)
                    })
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *right, *left)
                    })
            })
    }

    fn current_pro_root_advisor_white_early_setup_sibling_progress_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        _selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 3
            || game.mons_moves_count != 1
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value != 0
            || exact_context.delta.opponent_window_deny_gain != 0
            || exact_context.delta.drainer_attack_available
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::SpiritImpact
            || !approved.spirit_own_mana_setup_now
            || approved.spirit_same_turn_score_setup_now
            || !Self::turn_engine_root_evaluation_has_progress_surface(approved)
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
        {
            return None;
        }

        scored_roots
            .iter()
            .enumerate()
            .map(|(index, _)| index)
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                let strict_improvement = challenger.safe_supermana_progress_steps
                    < approved.safe_supermana_progress_steps
                    || challenger.safe_opponent_mana_progress_steps
                        < approved.safe_opponent_mana_progress_steps
                    || challenger.spirit_setup_gain > approved.spirit_setup_gain;
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::SpiritImpact
                    && challenger.spirit_own_mana_setup_now
                    && !challenger.spirit_same_turn_score_setup_now
                    && Self::turn_engine_root_evaluation_has_progress_surface(challenger)
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && challenger.inputs.first() == approved.inputs.first()
                    && challenger.supermana_progress == approved.supermana_progress
                    && challenger.opponent_mana_progress == approved.opponent_mana_progress
                    && challenger.safe_supermana_progress_steps
                        <= approved.safe_supermana_progress_steps
                    && challenger.safe_opponent_mana_progress_steps
                        <= approved.safe_opponent_mana_progress_steps
                    && challenger.score_path_best_steps <= approved.score_path_best_steps
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
                    && challenger.score >= approved.score.saturating_sub(64)
                    && challenger.root_rank <= approved.root_rank.saturating_add(8)
                    && strict_improvement
            })
            .max_by(|left, right| {
                let left_root = &scored_roots[*left];
                let right_root = &scored_roots[*right];
                approved
                    .safe_supermana_progress_steps
                    .saturating_sub(left_root.safe_supermana_progress_steps)
                    .cmp(
                        &approved
                            .safe_supermana_progress_steps
                            .saturating_sub(right_root.safe_supermana_progress_steps),
                    )
                    .then_with(|| {
                        approved
                            .spirit_setup_gain
                            .saturating_sub(left_root.spirit_setup_gain)
                            .cmp(
                                &approved
                                    .spirit_setup_gain
                                    .saturating_sub(right_root.spirit_setup_gain),
                            )
                    })
                    .then_with(|| left_root.score.cmp(&right_root.score))
                    .then_with(|| right_root.root_rank.cmp(&left_root.root_rank))
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *right, *left)
                    })
            })
    }

    fn current_pro_root_advisor_white_early_no_action_progress_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 3
            || game.mons_moves_count != 1
            || game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if !matches!(
            Self::turn_engine_root_evaluation_family(approved),
            TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
        ) || !approved.own_drainer_vulnerable
            || approved.own_drainer_walk_vulnerable
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.spirit_development
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                matches!(
                    Self::turn_engine_root_evaluation_family(challenger),
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                ) && !challenger.own_drainer_vulnerable
                    && !challenger.own_drainer_walk_vulnerable
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && !challenger.spirit_development
                    && !challenger.spirit_same_turn_score_setup_now
                    && !challenger.spirit_own_mana_setup_now
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && challenger.supermana_progress == approved.supermana_progress
                    && challenger.opponent_mana_progress == approved.opponent_mana_progress
                    && challenger.safe_supermana_progress_steps
                        == approved.safe_supermana_progress_steps
                    && challenger.safe_opponent_mana_progress_steps
                        == approved.safe_opponent_mana_progress_steps
                    && challenger.score >= approved.score.saturating_sub(32)
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_white_mana_only_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        _perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number < 5
            || game.mons_moves_count != 0
            || game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        const SCORE_GAP_MAX: i32 = 64;
        const ROOT_RANK_GAP_MAX: usize = 6;

        let approved = scored_roots.get(approved_index)?;
        if !matches!(
            Self::turn_engine_root_evaluation_family(approved),
            TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
        ) || !approved.own_drainer_vulnerable
            || approved.own_drainer_walk_vulnerable
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.spirit_development
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::ManaTempo
                    && !challenger.own_drainer_vulnerable
                    && !challenger.own_drainer_walk_vulnerable
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && !challenger.spirit_development
                    && !challenger.spirit_same_turn_score_setup_now
                    && !challenger.spirit_own_mana_setup_now
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && approved.score.saturating_sub(challenger.score) <= SCORE_GAP_MAX
                    && approved.root_rank.abs_diff(challenger.root_rank) <= ROOT_RANK_GAP_MAX
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_white_turn_three_safe_progress_surface_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 3
            || game.mons_moves_count != 0
            || game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if !matches!(
            Self::turn_engine_root_evaluation_family(approved),
            TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
        ) || Self::turn_engine_root_evaluation_is_unsafe(approved)
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.spirit_development
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                matches!(
                    Self::turn_engine_root_evaluation_family(challenger),
                    TurnPlanFamily::SafeSupermanaProgress
                        | TurnPlanFamily::SafeOpponentManaProgress
                ) && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && !challenger.spirit_development
                    && !challenger.spirit_same_turn_score_setup_now
                    && !challenger.spirit_own_mana_setup_now
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
                    && challenger.mana_handoff_to_opponent == approved.mana_handoff_to_opponent
                    && challenger.has_roundtrip == approved.has_roundtrip
                    && challenger.root_rank < approved.root_rank
                    && challenger.score >= approved.score.saturating_sub(32)
                    && Self::root_progress_or_setup_better(challenger, approved)
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_white_turn_three_no_action_recovery_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 3
            || game.mons_moves_count != 0
            || game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value > 1
            || exact_context.delta.opponent_window_deny_gain > 1
            || (exact_context.delta.same_turn_score_window_value == 0
                && exact_context.delta.opponent_window_deny_gain == 0)
            || exact_context.delta.drainer_safety >= 0
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if !Self::is_current_pro_non_concrete_mana_window_root(approved)
            || !approved.own_drainer_vulnerable
            || approved.own_drainer_walk_vulnerable
            || approved.spirit_development
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
        {
            return None;
        }

        scored_roots
            .iter()
            .enumerate()
            .filter(|(index, root)| {
                *index != approved_index
                    && Self::turn_engine_root_evaluation_family(root)
                        == TurnPlanFamily::DrainerSafetyRecovery
                    && root.classes.drainer_safety_recover
                    && !Self::turn_engine_root_evaluation_is_unsafe(root)
                    && !root.own_drainer_vulnerable
                    && !root.own_drainer_walk_vulnerable
                    && !root.wins_immediately
                    && !root.attacks_opponent_drainer
                    && !root.spirit_development
                    && !root.spirit_same_turn_score_setup_now
                    && !root.spirit_own_mana_setup_now
                    && root.same_turn_score_window_value == 0
                    && !root.scores_supermana_this_turn
                    && !root.scores_opponent_mana_this_turn
                    && !root.safe_supermana_pickup_now
                    && !root.safe_opponent_mana_pickup_now
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
                    && root.inputs.first() == approved.inputs.first()
                    && root.root_rank <= approved.root_rank
                    && root.score_path_best_steps > approved.score_path_best_steps
            })
            .min_by(|(left, _), (right, _)| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
            .map(|(index, _)| index)
    }

    fn current_pro_root_advisor_black_turn_four_weak_window_recovery_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number != 4
            || game.mons_moves_count < 1
            || game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value > 1
            || exact_context.delta.opponent_window_deny_gain > 1
            || (exact_context.delta.same_turn_score_window_value == 0
                && exact_context.delta.opponent_window_deny_gain == 0)
            || exact_context.delta.drainer_attack_available
            || exact_context.delta.drainer_safety >= 0
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if !Self::is_current_pro_non_concrete_mana_window_root(approved)
            || !approved.own_drainer_vulnerable
            || approved.own_drainer_walk_vulnerable
            || approved.spirit_development
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
        {
            return None;
        }

        scored_roots
            .iter()
            .enumerate()
            .filter(|(index, root)| {
                *index != approved_index
                    && Self::turn_engine_root_evaluation_family(root)
                        == TurnPlanFamily::DrainerSafetyRecovery
                    && root.classes.drainer_safety_recover
                    && !Self::turn_engine_root_evaluation_is_unsafe(root)
                    && !root.own_drainer_vulnerable
                    && !root.own_drainer_walk_vulnerable
                    && !root.wins_immediately
                    && !root.attacks_opponent_drainer
                    && !root.spirit_development
                    && !root.spirit_same_turn_score_setup_now
                    && !root.spirit_own_mana_setup_now
                    && root.same_turn_score_window_value == 0
                    && !root.scores_supermana_this_turn
                    && !root.scores_opponent_mana_this_turn
                    && !root.safe_supermana_pickup_now
                    && !root.safe_opponent_mana_pickup_now
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
                    && root.inputs.first() == approved.inputs.first()
                    && root.safe_supermana_progress_steps == approved.safe_supermana_progress_steps
                    && root.safe_opponent_mana_progress_steps
                        == approved.safe_opponent_mana_progress_steps
                    && root.root_rank <= approved.root_rank
                    && root.score_path_best_steps > approved.score_path_best_steps
            })
            .min_by(|(left, _), (right, _)| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
            .map(|(index, _)| index)
    }

    fn current_pro_root_advisor_white_early_safe_progress_setup_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 3
            || game.mons_moves_count != 1
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if !matches!(
            Self::turn_engine_root_evaluation_family(approved),
            TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
        ) || Self::turn_engine_root_evaluation_is_unsafe(approved)
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.spirit_development
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
        {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                let strong_setup_progress_rank_rescue =
                    approved.score.saturating_sub(challenger.score) <= 160
                        && challenger.spirit_setup_gain
                            >= approved.spirit_setup_gain.saturating_add(64)
                        && challenger.safe_supermana_progress_steps
                            == approved.safe_supermana_progress_steps
                        && challenger.safe_opponent_mana_progress_steps
                            == approved.safe_opponent_mana_progress_steps
                        && challenger.root_rank.saturating_add(6) <= approved.root_rank;
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::SpiritImpact
                    && challenger.spirit_own_mana_setup_now
                    && !challenger.spirit_same_turn_score_setup_now
                    && Self::turn_engine_root_evaluation_has_progress_surface(challenger)
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && challenger.mana_handoff_to_opponent == approved.mana_handoff_to_opponent
                    && challenger.has_roundtrip == approved.has_roundtrip
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
                    && (approved.score.saturating_sub(challenger.score) <= 32
                        || strong_setup_progress_rank_rescue)
                    && challenger.spirit_setup_gain >= approved.spirit_setup_gain.saturating_add(64)
                    && challenger.safe_supermana_progress_steps
                        <= approved.safe_supermana_progress_steps
                    && challenger.safe_opponent_mana_progress_steps
                        <= approved.safe_opponent_mana_progress_steps
                    && challenger.root_rank <= approved.root_rank.saturating_add(4)
            })
            .max_by(|left, right| {
                scored_roots[*left]
                    .score
                    .cmp(&scored_roots[*right].score)
                    .then_with(|| {
                        scored_roots[*right]
                            .spirit_setup_gain
                            .cmp(&scored_roots[*left].spirit_setup_gain)
                    })
                    .then_with(|| {
                        scored_roots[*right]
                            .root_rank
                            .cmp(&scored_roots[*left].root_rank)
                    })
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *right, *left)
                    })
            })
    }

    fn current_pro_root_advisor_white_early_setup_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        _perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 3
            || game.mons_moves_count != 1
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || selection_indices.len() != 1
            || scored_roots.is_empty()
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::SpiritImpact
            || !approved.spirit_own_mana_setup_now
            || approved.spirit_same_turn_score_setup_now
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
        {
            return None;
        }

        let context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if context.delta.same_turn_score_window_value == 0 {
            return None;
        }

        scored_roots
            .iter()
            .enumerate()
            .filter(|(index, root)| {
                *index != approved_index
                    && Self::turn_engine_root_evaluation_family(root) == TurnPlanFamily::ManaTempo
                    && !root.own_drainer_vulnerable
                    && !root.own_drainer_walk_vulnerable
                    && !root.wins_immediately
                    && !root.attacks_opponent_drainer
                    && !root.spirit_development
                    && !root.spirit_same_turn_score_setup_now
                    && !root.spirit_own_mana_setup_now
                    && root.same_turn_score_window_value == 0
                    && !root.scores_supermana_this_turn
                    && !root.scores_opponent_mana_this_turn
                    && !root.safe_supermana_pickup_now
                    && !root.safe_opponent_mana_pickup_now
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(root)
            })
            .min_by(|(left, _), (right, _)| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })
            .map(|(index, _)| index)
    }

    fn is_current_pro_late_white_spirit_followup_safe_progress_pair(
        game: &MonsGame,
        candidate: &RootEvaluation,
        incumbent: &RootEvaluation,
        config: AutomoveSearchConfig,
    ) -> bool {
        matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && game.active_color == Color::White
            && game.turn_number >= 8
            && game.mons_moves_count == 0
            && game.player_can_use_action()
            && game.player_can_move_mana()
            && Self::turn_engine_root_evaluation_family(candidate) == TurnPlanFamily::SpiritImpact
            && candidate.spirit_development
            && !candidate.spirit_same_turn_score_setup_now
            && !candidate.wins_immediately
            && !candidate.attacks_opponent_drainer
            && candidate.same_turn_score_window_value == 0
            && !candidate.scores_supermana_this_turn
            && !candidate.scores_opponent_mana_this_turn
            && !candidate.safe_supermana_pickup_now
            && !candidate.safe_opponent_mana_pickup_now
            && !candidate.mana_handoff_to_opponent
            && !candidate.has_roundtrip
            && !Self::turn_engine_root_evaluation_is_unsafe(candidate)
            && matches!(
                Self::turn_engine_root_evaluation_family(incumbent),
                TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::ManaTempo
            )
            && !incumbent.spirit_development
            && !incumbent.wins_immediately
            && !incumbent.attacks_opponent_drainer
            && incumbent.same_turn_score_window_value == 0
            && !incumbent.scores_supermana_this_turn
            && !incumbent.scores_opponent_mana_this_turn
            && !incumbent.safe_supermana_pickup_now
            && !incumbent.safe_opponent_mana_pickup_now
            && !incumbent.mana_handoff_to_opponent
            && !incumbent.has_roundtrip
            && !Self::turn_engine_root_evaluation_is_unsafe(incumbent)
            && candidate.inputs.first() == incumbent.inputs.first()
    }

    fn current_pro_root_advisor_white_late_followup_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        _selection_indices: &[usize],
        approved_index: usize,
        _perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number < 8
            || game.mons_moves_count != 0
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || scored_roots.is_empty()
        {
            return None;
        }

        const SCORE_GAP_MAX: i32 = 512;
        const ROOT_RANK_GAP_MAX: usize = 10;
        let approved = scored_roots.get(approved_index)?;
        if !matches!(
            Self::turn_engine_root_evaluation_family(approved),
            TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::ManaTempo
        ) {
            return None;
        }

        let candidates = scored_roots
            .iter()
            .enumerate()
            .map(|(index, _)| index)
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::is_current_pro_late_white_spirit_followup_safe_progress_pair(
                    game, challenger, approved, config,
                ) && approved.score.saturating_sub(challenger.score) <= SCORE_GAP_MAX
                    && approved.root_rank.abs_diff(challenger.root_rank) <= ROOT_RANK_GAP_MAX
            })
            .collect::<Vec<_>>();
        let has_own_setup_candidate = candidates
            .iter()
            .any(|index| scored_roots[*index].spirit_own_mana_setup_now);
        candidates.into_iter().min_by(|left, right| {
            let left_root = &scored_roots[*left];
            let right_root = &scored_roots[*right];
            if has_own_setup_candidate
                && left_root.spirit_own_mana_setup_now != right_root.spirit_own_mana_setup_now
            {
                return if left_root.spirit_own_mana_setup_now {
                    std::cmp::Ordering::Less
                } else {
                    std::cmp::Ordering::Greater
                };
            }
            if has_own_setup_candidate
                && left_root.spirit_setup_gain != right_root.spirit_setup_gain
            {
                return right_root
                    .spirit_setup_gain
                    .cmp(&left_root.spirit_setup_gain);
            }
            left_root
                .root_rank
                .cmp(&right_root.root_rank)
                .then_with(|| Self::compare_ranked_scored_root_indices(scored_roots, *left, *right))
        })
    }

    fn is_current_pro_white_mana_sibling_pair(
        candidate: &RootEvaluation,
        incumbent: &RootEvaluation,
        config: AutomoveSearchConfig,
    ) -> bool {
        matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && Self::turn_engine_root_evaluation_family(candidate) == TurnPlanFamily::ManaTempo
            && Self::turn_engine_root_evaluation_family(incumbent) == TurnPlanFamily::ManaTempo
            && candidate.efficiency == incumbent.efficiency
            && !candidate.own_drainer_vulnerable
            && !incumbent.own_drainer_vulnerable
            && !candidate.own_drainer_walk_vulnerable
            && !incumbent.own_drainer_walk_vulnerable
            && !Self::turn_engine_root_evaluation_is_unsafe(candidate)
            && !Self::turn_engine_root_evaluation_is_unsafe(incumbent)
            && Self::is_same_non_tactical_progress_lane_root_pair(candidate, incumbent)
    }

    fn is_current_pro_late_black_spirit_followup_mana_pair(
        game: &MonsGame,
        candidate: &RootEvaluation,
        incumbent: &RootEvaluation,
        config: AutomoveSearchConfig,
    ) -> bool {
        matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && Self::current_pro_is_late_black_action_mana_turn_start(game)
            && Self::turn_engine_root_evaluation_family(candidate) == TurnPlanFamily::SpiritImpact
            && candidate.spirit_development
            && !candidate.spirit_same_turn_score_setup_now
            && !candidate.spirit_own_mana_setup_now
            && !candidate.wins_immediately
            && !candidate.attacks_opponent_drainer
            && candidate.same_turn_score_window_value == 0
            && !candidate.scores_supermana_this_turn
            && !candidate.scores_opponent_mana_this_turn
            && !candidate.safe_supermana_pickup_now
            && !candidate.safe_opponent_mana_pickup_now
            && !candidate.mana_handoff_to_opponent
            && !candidate.has_roundtrip
            && !Self::turn_engine_root_evaluation_is_unsafe(candidate)
            && Self::turn_engine_root_evaluation_family(incumbent) == TurnPlanFamily::ManaTempo
            && !incumbent.wins_immediately
            && !incumbent.attacks_opponent_drainer
            && incumbent.same_turn_score_window_value == 0
            && !incumbent.scores_supermana_this_turn
            && !incumbent.scores_opponent_mana_this_turn
            && !incumbent.safe_supermana_pickup_now
            && !incumbent.safe_opponent_mana_pickup_now
            && !incumbent.mana_handoff_to_opponent
            && !incumbent.has_roundtrip
            && !Self::turn_engine_root_evaluation_is_unsafe(incumbent)
            && candidate.inputs.first() == incumbent.inputs.first()
    }

    fn current_pro_root_advisor_black_late_followup_competition_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        selection_indices: &[usize],
        approved_index: usize,
        _perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || !Self::current_pro_is_late_black_action_mana_turn_start(game)
            || scored_roots.is_empty()
        {
            return None;
        }

        const SCORE_GAP_MAX: i32 = 512;
        const ROOT_RANK_GAP_MAX: usize = 16;
        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::ManaTempo {
            return None;
        }

        selection_indices
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::is_current_pro_late_black_spirit_followup_mana_pair(
                    game, challenger, approved, config,
                ) && approved.score.saturating_sub(challenger.score) <= SCORE_GAP_MAX
                    && challenger.root_rank < approved.root_rank
                    && approved.root_rank.abs_diff(challenger.root_rank) <= ROOT_RANK_GAP_MAX
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_black_late_reply_risk_setup_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        reply_risk_shortlist: &[usize],
        approved_index: usize,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || !Self::current_pro_is_late_black_action_mana_turn_start(game)
            || reply_risk_shortlist.is_empty()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        if exact_context.delta.same_turn_score_window_value != 0
            || exact_context.delta.opponent_window_deny_gain != 0
            || exact_context.delta.drainer_attack_available
        {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        if Self::turn_engine_root_evaluation_family(approved) != TurnPlanFamily::ManaTempo
            || approved.spirit_development
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
            || Self::turn_engine_root_evaluation_has_progress_surface(approved)
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
            || approved.own_drainer_vulnerable
            || approved.own_drainer_walk_vulnerable
        {
            return None;
        }

        reply_risk_shortlist
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::SpiritImpact
                    && challenger.spirit_own_mana_setup_now
                    && !challenger.spirit_same_turn_score_setup_now
                    && !Self::turn_engine_root_evaluation_has_progress_surface(challenger)
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && !challenger.mana_handoff_to_opponent
                    && !challenger.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && !challenger.own_drainer_vulnerable
                    && !challenger.own_drainer_walk_vulnerable
                    && challenger.spirit_setup_gain >= approved.spirit_setup_gain.saturating_add(64)
                    && approved.score.saturating_sub(challenger.score) <= 2048
                    && challenger.safe_supermana_progress_steps
                        == approved.safe_supermana_progress_steps
                    && challenger.safe_opponent_mana_progress_steps
                        == approved.safe_opponent_mana_progress_steps
                    && challenger.score_path_best_steps == approved.score_path_best_steps
                    && challenger.root_rank < approved.root_rank
                    && approved.root_rank.abs_diff(challenger.root_rank) <= 4
            })
            .min_by(|left, right| {
                scored_roots[*left]
                    .root_rank
                    .cmp(&scored_roots[*right].root_rank)
                    .then_with(|| {
                        scored_roots[*right]
                            .spirit_setup_gain
                            .cmp(&scored_roots[*left].spirit_setup_gain)
                    })
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    })
            })
    }

    fn current_pro_root_advisor_black_late_weak_window_safe_progress_setup_override(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        reply_risk_shortlist: &[usize],
        approved_index: usize,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || !Self::current_pro_is_late_black_action_mana_turn_start(game)
            || reply_risk_shortlist.is_empty()
            || scored_roots.is_empty()
        {
            return None;
        }

        let exact_context =
            crate::models::automove_exact::exact_opportunity_context(game, game.active_color);
        let weak_window_context = exact_context.delta.same_turn_score_window_value <= 1
            && exact_context.delta.opponent_window_deny_gain <= 1
            && !exact_context.delta.drainer_attack_available
            && (exact_context.delta.same_turn_score_window_value > 0
                || exact_context.delta.opponent_window_deny_gain > 0);
        if !weak_window_context {
            return None;
        }

        let approved = scored_roots.get(approved_index)?;
        let approved_family = Self::turn_engine_root_evaluation_family(approved);
        if !matches!(
            approved_family,
            TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
        ) || !Self::turn_engine_root_evaluation_has_progress_surface(approved)
            || approved.spirit_development
            || approved.spirit_same_turn_score_setup_now
            || approved.spirit_own_mana_setup_now
            || approved.wins_immediately
            || approved.attacks_opponent_drainer
            || approved.same_turn_score_window_value > 0
            || approved.scores_supermana_this_turn
            || approved.scores_opponent_mana_this_turn
            || approved.safe_supermana_pickup_now
            || approved.safe_opponent_mana_pickup_now
            || approved.mana_handoff_to_opponent
            || approved.has_roundtrip
            || Self::turn_engine_root_evaluation_is_unsafe(approved)
            || approved.own_drainer_vulnerable
            || approved.own_drainer_walk_vulnerable
        {
            return None;
        }

        let approved_utility = Self::turn_engine_selected_override_utility(
            game,
            approved,
            perspective,
            config,
            approved_family,
        );

        reply_risk_shortlist
            .iter()
            .copied()
            .filter(|index| *index != approved_index)
            .filter(|index| {
                let challenger = &scored_roots[*index];
                let challenger_utility = Self::turn_engine_selected_override_utility(
                    game,
                    challenger,
                    perspective,
                    config,
                    TurnPlanFamily::SpiritImpact,
                );

                Self::turn_engine_root_evaluation_family(challenger) == TurnPlanFamily::SpiritImpact
                    && challenger.spirit_own_mana_setup_now
                    && !challenger.spirit_same_turn_score_setup_now
                    && Self::turn_engine_root_evaluation_has_progress_surface(challenger)
                    && !challenger.wins_immediately
                    && !challenger.attacks_opponent_drainer
                    && challenger.same_turn_score_window_value == 0
                    && !challenger.scores_supermana_this_turn
                    && !challenger.scores_opponent_mana_this_turn
                    && !challenger.safe_supermana_pickup_now
                    && !challenger.safe_opponent_mana_pickup_now
                    && challenger.mana_handoff_to_opponent == approved.mana_handoff_to_opponent
                    && challenger.has_roundtrip == approved.has_roundtrip
                    && !Self::turn_engine_root_evaluation_is_unsafe(challenger)
                    && challenger.own_drainer_vulnerable == approved.own_drainer_vulnerable
                    && challenger.own_drainer_walk_vulnerable
                        == approved.own_drainer_walk_vulnerable
                    && challenger.supermana_progress == approved.supermana_progress
                    && challenger.opponent_mana_progress == approved.opponent_mana_progress
                    && Self::current_pro_root_advisor_utility_competes(
                        challenger_utility,
                        approved_utility,
                    )
                    && approved.score.saturating_sub(challenger.score) <= 32
                    && challenger.spirit_setup_gain >= approved.spirit_setup_gain.saturating_add(64)
                    && challenger.root_rank <= approved.root_rank.saturating_add(4)
            })
            .max_by(|left, right| {
                scored_roots[*left]
                    .score
                    .cmp(&scored_roots[*right].score)
                    .then_with(|| {
                        scored_roots[*left]
                            .spirit_setup_gain
                            .cmp(&scored_roots[*right].spirit_setup_gain)
                    })
                    .then_with(|| {
                        scored_roots[*right]
                            .root_rank
                            .cmp(&scored_roots[*left].root_rank)
                    })
                    .then_with(|| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *right, *left)
                    })
            })
    }

    fn current_pro_root_advisor_priority_inputs(
        decision: &CurrentProRootAdvisorDecision,
    ) -> Vec<Vec<Input>> {
        let mut inputs = Vec::new();
        if let Some(injected_root) = decision.injected_root.as_ref().filter(|root| root.admitted) {
            inputs.push(injected_root.inputs.clone());
        }
        for entry in decision.preserved_family_representatives.iter() {
            if !inputs.contains(&entry.inputs) {
                inputs.push(entry.inputs.clone());
            }
        }
        inputs
    }

    fn current_pro_root_advisor_root_move_is_safe(root: &ScoredRootMove) -> bool {
        !Self::turn_engine_root_move_is_unsafe(root)
            && !root.mana_handoff_to_opponent
            && !root.has_roundtrip
    }

    fn current_pro_root_advisor_root_evaluation_is_safe(root: &RootEvaluation) -> bool {
        !Self::turn_engine_root_evaluation_is_unsafe(root)
            && !root.mana_handoff_to_opponent
            && !root.has_roundtrip
    }

    fn current_pro_root_advisor_utility_competes(
        candidate: TurnEngineUtility,
        incumbent: TurnEngineUtility,
    ) -> bool {
        crate::models::automove_turn_engine::compare_utility_primary_axes(candidate, incumbent)
            != std::cmp::Ordering::Less
            || candidate.supports_family_fallback(incumbent)
    }

    fn current_pro_root_advisor_find_root_move_representative<F>(
        game: &MonsGame,
        root_moves: &[ScoredRootMove],
        perspective: Color,
        config: AutomoveSearchConfig,
        predicate: F,
    ) -> Option<usize>
    where
        F: Fn(&ScoredRootMove) -> bool,
    {
        let anchor = root_moves.first()?;
        if predicate(anchor) && Self::current_pro_root_advisor_root_move_is_safe(anchor) {
            return None;
        }
        let anchor_utility = Self::turn_engine_scored_root_utility(
            game,
            anchor,
            perspective,
            config,
            Self::turn_engine_root_move_family(anchor),
        );

        root_moves
            .iter()
            .enumerate()
            .filter(|(_, root)| {
                predicate(root) && Self::current_pro_root_advisor_root_move_is_safe(root)
            })
            .filter(|(_, root)| {
                let candidate_utility = Self::turn_engine_scored_root_utility(
                    game,
                    root,
                    perspective,
                    config,
                    Self::turn_engine_root_move_family(root),
                );
                Self::current_pro_root_advisor_utility_competes(candidate_utility, anchor_utility)
            })
            .min_by(|(left, _), (right, _)| {
                Self::compare_ranked_root_indices(root_moves, (*left, 0), (*right, 0))
            })
            .map(|(index, _)| index)
    }

    fn current_pro_root_advisor_same_opening_setup_representative(
        root_moves: &[ScoredRootMove],
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        let anchor = root_moves.first()?;
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || !anchor.spirit_development
            || anchor.spirit_same_turn_score_setup_now
            || anchor.spirit_own_mana_setup_now
            || !Self::current_pro_root_advisor_root_move_is_safe(anchor)
        {
            return None;
        }

        root_moves
            .iter()
            .enumerate()
            .filter(|(_, root)| {
                Self::turn_engine_root_move_family(root) == TurnPlanFamily::SpiritImpact
                    && !(root.spirit_development
                        && !root.spirit_same_turn_score_setup_now
                        && !root.spirit_own_mana_setup_now)
                    && root.inputs.first() == anchor.inputs.first()
                    && root.efficiency == anchor.efficiency
                    && Self::current_pro_root_advisor_root_move_is_safe(root)
                    && !root.wins_immediately
                    && !root.attacks_opponent_drainer
                    && !root.scores_supermana_this_turn
                    && !root.scores_opponent_mana_this_turn
                    && !root.safe_supermana_pickup_now
                    && !root.safe_opponent_mana_pickup_now
                    && root.same_turn_score_window_value == 0
            })
            .min_by(|(left, _), (right, _)| {
                Self::compare_ranked_root_indices(root_moves, (*left, 0), (*right, 0))
            })
            .map(|(index, _)| index)
    }

    fn current_pro_root_advisor_selected_utility(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        perspective: Color,
        config: AutomoveSearchConfig,
        utility_cache: &mut std::collections::HashMap<usize, TurnEngineUtility>,
        index: usize,
    ) -> TurnEngineUtility {
        *utility_cache.entry(index).or_insert_with(|| {
            let root = &scored_roots[index];
            let family = Self::turn_engine_root_evaluation_family(root);
            Self::turn_engine_selected_override_utility(game, root, perspective, config, family)
        })
    }

    fn current_pro_root_advisor_find_scored_root_representative<F>(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        ordered_shortlist: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
        utility_cache: &mut std::collections::HashMap<usize, TurnEngineUtility>,
        predicate: F,
    ) -> Option<usize>
    where
        F: Fn(&RootEvaluation) -> bool,
    {
        let anchor_index = *ordered_shortlist.first()?;
        if ordered_shortlist.iter().any(|index| {
            let root = &scored_roots[*index];
            predicate(root) && Self::current_pro_root_advisor_root_evaluation_is_safe(root)
        }) {
            return None;
        }
        let anchor_utility = Self::current_pro_root_advisor_selected_utility(
            game,
            scored_roots,
            perspective,
            config,
            utility_cache,
            anchor_index,
        );

        scored_roots
            .iter()
            .enumerate()
            .filter(|(_, root)| {
                predicate(root) && Self::current_pro_root_advisor_root_evaluation_is_safe(root)
            })
            .filter(|(index, _)| {
                let candidate_utility = Self::current_pro_root_advisor_selected_utility(
                    game,
                    scored_roots,
                    perspective,
                    config,
                    utility_cache,
                    *index,
                );
                Self::current_pro_root_advisor_utility_competes(candidate_utility, anchor_utility)
            })
            .min_by(|(left, _), (right, _)| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })
            .map(|(index, _)| index)
    }

    fn current_pro_root_advisor_white_followup_representative(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        ordered_shortlist: &[usize],
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if ordered_shortlist.is_empty() {
            return None;
        }

        scored_roots
            .iter()
            .enumerate()
            .filter(|(index, root)| {
                !ordered_shortlist.contains(index)
                    && root.spirit_own_mana_setup_now
                    && root.opponent_mana_progress
                    && !Self::is_plain_spirit_development_root(root)
            })
            .filter(|(_, root)| Self::current_pro_root_advisor_root_evaluation_is_safe(root))
            .filter(|(_, root)| {
                ordered_shortlist.iter().any(|shortlist_index| {
                    Self::is_current_pro_white_spirit_followup_setup_pair(
                        game,
                        root,
                        &scored_roots[*shortlist_index],
                        config,
                    )
                })
            })
            .min_by(|(left, _), (right, _)| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })
            .map(|(index, _)| index)
    }

    fn current_pro_root_advisor_evaluate_injected_root(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        root_moves: &mut Vec<ScoredRootMove>,
        plan: &TurnPlan,
    ) -> Option<CurrentProInjectedRootAdvisorDecision> {
        let candidate_inputs = plan.compiled_chunks.first()?.clone();
        let mut simulated_root_moves = root_moves.clone();
        if Self::inject_turn_engine_root_candidate(
            game,
            perspective,
            config,
            &mut simulated_root_moves,
            plan,
        )
        .is_none()
        {
            return Some(CurrentProInjectedRootAdvisorDecision {
                inputs: candidate_inputs,
                family: plan.head_family,
                admitted: false,
                reason: CurrentProRootAdvisorReasonCode::RejectInjectedMacroRoot,
            });
        }

        let top = root_moves.first()?;
        let candidate_index = simulated_root_moves
            .iter()
            .position(|root| root.inputs == candidate_inputs)?;
        let candidate = &simulated_root_moves[candidate_index];
        let candidate_family = Self::turn_engine_root_move_family(candidate);
        if candidate.inputs == top.inputs {
            *root_moves = simulated_root_moves;
            return Some(CurrentProInjectedRootAdvisorDecision {
                inputs: candidate_inputs,
                family: candidate_family,
                admitted: true,
                reason: CurrentProRootAdvisorReasonCode::AdmitInjectedMacroRoot,
            });
        }

        let incumbent_utility = Self::turn_engine_scored_root_utility(
            game,
            top,
            perspective,
            config,
            Self::turn_engine_root_move_family(top),
        );
        let candidate_utility = Self::turn_engine_scored_root_utility(
            game,
            candidate,
            perspective,
            config,
            candidate_family,
        );
        let strict_primary_win = crate::models::automove_turn_engine::compare_utility_primary_axes(
            plan.utility,
            incumbent_utility,
        ) == std::cmp::Ordering::Greater
            || crate::models::automove_turn_engine::compare_utility_primary_axes(
                plan.head_utility,
                incumbent_utility,
            ) == std::cmp::Ordering::Greater
            || crate::models::automove_turn_engine::compare_utility_primary_axes(
                candidate_utility,
                incumbent_utility,
            ) == std::cmp::Ordering::Greater
            || plan
                .utility
                .strictly_dominates_override_axes(incumbent_utility)
            || candidate_utility.strictly_dominates_override_axes(incumbent_utility);
        let resolves_surface = (candidate.wins_immediately && !top.wins_immediately)
            || (candidate.attacks_opponent_drainer && !top.attacks_opponent_drainer)
            || ((candidate.scores_supermana_this_turn || candidate.scores_opponent_mana_this_turn)
                && !(top.scores_supermana_this_turn || top.scores_opponent_mana_this_turn))
            || (candidate.same_turn_score_window_value > top.same_turn_score_window_value
                && candidate.same_turn_score_window_value > 0)
            || (!Self::turn_engine_root_move_is_unsafe(candidate)
                && Self::turn_engine_root_move_is_unsafe(top))
            || (candidate.classes.drainer_safety_recover
                && top.own_drainer_vulnerable
                && !top.classes.drainer_safety_recover)
            || (Self::turn_engine_root_move_has_progress_surface(candidate)
                && !Self::turn_engine_root_move_has_progress_surface(top)
                && !Self::turn_engine_root_move_is_unsafe(candidate));
        let same_opening_plain_spirit_followup_competes =
            matches!(plan.head_family, TurnPlanFamily::SpiritImpact)
                && matches!(plan.goal_family, TurnPlanFamily::SpiritImpact)
                && root_moves
                    .iter()
                    .take(4)
                    .filter(|root| {
                        Self::is_current_pro_black_turn_six_plain_spirit_followup_setup_pair_move(
                            game, candidate, root, config,
                        )
                    })
                    .count()
                    >= 2
                && crate::models::automove_turn_engine::compare_utility_primary_axes(
                    candidate_utility,
                    incumbent_utility,
                ) != std::cmp::Ordering::Less
                && top.heuristic.saturating_sub(candidate.heuristic) <= 8;

        let admitted =
            strict_primary_win || resolves_surface || same_opening_plain_spirit_followup_competes;
        if admitted {
            *root_moves = simulated_root_moves;
        }
        Some(CurrentProInjectedRootAdvisorDecision {
            inputs: candidate_inputs,
            family: candidate_family,
            admitted,
            reason: if admitted {
                CurrentProRootAdvisorReasonCode::AdmitInjectedMacroRoot
            } else {
                CurrentProRootAdvisorReasonCode::RejectInjectedMacroRoot
            },
        })
    }

    fn current_pro_root_advisor_presearch(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        root_moves: &mut Vec<ScoredRootMove>,
        engine_head_plan: Option<&TurnPlan>,
    ) -> Option<CurrentProRootAdvisorDecision> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) || root_moves.is_empty() {
            return None;
        }
        if game.active_color == Color::Black
            && game.turn_number == 2
            && game.mons_moves_count <= 1
            && game.player_can_use_action()
            && game.player_can_move_mana()
        {
            return None;
        }

        let mut decision = CurrentProRootAdvisorDecision::default();
        Self::current_pro_root_advisor_push_unique(
            &mut decision.ordered_shortlist,
            Self::current_pro_root_advisor_entry_from_root_move(
                &root_moves[0],
                CurrentProRootAdvisorReasonCode::RankedRoot,
            ),
        );

        let representative_specs: [RootMoveRepresentativeSpec; 5] = [
            (
                CurrentProRootAdvisorReasonCode::PreserveSpiritRepresentative,
                |root| root.spirit_same_turn_score_setup_now || root.spirit_own_mana_setup_now,
            ),
            (
                CurrentProRootAdvisorReasonCode::PreserveSpiritRepresentative,
                |root| {
                    root.spirit_development
                        && !root.spirit_same_turn_score_setup_now
                        && !root.spirit_own_mana_setup_now
                },
            ),
            (
                CurrentProRootAdvisorReasonCode::PreserveSafeProgressRepresentative,
                |root| {
                    matches!(
                        MonsGameModel::turn_engine_root_move_family(root),
                        TurnPlanFamily::SafeSupermanaProgress
                    )
                },
            ),
            (
                CurrentProRootAdvisorReasonCode::PreserveSafeProgressRepresentative,
                |root| {
                    matches!(
                        MonsGameModel::turn_engine_root_move_family(root),
                        TurnPlanFamily::SafeOpponentManaProgress
                    )
                },
            ),
            (
                CurrentProRootAdvisorReasonCode::PreserveManaTempoRepresentative,
                |root| {
                    matches!(
                        MonsGameModel::turn_engine_root_move_family(root),
                        TurnPlanFamily::ManaTempo
                    )
                },
            ),
        ];
        for (reason, predicate) in representative_specs {
            if let Some(index) = Self::current_pro_root_advisor_find_root_move_representative(
                game,
                root_moves.as_slice(),
                perspective,
                config,
                predicate,
            ) {
                let entry =
                    Self::current_pro_root_advisor_entry_from_root_move(&root_moves[index], reason);
                Self::current_pro_root_advisor_push_unique(
                    &mut decision.preserved_family_representatives,
                    entry.clone(),
                );
                Self::current_pro_root_advisor_push_unique(&mut decision.ordered_shortlist, entry);
            }
        }
        if let Some(index) = Self::current_pro_root_advisor_same_opening_setup_representative(
            root_moves.as_slice(),
            config,
        ) {
            let entry = Self::current_pro_root_advisor_entry_from_root_move(
                &root_moves[index],
                CurrentProRootAdvisorReasonCode::PreserveSpiritRepresentative,
            );
            Self::current_pro_root_advisor_push_unique(
                &mut decision.preserved_family_representatives,
                entry.clone(),
            );
            Self::current_pro_root_advisor_push_unique(&mut decision.ordered_shortlist, entry);
        }

        if let Some(plan) = engine_head_plan {
            let injected_root = Self::current_pro_root_advisor_evaluate_injected_root(
                game,
                perspective,
                config,
                root_moves,
                plan,
            );
            if let Some(injected_root) = injected_root {
                if injected_root.admitted {
                    if let Some(index) = root_moves
                        .iter()
                        .position(|root| root.inputs == injected_root.inputs)
                    {
                        Self::current_pro_root_advisor_push_unique(
                            &mut decision.ordered_shortlist,
                            Self::current_pro_root_advisor_entry_from_root_move(
                                &root_moves[index],
                                CurrentProRootAdvisorReasonCode::AdmitInjectedMacroRoot,
                            ),
                        );
                    }
                }
                decision.injected_root = Some(injected_root);
            }
        }

        Some(decision)
    }

    fn current_pro_root_advisor_select_root(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Vec<Input> {
        let mut candidate_indices =
            Self::filtered_root_candidate_indices(game, scored_roots, perspective, config);
        if candidate_indices.is_empty() {
            candidate_indices = (0..scored_roots.len()).collect();
        }

        let mut decision = CurrentProRootAdvisorDecision::default();
        let mut ordered_shortlist = if config.enable_root_reply_risk_guard {
            Self::reply_risk_guard_shortlist_indices(
                scored_roots,
                candidate_indices.as_slice(),
                config,
            )
        } else {
            candidate_indices.clone()
        };
        if ordered_shortlist.is_empty() {
            ordered_shortlist = candidate_indices.clone();
        }
        let reply_risk_shortlist = ordered_shortlist.clone();
        let mut approval_shortlist = ordered_shortlist.clone();
        for index in ordered_shortlist.iter().copied() {
            Self::current_pro_root_advisor_push_unique(
                &mut decision.ordered_shortlist,
                Self::current_pro_root_advisor_entry_from_root_evaluation(
                    &scored_roots[index],
                    CurrentProRootAdvisorReasonCode::ReplyRiskShortlist,
                ),
            );
        }

        let mut utility_cache = std::collections::HashMap::<usize, TurnEngineUtility>::new();
        let mut spirit_followup_scores = std::collections::HashMap::<usize, i32>::new();
        let mut selection_indices = candidate_indices.clone();
        let representative_specs: [RootEvaluationRepresentativeSpec; 5] = [
            (
                CurrentProRootAdvisorReasonCode::PreserveSpiritRepresentative,
                |root| root.spirit_same_turn_score_setup_now || root.spirit_own_mana_setup_now,
            ),
            (
                CurrentProRootAdvisorReasonCode::PreserveSpiritRepresentative,
                |root| MonsGameModel::is_plain_spirit_development_root(root),
            ),
            (
                CurrentProRootAdvisorReasonCode::PreserveSafeProgressRepresentative,
                |root| {
                    matches!(
                        MonsGameModel::turn_engine_root_evaluation_family(root),
                        TurnPlanFamily::SafeSupermanaProgress
                    )
                },
            ),
            (
                CurrentProRootAdvisorReasonCode::PreserveSafeProgressRepresentative,
                |root| {
                    matches!(
                        MonsGameModel::turn_engine_root_evaluation_family(root),
                        TurnPlanFamily::SafeOpponentManaProgress
                    )
                },
            ),
            (
                CurrentProRootAdvisorReasonCode::PreserveManaTempoRepresentative,
                |root| {
                    matches!(
                        MonsGameModel::turn_engine_root_evaluation_family(root),
                        TurnPlanFamily::ManaTempo
                    )
                },
            ),
        ];
        for (reason, predicate) in representative_specs {
            if let Some(index) = Self::current_pro_root_advisor_find_scored_root_representative(
                game,
                scored_roots,
                ordered_shortlist.as_slice(),
                perspective,
                config,
                &mut utility_cache,
                predicate,
            ) {
                let entry = Self::current_pro_root_advisor_entry_from_root_evaluation(
                    &scored_roots[index],
                    reason,
                );
                Self::current_pro_root_advisor_push_unique(
                    &mut decision.preserved_family_representatives,
                    entry.clone(),
                );
                if !ordered_shortlist.contains(&index) {
                    ordered_shortlist.push(index);
                }
                if !selection_indices.contains(&index) {
                    selection_indices.push(index);
                }
                Self::current_pro_root_advisor_push_unique(&mut decision.ordered_shortlist, entry);
                if Self::current_pro_root_advisor_should_compete_in_approval_shortlist(
                    game,
                    scored_roots,
                    ordered_shortlist.as_slice(),
                    index,
                    &scored_roots[index],
                    reason,
                    perspective,
                    config,
                    &mut spirit_followup_scores,
                ) && !approval_shortlist.contains(&index)
                {
                    approval_shortlist.push(index);
                }
            }
        }
        if let Some(index) = Self::current_pro_root_advisor_white_followup_representative(
            game,
            scored_roots,
            ordered_shortlist.as_slice(),
            config,
        ) {
            let entry = Self::current_pro_root_advisor_entry_from_root_evaluation(
                &scored_roots[index],
                CurrentProRootAdvisorReasonCode::PreserveSpiritRepresentative,
            );
            Self::current_pro_root_advisor_push_unique(
                &mut decision.preserved_family_representatives,
                entry.clone(),
            );
            if !ordered_shortlist.contains(&index) {
                ordered_shortlist.push(index);
            }
            if !selection_indices.contains(&index) {
                selection_indices.push(index);
            }
            Self::current_pro_root_advisor_push_unique(&mut decision.ordered_shortlist, entry);
            if !approval_shortlist.contains(&index) {
                approval_shortlist.push(index);
            }
        }
        for reentry_index in [
            Self::current_pro_plain_spirit_cluster_progress_reentry(
                game,
                scored_roots,
                candidate_indices.as_slice(),
                perspective,
                config,
            ),
            Self::current_pro_plain_spirit_cluster_risky_recovery_reentry(
                game,
                scored_roots,
                candidate_indices.as_slice(),
                perspective,
                config,
            ),
            Self::current_pro_black_turn_six_spirit_reentry(
                game,
                scored_roots,
                ordered_shortlist.as_slice(),
                config,
            ),
            Self::current_pro_safe_progress_sibling_shortlist_extension(
                scored_roots,
                candidate_indices.as_slice(),
                ordered_shortlist.as_slice(),
                config,
            ),
            Self::current_pro_black_no_action_safe_progress_reentry(
                game,
                scored_roots,
                ordered_shortlist.as_slice(),
                config,
            ),
            Self::current_pro_black_no_action_mana_sibling_reentry(
                game,
                scored_roots,
                ordered_shortlist.as_slice(),
                config,
            ),
        ]
        .into_iter()
        .flatten()
        {
            let entry = Self::current_pro_root_advisor_entry_from_root_evaluation(
                &scored_roots[reentry_index],
                CurrentProRootAdvisorReasonCode::OmittedRootReentry,
            );
            Self::current_pro_root_advisor_push_unique(
                &mut decision.preserved_family_representatives,
                entry.clone(),
            );
            if !ordered_shortlist.contains(&reentry_index) {
                ordered_shortlist.push(reentry_index);
            }
            if !selection_indices.contains(&reentry_index) {
                selection_indices.push(reentry_index);
            }
            Self::current_pro_root_advisor_push_unique(&mut decision.ordered_shortlist, entry);
            if !approval_shortlist.contains(&reentry_index) {
                approval_shortlist.push(reentry_index);
            }
        }
        if let Some(reentry_index) = Self::current_pro_white_turn_four_mana_sibling_reentry(
            game,
            scored_roots,
            ordered_shortlist.as_slice(),
            perspective,
            config,
        ) {
            let entry = Self::current_pro_root_advisor_entry_from_root_evaluation(
                &scored_roots[reentry_index],
                CurrentProRootAdvisorReasonCode::OmittedRootReentry,
            );
            Self::current_pro_root_advisor_push_unique(
                &mut decision.preserved_family_representatives,
                entry.clone(),
            );
            if !ordered_shortlist.contains(&reentry_index) {
                ordered_shortlist.push(reentry_index);
            }
            if !selection_indices.contains(&reentry_index) {
                selection_indices.push(reentry_index);
            }
            Self::current_pro_root_advisor_push_unique(&mut decision.ordered_shortlist, entry);
            if !approval_shortlist.contains(&reentry_index) {
                approval_shortlist.push(reentry_index);
            }
        }
        if selection_indices.len() > 1 {
            selection_indices.sort_by(|left, right| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            });
        }

        let mut shortlist_config = config;
        shortlist_config.enable_root_reply_risk_guard = false;
        let mut baseline_selector_config = shortlist_config;
        baseline_selector_config.turn_engine_mode = TurnEngineMode::ProV1;
        let baseline_approved_inputs = Self::pick_root_move_with_exploration_from_candidate_indices(
            game,
            scored_roots,
            candidate_indices.as_slice(),
            perspective,
            baseline_selector_config,
        );
        let baseline_approved_index = scored_roots
            .iter()
            .position(|root| root.inputs == baseline_approved_inputs);
        let mut approved_index = if config.enable_root_reply_risk_guard {
            Self::pick_root_move_with_reply_risk_guard_from_shortlist(
                game,
                scored_roots,
                approval_shortlist.as_slice(),
                Some(selection_indices.as_slice()),
                perspective,
                config,
            )
            .map(|index| {
                (
                    index,
                    CurrentProRootAdvisorReasonCode::ApprovedReplyRiskGuard,
                )
            })
        } else {
            None
        }
        .unwrap_or_else(|| {
            let inputs = Self::pick_root_move_with_exploration_from_candidate_indices(
                game,
                scored_roots,
                selection_indices.as_slice(),
                perspective,
                shortlist_config,
            );
            let index = scored_roots
                .iter()
                .position(|root| root.inputs == inputs)
                .unwrap_or(ordered_shortlist[0]);
            (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedBaselineSelector,
            )
        });
        if let Some(index) = Self::current_pro_root_advisor_black_family_competition_override(
            game,
            scored_roots,
            reply_risk_shortlist.as_slice(),
            selection_indices.as_slice(),
            approved_index.0,
            perspective,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_black_opening_setup_sibling_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_black_early_safe_mana_followup_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_black_early_plain_spirit_followup_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_black_turn_four_vulnerable_progress_mana_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_black_turn_six_attack_vulnerable_progress_mana_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_black_turn_four_setup_cluster_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_early_same_lane_higher_score_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_black_setup_progress_competition_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                perspective,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_black_plain_spirit_setup_competition_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_black_no_action_progress_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_black_no_action_mana_sibling_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_black_turn_four_window_mana_sibling_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_black_turn_four_weak_window_recovery_override(
                game,
                scored_roots,
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = baseline_approved_index.and_then(|baseline_index| {
            Self::current_pro_root_advisor_black_baseline_alignment_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                baseline_index,
                config,
            )
        }) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedBaselineSelector,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_black_turn_start_guarded_baseline_mana_override(
                game,
                scored_roots,
                candidate_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedBaselineSelector,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_black_late_window_mana_safety_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_black_late_window_competition_override(
            game,
            scored_roots,
            approved_index.0,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_black_late_recovery_progress_competition_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_white_followup_mana_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            perspective,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_white_mana_competition_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            perspective,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_white_no_action_safe_progress_mana_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_white_window_progress_competition_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                perspective,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_white_action_mana_cluster_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            perspective,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_white_setup_progress_competition_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                perspective,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_white_early_followup_setup_competition_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_early_same_lane_higher_score_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_white_early_setup_sibling_progress_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_white_early_safe_progress_setup_competition_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_white_early_no_action_progress_competition_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_white_turn_three_safe_progress_surface_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = baseline_approved_index.and_then(|baseline_index| {
            Self::current_pro_root_advisor_white_turn_three_baseline_alignment_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                baseline_index,
                config,
            )
        }) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedBaselineSelector,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_white_turn_three_attack_bridge_escape(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_white_turn_three_no_action_recovery_override(
                game,
                scored_roots,
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_white_mana_only_competition_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            perspective,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_white_turn_five_weak_window_setup_override(
                game,
                scored_roots,
                selection_indices.as_slice(),
                approved_index.0,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_white_early_setup_competition_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            perspective,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_white_late_followup_competition_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            perspective,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_black_late_followup_competition_override(
            game,
            scored_roots,
            selection_indices.as_slice(),
            approved_index.0,
            perspective,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) = Self::current_pro_root_advisor_black_late_reply_risk_setup_override(
            game,
            scored_roots,
            reply_risk_shortlist.as_slice(),
            approved_index.0,
            config,
        ) {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }
        if let Some(index) =
            Self::current_pro_root_advisor_black_late_weak_window_safe_progress_setup_override(
                game,
                scored_roots,
                reply_risk_shortlist.as_slice(),
                approved_index.0,
                perspective,
                config,
            )
        {
            approved_index = (
                index,
                CurrentProRootAdvisorReasonCode::ApprovedFamilyCompetition,
            );
        }

        decision.approved_root = Some(Self::current_pro_root_advisor_entry_from_root_evaluation(
            &scored_roots[approved_index.0],
            approved_index.1,
        ));
        scored_roots[approved_index.0].inputs.clone()
    }

    fn pick_root_move_with_exploration(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Vec<Input> {
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        if scored_roots.is_empty() {
            return Vec::new();
        }

        if matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) {
            let selected =
                Self::current_pro_root_advisor_select_root(game, scored_roots, perspective, config);
            return if crate::models::automove_deadline::cancelled() {
                Vec::new()
            } else {
                selected
            };
        }

        let mut candidate_indices =
            Self::filtered_root_candidate_indices(game, scored_roots, perspective, config);
        if candidate_indices.is_empty() {
            candidate_indices = (0..scored_roots.len()).collect();
        }

        Self::pick_root_move_with_exploration_from_candidate_indices(
            game,
            scored_roots,
            candidate_indices.as_slice(),
            perspective,
            config,
        )
    }

    fn pick_root_move_with_exploration_from_candidate_indices(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Vec<Input> {
        if crate::models::automove_deadline::checkpoint() {
            return Vec::new();
        }
        if scored_roots.is_empty() {
            return Vec::new();
        }
        if candidate_indices.is_empty() {
            return Vec::new();
        }

        if config.enable_root_reply_risk_guard {
            if let Some(reply_guarded_index) = Self::pick_root_move_with_reply_risk_guard(
                game,
                scored_roots,
                candidate_indices,
                perspective,
                config,
            ) {
                if crate::models::automove_deadline::cancelled() {
                    return Vec::new();
                }
                return scored_roots[reply_guarded_index].inputs.clone();
            }
            if crate::models::automove_deadline::cancelled() {
                return Vec::new();
            }
        }

        let best_score = candidate_indices
            .iter()
            .map(|index| scored_roots[*index].score)
            .max()
            .unwrap_or(i32::MIN);
        let score_margin = config.root_efficiency_score_margin.max(0);

        let mut best_index = Self::best_scored_root_index(scored_roots, candidate_indices);
        let mut best_efficiency = i32::MIN;
        let mut best_shortlisted_score = i32::MIN;
        let prefer_spirit_development = config.enable_root_spirit_development_pref
            && Self::should_prefer_spirit_development(game, perspective);
        let current_pro_plain_spirit_projection_tiebreak =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && candidate_indices
                    .iter()
                    .filter(|index| Self::is_plain_spirit_development_root(&scored_roots[**index]))
                    .take(2)
                    .count()
                    >= 2;
        let spirit_turn_engine_projections =
            if prefer_spirit_development || current_pro_plain_spirit_projection_tiebreak {
                Self::turn_engine_spirit_root_projections(
                    scored_roots,
                    candidate_indices,
                    perspective,
                    config,
                )
            } else {
                std::collections::HashMap::new()
            };
        if crate::models::automove_deadline::cancelled() {
            return Vec::new();
        }
        let mut best_spirit_development = scored_roots[best_index].spirit_development;
        let mut best_spirit_same_turn_score_setup =
            scored_roots[best_index].spirit_same_turn_score_setup_now;
        let mut best_spirit_own_mana_setup = scored_roots[best_index].spirit_own_mana_setup_now;
        let mut best_supermana_progress = scored_roots[best_index].supermana_progress;
        let mut best_opponent_mana_progress = scored_roots[best_index].opponent_mana_progress;
        let mut best_score_path_best_steps = scored_roots[best_index].score_path_best_steps;
        let mut best_safe_supermana_progress_steps =
            scored_roots[best_index].safe_supermana_progress_steps;
        let mut best_safe_opponent_mana_progress_steps =
            scored_roots[best_index].safe_opponent_mana_progress_steps;
        let mut best_same_turn_score_window_value =
            scored_roots[best_index].same_turn_score_window_value;
        let mut best_spirit_setup_gain = scored_roots[best_index].spirit_setup_gain;
        let mut best_mana_handoff = scored_roots[best_index].mana_handoff_to_opponent;
        let mut best_has_roundtrip = scored_roots[best_index].has_roundtrip;
        let mut best_soft_priority = scored_roots[best_index].interview_soft_priority;
        let mut spirit_followup_floor_scores = std::collections::HashMap::new();

        for index in candidate_indices.iter().copied() {
            if crate::models::automove_deadline::checkpoint() {
                return Vec::new();
            }
            let evaluation = &scored_roots[index];
            let allow_close_plain_spirit_score_slack =
                matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                    && game.turn_number <= 3
                    && Self::is_plain_spirit_development_root(evaluation)
                    && Self::is_plain_spirit_development_root(&scored_roots[best_index])
                    && best_score.saturating_sub(evaluation.score) <= 16;
            let spirit_setup_competes_with_best =
                Self::current_pro_spirit_setup_competes_with_non_spirit_root(
                    game,
                    evaluation,
                    &scored_roots[best_index],
                    perspective,
                    config,
                );
            if evaluation.score + score_margin < best_score && !allow_close_plain_spirit_score_slack
            {
                continue;
            }
            let spirit_preference_challenge_order = if prefer_spirit_development
                && evaluation.spirit_development != best_spirit_development
            {
                Self::current_pro_spirit_score_challenge_order(
                    evaluation,
                    &scored_roots[best_index],
                )
                .or_else(|| {
                    if (evaluation.score - scored_roots[best_index].score).abs() <= 320 {
                        Self::current_pro_spirit_projection_challenge_order(
                            evaluation,
                            spirit_turn_engine_projections.get(&index),
                            &scored_roots[best_index],
                            spirit_turn_engine_projections.get(&best_index),
                        )
                    } else {
                        None
                    }
                })
            } else {
                None
            };
            let spirit_better = if matches!(
                spirit_preference_challenge_order,
                Some(std::cmp::Ordering::Greater | std::cmp::Ordering::Less)
            ) {
                false
            } else {
                prefer_spirit_development
                    && evaluation.spirit_development
                    && !best_spirit_development
                    && spirit_setup_competes_with_best
            };
            let equal_spirit_preference = !prefer_spirit_development
                || evaluation.spirit_development == best_spirit_development
                || spirit_preference_challenge_order.is_some()
                || (matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                    && spirit_setup_competes_with_best
                    && (evaluation.spirit_own_mana_setup_now
                        || evaluation.spirit_same_turn_score_setup_now));
            let spirit_same_turn_score_setup_better = evaluation.spirit_same_turn_score_setup_now
                && !best_spirit_same_turn_score_setup
                && spirit_setup_competes_with_best;
            let equal_spirit_same_turn_score_setup =
                evaluation.spirit_same_turn_score_setup_now == best_spirit_same_turn_score_setup;
            let spirit_setup_better = evaluation.spirit_own_mana_setup_now
                && !best_spirit_own_mana_setup
                && spirit_setup_competes_with_best;
            let equal_spirit_setup =
                evaluation.spirit_own_mana_setup_now == best_spirit_own_mana_setup;
            let spirit_development_setup_gain_better = prefer_spirit_development
                && evaluation.spirit_development
                && best_spirit_development
                && evaluation.spirit_setup_gain > best_spirit_setup_gain;
            let equal_spirit_development_setup_gain = !prefer_spirit_development
                || !evaluation.spirit_development
                || !best_spirit_development
                || evaluation.spirit_setup_gain == best_spirit_setup_gain;
            let compare_plain_spirit_projection = current_pro_plain_spirit_projection_tiebreak
                && Self::is_plain_spirit_development_root(evaluation)
                && Self::is_plain_spirit_development_root(&scored_roots[best_index]);
            let spirit_projection_order = if (prefer_spirit_development
                || compare_plain_spirit_projection)
                && evaluation.spirit_development
                && best_spirit_development
            {
                let close_spirit_roots =
                    (evaluation.score - scored_roots[best_index].score).abs() <= 192;
                match (
                    spirit_turn_engine_projections.get(&index),
                    spirit_turn_engine_projections.get(&best_index),
                ) {
                    (Some(candidate_projection), Some(best_projection)) => {
                        Some(Self::compare_spirit_root_projection_plans(
                            candidate_projection,
                            best_projection,
                            close_spirit_roots,
                        ))
                    }
                    (Some(_), None) => Some(std::cmp::Ordering::Greater),
                    (None, Some(_)) => Some(std::cmp::Ordering::Less),
                    (None, None) => None,
                }
            } else {
                None
            };
            let spirit_projection_better =
                matches!(spirit_projection_order, Some(std::cmp::Ordering::Greater));
            let equal_spirit_projection = spirit_projection_order.is_none()
                || matches!(spirit_projection_order, Some(std::cmp::Ordering::Equal));
            let spirit_followup_floor_order = Self::current_pro_spirit_followup_floor_order(
                game,
                scored_roots,
                index,
                best_index,
                perspective,
                config,
                &mut spirit_followup_floor_scores,
            );
            let spirit_followup_floor_better = matches!(
                spirit_followup_floor_order,
                Some(std::cmp::Ordering::Greater)
            );
            let equal_spirit_followup_floor = spirit_followup_floor_order.is_none()
                || matches!(spirit_followup_floor_order, Some(std::cmp::Ordering::Equal));
            let spirit_setup_supermana_progress_steps_better = evaluation.spirit_own_mana_setup_now
                && best_spirit_own_mana_setup
                && evaluation.supermana_progress
                && best_supermana_progress
                && Self::root_progress_steps_better(
                    evaluation.safe_supermana_progress_steps,
                    best_safe_supermana_progress_steps,
                );
            let equal_spirit_setup_supermana_progress_steps = !evaluation.spirit_own_mana_setup_now
                || !best_spirit_own_mana_setup
                || !evaluation.supermana_progress
                || !best_supermana_progress
                || evaluation.safe_supermana_progress_steps == best_safe_supermana_progress_steps;
            let spirit_setup_opponent_mana_progress_steps_better = evaluation
                .spirit_own_mana_setup_now
                && best_spirit_own_mana_setup
                && evaluation.opponent_mana_progress
                && best_opponent_mana_progress
                && Self::root_progress_steps_better(
                    evaluation.safe_opponent_mana_progress_steps,
                    best_safe_opponent_mana_progress_steps,
                );
            let equal_spirit_setup_opponent_mana_progress_steps = !evaluation
                .spirit_own_mana_setup_now
                || !best_spirit_own_mana_setup
                || !evaluation.opponent_mana_progress
                || !best_opponent_mana_progress
                || evaluation.safe_opponent_mana_progress_steps
                    == best_safe_opponent_mana_progress_steps;
            let spirit_setup_score_path_better = evaluation.spirit_own_mana_setup_now
                && best_spirit_own_mana_setup
                && Self::root_score_path_steps_better(
                    evaluation.score_path_best_steps,
                    best_score_path_best_steps,
                );
            let equal_spirit_setup_score_path = !evaluation.spirit_own_mana_setup_now
                || !best_spirit_own_mana_setup
                || evaluation.score_path_best_steps == best_score_path_best_steps;
            let supermana_progress_steps_better = Self::root_progress_steps_better(
                evaluation.safe_supermana_progress_steps,
                best_safe_supermana_progress_steps,
            );
            let equal_supermana_progress_steps =
                evaluation.safe_supermana_progress_steps == best_safe_supermana_progress_steps;
            let opponent_mana_progress_steps_better = Self::root_progress_steps_better(
                evaluation.safe_opponent_mana_progress_steps,
                best_safe_opponent_mana_progress_steps,
            );
            let equal_opponent_mana_progress_steps = evaluation.safe_opponent_mana_progress_steps
                == best_safe_opponent_mana_progress_steps;
            let same_turn_score_window_better =
                evaluation.same_turn_score_window_value > best_same_turn_score_window_value;
            let equal_same_turn_score_window =
                evaluation.same_turn_score_window_value == best_same_turn_score_window_value;
            let handoff_better = !evaluation.mana_handoff_to_opponent && best_mana_handoff;
            let equal_handoff = evaluation.mana_handoff_to_opponent == best_mana_handoff;
            let roundtrip_better = !evaluation.has_roundtrip && best_has_roundtrip;
            let equal_roundtrip = evaluation.has_roundtrip == best_has_roundtrip;
            let soft_better = evaluation.interview_soft_priority
                > best_soft_priority.saturating_add(config.interview_soft_score_margin.max(0));
            let soft_equal_or_disabled = evaluation
                .interview_soft_priority
                .saturating_add(config.interview_soft_score_margin.max(0))
                >= best_soft_priority;
            let efficiency_or_score_better = evaluation.efficiency > best_efficiency
                || (evaluation.efficiency == best_efficiency
                    && evaluation.score > best_shortlisted_score);
            let tie_break_better = if matches!(
                spirit_preference_challenge_order,
                Some(std::cmp::Ordering::Greater)
            ) {
                true
            } else if matches!(
                spirit_preference_challenge_order,
                Some(std::cmp::Ordering::Less)
            ) {
                false
            } else if soft_better {
                true
            } else if !soft_equal_or_disabled {
                false
            } else if same_turn_score_window_better {
                true
            } else if !equal_same_turn_score_window {
                false
            } else if spirit_same_turn_score_setup_better {
                true
            } else if !equal_spirit_same_turn_score_setup {
                false
            } else if spirit_setup_better {
                true
            } else if !equal_spirit_setup {
                false
            } else if spirit_development_setup_gain_better {
                true
            } else if !equal_spirit_development_setup_gain {
                false
            } else if spirit_followup_floor_better {
                true
            } else if !equal_spirit_followup_floor {
                false
            } else if spirit_projection_better {
                true
            } else if !equal_spirit_projection {
                false
            } else if spirit_setup_supermana_progress_steps_better {
                true
            } else if !equal_spirit_setup_supermana_progress_steps {
                false
            } else if spirit_setup_opponent_mana_progress_steps_better {
                true
            } else if !equal_spirit_setup_opponent_mana_progress_steps {
                false
            } else if spirit_setup_score_path_better {
                true
            } else if !equal_spirit_setup_score_path {
                false
            } else if supermana_progress_steps_better {
                true
            } else if !equal_supermana_progress_steps {
                false
            } else if opponent_mana_progress_steps_better {
                true
            } else if !equal_opponent_mana_progress_steps {
                false
            } else if handoff_better {
                true
            } else if !equal_handoff {
                false
            } else if roundtrip_better {
                true
            } else if !equal_roundtrip {
                false
            } else {
                efficiency_or_score_better
            };
            if spirit_better || (equal_spirit_preference && tie_break_better) {
                best_index = index;
                best_efficiency = evaluation.efficiency;
                best_shortlisted_score = evaluation.score;
                best_spirit_development = evaluation.spirit_development;
                best_spirit_same_turn_score_setup = evaluation.spirit_same_turn_score_setup_now;
                best_spirit_own_mana_setup = evaluation.spirit_own_mana_setup_now;
                best_supermana_progress = evaluation.supermana_progress;
                best_opponent_mana_progress = evaluation.opponent_mana_progress;
                best_score_path_best_steps = evaluation.score_path_best_steps;
                best_safe_supermana_progress_steps = evaluation.safe_supermana_progress_steps;
                best_safe_opponent_mana_progress_steps =
                    evaluation.safe_opponent_mana_progress_steps;
                best_same_turn_score_window_value = evaluation.same_turn_score_window_value;
                best_spirit_setup_gain = evaluation.spirit_setup_gain;
                best_mana_handoff = evaluation.mana_handoff_to_opponent;
                best_has_roundtrip = evaluation.has_roundtrip;
                best_soft_priority = evaluation.interview_soft_priority;
            }
        }

        scored_roots[best_index].inputs.clone()
    }

    fn compare_spirit_root_projection_plans(
        candidate: &TurnEngineRootProjection,
        incumbent: &TurnEngineRootProjection,
        close_spirit_roots: bool,
    ) -> std::cmp::Ordering {
        if close_spirit_roots {
            let head_axes_order = crate::models::automove_turn_engine::compare_utility_primary_axes(
                candidate.plan.head_utility,
                incumbent.plan.head_utility,
            );
            if head_axes_order != std::cmp::Ordering::Equal {
                return head_axes_order;
            }
            let primary_axes_order =
                crate::models::automove_turn_engine::compare_utility_primary_axes(
                    candidate.plan.utility,
                    incumbent.plan.utility,
                );
            if primary_axes_order != std::cmp::Ordering::Equal {
                return primary_axes_order;
            }
            let candidate_spirit_impact =
                matches!(candidate.plan.head_family, TurnPlanFamily::SpiritImpact);
            let incumbent_spirit_impact =
                matches!(incumbent.plan.head_family, TurnPlanFamily::SpiritImpact);
            if candidate_spirit_impact != incumbent_spirit_impact {
                return candidate_spirit_impact.cmp(&incumbent_spirit_impact);
            }
            let spirit_goal_family_order = Self::compare_close_spirit_goal_family(
                candidate.plan.head_family,
                candidate.plan.goal_family,
                incumbent.plan.head_family,
                incumbent.plan.goal_family,
            );
            if primary_axes_order == std::cmp::Ordering::Equal
                && spirit_goal_family_order != std::cmp::Ordering::Equal
            {
                return spirit_goal_family_order;
            }
        }

        turn_engine_compare_plans(&candidate.plan, &incumbent.plan)
    }

    fn current_pro_spirit_setup_competes_with_non_spirit_root(
        game: &MonsGame,
        candidate: &RootEvaluation,
        incumbent: &RootEvaluation,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> bool {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || incumbent.spirit_development
            || !(candidate.spirit_own_mana_setup_now || candidate.spirit_same_turn_score_setup_now)
        {
            return true;
        }
        if candidate.same_turn_score_window_value > incumbent.same_turn_score_window_value
            || candidate.score >= incumbent.score
        {
            return true;
        }
        if game.active_color == Color::Black
            && game.turn_number <= 2
            && candidate.spirit_own_mana_setup_now
            && !candidate.spirit_same_turn_score_setup_now
            && Self::turn_engine_root_evaluation_family(incumbent) == TurnPlanFamily::ManaTempo
            && !candidate.wins_immediately
            && !incumbent.wins_immediately
            && !candidate.attacks_opponent_drainer
            && !incumbent.attacks_opponent_drainer
            && !candidate.scores_supermana_this_turn
            && !incumbent.scores_supermana_this_turn
            && !candidate.scores_opponent_mana_this_turn
            && !incumbent.scores_opponent_mana_this_turn
            && !candidate.safe_supermana_pickup_now
            && !incumbent.safe_supermana_pickup_now
            && !candidate.safe_opponent_mana_pickup_now
            && !incumbent.safe_opponent_mana_pickup_now
            && !candidate.mana_handoff_to_opponent
            && !incumbent.mana_handoff_to_opponent
            && !candidate.has_roundtrip
            && !incumbent.has_roundtrip
            && !Self::turn_engine_root_evaluation_is_unsafe(candidate)
            && !Self::turn_engine_root_evaluation_is_unsafe(incumbent)
            && candidate.score.saturating_add(64) >= incumbent.score
            && candidate.spirit_setup_gain >= incumbent.spirit_setup_gain.saturating_add(48)
            && candidate.root_rank <= incumbent.root_rank
        {
            return true;
        }

        let candidate_family = Self::turn_engine_root_evaluation_family(candidate);
        let incumbent_family = Self::turn_engine_root_evaluation_family(incumbent);
        let candidate_utility = Self::turn_engine_selected_override_utility(
            game,
            candidate,
            perspective,
            config,
            candidate_family,
        );
        let incumbent_utility = Self::turn_engine_selected_override_utility(
            game,
            incumbent,
            perspective,
            config,
            incumbent_family,
        );

        crate::models::automove_turn_engine::compare_utility_primary_axes(
            candidate_utility,
            incumbent_utility,
        ) != std::cmp::Ordering::Less
    }

    fn compare_close_spirit_goal_family(
        candidate_head: TurnPlanFamily,
        candidate_goal: TurnPlanFamily,
        incumbent_head: TurnPlanFamily,
        incumbent_goal: TurnPlanFamily,
    ) -> std::cmp::Ordering {
        if !matches!(candidate_head, TurnPlanFamily::SpiritImpact)
            || !matches!(incumbent_head, TurnPlanFamily::SpiritImpact)
        {
            return std::cmp::Ordering::Equal;
        }

        Self::close_spirit_goal_family_priority(candidate_goal)
            .cmp(&Self::close_spirit_goal_family_priority(incumbent_goal))
    }

    fn close_spirit_goal_family_priority(family: TurnPlanFamily) -> i32 {
        match family {
            TurnPlanFamily::ImmediateScore => 7,
            TurnPlanFamily::DenyOpponentWindow => 6,
            TurnPlanFamily::DrainerKill => 5,
            TurnPlanFamily::DrainerSafetyRecovery => 4,
            TurnPlanFamily::SpiritImpact => 3,
            TurnPlanFamily::SafeSupermanaProgress => 2,
            TurnPlanFamily::SafeOpponentManaProgress => 1,
            TurnPlanFamily::ManaTempo => 0,
        }
    }

    fn is_plain_spirit_development_root(root: &RootEvaluation) -> bool {
        root.spirit_development
            && !root.spirit_same_turn_score_setup_now
            && !root.spirit_own_mana_setup_now
    }

    fn can_turn_engine_project_root(root: &RootEvaluation, perspective: Color) -> bool {
        root.game.active_color == perspective && root.game.winner_color().is_none()
    }

    fn current_pro_turn_engine_live(config: AutomoveSearchConfig) -> bool {
        config.enable_turn_engine_selector
            && matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
    }

    fn current_pro_low_budget_guard_live(config: AutomoveSearchConfig) -> bool {
        Self::current_pro_turn_engine_live(config) && config.enable_turn_engine_low_budget_guard
    }

    fn current_pro_mid_turn_tactical_guard_live(config: AutomoveSearchConfig) -> bool {
        Self::current_pro_turn_engine_live(config)
            && config.enable_turn_engine_mid_turn_tactical_guard
    }

    fn current_pro_secondary_analysis_live(config: AutomoveSearchConfig) -> bool {
        Self::current_pro_turn_engine_live(config) && config.enable_turn_engine_secondary_analysis
    }

    fn current_pro_selected_followup_projection_live(config: AutomoveSearchConfig) -> bool {
        Self::current_pro_secondary_analysis_live(config)
            && config.enable_turn_engine_selected_followup_projection
    }

    fn can_challenge_spirit_preference_root(root: &RootEvaluation, perspective: Color) -> bool {
        Self::can_turn_engine_project_root(root, perspective)
            && !Self::is_plain_spirit_development_root(root)
            && !root.spirit_same_turn_score_setup_now
            && !root.spirit_own_mana_setup_now
            && !Self::turn_engine_root_evaluation_is_unsafe(root)
            && !root.has_roundtrip
    }

    fn can_challenge_spirit_preference_root_with_recovery_projection(
        root: &RootEvaluation,
        perspective: Color,
    ) -> bool {
        Self::can_turn_engine_project_root(root, perspective)
            && !Self::is_plain_spirit_development_root(root)
            && !root.spirit_same_turn_score_setup_now
            && !root.spirit_own_mana_setup_now
            && !root.mana_handoff_to_opponent
            && !root.has_roundtrip
    }

    fn should_skip_current_pro_low_budget_state(game: &MonsGame) -> bool {
        if Self::current_pro_is_early_white_turn_start(game) {
            return false;
        }
        if game.player_can_use_action() || game.player_can_move_mana() {
            return false;
        }

        let perspective = game.active_color;
        let context = crate::models::automove_exact::exact_opportunity_context(game, perspective);
        !context.opponent_can_win_immediately
            && context.delta.same_turn_score_window_value <= 0
            && context.delta.opponent_window_deny_gain <= 0
            && !context.delta.drainer_attack_available
            && context.delta.safe_supermana_progress_steps.is_none()
            && context.delta.safe_opponent_mana_progress_steps.is_none()
            && context.delta.drainer_safety >= 0
    }

    pub(crate) fn should_disable_current_pro_mid_turn_tactical_engine(game: &MonsGame) -> bool {
        if game.active_color != Color::White
            || game.turn_number != 3
            || game.mons_moves_count == 0
            || game.mons_moves_count > 2
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
            || Self::current_pro_is_early_white_turn_start(game)
        {
            return false;
        }

        let perspective = game.active_color;
        let context = crate::models::automove_exact::exact_opportunity_context(game, perspective);
        !context.opponent_can_win_immediately
            && context.delta.drainer_safety < 0
            && context.delta.same_turn_score_window_value <= 0
            && context.delta.safe_supermana_progress_steps.is_none()
            && context.delta.safe_opponent_mana_progress_steps.is_none()
    }

    fn apply_current_pro_low_budget_search_clamp(
        game: &MonsGame,
        mut config: AutomoveSearchConfig,
    ) -> AutomoveSearchConfig {
        if !Self::current_pro_low_budget_guard_live(config) {
            return config;
        }

        if game.active_color == Color::Black
            && game.turn_number == 2
            && game.mons_moves_count <= 1
            && (!game.player_can_use_action() || !game.player_can_move_mana())
        {
            config.max_visited_nodes = config.max_visited_nodes.min(6_000);
            config.root_branch_limit = config.root_branch_limit.clamp(1, 12);
            config.root_reply_risk_reply_limit = config.root_reply_risk_reply_limit.clamp(1, 12);
            config.root_reply_risk_node_share_bp = config.root_reply_risk_node_share_bp.min(1_200);
        }

        config
    }

    fn current_pro_is_safe_early_black_opening_state(game: &MonsGame) -> bool {
        if game.active_color != Color::Black
            || game.turn_number != 2
            || game.mons_moves_count != 0
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
        {
            return false;
        }

        let perspective = game.active_color;
        let context = crate::models::automove_exact::exact_opportunity_context(game, perspective);
        !context.opponent_can_win_immediately
            && context.delta.same_turn_score_window_value <= 0
            && context.delta.opponent_window_deny_gain <= 0
            && !context.delta.drainer_attack_available
            && context.delta.drainer_safety >= 2
    }

    fn current_pro_use_fresh_live_head_plan(game: &MonsGame, config: AutomoveSearchConfig) -> bool {
        Self::current_pro_turn_engine_live(config)
            && game.active_color == Color::White
            && game.turn_number >= 3
            && game.mons_moves_count == 0
            && game.player_can_use_action()
            && game.player_can_move_mana()
    }

    fn current_pro_is_early_white_turn_start(game: &MonsGame) -> bool {
        game.active_color == Color::White
            && game.turn_number <= 3
            && !game.player_can_use_action()
            && !game.player_can_move_mana()
            && matches!(game.mons_moves_count, 0 | 3)
    }

    fn current_pro_is_white_turn_one_mana_only_followup(game: &MonsGame) -> bool {
        game.active_color == Color::White
            && game.turn_number == 1
            && game.is_first_turn()
            && game.mons_moves_count == 1
            && !game.player_can_use_action()
            && !game.player_can_move_mana()
    }

    fn current_pro_is_late_white_mana_only_turn_start(game: &MonsGame) -> bool {
        game.active_color == Color::White
            && game.turn_number >= 6
            && game.mons_moves_count == 0
            && !game.player_can_use_action()
            && game.player_can_move_mana()
    }

    fn current_pro_is_late_black_action_mana_turn_start(game: &MonsGame) -> bool {
        game.active_color == Color::Black
            && game.turn_number >= 8
            && game.mons_moves_count == 0
            && game.player_can_use_action()
            && game.player_can_move_mana()
    }

    fn projected_risky_recovery_root_utility(
        game: &MonsGame,
        root: &RootEvaluation,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<TurnEngineUtility> {
        if !config.enable_turn_engine_selector
            || !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
        {
            return None;
        }
        if !root.own_drainer_vulnerable
            || !Self::can_challenge_spirit_preference_root_with_recovery_projection(
                root,
                perspective,
            )
        {
            return None;
        }

        let family = Self::turn_engine_root_evaluation_family(root);
        let utility =
            Self::turn_engine_selected_override_utility(game, root, perspective, config, family);
        utility
            .supports_temporary_risk_recovery()
            .then_some(utility)
    }

    fn projected_recovery_utility_competes(
        candidate: TurnEngineUtility,
        incumbent: TurnEngineUtility,
    ) -> bool {
        candidate.supports_temporary_risk_recovery()
            && crate::models::automove_turn_engine::compare_utility_primary_axes(
                candidate, incumbent,
            ) != std::cmp::Ordering::Less
            && candidate.supports_family_fallback(incumbent)
    }

    fn current_pro_spirit_projection_challenge_order(
        candidate: &RootEvaluation,
        candidate_projection: Option<&TurnEngineRootProjection>,
        incumbent: &RootEvaluation,
        incumbent_projection: Option<&TurnEngineRootProjection>,
    ) -> Option<std::cmp::Ordering> {
        let candidate_plain_spirit = Self::is_plain_spirit_development_root(candidate);
        let incumbent_plain_spirit = Self::is_plain_spirit_development_root(incumbent);
        if candidate_plain_spirit == incumbent_plain_spirit {
            return None;
        }

        let (challenger, challenger_projection, spirit, spirit_projection, candidate_is_challenger) =
            if candidate_plain_spirit {
                (
                    incumbent,
                    incumbent_projection?,
                    candidate,
                    candidate_projection?,
                    false,
                )
            } else {
                (
                    candidate,
                    candidate_projection?,
                    incumbent,
                    incumbent_projection?,
                    true,
                )
            };

        let challenger_unsafe = Self::turn_engine_root_evaluation_is_unsafe(challenger);
        let spirit_unsafe = Self::turn_engine_root_evaluation_is_unsafe(spirit);
        let risky_recovery_challenge = challenger_unsafe
            && !spirit_unsafe
            && (Self::projected_recovery_utility_competes(
                challenger_projection.plan.utility,
                spirit_projection.plan.utility,
            ) || (challenger_projection.plan.goal_family == TurnPlanFamily::ImmediateScore
                && crate::models::automove_turn_engine::compare_utility_primary_axes(
                    challenger_projection.plan.utility,
                    spirit_projection.plan.utility,
                ) != std::cmp::Ordering::Less));
        if challenger_unsafe && !spirit_unsafe && !risky_recovery_challenge {
            return None;
        }
        if risky_recovery_challenge {
            return Some(if candidate_is_challenger {
                std::cmp::Ordering::Greater
            } else {
                std::cmp::Ordering::Less
            });
        }
        if challenger_projection.plan.head_family == TurnPlanFamily::SpiritImpact {
            return None;
        }
        if !challenger_projection
            .plan
            .utility
            .passes_override_guard(spirit_projection.plan.utility)
        {
            return None;
        }
        if crate::models::automove_turn_engine::compare_utility_primary_axes(
            challenger_projection.plan.head_utility,
            spirit_projection.plan.head_utility,
        ) == std::cmp::Ordering::Less
        {
            return None;
        }
        if crate::models::automove_turn_engine::compare_utility_primary_axes(
            challenger_projection.plan.utility,
            spirit_projection.plan.utility,
        ) != std::cmp::Ordering::Greater
        {
            return None;
        }

        Some(if candidate_is_challenger {
            std::cmp::Ordering::Greater
        } else {
            std::cmp::Ordering::Less
        })
    }

    fn current_pro_spirit_score_challenge_order(
        candidate: &RootEvaluation,
        incumbent: &RootEvaluation,
    ) -> Option<std::cmp::Ordering> {
        const SCORE_CHALLENGE_MARGIN: i32 = 40;

        let candidate_plain_spirit = Self::is_plain_spirit_development_root(candidate);
        let incumbent_plain_spirit = Self::is_plain_spirit_development_root(incumbent);
        if candidate_plain_spirit == incumbent_plain_spirit {
            return None;
        }

        let (challenger, spirit, candidate_is_challenger) = if candidate_plain_spirit {
            (incumbent, candidate, false)
        } else {
            (candidate, incumbent, true)
        };

        if Self::turn_engine_root_evaluation_is_unsafe(challenger)
            || challenger.has_roundtrip
            || challenger.score < spirit.score.saturating_add(SCORE_CHALLENGE_MARGIN)
            || challenger.same_turn_score_window_value < spirit.same_turn_score_window_value
        {
            return None;
        }

        Some(if candidate_is_challenger {
            std::cmp::Ordering::Greater
        } else {
            std::cmp::Ordering::Less
        })
    }

    fn projection_competes_with_spirit_pref(
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> bool {
        if !config.enable_turn_engine_selector
            || !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
        {
            return false;
        }

        let projections = Self::turn_engine_spirit_root_projections(
            scored_roots,
            candidate_indices,
            perspective,
            config,
        );
        if projections.len() < 2 {
            return false;
        }

        let mut spirit_indices = candidate_indices
            .iter()
            .copied()
            .filter(|index| {
                Self::is_plain_spirit_development_root(&scored_roots[*index])
                    && projections.contains_key(index)
            })
            .collect::<Vec<_>>();
        if spirit_indices.is_empty() {
            return false;
        }
        spirit_indices
            .sort_by(|a, b| Self::compare_ranked_scored_root_indices(scored_roots, *a, *b));
        let best_spirit_index = spirit_indices[0];
        let best_spirit = &scored_roots[best_spirit_index];
        let best_projection = projections.get(&best_spirit_index);

        candidate_indices.iter().copied().any(|index| {
            if index == best_spirit_index {
                return false;
            }
            let candidate = &scored_roots[index];
            let Some(candidate_projection) = projections.get(&index) else {
                return false;
            };
            let safe_challenger =
                Self::can_challenge_spirit_preference_root(candidate, perspective);
            let risky_recovery_challenger =
                Self::can_challenge_spirit_preference_root_with_recovery_projection(
                    candidate,
                    perspective,
                ) && candidate_projection.plan.head_family == TurnPlanFamily::DrainerSafetyRecovery;
            if !safe_challenger && !risky_recovery_challenger {
                return false;
            }
            if (candidate.score - best_spirit.score).abs() > 320 {
                return false;
            }
            matches!(
                Self::current_pro_spirit_projection_challenge_order(
                    candidate,
                    Some(candidate_projection),
                    best_spirit,
                    best_projection,
                ),
                Some(std::cmp::Ordering::Greater)
            )
        })
    }

    fn score_competes_with_spirit_pref(
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        mode: TurnEngineMode,
    ) -> bool {
        if !matches!(mode, TurnEngineMode::CurrentPro) {
            return false;
        }

        let mut spirit_indices = candidate_indices
            .iter()
            .copied()
            .filter(|index| Self::is_plain_spirit_development_root(&scored_roots[*index]))
            .collect::<Vec<_>>();
        if spirit_indices.is_empty() {
            return false;
        }
        spirit_indices
            .sort_by(|a, b| Self::compare_ranked_scored_root_indices(scored_roots, *a, *b));
        spirit_indices.truncate(3);

        candidate_indices.iter().copied().any(|index| {
            if spirit_indices.contains(&index) {
                return false;
            }
            let root = &scored_roots[index];
            if !Self::has_non_spirit_score_competition_surface(root) {
                return false;
            }
            spirit_indices.iter().all(|spirit_index| {
                matches!(
                    Self::current_pro_spirit_score_challenge_order(
                        root,
                        &scored_roots[*spirit_index],
                    ),
                    Some(std::cmp::Ordering::Greater)
                )
            })
        })
    }

    fn negative_deny_competes_with_spirit_pref(
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> bool {
        if !config.enable_turn_engine_selector
            || !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
        {
            return false;
        }

        let projections = Self::turn_engine_spirit_root_projections(
            scored_roots,
            candidate_indices,
            perspective,
            config,
        );
        let mut spirit_indices = candidate_indices
            .iter()
            .copied()
            .filter(|index| {
                Self::is_plain_spirit_development_root(&scored_roots[*index])
                    && projections.contains_key(index)
            })
            .collect::<Vec<_>>();
        if spirit_indices.is_empty() {
            return false;
        }
        spirit_indices
            .sort_by(|a, b| Self::compare_ranked_scored_root_indices(scored_roots, *a, *b));
        spirit_indices.truncate(3);

        if spirit_indices.iter().any(|index| {
            projections
                .get(index)
                .is_some_and(|projection| projection.plan.utility.has_nonnegative_deny_gain())
        }) {
            return false;
        }

        let best_spirit_score = spirit_indices
            .iter()
            .map(|index| scored_roots[*index].score)
            .max()
            .unwrap_or(i32::MIN);

        candidate_indices.iter().copied().any(|index| {
            if spirit_indices.contains(&index) {
                return false;
            }
            let root = &scored_roots[index];
            !root.spirit_development
                && !root.spirit_same_turn_score_setup_now
                && !root.spirit_own_mana_setup_now
                && !root.own_drainer_vulnerable
                && !root.mana_handoff_to_opponent
                && !root.has_roundtrip
                && root.score >= best_spirit_score
        })
    }

    fn safe_progress_competes_with_spirit_pref(
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        mode: TurnEngineMode,
    ) -> bool {
        if !matches!(mode, TurnEngineMode::CurrentPro) {
            return false;
        }

        let best_spirit_efficiency = candidate_indices
            .iter()
            .copied()
            .filter_map(|index| {
                let root = &scored_roots[index];
                (root.spirit_development && !root.mana_handoff_to_opponent)
                    .then_some(root.efficiency)
            })
            .max();

        let Some(best_spirit_efficiency) = best_spirit_efficiency else {
            return false;
        };
        candidate_indices.iter().copied().any(|index| {
            let root = &scored_roots[index];
            !root.own_drainer_vulnerable
                && !root.mana_handoff_to_opponent
                && !root.has_roundtrip
                && (root.supermana_progress || root.opponent_mana_progress)
                && root.interview_soft_priority > 0
                && root.efficiency >= best_spirit_efficiency
        })
    }

    fn followup_progress_competes_with_spirit_pref(
        _game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> bool {
        if !Self::current_pro_secondary_analysis_live(config) || candidate_indices.len() < 2 {
            return false;
        }

        let mut spirit_indices = candidate_indices
            .iter()
            .copied()
            .filter(|index| {
                let root = &scored_roots[*index];
                Self::is_plain_spirit_development_root(root)
                    && !Self::turn_engine_root_evaluation_is_unsafe(root)
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
            })
            .collect::<Vec<_>>();
        if spirit_indices.is_empty() {
            return false;
        }
        spirit_indices
            .sort_by(|a, b| Self::compare_ranked_scored_root_indices(scored_roots, *a, *b));
        spirit_indices.truncate(3);

        let best_spirit_rank = spirit_indices
            .iter()
            .map(|index| scored_roots[*index].root_rank)
            .min()
            .unwrap_or(usize::MAX);
        let best_spirit_score = spirit_indices
            .iter()
            .map(|index| scored_roots[*index].score)
            .max()
            .unwrap_or(i32::MIN);
        let mut followup_scores = std::collections::HashMap::new();
        let best_spirit_followup = spirit_indices
            .iter()
            .copied()
            .map(|index| {
                *followup_scores.entry(index).or_insert_with(|| {
                    Self::current_pro_spirit_followup_floor_score(
                        &scored_roots[index].game,
                        perspective,
                        config,
                    )
                })
            })
            .max()
            .unwrap_or(i32::MIN);

        const ROOT_RANK_MARGIN: usize = 2;
        const SCORE_MARGIN: i32 = 32;
        const FOLLOWUP_MARGIN: i32 = 32;

        candidate_indices.iter().copied().any(|index| {
            if spirit_indices.contains(&index) {
                return false;
            }
            let root = &scored_roots[index];
            if Self::turn_engine_root_evaluation_is_unsafe(root)
                || root.spirit_development
                || root.spirit_same_turn_score_setup_now
                || root.spirit_own_mana_setup_now
                || root.own_drainer_vulnerable
                || root.mana_handoff_to_opponent
                || root.has_roundtrip
                || root.root_rank > best_spirit_rank.saturating_add(ROOT_RANK_MARGIN)
                || root.score.saturating_add(SCORE_MARGIN) < best_spirit_score
                || root.interview_soft_priority <= 0
            {
                return false;
            }

            let followup = *followup_scores.entry(index).or_insert_with(|| {
                Self::current_pro_spirit_followup_floor_score(&root.game, perspective, config)
            });
            followup >= best_spirit_followup.saturating_add(FOLLOWUP_MARGIN)
        })
    }

    fn current_pro_safe_progress_reentry_after_safety_prefilter(
        _game: &MonsGame,
        scored_roots: &[RootEvaluation],
        pre_safety_candidate_indices: &[usize],
        kept_indices: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !Self::current_pro_secondary_analysis_live(config)
            || kept_indices.is_empty()
            || !kept_indices.iter().all(|index| {
                let root = &scored_roots[*index];
                Self::is_plain_spirit_development_root(root)
                    && !Self::turn_engine_root_evaluation_is_unsafe(root)
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
            })
        {
            return None;
        }

        let best_kept_rank = kept_indices
            .iter()
            .map(|index| scored_roots[*index].root_rank)
            .min()
            .unwrap_or(usize::MAX);
        let best_kept_score = kept_indices
            .iter()
            .map(|index| scored_roots[*index].score)
            .max()
            .unwrap_or(i32::MIN);
        let mut followup_scores = std::collections::HashMap::new();
        let best_kept_followup = kept_indices
            .iter()
            .copied()
            .map(|index| {
                *followup_scores.entry(index).or_insert_with(|| {
                    Self::current_pro_spirit_followup_floor_score(
                        &scored_roots[index].game,
                        perspective,
                        config,
                    )
                })
            })
            .max()
            .unwrap_or(i32::MIN);

        const ROOT_RANK_MARGIN: usize = 2;
        const SCORE_MARGIN: i32 = 32;
        const FOLLOWUP_MARGIN: i32 = 32;

        pre_safety_candidate_indices
            .iter()
            .copied()
            .filter(|index| !kept_indices.contains(index))
            .filter(|index| {
                let root = &scored_roots[*index];
                !Self::turn_engine_root_evaluation_is_unsafe(root)
                    && !root.own_drainer_vulnerable
                    && !root.own_drainer_walk_vulnerable
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
                    && !root.spirit_development
                    && !root.spirit_same_turn_score_setup_now
                    && !root.spirit_own_mana_setup_now
                    && root.interview_soft_priority > 0
                    && root.root_rank <= best_kept_rank.saturating_add(ROOT_RANK_MARGIN)
                    && root.score.saturating_add(SCORE_MARGIN) >= best_kept_score
            })
            .filter_map(|index| {
                let root = &scored_roots[index];
                let followup = *followup_scores.entry(index).or_insert_with(|| {
                    Self::current_pro_spirit_followup_floor_score(&root.game, perspective, config)
                });
                (followup >= best_kept_followup.saturating_add(FOLLOWUP_MARGIN))
                    .then_some((index, followup))
            })
            .max_by(
                |(left_index, left_followup), (right_index, right_followup)| {
                    left_followup.cmp(right_followup).then_with(|| {
                        Self::compare_ranked_scored_root_indices(
                            scored_roots,
                            *right_index,
                            *left_index,
                        )
                    })
                },
            )
            .map(|(index, _)| index)
    }

    fn current_pro_plain_spirit_cluster_progress_reentry(
        _game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !Self::current_pro_secondary_analysis_live(config)
            || candidate_indices.len() < 2
            || !candidate_indices.iter().all(|index| {
                let root = &scored_roots[*index];
                Self::is_plain_spirit_development_root(root)
                    && !Self::turn_engine_root_evaluation_is_unsafe(root)
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
            })
        {
            return None;
        }

        let best_candidate_score = candidate_indices
            .iter()
            .map(|index| scored_roots[*index].score)
            .max()
            .unwrap_or(i32::MIN);
        let best_candidate_rank = candidate_indices
            .iter()
            .map(|index| scored_roots[*index].root_rank)
            .min()
            .unwrap_or(usize::MAX);
        let mut followup_scores = std::collections::HashMap::new();
        let best_candidate_followup = candidate_indices
            .iter()
            .copied()
            .map(|index| {
                *followup_scores.entry(index).or_insert_with(|| {
                    Self::current_pro_spirit_followup_floor_score(
                        &scored_roots[index].game,
                        perspective,
                        config,
                    )
                })
            })
            .max()
            .unwrap_or(i32::MIN);

        const SCORE_MARGIN: i32 = 32;
        const FOLLOWUP_MARGIN: i32 = 32;
        const ROOT_RANK_ADVANTAGE: usize = 2;

        let followup_reentry = scored_roots
            .iter()
            .enumerate()
            .map(|(index, _)| index)
            .filter(|index| !candidate_indices.contains(index))
            .filter(|index| {
                let root = &scored_roots[*index];
                !Self::turn_engine_root_evaluation_is_unsafe(root)
                    && !root.own_drainer_vulnerable
                    && !root.own_drainer_walk_vulnerable
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
                    && !root.spirit_development
                    && !root.spirit_same_turn_score_setup_now
                    && !root.spirit_own_mana_setup_now
                    && root.score.saturating_add(SCORE_MARGIN) >= best_candidate_score
            })
            .filter_map(|index| {
                let root = &scored_roots[index];
                let followup = *followup_scores.entry(index).or_insert_with(|| {
                    Self::current_pro_spirit_followup_floor_score(&root.game, perspective, config)
                });
                (followup >= best_candidate_followup.saturating_add(FOLLOWUP_MARGIN))
                    .then_some((index, followup))
            })
            .max_by(
                |(left_index, left_followup), (right_index, right_followup)| {
                    left_followup.cmp(right_followup).then_with(|| {
                        Self::compare_ranked_scored_root_indices(
                            scored_roots,
                            *right_index,
                            *left_index,
                        )
                    })
                },
            )
            .map(|(index, _)| index);
        if followup_reentry.is_some() {
            return followup_reentry;
        }

        scored_roots
            .iter()
            .enumerate()
            .map(|(index, _)| index)
            .filter(|index| !candidate_indices.contains(index))
            .filter(|index| {
                let root = &scored_roots[*index];
                !Self::turn_engine_root_evaluation_is_unsafe(root)
                    && !root.own_drainer_vulnerable
                    && !root.own_drainer_walk_vulnerable
                    && !root.mana_handoff_to_opponent
                    && !root.has_roundtrip
                    && !root.spirit_development
                    && !root.spirit_same_turn_score_setup_now
                    && !root.spirit_own_mana_setup_now
                    && Self::turn_engine_root_evaluation_has_progress_surface(root)
                    && root.interview_soft_priority > 0
                    && root.score >= best_candidate_score
                    && root.root_rank.saturating_add(ROOT_RANK_ADVANTAGE) <= best_candidate_rank
            })
            .min_by(|left, right| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })
    }

    fn current_pro_plain_spirit_cluster_risky_recovery_reentry(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !Self::current_pro_turn_engine_live(config) {
            return None;
        }

        let safe_progress_anchor = candidate_indices.iter().copied().find(|index| {
            let root = &scored_roots[*index];
            !Self::turn_engine_root_evaluation_is_unsafe(root)
                && Self::turn_engine_root_evaluation_has_progress_surface(root)
                && root.interview_soft_priority > 0
                && !root.mana_handoff_to_opponent
                && !root.has_roundtrip
        })?;
        let anchor = &scored_roots[safe_progress_anchor];

        let mut best_candidate = None;
        for (index, root) in scored_roots.iter().enumerate() {
            if candidate_indices.contains(&index)
                || !Self::turn_engine_root_evaluation_is_unsafe(root)
                || root.own_drainer_walk_vulnerable
                || root.mana_handoff_to_opponent
                || root.has_roundtrip
                || !Self::is_same_non_tactical_progress_lane_root_pair(root, anchor)
                || root.score + 32 < anchor.score
            {
                continue;
            }
            let Some(utility) =
                Self::projected_risky_recovery_root_utility(game, root, perspective, config)
            else {
                continue;
            };
            let is_better = match best_candidate {
                None => true,
                Some((best_index, best_utility)) => {
                    crate::models::automove_turn_engine::compare_utility_primary_axes(
                        utility,
                        best_utility,
                    ) == std::cmp::Ordering::Greater
                        || (utility == best_utility
                            && Self::compare_ranked_scored_root_indices(
                                scored_roots,
                                index,
                                best_index,
                            ) == std::cmp::Ordering::Less)
                }
            };
            if is_better {
                best_candidate = Some((index, utility));
            }
        }

        best_candidate.map(|(index, _)| index)
    }

    fn has_non_spirit_score_competition_surface(root: &RootEvaluation) -> bool {
        root.wins_immediately
            || root.attacks_opponent_drainer
            || root.scores_supermana_this_turn
            || root.scores_opponent_mana_this_turn
            || root.safe_supermana_pickup_now
            || root.safe_opponent_mana_pickup_now
            || root.same_turn_score_window_value > 0
    }

    fn risky_score_competes_with_spirit_pref(
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        mode: TurnEngineMode,
    ) -> bool {
        if !matches!(mode, TurnEngineMode::CurrentPro) {
            return false;
        }

        let best_spirit_score = candidate_indices
            .iter()
            .copied()
            .filter_map(|index| {
                let root = &scored_roots[index];
                (root.spirit_development
                    || root.spirit_same_turn_score_setup_now
                    || root.spirit_own_mana_setup_now)
                    .then_some(root.score)
            })
            .max();
        let Some(best_spirit_score) = best_spirit_score else {
            return false;
        };

        candidate_indices.iter().copied().any(|index| {
            let root = &scored_roots[index];
            !root.spirit_development
                && !root.spirit_same_turn_score_setup_now
                && !root.spirit_own_mana_setup_now
                && root.own_drainer_vulnerable
                && !root.own_drainer_walk_vulnerable
                && !root.mana_handoff_to_opponent
                && !root.has_roundtrip
                && Self::has_non_spirit_score_competition_surface(root)
                && root.score >= best_spirit_score
        })
    }

    fn risky_recovery_competes_with_spirit_pref(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> bool {
        if !config.enable_turn_engine_selector
            || !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
        {
            return false;
        }

        let mut spirit_indices = candidate_indices
            .iter()
            .copied()
            .filter(|index| {
                let root = &scored_roots[*index];
                root.spirit_development
                    || root.spirit_same_turn_score_setup_now
                    || root.spirit_own_mana_setup_now
            })
            .collect::<Vec<_>>();
        if spirit_indices.is_empty() {
            return false;
        }
        spirit_indices
            .sort_by(|a, b| Self::compare_ranked_scored_root_indices(scored_roots, *a, *b));
        spirit_indices.truncate(3);

        let best_spirit_score = spirit_indices
            .iter()
            .map(|index| scored_roots[*index].score)
            .max()
            .unwrap_or(i32::MIN);
        let spirit_utilities = spirit_indices
            .iter()
            .map(|index| {
                let root = &scored_roots[*index];
                let family = Self::turn_engine_root_evaluation_family(root);
                Self::turn_engine_selected_override_utility(game, root, perspective, config, family)
            })
            .collect::<Vec<_>>();
        if spirit_utilities.is_empty() {
            return false;
        }

        candidate_indices.iter().copied().any(|index| {
            let root = &scored_roots[index];
            if root.spirit_development
                || root.spirit_same_turn_score_setup_now
                || root.spirit_own_mana_setup_now
                || !root.own_drainer_vulnerable
                || root.mana_handoff_to_opponent
                || root.has_roundtrip
                || root.score < best_spirit_score
            {
                return false;
            }
            let Some(candidate_utility) =
                Self::projected_risky_recovery_root_utility(game, root, perspective, config)
            else {
                return false;
            };
            spirit_utilities.iter().all(|spirit_utility| {
                Self::projected_recovery_utility_competes(candidate_utility, *spirit_utility)
            })
        })
    }

    fn turn_engine_spirit_root_projections(
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> std::collections::HashMap<usize, TurnEngineRootProjection> {
        if !Self::current_pro_secondary_analysis_live(config) {
            return std::collections::HashMap::new();
        }

        let mut spirit_shortlist = candidate_indices
            .iter()
            .copied()
            .filter(|index| {
                let root = &scored_roots[*index];
                Self::is_plain_spirit_development_root(root)
                    && Self::can_turn_engine_project_root(root, perspective)
            })
            .collect::<Vec<_>>();
        spirit_shortlist
            .sort_by(|a, b| Self::compare_ranked_scored_root_indices(scored_roots, *a, *b));
        spirit_shortlist.truncate(if config.enable_turn_engine_low_budget_guard {
            4
        } else {
            6
        });

        let mut challenger_shortlist = candidate_indices
            .iter()
            .copied()
            .filter(|index| {
                let root = &scored_roots[*index];
                Self::can_challenge_spirit_preference_root(root, perspective)
                    || Self::can_challenge_spirit_preference_root_with_recovery_projection(
                        root,
                        perspective,
                    )
            })
            .collect::<Vec<_>>();
        challenger_shortlist
            .sort_by(|a, b| Self::compare_ranked_scored_root_indices(scored_roots, *a, *b));
        challenger_shortlist.truncate(if config.enable_turn_engine_low_budget_guard {
            2
        } else {
            4
        });

        let mut shortlist = spirit_shortlist;
        for index in challenger_shortlist {
            if !shortlist.contains(&index) {
                shortlist.push(index);
            }
        }
        if shortlist.len() < 2 {
            return std::collections::HashMap::new();
        }

        let rerank_engine_config = Self::turn_engine_rerank_config(config);
        let full_engine_config = Self::turn_engine_config_from_search_config(config);
        let mut projections = std::collections::HashMap::new();
        #[cfg(test)]
        update_turn_engine_selector_diagnostics(|diagnostics| {
            diagnostics.spirit_projection_passes += 1;
            diagnostics.spirit_projection_root_count += shortlist.len();
        });
        for index in shortlist {
            let root = &scored_roots[index];
            let risky_recovery_challenger =
                Self::can_challenge_spirit_preference_root_with_recovery_projection(
                    root,
                    perspective,
                ) && !Self::can_challenge_spirit_preference_root(root, perspective);
            let engine_config = if risky_recovery_challenger {
                full_engine_config
            } else {
                rerank_engine_config
            };
            #[cfg(test)]
            update_turn_engine_selector_diagnostics(|diagnostics| {
                diagnostics.spirit_projection_plan_calls += 1;
            });
            if let Some(plan) = turn_engine_candidate_plan(&root.game, perspective, engine_config) {
                #[cfg(test)]
                update_turn_engine_selector_diagnostics(|diagnostics| {
                    diagnostics.spirit_projection_plan_hits += 1;
                });
                projections.insert(index, TurnEngineRootProjection { plan });
            }
        }
        projections
    }

    fn pick_root_move_with_reply_risk_guard_from_shortlist(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        candidate_indices: Option<&[usize]>,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if shortlist.is_empty() {
            return None;
        }

        let turn_engine_projections =
            Self::turn_engine_reply_risk_projections(scored_roots, shortlist, perspective, config);

        let root_node_budget = ((config.max_visited_nodes
            * config.root_reply_risk_node_share_bp.max(0) as usize)
            / 10_000)
            .max(shortlist.len())
            .max(1);
        let per_root_reply_limit = (root_node_budget / shortlist.len().max(1))
            .max(1)
            .min(config.root_reply_risk_reply_limit.max(1));

        let shortlist_snapshots = shortlist
            .iter()
            .copied()
            .map(|index| {
                let snapshot = Self::root_reply_risk_snapshot_with_projection(
                    &scored_roots[index],
                    turn_engine_projections.get(&index),
                    perspective,
                    config,
                    per_root_reply_limit,
                );
                (index, snapshot)
            })
            .collect::<Vec<_>>();
        let mut spirit_followup_scores = std::collections::HashMap::new();

        let best_plain_spirit_index = Self::current_pro_plain_spirit_reply_risk_pick(
            game,
            scored_roots,
            shortlist,
            shortlist_snapshots.as_slice(),
            &turn_engine_projections,
            perspective,
            config,
            &mut spirit_followup_scores,
        );
        if shortlist
            .iter()
            .all(|index| Self::is_plain_spirit_development_root(&scored_roots[*index]))
        {
            return best_plain_spirit_index;
        }

        if matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && shortlist_snapshots.iter().all(|(_, snapshot)| {
                !snapshot.allows_immediate_opponent_win && !snapshot.opponent_reaches_match_point
            })
            && !Self::reply_risk_shortlist_has_safe_plain_spirit_competition(
                scored_roots,
                shortlist,
                config,
            )
            && !Self::reply_risk_shortlist_has_white_spirit_followup_setup_competition(
                game,
                scored_roots,
                shortlist,
                config,
            )
            && !Self::reply_risk_shortlist_has_safe_progress_competition(
                scored_roots,
                shortlist,
                config,
            )
            && !Self::reply_risk_shortlist_has_black_mana_window_progress_competition(
                game,
                scored_roots,
                shortlist,
                config,
            )
            && !Self::reply_risk_shortlist_has_close_positive_score_competition(
                scored_roots,
                shortlist,
                config,
            )
        {
            let mut ordered_by_reply_floor = shortlist_snapshots
                .iter()
                .map(|(index, snapshot)| (*index, snapshot.worst_reply_score))
                .collect::<Vec<_>>();
            ordered_by_reply_floor.sort_by(
                |(left_index, left_score), (right_index, right_score)| {
                    right_score.cmp(left_score).then_with(|| {
                        Self::compare_ranked_scored_root_indices(
                            scored_roots,
                            *left_index,
                            *right_index,
                        )
                    })
                },
            );
            if ordered_by_reply_floor.len() == 1
                || ordered_by_reply_floor[0].1 > ordered_by_reply_floor[1].1
            {
                return Some(ordered_by_reply_floor[0].0);
            }
        }

        let (mut best_index, mut best_snapshot) = best_plain_spirit_index
            .and_then(|index| {
                shortlist_snapshots
                    .iter()
                    .copied()
                    .find(|(candidate_index, _)| *candidate_index == index)
            })
            .unwrap_or(shortlist_snapshots[0]);
        for (index, snapshot) in shortlist_snapshots.iter().copied() {
            if index == best_index {
                continue;
            }
            if Self::is_better_reply_risk_candidate(
                game,
                index,
                snapshot,
                best_index,
                best_snapshot,
                turn_engine_projections.get(&index),
                turn_engine_projections.get(&best_index),
                scored_roots,
                perspective,
                config,
                &mut spirit_followup_scores,
            ) {
                best_index = index;
                best_snapshot = snapshot;
            }
        }

        if matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) {
            if let Some(plain_spirit_index) = best_plain_spirit_index {
                if plain_spirit_index != best_index {
                    if let Some((_, plain_spirit_snapshot)) = shortlist_snapshots
                        .iter()
                        .copied()
                        .find(|(index, _)| *index == plain_spirit_index)
                    {
                        if let (Some(plain_spirit_projection), Some(best_projection)) = (
                            turn_engine_projections.get(&plain_spirit_index),
                            turn_engine_projections.get(&best_index),
                        ) {
                            if let Some(order) =
                                Self::current_pro_mixed_plain_spirit_reply_floor_order(
                                    plain_spirit_snapshot,
                                    plain_spirit_projection,
                                    best_snapshot,
                                    best_projection,
                                    config,
                                )
                            {
                                if order == std::cmp::Ordering::Greater {
                                    best_index = plain_spirit_index;
                                    best_snapshot = plain_spirit_snapshot;
                                }
                            }
                        }
                    }
                }
            }
        }

        let best_root = &scored_roots[best_index];
        if let Some(candidate_indices) = candidate_indices {
            if matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && !best_root.spirit_development
                && !best_root.spirit_same_turn_score_setup_now
                && !best_root.spirit_own_mana_setup_now
                && best_index >= 4
            {
                let omitted_setup_index = candidate_indices
                    .iter()
                    .copied()
                    .filter(|index| !shortlist.contains(index))
                    .filter(|index| scored_roots[*index].spirit_own_mana_setup_now)
                    .filter(|index| {
                        scored_roots[*index].root_rank.saturating_add(3) <= best_root.root_rank
                    })
                    .filter(|index| {
                        Self::is_same_opening_safe_setup_pair(
                            &scored_roots[*index],
                            best_root,
                            config,
                        )
                    })
                    .min_by(|left, right| {
                        Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
                    });
                if let Some(index) = omitted_setup_index {
                    let snapshot = Self::root_reply_risk_snapshot_with_projection(
                        &scored_roots[index],
                        None,
                        perspective,
                        config,
                        per_root_reply_limit,
                    );
                    let force_same_opening_setup =
                        Self::current_pro_omitted_same_opening_setup_competes(
                            &scored_roots[index],
                            snapshot,
                            best_root,
                            best_snapshot,
                            config,
                        );
                    if force_same_opening_setup
                        || Self::is_better_reply_risk_candidate(
                            game,
                            index,
                            snapshot,
                            best_index,
                            best_snapshot,
                            None,
                            turn_engine_projections.get(&best_index),
                            scored_roots,
                            perspective,
                            config,
                            &mut spirit_followup_scores,
                        )
                    {
                        best_index = index;
                    }
                }
            }
        }
        Some(best_index)
    }

    fn pick_root_move_with_reply_risk_guard(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        let shortlist =
            Self::reply_risk_guard_shortlist_indices(scored_roots, candidate_indices, config);
        Self::pick_root_move_with_reply_risk_guard_from_shortlist(
            game,
            scored_roots,
            shortlist.as_slice(),
            Some(candidate_indices),
            perspective,
            config,
        )
    }

    fn reply_risk_guard_shortlist_indices(
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        config: AutomoveSearchConfig,
    ) -> Vec<usize> {
        if candidate_indices.is_empty() {
            return Vec::new();
        }

        let best_score = candidate_indices
            .iter()
            .map(|index| scored_roots[*index].score)
            .max()
            .unwrap_or(i32::MIN);
        let worst_score = candidate_indices
            .iter()
            .map(|index| scored_roots[*index].score)
            .min()
            .unwrap_or(i32::MIN);
        let has_winning_candidate = candidate_indices
            .iter()
            .any(|index| scored_roots[*index].wins_immediately);
        if has_winning_candidate
            && best_score.saturating_sub(worst_score) > SMART_ROOT_REPLY_RISK_WINNER_SPREAD_SKIP
        {
            return Vec::new();
        }

        let score_margin = config.root_reply_risk_score_margin.max(0);
        let mut shortlist = candidate_indices
            .iter()
            .copied()
            .filter(|index| scored_roots[*index].score + score_margin >= best_score)
            .collect::<Vec<_>>();
        shortlist.sort_by(|a, b| Self::compare_ranked_scored_root_indices(scored_roots, *a, *b));
        if shortlist.is_empty() {
            return shortlist;
        }

        let shortlist_limit = config.root_reply_risk_shortlist_max.max(1);
        if shortlist.len() > shortlist_limit {
            if matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) {
                let truncated_has_spirit_contender =
                    shortlist.iter().take(shortlist_limit).any(|index| {
                        let root = &scored_roots[*index];
                        root.spirit_development
                            || root.spirit_same_turn_score_setup_now
                            || root.spirit_own_mana_setup_now
                    });
                let truncated_has_plain_spirit_contender = shortlist
                    .iter()
                    .take(shortlist_limit)
                    .any(|index| Self::is_plain_spirit_development_root(&scored_roots[*index]));
                let best_shortlist_score = shortlist
                    .first()
                    .map(|index| scored_roots[*index].score)
                    .unwrap_or(best_score);
                let mut extra_shortlist = shortlist
                    .iter()
                    .copied()
                    .skip(shortlist_limit)
                    .filter(|index| {
                        let root = &scored_roots[*index];
                        !truncated_has_spirit_contender
                            && best_shortlist_score.saturating_sub(root.score) <= 64
                            && (root.spirit_development
                                || root.spirit_same_turn_score_setup_now
                                || root.spirit_own_mana_setup_now)
                    })
                    .take(1)
                    .collect::<Vec<_>>();
                let has_plain_spirit_anchor = truncated_has_plain_spirit_contender
                    || extra_shortlist
                        .iter()
                        .any(|index| Self::is_plain_spirit_development_root(&scored_roots[*index]));
                if has_plain_spirit_anchor {
                    let plain_spirit_siblings = shortlist
                        .iter()
                        .copied()
                        .skip(shortlist_limit)
                        .filter(|index| {
                            let root = &scored_roots[*index];
                            Self::is_plain_spirit_development_root(root)
                                && best_shortlist_score.saturating_sub(root.score) <= 64
                        })
                        .filter(|index| !extra_shortlist.contains(index))
                        .take(2)
                        .collect::<Vec<_>>();
                    extra_shortlist.extend(plain_spirit_siblings);
                }
                shortlist.truncate(shortlist_limit);
                shortlist.extend(extra_shortlist);
            } else {
                shortlist.truncate(shortlist_limit);
            }
        }
        if let Some(extra_index) = Self::current_pro_safe_progress_sibling_shortlist_extension(
            scored_roots,
            candidate_indices,
            shortlist.as_slice(),
            config,
        ) {
            shortlist.push(extra_index);
            shortlist
                .sort_by(|a, b| Self::compare_ranked_scored_root_indices(scored_roots, *a, *b));
        }
        shortlist
    }

    fn turn_engine_reply_risk_projections(
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> std::collections::HashMap<usize, TurnEngineRootProjection> {
        let allow_safe_plain_spirit_projection = Self::current_pro_secondary_analysis_live(config)
            && Self::reply_risk_shortlist_has_safe_plain_spirit_competition(
                scored_roots,
                shortlist,
                config,
            );
        if (!Self::current_pro_secondary_analysis_live(config)
            && !allow_safe_plain_spirit_projection)
            || shortlist.len() < 2
        {
            return std::collections::HashMap::new();
        }
        let has_tactical_window = shortlist.first().is_some_and(|index| {
            let root = &scored_roots[*index];
            root.wins_immediately
                || root.attacks_opponent_drainer
                || root.scores_supermana_this_turn
                || root.scores_opponent_mana_this_turn
        });
        let has_spirit_development_window =
            matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                && shortlist.iter().copied().any(|index| {
                    let root = &scored_roots[index];
                    root.spirit_development
                        && !root.spirit_same_turn_score_setup_now
                        && !root.spirit_own_mana_setup_now
                        && root.game.active_color == perspective
                        && root.game.winner_color().is_none()
                });
        if !has_tactical_window && !has_spirit_development_window {
            return std::collections::HashMap::new();
        }

        let projection_limit = if has_spirit_development_window {
            if allow_safe_plain_spirit_projection {
                if config.enable_turn_engine_low_budget_guard {
                    shortlist.len().min(4)
                } else {
                    shortlist.len()
                }
            } else {
                shortlist
                    .len()
                    .min(if config.enable_turn_engine_low_budget_guard {
                        4
                    } else {
                        8
                    })
            }
        } else {
            shortlist
                .len()
                .min(if config.enable_turn_engine_low_budget_guard {
                    3
                } else {
                    6
                })
        };
        let rerank_engine_config = Self::turn_engine_rerank_config(config);
        let full_engine_config = Self::turn_engine_config_from_search_config(config);
        let mut projections = std::collections::HashMap::new();
        #[cfg(test)]
        update_turn_engine_selector_diagnostics(|diagnostics| {
            diagnostics.reply_projection_passes += 1;
        });
        for index in shortlist.iter().copied().take(projection_limit) {
            let root = &scored_roots[index];
            if root.game.active_color != perspective || root.game.winner_color().is_some() {
                continue;
            }
            #[cfg(test)]
            update_turn_engine_selector_diagnostics(|diagnostics| {
                diagnostics.reply_projection_root_count += 1;
                diagnostics.reply_projection_plan_calls += 1;
            });
            let current_pro = matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro);
            let vulnerable_recovery_projection = current_pro
                && root.own_drainer_vulnerable
                && !root.mana_handoff_to_opponent
                && !root.has_roundtrip;
            let engine_config = if vulnerable_recovery_projection {
                full_engine_config
            } else {
                rerank_engine_config
            };
            if let Some(plan) = turn_engine_candidate_plan(&root.game, perspective, engine_config) {
                #[cfg(test)]
                update_turn_engine_selector_diagnostics(|diagnostics| {
                    diagnostics.reply_projection_plan_hits += 1;
                });
                projections.insert(index, TurnEngineRootProjection { plan });
            }
        }
        let allow_safe_plain_spirit_projection_keep =
            Self::reply_risk_shortlist_has_safe_plain_spirit_competition(
                scored_roots,
                shortlist,
                config,
            );
        if matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && has_spirit_development_window
            && !has_tactical_window
            && !allow_safe_plain_spirit_projection_keep
            && !projections.values().any(|projection| {
                Self::is_informative_reply_risk_projection_family(projection.plan.head_family)
            })
        {
            return std::collections::HashMap::new();
        }
        projections
    }

    #[allow(clippy::too_many_arguments)]
    fn current_pro_plain_spirit_reply_risk_pick(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        shortlist_snapshots: &[(usize, RootReplyRiskSnapshot)],
        turn_engine_projections: &std::collections::HashMap<usize, TurnEngineRootProjection>,
        perspective: Color,
        config: AutomoveSearchConfig,
        spirit_followup_scores: &mut std::collections::HashMap<usize, i32>,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) || shortlist.len() < 2 {
            return None;
        }

        let spirit_shortlist = shortlist
            .iter()
            .copied()
            .filter(|index| Self::is_plain_spirit_development_root(&scored_roots[*index]))
            .collect::<Vec<_>>();
        if spirit_shortlist.len() < 2 {
            return None;
        }
        if spirit_shortlist
            .iter()
            .any(|index| !turn_engine_projections.contains_key(index))
        {
            return None;
        }

        let mut best_index = spirit_shortlist[0];
        for index in spirit_shortlist.iter().copied().skip(1) {
            let candidate = &scored_roots[index];
            let incumbent = &scored_roots[best_index];
            let candidate_projection = turn_engine_projections
                .get(&index)
                .expect("spirit shortlist candidate projection");
            let incumbent_projection = turn_engine_projections
                .get(&best_index)
                .expect("spirit shortlist incumbent projection");
            let candidate_snapshot = shortlist_snapshots
                .iter()
                .find(|(snapshot_index, _)| *snapshot_index == index)
                .map(|(_, snapshot)| *snapshot)
                .expect("spirit shortlist candidate snapshot");
            let incumbent_snapshot = shortlist_snapshots
                .iter()
                .find(|(snapshot_index, _)| *snapshot_index == best_index)
                .map(|(_, snapshot)| *snapshot)
                .expect("spirit shortlist incumbent snapshot");

            let candidate_is_better = if let Some(order) =
                Self::current_pro_early_black_plain_spirit_sibling_reply_order(
                    game,
                    scored_roots,
                    index,
                    candidate_snapshot,
                    best_index,
                    incumbent_snapshot,
                    perspective,
                    config,
                    spirit_followup_scores,
                ) {
                order == std::cmp::Ordering::Greater
            } else if let Some(order) = Self::current_pro_mixed_plain_spirit_reply_floor_order(
                candidate_snapshot,
                candidate_projection,
                incumbent_snapshot,
                incumbent_projection,
                config,
            ) {
                order == std::cmp::Ordering::Greater
            } else if let Some(order) = Self::current_pro_mixed_plain_spirit_projection_order(
                game,
                scored_roots,
                index,
                candidate_projection,
                best_index,
                incumbent_projection,
                perspective,
                config,
                spirit_followup_scores,
            ) {
                order == std::cmp::Ordering::Greater
            } else {
                let candidate_spirit_impact = matches!(
                    candidate_projection.plan.head_family,
                    TurnPlanFamily::SpiritImpact
                );
                let incumbent_spirit_impact = matches!(
                    incumbent_projection.plan.head_family,
                    TurnPlanFamily::SpiritImpact
                );
                if candidate_spirit_impact && incumbent_spirit_impact {
                    let close_spirit_roots = (candidate.score - incumbent.score).abs() <= 192;
                    Self::compare_spirit_root_projection_plans(
                        candidate_projection,
                        incumbent_projection,
                        close_spirit_roots,
                    ) == std::cmp::Ordering::Greater
                } else if let Some(order) = Self::current_pro_spirit_followup_floor_order(
                    game,
                    scored_roots,
                    index,
                    best_index,
                    perspective,
                    config,
                    spirit_followup_scores,
                ) {
                    order == std::cmp::Ordering::Greater
                } else {
                    Self::compare_ranked_scored_root_indices(scored_roots, index, best_index)
                        == std::cmp::Ordering::Less
                }
            };

            if candidate_is_better {
                best_index = index;
            }
        }

        Some(best_index)
    }

    #[allow(clippy::too_many_arguments)]
    fn current_pro_mixed_plain_spirit_projection_order(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_index: usize,
        candidate_projection: &TurnEngineRootProjection,
        incumbent_index: usize,
        incumbent_projection: &TurnEngineRootProjection,
        perspective: Color,
        config: AutomoveSearchConfig,
        spirit_followup_scores: &mut std::collections::HashMap<usize, i32>,
    ) -> Option<std::cmp::Ordering> {
        if !Self::current_pro_secondary_analysis_live(config) {
            return None;
        }

        let candidate_spirit_impact = matches!(
            candidate_projection.plan.head_family,
            TurnPlanFamily::SpiritImpact
        );
        let incumbent_spirit_impact = matches!(
            incumbent_projection.plan.head_family,
            TurnPlanFamily::SpiritImpact
        );
        if candidate_spirit_impact == incumbent_spirit_impact {
            return None;
        }

        let candidate = &scored_roots[candidate_index];
        let incumbent = &scored_roots[incumbent_index];
        let (
            non_spirit_root,
            non_spirit_projection,
            spirit_root,
            spirit_projection,
            candidate_is_non_spirit,
        ) = if candidate_spirit_impact {
            (
                incumbent,
                incumbent_projection,
                candidate,
                candidate_projection,
                false,
            )
        } else {
            (
                candidate,
                candidate_projection,
                incumbent,
                incumbent_projection,
                true,
            )
        };

        let followup_order = Self::current_pro_spirit_followup_floor_order(
            game,
            scored_roots,
            candidate_index,
            incumbent_index,
            perspective,
            config,
            spirit_followup_scores,
        );
        let non_spirit_index = if candidate_is_non_spirit {
            candidate_index
        } else {
            incumbent_index
        };
        let spirit_index = if candidate_is_non_spirit {
            incumbent_index
        } else {
            candidate_index
        };
        let non_spirit_non_tactical_goal =
            Self::close_spirit_goal_family_priority(non_spirit_projection.plan.goal_family)
                >= Self::close_spirit_goal_family_priority(spirit_projection.plan.goal_family)
                && Self::close_spirit_goal_family_priority(spirit_projection.plan.goal_family)
                    <= Self::close_spirit_goal_family_priority(TurnPlanFamily::SpiritImpact);
        let non_spirit_root_competes = non_spirit_non_tactical_goal
            && non_spirit_root.score >= spirit_root.score
            && Self::compare_ranked_scored_root_indices(
                scored_roots,
                non_spirit_index,
                spirit_index,
            ) == std::cmp::Ordering::Less
            && crate::models::automove_turn_engine::compare_utility_primary_axes(
                non_spirit_projection.plan.utility,
                spirit_projection.plan.utility,
            ) != std::cmp::Ordering::Less;
        if non_spirit_root_competes {
            return Some(if candidate_is_non_spirit {
                std::cmp::Ordering::Greater
            } else {
                std::cmp::Ordering::Less
            });
        }
        let spirit_goal_competes =
            Self::close_spirit_goal_family_priority(spirit_projection.plan.goal_family)
                > Self::close_spirit_goal_family_priority(non_spirit_projection.plan.goal_family)
                && crate::models::automove_turn_engine::compare_utility_primary_axes(
                    spirit_projection.plan.utility,
                    non_spirit_projection.plan.utility,
                ) != std::cmp::Ordering::Less
                && crate::models::automove_turn_engine::compare_utility_primary_axes(
                    spirit_projection.plan.head_utility,
                    non_spirit_projection.plan.head_utility,
                ) != std::cmp::Ordering::Less;
        if spirit_goal_competes {
            return Some(if candidate_is_non_spirit {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            });
        }
        let non_spirit_competes = matches!(
            non_spirit_projection.plan.head_family,
            TurnPlanFamily::SafeSupermanaProgress
                | TurnPlanFamily::SafeOpponentManaProgress
                | TurnPlanFamily::DrainerSafetyRecovery
        ) && (matches!(followup_order, Some(std::cmp::Ordering::Equal))
            || non_spirit_projection
                .plan
                .utility
                .supports_family_fallback(spirit_projection.plan.utility))
            && non_spirit_root.score >= spirit_root.score
            && turn_engine_compare_plans(&non_spirit_projection.plan, &spirit_projection.plan)
                == std::cmp::Ordering::Greater;

        if non_spirit_competes {
            Some(if candidate_is_non_spirit {
                std::cmp::Ordering::Greater
            } else {
                std::cmp::Ordering::Less
            })
        } else if candidate_spirit_impact {
            Some(std::cmp::Ordering::Greater)
        } else {
            Some(std::cmp::Ordering::Less)
        }
    }

    fn is_tactical_turn_engine_family(family: TurnPlanFamily) -> bool {
        matches!(
            family,
            TurnPlanFamily::ImmediateScore
                | TurnPlanFamily::DenyOpponentWindow
                | TurnPlanFamily::DrainerKill
        )
    }

    fn is_informative_reply_risk_projection_family(family: TurnPlanFamily) -> bool {
        Self::is_tactical_turn_engine_family(family)
            || matches!(
                family,
                TurnPlanFamily::SpiritImpact | TurnPlanFamily::DrainerSafetyRecovery
            )
    }

    fn root_reply_risk_snapshot(
        state_after_move: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        reply_limit: usize,
    ) -> RootReplyRiskSnapshot {
        if let Some(winner) = state_after_move.winner_color() {
            return if winner == perspective {
                RootReplyRiskSnapshot {
                    allows_immediate_opponent_win: false,
                    opponent_reaches_match_point: false,
                    worst_reply_score: SMART_TERMINAL_SCORE / 2,
                }
            } else {
                RootReplyRiskSnapshot {
                    allows_immediate_opponent_win: true,
                    opponent_reaches_match_point: true,
                    worst_reply_score: -SMART_TERMINAL_SCORE / 2,
                }
            };
        }

        if state_after_move.active_color == perspective {
            return RootReplyRiskSnapshot {
                allows_immediate_opponent_win: false,
                opponent_reaches_match_point: false,
                worst_reply_score: Self::evaluate_search_preferability(
                    state_after_move,
                    perspective,
                    config,
                ),
            };
        }

        let replies = Self::enumerate_legal_transitions(
            state_after_move,
            reply_limit.max(1),
            Self::automove_start_input_options(),
        );
        if replies.is_empty() {
            return RootReplyRiskSnapshot {
                allows_immediate_opponent_win: false,
                opponent_reaches_match_point: false,
                worst_reply_score: SMART_TERMINAL_SCORE / 4,
            };
        }

        let mut allows_immediate_opponent_win = false;
        let mut opponent_reaches_match_point = false;
        let mut worst_reply_score = i32::MAX;
        let mut evaluated_reply = false;

        for reply in replies {
            let after_reply = reply.game;
            evaluated_reply = true;

            let opponent_score_after = if perspective == Color::White {
                after_reply.black_score
            } else {
                after_reply.white_score
            };
            if Config::TARGET_SCORE - opponent_score_after <= 1 {
                opponent_reaches_match_point = true;
            }

            let reply_score = match after_reply.winner_color() {
                Some(winner) if winner == perspective => SMART_TERMINAL_SCORE / 2,
                Some(_) => {
                    allows_immediate_opponent_win = true;
                    opponent_reaches_match_point = true;
                    -SMART_TERMINAL_SCORE / 2
                }
                None => Self::evaluate_search_preferability(&after_reply, perspective, config),
            };
            worst_reply_score = worst_reply_score.min(reply_score);

            if allows_immediate_opponent_win {
                break;
            }
        }

        if !evaluated_reply || worst_reply_score == i32::MAX {
            worst_reply_score =
                Self::evaluate_search_preferability(state_after_move, perspective, config);
        }

        RootReplyRiskSnapshot {
            allows_immediate_opponent_win,
            opponent_reaches_match_point,
            worst_reply_score,
        }
    }

    fn should_use_reply_risk_projection_for_root(
        root: &RootEvaluation,
        projection: &TurnEngineRootProjection,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> bool {
        if !config.enable_turn_engine_selector
            || !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || root.game.active_color != perspective
            || root.game.winner_color().is_some()
            || Self::is_plain_spirit_development_root(root)
        {
            return false;
        }

        if matches!(projection.plan.head_family, TurnPlanFamily::SpiritImpact)
            && !root.spirit_development
            && root.own_drainer_vulnerable
        {
            return false;
        }

        if Self::is_informative_reply_risk_projection_family(projection.plan.head_family) {
            return true;
        }

        matches!(
            projection.plan.head_family,
            TurnPlanFamily::SafeSupermanaProgress | TurnPlanFamily::SafeOpponentManaProgress
        ) && !Self::is_plain_spirit_development_root(root)
            && Self::turn_engine_root_evaluation_has_progress_surface(root)
    }

    fn root_reply_risk_snapshot_with_projection(
        root: &RootEvaluation,
        projection: Option<&TurnEngineRootProjection>,
        perspective: Color,
        config: AutomoveSearchConfig,
        reply_limit: usize,
    ) -> RootReplyRiskSnapshot {
        if let Some(projection) = projection.filter(|projection| {
            Self::should_use_reply_risk_projection_for_root(root, projection, perspective, config)
        }) {
            return Self::root_reply_risk_snapshot(
                &projection.plan.end_game,
                perspective,
                config,
                reply_limit,
            );
        }

        Self::root_reply_risk_snapshot(&root.game, perspective, config, reply_limit)
    }

    fn is_safe_plain_spirit_reply_risk_pair(
        candidate: &RootEvaluation,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent: &RootEvaluation,
        incumbent_snapshot: RootReplyRiskSnapshot,
        config: AutomoveSearchConfig,
    ) -> bool {
        matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && Self::is_plain_spirit_development_root(candidate)
            && Self::is_plain_spirit_development_root(incumbent)
            && !Self::turn_engine_root_evaluation_is_unsafe(candidate)
            && !Self::turn_engine_root_evaluation_is_unsafe(incumbent)
            && !candidate_snapshot.allows_immediate_opponent_win
            && !incumbent_snapshot.allows_immediate_opponent_win
            && !candidate_snapshot.opponent_reaches_match_point
            && !incumbent_snapshot.opponent_reaches_match_point
            && !candidate.mana_handoff_to_opponent
            && !incumbent.mana_handoff_to_opponent
            && !candidate.has_roundtrip
            && !incumbent.has_roundtrip
            && !candidate.wins_immediately
            && !incumbent.wins_immediately
            && !candidate.attacks_opponent_drainer
            && !incumbent.attacks_opponent_drainer
            && !candidate.scores_supermana_this_turn
            && !incumbent.scores_supermana_this_turn
            && !candidate.scores_opponent_mana_this_turn
            && !incumbent.scores_opponent_mana_this_turn
            && !candidate.safe_supermana_pickup_now
            && !incumbent.safe_supermana_pickup_now
            && !candidate.safe_opponent_mana_pickup_now
            && !incumbent.safe_opponent_mana_pickup_now
            && !candidate.supermana_progress
            && !incumbent.supermana_progress
            && !candidate.opponent_mana_progress
            && !incumbent.opponent_mana_progress
            && candidate.same_turn_score_window_value == 0
            && incumbent.same_turn_score_window_value == 0
    }

    fn reply_risk_shortlist_has_safe_plain_spirit_competition(
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        config: AutomoveSearchConfig,
    ) -> bool {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) || shortlist.len() < 2 {
            return false;
        }

        for (position, candidate_index) in shortlist.iter().copied().enumerate() {
            for incumbent_index in shortlist.iter().copied().skip(position + 1) {
                let snapshot = RootReplyRiskSnapshot {
                    allows_immediate_opponent_win: false,
                    opponent_reaches_match_point: false,
                    worst_reply_score: 0,
                };
                if Self::is_safe_plain_spirit_reply_risk_pair(
                    &scored_roots[candidate_index],
                    snapshot,
                    &scored_roots[incumbent_index],
                    snapshot,
                    config,
                ) {
                    return true;
                }
            }
        }

        false
    }

    fn reply_risk_shortlist_has_safe_progress_competition(
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        config: AutomoveSearchConfig,
    ) -> bool {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) || shortlist.len() < 2 {
            return false;
        }

        for (position, candidate_index) in shortlist.iter().copied().enumerate() {
            for incumbent_index in shortlist.iter().copied().skip(position + 1) {
                let candidate = &scored_roots[candidate_index];
                let incumbent = &scored_roots[incumbent_index];
                if !Self::is_same_non_tactical_progress_lane_root_pair(candidate, incumbent) {
                    continue;
                }
                let candidate_safe = !Self::turn_engine_root_evaluation_is_unsafe(candidate);
                let incumbent_safe = !Self::turn_engine_root_evaluation_is_unsafe(incumbent);
                if candidate_safe != incumbent_safe {
                    return true;
                }
            }
        }

        false
    }

    fn reply_risk_shortlist_has_white_spirit_followup_setup_competition(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        config: AutomoveSearchConfig,
    ) -> bool {
        if shortlist.len() < 2 {
            return false;
        }

        for (position, candidate_index) in shortlist.iter().copied().enumerate() {
            for incumbent_index in shortlist.iter().copied().skip(position + 1) {
                if Self::is_current_pro_white_spirit_followup_setup_pair(
                    game,
                    &scored_roots[candidate_index],
                    &scored_roots[incumbent_index],
                    config,
                ) {
                    return true;
                }
            }
        }

        false
    }

    fn reply_risk_shortlist_has_black_mana_window_progress_competition(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        config: AutomoveSearchConfig,
    ) -> bool {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || shortlist.len() < 2
            || game.active_color != Color::Black
            || game.turn_number > 4
        {
            return false;
        }

        const WINDOW_PROGRESS_SCORE_MARGIN: i32 = 192;

        for (position, candidate_index) in shortlist.iter().copied().enumerate() {
            for incumbent_index in shortlist.iter().copied().skip(position + 1) {
                let candidate = &scored_roots[candidate_index];
                let incumbent = &scored_roots[incumbent_index];
                let candidate_window = Self::turn_engine_root_evaluation_family(candidate)
                    == TurnPlanFamily::ManaTempo
                    && candidate.same_turn_score_window_value > 0
                    && !candidate.wins_immediately
                    && !candidate.attacks_opponent_drainer
                    && !candidate.scores_supermana_this_turn
                    && !candidate.scores_opponent_mana_this_turn
                    && !candidate.safe_supermana_pickup_now
                    && !candidate.safe_opponent_mana_pickup_now
                    && !candidate.mana_handoff_to_opponent
                    && !candidate.has_roundtrip;
                let incumbent_window = Self::turn_engine_root_evaluation_family(incumbent)
                    == TurnPlanFamily::ManaTempo
                    && incumbent.same_turn_score_window_value > 0
                    && !incumbent.wins_immediately
                    && !incumbent.attacks_opponent_drainer
                    && !incumbent.scores_supermana_this_turn
                    && !incumbent.scores_opponent_mana_this_turn
                    && !incumbent.safe_supermana_pickup_now
                    && !incumbent.safe_opponent_mana_pickup_now
                    && !incumbent.mana_handoff_to_opponent
                    && !incumbent.has_roundtrip;
                if candidate_window == incumbent_window {
                    continue;
                }

                let (window, progress) = if candidate_window {
                    (candidate, incumbent)
                } else {
                    (incumbent, candidate)
                };
                if window.same_turn_score_window_value > 1
                    || progress.same_turn_score_window_value != 0
                    || Self::turn_engine_root_evaluation_family(progress)
                        != TurnPlanFamily::ManaTempo
                    || progress.own_drainer_vulnerable != window.own_drainer_vulnerable
                    || progress.own_drainer_walk_vulnerable != window.own_drainer_walk_vulnerable
                    || progress.mana_handoff_to_opponent
                    || progress.has_roundtrip
                {
                    continue;
                }

                if Self::root_progress_or_setup_better(progress, window)
                    && progress.score.saturating_add(WINDOW_PROGRESS_SCORE_MARGIN) >= window.score
                {
                    return true;
                }
            }
        }

        false
    }

    fn reply_risk_shortlist_has_close_positive_score_competition(
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        config: AutomoveSearchConfig,
    ) -> bool {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) || shortlist.len() < 2 {
            return false;
        }

        let mut positive_scores = shortlist
            .iter()
            .copied()
            .map(|index| scored_roots[index].score)
            .filter(|score| *score >= 0)
            .collect::<Vec<_>>();
        if positive_scores.len() < 2 {
            return false;
        }

        positive_scores.sort_unstable_by(|left, right| right.cmp(left));
        positive_scores[0].saturating_sub(positive_scores[1]) <= 64
    }

    fn is_same_non_tactical_progress_lane_root_pair(
        candidate: &RootEvaluation,
        incumbent: &RootEvaluation,
    ) -> bool {
        let same_progress_steps = candidate.safe_supermana_progress_steps
            == incumbent.safe_supermana_progress_steps
            && candidate.safe_opponent_mana_progress_steps
                == incumbent.safe_opponent_mana_progress_steps;
        let meaningful_progress_route = same_progress_steps
            && (candidate.safe_supermana_progress_steps < Config::BOARD_SIZE + 4
                || candidate.safe_opponent_mana_progress_steps < Config::BOARD_SIZE + 4);

        meaningful_progress_route
            && candidate.safe_supermana_pickup_now == incumbent.safe_supermana_pickup_now
            && candidate.safe_opponent_mana_pickup_now == incumbent.safe_opponent_mana_pickup_now
            && candidate.supermana_progress == incumbent.supermana_progress
            && candidate.opponent_mana_progress == incumbent.opponent_mana_progress
            && !candidate.classes.is_tactical_priority()
            && !incumbent.classes.is_tactical_priority()
            && !candidate.spirit_development
            && !incumbent.spirit_development
            && !candidate.spirit_same_turn_score_setup_now
            && !incumbent.spirit_same_turn_score_setup_now
            && !candidate.spirit_own_mana_setup_now
            && !incumbent.spirit_own_mana_setup_now
            && candidate.same_turn_score_window_value == 0
            && incumbent.same_turn_score_window_value == 0
            && !candidate.mana_handoff_to_opponent
            && !incumbent.mana_handoff_to_opponent
            && !candidate.has_roundtrip
            && !incumbent.has_roundtrip
    }

    fn current_pro_safe_progress_sibling_shortlist_extension(
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        shortlist: &[usize],
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) {
            return None;
        }

        let anchor_index = *shortlist.first()?;
        let anchor = &scored_roots[anchor_index];
        if !Self::turn_engine_root_evaluation_is_unsafe(anchor)
            || !Self::turn_engine_root_evaluation_has_progress_surface(anchor)
        {
            return None;
        }

        const SAFE_PROGRESS_SHORTLIST_SCORE_GAP_MAX: i32 = 320;

        candidate_indices
            .iter()
            .copied()
            .filter(|index| !shortlist.contains(index))
            .filter(|index| {
                let root = &scored_roots[*index];
                !Self::turn_engine_root_evaluation_is_unsafe(root)
                    && Self::is_same_non_tactical_progress_lane_root_pair(root, anchor)
                    && anchor.score.saturating_sub(root.score)
                        <= SAFE_PROGRESS_SHORTLIST_SCORE_GAP_MAX
            })
            .min_by(|left, right| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })
    }

    fn current_pro_white_turn_four_mana_sibling_reentry(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        shortlist: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<usize> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number != 3
            || game.mons_moves_count < 4
        {
            return None;
        }

        const WHITE_MANA_SIBLING_SCORE_GAP_MAX: i32 = 24;
        const WHITE_MANA_SIBLING_ROOT_RANK_GAP_MAX: usize = 4;

        let anchor_index = shortlist
            .iter()
            .copied()
            .filter(|index| {
                let root = &scored_roots[*index];
                Self::turn_engine_root_evaluation_family(root) == TurnPlanFamily::ManaTempo
                    && !root.own_drainer_vulnerable
                    && !root.own_drainer_walk_vulnerable
                    && Self::current_pro_root_advisor_root_evaluation_is_safe(root)
                    && !root.wins_immediately
                    && !root.attacks_opponent_drainer
                    && root.same_turn_score_window_value == 0
                    && !root.scores_supermana_this_turn
                    && !root.scores_opponent_mana_this_turn
                    && !root.safe_supermana_pickup_now
                    && !root.safe_opponent_mana_pickup_now
            })
            .min_by(|left, right| {
                Self::compare_ranked_scored_root_indices(scored_roots, *left, *right)
            })?;
        let anchor = &scored_roots[anchor_index];
        let anchor_utility = Self::turn_engine_root_plan_utility(
            game,
            anchor,
            perspective,
            config,
            TurnPlanFamily::ManaTempo,
        );
        let mut utility_cache = std::collections::HashMap::<usize, TurnEngineUtility>::new();
        let mut best_index = None;
        let mut best_utility = None;
        let mut best_is_dominance = false;

        for (index, candidate) in scored_roots.iter().enumerate() {
            let same_lane_near_best =
                Self::is_current_pro_white_mana_sibling_pair(candidate, anchor, config)
                    && anchor.score.saturating_sub(candidate.score)
                        <= WHITE_MANA_SIBLING_SCORE_GAP_MAX
                    && anchor.root_rank.abs_diff(candidate.root_rank)
                        <= WHITE_MANA_SIBLING_ROOT_RANK_GAP_MAX
                    && candidate.root_rank < anchor.root_rank;
            if shortlist.contains(&index)
                || Self::turn_engine_root_evaluation_family(candidate) != TurnPlanFamily::ManaTempo
                || candidate.own_drainer_vulnerable
                || candidate.own_drainer_walk_vulnerable
                || !Self::current_pro_root_advisor_root_evaluation_is_safe(candidate)
                || candidate.wins_immediately
                || candidate.attacks_opponent_drainer
                || candidate.same_turn_score_window_value > 0
                || candidate.scores_supermana_this_turn
                || candidate.scores_opponent_mana_this_turn
                || candidate.safe_supermana_pickup_now
                || candidate.safe_opponent_mana_pickup_now
                || (!same_lane_near_best && candidate.score < anchor.score.saturating_sub(96))
                || (candidate.root_rank >= anchor.root_rank && !same_lane_near_best)
            {
                continue;
            }

            let candidate_utility = *utility_cache.entry(index).or_insert_with(|| {
                Self::turn_engine_root_plan_utility(
                    game,
                    candidate,
                    perspective,
                    config,
                    TurnPlanFamily::ManaTempo,
                )
            });
            let utility_order = crate::models::automove_turn_engine::compare_utility_primary_axes(
                candidate_utility,
                anchor_utility,
            );
            let dominance_competes = utility_order == std::cmp::Ordering::Greater
                || candidate_utility.strictly_dominates_override_axes(anchor_utility);
            if !dominance_competes && !same_lane_near_best {
                continue;
            }

            let replace = match (best_index, best_utility) {
                (Some(current_index), Some(current_utility)) => {
                    let current = &scored_roots[current_index];
                    let current_same_lane_near_best =
                        Self::is_current_pro_white_mana_sibling_pair(current, anchor, config)
                            && anchor.score.saturating_sub(current.score)
                                <= WHITE_MANA_SIBLING_SCORE_GAP_MAX
                            && anchor.root_rank.abs_diff(current.root_rank)
                                <= WHITE_MANA_SIBLING_ROOT_RANK_GAP_MAX
                            && current.root_rank < anchor.root_rank;
                    if dominance_competes != best_is_dominance {
                        dominance_competes
                    } else if dominance_competes {
                        crate::models::automove_turn_engine::compare_utility_primary_axes(
                            candidate_utility,
                            current_utility,
                        ) == std::cmp::Ordering::Greater
                            || (crate::models::automove_turn_engine::compare_utility_primary_axes(
                                candidate_utility,
                                current_utility,
                            ) == std::cmp::Ordering::Equal
                                && Self::compare_ranked_scored_root_indices(
                                    scored_roots,
                                    index,
                                    current_index,
                                ) == std::cmp::Ordering::Less)
                    } else if same_lane_near_best && current_same_lane_near_best {
                        candidate.root_rank < current.root_rank
                            || (candidate.root_rank == current.root_rank
                                && Self::compare_ranked_scored_root_indices(
                                    scored_roots,
                                    index,
                                    current_index,
                                ) == std::cmp::Ordering::Less)
                    } else {
                        same_lane_near_best
                    }
                }
                _ => true,
            };
            if replace {
                best_index = Some(index);
                best_utility = Some(candidate_utility);
                best_is_dominance = dominance_competes;
            }
        }

        best_index
    }

    fn current_pro_safe_progress_sibling_order(
        candidate: &RootEvaluation,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent: &RootEvaluation,
        incumbent_snapshot: RootReplyRiskSnapshot,
        config: AutomoveSearchConfig,
    ) -> Option<std::cmp::Ordering> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) {
            return None;
        }

        if !Self::is_same_non_tactical_progress_lane_root_pair(candidate, incumbent)
            || candidate_snapshot.allows_immediate_opponent_win
            || incumbent_snapshot.allows_immediate_opponent_win
            || candidate_snapshot.opponent_reaches_match_point
            || incumbent_snapshot.opponent_reaches_match_point
        {
            return None;
        }

        let candidate_safe = !Self::turn_engine_root_evaluation_is_unsafe(candidate);
        let incumbent_safe = !Self::turn_engine_root_evaluation_is_unsafe(incumbent);
        if candidate_safe == incumbent_safe {
            return None;
        }

        const SAFE_PROGRESS_REPLY_FLOOR_MARGIN: i32 = 240;
        let candidate_reply_competes = candidate_snapshot.worst_reply_score
            + SAFE_PROGRESS_REPLY_FLOOR_MARGIN
            >= incumbent_snapshot.worst_reply_score;
        let incumbent_reply_competes = incumbent_snapshot.worst_reply_score
            + SAFE_PROGRESS_REPLY_FLOOR_MARGIN
            >= candidate_snapshot.worst_reply_score;

        if candidate_safe && candidate_reply_competes {
            Some(std::cmp::Ordering::Greater)
        } else if incumbent_safe && incumbent_reply_competes {
            Some(std::cmp::Ordering::Less)
        } else {
            None
        }
    }

    #[allow(clippy::too_many_arguments)]
    fn current_pro_black_plain_spirit_followup_reply_order(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_index: usize,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent_index: usize,
        incumbent_snapshot: RootReplyRiskSnapshot,
        perspective: Color,
        config: AutomoveSearchConfig,
        spirit_followup_scores: &mut std::collections::HashMap<usize, i32>,
    ) -> Option<std::cmp::Ordering> {
        let candidate = &scored_roots[candidate_index];
        let incumbent = &scored_roots[incumbent_index];
        let (plain, plain_snapshot, setup, setup_snapshot, candidate_is_plain) =
            if Self::is_current_pro_black_plain_spirit_followup_setup_pair(
                game, candidate, incumbent, config,
            ) {
                (
                    candidate,
                    candidate_snapshot,
                    incumbent,
                    incumbent_snapshot,
                    true,
                )
            } else if Self::is_current_pro_black_plain_spirit_followup_setup_pair(
                game, incumbent, candidate, config,
            ) {
                (
                    incumbent,
                    incumbent_snapshot,
                    candidate,
                    candidate_snapshot,
                    false,
                )
            } else {
                return None;
            };

        if plain_snapshot.allows_immediate_opponent_win
            || setup_snapshot.allows_immediate_opponent_win
            || plain_snapshot.opponent_reaches_match_point
            || setup_snapshot.opponent_reaches_match_point
        {
            return None;
        }

        const REPLY_FLOOR_MARGIN: i32 = 192;
        const FOLLOWUP_MARGIN: i32 = 32;
        const SETUP_GAIN_MARGIN: i32 = 32;
        const SETUP_SCORE_MARGIN: i32 = 64;

        let plain_followup = *spirit_followup_scores
            .entry(if candidate_is_plain {
                candidate_index
            } else {
                incumbent_index
            })
            .or_insert_with(|| {
                Self::current_pro_spirit_followup_floor_score(&plain.game, perspective, config)
            });
        let setup_followup = *spirit_followup_scores
            .entry(if candidate_is_plain {
                incumbent_index
            } else {
                candidate_index
            })
            .or_insert_with(|| {
                Self::current_pro_spirit_followup_floor_score(&setup.game, perspective, config)
            });

        let setup_has_close_top_seed = setup.root_rank <= plain.root_rank
            && setup.score.saturating_add(SETUP_SCORE_MARGIN) >= plain.score
            && setup.spirit_setup_gain >= plain.spirit_setup_gain.saturating_add(SETUP_GAIN_MARGIN)
            && setup_snapshot
                .worst_reply_score
                .saturating_add(REPLY_FLOOR_MARGIN)
                >= plain_snapshot.worst_reply_score
            && setup_followup.saturating_add(FOLLOWUP_MARGIN) >= plain_followup;
        if setup_has_close_top_seed {
            return Some(if candidate_is_plain {
                std::cmp::Ordering::Less
            } else {
                std::cmp::Ordering::Greater
            });
        }

        if plain_snapshot
            .worst_reply_score
            .saturating_add(REPLY_FLOOR_MARGIN)
            < setup_snapshot.worst_reply_score
        {
            return None;
        }
        if plain_followup < setup_followup.saturating_add(FOLLOWUP_MARGIN)
            && plain.score < setup.score
        {
            return None;
        }

        Some(if candidate_is_plain {
            std::cmp::Ordering::Greater
        } else {
            std::cmp::Ordering::Less
        })
    }

    #[allow(clippy::too_many_arguments)]
    fn current_pro_early_black_plain_spirit_sibling_reply_order(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_index: usize,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent_index: usize,
        incumbent_snapshot: RootReplyRiskSnapshot,
        perspective: Color,
        config: AutomoveSearchConfig,
        cached_scores: &mut std::collections::HashMap<usize, i32>,
    ) -> Option<std::cmp::Ordering> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number > 2
        {
            return None;
        }

        let candidate = &scored_roots[candidate_index];
        let incumbent = &scored_roots[incumbent_index];
        if !Self::is_plain_spirit_development_root(candidate)
            || !Self::is_plain_spirit_development_root(incumbent)
            || candidate.inputs.first() != incumbent.inputs.first()
            || candidate.own_drainer_vulnerable != incumbent.own_drainer_vulnerable
            || candidate.mana_handoff_to_opponent != incumbent.mana_handoff_to_opponent
            || candidate.has_roundtrip != incumbent.has_roundtrip
            || candidate.spirit_same_turn_score_setup_now
            || incumbent.spirit_same_turn_score_setup_now
            || candidate.spirit_own_mana_setup_now
            || incumbent.spirit_own_mana_setup_now
            || candidate.wins_immediately
            || incumbent.wins_immediately
            || candidate.attacks_opponent_drainer
            || incumbent.attacks_opponent_drainer
            || candidate_snapshot.allows_immediate_opponent_win
            || incumbent_snapshot.allows_immediate_opponent_win
            || candidate_snapshot.opponent_reaches_match_point
            || incumbent_snapshot.opponent_reaches_match_point
        {
            return None;
        }

        const SCORE_MARGIN: i32 = 48;
        const REPLY_MARGIN: i32 = 96;
        const FOLLOWUP_MARGIN: i32 = 32;

        let candidate_followup = *cached_scores.entry(candidate_index).or_insert_with(|| {
            Self::current_pro_spirit_followup_floor_score(&candidate.game, perspective, config)
        });
        let incumbent_followup = *cached_scores.entry(incumbent_index).or_insert_with(|| {
            Self::current_pro_spirit_followup_floor_score(&incumbent.game, perspective, config)
        });

        let candidate_better = candidate_snapshot.worst_reply_score
            >= incumbent_snapshot
                .worst_reply_score
                .saturating_add(REPLY_MARGIN)
            && candidate_followup.saturating_add(FOLLOWUP_MARGIN) >= incumbent_followup
            && candidate.score.saturating_add(SCORE_MARGIN) >= incumbent.score;
        let incumbent_better = incumbent_snapshot.worst_reply_score
            >= candidate_snapshot
                .worst_reply_score
                .saturating_add(REPLY_MARGIN)
            && incumbent_followup.saturating_add(FOLLOWUP_MARGIN) >= candidate_followup
            && incumbent.score.saturating_add(SCORE_MARGIN) >= candidate.score;

        if candidate_better == incumbent_better {
            let candidate_reply_close = candidate_snapshot.worst_reply_score
                < incumbent_snapshot
                    .worst_reply_score
                    .saturating_add(REPLY_MARGIN)
                && candidate_followup < incumbent_followup.saturating_add(REPLY_MARGIN);
            let incumbent_reply_close = incumbent_snapshot.worst_reply_score
                < candidate_snapshot
                    .worst_reply_score
                    .saturating_add(REPLY_MARGIN)
                && incumbent_followup < candidate_followup.saturating_add(REPLY_MARGIN);
            if candidate_reply_close && incumbent_reply_close && candidate.score != incumbent.score
            {
                return Some(if candidate.score > incumbent.score {
                    std::cmp::Ordering::Greater
                } else {
                    std::cmp::Ordering::Less
                });
            }
            return None;
        }

        Some(if candidate_better {
            std::cmp::Ordering::Greater
        } else {
            std::cmp::Ordering::Less
        })
    }

    fn current_pro_early_black_mana_progress_reply_order(
        game: &MonsGame,
        candidate: &RootEvaluation,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent: &RootEvaluation,
        incumbent_snapshot: RootReplyRiskSnapshot,
        config: AutomoveSearchConfig,
    ) -> Option<std::cmp::Ordering> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number > 4
            || candidate_snapshot.allows_immediate_opponent_win
            || incumbent_snapshot.allows_immediate_opponent_win
            || candidate_snapshot.opponent_reaches_match_point
            || incumbent_snapshot.opponent_reaches_match_point
        {
            return None;
        }

        let candidate_family = Self::turn_engine_root_evaluation_family(candidate);
        let incumbent_family = Self::turn_engine_root_evaluation_family(incumbent);
        let candidate_allowed = matches!(
            candidate_family,
            TurnPlanFamily::ManaTempo
                | TurnPlanFamily::SafeSupermanaProgress
                | TurnPlanFamily::SafeOpponentManaProgress
        );
        let incumbent_allowed = matches!(
            incumbent_family,
            TurnPlanFamily::ManaTempo
                | TurnPlanFamily::SafeSupermanaProgress
                | TurnPlanFamily::SafeOpponentManaProgress
        );
        if !candidate_allowed
            || !incumbent_allowed
            || candidate.root_rank.abs_diff(incumbent.root_rank) > 8
        {
            return None;
        }

        let candidate_progress_surface =
            Self::turn_engine_root_evaluation_has_progress_surface(candidate);
        let incumbent_progress_surface =
            Self::turn_engine_root_evaluation_has_progress_surface(incumbent);
        let candidate_progress_better = Self::root_progress_or_setup_better(candidate, incumbent)
            || (candidate_progress_surface && !incumbent_progress_surface);
        let incumbent_progress_better = Self::root_progress_or_setup_better(incumbent, candidate)
            || (incumbent_progress_surface && !candidate_progress_surface);
        if candidate_progress_better == incumbent_progress_better {
            return None;
        }

        let candidate_non_concrete_window = Self::turn_engine_root_evaluation_family(candidate)
            == TurnPlanFamily::ManaTempo
            && candidate.same_turn_score_window_value > 0
            && !candidate.wins_immediately
            && !candidate.attacks_opponent_drainer
            && !candidate.scores_supermana_this_turn
            && !candidate.scores_opponent_mana_this_turn
            && !candidate.safe_supermana_pickup_now
            && !candidate.safe_opponent_mana_pickup_now
            && !candidate.mana_handoff_to_opponent
            && !candidate.has_roundtrip;
        let incumbent_non_concrete_window = Self::turn_engine_root_evaluation_family(incumbent)
            == TurnPlanFamily::ManaTempo
            && incumbent.same_turn_score_window_value > 0
            && !incumbent.wins_immediately
            && !incumbent.attacks_opponent_drainer
            && !incumbent.scores_supermana_this_turn
            && !incumbent.scores_opponent_mana_this_turn
            && !incumbent.safe_supermana_pickup_now
            && !incumbent.safe_opponent_mana_pickup_now
            && !incumbent.mana_handoff_to_opponent
            && !incumbent.has_roundtrip;

        const WINDOW_PROGRESS_SCORE_MARGIN: i32 = 192;
        const WINDOW_PROGRESS_REPLY_FLOOR_MARGIN: i32 = 96;
        if candidate_non_concrete_window != incumbent_non_concrete_window {
            let (window, window_snapshot, progress, progress_snapshot, candidate_is_window) =
                if candidate_non_concrete_window {
                    (
                        candidate,
                        candidate_snapshot,
                        incumbent,
                        incumbent_snapshot,
                        true,
                    )
                } else {
                    (
                        incumbent,
                        incumbent_snapshot,
                        candidate,
                        candidate_snapshot,
                        false,
                    )
                };
            if window.same_turn_score_window_value <= 1
                && progress.same_turn_score_window_value == 0
                && Self::turn_engine_root_evaluation_family(progress) == TurnPlanFamily::ManaTempo
                && progress.own_drainer_vulnerable == window.own_drainer_vulnerable
                && progress.own_drainer_walk_vulnerable == window.own_drainer_walk_vulnerable
                && !progress.mana_handoff_to_opponent
                && !progress.has_roundtrip
                && Self::root_progress_or_setup_better(progress, window)
                && progress_snapshot
                    .worst_reply_score
                    .saturating_add(WINDOW_PROGRESS_REPLY_FLOOR_MARGIN)
                    >= window_snapshot.worst_reply_score
                && progress.score.saturating_add(WINDOW_PROGRESS_SCORE_MARGIN) >= window.score
            {
                return Some(if candidate_is_window {
                    std::cmp::Ordering::Less
                } else {
                    std::cmp::Ordering::Greater
                });
            }
        }

        const REPLY_FLOOR_MARGIN: i32 = 240;
        let candidate_reply_competes = candidate_snapshot
            .worst_reply_score
            .saturating_add(REPLY_FLOOR_MARGIN)
            >= incumbent_snapshot.worst_reply_score;
        let incumbent_reply_competes = incumbent_snapshot
            .worst_reply_score
            .saturating_add(REPLY_FLOOR_MARGIN)
            >= candidate_snapshot.worst_reply_score;

        if candidate_progress_better
            && candidate.score > incumbent.score
            && candidate_reply_competes
        {
            return Some(std::cmp::Ordering::Greater);
        }
        if incumbent_progress_better
            && incumbent.score > candidate.score
            && incumbent_reply_competes
        {
            return Some(std::cmp::Ordering::Less);
        }

        None
    }

    fn current_pro_risky_recovery_progress_sibling_order(
        candidate: &RootEvaluation,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent: &RootEvaluation,
        incumbent_snapshot: RootReplyRiskSnapshot,
        candidate_projection: Option<&TurnEngineRootProjection>,
        incumbent_projection: Option<&TurnEngineRootProjection>,
        config: AutomoveSearchConfig,
    ) -> Option<std::cmp::Ordering> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || !Self::is_same_non_tactical_progress_lane_root_pair(candidate, incumbent)
        {
            return None;
        }

        let candidate_unsafe = Self::turn_engine_root_evaluation_is_unsafe(candidate);
        let incumbent_unsafe = Self::turn_engine_root_evaluation_is_unsafe(incumbent);
        if candidate_unsafe == incumbent_unsafe {
            return None;
        }

        let (
            risky,
            risky_snapshot,
            risky_projection,
            safe,
            safe_snapshot,
            safe_projection,
            candidate_is_risky,
        ) = if candidate_unsafe {
            (
                candidate,
                candidate_snapshot,
                candidate_projection?,
                incumbent,
                incumbent_snapshot,
                incumbent_projection?,
                true,
            )
        } else {
            (
                incumbent,
                incumbent_snapshot,
                incumbent_projection?,
                candidate,
                candidate_snapshot,
                candidate_projection?,
                false,
            )
        };

        if risky_snapshot.allows_immediate_opponent_win
            || risky_snapshot.opponent_reaches_match_point
            || safe_snapshot.allows_immediate_opponent_win
            || safe_snapshot.opponent_reaches_match_point
            || risky_projection.plan.goal_family != TurnPlanFamily::ImmediateScore
            || crate::models::automove_turn_engine::compare_utility_primary_axes(
                risky_projection.plan.utility,
                safe_projection.plan.utility,
            ) == std::cmp::Ordering::Less
            || risky_snapshot.worst_reply_score + 160 < safe_snapshot.worst_reply_score
            || risky.score + 32 < safe.score
        {
            return None;
        }

        Some(if candidate_is_risky {
            std::cmp::Ordering::Greater
        } else {
            std::cmp::Ordering::Less
        })
    }

    fn is_same_opening_safe_setup_pair(
        candidate: &RootEvaluation,
        incumbent: &RootEvaluation,
        config: AutomoveSearchConfig,
    ) -> bool {
        matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && candidate.inputs.first() == incumbent.inputs.first()
            && candidate.efficiency == incumbent.efficiency
            && (candidate.score - incumbent.score).abs() <= 128
            && !Self::turn_engine_root_evaluation_is_unsafe(candidate)
            && !Self::turn_engine_root_evaluation_is_unsafe(incumbent)
            && !candidate.mana_handoff_to_opponent
            && !incumbent.mana_handoff_to_opponent
            && !candidate.has_roundtrip
            && !incumbent.has_roundtrip
            && !candidate.wins_immediately
            && !incumbent.wins_immediately
            && !candidate.attacks_opponent_drainer
            && !incumbent.attacks_opponent_drainer
            && !candidate.scores_supermana_this_turn
            && !incumbent.scores_supermana_this_turn
            && !candidate.scores_opponent_mana_this_turn
            && !incumbent.scores_opponent_mana_this_turn
            && candidate.same_turn_score_window_value == 0
            && incumbent.same_turn_score_window_value == 0
            && !candidate.supermana_progress
            && !incumbent.supermana_progress
            && !candidate.opponent_mana_progress
            && !incumbent.opponent_mana_progress
    }

    fn current_pro_omitted_same_opening_setup_competes(
        candidate: &RootEvaluation,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent: &RootEvaluation,
        incumbent_snapshot: RootReplyRiskSnapshot,
        config: AutomoveSearchConfig,
    ) -> bool {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || !candidate.spirit_own_mana_setup_now
            || incumbent.spirit_development
            || incumbent.spirit_same_turn_score_setup_now
            || incumbent.spirit_own_mana_setup_now
            || !Self::is_same_opening_safe_setup_pair(candidate, incumbent, config)
        {
            return false;
        }

        const ROOT_RANK_MARGIN: usize = 3;
        const SCORE_MARGIN: i32 = 128;
        const REPLY_FLOOR_MARGIN: i32 = 160;

        candidate.root_rank.saturating_add(ROOT_RANK_MARGIN) <= incumbent.root_rank
            && candidate.score.saturating_add(SCORE_MARGIN) >= incumbent.score
            && candidate_snapshot
                .worst_reply_score
                .saturating_add(REPLY_FLOOR_MARGIN)
                >= incumbent_snapshot.worst_reply_score
    }

    #[allow(clippy::too_many_arguments)]
    fn current_pro_early_black_plain_spirit_mana_reply_order(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_index: usize,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent_index: usize,
        incumbent_snapshot: RootReplyRiskSnapshot,
        perspective: Color,
        config: AutomoveSearchConfig,
        cached_scores: &mut std::collections::HashMap<usize, i32>,
    ) -> Option<std::cmp::Ordering> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::Black
            || game.turn_number > 4
        {
            return None;
        }

        let candidate = &scored_roots[candidate_index];
        let incumbent = &scored_roots[incumbent_index];
        let candidate_plain = Self::is_plain_spirit_development_root(candidate);
        let incumbent_plain = Self::is_plain_spirit_development_root(incumbent);
        let candidate_mana =
            Self::turn_engine_root_evaluation_family(candidate) == TurnPlanFamily::ManaTempo;
        let incumbent_mana =
            Self::turn_engine_root_evaluation_family(incumbent) == TurnPlanFamily::ManaTempo;
        if candidate_plain == incumbent_plain || candidate_mana == incumbent_mana {
            return None;
        }

        let (plain, plain_snapshot, mana, mana_snapshot, candidate_is_plain) =
            if candidate_plain && incumbent_mana {
                (
                    candidate,
                    candidate_snapshot,
                    incumbent,
                    incumbent_snapshot,
                    true,
                )
            } else if incumbent_plain && candidate_mana {
                (
                    incumbent,
                    incumbent_snapshot,
                    candidate,
                    candidate_snapshot,
                    false,
                )
            } else {
                return None;
            };

        if plain.inputs.first() != mana.inputs.first()
            || plain.own_drainer_vulnerable != mana.own_drainer_vulnerable
            || plain.own_drainer_walk_vulnerable != mana.own_drainer_walk_vulnerable
            || plain_snapshot.allows_immediate_opponent_win
            || mana_snapshot.allows_immediate_opponent_win
            || plain_snapshot.opponent_reaches_match_point
            || mana_snapshot.opponent_reaches_match_point
            || plain.mana_handoff_to_opponent
            || mana.mana_handoff_to_opponent
            || plain.has_roundtrip
            || mana.has_roundtrip
            || plain.wins_immediately
            || mana.wins_immediately
            || plain.attacks_opponent_drainer
            || mana.attacks_opponent_drainer
            || plain.same_turn_score_window_value > 0
            || mana.same_turn_score_window_value > 0
            || plain.scores_supermana_this_turn
            || mana.scores_supermana_this_turn
            || plain.scores_opponent_mana_this_turn
            || mana.scores_opponent_mana_this_turn
            || plain.safe_supermana_pickup_now
            || mana.safe_supermana_pickup_now
            || plain.safe_opponent_mana_pickup_now
            || mana.safe_opponent_mana_pickup_now
            || plain.spirit_same_turn_score_setup_now
            || plain.spirit_own_mana_setup_now
            || mana.spirit_same_turn_score_setup_now
            || mana.spirit_own_mana_setup_now
            || mana.supermana_progress
            || mana.opponent_mana_progress
        {
            return None;
        }

        const SCORE_MARGIN: i32 = 24;
        const REPLY_FLOOR_MARGIN: i32 = 192;
        const FOLLOWUP_MARGIN: i32 = 32;

        if plain.score.saturating_add(SCORE_MARGIN) < mana.score
            || plain_snapshot
                .worst_reply_score
                .saturating_add(REPLY_FLOOR_MARGIN)
                < mana_snapshot.worst_reply_score
        {
            return None;
        }

        let plain_followup = *cached_scores
            .entry(if candidate_is_plain {
                candidate_index
            } else {
                incumbent_index
            })
            .or_insert_with(|| {
                Self::current_pro_spirit_followup_floor_score(&plain.game, perspective, config)
            });
        let mana_followup = *cached_scores
            .entry(if candidate_is_plain {
                incumbent_index
            } else {
                candidate_index
            })
            .or_insert_with(|| {
                Self::current_pro_spirit_followup_floor_score(&mana.game, perspective, config)
            });
        if plain_followup.saturating_add(FOLLOWUP_MARGIN) < mana_followup {
            return None;
        }

        Some(if candidate_is_plain {
            std::cmp::Ordering::Greater
        } else {
            std::cmp::Ordering::Less
        })
    }

    #[allow(clippy::too_many_arguments)]
    fn current_pro_safe_non_spirit_followup_order(
        _game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_index: usize,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent_index: usize,
        incumbent_snapshot: RootReplyRiskSnapshot,
        perspective: Color,
        config: AutomoveSearchConfig,
        cached_scores: &mut std::collections::HashMap<usize, i32>,
    ) -> Option<std::cmp::Ordering> {
        if !Self::current_pro_secondary_analysis_live(config) {
            return None;
        }

        let candidate = &scored_roots[candidate_index];
        let incumbent = &scored_roots[incumbent_index];
        let candidate_plain_spirit = Self::is_plain_spirit_development_root(candidate);
        let incumbent_plain_spirit = Self::is_plain_spirit_development_root(incumbent);
        if candidate_plain_spirit == incumbent_plain_spirit {
            return None;
        }

        let (challenger, challenger_snapshot, spirit, spirit_snapshot, candidate_is_challenger) =
            if candidate_plain_spirit {
                (
                    incumbent,
                    incumbent_snapshot,
                    candidate,
                    candidate_snapshot,
                    false,
                )
            } else {
                (
                    candidate,
                    candidate_snapshot,
                    incumbent,
                    incumbent_snapshot,
                    true,
                )
            };

        if Self::turn_engine_root_evaluation_is_unsafe(challenger)
            || Self::turn_engine_root_evaluation_is_unsafe(spirit)
            || challenger_snapshot.allows_immediate_opponent_win
            || spirit_snapshot.allows_immediate_opponent_win
            || challenger_snapshot.opponent_reaches_match_point
            || spirit_snapshot.opponent_reaches_match_point
            || challenger.mana_handoff_to_opponent
            || spirit.mana_handoff_to_opponent
            || challenger.has_roundtrip
            || spirit.has_roundtrip
            || challenger.own_drainer_vulnerable
            || spirit.own_drainer_vulnerable
            || challenger.own_drainer_walk_vulnerable
            || spirit.own_drainer_walk_vulnerable
            || challenger.spirit_same_turn_score_setup_now
            || challenger.spirit_own_mana_setup_now
            || spirit.spirit_same_turn_score_setup_now
            || spirit.spirit_own_mana_setup_now
            || challenger.wins_immediately
            || spirit.wins_immediately
            || challenger.attacks_opponent_drainer
            || spirit.attacks_opponent_drainer
            || challenger.same_turn_score_window_value > 0
            || spirit.same_turn_score_window_value > 0
        {
            return None;
        }

        const SCORE_MARGIN: i32 = 32;
        const REPLY_FLOOR_MARGIN: i32 = 192;
        const FOLLOWUP_MARGIN: i32 = 48;
        let challenger_score = *cached_scores
            .entry(if candidate_is_challenger {
                candidate_index
            } else {
                incumbent_index
            })
            .or_insert_with(|| {
                Self::current_pro_spirit_followup_floor_score(&challenger.game, perspective, config)
            });
        let spirit_score = *cached_scores
            .entry(if candidate_is_challenger {
                incumbent_index
            } else {
                candidate_index
            })
            .or_insert_with(|| {
                Self::current_pro_spirit_followup_floor_score(&spirit.game, perspective, config)
            });

        let standard_reply_floor_competes = challenger_snapshot
            .worst_reply_score
            .saturating_add(REPLY_FLOOR_MARGIN)
            >= spirit_snapshot.worst_reply_score;
        let relaxed_opening_tempo_competes = _game.turn_number <= 2
            && !challenger.supermana_progress
            && !challenger.opponent_mana_progress
            && challenger.score >= spirit.score.saturating_add(48)
            && challenger_snapshot.worst_reply_score >= 96
            && challenger_score >= spirit_score;
        if challenger.score.saturating_add(SCORE_MARGIN) < spirit.score
            || (!standard_reply_floor_competes && !relaxed_opening_tempo_competes)
        {
            return None;
        }

        if challenger_score >= spirit_score.saturating_add(FOLLOWUP_MARGIN)
            || relaxed_opening_tempo_competes
        {
            Some(if candidate_is_challenger {
                std::cmp::Ordering::Greater
            } else {
                std::cmp::Ordering::Less
            })
        } else {
            None
        }
    }

    fn is_current_pro_white_spirit_followup_setup_pair(
        game: &MonsGame,
        candidate: &RootEvaluation,
        incumbent: &RootEvaluation,
        config: AutomoveSearchConfig,
    ) -> bool {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || game.active_color != Color::White
            || game.turn_number > 3
            || game.mons_moves_count < 1
            || !game.player_can_use_action()
            || !game.player_can_move_mana()
        {
            return false;
        }

        let candidate_setup_progress = candidate.spirit_own_mana_setup_now
            && candidate.opponent_mana_progress
            && !Self::is_plain_spirit_development_root(candidate);
        let incumbent_setup_progress = incumbent.spirit_own_mana_setup_now
            && incumbent.opponent_mana_progress
            && !Self::is_plain_spirit_development_root(incumbent);
        if candidate_setup_progress == incumbent_setup_progress {
            return false;
        }

        let (setup, plain) = if candidate_setup_progress
            && Self::is_plain_spirit_development_root(incumbent)
        {
            (candidate, incumbent)
        } else if incumbent_setup_progress && Self::is_plain_spirit_development_root(candidate) {
            (incumbent, candidate)
        } else {
            return false;
        };

        setup.inputs.first() == plain.inputs.first()
            && setup.efficiency == plain.efficiency
            && setup.own_drainer_vulnerable == plain.own_drainer_vulnerable
            && setup.own_drainer_walk_vulnerable == plain.own_drainer_walk_vulnerable
            && !setup.mana_handoff_to_opponent
            && !plain.mana_handoff_to_opponent
            && !setup.has_roundtrip
            && !plain.has_roundtrip
            && !setup.wins_immediately
            && !plain.wins_immediately
            && !setup.attacks_opponent_drainer
            && !plain.attacks_opponent_drainer
            && !setup.scores_supermana_this_turn
            && !plain.scores_supermana_this_turn
            && !setup.scores_opponent_mana_this_turn
            && !plain.scores_opponent_mana_this_turn
            && !setup.safe_supermana_pickup_now
            && !plain.safe_supermana_pickup_now
            && !setup.safe_opponent_mana_pickup_now
            && !plain.safe_opponent_mana_pickup_now
            && setup.same_turn_score_window_value == 0
            && plain.same_turn_score_window_value == 0
            && setup.supermana_progress == plain.supermana_progress
            && setup.safe_supermana_progress_steps == plain.safe_supermana_progress_steps
            && setup.opponent_mana_progress == plain.opponent_mana_progress
            && setup.safe_opponent_mana_progress_steps == plain.safe_opponent_mana_progress_steps
    }

    fn current_pro_white_spirit_followup_setup_reply_order(
        game: &MonsGame,
        candidate: &RootEvaluation,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent: &RootEvaluation,
        incumbent_snapshot: RootReplyRiskSnapshot,
        config: AutomoveSearchConfig,
    ) -> Option<std::cmp::Ordering> {
        if !Self::is_current_pro_white_spirit_followup_setup_pair(
            game, candidate, incumbent, config,
        ) {
            return None;
        }

        let candidate_setup_progress = candidate.spirit_own_mana_setup_now
            && candidate.opponent_mana_progress
            && !Self::is_plain_spirit_development_root(candidate);
        let incumbent_setup_progress = incumbent.spirit_own_mana_setup_now
            && incumbent.opponent_mana_progress
            && !Self::is_plain_spirit_development_root(incumbent);
        if candidate_setup_progress == incumbent_setup_progress {
            return None;
        }

        let (setup, setup_snapshot, plain, plain_snapshot, candidate_is_setup) =
            if candidate_setup_progress && Self::is_plain_spirit_development_root(incumbent) {
                (
                    candidate,
                    candidate_snapshot,
                    incumbent,
                    incumbent_snapshot,
                    true,
                )
            } else if incumbent_setup_progress && Self::is_plain_spirit_development_root(candidate)
            {
                (
                    incumbent,
                    incumbent_snapshot,
                    candidate,
                    candidate_snapshot,
                    false,
                )
            } else {
                return None;
            };

        if setup.inputs.first() != plain.inputs.first()
            || setup.efficiency != plain.efficiency
            || setup.own_drainer_vulnerable != plain.own_drainer_vulnerable
            || setup.own_drainer_walk_vulnerable != plain.own_drainer_walk_vulnerable
            || setup.mana_handoff_to_opponent
            || plain.mana_handoff_to_opponent
            || setup.has_roundtrip
            || plain.has_roundtrip
            || setup.wins_immediately
            || plain.wins_immediately
            || setup.attacks_opponent_drainer
            || plain.attacks_opponent_drainer
            || setup.scores_supermana_this_turn
            || plain.scores_supermana_this_turn
            || setup.scores_opponent_mana_this_turn
            || plain.scores_opponent_mana_this_turn
            || setup.safe_supermana_pickup_now
            || plain.safe_supermana_pickup_now
            || setup.safe_opponent_mana_pickup_now
            || plain.safe_opponent_mana_pickup_now
            || setup.same_turn_score_window_value > 0
            || plain.same_turn_score_window_value > 0
            || setup.supermana_progress
            || plain.supermana_progress
            || setup_snapshot.allows_immediate_opponent_win
            || plain_snapshot.allows_immediate_opponent_win
            || setup_snapshot.opponent_reaches_match_point
            || plain_snapshot.opponent_reaches_match_point
        {
            return None;
        }

        const SCORE_MARGIN: i32 = 96;
        const ROOT_RANK_MARGIN: usize = 8;
        let reply_floor_margin = if game.turn_number == 3 && game.mons_moves_count == 1 {
            192
        } else {
            96
        };

        if setup.score.saturating_add(SCORE_MARGIN) < plain.score
            || setup_snapshot
                .worst_reply_score
                .saturating_add(reply_floor_margin)
                < plain_snapshot.worst_reply_score
            || setup.root_rank > plain.root_rank.saturating_add(ROOT_RANK_MARGIN)
        {
            return None;
        }

        Some(if candidate_is_setup {
            std::cmp::Ordering::Greater
        } else {
            std::cmp::Ordering::Less
        })
    }

    fn is_quiet_non_tactical_reply_risk_root(root: &RootEvaluation) -> bool {
        !root.wins_immediately
            && !root.attacks_opponent_drainer
            && !root.classes.is_tactical_priority()
            && !root.scores_supermana_this_turn
            && !root.scores_opponent_mana_this_turn
            && !root.safe_supermana_pickup_now
            && !root.safe_opponent_mana_pickup_now
            && root.same_turn_score_window_value == 0
    }

    fn current_pro_safe_pickup_reply_order(
        candidate: &RootEvaluation,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent: &RootEvaluation,
        incumbent_snapshot: RootReplyRiskSnapshot,
        config: AutomoveSearchConfig,
    ) -> Option<std::cmp::Ordering> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) {
            return None;
        }

        let candidate_pickup =
            candidate.safe_supermana_pickup_now || candidate.safe_opponent_mana_pickup_now;
        let incumbent_pickup =
            incumbent.safe_supermana_pickup_now || incumbent.safe_opponent_mana_pickup_now;
        if candidate_pickup == incumbent_pickup {
            return None;
        }

        let (pickup, pickup_snapshot, other, other_snapshot, candidate_is_pickup) =
            if candidate_pickup {
                (
                    candidate,
                    candidate_snapshot,
                    incumbent,
                    incumbent_snapshot,
                    true,
                )
            } else {
                (
                    incumbent,
                    incumbent_snapshot,
                    candidate,
                    candidate_snapshot,
                    false,
                )
            };

        if Self::turn_engine_root_evaluation_is_unsafe(pickup)
            || pickup_snapshot.allows_immediate_opponent_win
            || pickup_snapshot.opponent_reaches_match_point
            || other_snapshot.allows_immediate_opponent_win
            || other_snapshot.opponent_reaches_match_point
            || pickup.mana_handoff_to_opponent
            || pickup.has_roundtrip
            || other.wins_immediately
            || other.attacks_opponent_drainer
        {
            return None;
        }

        let other_progress_like = other.supermana_progress
            || other.opponent_mana_progress
            || other.spirit_own_mana_setup_now
            || other.spirit_same_turn_score_setup_now
            || other.spirit_development;
        if !other_progress_like {
            return None;
        }

        const SCORE_MARGIN: i32 = 144;
        const REPLY_FLOOR_MARGIN: i32 = 192;
        if pickup.score.saturating_add(SCORE_MARGIN) < other.score
            || pickup_snapshot
                .worst_reply_score
                .saturating_add(REPLY_FLOOR_MARGIN)
                < other_snapshot.worst_reply_score
        {
            return None;
        }

        Some(if candidate_is_pickup {
            std::cmp::Ordering::Greater
        } else {
            std::cmp::Ordering::Less
        })
    }

    fn current_pro_normal_safety_reply_order(
        game: &MonsGame,
        candidate: &RootEvaluation,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent: &RootEvaluation,
        incumbent_snapshot: RootReplyRiskSnapshot,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Option<std::cmp::Ordering> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || !config.enable_normal_root_safety_rerank
            || !Self::is_quiet_non_tactical_reply_risk_root(candidate)
            || !Self::is_quiet_non_tactical_reply_risk_root(incumbent)
            || candidate.root_rank.abs_diff(incumbent.root_rank) > 8
            || (candidate.score - incumbent.score).abs() > 160
            || (candidate_snapshot.worst_reply_score - incumbent_snapshot.worst_reply_score).abs()
                > 320
        {
            return None;
        }

        let progress_or_setup_pair =
            Self::turn_engine_root_evaluation_has_progress_surface(candidate)
                || Self::turn_engine_root_evaluation_has_progress_surface(incumbent)
                || candidate.spirit_own_mana_setup_now
                || incumbent.spirit_own_mana_setup_now
                || candidate.spirit_same_turn_score_setup_now
                || incumbent.spirit_same_turn_score_setup_now
                || Self::is_same_non_tactical_progress_lane_root_pair(candidate, incumbent)
                || Self::is_same_opening_safe_setup_pair(candidate, incumbent, config);
        if !progress_or_setup_pair {
            return None;
        }

        let reply_limit = config.node_enum_limit.clamp(
            SMART_NORMAL_ROOT_SAFETY_REPLY_LIMIT_MIN,
            SMART_NORMAL_ROOT_SAFETY_REPLY_LIMIT_MAX,
        );
        let my_score_before = Self::score_for_color(game, perspective);
        let start_options = Self::automove_start_input_options();
        let candidate_normal_snapshot = Self::normal_root_safety_snapshot(
            &candidate.game,
            perspective,
            my_score_before,
            config,
            reply_limit,
            start_options,
        );
        let incumbent_normal_snapshot = Self::normal_root_safety_snapshot(
            &incumbent.game,
            perspective,
            my_score_before,
            config,
            reply_limit,
            start_options,
        );
        let safety_axes_differ = candidate_normal_snapshot.allows_immediate_opponent_win
            != incumbent_normal_snapshot.allows_immediate_opponent_win
            || candidate_normal_snapshot.opponent_reaches_match_point
                != incumbent_normal_snapshot.opponent_reaches_match_point
            || candidate_normal_snapshot.opponent_max_score_gain
                != incumbent_normal_snapshot.opponent_max_score_gain
            || candidate_normal_snapshot.my_score_gain != incumbent_normal_snapshot.my_score_gain
            || candidate_normal_snapshot.worst_reply_score
                != incumbent_normal_snapshot.worst_reply_score;

        if safety_axes_differ {
            if Self::is_better_normal_root_safety_candidate(
                candidate_normal_snapshot,
                candidate.score,
                incumbent_normal_snapshot,
                incumbent.score,
            ) {
                return Some(std::cmp::Ordering::Greater);
            }
            if Self::is_better_normal_root_safety_candidate(
                incumbent_normal_snapshot,
                incumbent.score,
                candidate_normal_snapshot,
                candidate.score,
            ) {
                return Some(std::cmp::Ordering::Less);
            }
        }

        if config.enable_normal_root_safety_deep_floor
            && Self::should_apply_normal_root_safety_deep_floor(game, perspective)
        {
            let candidate_floor = Self::normal_root_safety_deep_floor_score(
                &candidate.game,
                perspective,
                config,
                reply_limit,
            );
            let incumbent_floor = Self::normal_root_safety_deep_floor_score(
                &incumbent.game,
                perspective,
                config,
                reply_limit,
            );
            if candidate_floor != incumbent_floor {
                return Some(candidate_floor.cmp(&incumbent_floor));
            }
        }

        None
    }

    fn current_pro_mixed_plain_spirit_reply_floor_order(
        candidate_snapshot: RootReplyRiskSnapshot,
        candidate_projection: &TurnEngineRootProjection,
        incumbent_snapshot: RootReplyRiskSnapshot,
        incumbent_projection: &TurnEngineRootProjection,
        config: AutomoveSearchConfig,
    ) -> Option<std::cmp::Ordering> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro) {
            return None;
        }

        let candidate_spirit_impact = matches!(
            candidate_projection.plan.head_family,
            TurnPlanFamily::SpiritImpact
        );
        let incumbent_spirit_impact = matches!(
            incumbent_projection.plan.head_family,
            TurnPlanFamily::SpiritImpact
        );
        if candidate_spirit_impact == incumbent_spirit_impact {
            return None;
        }

        let (
            non_spirit_snapshot,
            non_spirit_projection,
            spirit_snapshot,
            spirit_projection,
            candidate_is_non_spirit,
        ) = if candidate_spirit_impact {
            (
                incumbent_snapshot,
                incumbent_projection,
                candidate_snapshot,
                candidate_projection,
                false,
            )
        } else {
            (
                candidate_snapshot,
                candidate_projection,
                incumbent_snapshot,
                incumbent_projection,
                true,
            )
        };

        let spirit_goal_priority =
            Self::close_spirit_goal_family_priority(spirit_projection.plan.goal_family);
        let non_spirit_goal_priority =
            Self::close_spirit_goal_family_priority(non_spirit_projection.plan.goal_family);
        if spirit_goal_priority
            > Self::close_spirit_goal_family_priority(TurnPlanFamily::SpiritImpact)
            || non_spirit_goal_priority < spirit_goal_priority
        {
            return None;
        }

        const REPLY_FLOOR_MARGIN: i32 = 128;
        if non_spirit_snapshot.worst_reply_score
            < spirit_snapshot
                .worst_reply_score
                .saturating_add(REPLY_FLOOR_MARGIN)
        {
            return None;
        }

        let utility_order = crate::models::automove_turn_engine::compare_utility_primary_axes(
            non_spirit_projection.plan.utility,
            spirit_projection.plan.utility,
        );
        if utility_order == std::cmp::Ordering::Less
            && !non_spirit_projection
                .plan
                .utility
                .supports_family_fallback(spirit_projection.plan.utility)
        {
            return None;
        }

        Some(if candidate_is_non_spirit {
            std::cmp::Ordering::Greater
        } else {
            std::cmp::Ordering::Less
        })
    }

    #[allow(clippy::too_many_arguments)]
    fn is_better_reply_risk_candidate(
        game: &MonsGame,
        candidate_index: usize,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent_index: usize,
        incumbent_snapshot: RootReplyRiskSnapshot,
        candidate_projection: Option<&TurnEngineRootProjection>,
        incumbent_projection: Option<&TurnEngineRootProjection>,
        scored_roots: &[RootEvaluation],
        perspective: Color,
        config: AutomoveSearchConfig,
        spirit_followup_scores: &mut std::collections::HashMap<usize, i32>,
    ) -> bool {
        let candidate = &scored_roots[candidate_index];
        let incumbent = &scored_roots[incumbent_index];

        if candidate.wins_immediately != incumbent.wins_immediately {
            return candidate.wins_immediately;
        }
        if candidate.attacks_opponent_drainer != incumbent.attacks_opponent_drainer {
            return candidate.attacks_opponent_drainer;
        }
        if candidate_snapshot.allows_immediate_opponent_win
            != incumbent_snapshot.allows_immediate_opponent_win
        {
            return !candidate_snapshot.allows_immediate_opponent_win;
        }
        if candidate_snapshot.opponent_reaches_match_point
            != incumbent_snapshot.opponent_reaches_match_point
        {
            return !candidate_snapshot.opponent_reaches_match_point;
        }
        if let Some(order) = Self::current_pro_late_safe_mana_root_order(
            game,
            candidate,
            candidate_snapshot,
            incumbent,
            incumbent_snapshot,
            config,
        ) {
            return order == std::cmp::Ordering::Greater;
        }
        if let Some(order) = Self::current_pro_risky_recovery_progress_sibling_order(
            candidate,
            candidate_snapshot,
            incumbent,
            incumbent_snapshot,
            candidate_projection,
            incumbent_projection,
            config,
        ) {
            return order == std::cmp::Ordering::Greater;
        }
        if let Some(order) = Self::current_pro_safe_progress_sibling_order(
            candidate,
            candidate_snapshot,
            incumbent,
            incumbent_snapshot,
            config,
        ) {
            return order == std::cmp::Ordering::Greater;
        }
        if let Some(order) = Self::current_pro_early_black_mana_progress_reply_order(
            game,
            candidate,
            candidate_snapshot,
            incumbent,
            incumbent_snapshot,
            config,
        ) {
            return order == std::cmp::Ordering::Greater;
        }
        if let Some(order) = Self::current_pro_early_black_plain_spirit_mana_reply_order(
            game,
            scored_roots,
            candidate_index,
            candidate_snapshot,
            incumbent_index,
            incumbent_snapshot,
            perspective,
            config,
            spirit_followup_scores,
        ) {
            return order == std::cmp::Ordering::Greater;
        }
        if let Some(order) = Self::current_pro_safe_non_spirit_followup_order(
            game,
            scored_roots,
            candidate_index,
            candidate_snapshot,
            incumbent_index,
            incumbent_snapshot,
            perspective,
            config,
            spirit_followup_scores,
        ) {
            return order == std::cmp::Ordering::Greater;
        }
        if let Some(order) = Self::current_pro_black_plain_spirit_followup_reply_order(
            game,
            scored_roots,
            candidate_index,
            candidate_snapshot,
            incumbent_index,
            incumbent_snapshot,
            perspective,
            config,
            spirit_followup_scores,
        ) {
            return order == std::cmp::Ordering::Greater;
        }
        if let Some(order) = Self::current_pro_early_black_plain_spirit_sibling_reply_order(
            game,
            scored_roots,
            candidate_index,
            candidate_snapshot,
            incumbent_index,
            incumbent_snapshot,
            perspective,
            config,
            spirit_followup_scores,
        ) {
            return order == std::cmp::Ordering::Greater;
        }
        if let Some(order) = Self::current_pro_white_spirit_followup_setup_reply_order(
            game,
            candidate,
            candidate_snapshot,
            incumbent,
            incumbent_snapshot,
            config,
        ) {
            return order == std::cmp::Ordering::Greater;
        }
        if let Some(order) = Self::current_pro_safe_pickup_reply_order(
            candidate,
            candidate_snapshot,
            incumbent,
            incumbent_snapshot,
            config,
        ) {
            return order == std::cmp::Ordering::Greater;
        }
        if let Some(order) = Self::current_pro_normal_safety_reply_order(
            game,
            candidate,
            candidate_snapshot,
            incumbent,
            incumbent_snapshot,
            perspective,
            config,
        ) {
            return order == std::cmp::Ordering::Greater;
        }
        let candidate_progress_adv =
            candidate.classes.carrier_progress && !incumbent.classes.carrier_progress;
        let incumbent_progress_adv =
            incumbent.classes.carrier_progress && !candidate.classes.carrier_progress;
        if candidate_progress_adv || incumbent_progress_adv {
            let no_tactical_priority = !candidate.classes.is_tactical_priority()
                && !incumbent.classes.is_tactical_priority();
            let tight_reply_floor =
                (candidate_snapshot.worst_reply_score - incumbent_snapshot.worst_reply_score).abs()
                    <= 80;
            if no_tactical_priority && tight_reply_floor {
                return candidate_progress_adv;
            }
        }
        if config.prefer_clean_reply_risk_roots {
            if candidate.mana_handoff_to_opponent != incumbent.mana_handoff_to_opponent {
                return !candidate.mana_handoff_to_opponent;
            }
            if candidate.has_roundtrip != incumbent.has_roundtrip {
                return !candidate.has_roundtrip;
            }
        }
        if Self::is_safe_plain_spirit_reply_risk_pair(
            candidate,
            candidate_snapshot,
            incumbent,
            incumbent_snapshot,
            config,
        ) {
            let close_plain_spirit_scores = (candidate.score - incumbent.score).abs() <= 16
                && candidate.efficiency == incumbent.efficiency;
            if close_plain_spirit_scores {
                if let (Some(candidate_projection), Some(incumbent_projection)) =
                    (candidate_projection, incumbent_projection)
                {
                    if let Some(order) = Self::current_pro_mixed_plain_spirit_reply_floor_order(
                        candidate_snapshot,
                        candidate_projection,
                        incumbent_snapshot,
                        incumbent_projection,
                        config,
                    ) {
                        return order == std::cmp::Ordering::Greater;
                    }
                    if let Some(order) = Self::current_pro_mixed_plain_spirit_projection_order(
                        game,
                        scored_roots,
                        candidate_index,
                        candidate_projection,
                        incumbent_index,
                        incumbent_projection,
                        perspective,
                        config,
                        spirit_followup_scores,
                    ) {
                        return order == std::cmp::Ordering::Greater;
                    }
                    let any_spirit_impact = matches!(
                        candidate_projection.plan.head_family,
                        TurnPlanFamily::SpiritImpact
                    ) || matches!(
                        incumbent_projection.plan.head_family,
                        TurnPlanFamily::SpiritImpact
                    );
                    if any_spirit_impact {
                        let close_spirit_roots = (candidate.score - incumbent.score).abs() <= 192;
                        let engine_order = Self::compare_spirit_root_projection_plans(
                            candidate_projection,
                            incumbent_projection,
                            close_spirit_roots,
                        );
                        if engine_order != std::cmp::Ordering::Equal {
                            return engine_order == std::cmp::Ordering::Greater;
                        }
                    }
                }
            }
            if let Some(order) = Self::current_pro_spirit_followup_floor_order(
                game,
                scored_roots,
                candidate_index,
                incumbent_index,
                perspective,
                config,
                spirit_followup_scores,
            ) {
                if order != std::cmp::Ordering::Equal {
                    return order == std::cmp::Ordering::Greater;
                }
            }
            return Self::compare_ranked_scored_root_indices(
                scored_roots,
                candidate_index,
                incumbent_index,
            ) == std::cmp::Ordering::Less;
        }
        if let Some(order) = Self::current_pro_spirit_followup_floor_order(
            game,
            scored_roots,
            candidate_index,
            incumbent_index,
            perspective,
            config,
            spirit_followup_scores,
        ) {
            if order != std::cmp::Ordering::Equal {
                return order == std::cmp::Ordering::Greater;
            }
        }
        if matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && (candidate_snapshot.worst_reply_score - incumbent_snapshot.worst_reply_score).abs()
                > 240
        {
            return candidate_snapshot.worst_reply_score > incumbent_snapshot.worst_reply_score;
        }
        if matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            && candidate.interview_soft_priority != incumbent.interview_soft_priority
            && candidate.efficiency >= incumbent.efficiency
        {
            return candidate.interview_soft_priority > incumbent.interview_soft_priority;
        }
        if config.enable_interview_deterministic_tiebreak
            && candidate.spirit_own_mana_setup_now != incumbent.spirit_own_mana_setup_now
        {
            let same_opening_safe_setup_pair =
                Self::is_same_opening_safe_setup_pair(candidate, incumbent, config);
            if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                || same_opening_safe_setup_pair
                || (candidate_snapshot.worst_reply_score - incumbent_snapshot.worst_reply_score)
                    .abs()
                    <= 120
            {
                return candidate.spirit_own_mana_setup_now;
            }
        }
        if config.enable_interview_deterministic_tiebreak
            && candidate.spirit_own_mana_setup_now
            && incumbent.spirit_own_mana_setup_now
            && candidate.supermana_progress
            && incumbent.supermana_progress
            && candidate.safe_supermana_progress_steps != incumbent.safe_supermana_progress_steps
        {
            return Self::root_progress_steps_better(
                candidate.safe_supermana_progress_steps,
                incumbent.safe_supermana_progress_steps,
            );
        }
        if config.enable_interview_deterministic_tiebreak
            && candidate.spirit_own_mana_setup_now
            && incumbent.spirit_own_mana_setup_now
            && candidate.opponent_mana_progress
            && incumbent.opponent_mana_progress
            && candidate.safe_opponent_mana_progress_steps
                != incumbent.safe_opponent_mana_progress_steps
        {
            return Self::root_progress_steps_better(
                candidate.safe_opponent_mana_progress_steps,
                incumbent.safe_opponent_mana_progress_steps,
            );
        }
        if config.enable_interview_deterministic_tiebreak
            && candidate.spirit_own_mana_setup_now
            && incumbent.spirit_own_mana_setup_now
            && candidate.score_path_best_steps != incumbent.score_path_best_steps
        {
            return Self::root_score_path_steps_better(
                candidate.score_path_best_steps,
                incumbent.score_path_best_steps,
            );
        }
        if config.enable_interview_deterministic_tiebreak
            && candidate.spirit_development != incumbent.spirit_development
        {
            if let Some(order) = Self::current_pro_spirit_projection_challenge_order(
                candidate,
                candidate_projection,
                incumbent,
                incumbent_projection,
            ) {
                return order == std::cmp::Ordering::Greater;
            }
            if let Some(order) =
                Self::current_pro_spirit_score_challenge_order(candidate, incumbent)
            {
                if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                    || (candidate_snapshot.worst_reply_score - incumbent_snapshot.worst_reply_score)
                        .abs()
                        <= 120
                {
                    return order == std::cmp::Ordering::Greater;
                }
            }
            if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                || (candidate_snapshot.worst_reply_score - incumbent_snapshot.worst_reply_score)
                    .abs()
                    <= 120
            {
                return candidate.spirit_development;
            }
        }
        if let (Some(candidate_projection), Some(incumbent_projection)) =
            (candidate_projection, incumbent_projection)
        {
            let reply_floor_close =
                (candidate_snapshot.worst_reply_score - incumbent_snapshot.worst_reply_score).abs()
                    <= 100;
            let spirit_reply_floor_close = reply_floor_close;
            let no_terminal_risk = !candidate_snapshot.allows_immediate_opponent_win
                && !incumbent_snapshot.allows_immediate_opponent_win
                && !candidate_snapshot.opponent_reaches_match_point
                && !incumbent_snapshot.opponent_reaches_match_point;
            let tactical_projection =
                Self::is_tactical_turn_engine_family(candidate_projection.plan.head_family)
                    || Self::is_tactical_turn_engine_family(incumbent_projection.plan.head_family);
            let spirit_phase = candidate.spirit_own_mana_setup_now
                || incumbent.spirit_own_mana_setup_now
                || candidate.spirit_same_turn_score_setup_now
                || incumbent.spirit_same_turn_score_setup_now
                || candidate.spirit_development
                || incumbent.spirit_development;
            let plain_spirit_development_projection =
                matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                    && Self::is_plain_spirit_development_root(candidate)
                    && Self::is_plain_spirit_development_root(incumbent)
                    && spirit_reply_floor_close
                    && candidate_projection
                        .plan
                        .utility
                        .has_nonnegative_deny_gain()
                    && incumbent_projection
                        .plan
                        .utility
                        .has_nonnegative_deny_gain()
                    && no_terminal_risk;
            if plain_spirit_development_projection {
                let close_spirit_roots = (candidate.score - incumbent.score).abs() <= 192;
                let engine_order = Self::compare_spirit_root_projection_plans(
                    candidate_projection,
                    incumbent_projection,
                    close_spirit_roots,
                );
                if engine_order != std::cmp::Ordering::Equal {
                    return engine_order == std::cmp::Ordering::Greater;
                }
            }
            let spirit_development_projection =
                matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
                    && candidate.spirit_development
                    && incumbent.spirit_development
                    && !candidate.spirit_same_turn_score_setup_now
                    && !incumbent.spirit_same_turn_score_setup_now
                    && !candidate.spirit_own_mana_setup_now
                    && !incumbent.spirit_own_mana_setup_now
                    && spirit_reply_floor_close
                    && no_terminal_risk;
            if spirit_development_projection {
                let close_spirit_roots = (candidate.score - incumbent.score).abs() <= 192;
                let engine_order = Self::compare_spirit_root_projection_plans(
                    candidate_projection,
                    incumbent_projection,
                    close_spirit_roots,
                );
                if engine_order != std::cmp::Ordering::Equal {
                    return engine_order == std::cmp::Ordering::Greater;
                }
            }
            if tactical_projection && no_terminal_risk && reply_floor_close && !spirit_phase {
                let engine_order = turn_engine_compare_plans(
                    &candidate_projection.plan,
                    &incumbent_projection.plan,
                );
                if engine_order != std::cmp::Ordering::Equal {
                    return engine_order == std::cmp::Ordering::Greater;
                }
            }
        }
        if candidate.interview_soft_priority != incumbent.interview_soft_priority {
            return candidate.interview_soft_priority > incumbent.interview_soft_priority;
        }
        if candidate_snapshot.worst_reply_score != incumbent_snapshot.worst_reply_score {
            return candidate_snapshot.worst_reply_score > incumbent_snapshot.worst_reply_score;
        }
        if candidate.score != incumbent.score {
            return candidate.score > incumbent.score;
        }
        if candidate.efficiency != incumbent.efficiency {
            return candidate.efficiency > incumbent.efficiency;
        }
        false
    }

    fn is_flat_late_mana_only_reply_root(root: &RootEvaluation) -> bool {
        !root.wins_immediately
            && !root.attacks_opponent_drainer
            && !root.classes.is_tactical_priority()
            && !root.spirit_development
            && !root.spirit_same_turn_score_setup_now
            && !root.spirit_own_mana_setup_now
            && !root.supermana_progress
            && !root.opponent_mana_progress
            && !root.scores_supermana_this_turn
            && !root.scores_opponent_mana_this_turn
            && !root.safe_supermana_pickup_now
            && !root.safe_opponent_mana_pickup_now
            && root.same_turn_score_window_value <= 0
            && !root.mana_handoff_to_opponent
            && !root.has_roundtrip
    }

    fn current_pro_late_safe_mana_root_order(
        game: &MonsGame,
        candidate: &RootEvaluation,
        candidate_snapshot: RootReplyRiskSnapshot,
        incumbent: &RootEvaluation,
        incumbent_snapshot: RootReplyRiskSnapshot,
        config: AutomoveSearchConfig,
    ) -> Option<std::cmp::Ordering> {
        if !matches!(config.turn_engine_mode, TurnEngineMode::CurrentPro)
            || !config.enable_turn_engine_late_safe_mana_root_preference
            || !Self::current_pro_is_late_white_mana_only_turn_start(game)
        {
            return None;
        }
        if candidate_snapshot.allows_immediate_opponent_win
            || incumbent_snapshot.allows_immediate_opponent_win
            || candidate_snapshot.opponent_reaches_match_point
            || incumbent_snapshot.opponent_reaches_match_point
        {
            return None;
        }
        if !Self::is_flat_late_mana_only_reply_root(candidate)
            || !Self::is_flat_late_mana_only_reply_root(incumbent)
        {
            return None;
        }

        let (safe_root, vulnerable_root, safe_is_candidate) =
            if !candidate.own_drainer_vulnerable && incumbent.own_drainer_vulnerable {
                (candidate, incumbent, true)
            } else if candidate.own_drainer_vulnerable && !incumbent.own_drainer_vulnerable {
                (incumbent, candidate, false)
            } else {
                return None;
            };

        if vulnerable_root.score <= safe_root.score
            || vulnerable_root.score.saturating_sub(safe_root.score) > 16
            || vulnerable_root.root_rank.abs_diff(safe_root.root_rank) > 4
            || vulnerable_root.efficiency <= safe_root.efficiency
        {
            return None;
        }

        Some(if safe_is_candidate {
            std::cmp::Ordering::Greater
        } else {
            std::cmp::Ordering::Less
        })
    }

    #[cfg(test)]
    fn pick_root_move_with_normal_safety(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_indices: &[usize],
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> Vec<Input> {
        if scored_roots.is_empty() {
            return Vec::new();
        }

        let eligible_indices = if candidate_indices.is_empty() {
            (0..scored_roots.len()).collect::<Vec<_>>()
        } else {
            candidate_indices.to_vec()
        };
        let best_score = eligible_indices
            .iter()
            .map(|index| scored_roots[*index].score)
            .max()
            .unwrap_or(i32::MIN);
        let score_margin = SMART_NORMAL_ROOT_SAFETY_SCORE_MARGIN.max(0);
        let reply_limit = config.node_enum_limit.clamp(
            SMART_NORMAL_ROOT_SAFETY_REPLY_LIMIT_MIN,
            SMART_NORMAL_ROOT_SAFETY_REPLY_LIMIT_MAX,
        );
        let my_score_before = if perspective == Color::White {
            game.white_score
        } else {
            game.black_score
        };
        let shortlist_limit = SMART_NORMAL_ROOT_SAFETY_SHORTLIST_MAX.max(1);

        let mut shortlist = eligible_indices
            .iter()
            .copied()
            .filter(|index| scored_roots[*index].score + score_margin >= best_score)
            .collect::<Vec<_>>();
        shortlist.sort_by(|a, b| Self::compare_ranked_scored_root_indices(scored_roots, *a, *b));
        if shortlist.len() > shortlist_limit {
            shortlist.truncate(shortlist_limit);
        }

        if shortlist.is_empty() {
            let best_index =
                Self::best_scored_root_index(scored_roots, eligible_indices.as_slice());
            return scored_roots[best_index].inputs.clone();
        }

        let best_scored_index = shortlist[0];
        if shortlist.len() < 2 {
            return scored_roots[best_scored_index].inputs.clone();
        }

        let shortlist_indices = shortlist;
        let start_options = Self::automove_start_input_options();

        let mut best_index = best_scored_index;
        let mut best_snapshot = Self::normal_root_safety_snapshot(
            &scored_roots[best_scored_index].game,
            perspective,
            my_score_before,
            config,
            reply_limit,
            start_options,
        );

        for index in shortlist_indices.iter().copied().skip(1) {
            let evaluation = &scored_roots[index];
            let snapshot = Self::normal_root_safety_snapshot(
                &evaluation.game,
                perspective,
                my_score_before,
                config,
                reply_limit,
                start_options,
            );
            if Self::is_better_normal_root_safety_candidate(
                snapshot,
                evaluation.score,
                best_snapshot,
                scored_roots[best_index].score,
            ) {
                best_index = index;
                best_snapshot = snapshot;
            }
        }

        let selected_index = if config.enable_normal_root_safety_deep_floor
            && Self::should_apply_normal_root_safety_deep_floor(game, perspective)
        {
            Self::pick_normal_root_with_deep_floor(
                scored_roots,
                shortlist_indices.as_slice(),
                best_index,
                perspective,
                config,
            )
        } else {
            best_index
        };

        scored_roots[selected_index].inputs.clone()
    }

    fn normal_root_safety_snapshot(
        state_after_move: &MonsGame,
        perspective: Color,
        my_score_before: i32,
        config: AutomoveSearchConfig,
        reply_limit: usize,
        start_options: SuggestedStartInputOptions,
    ) -> NormalRootSafetySnapshot {
        let my_score_after_move = if perspective == Color::White {
            state_after_move.white_score
        } else {
            state_after_move.black_score
        };
        let my_score_gain = (my_score_after_move - my_score_before).max(0);

        if let Some(winner) = state_after_move.winner_color() {
            return if winner == perspective {
                NormalRootSafetySnapshot {
                    allows_immediate_opponent_win: false,
                    opponent_reaches_match_point: false,
                    opponent_max_score_gain: 0,
                    my_score_gain,
                    worst_reply_score: SMART_TERMINAL_SCORE / 2,
                }
            } else {
                NormalRootSafetySnapshot {
                    allows_immediate_opponent_win: true,
                    opponent_reaches_match_point: true,
                    opponent_max_score_gain: Config::TARGET_SCORE,
                    my_score_gain,
                    worst_reply_score: -SMART_TERMINAL_SCORE / 2,
                }
            };
        }

        if state_after_move.active_color == perspective {
            return NormalRootSafetySnapshot {
                allows_immediate_opponent_win: false,
                opponent_reaches_match_point: false,
                opponent_max_score_gain: 0,
                my_score_gain,
                worst_reply_score: Self::evaluate_search_preferability(
                    state_after_move,
                    perspective,
                    config,
                ),
            };
        }

        let opponent_score_before = if perspective == Color::White {
            state_after_move.black_score
        } else {
            state_after_move.white_score
        };
        let replies =
            Self::enumerate_legal_transitions(state_after_move, reply_limit.max(1), start_options);
        if replies.is_empty() {
            return NormalRootSafetySnapshot {
                allows_immediate_opponent_win: false,
                opponent_reaches_match_point: false,
                opponent_max_score_gain: 0,
                my_score_gain,
                worst_reply_score: SMART_TERMINAL_SCORE / 4,
            };
        }

        let mut allows_immediate_opponent_win = false;
        let mut opponent_reaches_match_point = false;
        let mut opponent_max_score_gain = 0i32;
        let mut worst_reply_score = i32::MAX;
        let mut evaluated_reply = false;

        for reply in replies {
            let after_reply = reply.game;
            evaluated_reply = true;

            let opponent_score_after = if perspective == Color::White {
                after_reply.black_score
            } else {
                after_reply.white_score
            };
            let score_gain = (opponent_score_after - opponent_score_before).max(0);
            opponent_max_score_gain = opponent_max_score_gain.max(score_gain);
            if Config::TARGET_SCORE - opponent_score_after <= 1 {
                opponent_reaches_match_point = true;
            }

            let reply_score = match after_reply.winner_color() {
                Some(winner) if winner == perspective => SMART_TERMINAL_SCORE / 2,
                Some(_) => {
                    allows_immediate_opponent_win = true;
                    opponent_reaches_match_point = true;
                    -SMART_TERMINAL_SCORE / 2
                }
                None => Self::evaluate_search_preferability(&after_reply, perspective, config),
            };
            worst_reply_score = worst_reply_score.min(reply_score);

            if allows_immediate_opponent_win {
                break;
            }
        }

        if !evaluated_reply || worst_reply_score == i32::MAX {
            worst_reply_score =
                Self::evaluate_search_preferability(state_after_move, perspective, config);
        }

        NormalRootSafetySnapshot {
            allows_immediate_opponent_win,
            opponent_reaches_match_point,
            opponent_max_score_gain,
            my_score_gain,
            worst_reply_score,
        }
    }

    fn should_apply_normal_root_safety_deep_floor(game: &MonsGame, perspective: Color) -> bool {
        let (my_score, opponent_score) = if perspective == Color::White {
            (game.white_score, game.black_score)
        } else {
            (game.black_score, game.white_score)
        };
        let my_distance_to_win = Config::TARGET_SCORE - my_score;
        let opponent_distance_to_win = Config::TARGET_SCORE - opponent_score;
        my_distance_to_win <= SMART_NORMAL_ROOT_SAFETY_DEEP_FLOOR_SCORE_RACE_TRIGGER
            || opponent_distance_to_win <= SMART_NORMAL_ROOT_SAFETY_DEEP_FLOOR_SCORE_RACE_TRIGGER
    }

    #[cfg(test)]
    fn pick_normal_root_with_deep_floor(
        scored_roots: &[RootEvaluation],
        shortlist_indices: &[usize],
        selected_index: usize,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> usize {
        if shortlist_indices.len() < 2 {
            return selected_index;
        }

        let shortlist_best_score = shortlist_indices
            .iter()
            .map(|index| scored_roots[*index].score)
            .max()
            .unwrap_or(i32::MIN);
        let margin = SMART_NORMAL_ROOT_SAFETY_DEEP_FLOOR_SCORE_MARGIN.max(0);
        let max_candidates = SMART_NORMAL_ROOT_SAFETY_DEEP_FLOOR_MAX_CANDIDATES.max(1);
        let reply_limit = config.node_enum_limit.clamp(
            SMART_NORMAL_ROOT_SAFETY_DEEP_FLOOR_REPLY_LIMIT_MIN,
            SMART_NORMAL_ROOT_SAFETY_DEEP_FLOOR_REPLY_LIMIT_MAX,
        );

        let mut candidate_indices = shortlist_indices
            .iter()
            .copied()
            .filter(|index| scored_roots[*index].score + margin >= shortlist_best_score)
            .collect::<Vec<_>>();
        if candidate_indices.len() > max_candidates {
            candidate_indices.truncate(max_candidates);
        }
        if !candidate_indices.contains(&selected_index) {
            candidate_indices.push(selected_index);
        }
        if candidate_indices.len() < 2 {
            return selected_index;
        }

        let mut best_index = selected_index;
        let mut best_floor_score = i32::MIN;
        for index in candidate_indices {
            let floor_score = Self::normal_root_safety_deep_floor_score(
                &scored_roots[index].game,
                perspective,
                config,
                reply_limit,
            );
            if floor_score > best_floor_score
                || (floor_score == best_floor_score
                    && Self::compare_ranked_scored_root_indices(scored_roots, index, best_index)
                        == std::cmp::Ordering::Less)
            {
                best_floor_score = floor_score;
                best_index = index;
            }
        }

        best_index
    }

    fn normal_root_safety_deep_floor_score(
        state_after_move: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
        reply_limit: usize,
    ) -> i32 {
        if crate::models::automove_deadline::checkpoint() {
            return 0;
        }
        if let Some(winner) = state_after_move.winner_color() {
            return if winner == perspective {
                SMART_TERMINAL_SCORE / 2
            } else {
                -SMART_TERMINAL_SCORE / 2
            };
        }
        if state_after_move.active_color == perspective {
            return Self::evaluate_search_preferability(state_after_move, perspective, config);
        }

        let mut probe = config;
        probe.depth = 1;
        probe.max_visited_nodes = (config.max_visited_nodes / 18).clamp(110, 360);
        probe.root_branch_limit = config.node_branch_limit.clamp(5, 12);
        probe.node_branch_limit = config.node_branch_limit.saturating_sub(4).clamp(4, 10);
        probe.root_enum_limit = (probe.root_branch_limit * 3).clamp(probe.root_branch_limit, 48);
        probe.node_enum_limit = (probe.node_branch_limit * 3).clamp(probe.node_branch_limit, 36);

        let replies = Self::enumerate_legal_transitions(
            state_after_move,
            reply_limit.max(1),
            Self::automove_start_input_options(),
        );
        if crate::models::automove_deadline::cancelled() {
            return 0;
        }
        if replies.is_empty() {
            return SMART_TERMINAL_SCORE / 4;
        }

        let mut worst = i32::MAX;
        for reply in replies {
            if crate::models::automove_deadline::checkpoint() {
                return 0;
            }
            let after_reply = reply.game;
            let score = match after_reply.winner_color() {
                Some(winner) if winner == perspective => SMART_TERMINAL_SCORE / 2,
                Some(_) => -SMART_TERMINAL_SCORE / 2,
                None => {
                    let mut visited_nodes = 0usize;
                    let mut transposition_table = U64HashMap::<TranspositionEntry>::default();
                    let mut probe_extension_nodes_used = 0usize;
                    let mut probe_quiescence_nodes_used = 0usize;
                    Self::search_score(
                        &after_reply,
                        perspective,
                        1,
                        i32::MIN,
                        i32::MAX,
                        &mut visited_nodes,
                        probe,
                        &mut transposition_table,
                        0,
                        &mut probe_extension_nodes_used,
                        0,
                        true,
                        &mut probe_quiescence_nodes_used,
                    )
                }
            };
            if crate::models::automove_deadline::cancelled() {
                return 0;
            }
            worst = worst.min(score);
        }

        if worst == i32::MAX {
            Self::evaluate_search_preferability(state_after_move, perspective, config)
        } else {
            worst
        }
    }

    fn current_pro_spirit_followup_probe_config(
        mut config: AutomoveSearchConfig,
    ) -> AutomoveSearchConfig {
        config.enable_turn_engine_selector = false;
        config.turn_engine_mode = TurnEngineMode::ProV1;
        config.enable_turn_head_rerank = false;
        config.enable_root_reply_risk_guard = false;
        config.enable_root_spirit_development_pref = false;
        config.enable_normal_root_safety_deep_floor = false;
        config.depth = config.depth.clamp(1, 3);
        config.max_visited_nodes = (config.max_visited_nodes / 8).clamp(300, 1400);
        config.root_branch_limit = config.node_branch_limit.clamp(5, 10);
        config.node_branch_limit = config.node_branch_limit.saturating_sub(4).clamp(4, 8);
        config.root_enum_limit = (config.root_branch_limit * 3).clamp(config.root_branch_limit, 36);
        config.node_enum_limit = (config.node_branch_limit * 3).clamp(config.node_branch_limit, 24);
        config
    }

    fn current_pro_spirit_followup_floor_score(
        state_after_move: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> i32 {
        if crate::models::automove_deadline::checkpoint() {
            return 0;
        }
        let cache_key = TurnEngineSelectorFollowupFloorCacheKey {
            state_hash: Self::search_state_hash(state_after_move),
            perspective,
        };
        if let Some(cached) = TURN_ENGINE_SELECTOR_FOLLOWUP_FLOOR_CACHE
            .with(|cache| cache.borrow().get(&cache_key).copied())
        {
            return cached;
        }

        #[cfg(test)]
        update_turn_engine_selector_diagnostics(|diagnostics| {
            diagnostics.followup_floor_calls += 1
        });
        if let Some(winner) = state_after_move.winner_color() {
            let result = if winner == perspective {
                SMART_TERMINAL_SCORE / 2
            } else {
                -SMART_TERMINAL_SCORE / 2
            };
            if crate::models::automove_deadline::cache_write_allowed() {
                TURN_ENGINE_SELECTOR_FOLLOWUP_FLOOR_CACHE.with(|cache| {
                    cache.borrow_mut().insert(cache_key, result);
                });
            }
            return result;
        }

        if config.enable_turn_engine_low_budget_guard {
            let probe = Self::current_pro_spirit_followup_probe_config(config);
            let result = Self::evaluate_search_preferability(state_after_move, perspective, probe);
            if crate::models::automove_deadline::cache_write_allowed() {
                TURN_ENGINE_SELECTOR_FOLLOWUP_FLOOR_CACHE.with(|cache| {
                    cache.borrow_mut().insert(cache_key, result);
                });
            }
            return result;
        }

        let probe = Self::current_pro_spirit_followup_probe_config(config);
        let mut continuation = state_after_move.clone_for_simulation();
        let max_rollout_plies = if state_after_move.turn_number <= 1 {
            10
        } else {
            6
        };
        let mut rollout_plies = 0usize;

        while continuation.winner_color().is_none() && rollout_plies < max_rollout_plies {
            if crate::models::automove_deadline::checkpoint() {
                return 0;
            }
            let inputs = Self::smart_search_best_inputs_internal(&continuation, probe, true);
            if crate::models::automove_deadline::cancelled() {
                return 0;
            }
            if inputs.is_empty() {
                break;
            }
            continuation.process_input(inputs, false, false);
            rollout_plies += 1;
        }

        let result = if let Some(winner) = continuation.winner_color() {
            if winner == perspective {
                SMART_TERMINAL_SCORE / 2
            } else {
                -SMART_TERMINAL_SCORE / 2
            }
        } else {
            Self::evaluate_search_preferability(&continuation, perspective, probe)
        };
        if crate::models::automove_deadline::cache_write_allowed() {
            TURN_ENGINE_SELECTOR_FOLLOWUP_FLOOR_CACHE.with(|cache| {
                cache.borrow_mut().insert(cache_key, result);
            });
        }
        result
    }

    fn current_pro_spirit_followup_floor_order(
        game: &MonsGame,
        scored_roots: &[RootEvaluation],
        candidate_index: usize,
        incumbent_index: usize,
        perspective: Color,
        config: AutomoveSearchConfig,
        cached_scores: &mut std::collections::HashMap<usize, i32>,
    ) -> Option<std::cmp::Ordering> {
        if !Self::current_pro_secondary_analysis_live(config)
            || game.turn_number > 3
            || !Self::is_plain_spirit_development_root(&scored_roots[candidate_index])
            || !Self::is_plain_spirit_development_root(&scored_roots[incumbent_index])
        {
            return None;
        }

        let candidate = &scored_roots[candidate_index];
        let incumbent = &scored_roots[incumbent_index];
        if Self::turn_engine_root_evaluation_is_unsafe(candidate)
            || Self::turn_engine_root_evaluation_is_unsafe(incumbent)
            || candidate.mana_handoff_to_opponent
            || incumbent.mana_handoff_to_opponent
            || candidate.has_roundtrip
            || incumbent.has_roundtrip
            || candidate.supermana_progress
            || candidate.opponent_mana_progress
            || incumbent.supermana_progress
            || incumbent.opponent_mana_progress
            || candidate.same_turn_score_window_value > 0
            || incumbent.same_turn_score_window_value > 0
            || candidate.spirit_same_turn_score_setup_now
            || incumbent.spirit_same_turn_score_setup_now
            || candidate.spirit_own_mana_setup_now
            || incumbent.spirit_own_mana_setup_now
            || (candidate.score - incumbent.score).abs() > 224
        {
            return None;
        }

        let candidate_score = *cached_scores.entry(candidate_index).or_insert_with(|| {
            Self::current_pro_spirit_followup_floor_score(&candidate.game, perspective, config)
        });
        let incumbent_score = *cached_scores.entry(incumbent_index).or_insert_with(|| {
            Self::current_pro_spirit_followup_floor_score(&incumbent.game, perspective, config)
        });

        const FLOOR_MARGIN: i32 = 32;
        if candidate_score >= incumbent_score.saturating_add(FLOOR_MARGIN) {
            Some(std::cmp::Ordering::Greater)
        } else if incumbent_score >= candidate_score.saturating_add(FLOOR_MARGIN) {
            Some(std::cmp::Ordering::Less)
        } else {
            Some(std::cmp::Ordering::Equal)
        }
    }

    fn evaluate_search_preferability(
        game: &MonsGame,
        perspective: Color,
        config: AutomoveSearchConfig,
    ) -> i32 {
        Self::cached_search_preferability_score(game, perspective, config.scoring_weights)
    }

    fn cached_search_preferability_score(
        game: &MonsGame,
        perspective: Color,
        scoring_weights: &'static ScoringWeights,
    ) -> i32 {
        Self::cached_search_preferability_score_with_hash(
            game,
            perspective,
            scoring_weights,
            Self::search_state_hash(game),
        )
    }

    fn cached_search_preferability_score_with_hash(
        game: &MonsGame,
        perspective: Color,
        scoring_weights: &'static ScoringWeights,
        state_hash: u64,
    ) -> i32 {
        let cache_key = SearchPreferabilityCacheKey {
            state_hash,
            perspective,
            scoring_weights_key: scoring_weights as *const ScoringWeights as usize as u64,
        };
        if let Some(cached) =
            SEARCH_PREFERABILITY_CACHE.with(|cache| cache.borrow().get(&cache_key).copied())
        {
            #[cfg(test)]
            update_turn_engine_selector_diagnostics(|diagnostics| {
                diagnostics.search_preferability_cache_hits += 1;
            });
            return cached;
        }

        #[cfg(test)]
        update_turn_engine_selector_diagnostics(|diagnostics| {
            diagnostics.search_preferability_builds += 1;
        });
        let score = evaluate_preferability_with_weights_and_exact_policy(
            game,
            perspective,
            scoring_weights,
            false,
        );
        if crate::models::automove_deadline::cache_write_allowed() {
            SEARCH_PREFERABILITY_CACHE.with(|cache| {
                let mut cache = cache.borrow_mut();
                if cache.len() >= SMART_SEARCH_PREFERABILITY_CACHE_MAX_ENTRIES
                    && !cache.contains_key(&cache_key)
                {
                    cache.clear();
                }
                cache.insert(cache_key, score);
            });
        }
        score
    }

    fn is_better_normal_root_safety_candidate(
        candidate_snapshot: NormalRootSafetySnapshot,
        candidate_score: i32,
        incumbent_snapshot: NormalRootSafetySnapshot,
        incumbent_score: i32,
    ) -> bool {
        if candidate_snapshot.allows_immediate_opponent_win
            != incumbent_snapshot.allows_immediate_opponent_win
        {
            return !candidate_snapshot.allows_immediate_opponent_win;
        }
        if candidate_snapshot.opponent_reaches_match_point
            != incumbent_snapshot.opponent_reaches_match_point
        {
            return !candidate_snapshot.opponent_reaches_match_point;
        }
        if candidate_snapshot.opponent_max_score_gain != incumbent_snapshot.opponent_max_score_gain
        {
            return candidate_snapshot.opponent_max_score_gain
                < incumbent_snapshot.opponent_max_score_gain;
        }
        if candidate_snapshot.my_score_gain != incumbent_snapshot.my_score_gain {
            return candidate_snapshot.my_score_gain > incumbent_snapshot.my_score_gain;
        }
        if candidate_snapshot.worst_reply_score != incumbent_snapshot.worst_reply_score {
            return candidate_snapshot.worst_reply_score > incumbent_snapshot.worst_reply_score;
        }
        candidate_score > incumbent_score
    }
}

#[cfg(test)]
mod evaluation_cache_tests {
    use super::*;

    fn game_with_items(items: Vec<(Location, Item)>, active_color: Color) -> MonsGame {
        let mut game = MonsGame::new(false, GameVariant::Classic);
        game.replace_board_items(items);
        game.active_color = active_color;
        game.turn_number = 2;
        game.actions_used_count = 0;
        game.mana_moves_count = 0;
        game.mons_moves_count = 0;
        game.white_score = 0;
        game.black_score = 0;
        game.white_potions_count = 0;
        game.black_potions_count = 0;
        game
    }

    #[test]
    fn automove_deadline_discards_partial_enumeration_and_root_ranking() {
        let game = MonsGame::new(false, GameVariant::Classic);
        crate::models::automove_deadline::with_test_clock(0.0, || {
            crate::models::automove_deadline::with_deadline_if_absent(0.0, || {
                let transitions = MonsGameModel::enumerate_legal_transitions(
                    &game,
                    64,
                    SuggestedStartInputOptions::default(),
                );
                assert!(crate::models::automove_deadline::cancelled());
                assert!(transitions.is_empty());

                let roots = MonsGameModel::ranked_root_moves(
                    &game,
                    game.active_color,
                    AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
                );
                assert!(roots.is_empty());
            });
        });
    }

    #[test]
    fn automove_deadline_blocks_long_lived_evaluation_cache_writes() {
        clear_turn_engine_selector_followup_floor_cache();
        let game = MonsGame::new(false, GameVariant::Classic);
        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        crate::models::automove_deadline::with_test_clock(0.0, || {
            crate::models::automove_deadline::with_deadline_if_absent(0.0, || {
                let _ =
                    MonsGameModel::move_efficiency_snapshot(&game, game.active_color, false, false);
                let _ = MonsGameModel::cached_search_preferability_score(
                    &game,
                    game.active_color,
                    config.scoring_weights,
                );
            });
        });
        assert_eq!(move_efficiency_snapshot_cache_len(), 0);
        assert_eq!(search_preferability_cache_len(), 0);
    }

    #[test]
    fn move_efficiency_snapshot_cache_reuses_repeated_state() {
        clear_turn_engine_selector_followup_floor_cache();
        let game = game_with_items(
            vec![
                (
                    Location::new(6, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(8, 8),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
        );

        let first = MonsGameModel::move_efficiency_snapshot(&game, Color::White, true, true);
        let second = MonsGameModel::move_efficiency_snapshot(&game, Color::White, true, true);

        assert_eq!(move_efficiency_snapshot_cache_len(), 1);
        assert_eq!(
            first.my_best_drainer_to_mana_steps,
            second.my_best_drainer_to_mana_steps
        );
        assert_eq!(
            first.my_same_turn_score_value,
            second.my_same_turn_score_value
        );
    }

    #[test]
    fn search_preferability_cache_reuses_repeated_state() {
        clear_turn_engine_selector_followup_floor_cache();
        let game = game_with_items(
            vec![
                (
                    Location::new(9, 1),
                    Item::MonWithMana {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                        mana: Mana::Regular(Color::White),
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
        );
        let config =
            MonsGameModel::shipping_search_config_for_game(&game, SmartAutomovePreference::Pro);

        let first = MonsGameModel::evaluate_search_preferability(&game, Color::White, config);
        let second = MonsGameModel::evaluate_search_preferability(&game, Color::White, config);

        assert_eq!(search_preferability_cache_len(), 1);
        assert_eq!(first, second);
    }

    #[test]
    fn move_efficiency_snapshot_tactical_projection_matches_exact_summary() {
        clear_turn_engine_selector_followup_floor_cache();
        clear_exact_state_analysis_cache();
        clear_exact_query_diagnostics();
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 1),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
            ],
            Color::White,
        );

        let snapshot = MonsGameModel::move_efficiency_snapshot(&game, Color::White, true, true);
        let diagnostics = exact_query_diagnostics_snapshot();
        let turn = exact_turn_summary(&game, Color::White);
        let projection = crate::models::automove_exact::exact_turn_tactical_projection(
            &game,
            Color::White,
            MonsGameModel::move_efficiency_tactical_projection_flags(),
        );

        assert_eq!(diagnostics.exact_turn_summary_builds, 0);
        assert_eq!(
            snapshot.my_same_turn_score_value,
            turn.spirit_assisted_score_value
        );
        assert_eq!(
            snapshot.my_same_turn_opponent_mana_score_value,
            projection.spirit_assisted_denial_value
        );
        assert_eq!(
            snapshot.my_safe_supermana_progress,
            turn.safe_supermana_progress
        );
        assert_eq!(
            snapshot.my_safe_supermana_progress_steps,
            turn.safe_supermana_progress_steps
                .unwrap_or(Config::BOARD_SIZE + 4)
        );
        assert_eq!(
            snapshot.my_safe_opponent_mana_progress,
            turn.safe_opponent_mana_progress
        );
        assert_eq!(
            snapshot.my_safe_opponent_mana_progress_steps,
            turn.safe_opponent_mana_progress_steps
                .unwrap_or(Config::BOARD_SIZE + 4)
        );
    }

    #[test]
    fn move_efficiency_delta_after_snapshot_helper_matches_existing_path() {
        clear_turn_engine_selector_followup_floor_cache();
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(6, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
        );
        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        let transition = MonsGameModel::enumerate_legal_transitions(
            &game,
            config.node_enum_limit,
            MonsGameModel::automove_start_input_options(),
        )
        .into_iter()
        .find(|transition| !transition.events.is_empty())
        .expect("expected at least one legal transition");
        let child_hash = MonsGameModel::search_state_hash(&transition.game);
        let before_hash = MonsGameModel::search_state_hash(&game);
        let before = MonsGameModel::move_efficiency_snapshot_with_hash(
            &game,
            Color::White,
            false,
            false,
            before_hash,
        );
        let after = MonsGameModel::move_efficiency_snapshot_uncached_with_hash(
            &transition.game,
            Color::White,
            false,
            false,
            child_hash,
        );

        let baseline = MonsGameModel::move_efficiency_delta_from_before_snapshot(
            &game,
            &transition.game,
            Color::White,
            transition.events.as_slice(),
            before,
            child_hash,
            false,
            false,
            false,
            false,
            false,
            config.root_backtrack_penalty,
            config.root_mana_handoff_penalty,
        );
        let reused = MonsGameModel::move_efficiency_delta_from_before_snapshot_with_after_snapshot(
            &game,
            &transition.game,
            Color::White,
            transition.events.as_slice(),
            before,
            after,
            false,
            false,
            false,
            config.root_backtrack_penalty,
            config.root_mana_handoff_penalty,
        );

        assert_eq!(baseline, reused);
    }
}

#[cfg(test)]
mod smart_automove_tests {
    use super::*;
    use rand::rngs::StdRng;
    use rand::{Rng, SeedableRng};
    use std::collections::HashSet;

    fn game_with_items(
        items: Vec<(Location, Item)>,
        active_color: Color,
        turn_number: i32,
    ) -> MonsGame {
        let mut game = MonsGame::new(false, GameVariant::Classic);
        game.replace_board_items(items);
        game.active_color = active_color;
        game.turn_number = turn_number;
        game.actions_used_count = 0;
        game.mana_moves_count = 0;
        game.mons_moves_count = 0;
        game.white_score = 0;
        game.black_score = 0;
        game.white_potions_count = 0;
        game.black_potions_count = 0;
        game
    }

    fn apply_seeded_runtime_move(game: &mut MonsGame, rng: &mut StdRng) -> bool {
        let legal_inputs =
            MonsGameModel::enumerate_legal_inputs(game, 256, SuggestedStartInputOptions::default());
        if legal_inputs.is_empty() {
            return false;
        }
        let random_index = rng.gen_range(0..legal_inputs.len());
        matches!(
            game.process_input(legal_inputs[random_index].clone(), false, false),
            Output::Events(_)
        )
    }

    fn seeded_runtime_speed_positions(
        seed: u64,
        count: usize,
        min_plies: usize,
        max_plies: usize,
    ) -> Vec<MonsGame> {
        let mut rng = StdRng::seed_from_u64(seed);
        let mut positions = Vec::with_capacity(count);

        while positions.len() < count.max(1) {
            let mut game = MonsGame::new(false, GameVariant::Classic);
            let plies = rng.gen_range(min_plies..=max_plies.max(min_plies));
            let mut valid = true;

            for _ in 0..plies {
                if game.winner_color().is_some() || !apply_seeded_runtime_move(&mut game, &mut rng)
                {
                    valid = false;
                    break;
                }
            }

            if valid && game.winner_color().is_none() {
                positions.push(game);
            }
        }

        positions
    }

    fn immediate_score_runtime_fixture() -> MonsGame {
        for location in [
            Location::new(9, 0),
            Location::new(9, 1),
            Location::new(9, 2),
            Location::new(8, 1),
        ] {
            let mut probe = game_with_items(
                vec![
                    (
                        location,
                        Item::MonWithMana {
                            mon: Mon::new(MonKind::Drainer, Color::White, 0),
                            mana: Mana::Regular(Color::Black),
                        },
                    ),
                    (
                        Location::new(0, 10),
                        Item::Mon {
                            mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                        },
                    ),
                ],
                Color::White,
                3,
            );
            probe.white_score = Config::TARGET_SCORE - 2;
            let has_immediate_win = MonsGameModel::enumerate_legal_inputs(
                &probe,
                96,
                SuggestedStartInputOptions::for_automove(),
            )
            .into_iter()
            .any(|inputs| {
                let mut after = probe.clone_for_simulation();
                matches!(after.process_input(inputs, false, false), Output::Events(_))
                    && after.winner_color() == Some(Color::White)
            });
            if has_immediate_win {
                return probe;
            }
        }

        panic!("expected an immediate-score runtime fixture");
    }

    fn cold_public_runtime_gate_fixtures() -> Vec<MonsGame> {
        let mut fixtures = seeded_runtime_speed_positions(0x5eed_0ee1, 3, 2, 6);
        fixtures.extend(seeded_runtime_speed_positions(0x5eed_1d00, 3, 10, 16));
        fixtures.extend([
            immediate_score_runtime_fixture(),
            game_with_items(
                vec![
                    (
                        Location::new(5, 5),
                        Item::Mon {
                            mon: Mon::new(MonKind::Mystic, Color::White, 0),
                        },
                    ),
                    (
                        Location::new(10, 5),
                        Item::Mon {
                            mon: Mon::new(MonKind::Drainer, Color::White, 0),
                        },
                    ),
                    (
                        Location::new(7, 7),
                        Item::Mon {
                            mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                        },
                    ),
                ],
                Color::White,
                2,
            ),
            game_with_items(
                vec![
                    (
                        Location::new(6, 5),
                        Item::Mon {
                            mon: Mon::new(MonKind::Drainer, Color::White, 0),
                        },
                    ),
                    (
                        Location::new(5, 5),
                        Item::Mana {
                            mana: Mana::Supermana,
                        },
                    ),
                    (
                        Location::new(0, 10),
                        Item::Mon {
                            mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                        },
                    ),
                ],
                Color::White,
                2,
            ),
        ]);

        fixtures
    }

    fn clone_model_for_smart_automove_tests(model: &MonsGameModel) -> MonsGameModel {
        MonsGameModel::with_game(model.game.clone_for_simulation())
    }

    fn exhaustive_same_turn_reachable<F>(game: &MonsGame, color: Color, predicate: F) -> bool
    where
        F: Fn(&MonsGame, &[Event]) -> bool,
    {
        fn visit<F>(game: &MonsGame, color: Color, seen: &mut HashSet<u64>, predicate: &F) -> bool
        where
            F: Fn(&MonsGame, &[Event]) -> bool,
        {
            if game.active_color != color {
                return false;
            }

            let state_hash = MonsGameModel::search_state_hash(game);
            if !seen.insert(state_hash) {
                return false;
            }

            for transition in MonsGameModel::enumerate_legal_transitions(
                game,
                usize::MAX,
                SuggestedStartInputOptions::for_automove(),
            ) {
                if predicate(&transition.game, &transition.events) {
                    return true;
                }
                if transition.game.active_color == color
                    && visit(&transition.game, color, seen, predicate)
                {
                    return true;
                }
            }

            false
        }

        if predicate(game, &[]) {
            return true;
        }

        let mut seen = HashSet::new();
        visit(game, color, &mut seen, &predicate)
    }

    fn exhaustive_same_turn_reachable_with_spirit_history<F>(
        game: &MonsGame,
        color: Color,
        predicate: F,
    ) -> bool
    where
        F: Fn(&MonsGame, bool) -> bool,
    {
        fn visit<F>(
            game: &MonsGame,
            color: Color,
            spirit_used: bool,
            seen: &mut HashSet<(u64, bool)>,
            predicate: &F,
        ) -> bool
        where
            F: Fn(&MonsGame, bool) -> bool,
        {
            if game.active_color != color {
                return false;
            }

            let state_hash = MonsGameModel::search_state_hash(game);
            if !seen.insert((state_hash, spirit_used)) {
                return false;
            }

            for transition in MonsGameModel::enumerate_legal_transitions(
                game,
                usize::MAX,
                SuggestedStartInputOptions::for_automove(),
            ) {
                let used_spirit = spirit_used
                    || MonsGameModel::events_include_spirit_target_move(&transition.events);
                if predicate(&transition.game, used_spirit) {
                    return true;
                }
                if transition.game.active_color == color
                    && visit(&transition.game, color, used_spirit, seen, predicate)
                {
                    return true;
                }
            }

            false
        }

        if predicate(game, false) {
            return true;
        }

        let mut seen = HashSet::new();
        visit(game, color, false, &mut seen, &predicate)
    }

    fn drainer_carries_exact_safe_mana(board: &Board, color: Color, wanted_mana: Mana) -> bool {
        board.occupied().any(|(location, item)| {
            matches!(
                item,
                Item::MonWithMana { mon, mana }
                    if mon.color == color
                        && mon.kind == MonKind::Drainer
                        && !mon.is_fainted()
                        && *mana == wanted_mana
                        && crate::models::automove_exact::is_drainer_exactly_safe_next_turn_on_board(
                            board,
                            color,
                            location,
                        )
            )
        })
    }

    #[test]
    fn pro_mode_is_accepted_and_produces_legal_inputs() {
        assert_eq!(
            SmartAutomovePreference::from_api_value("pro"),
            Some(SmartAutomovePreference::Pro)
        );
        assert_eq!(SmartAutomovePreference::Pro.as_api_value(), "pro");
        assert_eq!(SmartAutomovePreference::from_api_value("expert"), None);
        assert_eq!(SmartAutomovePreference::from_api_value("EXPERT"), None);

        let game = MonsGame::new(false, GameVariant::Classic);
        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.depth = 1;
        config.max_visited_nodes = 16;
        config.root_branch_limit = 1;
        config.node_branch_limit = 1;
        config.root_enum_limit = 1;
        config.node_enum_limit = 1;
        let inputs = MonsGameModel::smart_search_best_inputs(&game, config);
        assert!(
            !inputs.is_empty(),
            "pro mode should produce at least one input from initial position"
        );
        assert!(
            MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs).is_some(),
            "pro mode selected inputs should be legal"
        );
    }

    #[test]
    #[ignore = "release gate: all variants and public modes must be deterministic and replayable"]
    fn smart_automove_public_variant_legality_and_replay_gate() {
        for variant_id in 0..=11 {
            let variant = GameVariant::from_id(variant_id).expect("known public game variant");
            let game = MonsGame::new(false, variant);

            for preference in [
                SmartAutomovePreference::Fast,
                SmartAutomovePreference::Normal,
                SmartAutomovePreference::Pro,
            ] {
                let mut observed = None;
                for repeat in 0..2 {
                    clear_exact_state_analysis_cache();
                    clear_turn_engine_plan_cache();
                    clear_turn_engine_selector_followup_floor_cache();
                    let model = MonsGameModel::with_game(game.clone_for_simulation());
                    let output = model.smart_automove_output(preference);
                    assert_eq!(output.kind, OutputModelKind::Events);

                    let input_fen = output.input_fen();
                    assert!(!input_fen.is_empty());
                    let mut replay = game.clone_for_simulation();
                    assert!(matches!(
                        replay.process_input(Input::array_from_fen(&input_fen), false, false),
                        Output::Events(_)
                    ));
                    let replay_fen = replay.fen();

                    if let Some((expected_inputs, expected_replay)) = observed.as_ref() {
                        assert_eq!(
                            input_fen,
                            *expected_inputs,
                            "variant={variant_id} mode={} repeat={repeat}",
                            preference.as_api_value(),
                        );
                        assert_eq!(
                            replay_fen,
                            *expected_replay,
                            "variant={variant_id} mode={} repeat={repeat}",
                            preference.as_api_value(),
                        );
                    } else {
                        observed = Some((input_fen, replay_fen));
                    }
                }
            }
        }
    }

    #[test]
    fn smart_automove_preserves_immediate_score_fixture_for_all_modes() {
        let game = immediate_score_runtime_fixture();

        for preference in [
            SmartAutomovePreference::Fast,
            SmartAutomovePreference::Normal,
            SmartAutomovePreference::Pro,
        ] {
            let model = MonsGameModel::with_game(game.clone_for_simulation());
            let search_inputs = MonsGameModel::smart_search_best_inputs(
                &model.game,
                model.shipping_search_config_for_preference(preference),
            );
            assert!(
                !search_inputs.is_empty(),
                "{} smart search should not fall back on the immediate-score fixture",
                preference.as_api_value()
            );
            let output = model.smart_automove_output(preference);
            assert_eq!(
                output.kind,
                OutputModelKind::Events,
                "{} should produce a legal move in the immediate-score fixture",
                preference.as_api_value()
            );

            let mut after = game.clone_for_simulation();
            let inputs = Input::array_from_fen(output.input_fen().as_str());
            assert!(
                matches!(after.process_input(inputs, false, false), Output::Events(_)),
                "{} should stay legal in the immediate-score fixture",
                preference.as_api_value()
            );
            assert!(
                after.white_score > game.white_score,
                "{} should convert the immediate-score fixture by scoring immediately",
                preference.as_api_value()
            );
            assert_eq!(
                after.winner_color(),
                Some(Color::White),
                "{} should convert the immediate-score fixture",
                preference.as_api_value()
            );
        }
    }

    #[test]
    fn pro_runtime_uses_engine_allowed_rerank_override_on_immediate_score_fixture() {
        clear_turn_engine_selector_diagnostics();
        crate::models::automove_turn_engine::clear_turn_engine_plan_cache();

        let game = immediate_score_runtime_fixture();
        let model = MonsGameModel::with_game(game.clone_for_simulation());
        let config = model.shipping_search_config_for_preference(SmartAutomovePreference::Pro);
        assert!(
            !config.enable_turn_engine_selector,
            "base runtime pro config should still have the full turn engine disabled"
        );
        assert!(
            config.enable_turn_head_rerank,
            "fixture should exercise the head-rerank replacement seam"
        );

        let inputs = MonsGameModel::smart_search_best_inputs(&game, config);
        assert!(
            !inputs.is_empty(),
            "selector should still produce a legal immediate-score move"
        );
        let diagnostics = turn_engine_selector_diagnostics_snapshot();
        assert_eq!(
            diagnostics.last_return_stage,
            "search_only_engine_allowed_head"
        );
    }

    #[test]
    fn active_turn_exact_tactical_queries_match_exhaustive_curated_fixtures() {
        let attack_game = game_with_items(
            vec![
                (
                    Location::new(5, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 7),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        let attack_turn =
            crate::models::automove_exact::exact_turn_summary(&attack_game, Color::White);
        assert_eq!(
            attack_turn.can_attack_opponent_drainer,
            exhaustive_same_turn_reachable(&attack_game, Color::White, |_, events| {
                events.iter().any(|event| {
                    matches!(
                        event,
                        Event::MonFainted { mon, .. }
                            if mon.kind == MonKind::Drainer && mon.color == Color::Black
                    )
                })
            })
        );

        let supermana_game = game_with_items(
            vec![
                (
                    Location::new(6, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        let supermana_turn =
            crate::models::automove_exact::exact_turn_summary(&supermana_game, Color::White);
        assert_eq!(
            supermana_turn.safe_supermana_progress,
            exhaustive_same_turn_reachable(&supermana_game, Color::White, |reachable, events| {
                events.iter().any(|event| {
                    matches!(event, Event::ManaScored { mana, .. } if *mana == Mana::Supermana)
                }) || drainer_carries_exact_safe_mana(&reachable.board, Color::White, Mana::Supermana)
            })
        );

        let opponent_mana_game = game_with_items(
            vec![
                (
                    Location::new(6, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 4),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        let opponent_mana_turn =
            crate::models::automove_exact::exact_turn_summary(&opponent_mana_game, Color::White);
        assert_eq!(
            opponent_mana_turn.safe_opponent_mana_progress,
            exhaustive_same_turn_reachable(
                &opponent_mana_game,
                Color::White,
                |reachable, events| {
                    events.iter().any(|event| {
                        matches!(
                            event,
                            Event::ManaScored { mana, .. }
                                if *mana == Mana::Regular(Color::Black)
                        )
                    }) || drainer_carries_exact_safe_mana(
                        &reachable.board,
                        Color::White,
                        Mana::Regular(Color::Black),
                    )
                }
            )
        );

        let mut spirit_game = game_with_items(
            vec![
                (
                    Location::new(5, 1),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(8, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        spirit_game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 2;
        let spirit_turn =
            crate::models::automove_exact::exact_turn_summary(&spirit_game, Color::White);
        let exhaustive_spirit_score = exhaustive_same_turn_reachable_with_spirit_history(
            &spirit_game,
            Color::White,
            |reachable, spirit_used| spirit_used && reachable.white_score > 0,
        );
        let exhaustive_spirit_denial = exhaustive_same_turn_reachable_with_spirit_history(
            &spirit_game,
            Color::White,
            |reachable, spirit_used| {
                spirit_used
                    && (reachable.white_score >= Mana::Regular(Color::Black).score(Color::White)
                        || drainer_carries_exact_safe_mana(
                            &reachable.board,
                            Color::White,
                            Mana::Regular(Color::Black),
                        ))
            },
        );
        assert_eq!(spirit_turn.spirit_assisted_score, exhaustive_spirit_score);
        assert_eq!(spirit_turn.spirit_assisted_denial, exhaustive_spirit_denial);
    }

    #[test]
    fn smart_automove_sync_matches_async_pipeline_on_sparse_tactical_position_for_all_modes() {
        let game = game_with_items(
            vec![
                (
                    Location::new(5, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::White, 0),
                    },
                ),
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 7),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        let base_model = MonsGameModel::with_game(game);

        for preference in [
            SmartAutomovePreference::Fast,
            SmartAutomovePreference::Normal,
            SmartAutomovePreference::Pro,
        ] {
            let sync_model = clone_model_for_smart_automove_tests(&base_model);
            let async_model = clone_model_for_smart_automove_tests(&base_model);

            let sync_output = sync_model.smart_automove_output(preference);
            let async_output = async_model.smart_automove_output_via_async_loop(preference);

            assert_eq!(sync_output.kind, OutputModelKind::Events);
            assert_eq!(async_output.kind, OutputModelKind::Events);
            assert_eq!(
                sync_output.input_fen(),
                async_output.input_fen(),
                "sync production helper should match test-only async pipeline for {}",
                preference.as_api_value()
            );
        }
    }

    #[test]
    fn smart_automove_pro_matches_frontier_bounded_tactical_selector_on_release_fixture() {
        let game = MonsGame::from_fen(
            "0 0 w 0 0 1 0 0 1 n03y0xs0xd0xa0xe0xn03/n11/n11/n04xxmn01xxmn04/n03xxmn01xxmn01xxmn03/xxQn04xxUn04xxQ/n03xxMn01xxMn01xxMn03/n04xxMn01xxMn04/n11/n11/n02E0xn01A0xD0xS0xY0xn03",
            false,
        )
        .expect("release Pro route fixture should be valid");
        let model = MonsGameModel::with_game(game.clone_for_simulation());
        let config = model.shipping_search_config_for_preference(SmartAutomovePreference::Pro);
        automove_deadline::with_test_clock(0.0, || {
            clear_exact_state_analysis_cache();
            clear_turn_engine_plan_cache();
            clear_turn_engine_selector_followup_floor_cache();
            let expected_inputs =
                automove_runtime_variants::select_current_pro_bounded_tactical_inputs(
                    &game, config,
                );
            let expected_input_fen = Input::fen_from_array(&expected_inputs);
            clear_exact_state_analysis_cache();
            clear_turn_engine_plan_cache();
            clear_turn_engine_selector_followup_floor_cache();
            let shipping_inputs =
                automove_runtime_variants::select_shipping_search_inputs(&game, config);
            let shipping_input_fen = Input::fen_from_array(&shipping_inputs);

            assert_eq!(expected_input_fen, "l10,5;l9,4");
            assert_eq!(shipping_input_fen, "l10,6;l9,6");
            assert_ne!(expected_input_fen, shipping_input_fen);

            clear_exact_state_analysis_cache();
            clear_turn_engine_plan_cache();
            clear_turn_engine_selector_followup_floor_cache();
            let output = model.smart_automove_output(SmartAutomovePreference::Pro);

            assert_eq!(output.kind, OutputModelKind::Events);
            assert_eq!(output.input_fen(), expected_input_fen.as_str());
            assert_ne!(output.input_fen(), shipping_input_fen.as_str());
        });
    }

    #[test]
    #[ignore = "release gate: independently cold public calls must remain legal and under 700ms"]
    fn smart_automove_public_runtime_budget_gate() {
        use std::time::Instant;

        for (fixture_index, game) in cold_public_runtime_gate_fixtures().into_iter().enumerate() {
            for preference in [
                SmartAutomovePreference::Fast,
                SmartAutomovePreference::Normal,
                SmartAutomovePreference::Pro,
            ] {
                clear_exact_state_analysis_cache();
                clear_turn_engine_plan_cache();
                clear_turn_engine_selector_followup_floor_cache();
                let model = MonsGameModel::with_game(game.clone_for_simulation());
                let start = Instant::now();
                let output = model.smart_automove_output(preference);
                let elapsed_ms = start.elapsed().as_secs_f64() * 1_000.0;
                assert_eq!(output.kind, OutputModelKind::Events);

                let mut replay = game.clone_for_simulation();
                let inputs = Input::array_from_fen(output.input_fen().as_str());
                assert!(matches!(
                    replay.process_input(inputs, false, false),
                    Output::Events(_)
                ));
                println!(
                    "AUTOMOVE_PUBLIC_RUNTIME fixture={} mode={} elapsed_ms={:.3}",
                    fixture_index,
                    preference.as_api_value(),
                    elapsed_ms,
                );
                assert!(
                    elapsed_ms <= 700.0,
                    "public {} call exceeded 700ms on fixture {}: {:.3}ms",
                    preference.as_api_value(),
                    fixture_index,
                    elapsed_ms,
                );
            }
        }
    }

    #[test]
    fn pro_runtime_profile_applies_expected_tuning() {
        let base_game = MonsGame::new(false, GameVariant::Classic);
        let independent_model = MonsGameModel::with_game(base_game);
        let independent_config =
            independent_model.shipping_search_config_for_preference(SmartAutomovePreference::Pro);
        assert_eq!(
            independent_config.max_visited_nodes,
            (SMART_AUTOMOVE_PRO_MAX_VISITED_NODES as usize).saturating_mul(9) / 8
        );
        assert!(independent_config.enable_turn_head_rerank);
        assert!(
            !independent_config.enable_turn_engine_selector,
            "shipping Pro search config must keep the turn-engine selector disabled"
        );
        assert_eq!(independent_config.root_reply_risk_score_margin, 165);
        assert_eq!(independent_config.root_reply_risk_shortlist_max, 9);
        assert_eq!(independent_config.root_reply_risk_reply_limit, 24);
        assert_eq!(independent_config.root_reply_risk_node_share_bp, 2_000);
        assert!(independent_config.enable_normal_root_safety_deep_floor);
        assert_eq!(independent_config.root_drainer_safety_score_margin, 4_800);
        assert_eq!(independent_config.selective_extension_node_share_bp, 1_500);
        assert_eq!(
            independent_config.interview_soft_opponent_mana_progress_bonus,
            320
        );
        assert_eq!(
            independent_config.interview_soft_opponent_mana_score_bonus,
            400
        );
        assert!(independent_config.enable_quiescence_search);
        assert_eq!(independent_config.quiescence_node_budget, 120);
        assert!(independent_config.enable_quiescence_tactical_children_only);
        assert_eq!(independent_config.quiescence_tactical_enum_limit, 12);
    }

    #[test]
    fn live_spirit_setup_gain_marks_runtime_setup_root_positive() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let candidate = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("runtime spirit setup inputs should build a scored root");

        assert!(
            candidate.spirit_setup_gain > 0,
            "runtime setup roots should retain a live positive setup signal when static exact eval is disabled"
        );
    }

    #[test]
    fn current_pro_preserves_safe_pickup_and_non_concrete_progress_precedence() {
        let game = MonsGame::new(false, GameVariant::Classic);
        let mut pickup = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 1),
            920,
            false,
            false,
            false,
        );
        pickup.safe_supermana_pickup_now = true;

        let mut progress = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 2),
            980,
            false,
            false,
            false,
        );
        progress.supermana_progress = true;

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.enable_turn_engine_selector = true;
        config.turn_engine_mode = TurnEngineMode::CurrentPro;

        assert_eq!(
            MonsGameModel::current_pro_safe_pickup_reply_order(
                &pickup,
                RootReplyRiskSnapshot {
                    allows_immediate_opponent_win: false,
                    opponent_reaches_match_point: false,
                    worst_reply_score: 480,
                },
                &progress,
                RootReplyRiskSnapshot {
                    allows_immediate_opponent_win: false,
                    opponent_reaches_match_point: false,
                    worst_reply_score: 620,
                },
                config,
            ),
            Some(std::cmp::Ordering::Greater),
        );

        assert_current_pro_non_concrete_progress_does_not_replace_unsafe_root();
    }

    fn assert_current_pro_non_concrete_progress_does_not_replace_unsafe_root() {
        fn current_pro_candidate_config(game: &MonsGame) -> AutomoveSearchConfig {
            let mut config = MonsGameModel::with_game(game.clone())
                .shipping_search_config_for_preference(SmartAutomovePreference::Pro);
            if config.depth >= SMART_AUTOMOVE_PRO_DEPTH as usize
                && config.enable_normal_root_safety_deep_floor
            {
                config.enable_turn_head_rerank = false;
                config.enable_turn_engine_selector = true;
                config.turn_engine_mode = TurnEngineMode::CurrentPro;
                config.turn_engine_seed_cap = 14;
                config.turn_engine_beam_width = 5;
                config.turn_engine_per_node_family_cap = 4;
                config.turn_engine_step_cap = 6;
                config.turn_engine_opponent_seed_cap = 6;
                config.turn_engine_opponent_beam_width = 2;
                config.turn_engine_reply_seed_cap = 3;
                config.turn_engine_reply_beam_width = 1;
                config.turn_engine_expansion_cap = 176;
                config.turn_engine_enable_spirit_family = true;
                config.root_reply_risk_reply_limit = config.root_reply_risk_reply_limit.min(24);
                config.root_reply_risk_node_share_bp =
                    config.root_reply_risk_node_share_bp.min(2_000);
                config.enable_turn_engine_low_budget_guard = true;
                config.enable_turn_engine_mid_turn_tactical_guard = true;
                config.enable_turn_engine_late_safe_mana_root_preference = true;
            }
            config
        }

        let game = MonsGame::from_fen(
            "0 0 b 1 0 4 0 0 6 n03y0xn07/n07e0xn03/n06d0xa0xn03/n03xxmn03xxmn03/n02s0xxxmxxmn02xxmn03/xxQn04xxUn01xxMn02xxQ/n05xxMn05/n02xxMn03xxMn03Y0x/n03xxMn03D0xn03/n05A0xn05/n03E0xn02S0xn04",
            false,
        )
        .expect("primary_ext_sensitive_no_ext_a fen should be valid");
        let perspective = game.active_color;
        let config = current_pro_candidate_config(&game);
        let root_moves = MonsGameModel::ranked_root_moves(&game, perspective, config);
        let mut visited_nodes = 0usize;
        let mut alpha = i32::MIN;
        let mut scored_roots = Vec::with_capacity(root_moves.len());
        let mut transposition_table = U64HashMap::default();
        let extension_node_budget =
            if config.enable_selective_extensions && config.selective_extension_node_share_bp > 0 {
                ((config.max_visited_nodes * config.selective_extension_node_share_bp as usize)
                    / 10_000)
                    .max(1)
            } else {
                0
            };
        let mut extension_nodes_used = 0usize;
        let mut quiescence_nodes_used = 0usize;

        for candidate in root_moves {
            if visited_nodes >= config.max_visited_nodes {
                break;
            }
            visited_nodes += 1;
            let candidate_score = MonsGameModel::evaluate_root_candidate_score(
                &candidate,
                perspective,
                alpha,
                &mut visited_nodes,
                config,
                &mut transposition_table,
                &mut extension_nodes_used,
                extension_node_budget,
                true,
                &mut quiescence_nodes_used,
            );
            alpha = alpha.max(candidate_score);
            scored_roots.push(root_evaluation_for_test(&candidate, candidate_score));
        }

        let selected_inputs = MonsGameModel::pick_root_move_with_exploration(
            &game,
            &scored_roots,
            perspective,
            config,
        );
        assert_eq!(Input::fen_from_array(&selected_inputs), "l0,3;l1,3");

        let plan = turn_engine_candidate_plan(
            &game,
            perspective,
            MonsGameModel::turn_engine_config_from_search_config(config),
        )
        .expect("fixture should produce a progress head");
        assert_eq!(plan.head_family, TurnPlanFamily::SafeSupermanaProgress);
        assert_eq!(
            Input::fen_from_array(
                plan.compiled_chunks
                    .first()
                    .expect("turn-engine plan should have a first chunk"),
            ),
            "l2,6;l3,7",
        );
        assert_eq!(plan.compiled_chunks.len(), 1);

        assert!(
            !MonsGameModel::accept_turn_engine_head_after_search(
                &game,
                perspective,
                config,
                &scored_roots,
                selected_inputs.as_slice(),
                &plan,
            ),
            "a non-concrete one-chunk progress head should not override an unsafe non-progress selected root",
        );
        assert_eq!(
            Input::fen_from_array(&MonsGameModel::smart_search_best_inputs(&game, config)),
            "l0,3;l1,3",
        );
    }

    #[test]
    fn runtime_preferences_configure_exact_lite_features() {
        let preferences = [
            SmartAutomovePreference::Fast,
            SmartAutomovePreference::Normal,
            SmartAutomovePreference::Pro,
        ];

        for preference in preferences {
            let independent_model =
                MonsGameModel::with_game(MonsGame::new(false, GameVariant::Classic));
            let independent_config =
                independent_model.shipping_search_config_for_preference(preference);
            if preference == SmartAutomovePreference::Normal {
                assert!(
                    independent_config.enable_exact_lite_progress_checks,
                    "{} should enable exact-lite progress checks in runtime defaults",
                    preference.as_api_value()
                );
                assert!(
                    independent_config.enable_exact_lite_spirit_window_checks,
                    "{} should enable exact-lite spirit checks in runtime defaults",
                    preference.as_api_value()
                );
                assert_eq!(
                    independent_config.exact_lite_root_call_budget,
                    1,
                    "{} should set exact-lite root budget to one in runtime defaults",
                    preference.as_api_value()
                );
                assert_eq!(
                    independent_config.exact_lite_static_call_budget,
                    1,
                    "{} should set exact-lite static budget to one in runtime defaults",
                    preference.as_api_value()
                );
            } else {
                assert!(
                    !independent_config.enable_exact_lite_progress_checks,
                    "{} should disable exact-lite progress checks in runtime defaults",
                    preference.as_api_value()
                );
                assert!(
                    !independent_config.enable_exact_lite_spirit_window_checks,
                    "{} should disable exact-lite spirit checks in runtime defaults",
                    preference.as_api_value()
                );
                assert_eq!(
                    independent_config.exact_lite_root_call_budget,
                    0,
                    "{} should keep exact-lite root budget at zero in runtime defaults",
                    preference.as_api_value()
                );
                assert_eq!(
                    independent_config.exact_lite_static_call_budget,
                    0,
                    "{} should keep exact-lite static budget at zero in runtime defaults",
                    preference.as_api_value()
                );
            }
        }
    }

    #[test]
    fn forced_drainer_attack_selects_a_drainer_fainting_turn_when_available() {
        let white_mystic = Mon::new(MonKind::Mystic, Color::White, 0);
        let white_drainer = Mon::new(MonKind::Drainer, Color::White, 0);
        let black_drainer = Mon::new(MonKind::Drainer, Color::Black, 0);
        let game = game_with_items(
            vec![
                (Location::new(5, 5), Item::Mon { mon: white_mystic }),
                (Location::new(10, 5), Item::Mon { mon: white_drainer }),
                (Location::new(7, 7), Item::Mon { mon: black_drainer }),
            ],
            Color::White,
            2,
        );

        let inputs = MonsGameModel::smart_search_best_inputs(
            &game,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );
        let (_, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("expected selected inputs to be legal");
        assert!(MonsGameModel::events_include_opponent_drainer_fainted(
            &events,
            Color::White
        ));
    }

    #[test]
    fn forced_drainer_attack_with_bomb_survives_root_enumeration_cutoff() {
        let game = game_with_items(
            vec![
                (
                    Location::new(10, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Angel, Color::White, 0),
                    },
                ),
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(8, 2),
                    Item::Consumable {
                        consumable: Consumable::BombOrPotion,
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.root_enum_limit = 0;
        let inputs = MonsGameModel::smart_search_best_inputs(&game, config);
        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("expected selected bomb inputs to be legal");
        let attacks_now =
            MonsGameModel::events_include_opponent_drainer_fainted(&events, Color::White);
        let mut continuation_budget = SMART_FORCED_DRAINER_ATTACK_FALLBACK_NODE_BUDGET_FAST;
        let attacks_later_this_turn = after.active_color == Color::White
            && MonsGameModel::can_attack_opponent_drainer_before_turn_ends(
                &after,
                Color::White,
                SMART_FORCED_DRAINER_ATTACK_FALLBACK_ENUM_LIMIT_FAST,
                SuggestedStartInputOptions::for_automove(),
                &mut continuation_budget,
                &mut U64HashSet::default(),
            );
        assert!(
            attacks_now || attacks_later_this_turn,
            "selected line must keep same-turn drainer attack reachable"
        );
    }

    #[test]
    fn bounded_lexicographic_transition_prefix_matches_full_sorted_enumeration() {
        fn signatures(
            transitions: &[LegalInputTransition],
        ) -> Vec<(Vec<Input>, String, Vec<Event>)> {
            transitions
                .iter()
                .map(|transition| {
                    (
                        transition.inputs.clone(),
                        transition.game.fen(),
                        transition.events.clone(),
                    )
                })
                .collect()
        }

        let mut game = game_with_items(
            vec![
                (
                    Location::new(5, 5),
                    Item::MonWithConsumable {
                        mon: Mon::new(MonKind::Demon, Color::White, 0),
                        consumable: Consumable::Bomb,
                    },
                ),
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;
        let start_options = MonsGameModel::automove_start_input_options();
        let full = MonsGameModel::enumerate_legal_transitions(&game, usize::MAX, start_options);
        let full_signatures = signatures(full.as_slice());
        assert!(
            full_signatures.len() >= 4,
            "fixture should expose a useful prefix"
        );

        let mut attacker_mask = [false; BOARD_CELLS];
        for location in
            MonsGameModel::find_potential_drainer_attacker_locations(&game, Color::White)
        {
            attacker_mask[location.index()] = true;
        }
        let targeted_full = full
            .iter()
            .filter(|transition| {
                matches!(
                    transition.inputs.first(),
                    Some(Input::Location(location)) if attacker_mask[location.index()]
                )
            })
            .cloned()
            .collect::<Vec<_>>();
        let targeted_full_signatures = signatures(targeted_full.as_slice());
        assert!(
            targeted_full_signatures.len() >= 2,
            "fixture should expose multiple targeted roots"
        );
        assert!(
            targeted_full_signatures.len() < full_signatures.len(),
            "targeted mask must exclude at least one full transition"
        );
        assert!(full.iter().any(|transition| {
            !matches!(
                transition.inputs.first(),
                Some(Input::Location(location)) if attacker_mask[location.index()]
            )
        }));

        let mut unfiltered_caps = (1..=full_signatures.len().min(16)).collect::<Vec<_>>();
        unfiltered_caps.extend([0, full_signatures.len(), full_signatures.len() + 1]);
        unfiltered_caps.sort_unstable();
        unfiltered_caps.dedup();
        for cap in unfiltered_caps {
            let bounded = MonsGameModel::enumerate_legal_transitions_lexicographic_bounded(
                &game,
                cap,
                start_options,
                None,
            );
            assert_eq!(
                signatures(bounded.as_slice()),
                full_signatures
                    .iter()
                    .take(cap)
                    .cloned()
                    .collect::<Vec<_>>(),
                "unfiltered lexical prefix diverged at cap {cap}"
            );
        }

        let mut targeted_caps = (1..=targeted_full_signatures.len().min(16)).collect::<Vec<_>>();
        targeted_caps.extend([
            0,
            targeted_full_signatures.len(),
            targeted_full_signatures.len() + 1,
        ]);
        targeted_caps.sort_unstable();
        targeted_caps.dedup();
        for cap in targeted_caps {
            let bounded = MonsGameModel::enumerate_legal_transitions_lexicographic_bounded(
                &game,
                cap,
                start_options,
                Some(&attacker_mask),
            );
            assert_eq!(
                signatures(bounded.as_slice()),
                targeted_full_signatures
                    .iter()
                    .take(cap)
                    .cloned()
                    .collect::<Vec<_>>(),
                "targeted lexical prefix diverged at cap {cap}"
            );
        }

        let empty_mask = [false; BOARD_CELLS];
        assert!(
            MonsGameModel::enumerate_legal_transitions_lexicographic_bounded(
                &game,
                full_signatures.len() + 1,
                start_options,
                Some(&empty_mask),
            )
            .is_empty()
        );

        let mut distinct_root_locations = full
            .iter()
            .filter_map(|transition| match transition.inputs.first() {
                Some(Input::Location(location)) => Some(*location),
                _ => None,
            })
            .collect::<Vec<_>>();
        distinct_root_locations.sort_unstable();
        distinct_root_locations.dedup();
        assert!(distinct_root_locations.len() >= 2);
        let manually_selected_root = distinct_root_locations[1];
        let mut manual_mask = [false; BOARD_CELLS];
        manual_mask[manually_selected_root.index()] = true;
        let manual_full = full
            .iter()
            .filter(|transition| {
                matches!(
                    transition.inputs.first(),
                    Some(Input::Location(location)) if *location == manually_selected_root
                )
            })
            .cloned()
            .collect::<Vec<_>>();
        let manual_bounded = MonsGameModel::enumerate_legal_transitions_lexicographic_bounded(
            &game,
            manual_full.len() + 1,
            start_options,
            Some(&manual_mask),
        );
        assert_eq!(
            signatures(manual_bounded.as_slice()),
            signatures(manual_full.as_slice())
        );

        crate::models::automove_deadline::with_test_clock(0.0, || {
            crate::models::automove_deadline::with_deadline_if_absent(0.0, || {
                let mut partial = Vec::new();
                let mut partial_transitions = vec![full[0].clone()];
                let mut simulated = game.clone_for_simulation();
                MonsGameModel::collect_legal_transitions_lexicographic_bounded(
                    &mut simulated,
                    &mut partial,
                    &mut partial_transitions,
                    16,
                    start_options,
                    None,
                );
                assert!(partial_transitions.is_empty());
                assert!(
                    MonsGameModel::enumerate_legal_transitions_lexicographic_bounded(
                        &game,
                        16,
                        start_options,
                        None,
                    )
                    .is_empty()
                );
            });
            assert!(crate::models::automove_deadline::take_previous_timeout());
        });
    }

    #[test]
    fn drainer_vulnerability_detection_respects_friendly_angel_guard() {
        let white_drainer = Mon::new(MonKind::Drainer, Color::White, 0);
        let white_angel = Mon::new(MonKind::Angel, Color::White, 0);
        let black_mystic = Mon::new(MonKind::Mystic, Color::Black, 0);

        let unguarded = game_with_items(
            vec![
                (Location::new(5, 5), Item::Mon { mon: white_drainer }),
                (Location::new(3, 3), Item::Mon { mon: black_mystic }),
            ],
            Color::Black,
            2,
        );
        assert!(MonsGameModel::is_own_drainer_immediately_vulnerable(
            &unguarded,
            Color::White,
            false,
        ));

        let guarded = game_with_items(
            vec![
                (Location::new(5, 5), Item::Mon { mon: white_drainer }),
                (Location::new(5, 4), Item::Mon { mon: white_angel }),
                (Location::new(3, 3), Item::Mon { mon: black_mystic }),
            ],
            Color::Black,
            2,
        );
        assert!(!MonsGameModel::is_own_drainer_immediately_vulnerable(
            &guarded,
            Color::White,
            false,
        ));
    }

    #[test]
    fn drainer_vulnerability_detects_multi_step_mystic_attack() {
        let game = game_with_items(
            vec![
                (
                    Location::new(8, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(4, 7),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        assert!(
            !crate::models::automove_exact::is_drainer_under_immediate_threat(
                &game.board,
                Color::White,
                Location::new(8, 5),
                false,
            ),
            "pure immediate geometry should not already see this multi-step attack"
        );
        assert!(
            !crate::models::automove_exact::is_drainer_under_walk_threat(
                &game.board,
                Color::White,
                Location::new(8, 5),
                false,
            ),
            "one-step walk geometry should not already see this two-step mystic attack"
        );
        assert!(MonsGameModel::is_own_drainer_vulnerable_next_turn(
            &game,
            Color::White,
            true,
        ));
    }

    #[test]
    fn selected_line_avoids_drainer_vulnerability_when_safe_root_exists() {
        let game = game_with_items(
            vec![
                (
                    Location::new(8, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 4),
                    Item::Mon {
                        mon: Mon::new(MonKind::Angel, Color::White, 0),
                    },
                ),
                (
                    Location::new(6, 7),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let roots = MonsGameModel::ranked_root_moves(&game, Color::White, config);
        assert!(
            roots.iter().any(|root| !root.own_drainer_vulnerable),
            "scenario should offer at least one safe root"
        );

        let inputs = MonsGameModel::smart_search_best_inputs(&game, config);
        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected drainer-safety inputs should be legal");
        assert!(
            !MonsGameModel::is_own_drainer_vulnerable_next_turn(
                &after,
                Color::White,
                true,
            ),
            "selected line should keep drainer safe when a safe root exists, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn selected_line_avoids_drainer_vulnerability_with_root_cutoff() {
        let game = game_with_items(
            vec![
                (
                    Location::new(8, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 4),
                    Item::Mon {
                        mon: Mon::new(MonKind::Angel, Color::White, 0),
                    },
                ),
                (
                    Location::new(6, 7),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let roots = MonsGameModel::ranked_root_moves(&game, Color::White, config);
        assert!(
            roots.iter().any(|root| !root.own_drainer_vulnerable),
            "scenario should offer at least one safe root"
        );

        config.root_enum_limit = 0;
        let inputs = MonsGameModel::smart_search_best_inputs(&game, config);
        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected drainer-safety inputs should be legal");
        assert!(
            !MonsGameModel::is_own_drainer_vulnerable_next_turn(
                &after,
                Color::White,
                true,
            ),
            "selected line should keep drainer safe under capped root enumeration when a safe root exists, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn spirit_development_is_preferred_when_drainer_progress_is_still_available() {
        let spirit = Mon::new(MonKind::Spirit, Color::White, 0);
        let spirit_base = Board::new().base(spirit);
        let game = game_with_items(
            vec![
                (spirit_base, Item::Mon { mon: spirit }),
                (
                    Location::new(9, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 5),
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                ),
                (
                    Location::new(1, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let inputs = MonsGameModel::smart_search_best_inputs(
            &game,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );
        let (after, _) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected spirit development inputs should be legal");
        assert!(
            !MonsGameModel::has_awake_spirit_on_base(&after.board, Color::White),
            "selected move should deploy awake spirit off base when core progress remains available"
        );
    }

    #[test]
    fn spirit_scores_opponent_mana_when_immediately_available() {
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 1),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let inputs = MonsGameModel::smart_search_best_inputs(
            &game,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );
        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected spirit-score inputs should be legal");
        assert!(
            after.white_score >= game.white_score + 2,
            "selected line should score opponent mana immediately, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn spirit_scores_own_mana_when_immediately_available() {
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 1),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                ),
                (
                    Location::new(9, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 5),
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let inputs = MonsGameModel::smart_search_best_inputs(
            &game,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );
        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected spirit-score inputs should be legal");
        assert!(
            after.white_score > game.white_score,
            "selected line should score own mana immediately, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn safe_supermana_pickup_is_preferred_when_available() {
        let white_spirit = Mon::new(MonKind::Spirit, Color::White, 0);
        let white_spirit_base = Board::new().base(white_spirit);
        let game = game_with_items(
            vec![
                (
                    Location::new(6, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (white_spirit_base, Item::Mon { mon: white_spirit }),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let inputs = MonsGameModel::smart_search_best_inputs(
            &game,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );
        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected supermana inputs should be legal");
        assert!(
            matches!(
                after.board.item(Location::new(5, 5)),
                Some(Item::MonWithMana {
                    mon,
                    mana: Mana::Supermana,
                }) if mon.color == Color::White && mon.kind == MonKind::Drainer
            ),
            "selected line should pick up safe supermana, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn safe_opponent_mana_pickup_is_preferred_when_available() {
        let white_spirit = Mon::new(MonKind::Spirit, Color::White, 0);
        let white_spirit_base = Board::new().base(white_spirit);
        let game = game_with_items(
            vec![
                (
                    Location::new(6, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 4),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (white_spirit_base, Item::Mon { mon: white_spirit }),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let inputs = MonsGameModel::smart_search_best_inputs(
            &game,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );
        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected opponent-mana inputs should be legal");
        assert!(
            matches!(
                after.board.item(Location::new(5, 4)),
                Some(Item::MonWithMana {
                    mon,
                    mana: Mana::Regular(Color::Black),
                }) if mon.color == Color::White && mon.kind == MonKind::Drainer
            ),
            "selected line should pick up safe opponent mana, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn unsafe_multi_step_attack_supermana_pickup_is_not_flagged_safe() {
        let game = game_with_items(
            vec![
                (
                    Location::new(9, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(8, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(4, 7),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::Black, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let candidate = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true),
            &[
                Input::Location(Location::new(9, 5)),
                Input::Location(Location::new(8, 5)),
            ],
        )
        .expect("supermana pickup inputs should build a scored root");

        assert!(
            candidate.own_drainer_vulnerable,
            "pickup square should be exactly vulnerable to the black mystic"
        );
        assert!(
            !candidate.safe_supermana_pickup_now,
            "unsafe exact-vulnerable supermana pickup must not be flagged safe"
        );
    }

    #[test]
    fn unsafe_multi_step_attack_opponent_mana_pickup_is_not_flagged_safe() {
        let game = game_with_items(
            vec![
                (
                    Location::new(9, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(8, 5),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(4, 7),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::Black, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let candidate = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true),
            &[
                Input::Location(Location::new(9, 5)),
                Input::Location(Location::new(8, 5)),
            ],
        )
        .expect("opponent-mana pickup inputs should build a scored root");

        assert!(
            candidate.own_drainer_vulnerable,
            "pickup square should be exactly vulnerable to the black mystic"
        );
        assert!(
            !candidate.safe_opponent_mana_pickup_now,
            "unsafe exact-vulnerable opponent-mana pickup must not be flagged safe"
        );
    }

    fn root_evaluation_for_test(candidate: &ScoredRootMove, score: i32) -> RootEvaluation {
        RootEvaluation {
            root_rank: 0,
            score,
            efficiency: candidate.efficiency,
            inputs: candidate.inputs.clone(),
            game: candidate.game.clone(),
            wins_immediately: candidate.wins_immediately,
            attacks_opponent_drainer: candidate.attacks_opponent_drainer,
            own_drainer_vulnerable: candidate.own_drainer_vulnerable,
            own_drainer_walk_vulnerable: candidate.own_drainer_walk_vulnerable,
            spirit_development: candidate.spirit_development,
            keeps_awake_spirit_on_base: candidate.keeps_awake_spirit_on_base,
            mana_handoff_to_opponent: candidate.mana_handoff_to_opponent,
            has_roundtrip: candidate.has_roundtrip,
            scores_supermana_this_turn: candidate.scores_supermana_this_turn,
            scores_opponent_mana_this_turn: candidate.scores_opponent_mana_this_turn,
            safe_supermana_pickup_now: candidate.safe_supermana_pickup_now,
            safe_opponent_mana_pickup_now: candidate.safe_opponent_mana_pickup_now,
            safe_supermana_progress_steps: candidate.safe_supermana_progress_steps,
            safe_opponent_mana_progress_steps: candidate.safe_opponent_mana_progress_steps,
            score_path_best_steps: candidate.score_path_best_steps,
            same_turn_score_window_value: candidate.same_turn_score_window_value,
            spirit_setup_gain: candidate.spirit_setup_gain,
            spirit_same_turn_score_setup_now: candidate.spirit_same_turn_score_setup_now,
            spirit_own_mana_setup_now: candidate.spirit_own_mana_setup_now,
            supermana_progress: candidate.supermana_progress,
            opponent_mana_progress: candidate.opponent_mana_progress,
            interview_soft_priority: candidate.interview_soft_priority,
            classes: candidate.classes,
        }
    }

    fn turn_engine_projection_for_test(
        game: &MonsGame,
        family: TurnPlanFamily,
        eval_score: i32,
        marker: Location,
    ) -> TurnEngineRootProjection {
        TurnEngineRootProjection {
            plan: TurnPlan {
                actions: Vec::new(),
                compiled_chunks: vec![vec![Input::Location(marker)]],
                end_game: game.clone_for_simulation(),
                end_snapshot: TurnSnapshot::from_game(game),
                utility: TurnEngineUtility::from_eval_score_for_test(eval_score),
                head_utility: TurnEngineUtility::from_eval_score_for_test(eval_score),
                head_family: family,
                goal_family: family,
                package_meta: TurnPackageMeta::default(),
            },
        }
    }

    fn root_evaluation_for_projection_test(
        game: &MonsGame,
        input: Location,
        score: i32,
        spirit_development: bool,
        own_drainer_vulnerable: bool,
        has_roundtrip: bool,
    ) -> RootEvaluation {
        RootEvaluation {
            root_rank: 0,
            score,
            efficiency: 0,
            inputs: vec![Input::Location(input)],
            game: game.clone_for_simulation(),
            wins_immediately: false,
            attacks_opponent_drainer: false,
            own_drainer_vulnerable,
            own_drainer_walk_vulnerable: false,
            spirit_development,
            keeps_awake_spirit_on_base: !spirit_development,
            mana_handoff_to_opponent: false,
            has_roundtrip,
            scores_supermana_this_turn: false,
            scores_opponent_mana_this_turn: false,
            safe_supermana_pickup_now: false,
            safe_opponent_mana_pickup_now: false,
            safe_supermana_progress_steps: Config::BOARD_SIZE + 4,
            safe_opponent_mana_progress_steps: Config::BOARD_SIZE + 4,
            score_path_best_steps: Config::BOARD_SIZE * 3,
            same_turn_score_window_value: 0,
            spirit_setup_gain: 0,
            spirit_same_turn_score_setup_now: false,
            spirit_own_mana_setup_now: false,
            supermana_progress: false,
            opponent_mana_progress: false,
            interview_soft_priority: 0,
            classes: MoveClassFlags::default(),
        }
    }

    #[test]
    fn white_turn_three_baseline_alignment_override_prefers_split_trace_baseline_root() {
        let game = MonsGame::from_fen(
            "0 0 w 1 0 4 0 0 3 n03y0xn03e0xn03/n05a0xn05/n02xxmn01s0xn01d0xn04/n06xxmn04/n03xxmn01xxmn01xxmn03/xxQn04xxUn04xxQ/n05xxMn01xxMn03/n03xxMxxMn01xxMn04/E0xn04S0xn05/n05D0xn05/n04A0xn03Y0xn02",
            false,
        )
        .expect("valid white split-trace fen");
        let mut approved = root_evaluation_for_projection_test(
            &game,
            Location::new(8, 0),
            370,
            false,
            false,
            false,
        );
        approved.root_rank = 0;
        approved.safe_supermana_progress_steps = 4;
        approved.safe_opponent_mana_progress_steps = 5;
        approved.spirit_setup_gain = 60;

        let mut baseline = root_evaluation_for_projection_test(
            &game,
            Location::new(10, 8),
            393,
            false,
            false,
            false,
        );
        baseline.root_rank = 2;
        baseline.safe_supermana_progress_steps = 4;
        baseline.safe_opponent_mana_progress_steps = 5;
        baseline.spirit_setup_gain = 60;

        let scored_roots = [approved, baseline];
        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.turn_engine_mode = TurnEngineMode::CurrentPro;

        assert_eq!(
            MonsGameModel::current_pro_root_advisor_white_turn_three_baseline_alignment_override(
                &game,
                &scored_roots,
                &[0, 1],
                0,
                1,
                config,
            ),
            Some(1),
        );
    }

    #[test]
    fn white_turn_three_attack_bridge_escape_prefers_full_root_mana_escape() {
        let game = MonsGame::from_fen(
            "0 0 w 0 0 2 0 0 3 n11/n03y0xn01s0xa0xe0xn03/n05d0xn05/n03xxmxxmn01xxmn04/n05xxmn01xxmn03/xxQn04xxUn04xxQ/n01E0xn01xxMn01xxMn01xxMn03/n04xxMn01xxMn04/n11/n04D0xS0xn01Y0xn03/n04A0xn06",
            false,
        )
        .expect("valid white bridge fen");
        let mut approved = root_evaluation_for_projection_test(
            &game,
            Location::new(9, 5),
            95_582,
            true,
            false,
            false,
        );
        approved.root_rank = 0;
        approved.spirit_own_mana_setup_now = true;
        approved.spirit_setup_gain = 132;
        approved.safe_supermana_progress_steps = 4;
        approved.safe_opponent_mana_progress_steps = 5;

        let mut baseline = root_evaluation_for_projection_test(
            &game,
            Location::new(6, 1),
            1_510,
            false,
            false,
            false,
        );
        baseline.root_rank = 0;
        baseline.inputs = vec![
            Input::Location(Location::new(6, 1)),
            Input::Location(Location::new(5, 0)),
            Input::Modifier(Modifier::SelectBomb),
        ];
        baseline.spirit_setup_gain = 60;
        baseline.safe_supermana_progress_steps = 4;
        baseline.safe_opponent_mana_progress_steps = 5;

        let scored_roots = [approved, baseline];
        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.turn_engine_mode = TurnEngineMode::CurrentPro;

        assert_eq!(
            MonsGameModel::current_pro_root_advisor_white_turn_three_attack_bridge_escape(
                &game,
                &scored_roots,
                &[0],
                0,
                config,
            ),
            Some(1),
        );
    }

    #[test]
    fn black_late_window_mana_safety_override_allows_rank_zero_escape_with_progress_gain() {
        let game = MonsGame::from_fen(
            "3 1 b 1 0 2 0 0 14 n11/n07a0xd0xxxmn01/n01xxmn03s0xn05/n03xxmn07/n05xxmn01e0xn01Y0xn01/n11/n04xxUn01S0xn04/n04xxMn06/n01y0xA0xn04xxMn03/n01D0xn09/n03E1xn07",
            false,
        )
        .expect("valid black late fast trace fen");
        let mut approved = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 8),
            -809_483,
            false,
            true,
            false,
        );
        approved.root_rank = 0;
        approved.inputs = vec![
            Input::Location(Location::new(1, 8)),
            Input::Location(Location::new(1, 9)),
        ];
        approved.same_turn_score_window_value = 1;
        approved.safe_supermana_progress_steps = 15;
        approved.safe_opponent_mana_progress_steps = 15;

        let mut challenger = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 8),
            -809_492,
            false,
            false,
            false,
        );
        challenger.root_rank = 1;
        challenger.inputs = vec![
            Input::Location(Location::new(1, 8)),
            Input::Location(Location::new(0, 8)),
        ];
        challenger.safe_supermana_progress_steps = 6;
        challenger.safe_opponent_mana_progress_steps = 7;

        let scored_roots = [approved, challenger];
        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.turn_engine_mode = TurnEngineMode::CurrentPro;

        assert_eq!(
            MonsGameModel::current_pro_root_advisor_black_late_window_mana_safety_override(
                &game,
                &scored_roots,
                &[0, 1],
                0,
                config,
            ),
            Some(1),
        );
    }

    #[test]
    fn black_late_window_mana_safety_override_keeps_rank_zero_window_without_progress_gain() {
        let game = MonsGame::from_fen(
            "3 1 b 1 0 2 0 0 14 n11/n07a0xd0xxxmn01/n01xxmn03s0xn05/n03xxmn07/n05xxmn01e0xn01Y0xn01/n11/n04xxUn01S0xn04/n04xxMn06/n01y0xA0xn04xxMn03/n01D0xn09/n03E1xn07",
            false,
        )
        .expect("valid black late fast trace fen");
        let mut approved = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 8),
            -809_483,
            false,
            true,
            false,
        );
        approved.root_rank = 0;
        approved.inputs = vec![
            Input::Location(Location::new(1, 8)),
            Input::Location(Location::new(1, 9)),
        ];
        approved.same_turn_score_window_value = 1;
        approved.safe_supermana_progress_steps = 15;
        approved.safe_opponent_mana_progress_steps = 15;

        let mut challenger = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 8),
            -809_492,
            false,
            false,
            false,
        );
        challenger.root_rank = 1;
        challenger.inputs = vec![
            Input::Location(Location::new(1, 8)),
            Input::Location(Location::new(0, 8)),
        ];
        challenger.safe_supermana_progress_steps = 15;
        challenger.safe_opponent_mana_progress_steps = 15;

        let scored_roots = [approved, challenger];
        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.turn_engine_mode = TurnEngineMode::CurrentPro;

        assert_eq!(
            MonsGameModel::current_pro_root_advisor_black_late_window_mana_safety_override(
                &game,
                &scored_roots,
                &[0, 1],
                0,
                config,
            ),
            None,
        );
    }

    #[test]
    fn black_late_reply_risk_setup_override_prefers_shortlisted_own_setup_root() {
        let game = MonsGame::from_fen(
            "1 1 b 0 0 0 0 0 8 d0xn10/n05s0xa0xe0xxxmn02/n11/n07xxmn03/n03xxmn02xxmn04/n10xxQ/n02y0xn01D0UxxMn01xxMn03/n02xxMS0xn01A0xxxMn04/n06Y0xn04/n03E0xn07/n11",
            false,
        )
        .expect("valid black late reply-risk setup fen");
        let mut approved = root_evaluation_for_projection_test(
            &game,
            Location::new(6, 2),
            2155,
            false,
            false,
            false,
        );
        approved.root_rank = 2;
        approved.inputs = vec![
            Input::Location(Location::new(6, 2)),
            Input::Location(Location::new(5, 3)),
        ];
        approved.safe_supermana_progress_steps = 15;
        approved.safe_opponent_mana_progress_steps = 6;
        approved.score_path_best_steps = 33;
        approved.spirit_setup_gain = 24;

        let mut challenger = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 5),
            453,
            true,
            false,
            false,
        );
        challenger.root_rank = 0;
        challenger.inputs = vec![
            Input::Location(Location::new(1, 5)),
            Input::Location(Location::new(3, 7)),
            Input::Location(Location::new(2, 8)),
        ];
        challenger.spirit_own_mana_setup_now = true;
        challenger.safe_supermana_progress_steps = 15;
        challenger.safe_opponent_mana_progress_steps = 6;
        challenger.score_path_best_steps = 33;
        challenger.spirit_setup_gain = 96;

        let scored_roots = [approved, challenger];
        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.turn_engine_mode = TurnEngineMode::CurrentPro;

        assert_eq!(
            MonsGameModel::current_pro_root_advisor_black_late_reply_risk_setup_override(
                &game,
                &scored_roots,
                &[1],
                0,
                config,
            ),
            Some(1),
        );
    }

    #[test]
    fn black_late_reply_risk_setup_override_keeps_small_setup_gain_mana_root() {
        let game = MonsGame::from_fen(
            "1 1 b 0 0 0 0 0 8 d0xn10/n05s0xa0xe0xxxmn02/n11/n07xxmn03/n03xxmn02xxmn04/n10xxQ/n02y0xn01D0UxxMn01xxMn03/n02xxMS0xn01A0xxxMn04/n06Y0xn04/n03E0xn07/n11",
            false,
        )
        .expect("valid black late reply-risk setup fen");
        let mut approved = root_evaluation_for_projection_test(
            &game,
            Location::new(6, 2),
            2155,
            false,
            false,
            false,
        );
        approved.root_rank = 2;
        approved.inputs = vec![
            Input::Location(Location::new(6, 2)),
            Input::Location(Location::new(5, 3)),
        ];
        approved.safe_supermana_progress_steps = 15;
        approved.safe_opponent_mana_progress_steps = 6;
        approved.score_path_best_steps = 33;
        approved.spirit_setup_gain = 24;

        let mut challenger = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 5),
            453,
            true,
            false,
            false,
        );
        challenger.root_rank = 0;
        challenger.inputs = vec![
            Input::Location(Location::new(1, 5)),
            Input::Location(Location::new(3, 7)),
            Input::Location(Location::new(2, 8)),
        ];
        challenger.spirit_own_mana_setup_now = true;
        challenger.safe_supermana_progress_steps = 15;
        challenger.safe_opponent_mana_progress_steps = 6;
        challenger.score_path_best_steps = 33;
        challenger.spirit_setup_gain = 80;

        let scored_roots = [approved, challenger];
        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.turn_engine_mode = TurnEngineMode::CurrentPro;

        assert_eq!(
            MonsGameModel::current_pro_root_advisor_black_late_reply_risk_setup_override(
                &game,
                &scored_roots,
                &[1],
                0,
                config,
            ),
            None,
        );
    }

    #[test]
    fn black_late_weak_window_safe_progress_setup_override_prefers_own_setup_progress_root() {
        let game = MonsGame::from_fen(
            "2 1 b 0 0 0 0 0 10 n05d0xn03xxmn01/n04a0xn02e0xn03/n05s0xn05/E0xn02xxmn03xxmn03/n05xxmn05/n05xxUn04xxQ/n05xxMn05/n03S0xn01Y0xxxMn04/n03y0xn04xxMn02/n03A0xn07/n05D1xn05",
            false,
        )
        .expect("valid black confirm fast setup split fen");
        let mut approved = root_evaluation_for_projection_test(
            &game,
            Location::new(0, 5),
            -806_965,
            false,
            false,
            false,
        );
        approved.root_rank = 1;
        approved.inputs = vec![
            Input::Location(Location::new(0, 5)),
            Input::Location(Location::new(1, 5)),
        ];
        approved.supermana_progress = true;
        approved.safe_supermana_progress_steps = 4;
        approved.safe_opponent_mana_progress_steps = 5;
        approved.score_path_best_steps = 33;
        approved.spirit_setup_gain = 60;

        let mut shortlisted_plain = root_evaluation_for_projection_test(
            &game,
            Location::new(2, 5),
            -806_965,
            true,
            false,
            false,
        );
        shortlisted_plain.root_rank = 0;
        shortlisted_plain.inputs = vec![
            Input::Location(Location::new(2, 5)),
            Input::Location(Location::new(0, 5)),
            Input::Location(Location::new(1, 5)),
        ];
        shortlisted_plain.supermana_progress = true;
        shortlisted_plain.opponent_mana_progress = true;
        shortlisted_plain.safe_supermana_progress_steps = 4;
        shortlisted_plain.safe_opponent_mana_progress_steps = 5;
        shortlisted_plain.score_path_best_steps = 33;
        shortlisted_plain.spirit_setup_gain = 84;

        let mut shipping = root_evaluation_for_projection_test(
            &game,
            Location::new(2, 5),
            -806_965,
            true,
            false,
            false,
        );
        shipping.root_rank = 5;
        shipping.inputs = vec![
            Input::Location(Location::new(2, 5)),
            Input::Location(Location::new(3, 7)),
            Input::Location(Location::new(2, 8)),
        ];
        shipping.spirit_own_mana_setup_now = true;
        shipping.supermana_progress = true;
        shipping.safe_supermana_progress_steps = 5;
        shipping.safe_opponent_mana_progress_steps = 6;
        shipping.score_path_best_steps = 33;
        shipping.spirit_setup_gain = 124;

        let scored_roots = [approved, shortlisted_plain, shipping];
        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.turn_engine_mode = TurnEngineMode::CurrentPro;

        assert_eq!(
            MonsGameModel::current_pro_root_advisor_black_late_weak_window_safe_progress_setup_override(
                &game,
                &scored_roots,
                &[2, 1, 0],
                0,
                game.active_color,
                config,
            ),
            Some(2),
        );
    }

    #[test]
    fn black_late_weak_window_safe_progress_setup_override_keeps_small_setup_gain_root() {
        let game = MonsGame::from_fen(
            "2 1 b 0 0 0 0 0 10 n05d0xn03xxmn01/n04a0xn02e0xn03/n05s0xn05/E0xn02xxmn03xxmn03/n05xxmn05/n05xxUn04xxQ/n05xxMn05/n03S0xn01Y0xxxMn04/n03y0xn04xxMn02/n03A0xn07/n05D1xn05",
            false,
        )
        .expect("valid black confirm fast setup split fen");
        let mut approved = root_evaluation_for_projection_test(
            &game,
            Location::new(0, 5),
            -806_965,
            false,
            false,
            false,
        );
        approved.root_rank = 1;
        approved.inputs = vec![
            Input::Location(Location::new(0, 5)),
            Input::Location(Location::new(1, 5)),
        ];
        approved.supermana_progress = true;
        approved.safe_supermana_progress_steps = 4;
        approved.safe_opponent_mana_progress_steps = 5;
        approved.score_path_best_steps = 33;
        approved.spirit_setup_gain = 60;

        let mut challenger = root_evaluation_for_projection_test(
            &game,
            Location::new(2, 5),
            -806_965,
            true,
            false,
            false,
        );
        challenger.root_rank = 5;
        challenger.inputs = vec![
            Input::Location(Location::new(2, 5)),
            Input::Location(Location::new(3, 7)),
            Input::Location(Location::new(2, 8)),
        ];
        challenger.spirit_own_mana_setup_now = true;
        challenger.supermana_progress = true;
        challenger.safe_supermana_progress_steps = 5;
        challenger.safe_opponent_mana_progress_steps = 6;
        challenger.score_path_best_steps = 33;
        challenger.spirit_setup_gain = 120;

        let scored_roots = [approved, challenger];
        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.turn_engine_mode = TurnEngineMode::CurrentPro;

        assert_eq!(
            MonsGameModel::current_pro_root_advisor_black_late_weak_window_safe_progress_setup_override(
                &game,
                &scored_roots,
                &[1],
                0,
                game.active_color,
                config,
            ),
            None,
        );
    }

    #[test]
    fn white_early_safe_progress_setup_override_allows_large_rank_rescue() {
        let game = MonsGame::from_fen(
            "0 0 w 0 0 1 0 0 3 n05d0xn05/n05s0xa0xe0xn03/n03y0xn03xxmn03/n02xxmn01xxmn06/n05xxmn01xxmn03/xxQn04xxUn04xxQ/n03xxMn01xxMn01xxMn03/n04xxMn01xxMn04/n05D0xn05/n03E0xn01A0xn01S0xY0xn02/n11",
            false,
        )
        .expect("valid white fast ply10 fen");
        let mut approved = root_evaluation_for_projection_test(
            &game,
            Location::new(9, 7),
            1509,
            false,
            false,
            false,
        );
        approved.root_rank = 11;
        approved.inputs = vec![
            Input::Location(Location::new(9, 7)),
            Input::Location(Location::new(8, 6)),
        ];
        approved.supermana_progress = true;
        approved.safe_supermana_progress_steps = 3;
        approved.safe_opponent_mana_progress_steps = 4;
        approved.spirit_setup_gain = 68;

        let mut challenger = root_evaluation_for_projection_test(
            &game,
            Location::new(9, 7),
            1368,
            true,
            false,
            false,
        );
        challenger.root_rank = 1;
        challenger.inputs = vec![
            Input::Location(Location::new(9, 7)),
            Input::Location(Location::new(7, 6)),
            Input::Location(Location::new(7, 7)),
        ];
        challenger.spirit_own_mana_setup_now = true;
        challenger.supermana_progress = true;
        challenger.opponent_mana_progress = true;
        challenger.safe_supermana_progress_steps = 3;
        challenger.safe_opponent_mana_progress_steps = 4;
        challenger.spirit_setup_gain = 140;

        let scored_roots = [approved, challenger];
        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.turn_engine_mode = TurnEngineMode::CurrentPro;

        assert_eq!(
            MonsGameModel::current_pro_root_advisor_white_early_safe_progress_setup_competition_override(
                &game,
                &scored_roots,
                &[0, 1],
                0,
                config,
            ),
            Some(1),
        );
    }

    #[test]
    fn white_early_safe_progress_setup_override_keeps_large_gap_without_rank_rescue() {
        let game = MonsGame::from_fen(
            "0 0 w 0 0 1 0 0 3 n05d0xn05/n05s0xa0xe0xn03/n03y0xn03xxmn03/n02xxmn01xxmn06/n05xxmn01xxmn03/xxQn04xxUn04xxQ/n03xxMn01xxMn01xxMn03/n04xxMn01xxMn04/n05D0xn05/n03E0xn01A0xn01S0xY0xn02/n11",
            false,
        )
        .expect("valid white fast ply10 fen");
        let mut approved = root_evaluation_for_projection_test(
            &game,
            Location::new(9, 7),
            1509,
            false,
            false,
            false,
        );
        approved.root_rank = 5;
        approved.inputs = vec![
            Input::Location(Location::new(9, 7)),
            Input::Location(Location::new(8, 6)),
        ];
        approved.supermana_progress = true;
        approved.safe_supermana_progress_steps = 3;
        approved.safe_opponent_mana_progress_steps = 4;
        approved.spirit_setup_gain = 68;

        let mut challenger = root_evaluation_for_projection_test(
            &game,
            Location::new(9, 7),
            1368,
            true,
            false,
            false,
        );
        challenger.root_rank = 1;
        challenger.inputs = vec![
            Input::Location(Location::new(9, 7)),
            Input::Location(Location::new(7, 6)),
            Input::Location(Location::new(7, 7)),
        ];
        challenger.spirit_own_mana_setup_now = true;
        challenger.supermana_progress = true;
        challenger.opponent_mana_progress = true;
        challenger.safe_supermana_progress_steps = 3;
        challenger.safe_opponent_mana_progress_steps = 4;
        challenger.spirit_setup_gain = 140;

        let scored_roots = [approved, challenger];
        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.turn_engine_mode = TurnEngineMode::CurrentPro;

        assert_eq!(
            MonsGameModel::current_pro_root_advisor_white_early_safe_progress_setup_competition_override(
                &game,
                &scored_roots,
                &[0, 1],
                0,
                config,
            ),
            None,
        );
    }

    fn ranked_child_state_for_test(
        game: &MonsGame,
        hash: u64,
        ordering_efficiency: i32,
        classes: MoveClassFlags,
    ) -> RankedChildState {
        RankedChildState {
            game: game.clone_for_simulation(),
            hash,
            ordering_efficiency,
            tactical_extension_trigger: false,
            quiet_reduction_candidate: false,
            classes,
        }
    }

    #[test]
    fn child_priority_sort_prefers_higher_exact_ordering_efficiency_when_heuristic_tied() {
        let game = game_with_items(
            vec![
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            1,
        );

        let quiet = MoveClassFlags {
            quiet: true,
            ..MoveClassFlags::default()
        };
        let mut scored_states = [
            (100, ranked_child_state_for_test(&game, 1, 0, quiet)),
            (100, ranked_child_state_for_test(&game, 2, 40, quiet)),
        ];

        scored_states.sort_by(|a, b| MonsGameModel::compare_ranked_child_entries(a, b, true));
        assert_eq!(scored_states[0].1.hash, 2);
    }

    #[test]
    fn child_priority_sort_prefers_tactical_class_when_efficiency_tied() {
        let game = game_with_items(
            vec![
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            1,
        );

        let quiet = MoveClassFlags {
            quiet: true,
            ..MoveClassFlags::default()
        };
        let tactical = MoveClassFlags {
            carrier_progress: true,
            ..MoveClassFlags::default()
        };
        let mut scored_states = [
            (100, ranked_child_state_for_test(&game, 1, 20, quiet)),
            (100, ranked_child_state_for_test(&game, 2, 20, tactical)),
        ];

        scored_states.sort_by(|a, b| MonsGameModel::compare_ranked_child_entries(a, b, true));
        assert_eq!(scored_states[0].1.hash, 2);
    }

    #[test]
    fn quiet_reduction_candidate_skips_carrier_progress_class() {
        let classes = MoveClassFlags {
            carrier_progress: true,
            ..MoveClassFlags::default()
        };

        assert!(
            !MonsGameModel::is_quiet_reduction_candidate(-10, false, classes),
            "carrier-progress child should stay at full depth even when ordering efficiency is non-positive"
        );
    }

    #[test]
    fn quiet_reduction_candidate_still_applies_to_true_quiet_child() {
        let classes = MoveClassFlags {
            quiet: true,
            ..MoveClassFlags::default()
        };

        assert!(
            MonsGameModel::is_quiet_reduction_candidate(0, false, classes),
            "true quiet child should remain eligible for quiet reduction"
        );
    }

    #[test]
    fn child_search_priority_candidate_includes_positive_exact_continuation() {
        let game = game_with_items(
            vec![
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            1,
        );

        let quiet = MoveClassFlags {
            quiet: true,
            ..MoveClassFlags::default()
        };
        let child = ranked_child_state_for_test(&game, 1, 20, quiet);
        assert!(MonsGameModel::is_child_search_priority_candidate(&child));
    }

    #[test]
    fn selective_extension_candidate_preserves_existing_tactical_trigger() {
        let classes = MoveClassFlags {
            quiet: true,
            ..MoveClassFlags::default()
        };

        assert!(
            MonsGameModel::is_selective_extension_candidate(true, -20, classes),
            "existing tactical trigger should still qualify for selective extension"
        );
    }

    #[test]
    fn selective_extension_candidate_includes_positive_carrier_progress() {
        let classes = MoveClassFlags {
            carrier_progress: true,
            ..MoveClassFlags::default()
        };

        assert!(
            MonsGameModel::is_selective_extension_candidate(false, 15, classes),
            "positive exact carrier progress should qualify for selective extension"
        );
    }

    #[test]
    fn selective_extension_candidate_skips_nonpositive_carrier_progress() {
        let classes = MoveClassFlags {
            carrier_progress: true,
            ..MoveClassFlags::default()
        };

        assert!(
            !MonsGameModel::is_selective_extension_candidate(false, 0, classes),
            "non-improving carrier progress should not consume selective extension budget"
        );
    }

    #[test]
    fn selective_extension_candidate_skips_positive_material_only_child() {
        let classes = MoveClassFlags {
            material: true,
            ..MoveClassFlags::default()
        };

        assert!(
            !MonsGameModel::is_selective_extension_candidate(false, 20, classes),
            "material-only child should not consume selective extension budget without a tactical trigger"
        );
    }

    #[test]
    fn frontier_tactical_potential_detects_safe_supermana_progress() {
        let game = game_with_items(
            vec![
                (
                    Location::new(6, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        assert!(
            MonsGameModel::has_frontier_tactical_potential(&game),
            "same-turn safe supermana progress should block frontier futility pruning"
        );
    }

    #[test]
    fn frontier_tactical_potential_is_false_on_quiet_board() {
        let game = game_with_items(
            vec![
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            1,
        );

        assert!(
            !MonsGameModel::has_frontier_tactical_potential(&game),
            "quiet frontier state should remain eligible for futility pruning"
        );
    }

    #[test]
    fn frontier_tactical_potential_detects_spirit_supermana_progress() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(7, 1),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(10, 2),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(7, 3),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::Black, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        assert!(
            MonsGameModel::has_frontier_tactical_potential(&game),
            "spirit-assisted supermana progress should block frontier futility pruning"
        );
    }

    #[test]
    fn root_focus_scout_score_rewards_exact_safe_supermana_progress() {
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let mut progress = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(7, 5)),
                Input::Location(Location::new(6, 5)),
            ],
        )
        .expect("drainer advance should build a scored root");
        progress.heuristic = 0;
        progress.efficiency = 0;
        assert!(progress.supermana_progress);
        assert!(!progress.safe_supermana_pickup_now);
        assert!(!progress.spirit_own_mana_setup_now);

        let mut filler = progress.clone();
        filler.inputs = vec![Input::Location(Location::new(0, 0))];
        filler.supermana_progress = false;
        filler.safe_supermana_progress_steps = Config::BOARD_SIZE + 4;
        filler.heuristic = 0;
        filler.efficiency = 0;

        assert!(
            MonsGameModel::root_focus_scout_score(&progress)
                > MonsGameModel::root_focus_scout_score(&filler),
            "exact safe supermana progress should improve scout fallback score"
        );
    }

    #[test]
    fn tactical_child_top2_promotes_close_exact_continuation_child() {
        let game = game_with_items(
            vec![
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            1,
        );

        let quiet = MoveClassFlags {
            quiet: true,
            ..MoveClassFlags::default()
        };
        let mut scored_states = vec![
            (1_000, ranked_child_state_for_test(&game, 1, 0, quiet)),
            (995, ranked_child_state_for_test(&game, 2, 0, quiet)),
            (986, ranked_child_state_for_test(&game, 3, 20, quiet)),
        ];

        MonsGameModel::enforce_tactical_child_top2(&mut scored_states, true, false);
        assert_eq!(scored_states[1].1.hash, 3);
    }

    #[test]
    fn truncate_child_states_keeps_exact_continuation_within_margin() {
        let game = game_with_items(
            vec![
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            1,
        );

        let quiet = MoveClassFlags {
            quiet: true,
            ..MoveClassFlags::default()
        };
        let truncated = MonsGameModel::truncate_child_states_with_coverage(
            vec![
                (1_000, ranked_child_state_for_test(&game, 1, 0, quiet)),
                (950, ranked_child_state_for_test(&game, 2, 20, quiet)),
            ],
            1,
            true,
            false,
        );
        assert_eq!(truncated.len(), 1);
        assert_eq!(truncated[0].1.hash, 2);
    }

    #[test]
    fn truncate_child_states_drops_distant_exact_continuation_outside_margin() {
        let game = game_with_items(
            vec![
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            1,
        );

        let quiet = MoveClassFlags {
            quiet: true,
            ..MoveClassFlags::default()
        };
        let truncated = MonsGameModel::truncate_child_states_with_coverage(
            vec![
                (1_000, ranked_child_state_for_test(&game, 1, 0, quiet)),
                (800, ranked_child_state_for_test(&game, 2, 20, quiet)),
            ],
            1,
            true,
            false,
        );
        assert_eq!(truncated.len(), 1);
        assert_eq!(truncated[0].1.hash, 1);
    }

    #[test]
    fn truncate_root_candidates_keeps_exact_safe_supermana_progress() {
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let mut progress = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(7, 5)),
                Input::Location(Location::new(6, 5)),
            ],
        )
        .expect("drainer advance should build a scored root");
        progress.heuristic = 100;
        progress.efficiency = 0;
        assert!(progress.supermana_progress);
        assert!(progress.safe_supermana_progress_steps < Config::BOARD_SIZE + 4);
        assert!(!progress.safe_supermana_pickup_now);

        let mut filler_a = progress.clone();
        filler_a.inputs = vec![Input::Location(Location::new(0, 0))];
        filler_a.supermana_progress = false;
        filler_a.safe_supermana_progress_steps = Config::BOARD_SIZE + 4;
        filler_a.heuristic = 2_000;

        let mut filler_b = filler_a.clone();
        filler_b.inputs = vec![Input::Location(Location::new(0, 1))];
        filler_b.heuristic = 1_950;

        progress.heuristic = 1_885;

        let truncated = MonsGameModel::truncate_root_candidates_with_class_coverage(
            vec![filler_a, filler_b, progress.clone()],
            2,
            false,
        );
        assert!(
            truncated
                .iter()
                .any(|candidate| candidate.inputs == progress.inputs),
            "exact safe supermana progress root should survive root branch truncation"
        );
    }

    #[test]
    fn truncate_root_candidates_keeps_exact_safe_opponent_mana_progress() {
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 6),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 4),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let mut progress = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(7, 6)),
                Input::Location(Location::new(6, 5)),
            ],
        )
        .expect("drainer advance should build a scored root");
        progress.heuristic = 1_885;
        progress.efficiency = 0;
        assert!(progress.opponent_mana_progress);
        assert!(progress.safe_opponent_mana_progress_steps < Config::BOARD_SIZE + 4);
        assert!(!progress.safe_opponent_mana_pickup_now);

        let mut filler_a = progress.clone();
        filler_a.inputs = vec![Input::Location(Location::new(0, 0))];
        filler_a.opponent_mana_progress = false;
        filler_a.safe_opponent_mana_progress_steps = Config::BOARD_SIZE + 4;
        filler_a.heuristic = 2_000;

        let mut filler_b = filler_a.clone();
        filler_b.inputs = vec![Input::Location(Location::new(0, 1))];
        filler_b.heuristic = 1_950;

        let truncated = MonsGameModel::truncate_root_candidates_with_class_coverage(
            vec![filler_a, filler_b, progress.clone()],
            2,
            false,
        );
        assert!(
            truncated
                .iter()
                .any(|candidate| candidate.inputs == progress.inputs),
            "exact safe opponent-mana progress root should survive root branch truncation"
        );
    }

    #[test]
    fn truncate_root_candidates_prefers_stronger_exact_same_turn_score_window() {
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let mut stronger = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(7, 5)),
                Input::Location(Location::new(6, 5)),
            ],
        )
        .expect("drainer advance should build a scored root");
        stronger.inputs = vec![Input::Location(Location::new(1, 1))];
        stronger.heuristic = 1_900;
        stronger.efficiency = 0;
        stronger.same_turn_score_window_value = 2;
        stronger.spirit_same_turn_score_setup_now = true;
        stronger.own_drainer_vulnerable = false;
        stronger.mana_handoff_to_opponent = false;

        let mut weaker = stronger.clone();
        weaker.inputs = vec![Input::Location(Location::new(0, 0))];
        weaker.heuristic = 2_000;
        weaker.same_turn_score_window_value = 1;
        weaker.spirit_same_turn_score_setup_now = false;

        let truncated = MonsGameModel::truncate_root_candidates_with_class_coverage(
            vec![weaker, stronger.clone()],
            1,
            false,
        );
        assert_eq!(truncated.len(), 1);
        assert_eq!(
            truncated[0].inputs, stronger.inputs,
            "stronger exact same-turn score window root should survive branch truncation even when a weaker window root has higher heuristic"
        );
    }

    #[test]
    fn tactical_child_top2_promotes_close_carrier_progress_child() {
        let game = game_with_items(
            vec![
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            1,
        );

        let quiet = MoveClassFlags {
            quiet: true,
            ..MoveClassFlags::default()
        };
        let carrier_progress = MoveClassFlags {
            carrier_progress: true,
            ..MoveClassFlags::default()
        };
        let mut scored_states = vec![
            (1_000, ranked_child_state_for_test(&game, 1, 0, quiet)),
            (995, ranked_child_state_for_test(&game, 2, 0, quiet)),
            (
                992,
                ranked_child_state_for_test(&game, 3, 20, carrier_progress),
            ),
        ];

        MonsGameModel::enforce_tactical_child_top2(&mut scored_states, true, false);
        assert_eq!(scored_states[1].1.hash, 3);
    }

    #[test]
    fn tactical_child_top2_keeps_existing_carrier_progress_in_front() {
        let game = game_with_items(
            vec![
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            1,
        );

        let quiet = MoveClassFlags {
            quiet: true,
            ..MoveClassFlags::default()
        };
        let carrier_progress = MoveClassFlags {
            carrier_progress: true,
            ..MoveClassFlags::default()
        };
        let mut scored_states = vec![
            (
                1_000,
                ranked_child_state_for_test(&game, 1, 20, carrier_progress),
            ),
            (995, ranked_child_state_for_test(&game, 2, 0, quiet)),
            (994, ranked_child_state_for_test(&game, 3, 0, quiet)),
        ];

        MonsGameModel::enforce_tactical_child_top2(&mut scored_states, true, false);
        assert_eq!(scored_states[0].1.hash, 1);
        assert_eq!(scored_states[1].1.hash, 2);
    }

    #[test]
    fn opponent_spirit_supermana_progress_child_is_not_quiet_reduced() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(6, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::Black, 0),
                    },
                ),
                (
                    Location::new(3, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
                (
                    Location::new(5, 8),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
            ],
            Color::Black,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.node_enum_limit = 256;
        config.node_branch_limit = 64;
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::Black, true);
        let child = MonsGameModel::build_scored_root_move(
            &game,
            Color::Black,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(6, 10)),
                Input::Location(Location::new(5, 8)),
                Input::Location(Location::new(4, 9)),
            ],
        )
        .expect("mirrored opponent spirit supermana handoff inputs should build a scored root");
        let child_hash = MonsGameModel::search_state_hash(&child.game);

        let children = MonsGameModel::ranked_child_states(&game, Color::White, false, None, config);
        let ranked_child = children
            .iter()
            .find(|candidate| candidate.hash == child_hash)
            .expect("ranked child states should include mirrored opponent spirit setup");

        assert!(
            ranked_child.ordering_efficiency > 0,
            "dangerous opponent spirit setup should have positive actor-relative ordering efficiency"
        );
        assert!(
            !ranked_child.quiet_reduction_candidate,
            "dangerous opponent spirit setup should not be marked for quiet reduction"
        );
        assert!(
            MonsGameModel::is_selective_extension_candidate(
                ranked_child.tactical_extension_trigger,
                ranked_child.ordering_efficiency,
                ranked_child.classes,
            ),
            "dangerous opponent spirit setup should qualify for selective extension"
        );
    }

    #[test]
    fn shorter_exact_safe_supermana_progress_gets_higher_root_priority() {
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            1,
        );

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(7, 5)),
                Input::Location(Location::new(6, 5)),
            ],
        )
        .expect("short supermana progress inputs should build a scored root");
        let mut long = short.clone();
        long.safe_supermana_progress_steps = 2;
        long.interview_soft_priority = MonsGameModel::interview_root_soft_priority(
            config,
            true,
            false,
            2,
            Config::BOARD_SIZE + 4,
            false,
            false,
            false,
            false,
            false,
        );

        assert!(short.supermana_progress);
        assert_eq!(short.safe_supermana_progress_steps, 1);
        assert_eq!(
            short.interview_soft_priority,
            MonsGameModel::interview_root_soft_priority(
                config,
                true,
                false,
                1,
                Config::BOARD_SIZE + 4,
                false,
                false,
                false,
                false,
                false,
            )
        );
        assert!(short.interview_soft_priority > long.interview_soft_priority);
        assert!(MonsGameModel::is_better_tactical_root_candidate(
            &short, &long
        ));
        let picked = MonsGameModel::pick_root_move_with_exploration(
            &game,
            &[
                root_evaluation_for_test(&long, 100),
                root_evaluation_for_test(&short, 100),
            ],
            Color::White,
            config,
        );
        assert_eq!(picked, short.inputs);
    }

    #[test]
    fn shorter_exact_safe_opponent_mana_progress_gets_higher_root_priority() {
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 4),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            1,
        );

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(7, 5)),
                Input::Location(Location::new(6, 4)),
            ],
        )
        .expect("short opponent mana progress inputs should build a scored root");
        let mut long = short.clone();
        long.safe_opponent_mana_progress_steps = 2;
        long.interview_soft_priority = MonsGameModel::interview_root_soft_priority(
            config,
            false,
            true,
            Config::BOARD_SIZE + 4,
            2,
            false,
            false,
            false,
            false,
            false,
        );

        assert!(short.opponent_mana_progress);
        assert_eq!(short.safe_opponent_mana_progress_steps, 1);
        assert_eq!(
            short.interview_soft_priority,
            MonsGameModel::interview_root_soft_priority(
                config,
                false,
                true,
                Config::BOARD_SIZE + 4,
                1,
                false,
                false,
                false,
                false,
                false,
            )
        );
        assert!(short.interview_soft_priority > long.interview_soft_priority);
        assert!(MonsGameModel::is_better_tactical_root_candidate(
            &short, &long
        ));
        let picked = MonsGameModel::pick_root_move_with_exploration(
            &game,
            &[
                root_evaluation_for_test(&long, 100),
                root_evaluation_for_test(&short, 100),
            ],
            Color::White,
            config,
        );
        assert_eq!(picked, short.inputs);
    }

    #[test]
    fn ranked_root_moves_surface_safe_opponent_mana_pickup_when_available() {
        let white_spirit = Mon::new(MonKind::Spirit, Color::White, 0);
        let white_spirit_base = Board::new().base(white_spirit);
        let game = game_with_items(
            vec![
                (
                    Location::new(6, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 4),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (white_spirit_base, Item::Mon { mon: white_spirit }),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let roots = MonsGameModel::ranked_root_moves(
            &game,
            Color::White,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );

        assert!(
            roots.iter().any(|root| root.safe_opponent_mana_pickup_now),
            "expected a surfaced safe opponent-mana pickup root, got inputs={:?}",
            roots
                .iter()
                .map(|root| root.inputs.clone())
                .collect::<Vec<_>>()
        );
    }

    #[test]
    fn ranked_root_moves_surface_safe_supermana_pickup_with_root_cutoff_when_available() {
        let white_spirit = Mon::new(MonKind::Spirit, Color::White, 0);
        let white_spirit_base = Board::new().base(white_spirit);
        let game = game_with_items(
            vec![
                (
                    Location::new(6, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (white_spirit_base, Item::Mon { mon: white_spirit }),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.root_enum_limit = 1;
        let roots = MonsGameModel::ranked_root_moves(&game, Color::White, config);

        assert!(
            roots.iter().any(|root| root.safe_supermana_pickup_now),
            "expected a surfaced safe supermana pickup root under cutoff, got inputs={:?}",
            roots
                .iter()
                .map(|root| root.inputs.clone())
                .collect::<Vec<_>>()
        );
    }

    #[test]
    fn ranked_root_moves_surface_safe_opponent_mana_pickup_with_root_cutoff_when_available() {
        let white_spirit = Mon::new(MonKind::Spirit, Color::White, 0);
        let white_spirit_base = Board::new().base(white_spirit);
        let game = game_with_items(
            vec![
                (
                    Location::new(6, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 4),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (white_spirit_base, Item::Mon { mon: white_spirit }),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.root_enum_limit = 1;
        let roots = MonsGameModel::ranked_root_moves(&game, Color::White, config);

        assert!(
            roots.iter().any(|root| root.safe_opponent_mana_pickup_now),
            "expected a surfaced safe opponent-mana pickup root under cutoff, got inputs={:?}",
            roots
                .iter()
                .map(|root| root.inputs.clone())
                .collect::<Vec<_>>()
        );
    }

    #[test]
    fn ranked_root_moves_surface_spirit_opponent_mana_score_when_available() {
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 1),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
                (
                    Location::new(10, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let roots = MonsGameModel::ranked_root_moves(
            &game,
            Color::White,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );

        assert!(
            roots.iter().any(|root| {
                MonsGameModel::apply_inputs_for_search_with_events(&game, &root.inputs)
                    .map(|(after, events)| {
                        MonsGameModel::events_include_spirit_target_move(&events)
                            && MonsGameModel::events_score_opponent_mana(&events, Color::White)
                            && after.white_score >= game.white_score + 2
                    })
                    .unwrap_or(false)
            }),
            "expected a surfaced spirit opponent-mana score root, got inputs={:?}",
            roots
                .iter()
                .map(|root| root.inputs.clone())
                .collect::<Vec<_>>()
        );
    }

    #[test]
    fn ranked_root_moves_surface_spirit_setup_into_same_turn_score_when_available() {
        let game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(4, 1),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 0),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        assert!(
            crate::models::automove_exact::exact_turn_summary(&game, Color::White)
                .spirit_assisted_score
        );
        let roots = MonsGameModel::ranked_root_moves(
            &game,
            Color::White,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );

        assert!(
            roots.iter().any(|root| {
                MonsGameModel::apply_inputs_for_search_with_events(&game, &root.inputs)
                    .map(|(after, _)| {
                        crate::models::automove_exact::exact_state_analysis(&after)
                            .color_summary(Color::White)
                            .immediate_window
                            .best_score
                            > 0
                    })
                    .unwrap_or(false)
            }),
            "expected a surfaced root that preserves the spirit-assisted same-turn score window, got inputs={:?}",
            roots
                .iter()
                .map(|root| root.inputs.clone())
                .collect::<Vec<_>>()
        );
    }

    #[test]
    fn selected_line_preserves_same_turn_supermana_threat_when_available() {
        let white_spirit = Mon::new(MonKind::Spirit, Color::White, 0);
        let white_spirit_base = Board::new().base(white_spirit);
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (white_spirit_base, Item::Mon { mon: white_spirit }),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let exact_turn_before =
            crate::models::automove_exact::exact_turn_summary(&game, Color::White);
        assert!(
            exact_turn_before.safe_supermana_progress,
            "scenario should start with an exact same-turn supermana threat"
        );

        let inputs = MonsGameModel::smart_search_best_inputs(
            &game,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );
        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected supermana-threat inputs should be legal");
        let exact_turn_after =
            crate::models::automove_exact::exact_turn_summary(&after, Color::White);
        assert!(
            exact_turn_after.safe_supermana_progress
                || after.board.occupied().find(|(_, item)| matches!(
                        item,
                        Item::MonWithMana {
                            mon,
                            mana: Mana::Supermana,
                        } if mon.color == Color::White && mon.kind == MonKind::Drainer
                    )).is_some(),
            "selected line should preserve the exact same-turn supermana threat, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn selected_line_preserves_same_turn_supermana_threat_with_root_cutoff() {
        let white_spirit = Mon::new(MonKind::Spirit, Color::White, 0);
        let white_spirit_base = Board::new().base(white_spirit);
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (white_spirit_base, Item::Mon { mon: white_spirit }),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let exact_turn_before =
            crate::models::automove_exact::exact_turn_summary(&game, Color::White);
        assert!(
            exact_turn_before.safe_supermana_progress,
            "scenario should start with an exact same-turn supermana threat"
        );

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.root_enum_limit = 0;
        let inputs = MonsGameModel::smart_search_best_inputs(&game, config);
        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected supermana-threat inputs should be legal");
        let exact_turn_after =
            crate::models::automove_exact::exact_turn_summary(&after, Color::White);
        assert!(
            exact_turn_after.safe_supermana_progress
                || after.board.occupied().find(|(_, item)| matches!(
                        item,
                        Item::MonWithMana {
                            mon,
                            mana: Mana::Supermana,
                        } if mon.color == Color::White && mon.kind == MonKind::Drainer
                    )).is_some(),
            "selected line should preserve the exact same-turn supermana threat under capped root enumeration, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn spirit_converts_supermana_with_root_cutoff_when_direct_pickup_is_unsafe() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(7, 1),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(10, 2),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(7, 3),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::Black, 0),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;
        let exact_turn_before =
            crate::models::automove_exact::exact_turn_summary(&game, Color::White);
        assert!(
            !exact_turn_before.safe_supermana_progress,
            "direct safe supermana pickup should be unavailable before the spirit move"
        );

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.root_enum_limit = 0;
        let inputs = MonsGameModel::smart_search_best_inputs(&game, config);
        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected spirit-supermana inputs should be legal");
        let spirit_supermana_to_safe_drainer =
            events.iter().any(|event| {
                matches!(
                    event,
                    Event::SpiritTargetMove {
                        item: Item::Mana {
                            mana: Mana::Supermana,
                        },
                        to,
                        ..
                    } if *to == Location::new(10, 2)
                )
            }) && after.board.occupied().any(|(location, item)| {
                matches!(
                    item,
                    Item::MonWithMana {
                        mon,
                        mana: Mana::Supermana,
                    } if location == Location::new(10, 2)
                        && mon.color == Color::White
                        && mon.kind == MonKind::Drainer
                )
            });
        assert!(
            after.white_score >= game.white_score + 2 || spirit_supermana_to_safe_drainer,
            "selected line should convert the spirit supermana opportunity into either an immediate score or a safe drainer handoff under capped root enumeration, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn move_efficiency_rewards_faster_exact_safe_supermana_progress() {
        let game = game_with_items(
            vec![
                (
                    Location::new(6, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let exact_turn_before =
            crate::models::automove_exact::exact_turn_summary(&game, Color::White);
        assert_eq!(exact_turn_before.safe_supermana_progress_steps, Some(1));

        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(
            &game,
            &[
                Input::Location(Location::new(6, 5)),
                Input::Location(Location::new(5, 5)),
            ],
        )
        .expect("shortening supermana path inputs should be legal");
        let exact_turn_after =
            crate::models::automove_exact::exact_turn_summary(&after, Color::White);
        assert_eq!(exact_turn_after.safe_supermana_progress_steps, Some(0));

        let delta = MonsGameModel::move_efficiency_delta(
            &game,
            &after,
            Color::White,
            &events,
            false,
            false,
            false,
            true,
            false,
            0,
            0,
        );
        assert!(delta > 0);
    }

    #[test]
    fn move_efficiency_rewards_faster_exact_safe_opponent_mana_progress() {
        let game = game_with_items(
            vec![
                (
                    Location::new(6, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 4),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let exact_turn_before =
            crate::models::automove_exact::exact_turn_summary(&game, Color::White);
        assert_eq!(exact_turn_before.safe_opponent_mana_progress_steps, Some(1));

        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(
            &game,
            &[
                Input::Location(Location::new(6, 5)),
                Input::Location(Location::new(5, 4)),
            ],
        )
        .expect("shortening opponent mana path inputs should be legal");
        let exact_turn_after =
            crate::models::automove_exact::exact_turn_summary(&after, Color::White);
        assert_eq!(exact_turn_after.safe_opponent_mana_progress_steps, Some(0));

        let delta = MonsGameModel::move_efficiency_delta(
            &game,
            &after,
            Color::White,
            &events,
            false,
            false,
            false,
            true,
            false,
            0,
            0,
        );
        assert!(delta > 0);
    }

    #[test]
    fn spirit_moves_own_mana_closer_to_pool_when_setup_is_strongest() {
        let white_spirit = Mon::new(MonKind::Spirit, Color::White, 0);
        let game = game_with_items(
            vec![
                (Location::new(9, 7), Item::Mon { mon: white_spirit }),
                (
                    Location::new(9, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 8),
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let inputs = MonsGameModel::smart_search_best_inputs(
            &game,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );
        let (_, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected spirit-setup inputs should be legal");
        let spirit_moved_own_mana_closer = events.iter().any(|event| {
            let Event::SpiritTargetMove {
                item:
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                from,
                to,
                ..
            } = event
            else {
                return false;
            };
            let from_pool_steps = from
                .distance(&Location::new(10, 0))
                .min(from.distance(&Location::new(10, 10)));
            let to_pool_steps = to
                .distance(&Location::new(10, 0))
                .min(to.distance(&Location::new(10, 10)));
            to_pool_steps < from_pool_steps
        });
        assert!(
            spirit_moved_own_mana_closer,
            "selected line should use spirit to move own mana closer to a pool, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn spirit_supermana_setup_prefers_shorter_exact_secure_continuation() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit supermana handoff inputs should build a scored root");
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_supermana_progress_steps = short.safe_supermana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);

        assert!(short.spirit_own_mana_setup_now);
        assert!(short.supermana_progress);
        assert!(short.safe_supermana_progress_steps < Config::BOARD_SIZE + 4);
        assert!(short.score_path_best_steps > 0);
        assert!(long.score_path_best_steps < short.score_path_best_steps);
        assert!(MonsGameModel::is_better_tactical_root_candidate(
            &short, &long
        ));
        let picked = MonsGameModel::pick_root_move_with_exploration(
            &game,
            &[
                root_evaluation_for_test(&long, 100),
                root_evaluation_for_test(&short, 100),
            ],
            Color::White,
            config,
        );
        assert_eq!(picked, short.inputs);
    }

    #[test]
    fn filtered_root_candidates_prefer_spirit_supermana_exact_secure_continuation() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit supermana handoff inputs should build a scored root");
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_supermana_progress_steps = short.safe_supermana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);

        let filtered = MonsGameModel::filtered_root_candidate_indices(
            &game,
            &[
                root_evaluation_for_test(&long, 100),
                root_evaluation_for_test(&short, 100),
            ],
            Color::White,
            config,
        );
        assert_eq!(filtered, vec![1]);
    }

    #[test]
    fn root_priority_sort_prefers_spirit_supermana_exact_continuation_when_heuristic_tied() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let mut short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit supermana handoff inputs should build a scored root");
        short.heuristic = 100;
        short.efficiency = 0;
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_supermana_progress_steps = short.safe_supermana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);
        long.heuristic = 100;
        long.efficiency = 0;

        let short_inputs = short.inputs.clone();
        let long_inputs = long.inputs.clone();
        let mut candidates = vec![long, short];
        MonsGameModel::sort_root_candidates_by_search_priority(candidates.as_mut_slice());

        let short_pos = candidates
            .iter()
            .position(|candidate| candidate.inputs == short_inputs)
            .expect("short spirit root should remain in sorted roots");
        let long_pos = candidates
            .iter()
            .position(|candidate| candidate.inputs == long_inputs)
            .expect("long spirit root should remain in sorted roots");
        assert!(short_pos < long_pos);
    }

    #[test]
    fn ranked_score_sort_prefers_spirit_supermana_exact_continuation_when_score_tied() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit supermana handoff inputs should build a scored root");
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_supermana_progress_steps = short.safe_supermana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);

        let short_inputs = short.inputs.clone();
        let long_inputs = long.inputs.clone();
        let ordered =
            MonsGameModel::sort_root_moves_by_ranked_scores(vec![long, short], &[100, 100]);
        let short_pos = ordered
            .iter()
            .position(|candidate| candidate.inputs == short_inputs)
            .expect("short spirit root should remain in ranked-score order");
        let long_pos = ordered
            .iter()
            .position(|candidate| candidate.inputs == long_inputs)
            .expect("long spirit root should remain in ranked-score order");
        assert!(short_pos < long_pos);
    }

    #[test]
    fn reply_risk_shortlist_prefers_spirit_supermana_exact_continuation_when_score_tied() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.root_reply_risk_score_margin = 0;
        config.root_reply_risk_shortlist_max = 1;
        config.root_reply_risk_reply_limit = 1;
        config.root_reply_risk_node_share_bp = 1_000;
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit supermana handoff inputs should build a scored root");
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_supermana_progress_steps = short.safe_supermana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);

        let picked = MonsGameModel::pick_root_move_with_reply_risk_guard(
            &game,
            &[
                root_evaluation_for_test(&long, 100),
                root_evaluation_for_test(&short, 100),
            ],
            &[0, 1],
            Color::White,
            config,
        );
        assert_eq!(picked, Some(1));
    }

    #[test]
    fn reply_risk_spirit_tiebreak_stays_ahead_of_turn_engine_projection_ordering() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.enable_turn_engine_selector = true;
        config.enable_interview_deterministic_tiebreak = true;
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("short spirit supermana handoff inputs should build a scored root");
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_supermana_progress_steps = short.safe_supermana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);

        let scored_roots = [
            root_evaluation_for_test(&long, 100),
            root_evaluation_for_test(&short, 100),
        ];
        let snapshot = RootReplyRiskSnapshot {
            allows_immediate_opponent_win: false,
            opponent_reaches_match_point: false,
            worst_reply_score: 100,
        };
        let short_projection = turn_engine_projection_for_test(
            &scored_roots[1].game,
            TurnPlanFamily::ImmediateScore,
            10,
            Location::new(1, 0),
        );
        let long_projection = turn_engine_projection_for_test(
            &scored_roots[0].game,
            TurnPlanFamily::DrainerKill,
            200,
            Location::new(0, 0),
        );

        assert!(
            MonsGameModel::is_better_reply_risk_candidate(
                &game,
                1,
                snapshot,
                0,
                snapshot,
                Some(&short_projection),
                Some(&long_projection),
                &scored_roots,
                Color::White,
                config,
                &mut std::collections::HashMap::new(),
            ),
            "spirit/setup deterministic tie-break should win before turn-engine plan ordering",
        );
    }

    #[test]
    fn normal_root_safety_shortlist_prefers_spirit_supermana_exact_continuation_when_score_tied() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.enable_normal_root_safety_deep_floor = false;
        config.node_enum_limit = 1;
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit supermana handoff inputs should build a scored root");
        let mut long = short.clone();
        long.safe_supermana_progress_steps = short.safe_supermana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);

        let picked = MonsGameModel::pick_root_move_with_normal_safety(
            &game,
            &[
                root_evaluation_for_test(&long, 100),
                root_evaluation_for_test(&short, 100),
            ],
            &[0, 1],
            Color::White,
            config,
        );
        assert_eq!(picked, short.inputs);
    }

    #[test]
    fn focused_root_candidates_prioritize_spirit_supermana_exact_continuation_when_scout_tied() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.depth = 2;
        config.root_focus_k = 2;
        config.enable_two_pass_root_allocation = true;
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let mut short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit supermana handoff inputs should build a scored root");
        short.heuristic = 100;
        short.efficiency = 0;
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_supermana_progress_steps = short.safe_supermana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);
        long.heuristic = 100;
        long.efficiency = 0;

        let mut filler = short.clone();
        filler.inputs = vec![Input::Location(Location::new(0, 1))];
        filler.spirit_own_mana_setup_now = false;
        filler.supermana_progress = false;
        filler.score_path_best_steps = Config::BOARD_SIZE * 3;
        filler.safe_supermana_progress_steps = Config::BOARD_SIZE + 4;
        filler.heuristic = 2_000;
        filler.efficiency = 0;

        let mut low = short.clone();
        low.inputs = vec![Input::Location(Location::new(0, 2))];
        low.spirit_own_mana_setup_now = false;
        low.supermana_progress = false;
        low.score_path_best_steps = Config::BOARD_SIZE * 3;
        low.safe_supermana_progress_steps = Config::BOARD_SIZE + 4;
        low.heuristic = -1_000;
        low.efficiency = 0;

        let short_inputs = short.inputs.clone();
        let long_inputs = long.inputs.clone();
        let (focused, _) = MonsGameModel::focused_root_candidates(
            &game,
            Color::White,
            vec![long, filler, low, short],
            config,
            true,
        );
        let short_pos = focused
            .iter()
            .position(|candidate| candidate.inputs == short_inputs)
            .expect("short spirit root should survive focus");
        let long_pos = focused
            .iter()
            .position(|candidate| candidate.inputs == long_inputs)
            .expect("long spirit root should survive focus");
        assert!(short_pos < long_pos);
    }

    #[test]
    fn focused_root_candidates_keep_exact_safe_supermana_progress_with_scout_bonus() {
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.depth = 2;
        config.root_focus_k = 1;
        config.enable_two_pass_root_allocation = true;
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let mut progress = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(7, 5)),
                Input::Location(Location::new(6, 5)),
            ],
        )
        .expect("drainer advance should build a scored root");
        progress.heuristic = 0;
        progress.efficiency = 0;
        assert!(progress.supermana_progress);
        assert!(!progress.safe_supermana_pickup_now);
        assert!(!progress.spirit_own_mana_setup_now);

        let mut filler = progress.clone();
        filler.inputs = vec![Input::Location(Location::new(0, 0))];
        filler.supermana_progress = false;
        filler.safe_supermana_progress_steps = Config::BOARD_SIZE + 4;
        filler.score_path_best_steps = Config::BOARD_SIZE * 3;
        filler.heuristic = 2_500;
        filler.efficiency = 0;

        let progress_inputs = progress.inputs.clone();
        let (focused, _) = MonsGameModel::focused_root_candidates(
            &game,
            Color::White,
            vec![filler, progress],
            config,
            true,
        );

        assert!(
            focused.iter().any(|candidate| candidate.inputs == progress_inputs),
            "exact safe supermana progress root should survive scout focus despite lower raw heuristic"
        );
    }

    #[test]
    fn focused_root_candidates_prioritize_spirit_supermana_exact_continuation_on_narrow_spread_fallback(
    ) {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.depth = 2;
        config.root_focus_k = 2;
        config.enable_two_pass_root_allocation = true;
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let mut short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit supermana handoff inputs should build a scored root");
        short.heuristic = 100;
        short.efficiency = 0;
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_supermana_progress_steps = short.safe_supermana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);
        long.heuristic = 100;
        long.efficiency = 0;

        let mut filler = short.clone();
        filler.inputs = vec![Input::Location(Location::new(0, 1))];
        filler.spirit_own_mana_setup_now = false;
        filler.supermana_progress = false;
        filler.score_path_best_steps = Config::BOARD_SIZE * 3;
        filler.safe_supermana_progress_steps = Config::BOARD_SIZE + 4;
        filler.heuristic = 0;
        filler.efficiency = 0;

        let short_inputs = short.inputs.clone();
        let long_inputs = long.inputs.clone();
        let (focused, _) = MonsGameModel::focused_root_candidates(
            &game,
            Color::White,
            vec![long, filler, short],
            config,
            true,
        );
        assert_eq!(focused.len(), 3);
        let short_pos = focused
            .iter()
            .position(|candidate| candidate.inputs == short_inputs)
            .expect("short spirit root should survive narrow fallback");
        let long_pos = focused
            .iter()
            .position(|candidate| candidate.inputs == long_inputs)
            .expect("long spirit root should survive narrow fallback");
        assert!(short_pos < long_pos);
    }

    #[test]
    fn spirit_opponent_mana_setup_prefers_shorter_exact_secure_continuation() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit opponent mana handoff inputs should build a scored root");
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_opponent_mana_progress_steps = short.safe_opponent_mana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);

        assert!(short.spirit_own_mana_setup_now);
        assert!(short.opponent_mana_progress);
        assert!(short.safe_opponent_mana_progress_steps < Config::BOARD_SIZE + 4);
        assert!(short.score_path_best_steps > 0);
        assert!(long.score_path_best_steps < short.score_path_best_steps);
        assert!(MonsGameModel::is_better_tactical_root_candidate(
            &short, &long
        ));
        let picked = MonsGameModel::pick_root_move_with_exploration(
            &game,
            &[
                root_evaluation_for_test(&long, 100),
                root_evaluation_for_test(&short, 100),
            ],
            Color::White,
            config,
        );
        assert_eq!(picked, short.inputs);
    }

    #[test]
    fn close_spirit_projection_prefers_immediate_score_goal_when_primary_axes_tie() {
        let game = MonsGame::new(false, GameVariant::Classic);
        let winning_projection = TurnEngineRootProjection {
            plan: TurnPlan {
                actions: Vec::new(),
                compiled_chunks: vec![vec![Input::Location(Location::new(1, 3))]],
                end_game: game.clone_for_simulation(),
                end_snapshot: TurnSnapshot::from_game(&game),
                utility: TurnEngineUtility::from_components_for_test(0, 1, 616, -220, 0, 2, 606),
                head_utility: TurnEngineUtility::from_components_for_test(
                    0, 1, -306, -220, 0, -1, 403,
                ),
                head_family: TurnPlanFamily::SpiritImpact,
                goal_family: TurnPlanFamily::ImmediateScore,
                package_meta: TurnPackageMeta::default(),
            },
        };
        let losing_projection = TurnEngineRootProjection {
            plan: TurnPlan {
                actions: Vec::new(),
                compiled_chunks: vec![vec![Input::Location(Location::new(1, 4))]],
                end_game: game.clone_for_simulation(),
                end_snapshot: TurnSnapshot::from_game(&game),
                utility: TurnEngineUtility::from_components_for_test(0, 1, 616, -220, 0, 2, 790),
                head_utility: TurnEngineUtility::from_components_for_test(
                    0, 1, -306, -220, 0, -1, 403,
                ),
                head_family: TurnPlanFamily::SpiritImpact,
                goal_family: TurnPlanFamily::SpiritImpact,
                package_meta: TurnPackageMeta::default(),
            },
        };

        assert_eq!(
            MonsGameModel::compare_spirit_root_projection_plans(
                &winning_projection,
                &losing_projection,
                true,
            ),
            std::cmp::Ordering::Greater,
        );
    }

    #[test]
    fn current_pro_prefers_tactical_goal_then_safe_recovery() {
        let mut game = MonsGame::new(false, GameVariant::Classic);
        game.active_color = Color::Black;
        game.turn_number = 2;

        let mut spirit_root = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 3),
            924,
            true,
            false,
            false,
        );
        spirit_root.efficiency = 90;
        let mut progress_root = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 5),
            924,
            true,
            false,
            false,
        );
        progress_root.efficiency = 90;

        let mut spirit_projection = TurnEngineRootProjection {
            plan: TurnPlan {
                actions: Vec::new(),
                compiled_chunks: vec![vec![Input::Location(Location::new(1, 3))]],
                end_game: game.clone_for_simulation(),
                end_snapshot: TurnSnapshot::from_game(&game),
                utility: TurnEngineUtility::from_components_for_test(0, 1, 616, -220, 0, 2, 365),
                head_utility: TurnEngineUtility::from_components_for_test(
                    0, 1, 594, -220, 0, 2, 479,
                ),
                head_family: TurnPlanFamily::SpiritImpact,
                goal_family: TurnPlanFamily::ImmediateScore,
                package_meta: TurnPackageMeta::default(),
            },
        };
        let progress_projection = TurnEngineRootProjection {
            plan: TurnPlan {
                actions: Vec::new(),
                compiled_chunks: vec![vec![Input::Location(Location::new(1, 5))]],
                end_game: game.clone_for_simulation(),
                end_snapshot: TurnSnapshot::from_game(&game),
                utility: TurnEngineUtility::from_components_for_test(0, 1, 616, -220, 0, 2, 898),
                head_utility: TurnEngineUtility::from_components_for_test(
                    0, 1, -306, -220, 0, -1, 346,
                ),
                head_family: TurnPlanFamily::SafeSupermanaProgress,
                goal_family: TurnPlanFamily::DrainerSafetyRecovery,
                package_meta: TurnPackageMeta::default(),
            },
        };
        let scored_roots = [spirit_root, progress_root];
        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Pro);
        config.enable_turn_engine_selector = true;
        config.turn_engine_mode = TurnEngineMode::CurrentPro;

        assert_eq!(
            MonsGameModel::current_pro_mixed_plain_spirit_projection_order(
                &game,
                &scored_roots,
                0,
                &spirit_projection,
                1,
                &progress_projection,
                Color::Black,
                config,
                &mut std::collections::HashMap::new(),
            ),
            Some(std::cmp::Ordering::Greater),
        );

        spirit_projection.plan.goal_family = TurnPlanFamily::SpiritImpact;
        assert_eq!(
            MonsGameModel::current_pro_mixed_plain_spirit_projection_order(
                &game,
                &scored_roots,
                1,
                &progress_projection,
                0,
                &spirit_projection,
                Color::Black,
                config,
                &mut std::collections::HashMap::new(),
            ),
            Some(std::cmp::Ordering::Greater),
        );
    }

    #[test]
    fn current_pro_spirit_challenge_accepts_only_safe_non_spirit_roots() {
        let mut game = MonsGame::new(false, GameVariant::Classic);
        game.active_color = Color::Black;
        let spirit_root = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 4),
            924,
            true,
            false,
            false,
        );
        let challenger_root = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 2),
            904,
            false,
            false,
            false,
        );
        let spirit_projection = TurnEngineRootProjection {
            plan: TurnPlan {
                actions: Vec::new(),
                compiled_chunks: vec![vec![Input::Location(Location::new(1, 4))]],
                end_game: game.clone_for_simulation(),
                end_snapshot: TurnSnapshot::from_game(&game),
                utility: TurnEngineUtility::from_components_for_test(0, 1, 572, -220, 0, 2, 790),
                head_utility: TurnEngineUtility::from_components_for_test(
                    0, 1, 572, -220, 0, 2, 503,
                ),
                head_family: TurnPlanFamily::SpiritImpact,
                goal_family: TurnPlanFamily::SpiritImpact,
                package_meta: TurnPackageMeta::default(),
            },
        };
        let challenger_projection = TurnEngineRootProjection {
            plan: TurnPlan {
                actions: Vec::new(),
                compiled_chunks: vec![vec![Input::Location(Location::new(1, 2))]],
                end_game: game.clone_for_simulation(),
                end_snapshot: TurnSnapshot::from_game(&game),
                utility: TurnEngineUtility::from_components_for_test(0, 1, 616, -220, 0, 2, 640),
                head_utility: TurnEngineUtility::from_components_for_test(
                    0, 1, 616, -220, 0, 2, 520,
                ),
                head_family: TurnPlanFamily::ManaTempo,
                goal_family: TurnPlanFamily::ImmediateScore,
                package_meta: TurnPackageMeta::default(),
            },
        };

        assert_eq!(
            MonsGameModel::current_pro_spirit_projection_challenge_order(
                &challenger_root,
                Some(&challenger_projection),
                &spirit_root,
                Some(&spirit_projection),
            ),
            Some(std::cmp::Ordering::Greater),
        );

        let unsafe_challenger = root_evaluation_for_projection_test(
            &game,
            Location::new(1, 2),
            904,
            false,
            true,
            false,
        );
        let unsafe_projection = turn_engine_projection_for_test(
            &game,
            TurnPlanFamily::ManaTempo,
            900,
            Location::new(1, 2),
        );
        assert_eq!(
            MonsGameModel::current_pro_spirit_projection_challenge_order(
                &unsafe_challenger,
                Some(&unsafe_projection),
                &spirit_root,
                Some(&spirit_projection),
            ),
            None,
        );
    }

    #[test]
    fn filtered_root_candidates_prefer_spirit_opponent_exact_secure_continuation() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit opponent mana handoff inputs should build a scored root");
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_opponent_mana_progress_steps = short.safe_opponent_mana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);

        let filtered = MonsGameModel::filtered_root_candidate_indices(
            &game,
            &[
                root_evaluation_for_test(&long, 100),
                root_evaluation_for_test(&short, 100),
            ],
            Color::White,
            config,
        );
        assert_eq!(filtered, vec![1]);
    }

    #[test]
    fn root_priority_sort_prefers_spirit_opponent_exact_continuation_when_heuristic_tied() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let mut short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit opponent mana handoff inputs should build a scored root");
        short.heuristic = 100;
        short.efficiency = 0;
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_opponent_mana_progress_steps = short.safe_opponent_mana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);
        long.heuristic = 100;
        long.efficiency = 0;

        let short_inputs = short.inputs.clone();
        let long_inputs = long.inputs.clone();
        let mut candidates = vec![long, short];
        MonsGameModel::sort_root_candidates_by_search_priority(candidates.as_mut_slice());

        let short_pos = candidates
            .iter()
            .position(|candidate| candidate.inputs == short_inputs)
            .expect("short spirit root should remain in sorted roots");
        let long_pos = candidates
            .iter()
            .position(|candidate| candidate.inputs == long_inputs)
            .expect("long spirit root should remain in sorted roots");
        assert!(short_pos < long_pos);
    }

    #[test]
    fn ranked_score_sort_prefers_spirit_opponent_exact_continuation_when_score_tied() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit opponent mana handoff inputs should build a scored root");
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_opponent_mana_progress_steps = short.safe_opponent_mana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);

        let short_inputs = short.inputs.clone();
        let long_inputs = long.inputs.clone();
        let ordered =
            MonsGameModel::sort_root_moves_by_ranked_scores(vec![long, short], &[100, 100]);
        let short_pos = ordered
            .iter()
            .position(|candidate| candidate.inputs == short_inputs)
            .expect("short spirit root should remain in ranked-score order");
        let long_pos = ordered
            .iter()
            .position(|candidate| candidate.inputs == long_inputs)
            .expect("long spirit root should remain in ranked-score order");
        assert!(short_pos < long_pos);
    }

    #[test]
    fn reply_risk_shortlist_prefers_spirit_opponent_exact_continuation_when_score_tied() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.root_reply_risk_score_margin = 0;
        config.root_reply_risk_shortlist_max = 1;
        config.root_reply_risk_reply_limit = 1;
        config.root_reply_risk_node_share_bp = 1_000;
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit opponent mana handoff inputs should build a scored root");
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_opponent_mana_progress_steps = short.safe_opponent_mana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);

        let picked = MonsGameModel::pick_root_move_with_reply_risk_guard(
            &game,
            &[
                root_evaluation_for_test(&long, 100),
                root_evaluation_for_test(&short, 100),
            ],
            &[0, 1],
            Color::White,
            config,
        );
        assert_eq!(picked, Some(1));
    }

    #[test]
    fn normal_root_safety_shortlist_prefers_spirit_opponent_exact_continuation_when_score_tied() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.enable_normal_root_safety_deep_floor = false;
        config.node_enum_limit = 1;
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit opponent mana handoff inputs should build a scored root");
        let mut long = short.clone();
        long.safe_opponent_mana_progress_steps = short.safe_opponent_mana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);

        let picked = MonsGameModel::pick_root_move_with_normal_safety(
            &game,
            &[
                root_evaluation_for_test(&long, 100),
                root_evaluation_for_test(&short, 100),
            ],
            &[0, 1],
            Color::White,
            config,
        );
        assert_eq!(picked, short.inputs);
    }

    #[test]
    fn focused_root_candidates_prioritize_spirit_opponent_exact_continuation_when_scout_tied() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.depth = 2;
        config.root_focus_k = 2;
        config.enable_two_pass_root_allocation = true;
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let mut short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit opponent mana handoff inputs should build a scored root");
        short.heuristic = 100;
        short.efficiency = 0;
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_opponent_mana_progress_steps = short.safe_opponent_mana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);
        long.heuristic = 100;
        long.efficiency = 0;

        let mut filler = short.clone();
        filler.inputs = vec![Input::Location(Location::new(0, 1))];
        filler.spirit_own_mana_setup_now = false;
        filler.opponent_mana_progress = false;
        filler.score_path_best_steps = Config::BOARD_SIZE * 3;
        filler.safe_opponent_mana_progress_steps = Config::BOARD_SIZE + 4;
        filler.heuristic = 2_000;
        filler.efficiency = 0;

        let mut low = short.clone();
        low.inputs = vec![Input::Location(Location::new(0, 2))];
        low.spirit_own_mana_setup_now = false;
        low.opponent_mana_progress = false;
        low.score_path_best_steps = Config::BOARD_SIZE * 3;
        low.safe_opponent_mana_progress_steps = Config::BOARD_SIZE + 4;
        low.heuristic = -1_000;
        low.efficiency = 0;

        let short_inputs = short.inputs.clone();
        let long_inputs = long.inputs.clone();
        let (focused, _) = MonsGameModel::focused_root_candidates(
            &game,
            Color::White,
            vec![long, filler, low, short],
            config,
            true,
        );
        let short_pos = focused
            .iter()
            .position(|candidate| candidate.inputs == short_inputs)
            .expect("short spirit root should survive focus");
        let long_pos = focused
            .iter()
            .position(|candidate| candidate.inputs == long_inputs)
            .expect("long spirit root should survive focus");
        assert!(short_pos < long_pos);
    }

    #[test]
    fn focused_root_candidates_prioritize_spirit_opponent_exact_continuation_on_narrow_spread_fallback(
    ) {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(4, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 2),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.depth = 2;
        config.root_focus_k = 2;
        config.enable_two_pass_root_allocation = true;
        let own_drainer_vulnerable_before =
            MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let mut short = MonsGameModel::build_scored_root_move(
            &game,
            Color::White,
            config,
            own_drainer_vulnerable_before,
            &[
                Input::Location(Location::new(4, 0)),
                Input::Location(Location::new(5, 2)),
                Input::Location(Location::new(6, 1)),
            ],
        )
        .expect("spirit opponent mana handoff inputs should build a scored root");
        short.heuristic = 100;
        short.efficiency = 0;
        let mut long = short.clone();
        long.inputs = vec![Input::Location(Location::new(0, 0))];
        long.safe_opponent_mana_progress_steps = short.safe_opponent_mana_progress_steps + 2;
        long.score_path_best_steps = short.score_path_best_steps.saturating_sub(1);
        long.heuristic = 100;
        long.efficiency = 0;

        let mut filler = short.clone();
        filler.inputs = vec![Input::Location(Location::new(0, 1))];
        filler.spirit_own_mana_setup_now = false;
        filler.opponent_mana_progress = false;
        filler.score_path_best_steps = Config::BOARD_SIZE * 3;
        filler.safe_opponent_mana_progress_steps = Config::BOARD_SIZE + 4;
        filler.heuristic = 0;
        filler.efficiency = 0;

        let short_inputs = short.inputs.clone();
        let long_inputs = long.inputs.clone();
        let (focused, _) = MonsGameModel::focused_root_candidates(
            &game,
            Color::White,
            vec![long, filler, short],
            config,
            true,
        );
        assert_eq!(focused.len(), 3);
        let short_pos = focused
            .iter()
            .position(|candidate| candidate.inputs == short_inputs)
            .expect("short spirit root should survive narrow fallback");
        let long_pos = focused
            .iter()
            .position(|candidate| candidate.inputs == long_inputs)
            .expect("long spirit root should survive narrow fallback");
        assert!(short_pos < long_pos);
    }

    #[test]
    fn spirit_moves_opponent_mana_closer_to_pool_when_setup_is_strongest() {
        let white_spirit = Mon::new(MonKind::Spirit, Color::White, 0);
        let game = game_with_items(
            vec![
                (Location::new(9, 7), Item::Mon { mon: white_spirit }),
                (
                    Location::new(9, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 8),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let inputs = MonsGameModel::smart_search_best_inputs(
            &game,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );
        let (_, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected spirit-opponent-setup inputs should be legal");
        let spirit_moved_opponent_mana_closer = events.iter().any(|event| {
            let Event::SpiritTargetMove {
                item:
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                from,
                to,
                ..
            } = event
            else {
                return false;
            };
            let from_pool_steps = from
                .distance(&Location::new(10, 0))
                .min(from.distance(&Location::new(10, 10)));
            let to_pool_steps = to
                .distance(&Location::new(10, 0))
                .min(to.distance(&Location::new(10, 10)));
            to_pool_steps < from_pool_steps
        });
        assert!(
            spirit_moved_opponent_mana_closer,
            "selected line should use spirit to move opponent mana closer to a pool, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn spirit_moves_opponent_mana_closer_to_pool_with_root_cutoff_when_setup_is_strongest() {
        let white_spirit = Mon::new(MonKind::Spirit, Color::White, 0);
        let game = game_with_items(
            vec![
                (Location::new(9, 7), Item::Mon { mon: white_spirit }),
                (
                    Location::new(9, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 8),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.root_enum_limit = 0;
        let inputs = MonsGameModel::smart_search_best_inputs(&game, config);
        let (_, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected spirit-opponent-setup inputs should be legal");
        let spirit_moved_opponent_mana_closer = events.iter().any(|event| {
            let Event::SpiritTargetMove {
                item:
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                from,
                to,
                ..
            } = event
            else {
                return false;
            };
            let from_pool_steps = from
                .distance(&Location::new(10, 0))
                .min(from.distance(&Location::new(10, 10)));
            let to_pool_steps = to
                .distance(&Location::new(10, 0))
                .min(to.distance(&Location::new(10, 10)));
            to_pool_steps < from_pool_steps
        });
        assert!(
            spirit_moved_opponent_mana_closer,
            "selected line should use spirit to move opponent mana closer to a pool under capped root enumeration, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn selected_line_preserves_same_turn_opponent_mana_threat_when_available() {
        let game = game_with_items(
            vec![
                (
                    Location::new(5, 3),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 3),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        let exact_turn_before =
            crate::models::automove_exact::exact_turn_summary(&game, Color::White);
        assert!(
            exact_turn_before.safe_opponent_mana_progress
                || exact_turn_before.spirit_assisted_denial,
            "scenario should start with an exact same-turn opponent-mana threat"
        );

        let inputs = MonsGameModel::smart_search_best_inputs(
            &game,
            AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast),
        );
        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected spirit-setup inputs should be legal");
        let exact_turn_after =
            crate::models::automove_exact::exact_turn_summary(&after, Color::White);
        assert!(
            exact_turn_after.safe_opponent_mana_progress || exact_turn_after.spirit_assisted_denial,
            "selected line should preserve the exact same-turn opponent-mana threat, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn selected_line_preserves_same_turn_opponent_mana_threat_with_root_cutoff() {
        let game = game_with_items(
            vec![
                (
                    Location::new(5, 3),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 0),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 3),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(0, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        let exact_turn_before =
            crate::models::automove_exact::exact_turn_summary(&game, Color::White);
        assert!(
            exact_turn_before.safe_opponent_mana_progress
                || exact_turn_before.spirit_assisted_denial,
            "scenario should start with an exact same-turn opponent-mana threat"
        );

        let mut config = AutomoveSearchConfig::from_preference(SmartAutomovePreference::Fast);
        config.root_enum_limit = 0;
        let inputs = MonsGameModel::smart_search_best_inputs(&game, config);
        let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(&game, &inputs)
            .expect("selected opponent-mana-threat inputs should be legal");
        let exact_turn_after =
            crate::models::automove_exact::exact_turn_summary(&after, Color::White);
        assert!(
            exact_turn_after.safe_opponent_mana_progress || exact_turn_after.spirit_assisted_denial,
            "selected line should preserve the exact same-turn opponent-mana threat under capped root enumeration, inputs={:?}, events={:?}",
            inputs,
            events
        );
    }

    #[test]
    fn legal_transition_enumeration_matches_apply_helper() {
        let game = MonsGame::new(false, GameVariant::Classic);
        let transitions = MonsGameModel::enumerate_legal_transitions(
            &game,
            64,
            SuggestedStartInputOptions::for_automove(),
        );
        assert!(!transitions.is_empty());

        for transition in transitions.iter().take(16) {
            let (after, events) = MonsGameModel::apply_inputs_for_search_with_events(
                &game,
                transition.inputs.as_slice(),
            )
            .expect("enumerated transition input should remain legal");
            assert_eq!(after.fen(), transition.game.fen());
            assert_eq!(events, transition.events);
        }
    }

    #[test]
    fn exact_drainer_attack_oracle_matches_exhaustive_same_turn_search() {
        let game = game_with_items(
            vec![
                (
                    Location::new(5, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 7),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let exact = crate::models::automove_exact::exact_turn_summary(&game, Color::White)
            .can_attack_opponent_drainer;
        let exhaustive = exhaustive_same_turn_reachable(&game, Color::White, |_, events| {
            MonsGameModel::events_include_opponent_drainer_fainted(events, Color::White)
        });

        assert_eq!(exact, exhaustive);
    }

    #[test]
    fn exact_drainer_vulnerability_oracle_matches_exhaustive_next_turn_search() {
        let game = game_with_items(
            vec![
                (
                    Location::new(8, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(4, 7),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let exact = MonsGameModel::is_own_drainer_vulnerable_next_turn(&game, Color::White, true);
        let mut opponent_turn = game.clone_for_simulation();
        opponent_turn.active_color = Color::Black;
        opponent_turn.actions_used_count = 0;
        opponent_turn.mons_moves_count = 0;
        let exhaustive =
            exhaustive_same_turn_reachable(&opponent_turn, Color::Black, |_, events| {
                MonsGameModel::events_include_opponent_drainer_fainted(events, Color::Black)
            });

        assert_eq!(exact, exhaustive);
    }

    #[test]
    fn exact_drainer_pickup_score_oracle_matches_exhaustive_same_turn_search() {
        let game = game_with_items(
            vec![
                (
                    Location::new(8, 1),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::White),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let exact = crate::models::automove_exact::exact_state_analysis(&game)
            .white
            .best_drainer_pickup
            .is_some_and(|path| path.total_moves <= Config::MONS_MOVES_PER_TURN);
        let exhaustive = exhaustive_same_turn_reachable(&game, Color::White, |state, _| {
            state.white_score > game.white_score
        });

        assert_eq!(exact, exhaustive);
    }

    #[test]
    fn exact_safe_supermana_progress_oracle_matches_reachable_state_search() {
        let game = game_with_items(
            vec![
                (
                    Location::new(6, 5),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(5, 5),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(0, 10),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let exact = crate::models::automove_exact::exact_turn_summary(&game, Color::White)
            .safe_supermana_progress;
        let exhaustive = exhaustive_same_turn_reachable(&game, Color::White, |state, _| {
            drainer_carries_exact_safe_mana(&state.board, Color::White, Mana::Supermana)
        });

        assert_eq!(exact, exhaustive);
    }

    #[test]
    fn exact_spirit_supermana_progress_oracle_matches_exhaustive_same_turn_search() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(5, 1),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(8, 2),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 1),
                    Item::Mana {
                        mana: Mana::Supermana,
                    },
                ),
                (
                    Location::new(5, 3),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let exact = crate::models::automove_exact::exact_turn_summary(&game, Color::White)
            .spirit_assisted_supermana_progress;
        let exhaustive = exhaustive_same_turn_reachable(&game, Color::White, |state, _| {
            state.white_score > game.white_score
                || drainer_carries_exact_safe_mana(&state.board, Color::White, Mana::Supermana)
        });

        assert_eq!(exact, exhaustive);
    }

    #[test]
    fn exact_spirit_opponent_mana_progress_oracle_matches_exhaustive_same_turn_search() {
        let mut game = game_with_items(
            vec![
                (
                    Location::new(5, 1),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(8, 2),
                    Item::Mon {
                        mon: Mon::new(MonKind::Drainer, Color::White, 0),
                    },
                ),
                (
                    Location::new(7, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
                (
                    Location::new(5, 3),
                    Item::Mon {
                        mon: Mon::new(MonKind::Mystic, Color::Black, 0),
                    },
                ),
            ],
            Color::White,
            2,
        );
        game.mons_moves_count = Config::MONS_MOVES_PER_TURN - 1;

        let exact = crate::models::automove_exact::exact_turn_summary(&game, Color::White)
            .spirit_assisted_opponent_mana_progress;
        let exhaustive = exhaustive_same_turn_reachable(&game, Color::White, |state, _| {
            state.white_score > game.white_score
                || drainer_carries_exact_safe_mana(
                    &state.board,
                    Color::White,
                    Mana::Regular(Color::Black),
                )
        });

        assert_eq!(exact, exhaustive);
    }

    #[test]
    fn exact_spirit_opponent_mana_score_oracle_matches_exhaustive_same_turn_search() {
        let game = game_with_items(
            vec![
                (
                    Location::new(7, 1),
                    Item::Mon {
                        mon: Mon::new(MonKind::Spirit, Color::White, 0),
                    },
                ),
                (
                    Location::new(9, 1),
                    Item::Mana {
                        mana: Mana::Regular(Color::Black),
                    },
                ),
            ],
            Color::White,
            2,
        );

        let exact = crate::models::automove_exact::exact_state_analysis(&game)
            .white
            .spirit
            .same_turn_opponent_mana_score;
        let exhaustive = exhaustive_same_turn_reachable(&game, Color::White, |state, _| {
            state.white_score > game.white_score
        });

        assert_eq!(exact, exhaustive);
    }

    #[test]
    fn model_remove_item_invalidates_cached_start_suggestions() {
        let mut model = MonsGameModel::new_for_simulation(GameVariant::Classic);
        let initial_suggestions = match model.game.process_input(vec![], true, false) {
            Output::LocationsToStartFrom(locations) => locations,
            output => panic!("expected start locations, got {:?}", output),
        };
        let removed = initial_suggestions
            .iter()
            .copied()
            .find(|location| model.game.board.item(*location).is_some())
            .expect("expected removable starting piece");

        model.remove_item(removed);

        let updated_suggestions = match model.game.process_input(vec![], true, false) {
            Output::LocationsToStartFrom(locations) => locations,
            output => panic!("expected start locations after removal, got {:?}", output),
        };
        assert!(!updated_suggestions.contains(&removed));
    }
}

fn random_index(len: usize) -> usize {
    assert!(len > 0, "random index requires a non-empty collection");
    let unbiased_upper_bound = usize::MAX - usize::MAX % len;

    loop {
        let mut bytes = [0_u8; std::mem::size_of::<usize>()];
        getrandom::getrandom(&mut bytes).expect("secure randomness should be available");
        let value = usize::from_ne_bytes(bytes);
        if value < unbiased_upper_bound {
            return value % len;
        }
    }
}

#[wasm_bindgen]
#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub enum OutputModelKind {
    InvalidInput,
    LocationsToStartFrom,
    NextInputOptions,
    Events,
}

#[wasm_bindgen]
#[derive(Debug, PartialEq, Eq, Clone)]
pub struct OutputModel {
    pub kind: OutputModelKind,
    locations: Vec<Location>,
    next_inputs: Vec<NextInputModel>,
    events: Vec<EventModel>,
    input_fen: String,
}

#[wasm_bindgen]
impl OutputModel {
    pub fn locations(&self) -> Vec<Location> {
        self.locations.clone()
    }

    pub fn next_inputs(&self) -> Vec<NextInputModel> {
        self.next_inputs.clone()
    }

    pub fn events(&self) -> Vec<EventModel> {
        self.events.clone()
    }

    pub fn input_fen(&self) -> String {
        self.input_fen.clone()
    }
}

impl OutputModel {
    fn new(output: Output, input_fen: &str) -> Self {
        match output {
            Output::InvalidInput => Self {
                kind: OutputModelKind::InvalidInput,
                locations: vec![],
                next_inputs: vec![],
                events: vec![],
                input_fen: input_fen.to_string(),
            },
            Output::LocationsToStartFrom(locations) => Self {
                kind: OutputModelKind::LocationsToStartFrom,
                locations,
                next_inputs: vec![],
                events: vec![],
                input_fen: input_fen.to_string(),
            },
            Output::NextInputOptions(next_inputs) => Self {
                kind: OutputModelKind::NextInputOptions,
                locations: vec![],
                next_inputs: next_inputs
                    .into_iter()
                    .map(|input| NextInputModel::new(&input))
                    .collect(),
                events: vec![],
                input_fen: input_fen.to_string(),
            },
            Output::Events(events) => Self {
                kind: OutputModelKind::Events,
                locations: vec![],
                next_inputs: vec![],
                events: events
                    .into_iter()
                    .map(|event| EventModel::new(&event))
                    .collect(),
                input_fen: input_fen.to_string(),
            },
        }
    }
}

#[wasm_bindgen]
#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub struct SquareModel {
    pub kind: SquareModelKind,
    pub color: Option<Color>,
    pub mon_kind: Option<MonKind>,
}

#[wasm_bindgen]
#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub enum SquareModelKind {
    Regular,
    ConsumableBase,
    SupermanaBase,
    ManaBase,
    ManaPool,
    MonBase,
}

impl SquareModel {
    fn new(item: &Square) -> Self {
        match item {
            Square::Regular => SquareModel {
                kind: SquareModelKind::Regular,
                color: None,
                mon_kind: None,
            },
            Square::ConsumableBase => SquareModel {
                kind: SquareModelKind::ConsumableBase,
                color: None,
                mon_kind: None,
            },
            Square::SupermanaBase => SquareModel {
                kind: SquareModelKind::SupermanaBase,
                color: None,
                mon_kind: None,
            },
            Square::ManaBase { color } => SquareModel {
                kind: SquareModelKind::ManaBase,
                color: Some(*color),
                mon_kind: None,
            },
            Square::ManaPool { color } => SquareModel {
                kind: SquareModelKind::ManaPool,
                color: Some(*color),
                mon_kind: None,
            },
            Square::MonBase { kind, color } => SquareModel {
                kind: SquareModelKind::MonBase,
                color: Some(*color),
                mon_kind: Some(*kind),
            },
        }
    }
}

#[wasm_bindgen]
#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub enum ItemModelKind {
    Mon,
    Mana,
    MonWithMana,
    MonWithConsumable,
    Consumable,
}

#[wasm_bindgen]
#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub struct ItemModel {
    pub kind: ItemModelKind,
    pub mon: Option<Mon>,
    pub mana: Option<ManaModel>,
    pub consumable: Option<Consumable>,
}

impl ItemModel {
    fn new(item: &Item) -> Self {
        let (kind, mon, mana, consumable) = match item {
            Item::Mon { mon } => (ItemModelKind::Mon, Some(*mon), None, None),
            Item::Mana { mana } => (ItemModelKind::Mana, None, Some(ManaModel::new(mana)), None),
            Item::MonWithMana { mon, mana } => (
                ItemModelKind::MonWithMana,
                Some(*mon),
                Some(ManaModel::new(mana)),
                None,
            ),
            Item::MonWithConsumable { mon, consumable } => (
                ItemModelKind::MonWithConsumable,
                Some(*mon),
                None,
                Some(*consumable),
            ),
            Item::Consumable { consumable } => {
                (ItemModelKind::Consumable, None, None, Some(*consumable))
            }
        };
        Self {
            kind,
            mon,
            mana,
            consumable,
        }
    }
}

#[wasm_bindgen]
#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub struct ManaModel {
    pub kind: ManaKind,
    pub color: Color,
}

impl ManaModel {
    fn new(item: &Mana) -> Self {
        match item {
            Mana::Regular(color) => ManaModel {
                kind: ManaKind::Regular,
                color: *color,
            },
            Mana::Supermana => ManaModel {
                kind: ManaKind::Supermana,
                color: Color::White,
            },
        }
    }
}

#[wasm_bindgen]
#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub enum ManaKind {
    Regular,
    Supermana,
}

#[wasm_bindgen]
#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub struct NextInputModel {
    pub location: Option<Location>,
    pub modifier: Option<Modifier>,
    pub kind: NextInputKind,
    pub actor_mon_item: Option<ItemModel>,
}

impl NextInputModel {
    fn new(input: &NextInput) -> Self {
        Self {
            location: match input.input {
                Input::Location(loc) => Some(loc),
                _ => None,
            },
            modifier: match input.input {
                Input::Modifier(modifier) => Some(modifier),
                _ => None,
            },
            kind: input.kind,
            actor_mon_item: input.actor_mon_item.as_ref().map(ItemModel::new),
        }
    }
}

#[wasm_bindgen]
#[derive(Debug, PartialEq, Eq, Clone, Copy)]
pub enum EventModelKind {
    MonMove,
    ManaMove,
    ManaScored,
    MysticAction,
    DemonAction,
    DemonAdditionalStep,
    SpiritTargetMove,
    PickupBomb,
    PickupPotion,
    PickupMana,
    MonFainted,
    ManaDropped,
    SupermanaBackToBase,
    BombAttack,
    MonAwake,
    BombExplosion,
    NextTurn,
    GameOver,
    Takeback,
    UsePotion,
}

#[wasm_bindgen]
#[derive(Debug, PartialEq, Eq, Clone)]
pub struct EventModel {
    pub kind: EventModelKind,
    pub item: Option<ItemModel>,
    pub mon: Option<Mon>,
    pub mana: Option<ManaModel>,
    pub loc1: Option<Location>,
    pub loc2: Option<Location>,
    pub color: Option<Color>,
}

impl EventModel {
    fn new(event: &Event) -> Self {
        match event {
            Event::MonMove { item, from, to } => EventModel {
                kind: EventModelKind::MonMove,
                item: Some(ItemModel::new(item)),
                mon: None,
                mana: None,
                loc1: Some(*from),
                loc2: Some(*to),
                color: None,
            },
            Event::ManaMove { mana, from, to } => EventModel {
                kind: EventModelKind::ManaMove,
                item: None,
                mon: None,
                mana: Some(ManaModel::new(mana)),
                loc1: Some(*from),
                loc2: Some(*to),
                color: None,
            },
            Event::ManaScored { mana, at } => EventModel {
                kind: EventModelKind::ManaScored,
                item: None,
                mon: None,
                mana: Some(ManaModel::new(mana)),
                loc1: Some(*at),
                loc2: None,
                color: None,
            },
            Event::MysticAction { mystic, from, to } => EventModel {
                kind: EventModelKind::MysticAction,
                item: None,
                mon: Some(*mystic),
                mana: None,
                loc1: Some(*from),
                loc2: Some(*to),
                color: None,
            },
            Event::DemonAction { demon, from, to } => EventModel {
                kind: EventModelKind::DemonAction,
                item: None,
                mon: Some(*demon),
                mana: None,
                loc1: Some(*from),
                loc2: Some(*to),
                color: None,
            },
            Event::DemonAdditionalStep { demon, from, to } => EventModel {
                kind: EventModelKind::DemonAdditionalStep,
                item: None,
                mon: Some(*demon),
                mana: None,
                loc1: Some(*from),
                loc2: Some(*to),
                color: None,
            },
            Event::SpiritTargetMove {
                item,
                from,
                to,
                by: _,
            } => EventModel {
                kind: EventModelKind::SpiritTargetMove,
                item: Some(ItemModel::new(item)),
                mon: None,
                mana: None,
                loc1: Some(*from),
                loc2: Some(*to),
                color: None,
            },
            Event::PickupBomb { by, at } => EventModel {
                kind: EventModelKind::PickupBomb,
                item: None,
                mon: Some(*by),
                mana: None,
                loc1: Some(*at),
                loc2: None,
                color: None,
            },
            Event::PickupPotion { by, at } => EventModel {
                kind: EventModelKind::PickupPotion,
                item: Some(ItemModel::new(by)),
                mon: None,
                mana: None,
                loc1: Some(*at),
                loc2: None,
                color: None,
            },
            Event::PickupMana { mana, by, at } => EventModel {
                kind: EventModelKind::PickupMana,
                item: None,
                mon: Some(*by),
                mana: Some(ManaModel::new(mana)),
                loc1: Some(*at),
                loc2: None,
                color: None,
            },
            Event::MonFainted { mon, from, to } => EventModel {
                kind: EventModelKind::MonFainted,
                item: None,
                mon: Some(*mon),
                mana: None,
                loc1: Some(*from),
                loc2: Some(*to),
                color: None,
            },
            Event::ManaDropped { mana, at } => EventModel {
                kind: EventModelKind::ManaDropped,
                item: None,
                mon: None,
                mana: Some(ManaModel::new(mana)),
                loc1: Some(*at),
                loc2: None,
                color: None,
            },
            Event::SupermanaBackToBase { from, to } => EventModel {
                kind: EventModelKind::SupermanaBackToBase,
                item: None,
                mon: None,
                mana: None,
                loc1: Some(*from),
                loc2: Some(*to),
                color: None,
            },
            Event::BombAttack { by, from, to } => EventModel {
                kind: EventModelKind::BombAttack,
                item: None,
                mon: Some(*by),
                mana: None,
                loc1: Some(*from),
                loc2: Some(*to),
                color: None,
            },
            Event::MonAwake { mon, at } => EventModel {
                kind: EventModelKind::MonAwake,
                item: None,
                mon: Some(*mon),
                mana: None,
                loc1: Some(*at),
                loc2: None,
                color: None,
            },
            Event::BombExplosion { at } => EventModel {
                kind: EventModelKind::BombExplosion,
                item: None,
                mon: None,
                mana: None,
                loc1: Some(*at),
                loc2: None,
                color: None,
            },
            Event::NextTurn { color } => EventModel {
                kind: EventModelKind::NextTurn,
                item: None,
                mon: None,
                mana: None,
                loc1: None,
                loc2: None,
                color: Some(*color),
            },
            Event::GameOver { winner } => EventModel {
                kind: EventModelKind::GameOver,
                item: None,
                mon: None,
                mana: None,
                loc1: None,
                loc2: None,
                color: Some(*winner),
            },
            Event::Takeback => EventModel {
                kind: EventModelKind::Takeback,
                item: None,
                mon: None,
                mana: None,
                loc1: None,
                loc2: None,
                color: None,
            },
            Event::UsePotion { from, to } => EventModel {
                kind: EventModelKind::UsePotion,
                item: None,
                mon: None,
                mana: None,
                loc1: Some(*from),
                loc2: Some(*to),
                color: None,
            },
        }
    }
}

#[wasm_bindgen]
#[derive(Debug, PartialEq, Eq, Clone)]
pub struct VerboseTrackingEntityModel {
    fen: String,
    color: Color,
    events: Vec<Event>,
}

impl VerboseTrackingEntityModel {
    fn new(entity: &VerboseTrackingEntity) -> Self {
        Self {
            fen: entity.fen.clone(),
            color: entity.color,
            events: entity.events.clone(),
        }
    }
}

#[wasm_bindgen]
impl VerboseTrackingEntityModel {
    pub fn fen(&self) -> String {
        self.fen.clone()
    }
    pub fn color(&self) -> Color {
        self.color
    }
    pub fn events(&self) -> Vec<EventModel> {
        self.events.iter().map(EventModel::new).collect()
    }
    pub fn events_fen(&self) -> String {
        self.events
            .iter()
            .map(|e| e.fen())
            .collect::<Vec<_>>()
            .join(" ")
    }
}
